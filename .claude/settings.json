{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "r=\"$CLAUDE_PROJECT_DIR\"; [ -n \"$r\" ] || r=$(git rev-parse --show-toplevel 2>/dev/null); [ -n \"$r\" ] || r=$PWD; p=$(cat); printf '%s' \"$p\" | node \"$r/.claude/tools/spec-lease.mjs\" hook; c=$?; [ \"$c\" = 0 ] || exit 2",
            "type": "command"
          }
        ],
        "matcher": "Edit|Write|MultiEdit|NotebookEdit|Bash"
      }
    ]
  },
  "permissions": {
    "allow": []
  }
}
