{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '(.tool_input.file_path // .tool_input.notebook_path) // empty'); case \"$f\" in /Users/joan/projects/lenny/spec/*|spec/*|./spec/*) echo \"blocked: /Users/joan/projects/lenny/spec/ is read-only per project policy. The spec is the source of truth; propose edits in chat for review. Path: $f\" >&2; exit 2 ;; esac"
          }
        ]
      }
    ]
  }
}
