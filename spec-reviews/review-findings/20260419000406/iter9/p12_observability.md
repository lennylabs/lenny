## OBS iter9 review — regressions from iter8 fix commit `df0e675`

**Scope:** observability surfaces touched by iter8 fix: `spec/16_observability.md` §16.5 alerts (`AdmissionPlaneFeatureFlagDowngrade`, `ElicitationContentTamperDetected`, `EphemeralContainerCredGuardUnavailable`) and §16.7 audit events (`elicitation.content_tamper_detected`, `deployment.feature_flag_downgrade_acknowledged`); `docs/reference/metrics.md` catalog sync.

**Note on agent availability:** The dispatched OBS subagent hit a usage-rate-limit failure (`"You've hit your limit · resets 4am (America/Los_Angeles)"`); this review was performed inline by the parent agent against the same regressions-only scope.

### Inspection results — no observability regressions detected

1. **`AdmissionPlaneFeatureFlagDowngrade` PromQL expression** — four per-`(flag, webhook)` PrometheusRule expressions, each of the form `(kube_configmap_labels{namespace=..., configmap="lenny-deployment-phase-stamp", label_lenny_dev_flag_<slug>_enabled="true"}) unless on() (kube_validatingwebhookconfiguration_info{name="<webhook>"})` with static rule labels `flag_name`, `expected_webhook_name`. `unless on()` with empty label set is valid PromQL for vector-vector operation producing "LHS series unless any RHS series exists". Prior iter7 regressions (KIN-040/OBS-043/FMR-023) where the expression used `on()/group_right()` on scalar-vector operations, referenced the nonexistent `webhook_name` label on `kube_validatingwebhookconfiguration_info`, or used `kube_configmap_info` which has no per-flag label encoding are all resolved.
2. **`--metric-labels-allowlist` operator precondition** — documented in §16.5 alert rule narrative and re-documented in `docs/operator-guide/configuration.md` line 500 with the concrete `--metric-labels-allowlist=configmaps=[lenny.dev/flag-*]` value and a `kubectl get --raw=.../metrics` verification command. Alert-inoperative-when-allowlist-missing failure mode (evaluates to empty rather than firing falsely) correctly stated.
3. **`ElicitationContentTamperDetected` alert expression** — `increase(lenny_elicitation_content_tamper_detected_total[5m]) > 0` with labels `origin_pod`, `tampering_pod`. `{message, schema}` vocabulary consistent with §9.2 and §16.7 after iter8 normalization. Paired runbook `docs/runbooks/elicitation-content-tamper-detected.md` present.
4. **`EphemeralContainerCredGuardUnavailable` alert expression** — `up{job="lenny-ephemeral-container-cred-guard"} == 0` for more than 5 minutes. Narrative correctly states the credential-boundary invariant remains protected by fail-closed policy while the webhook is down (operator impact = all `kubectl debug`-style ephemeral-container workflows fail until webhook recovery). Paired runbook `docs/runbooks/ephemeral-container-cred-guard-unavailable.md` present.
5. **§16.7 `deployment.feature_flag_downgrade_acknowledged` audit event** — full payload defined (`flag_name`, `expected_webhook_name`, `acknowledged_by_sub`, `acknowledged_by_tenant_id`, `justification` (required), `acknowledged_at`); severity Notice; retention `audit.gdprRetentionDays`; not sampled. Paired with §17.7 runbook Diagnosis step 4. Cross-reference symmetry with `compliance.profile_decommissioned` (tenant-scope posture downgrade) explicitly noted.
6. **`docs/reference/metrics.md` catalog sync** — all three new alert rows present (lines 477, 526, 527); `{message, schema}` vocabulary on `lenny_elicitation_content_tamper_detected_total` entry (line 251) consistent with spec.

No observability regressions detected in the iter8 fix envelope.
