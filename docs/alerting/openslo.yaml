# Code generated by cmd/gen-alerting-rules from the §16.5 SLO catalog
# (SLODefinitions) in pkg/alerting/rules. DO NOT EDIT. Run
# `make generate` to refresh.
#
# spec: §16.10 lines 732-736 — operator-facing OpenSLO v1 reference,
# rendered with deployment_tier="tier1" as a concrete example. The
# chart fragment charts/lenny/files/openslo.yaml carries a tier
# placeholder the Helm chart substitutes per deployment.
apiVersion: openslo/v1
kind: SLI
metadata:
  name: session-creation-success-rate-sli
  labels:
    deployment_tier: tier1
spec:
  description: Session creation success rate >= 99.5%
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_gateway_requests_total{deployment_tier="tier1",method="POST",route="/v1/sessions",status_class!="5xx"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_gateway_requests_total{deployment_tier="tier1",method="POST",route="/v1/sessions"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: session-creation-success-rate
  labels:
    deployment_tier: tier1
spec:
  description: Session creation success rate >= 99.5%
  service: lenny
  indicatorRef: session-creation-success-rate-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Session creation success rate >= 99.5%
      target: 0.995
  alertPolicies:
    - session-creation-success-rate-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: session-creation-success-rate-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Session creation success rate >= 99.5%
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: session-creation-success-rate-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: session-creation-success-rate-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: session-creation-latency-sli
  labels:
    deployment_tier: tier1
spec:
  description: Session creation latency P99 < 500ms
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_gateway_request_duration_seconds_bucket{deployment_tier="tier1",method="POST",route="/v1/sessions",le="0.5"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_gateway_request_duration_seconds_count{deployment_tier="tier1",method="POST",route="/v1/sessions"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: session-creation-latency
  labels:
    deployment_tier: tier1
spec:
  description: Session creation latency P99 < 500ms
  service: lenny
  indicatorRef: session-creation-latency-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Session creation latency P99 < 500ms
      target: 0.99
  alertPolicies:
    - session-creation-latency-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: session-creation-latency-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Session creation latency P99 < 500ms
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: session-creation-latency-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: session-creation-latency-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: session-availability-sli
  labels:
    deployment_tier: tier1
spec:
  description: Session availability >= 99.9%
  ratioMetric:
    counter: false
    good:
      metricSource:
        type: prometheus
        spec:
          query: (1 - lenny_session_unavailability_ratio{deployment_tier="tier1"})
    total:
      metricSource:
        type: prometheus
        spec:
          query: vector(1)
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: session-availability
  labels:
    deployment_tier: tier1
spec:
  description: Session availability >= 99.9%
  service: lenny
  indicatorRef: session-availability-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Session availability >= 99.9%
      target: 0.999
  alertPolicies:
    - session-availability-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: session-availability-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Session availability >= 99.9%
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: session-availability-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: session-availability-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: gateway-availability-sli
  labels:
    deployment_tier: tier1
spec:
  description: Gateway availability >= 99.95%
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_gateway_requests_total{deployment_tier="tier1",status_class!="5xx"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_gateway_requests_total{deployment_tier="tier1"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: gateway-availability
  labels:
    deployment_tier: tier1
spec:
  description: Gateway availability >= 99.95%
  service: lenny
  indicatorRef: gateway-availability-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Gateway availability >= 99.95%
      target: 0.9995
  alertPolicies:
    - gateway-availability-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: gateway-availability-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Gateway availability >= 99.95%
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: gateway-availability-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: gateway-availability-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: startup-latency-runc-sli
  labels:
    deployment_tier: tier1
spec:
  description: Startup latency P95 < 2s (runc)
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_session_startup_duration_seconds_bucket{deployment_tier="tier1",isolation_profile="runc",le="2"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_session_startup_duration_seconds_count{deployment_tier="tier1",isolation_profile="runc"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: startup-latency-runc
  labels:
    deployment_tier: tier1
spec:
  description: Startup latency P95 < 2s (runc)
  service: lenny
  indicatorRef: startup-latency-runc-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Startup latency P95 < 2s (runc)
      target: 0.95
  alertPolicies:
    - startup-latency-runc-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: startup-latency-runc-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Startup latency P95 < 2s (runc)
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: startup-latency-runc-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: startup-latency-runc-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: startup-latency-gvisor-sli
  labels:
    deployment_tier: tier1
spec:
  description: Startup latency P95 < 5s (gVisor)
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_session_startup_duration_seconds_bucket{deployment_tier="tier1",isolation_profile="gvisor",le="5"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_session_startup_duration_seconds_count{deployment_tier="tier1",isolation_profile="gvisor"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: startup-latency-gvisor
  labels:
    deployment_tier: tier1
spec:
  description: Startup latency P95 < 5s (gVisor)
  service: lenny
  indicatorRef: startup-latency-gvisor-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Startup latency P95 < 5s (gVisor)
      target: 0.95
  alertPolicies:
    - startup-latency-gvisor-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: startup-latency-gvisor-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Startup latency P95 < 5s (gVisor)
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: startup-latency-gvisor-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: startup-latency-gvisor-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: ttft-sli
  labels:
    deployment_tier: tier1
spec:
  description: Time to first token P95 < 10s
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_session_time_to_first_token_seconds_bucket{deployment_tier="tier1",le="10"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_session_time_to_first_token_seconds_count{deployment_tier="tier1"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: ttft
  labels:
    deployment_tier: tier1
spec:
  description: Time to first token P95 < 10s
  service: lenny
  indicatorRef: ttft-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Time to first token P95 < 10s
      target: 0.95
  alertPolicies:
    - ttft-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: ttft-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Time to first token P95 < 10s
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: ttft-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: ttft-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
---
apiVersion: openslo/v1
kind: SLI
metadata:
  name: checkpoint-duration-sli
  labels:
    deployment_tier: tier1
spec:
  description: Checkpoint duration P95 < 2s (<= 100MB)
  ratioMetric:
    counter: true
    good:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_checkpoint_duration_seconds_bucket{deployment_tier="tier1",le="2"}[1h]))
    total:
      metricSource:
        type: prometheus
        spec:
          query: sum(rate(lenny_checkpoint_duration_seconds_count{deployment_tier="tier1"}[1h]))
---
apiVersion: openslo/v1
kind: SLO
metadata:
  name: checkpoint-duration
  labels:
    deployment_tier: tier1
spec:
  description: Checkpoint duration P95 < 2s (<= 100MB)
  service: lenny
  indicatorRef: checkpoint-duration-sli
  timeWindow:
    - duration: 30d
      isRolling: true
  budgetingMethod: Occurrences
  objectives:
    - displayName: Checkpoint duration P95 < 2s (<= 100MB)
      target: 0.95
  alertPolicies:
    - checkpoint-duration-burnrate
---
apiVersion: openslo/v1
kind: AlertPolicy
metadata:
  name: checkpoint-duration-burnrate
  labels:
    deployment_tier: tier1
spec:
  description: Multi-window error-budget burn-rate policy for Checkpoint duration P95 < 2s (<= 100MB)
  alertWhenBreaching: true
  conditions:
    - kind: AlertCondition
      metadata:
        name: checkpoint-duration-fast-burn
      spec:
        description: Fast-window (1h) error-budget burn rate exceeds the page threshold.
        severity: critical
        condition:
          kind: burnrate
          op: gt
          threshold: 14
          lookbackWindow: 1h
          alertAfter: 1h
    - kind: AlertCondition
      metadata:
        name: checkpoint-duration-slow-burn
      spec:
        description: Slow-window (6h) error-budget burn rate exceeds the warning threshold.
        severity: warning
        condition:
          kind: burnrate
          op: gt
          threshold: 3
          lookbackWindow: 6h
          alertAfter: 6h
