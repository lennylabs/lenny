# Proposal: Task-mode gateway driver: `ReportTaskCleanup` RPC, disposition write ownership, and between-task lifecycle reconciliation

- **Status:** Verified (2026-06-09). Converged after 4 adversarial review rounds (1 findings fixed); awaiting sign-off.
- **Date:** 2026-06-09.
- **Scope:** Adds the adapter→gateway `ReportTaskCleanup` RPC, records the gateway's `Sandbox` status write window in §4.6.3, defines the §6.2 task-mode disposition driver and `task_cleanup` watchdog, aligns `task_ready` on dispatch-time semantics across every site that describes it, clarifies task-mode ingestion in §15.1, and removes the stray task envelopes from the JSONL schema. Unblocks F-5.2.30.

This document stages the proposed spec and schema edits. It does not modify any spec file. Apply the changes in the "Proposed spec and schema changes" section after sign-off.

## 1. Problem

F-5.2.30 (`BUILD-GAPS.md:3689`) defers the gateway task-execution driver on two claimed spec gaps, and its recorded unblock condition is "Re-attempt once §15/§4.7 define the gateway-side task-control surface" (`BUILD-GAPS.md:3715`).

The first claimed gap, a missing `/v1/tasks` ingestion route, is a defect in the finding rather than in the spec. No `/v1/tasks` route exists anywhere in `spec/`, `schemas/`, or `charts/`, and none ever existed in spec git history (`git log -S` is empty). The spec's ingestion path for task-mode work is session creation against an `executionMode: task` pool (`spec/05_runtime-registry-and-pool-model.md:475`; `spec/07_session-lifecycle.md:65-75`), and the code already ingests `executionMode: task` through the session path (`pkg/gateway/sessionserver/start.go:1185`). The finding's supporting citations are also stale: `tests/tier7_load_cloud/task_mode_test.go` does not exist, and the tier-12 load scenario submits tasks via `POST /v1/sessions/start`, `POST /v1/sessions/{id}/messages`, and `POST /v1/sessions/{id}/terminate` (`tests/tier12_load_cloud/scenarios/task_throughput/main.js:53,62,67`), never via `/v1/tasks`.

The second gap is genuine: the spec defines no gateway-side task-control surface. The §4.7 Gateway→Adapter RPC table (`spec/04_system-components.md:618-638`) has no task-lifecycle RPC, the adapter→gateway control-event table (`spec/04_system-components.md:652-662`) carries no task or scrub event, and `pkg/adapter/controlchannel.go:86` confirms that no gateway→adapter frame exists on that stream. The between-task loop (`spec/05_runtime-registry-and-pool-model.md:415`) is adapter-executed and adapter-initiated: the runtime's terminal `{type: "response"}` frame (`spec/15_external-api-surface.md:1889-1891`) chains into `task_complete` per `spec/06_warm-pod-model.md:142`. Its tail, however, has unspecified gateway dependencies. The gateway needs the scrub result to resolve the §6.2 disposition (`spec/06_warm-pod-model.md:147-156`), write the pod phase and the `scrub_warning` annotation, feed `taskcleanup.Decide`, and emit the §5.2 metrics; and `task_ready` is gated on the gateway's next-task materialization (`spec/04_system-components.md:702`) and a per-task `AssignCredentials` lease (`spec/06_warm-pod-model.md:26`).

Secondary spec defects block a coherent driver spec:

- **`task_ready` ordering is contradictory.** Every site that describes the frame mixes the post-scrub timing clause with content that exists only at dispatch. `spec/04_system-components.md:702` and the `schemas/lifecycle-events.schema.json:202-211` def open with "Sent after scrub completes and the new workspace is materialized" while carrying the required `taskId` field and the regenerated-manifest behavior; `spec/15_external-api-surface.md:1461` places the frame after scrub in its timing clause yet states the adapter sends it "with the new task's ID"; only the stray JSONL def (`schemas/lenny-adapter-jsonl.schema.json:255`) states outright that `taskId` is the gateway-assigned new task identifier; and `spec/05_runtime-registry-and-pool-model.md:415,419` and the `task_ready` clause at `spec/06_warm-pod-model.md:76` are purely post-scrub. The post-scrub reading is unimplementable: no next task is assigned at scrub completion, so there is no task ID, workspace, credential lease, or manifest to be ready for. The adapter code implements the dispatch-time reading (`pkg/adapter/lifecyclechannel.go:520-528`).
- **The persistent-runtime claim is false.** `spec/04_system-components.md:701` states the runtime "remains alive for the next task", contradicting scrub step 1 (`kill -9 -1` as the sandbox user, `spec/05_runtime-registry-and-pool-model.md:428`), the explicit SDK-process kill at `spec/06_warm-pod-model.md:76`, and the per-task runtime spawn at `spec/06_warm-pod-model.md:26`.
- **The gateway-resolved disposition has no settled write path.** The §4.6.3 ownership table assigns `Sandbox.status.*` exclusively to the WarmPoolController (`spec/04_system-components.md:574`), and the gateway's enumerated write surface (`spec/04_system-components.md:588`) covers neither the disposition phases nor the `scrub_warning` annotation. Meanwhile the shipped claim path already writes `Sandbox.status.phase` from the gateway under its own field manager with an explicit drain-yield protocol (`pkg/gateway/podclaim/gatewaystatus.go:16-56`), citing a "§4.6.3 gateway carve-out" the spec does not contain, and the shipped chart already grants the gateway `get`/`update`/`patch` on `sandboxes/status` (`charts/lenny/templates/gateway-deployment.yaml:130-132`). The per-pod retirement counters feeding the disposition exist only in gateway process memory (`pkg/gateway/taskdriver/taskdriver.go:101-104`), against the §10.1 statelessness rule (`spec/10_gateway-internals.md:5`).

The adapter-side primitives are built and unit-tested with zero production callers: `pkg/adapter/scrub.Run` (`pkg/adapter/scrub/scrub.go:232`), `RequestTaskComplete` and `SignalTaskReady` (`pkg/adapter/lifecyclechannel.go:500,523`), and `pkg/sandbox/taskcleanup.Decide` (`pkg/sandbox/taskcleanup/taskcleanup.go:173`, called only by the leaf package `pkg/gateway/taskdriver`).

This proposal defines the missing surface, reconciles the contradictions, and corrects the finding's premises. It unblocks F-5.2.30.

## 2. Decisions

The following were decided in drafting and adversarial challenge and constrain this design.

- **No `/v1/tasks` route is added.** The route never existed in spec git history, the F-5.2.30 deliverable's cited §15.1 task-submission path is not defined by §15.1, and the spec consistently models a task-mode task as a session: per-task credential lease (`spec/06_warm-pod-model.md:26`), `TaskRecord.sessionId` (`spec/08_recursive-delegation.md:814`), session surfaced as an MCP or A2A task (`spec/07_session-lifecycle.md:116`), and the post-v1 A2A adapter creating Lenny sessions (`spec/21_planned-post-v1.md:5`). The fix is a §15.1 clarification.
- **The between-task loop start stays adapter-observed.** `spec/15_external-api-surface.md:1889-1891` defines task completion as the runtime's terminal `{type: "response"}` frame (or process exit), and `spec/05_runtime-registry-and-pool-model.md:415` plus `spec/06_warm-pod-model.md:142` chain that event into the adapter sending `task_complete`. No gateway RPC triggers the loop; the new surface sits at the loop's tail.
- **The report transport is one unary adapter→gateway RPC** (`ReportTaskCleanup`) on the existing `GatewayControl` service, following the `ExtendLease` precedent (`spec/04_system-components.md:644`). The `GatewayControl` doc comment (`schemas/lenny-adapter.proto:204-208`) states that the service "exists because a few §8 control operations originate at the pod and need a gateway decision"; `ReportTaskCleanup` is a §5.2/§6.2 operation, so Section 9.7.1 stages a widening of the service comment, whose §8 scoping the existing §9.1 platform-tool RPCs (`ListPlatformTools`, `CallPlatformTool`) and §9.3 connector RPCs (`ListSessionConnectors`, `ListConnectorTools`, and `CallConnectorTool`) already strain. The transport introduces no new gateway→adapter stream frame, no new service, and no new RPC on the gateway-initiated `Adapter` service.
- **The disposition decision stays in the gateway**, matching the existing spec attribution (`spec/05_runtime-registry-and-pool-model.md:446,452,455`; the gateway label read at the `task_cleanup` transition, `spec/06_warm-pod-model.md:181`) and the built `pkg/gateway/taskdriver` plus `pkg/sandbox/taskcleanup` split. The `ReportTaskCleanup` response carries the resolved disposition (`reuse`, `rewarm_sdk`, or `retire`) so the adapter knows whether to run the SDK reconnect, reconciling `spec/06_warm-pod-model.md:76` (the adapter executes the re-warm) with `spec/06_warm-pod-model.md:181` (the gateway gates it).
- **Disposition writes extend the gateway's already-implemented `Sandbox.status.phase` field-manager window** (`pkg/gateway/podclaim/gatewaystatus.go:16-56`: SSA Apply under the gateway field manager with `ForceOwnership` during the claim window, and a non-Apply `draining` write that ends the gateway's Apply claim at the window's close). The spec edit records that window in §4.6.3, which currently contradicts both the code and the shipped chart by assigning `Sandbox.status.*` exclusively to the WarmPoolController.
- **Per-pod retirement counters and the per-task resolution record are externalized.** `tasksCompleted` and `scrubFailureCount` move from gateway memory to `Sandbox.status.taskState`, are read back at each `task_cleanup`, and are mirrored into `agent_pod_state`. The same status object records `lastTaskId`, `lastDisposition`, and `lastScrubWarning`, which answer a retried `ReportTaskCleanup` after the resolution has been written. This satisfies the §10.1 rule (stateless replicas over externalized state) and survives both coordinator handoff and ordinary cross-replica claim turnover. The store choice is surfaced as open decision 1 because the spec offers more than one precedented store.
- **`task_ready` carries dispatch-time semantics everywhere.** The dispatch-time reading is operative (the frame payload carries the new task's ID, stated at `spec/15_external-api-surface.md:1461` and `schemas/lenny-adapter-jsonl.schema.json:255`; per-task manifest regeneration at `spec/04_system-components.md:711`; the SDK `OnCreate` contract at `spec/15_external-api-surface.md:2568-2573`; `pkg/adapter/lifecyclechannel.go:520-528`), and the scrub-time reading is unimplementable because no next task is assigned at scrub completion. §5.2 lines 415 and 419, §6.1 line 76, the §15.4.1 line 1461 timing clause, and the `lifecycle-events.schema.json` defs (Section 9.9) are aligned to it.
- **The per-task runtime process model is canon.** `spec/06_warm-pod-model.md:26` rewrites credentials "before the runtime binary is spawned for each task", and scrub step 1 kills the SDK process per `spec/06_warm-pod-model.md:76`. Next-task dispatch reuses the existing claim-path RPCs (`AssignCredentials`, `PrepareWorkspace`/`FinalizeWorkspace`, and `StartSession` or `ConfigureWorkspace`); no `StartTask` RPC is introduced. The "remains alive for the next task" clause at `spec/04_system-components.md:701` is corrected.
- **The stale BUILD-GAPS claim about the load scenarios is dropped.** `tests/tier12_load_cloud/scenarios/task_throughput/main.js:53,62,67` submits tasks via the `/v1/sessions` routes and never referenced `/v1/tasks` at any commit.
- **No dual modes or compatibility shims are added.** The stray adapter↔gateway task envelopes in `schemas/lenny-adapter-jsonl.schema.json` are removed rather than deprecated, since no deployments exist.

## 3. The `ReportTaskCleanup` exchange

The gateway has no specified way to learn the scrub outcome it needs for the §6.2 disposition, the `scrub_warning` annotation, the `taskcleanup.Decide` inputs, and the §5.2 metrics: the §4.7 control-event table (`spec/04_system-components.md:652-662`) is task-free, and `pkg/adapter/controlchannel.go:86` documents that no gateway→adapter frame exists on the stream. Symmetrically, the adapter has no specified way to learn whether to run the §6.1 SDK re-warm, a decision gated on inputs only the gateway reads (`spec/06_warm-pod-model.md:181`).

A single request/response RPC on the existing adapter→gateway `GatewayControl` service covers both directions in one exchange, following the `ExtendLease` precedent (`spec/04_system-components.md:644`). The request carries the scrub outcome (`scrub_result`, `failed_step`, `cleanup_commands_exit`, and the step-7 `vm_restart` result); the response carries the resolved disposition (`reuse`, `rewarm_sdk`, or `retire`) and the `scrub_warning` flag. The RPC is idempotent per `task_id`: a retried report receives the already-resolved disposition without a second write or metric emission. The resolution is recorded in `Sandbox.status.taskState` in the same status Apply that advances the counters (Section 5), so the recall works on any gateway replica and after the pod has left `task_cleanup`. The adapter retries an undeliverable report with backoff (initial 1s, cap 30s) until acknowledged; the pod remains in `task_cleanup`, and the watchdog in Section 6 of this proposal bounds the wait when the report never arrives. The report is sent for every task whose pod enters `task_cleanup`, covering the §6.2 `cancelled → task_cleanup` entry (`spec/06_warm-pod-model.md:146`) as well as ordinary completion; the cancellation path runs the same between-task sequence without the `task_complete` exchange.

The grep-verified RPC name space (`ReportTaskCleanup`, `BeginTaskCleanup`, `StartTask`) is unclaimed; `ReportTaskCleanup` matches the operation's direction and content.

## 4. Between-task lifecycle and `task_ready` ordering

`spec/05_runtime-registry-and-pool-model.md:415` is the normative description of the between-task loop, and its current ordering (scrub → `task_ready` → pod available for next task) is unimplementable independent of this proposal: `task_ready` must carry the new task's ID, and the new task's workspace, credential lease, and manifest exist only after the next dispatch. The loop text also leaves the gateway out entirely, leaving the disposition, annotation, and counter writes unattributed at the loop's tail. The staged §5.2 edit names the `ReportTaskCleanup` report, names `reuse` and `rewarm_sdk` as distinct dispositions matching the RPC's response enum, routes the next task through the standard claim flow, moves `task_ready` to dispatch, and retains the two existing normative clauses of line 415 (the `cleanupCommands` access parenthetical and the per-task-setup sentence). The gateway-action enumeration is deferred by cross-reference to the §6.2 driver paragraph so the spec holds one normative list.

The same dispatch-time semantics are propagated to every other site that describes the loop:

- `spec/04_system-components.md:701` (`task_complete` row): the frame does not terminate the runtime process, scrub step 1 kills it, and the runtime serving the next task is spawned at dispatch or re-established by the preConnect SDK re-warm.
- `spec/04_system-components.md:702` (`task_ready` row): sent at the dispatch of the next task; the receiver is the runtime process serving the new task.
- `spec/06_warm-pod-model.md:76` (preConnect task-mode row): the re-warm is triggered by the `rewarm_sdk` disposition in the `ReportTaskCleanup` response, and `task_ready` moves to dispatch. The current row sends `task_ready` before the SDK re-spawn, to a process the scrub already killed.
- `spec/15_external-api-surface.md:1461`: the timing clause gains the dispatch condition.

The runtime-author-guide mirrors of the loop (`docs/runtime-author-guide/lifecycle.md` and `docs/runtime-author-guide/adapter-contract.md`) are reconciled in the docs follow-up (Section 9.12).

The §5.2 edit (Section 9.2.1) lands only with the `ReportTaskCleanup` RPC (Section 9.1.1) and the §6.2 driver paragraph it cross-references (Section 9.3.3). If either is rejected, the §5.2 edit reduces to the `task_ready` reordering in lines 415 and 419 plus the line-446 attribution fix, which the pre-existing contradiction with `spec/04_system-components.md:702`, both schemas, `spec/15_external-api-surface.md:1461`, and `spec/06_warm-pod-model.md:26` justifies on its own.

## 5. Disposition write ownership and counter persistence

The §4.6.3 ownership table assigns `Sandbox.status.*` exclusively to the WarmPoolController, yet the implemented claim path writes `status.phase` from the gateway across the whole claim window under the `lenny-gateway` field manager with `ForceOwnership`, citing a carve-out the spec does not contain, and the shipped chart already grants the gateway `get`/`update`/`patch` on `sandboxes/status`. The spec therefore contradicts both the code and the chart today, and the task driver's writes (the §6.2 disposition phase plus the `scrub_warning` annotation) cannot be specified against the current table. The staged edit records the window the code implements, with the following accuracy constraints verified against the implementation:

- The gateway SSA-applies all claim-window phases, including the terminal dispositions `completed`, `failed`, `cancelled`, and `expired`, under its field manager (`failPhase` writes `failed` via SSA Apply with `ForceOwnership`, `pkg/gateway/podsession/binder.go:746-760`; an expired session maps to the `expired` phase through the same release path, `pkg/gateway/sessionserver/usage.go:53-54` and `pkg/gateway/executor/pod.go:284,299-300`). The reclamation step writes `draining`, and only `draining`, with a non-Apply status `Update` (`pkg/gateway/podclaim/gatewaystatus.go:31-41`). Per the Kubernetes SSA model, a non-Apply write removes other managers' claims on the fields it changes and transfers ownership of those fields to the writer's own update-operation manager, so the yield ends the gateway's Apply claim on `status.phase` but leaves the field owned by an update-operation manager, and a later non-forcing SSA Apply that changes the field conflicts with that owner exactly as it would with an Apply owner (manager identity includes the operation). The WarmPoolController's own `status.phase` transitions are therefore specified as the same non-Apply status `Update`, covered by the `update` verb staged in Section 9.1.9: a status `Update` is never rejected with a managed-fields conflict, so each transition lands regardless of which manager holds the field and takes ownership in turn, and the gateway's next claim re-takes the field with its forced Apply. The shipped WarmPoolController phase transitions use non-forcing SSA Apply under `retryOnConflictSSA` (`pkg/controller/sandbox/controller.go:610-622`; `pkg/controller/warmpool/pod_reconciler.go:447-476`), which would conflict deterministically against the yield's update-operation owner and be abandoned after five attempts (`pkg/controller/warmpool/controller_test.go:44-47` concedes the cross-strategy conflict); the conversion to non-Apply `Update` is a code change recorded in the Files-touched table. The WarmPoolController's broader status Apply (`buildSandboxStatusPatch`, `pkg/controller/sandbox/controller.go:641-692`) carries `status.phase` unconditionally today (`controller.go:686` sets `patch.Status.Phase = want.Phase`, and the patch is built whenever any of `status.phase`, `status.podName`, `status.nodeName`, `status.podIP`, or `status.observedGeneration` changes, `controller.go:669-674`), so on a pooled pod whose phase the gateway last wrote through a yield, every non-phase update (`status.podIP`, `status.nodeName`, `status.observedGeneration`) would re-assert management of `status.phase` and conflict with the gateway's update-operation owner, because SSA conflict detection fires on a non-forcing Apply asserting management of another manager's field regardless of whether the value differs. The broader Apply is therefore changed to omit `status.phase` (it carries the field only when `decision.Action == lifecycle.ActionSetPhase`, the case the non-Apply `Update` conversion already handles), so it asserts management of `status.phase` on no pooled pod and lands its non-phase fields against any phase owner without conflict; the omit-phase change is a code change recorded in the Files-touched table. Task mode maps `task_cleanup → failed` onto the Apply path and every pool-returning disposition (`task_cleanup → idle`, `task_cleanup → sdk_connecting`, and `task_cleanup → draining`) onto the same non-Apply yield, and follows the `task_cleanup → failed` Apply in the same pass with a `failed → draining` non-Apply yield over the reclamation edge (`pkg/sandbox/state/state.go:259`; the §6.2 listing line is staged in Section 9.3.2), mirroring the session-mode release sequence in which the gateway writes the terminal phase and then drains the pod (`pkg/gateway/podsession/binder.go:1471-1492`). The shipped concurrent-mode last-slot release departs from this pattern: `ReleaseSlot` writes `slot_active → idle` via SSA Apply with `ForceOwnership` (`pkg/gateway/podclaim/slotclaimer.go:681-694`), so a pooled `idle` concurrent-mode pod retains a live gateway claim on `status.phase` and the WarmPoolController's non-forcing drain Apply on it would conflict deterministically and be abandoned by `retryOnConflictSSA`. The staged window therefore routes the last-slot `slot_active → idle` release through the same non-Apply yield (the gateway's final Apply zeroes `status.activeSlots` while the phase is still `slot_active`); the `ReleaseSlot` conversion is a code change recorded in the Files-touched table, parallel to the `pkg/controller/warmpool/gc.go` rows. With every pool return on the yield path, the WarmPoolController is the sole phase writer once the pod heads back toward the pool: it writes `draining → terminated`, writes `sdk_connecting → idle` when the between-task re-warm completes (the re-warm readiness signal and the re-anchored watchdog clock are specified in Section 9.3.1), and retains its ordinary drain and failure transitions on pooled pods, each through the non-Apply `Update` above. The gateway (on the fail disposition) and the `task_cleanup` watchdog (Section 6) complete the `failed → draining` reclamation themselves because a claim-less `failed` task-mode pod has no other writer: the driver releases the per-task claim on every disposition, the §4.6.1 orphan-loop recovery runs only at the moment it collects a stale claim, the lifecycle planner takes no action on terminal phases (`pkg/controller/sandbox/lifecycle/lifecycle.go:103-107`), and no shipped WarmPoolController path drains `failed` pods (the `patchSandboxDraining` callers are the certificate-expiry replacement paths and the planner's idle scale-down: `pkg/controller/warmpool/pod_reconciler.go:337,399`; `pkg/controller/warmpool/controller.go:664`).
- The gateway-owned carve-out is `status.phase`, `status.activeSlots`, `status.tenantId`, and the new `status.taskState.*` (`pkg/apis/lenny/v1alpha1/sandbox_types.go:95-114`; `pkg/gateway/podclaim/gatewaystatus.go:27-29`). `Sandbox.status.conditions` sits outside the carve-out as a shared lifecycle-history surface, because the shipped gateway writes it from inside the claim window: the release path writes the `Terminated` condition between the terminal-phase Apply and the drain yield (`pkg/gateway/podsession/binder.go:1479-1484`), the suspend path writes the `Suspended` condition after the `suspended` Apply (`pkg/gateway/podsession/binder.go:1547-1552`), and the WarmPoolController writes `OrphanClaimReclaimed` (`pkg/controller/warmpool/gc.go:243-248`), all through `pkg/sandbox/condition` under per-condition field managers (`lenny-sandbox-cond-<type>` with `ForceOwnership`, `pkg/sandbox/condition/condition.go:82-84`), a package whose doc names both writers (`condition.go:3-7`). §6.2 makes `.status.conditions` part of the authoritative state (`spec/06_warm-pod-model.md:305`), so the staged table records it as a shared row rather than leaving it under the WarmPoolController-exclusive remainder, which the shipped gateway writes would contradict.
- The RBAC reconciliation covers both `Sandbox` rules in the shipped chart. On the `status` subresource the grant is `get`/`update`/`patch`: `update` is required by the non-Apply phase yields, and the chart already grants exactly this set (`charts/lenny/templates/gateway-deployment.yaml:130-132`). On the `Sandbox` main resource the staged sentence records `get`/`list`/`watch` for claim-path reads of pool inventory and `patch` for the §5.2 slot-claim tenant-pinning label Apply: `pkg/gateway/podclaim/slotclaimer.go:589-600` SSA-applies `lenny.dev/tenant-id` on the main resource, and the chart grants the verb set at lines 122-129 with a comment naming the tenant-pinning patch as the reason. The staged sentence reconciles the spec with both shipped grants. The `Pods` and `SandboxClaim` clauses of the same sentence are recorded at the shipped verb sets for the same reason: `get`/`list`/`watch`/`patch` on Pods (the read verbs resolve the claimed pod's IP for the adapter dial, `charts/lenny/templates/gateway-deployment.yaml:143-148`) and `get`/`list`/`watch`/`create`/`delete` on `SandboxClaim` (`charts/lenny/templates/gateway-deployment.yaml:140-142`), rather than the narrower pre-existing spec wording, which would re-stage the mismatch this bullet reconciles for `Sandbox`.
- The "never force-conflict" bullet (`spec/04_system-components.md:582`) is scoped to WarmPoolController and PoolScalingController reconciliation code, with the gateway status window named as the sanctioned cross-manager takeover whose return path is the non-Apply phase yields: the session-mode `draining` hand-back, the concurrent-mode last-slot `slot_active → idle` release, and the task-mode disposition writes (`task_cleanup → idle`, `task_cleanup → sdk_connecting`, the retiring `task_cleanup → draining`, and the `failed → draining` reclamation yield that follows the `onCleanupFailure: fail` terminal write), matching the yield set in the status-window paragraph. Without this scoping, §4.6.3 would prohibit in one paragraph what it specifies in the next.

The per-pod retirement counters move to `Sandbox.status.taskState` (`tasksCompleted`, `scrubFailureCount`), written inside the same status Apply the gateway must make per task anyway and read back at each `task_cleanup`. The same Apply records the per-task resolution (`lastTaskId`, `lastDisposition`, `lastScrubWarning`): the §10.1 rule that bars gateway memory for the counters bars it equally for the resolved disposition, which a retried `ReportTaskCleanup` must be answered with on any replica and after the pod has left `task_cleanup`. The in-memory counters at `pkg/gateway/taskdriver/taskdriver.go:101-104` (advanced at lines 158-168) violate the §10.1 statelessness rule because task-mode pods return to `idle` between tasks and the next task claims through the standard flow on any replica. The disposition's pod-uptime input needs no externalization: unlike the counters (and the in-memory `BootTime` the shipped driver holds beside them), it is computed at resolution time from the pod's start time on the API object, which any replica reads. The WarmPoolController mirrors the counters into `agent_pod_state`, preserving the `spec/12_storage-architecture.md:455` contract that the table mirrors `Sandbox` CRD status fields. Alternative stores with spec precedent exist; the choice is open decision 1. The `Sandbox.status.taskState` field additions (`pkg/apis/lenny/v1alpha1/sandbox_types.go` and the regenerated `charts/lenny/crds/lenny.dev_sandboxes.yaml`) accompany the implementation, like the `taskCleanupTimeoutSeconds` CRD field in Section 6; this proposal stages the spec definitions only.

## 6. The disposition driver and the `task_cleanup` watchdog

§6.2 defines the `task_cleanup` branch table (`spec/06_warm-pod-model.md:147-156`), states the gateway's `lenny.dev/host-schedulable` label read at the transition (`spec/06_warm-pod-model.md:181`), and attributes the metrics, the uptime check, and the pool return to the gateway (`spec/05_runtime-registry-and-pool-model.md:446,452,455`), but it never states the trigger, the counter source, or any liveness bound. No existing mechanism bounds a pod stuck in `task_cleanup`: the `sdk_connecting` watchdog (`spec/06_warm-pod-model.md:69`) covers only `sdk_connecting`, and the `ReportTaskCleanup` adapter retry (Section 3) covers gateway unavailability but not adapter death. The only mechanism that incidentally fires is the §4.6.1 orphan-`SandboxClaim` GC (`spec/04_system-components.md:479`), which returns the pod to `idle`; for a dirty mid-scrub pod that is the wrong, security-relevant outcome, because an unscrubbed pod would re-enter the pool.

The staged edits add a driver paragraph, a watchdog paragraph, and transition-listing lines to §6.2, plus a reconciling sentence replacement and clause in §4.6.1:

- **Driver paragraph.** The paragraph is declared the single normative site for resolution semantics. It states the `task_cleanup` entry write (the gateway writes `attached → task_cleanup` on the Apply path when its session-result path observes the task's terminal outcome, and `cancelled → task_cleanup` when the cancellation it initiated is acknowledged; without a named entry writer the phase precondition and the watchdog's adapter-death coverage would be incoherent, since the adapter↔runtime `task_complete` exchange is a unix-socket exchange the gateway cannot observe), the trigger (`ReportTaskCleanup` arrival, once per task whose pod enters `task_cleanup`, covering both the completion and the cancellation entries), the counter source (`Sandbox.status.taskState` advanced by this task's outcome, with `tasksCompleted` advancing on cancelled tasks as well), the host-schedulable label read, the `SandboxClaim` release on every disposition (ordered after the resolution's phase writes, so a crash mid-resolution leaves either a watchdog-bounded `task_cleanup` pod or a stale claim the orphan loop collects and routes), the fail-disposition reclamation sequence (the `task_cleanup → failed` Apply followed in the same pass by the `failed → draining` yield, because a claim-less `failed` pod has no other reclamation writer), the phase precondition (the gateway resolves and writes only while `Sandbox.status.phase` is still `task_cleanup`), the resolution record (`lastTaskId`, `lastDisposition`, and `lastScrubWarning`, written in the counter Apply so a retried report is answered from persisted state on any replica and at any later phase), and the late-report rule (after the watchdog has written `failed`, the gateway performs no phase write, responds with `disposition: retire`, releases the `SandboxClaim` if still present, and the failure is accounted once, on the watchdog path). The write-and-metric enumeration lives on the §4.7 `ReportTaskCleanup` row, and the §5.2 lifecycle text cross-references this paragraph, so the spec holds one normative statement of each behavior.
- **Watchdog paragraph.** The WarmPoolController bounds `task_cleanup` with `taskCleanupTimeoutSeconds` (default: the pool's `taskPolicy.cleanupTimeoutSeconds` plus 120 seconds; per-pool in the `scalingPolicy` block, following the `sdkConnectTimeoutSeconds` placement at `spec/06_warm-pod-model.md:69`). The timer is anchored to the gateway's `task_cleanup` entry write, so an adapter that dies at any point in the between-task sequence is bounded. On expiry the WarmPoolController writes `failed` and then `draining`, two sequential non-Apply status updates in the same pass: the first lands against the gateway's field-manager claim on `status.phase`, strips the claim on exactly the field it writes, and takes ownership (the standard WarmPoolController phase-write mechanism in Section 9.1.5), so the status-window text and this watchdog compose, and the second reclaims the pod through the `failed → draining` edge, because the orphan loop's claim-coupled recovery cannot be relied on: claim age is measured from creation at task dispatch, so for any task that runs longer than `claimOrphanTimeout` minus `taskCleanupTimeoutSeconds` (about 2.5 minutes with the §5.2 example `cleanupTimeoutSeconds: 30`), the loop collects the session-less per-task claim while the pod is still in `task_cleanup`, so a later `failed` write would find no claim left to trigger recovery. `{task_cleanup, failed}` and `{failed, draining}` are both legal edges in `ValidTransitions` (`pkg/sandbox/state/state.go:224,259`), but the §6.2 listing defines only the first (`spec/06_warm-pod-model.md:156`): the watchdog trigger line is a new trigger on that listed edge, consistent with the listing's multi-line-per-edge style, and Section 9.3.2 stages the `failed → draining` listing line the follow-on write requires. The expiry increments `lenny_warmpool_task_cleanup_timeout_total`, and a `TaskCleanupTimeout` warning alert covers sustained rates (Section 9.6), mirroring the `sdk_connecting` watchdog's counter-and-alert pattern; without the dedicated counter, watchdog-fired replacements would be indistinguishable from `onCleanupFailure: fail` retirements. The watchdog performs no claim write: on the no-report path the per-task `SandboxClaim` is session-less and the §4.6.1 orphan loop collects it, because its claim-deletion half is phase-independent (Section 9.1.8).
- **Orphan-GC reconciliation.** The §4.6.1 edit replaces the unconditional return-to-idle sentence with a phase-routed lead-in and inserts a clause that splits the GC's delete-claim-and-recover-pod path, gates the pod-recovery write on the pod's current binding, and routes it by phase; without the replacement the surviving sentence would contradict the routing in the same paragraph. Claim deletion stays phase-independent, so stale session-less claims referencing pods in `failed`, `task_cleanup`, or terminal phases remain collectable (the pre-attached failure path at `pkg/gateway/podsession/binder.go:750-755` and the watchdog's no-report path both depend on this). The pod-recovery half is gated on the collected claim being the pod's current binding: in task mode the pod returns to the pool and is re-claimed by the next task while a prior task's claim can survive a gateway crash between the disposition's phase writes and the claim release, so the GC can collect a stale claim after the pod is already serving a later task, and an ungated phase-routed write would drain that live pod. The recovery write is skipped when another live `SandboxClaim` references the pod. That single prong suffices: the claim path creates the `SandboxClaim` before its first phase Apply (`pkg/gateway/podclaim/claimer.go:103-114`), so a re-claimed pod carries the later task's live claim in every phase the routing writes. (`Sandbox.status.sessionId` is not part of the gate: the only writers of that field, `pkg/podregistry/crd.go:177,224`, sit in a package no production binary constructs, the production claim path patches only `phase`, `activeSlots`, and `tenantId` (`pkg/gateway/podclaim/gatewaystatus.go:47-56`; `pkg/gateway/podclaim/slotclaimer.go:46`), and the staged §4.6.3 carve-out sanctions no gateway writer for it.) The gated recovery covers the gateway-crash windows. The create-and-start window spans `claimed` through `attached`: the path runs the pod claim and the full bind before the session row is persisted (`pkg/gateway/sessionserver/start.go:478-523`), the bind advances `receiving_uploads` through `attached` (`pkg/gateway/podsession/binder.go:604-708`), and a gateway crash anywhere in that window leaves a session-less claim referencing a pod in any of those phases, or in `failed` when a pre-attached failure write landed first. The release window sits between the gateway's terminal-disposition Apply and the drain yield: the release path writes the terminal phase, when the disposition has a valid edge from the pod's current phase, before draining (`pkg/gateway/podsession/binder.go:1465-1492`; with no valid edge the terminal write is skipped and the drain yield runs directly, which makes the live-gateway release a trigger of the `attached → draining` and `claimed → draining` listing lines staged in Section 9.3.2), no code deletes the session-mode claim on that path (the claim deletion follows the `released` claim phase per §4.6.3), and the GC treats a terminal session as inactive (`pkg/controller/warmpool/gc.go:61-67`; `cmd/lenny-controller/main.go:649-653`), so a crash in that window leaves a session-less claim referencing a pod in `completed`, `cancelled`, or `expired`. No other mechanism recovers those pods: the WarmPoolController lifecycle planner takes no action on terminal phases (`pkg/controller/sandbox/lifecycle/lifecycle.go:103-107`) and the §10.1 orphan-session reconciler covers only sessions in non-terminal states (`spec/10_gateway-internals.md:51`). Every recovery write uses an edge `ValidTransitions` defines, and Section 9.3.2 stages the §6.2 listing lines for the drain edges the routing relies on, because the current §6.2 listing sources `draining` only from `task_cleanup`, `idle`, and `slot_active` (`spec/06_warm-pod-model.md:149-153,157,165-167`) while `ValidTransitions` documents the additional drain sources as unlisted extensions of the §6.2 cleanup model (`pkg/sandbox/state/state.go:235-241,246-257`). A pod still in `claimed` is written to `draining` through the claim-time abort edge, whose comment names the gateway crash between the SSA claim and the setup chain as its scenario and drains the pod rather than stranding it (`pkg/sandbox/state/state.go:235-243`); the state machine defines no `claimed → idle` edge, so a return to `idle` would add a new edge for a path the abort edge already covers. A pod in `receiving_uploads`, `finalizing_workspace`, `running_setup`, or `starting_session` is written to `failed` through its existing pre-attached failure edge (`pkg/sandbox/state/state.go:164-181`) and then to `draining` through the `failed → draining` reclamation edge, two sequential writes in the same pass, because the state machine defines no direct `→ draining` edge for those phases. A pod in `attached`, in `failed`, or in the session-terminal phases `completed`, `cancelled`, or `expired` is written to `draining` directly (`pkg/sandbox/state/state.go:244,258-261`) so the WarmPoolController drains and replaces it: a partially materialized or used pod cannot return to `idle` without violating the §6.1 one-session exclusivity invariant. In task mode the same terminal route recovers a pod stranded in `cancelled` by a gateway crash before the `task_cleanup` entry write, whose watchdog timer starts only at that write, and a pod stranded in `failed` by a crash between the fail disposition's `failed` Apply and its `failed → draining` yield, before the claim release that follows the phase writes (Section 9.3.3). A pod in `task_cleanup` is bounded exclusively by `taskCleanupTimeoutSeconds` and receives no recovery write from the GC, because a mid-scrub pod can hold residual task state. A pod in `sdk_connecting` receives no recovery write: its stale claim belongs to a released task (the `rewarm_sdk` disposition releases the claim while the adapter re-warms), the re-warm is bounded by the re-anchored `sdkConnectTimeoutSeconds` watchdog (Section 9.3.1), and the WarmPoolController returns the pod to `idle` when the re-warm completes. A pod in `slot_active` also receives no recovery write: a gateway crash during slot release can leave a session-less per-slot claim referencing a pod whose sibling slots are still live (the claim delete sits at `pkg/gateway/podclaim/slotclaimer.go:647`), and the phase-independent half deletes the stale claim with the pod left in place; the replaced unconditional flip would have zeroed a multi-slot pod. The dead slot's count is not recovered: `ReleaseSlot` deletes the claim before the Redis `Counter.Release` and the `status.activeSlots` decrement (`pkg/gateway/podclaim/slotclaimer.go:647-688`), so any crash ordering that leaves a stale claim has already skipped the count release, the GC deletes only the claim, the last-slot `slot_active → idle` yield fires only when the decrement reaches zero (`slotclaimer.go:682-684`), and rehydration reseeds the counter from live sessions only after a Redis restart (`pkg/gateway/slotcounter/slotcounter.go:14-21`). The pod therefore stays `slot_active` at reduced capacity (fully unclaimable for `maxConcurrent: 1` pools) until the §6.2 `slot_active → draining` `maxPodUptimeSeconds` drain reclaims it (`spec/06_warm-pod-model.md:166`); the staged §4.6.1 text states this outcome rather than an idle return. Every recovery write is a non-Apply status `Update`: in the crash scenario no yield ever ran, so the dead gateway's Apply claim on `status.phase` persists, a WarmPoolController non-forcing SSA Apply could never land (`retryOnConflictSSA` abandons after five attempts, `pkg/controller/warmpool/controller.go:47-74`), and the non-Apply `Update` lands against the claim, strips it, and takes ownership, which is the standard WarmPoolController phase-write mechanism of Section 9.1.5, covered by the `update` verb staged in Section 9.1.9. The shipped `ClaimGarbageCollector.returnToIdle` (`pkg/controller/warmpool/gc.go:201-253`) implements the current unconditional text, flips any phase other than `idle` and `draining` back to `idle`, consults no binding datum, and writes via non-forcing SSA Apply; the binding gate, the phase-routed recovery, and the conversion to a non-Apply `Update` are code changes that land with the clause, flagged here and in the Files-touched table rather than silently specified.

The `taskCleanupTimeoutSeconds` CRD field accompanies the implementation; this proposal stages the spec definition only.

## 7. Task-mode ingestion

The task-equals-session model is stated outside §15 (`spec/05_runtime-registry-and-pool-model.md:475`; `spec/07_session-lifecycle.md:69-73`), but §15 itself, the API-surface chapter, is silent: the §15.1 route table (`spec/15_external-api-surface.md:593-610`) is sessions-only and no §15 sentence addresses task submission. That silence caused a multi-cycle High-severity deferral loop in which F-5.2.30 cites a "§15.1 task-submission path" that never existed (`BUILD-GAPS.md:3695`). Fixing only the ledger would leave the ambiguity that regenerates the phantom on the next audit.

The staged paragraph states that task-mode work is submitted through the same session endpoints, that pod reuse between tasks is pool-internal and surfaces on the REST API through the `sessionIsolationLevel` fields `podReuse`, `scrubPolicy`, and `residualStateWarning`, that v1 defines no separate task-submission route, and that the tasks-suffixed paths in §21 are post-v1 `ExternalProtocolAdapter` routes. Two claims from earlier drafting are deliberately absent: an unconditional return-to-pool claim (false for Standard- and Basic-level pools per `spec/05_runtime-registry-and-pool-model.md:420` and for every retiring disposition per `spec/06_warm-pod-model.md:147-156`) and a generalization that every §21 adapter "resolves to session creation internally" (explicit only for A2A at `spec/21_planned-post-v1.md:5`).

## 8. Lifecycle envelope cleanup

`schemas/lenny-adapter-jsonl.schema.json:228-262` defines `task_complete` ("Outbound (adapter → gateway)"), `task_complete_acknowledged` ("Inbound (gateway → adapter)"), and `task_ready` ("Outbound (adapter → gateway)") as frames on the adapter↔agent stdin/stdout channel. The directions contradict the §4.7 frame table (all are adapter↔runtime unix-socket frames, `spec/04_system-components.md:701-708`), and the purported "separate adapter→gateway control surface" (`schemas/lifecycle-events.schema.json:5`) has no spec definition and no implementation: zero production code emits or parses the JSONL task envelopes, and the only emitters are `pkg/adapter/lifecyclechannel.go:504,525` on the lifecycle channel. Git history confirms a transplant error: the envelopes were added in `ed8546c3` citing §15.4 line 1461, a line that places the frames on the lifecycle channel, and the spec-conformant definitions arrived later in `schemas/lifecycle-events.schema.json` (`65d359a7`). The trio is a stale duplicate, and the smaller alternative (fixing the direction labels in place) would keep two canonical schemas defining the same frames with the frames in the wrong channel's schema.

The removal carries determinate dependents, all staged:

- `spec/15_external-api-surface.md:1425` states the JSONL schema covers "every lifecycle-channel message", and line 1428 declares any artifact-prose discrepancy a release-blocking bug. The bullet is rescoped to the stdin/stdout messages, `schemas/lifecycle-events.schema.json` is added to the artifact list (matching `spec/18_build-sequence.md:165`), and the artifact-count framing at line 1422 is reworded to remove the count.
- `schemas/examples/jsonl.task_complete.json`, `schemas/examples/jsonl.task_complete_acknowledged.json`, and `schemas/examples/jsonl.task_ready.json` are deleted, and the corresponding entries in `tests/tier0_static/schemas_test.go:189-191` (`TestAdapterJSONLExamplesValidate`) are removed; tier 0 fails otherwise.
- The stale taxonomy comment at `schemas/lenny-adapter.proto:1320-1324` ("The lifecycle event taxonomy is defined in lenny-adapter-jsonl.schema.json") is corrected to point at the §4.7 control-event table and `pkg/adapter/controlchannel.go`; `pkg/proto/adapter/v1/*.pb.go` regenerates from the proto.
- The `schemas/lifecycle-events.schema.json` description drops the "separate adapter→gateway control surface" claim. Only the replacement cross-reference to `ReportTaskCleanup` is conditioned on the RPC's acceptance; the deletions are correct independent of it, and if the RPC is rejected the cross-reference sentence is dropped.
- Docs-sync follow-up: `docs/runtime-author-guide/adapter-contract.md:659` describes the JSONL schema as including lifecycle frames and is reconciled in the standard docs pass after application (Section 9.12). `docs/runtime-author-guide/publishing.md:335` needs no change on this point: it already scopes the JSONL schema to stdin/stdout frames.

## 9. Proposed spec and schema changes

### 9.1 `spec/04_system-components.md`

**9.1.1 §4.7, *Adapter → Gateway RPCs* table.** Add a row after the `ExtendLease` row (line 644):

```markdown
| `ReportTaskCleanup` | Task-mode only. Sent once per task whose pod enters `task_cleanup` ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine)): on task completion after the [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) between-task sequence (the `task_complete` exchange, step-0 credential purge, `cleanupCommands`, Lenny scrub, and the step-7 guest restart re-verification when applicable) finishes, and on task cancellation (`cancelled → task_cleanup`) after the same sequence without the `task_complete` exchange. Request: `session_id`, `task_id`, `scrub_result` (`succeeded` \| `failed`), `failed_step` (string, present on failure), `cleanup_commands_exit` (int), `vm_restart` (`skipped` \| `succeeded` \| `failed`). The gateway resolves the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` disposition exactly once per `task_id` (the RPC is idempotent on retry: a retried report is answered from the `Sandbox.status.taskState` resolution record with no second write or metric emission), writes the pod phase, annotation, and task counters, releases the per-task `SandboxClaim`, and emits the [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) scrub and retirement metrics and, when the resolved disposition retires the pod (`task_cleanup → draining` or `task_cleanup → failed`), the [Section 16.1](16_observability.md#161-metrics) `lenny_task_reuse_count` observation (once per pod, recording the final `Sandbox.status.taskState.tasksCompleted` value, with cancelled tasks included in the value); resolution semantics are defined in [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver". Response: `disposition` (`reuse` \| `rewarm_sdk` \| `retire`) and `scrub_warning` (bool). On `rewarm_sdk` the adapter starts the SDK connect sequence ([Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like)); on `retire` the adapter prepares for termination; on `reuse` the pod idles until the next claim. The adapter retries an undeliverable report with backoff (initial 1s, cap 30s) until acknowledged; the pod remains in `task_cleanup` and the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` watchdog bounds the wait. |
```

**9.1.2 §4.7, lifecycle channel message-schema table, `task_complete` row (line 701).** Replace the Notes cell with:

```markdown
Between-task signal in task mode ([Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes)). The current task is finished; the runtime must release task-specific resources (open files, temp state) and reply with `task_complete_acknowledged`. The frame itself does not terminate the runtime process; scrub step 1 ([Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes)) kills it along with all other task processes, and the runtime serving the next task is spawned at dispatch ([Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like) task-mode credential lease lifecycle) or re-established by the preConnect SDK re-warm ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine)).
```

**9.1.3 §4.7, lifecycle channel message-schema table, `task_ready` row (line 702).** Replace the Notes cell with:

```markdown
Sent at the dispatch of the next task, after scrub has completed and the new task's workspace is materialized. Carries the new task's ID. The receiver is the runtime process serving the new task: the re-warmed SDK process in preConnect pools, or the freshly spawned runtime once it connects the lifecycle channel. The runtime re-reads the adapter manifest (regenerated per task) and prepares for the next `{type: "message"}` on stdin.
```

**9.1.4 §4.6.3 ownership table (line 574).** Replace the single `Sandbox` / `status.*` row with three rows:

```markdown
| `Sandbox`         | `status.phase`, `status.activeSlots`, `status.tenantId`, `status.taskState.*` (claim-window carve-out) | Gateway (during the claim window) | Written via SSA Apply under the `lenny-gateway` field manager; the gateway's Apply claim on `status.phase` ends with the non-Apply phase yields (`draining`, the concurrent-mode last-slot `slot_active → idle` release, the task-mode pool-return dispositions, and the task-mode `failed → draining` reclamation yield); see "Gateway status window" below |
| `Sandbox`         | `status.conditions`                                | Gateway and WarmPoolController (shared) | [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) lifecycle-history conditions, written via SSA Apply under per-condition field managers (`lenny-sandbox-cond-<type>`): the gateway writes `Suspended` and `Terminated` during the claim window, and the WarmPoolController writes `OrphanClaimReclaimed`; the per-type managers prevent cross-writer conflicts |
| `Sandbox`         | `status.*` (excluding the claim-window carve-out and `status.conditions`) | WarmPoolController         | State machine transitions, health; sole writer outside the claim window |
```

**9.1.5 §4.6.3, new paragraph.** Insert after the paragraph beginning `**Server-Side Apply (SSA) enforcement:**` (line 577) and before `**SSA conflict retry policy (crash recovery).**`:

```markdown
**Gateway status window (`Sandbox` claim-window carve-out):** The gateway writes the `Sandbox` status carve-out fields (`status.phase`, `status.activeSlots`, `status.tenantId`, and `status.taskState.*`) during the claim window: from the gateway's claim of an `idle` pod until the yield write that ends the gateway's Apply claim on `status.phase`. Writes use SSA Apply under the `lenny-gateway` field manager with `Force: true`; this window is the sanctioned cross-manager takeover, and the "never force-conflict" rule below does not apply to it. The window covers the session-mode setup, hold, and terminal phases (`claimed` through `attached`, the `suspended` hold while a suspended session retains its pod, and the terminal dispositions `completed`, `failed`, `cancelled`, and `expired`), the concurrent-mode slot claim and slot-count writes (`idle → slot_active` and the `status.activeSlots` advances), and the task-mode `attached`/`cancelled` → `task_cleanup` cycle (the entry write and its trigger are defined in [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver"), all on the Apply path. During the same window the gateway also records the lifecycle-history conditions `Suspended` (with the `suspended` hold) and `Terminated` (with the terminal disposition) on `Sandbox.status.conditions`, the shared row in the ownership table above: condition writes use SSA Apply under per-condition field managers (`lenny-sandbox-cond-<type>`), the WarmPoolController writes `OrphanClaimReclaimed` through the same mechanism, and the per-type managers prevent cross-writer conflicts. `status.conditions` is not a carve-out field, and its per-condition managers are independent of the `status.phase` yield protocol below. The claim window ends with a non-Apply status `Update` that writes the new phase, and only the phase: per the Kubernetes SSA model, a non-Apply write removes other managers' claims on exactly the fields it changes and transfers ownership of those fields to the writer's own update-operation manager, so the gateway's Apply claim on `status.phase` is stripped while its claims on the other carve-out fields are untouched. The yield does not leave the field unowned, and a non-forcing SSA Apply that changes a field held by an update-operation manager is rejected with a conflict, so the WarmPoolController performs its own `status.phase` transitions with the same non-Apply status `Update` (under the `update` grant in the RBAC enforcement paragraph below): a status `Update` is never rejected with a managed-fields conflict, each transition lands regardless of which manager holds the field and takes ownership in turn, and the gateway's next claim re-takes the field with its forced Apply. Every phase change the WarmPoolController makes is a non-Apply `Update`. Its other status Applies (the SSA Apply that carries `status.podName`, `status.nodeName`, `status.podIP`, and `status.observedGeneration`) omit `status.phase`, so they assert management of no field the gateway's update-operation manager holds and land against any phase owner without conflict. The session-mode reclamation step writes `draining` this way. In task mode, every disposition write that sends the pod back toward the pool uses the same yield: `task_cleanup → idle` (`reuse`), `task_cleanup → sdk_connecting` (`rewarm_sdk`), and the retiring `task_cleanup → draining` are all non-Apply phase writes. In concurrent mode, the last slot's release returns the pod the same way: the gateway's final Apply of the window zeroes `status.activeSlots` while the phase is still `slot_active`, and the `slot_active → idle` write is the same non-Apply phase yield. The WarmPoolController is therefore the sole phase writer on pooled pods: it writes `draining → terminated`, writes `sdk_connecting → idle` when the between-task SDK re-warm completes (the re-warm readiness signal and the re-anchored `sdkConnectTimeoutSeconds` watchdog are defined in the [Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like) task-mode `preConnect` row), and retains its ordinary drain and failure transitions on `idle` pods. The gateway does not observe re-warm completion; the next task's claim selects the pod once it is `idle`. `task_cleanup → failed` (`onCleanupFailure: fail`) is written on the Apply path like the other terminal dispositions, and the gateway follows it in the same pass with a `failed → draining` non-Apply yield over the reclamation edge in the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) transition listing, mirroring the session-mode release sequence (the terminal Apply, then the drain yield), so the WarmPoolController drains and replaces the pod: the fail disposition releases the per-task `SandboxClaim` like every other disposition, so no stale claim remains to trigger the [Section 4.6.1](#461-warm-pool-controller-pod-lifecycle) orphan-loop recovery, and without the yield no writer would reclaim the `failed` pod. The claim release follows the phase writes of the resolution, so a gateway crash mid-resolution leaves either a pod still in `task_cleanup`, which the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` watchdog bounds, or a stale session-less claim whose collection routes the pod by phase ([Section 4.6.1](#461-warm-pool-controller-pod-lifecycle)). Immediately before the disposition's phase writes, while `status.phase` is still `task_cleanup`, the gateway makes one SSA Apply to advance `status.taskState`: the per-pod cumulative counters `tasksCompleted` (integer; advanced for every task that enters `task_cleanup`, including cancelled tasks, because the counter measures pod wear against `maxTasksPerPod`) and `scrubFailureCount` (integer) that feed the task-cleanup disposition, plus the resolution record `lastTaskId` (string), `lastDisposition` (string), and `lastScrubWarning` (boolean) that answers `ReportTaskCleanup` retries ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver"). The gateway reads `status.taskState` back at each `task_cleanup`, so the counters and the resolution record survive coordinator handoff and cross-replica claim turnover ([Section 10.1](10_gateway-internals.md#101-horizontal-scaling)). The gateway's field-manager claims on `status.taskState.*` (task mode) and on `status.activeSlots` and `status.tenantId` (concurrent mode) persist while the pod sits in the pool; the WarmPoolController writes no carve-out field other than `status.phase` (its `agent_pod_state` mirror is a read), so the retained claims conflict with no WarmPoolController Apply. The `scrub_warning` annotation ([Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) `onCleanupFailure: warn`) is written on the underlying Pod through the gateway's existing `patch` grant on Pods in agent namespaces.
```

**9.1.6 §4.6.3, SSA conflict retry policy item 2 (line 582).** Append to the "Never force-conflict" bullet, after "...would defeat the ownership boundary enforced by the SSA mechanism.":

```markdown
This prohibition is scoped to WarmPoolController and PoolScalingController reconciliation code. The gateway status window above is the sanctioned cross-manager takeover of the `Sandbox` status carve-out fields; its return path is the non-Apply phase yields defined there: the session-mode `draining` hand-back, the concurrent-mode last-slot `slot_active → idle` release, and the task-mode disposition writes (`task_cleanup → idle`, `task_cleanup → sdk_connecting`, the retiring `task_cleanup → draining`, and the `failed → draining` reclamation yield that follows the `onCleanupFailure: fail` terminal write), each of which ends the gateway's Apply claim on `status.phase`. The WarmPoolController's own phase transitions are non-Apply status `Updates` (gateway status window above), which are never rejected with a managed-fields conflict, so no transition in either direction force-conflicts.
```

**9.1.7 §4.6.3, Gateway ServiceAccount RBAC grants paragraph (line 588).** Replace the sentence beginning "Required grants:" with:

```markdown
Required grants: `get`/`list`/`watch`/`patch` on `Pods` in agent namespaces (`lenny-agents`, `lenny-agents-kata`), where `patch` serves the tenant-id and state label mutations and the `scrub_warning` annotation and the read verbs serve claim-path resolution of the claimed pod's IP for the adapter dial, `get`/`list`/`watch`/`create`/`delete` on `SandboxClaim` resources for the claim/release lifecycle and claim reads, `get`/`list`/`watch` on `Sandbox` resources for claim-path reads of pool inventory, `patch` on `Sandbox` resources for the [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) slot-claim tenant-pinning label Apply (`lenny.dev/tenant-id` is stamped on the main resource, which the `status` subresource grant does not cover), and `get`/`update`/`patch` on the `Sandbox` `status` subresource for the gateway status window above (`get` and `patch` serve the SSA Apply writes to the claim-window carve-out fields; `update` serves the non-Apply phase yields).
```

**9.1.8 §4.6.1, orphaned `SandboxClaim` detection paragraph (line 479).** Replace the sentence "If no active session exists, the claim is deleted and the underlying `Sandbox` pod is transitioned back to `idle`, returning it to the warm pool." with "If no active session exists, the claim is deleted and the underlying `Sandbox` pod is recovered according to its binding and its `status.phase` (the gate and the full routing follow)." and insert after the replacement:

```markdown
The claim-deletion half of this path is phase-independent: a session-less claim older than `claimOrphanTimeout` is deleted regardless of the referenced `Sandbox.status.phase`, so stale claims referencing pods in `failed`, `task_cleanup`, or terminal phases cannot accumulate (the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` watchdog relies on this for its no-report path). The pod-recovery half runs only when the collected claim is the pod's current binding: when another live `SandboxClaim` references the pod, the stale claim is deleted with no recovery write. The gate exists because task-mode pods outlive their claims: the pod returns to the pool and the next task re-claims it through the standard claim flow ([Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes)), so a gateway crash between a disposition's phase writes and the claim release leaves a stale claim that the loop can collect after the pod is already serving a later task, and an ungated recovery write would drain that live pod. The claim flow creates the `SandboxClaim` before the gateway's first phase write of the claim window ("Gateway status window" in [Section 4.6.3](#463-crd-field-ownership-and-write-boundaries)), so a re-claimed pod carries the later task's live claim in every phase the recovery routing below writes. The gated recovery covers the following gateway-crash windows. Session creation claims the pod and runs the full bind before the session row is persisted ([Section 7.1](07_session-lifecycle.md#71-normal-flow) atomicity), so a crash in that window can leave a session-less claim referencing a pod in any phase from `claimed` through `attached`, or in `failed` from a pre-attached failure write. Session release writes the terminal phase, when the disposition has a valid edge from the pod's current phase, before the drain yield ("Gateway status window" in [Section 4.6.3](#463-crd-field-ownership-and-write-boundaries)); when no such edge exists (for example a session expiring while `attached`), the terminal write is skipped and the yield drains the pod directly. A terminal session no longer backs its claim, so a crash between the terminal write and the yield can leave a session-less claim referencing a pod in `completed`, `cancelled`, or `expired`; a crash before the yield on the skipped-terminal path leaves the pod in `attached` or `claimed`, which the routing below already drains. Every recovery write follows an edge in the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) transition listing. A pod still in `claimed` is transitioned to `draining` for replacement through the claim-time abort edge: a pod claimed but torn down before workspace start drains and terminates rather than stranding, and the state machine defines no `claimed → idle` edge. A pod in `receiving_uploads`, `finalizing_workspace`, `running_setup`, or `starting_session` is transitioned to `failed` through its pre-attached failure edge and then to `draining` for replacement, because those phases carry no direct `→ draining` edge. A pod in `attached`, in `failed`, or in the session-terminal phases `completed`, `cancelled`, or `expired` is transitioned to `draining` for replacement directly. A partially materialized or used session-mode pod cannot return to `idle` without violating the [Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like) one-session exclusivity invariant. In task mode, the same route recovers a pod stranded in `cancelled` by a gateway crash before the `task_cleanup` entry write (the `task_cleanup` watchdog timer starts only at that write) and a pod stranded in `failed` by a crash between the fail disposition's `failed` Apply and its `failed → draining` yield, whose claim release had not yet run because the release follows the phase writes ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver"). Every recovery write is a non-Apply status `Update` that writes the new phase, and only the phase: a crashed gateway never executes its yield, so its Apply claim on `status.phase` persists in `managedFields`, and a non-Apply `Update` lands against that claim, strips it on exactly the field it writes, and takes ownership (the standard WarmPoolController phase-write mechanism; see "Gateway status window" in [Section 4.6.3](#463-crd-field-ownership-and-write-boundaries), covered by the WarmPoolController's `update` grant on the `Sandbox` `status` subresource). A pod in `task_cleanup` receives no recovery write from the orphan loop: a mid-scrub pod can hold residual task state, and its wait is bounded exclusively by the `task_cleanup` watchdog ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine)). A pod in `sdk_connecting` receives no recovery write: the stale claim belongs to a released task (the `rewarm_sdk` disposition releases the claim while the adapter re-establishes SDK-warm state), the re-warm is bounded by the `sdkConnectTimeoutSeconds` watchdog ([Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like)), and the WarmPoolController returns the pod to `idle` when the re-warm completes. A pod in `slot_active` receives no recovery write either: its sibling slots may still be live, so the stale per-slot claim is deleted by the phase-independent half and the pod is left in place. The dead slot's count is not recovered: the slot release deletes the claim before its count release, so a crash that leaves a stale claim has already skipped the decrement, and the last-slot `slot_active → idle` yield ("Gateway status window" in [Section 4.6.3](#463-crd-field-ownership-and-write-boundaries)) fires only when the count reaches zero. The pod continues serving its live slots at reduced capacity until the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `slot_active → draining` `maxPodUptimeSeconds` drain reclaims it; the [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) post-recovery rehydration after a Redis restart is the only count resync.
```

On application, accompanying code changes gate, reroute, and convert the shipped `ClaimGarbageCollector.returnToIdle` (`pkg/controller/warmpool/gc.go:201-253`): the binding gate (the recovery write is skipped when another live `SandboxClaim` references the pod; the claim path creates the `SandboxClaim` before its first phase Apply, `pkg/gateway/podclaim/claimer.go:103-114`, so the later task's claim exists in every recovery-write phase) precedes the phase routing; the phase-routed recovery (`claimed → draining` through the abort edge at `pkg/sandbox/state/state.go:243`; `receiving_uploads`, `finalizing_workspace`, `running_setup`, and `starting_session` → `failed` → `draining` through the pre-attached failure edges at `pkg/sandbox/state/state.go:164-181` and the reclamation edge at `pkg/sandbox/state/state.go:259`; `attached`, `failed`, and the session-terminal phases `completed`, `cancelled`, and `expired` → `draining` through the edges at `pkg/sandbox/state/state.go:244,258-261`; no write for `task_cleanup`, `sdk_connecting`, `slot_active`, `idle`, `draining`, and `terminated`; every routed write is an edge already present in `ValidTransitions` and passes `state.IsValid`) replaces the current flip of any phase other than `idle` and `draining` back to `idle`; and the write converts from a non-forcing SSA Apply under `retryOnConflictSSA` (`pkg/controller/warmpool/controller.go:47-74`, which conflicts deterministically against a crashed gateway's persisting `status.phase` claim and gives up after five attempts) to the non-Apply status `Update` the staged clause specifies. Each change is recorded in the Files-touched table. Claim deletion in `ClaimGarbageCollector.reclaim` is already phase-independent and needs no change. The sentence following the replaced one in the source paragraph ("This handles the crash scenario where a gateway creates a `SandboxClaim` but crashes before persisting the session to Postgres — on recovery, the gateway has no record of the claim, so the controller's orphan loop reclaims it.") is retained verbatim and remains accurate: the inserted clause defines how the orphan loop's reclamation routes that scenario's pod.

**9.1.9 §4.6.3, RBAC-enforcement paragraph (line 586).** In the sentence enumerating the WarmPoolController ServiceAccount grants, replace the clause `` `get`/`patch` on `status` subresources of `Sandbox`, `SandboxTemplate`, and `SandboxWarmPool` (SSA status updates require `get` to read the current resource and `patch` to apply the partial object — `update` is insufficient for SSA Apply requests) `` with:

```markdown
`get`/`patch` on `status` subresources of `Sandbox`, `SandboxTemplate`, and `SandboxWarmPool` (SSA status updates require `get` to read the current resource and `patch` to apply the partial object — `update` is insufficient for SSA Apply requests) plus `update` on the `Sandbox` `status` subresource (required by the WarmPoolController's non-Apply `status.phase` transitions: the pooled-pod transitions named in the gateway status window, the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` watchdog's `failed` and follow-on `draining` writes, and the [Section 4.6.1](#461-warm-pool-controller-pod-lifecycle) orphan-GC recovery writes, each of which lands against any manager's claim on `status.phase` and takes ownership of the field; see "Gateway status window" above)
```

The shipped chart already grants the controller `get`/`update`/`patch` on `sandboxes/status` (`charts/lenny/templates/controller-rbac.yaml:57-59`); the staged clause reconciles the spec with that grant for the same reason Section 9.1.7 reconciles the gateway grant.

**9.1.10 §4.6.3, `SandboxClaim.status.phase` enumeration, `released` bullet (line 593).** Replace the bullet with:

```markdown
- `released` — the gateway has released the pod back to the pool; the referenced `Sandbox.status.phase` has transitioned to `idle` (for a recyclable pod), to `draining`/`terminated` (for a pod being replaced), or, in task mode, to `sdk_connecting` (the `rewarm_sdk` disposition releases the claim while the adapter re-establishes SDK-warm state; [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver") or `failed` (a late report whose release lands between the `task_cleanup` watchdog's `failed` write and its follow-on `draining` write; the `onCleanupFailure: fail` disposition itself releases the claim after its `failed → draining` yield, so its referenced pod is in `draining`). The `SandboxClaim` resource is deleted shortly after entering `released`; it is a terminal phase.
```

The pre-edit bullet restricts `released` to a referenced pod in `idle` or `draining`/`terminated`. The Section 9.3.3 driver paragraph releases the claim on every disposition and cites this enumeration, so without the extension the staged §6.2 text would contradict the §4.6.3 definition on the `rewarm_sdk` path and on a late report released between the watchdog's `failed` write and its follow-on `draining` write (the `onCleanupFailure: fail` disposition releases after its `failed → draining` yield, which the `draining` case already covers). The `bound`/`active`/`released`/`failed` phase-value enumeration itself is unchanged, so the CRD OpenAPI validation sentence below the bullet is unaffected.

### 9.2 `spec/05_runtime-registry-and-pool-model.md`

**9.2.1 Line 415.** Replace the lifecycle sentence with:

```markdown
Lifecycle: task completes (the runtime emits its terminal `{type: "response"}` frame, [Section 15.4.1](15_external-api-surface.md#1541-adapterbinary-protocol)) → adapter sends `task_complete` on the lifecycle channel → runtime replies with `task_complete_acknowledged` → adapter removes `/run/lenny/credentials.json` (credential purge must precede any deployer code; see scrub step 0 below) → deployer-defined `cleanupCommands` execute (have access to task state, but NOT the previous task's credential file) → Lenny scrub runs → adapter reports the scrub outcome to the gateway via `ReportTaskCleanup` ([Section 4.7](04_system-components.md#47-runtime-adapter)) → the gateway resolves and applies the `task_cleanup` disposition ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver") → on a `reuse` disposition the pod returns to `idle`; on a `rewarm_sdk` disposition (preConnect pools) the adapter re-establishes SDK-warm state through `sdk_connecting`, and the WarmPoolController returns the pod to `idle` when the re-warm completes ([Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like) task-mode `preConnect` row) → the next task claims the pod through the standard claim flow with a fresh `AssignCredentials` lease, workspace materialization, and a regenerated adapter manifest ([Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like)) → at dispatch the adapter sends `task_ready` with the new task's ID. A cancelled task enters the same sequence at the credential-purge step ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `cancelled → task_cleanup`); the `task_complete` exchange occurs only on the completion path. `setupCommands` run once per pod at start rather than per task. Per-task setup belongs in the runtime's initialization.
```

**9.2.2 Line 419, Full-level bullet.** Replace with:

```markdown
- **Full-level:** Pod reuse works as described above. The adapter sends `task_complete` on the lifecycle channel, the runtime replies with `task_complete_acknowledged`, scrub runs, the adapter reports the outcome via `ReportTaskCleanup` ([Section 4.7](04_system-components.md#47-runtime-adapter)), the pod returns to the pool under the gateway-resolved disposition, and the adapter sends `task_ready` at the next task's dispatch.
```

**9.2.3 Line 446, `onCleanupFailure: warn` bullet.** Replace the opening sentence "the pod is returned to the available pool with a `scrub_warning` annotation." with:

```markdown
the gateway returns the pod to the available pool and writes the `scrub_warning` annotation ([Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window).
```

The remainder of the bullet (gateway logging, both scrub metrics, the preConnect interaction, and the `maxScrubFailures` retirement) is unchanged.

**9.2.4 Line 410, `taskPolicy` example comment.** Replace the `maxTasksPerPod` line with:

```yaml
  maxTasksPerPod: 50 # required — pod retired after this many tasks (completed or cancelled)
```

**9.2.5 Line 451, task-count-limit retirement bullet.** Replace the opening two sentences ("The pod's completed task count reaches `maxTasksPerPod`. After the current task completes and scrub finishes, the pod transitions to `draining` instead of returning to `idle`.") with:

```markdown
The pod's cumulative task count reaches `maxTasksPerPod`. The counter advances for every task that enters `task_cleanup`, including cancelled tasks, because it measures pod wear ([Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) `Sandbox.status.taskState.tasksCompleted`). After the current task's between-task sequence finishes, the pod transitions to `draining` instead of returning to `idle`.
```

The remainder of the bullet (replacement provisioning and the no-default rationale) is unchanged. Without the 9.2.4 and 9.2.5 edits, the staged counter semantics (advance on every `task_cleanup` entry, Sections 9.1.5 and 9.3.3) and the unedited §5.2 retirement predicate ("completed tasks", `spec/05_runtime-registry-and-pool-model.md:410,451`) would disagree on whether a cancelled task counts toward `maxTasksPerPod`. The same completed-task wording is mirrored in the `MaxTasksPerPod` field comment at `pkg/apis/lenny/v1alpha1/sandboxtemplate_types.go:69` and in the CRD schemas generated from it (`charts/lenny/crds/lenny.dev_sandboxtemplates.yaml:282`; `pkg/embedded/crds/lenny.dev_sandboxtemplates.yaml:268`); the comment reword to the cumulative semantics above and the CRD regeneration accompany the implementation and are recorded in the Files-touched table.

### 9.3 `spec/06_warm-pod-model.md`

**9.3.1 §6.1, `preConnect` compatibility table, `task` row (line 76).** Two spans change. First, replace the cell's opening through "...along with all other task processes." (the two sentences ending at that phrase) with the text below. Second, delete the standalone sentence "The SDK is re-warmed after the next scrub.", which sits after the `sdkWarmBlockingPaths` sentences; the staged text's `rewarm_sdk` sentence subsumes it. The `sdkWarmBlockingPaths` per-task evaluation sentences and the circuit-breaker sentence are retained verbatim.

```markdown
SDK process pre-connected once during warm phase (same as session mode). Between tasks: the SDK process is terminated during Lenny scrub step 1 (`kill -9 -1` as sandbox user) along with all other task processes. When the gateway's `ReportTaskCleanup` response carries the `rewarm_sdk` disposition ([Section 4.7](04_system-components.md#47-runtime-adapter)), the adapter re-establishes SDK-warm state by calling the SDK connect sequence again. Re-warm completion is signaled through container readiness: the adapter surfaces SDK-warm state through its container readiness probe, so the scrub-step-1 kill drops the pod's readiness and the re-established SDK connection restores it (the `lenny.dev/sandbox-ready` readiness gate set during the initial warm stays true, so Pod readiness follows the probe), and the WarmPoolController writes `sdk_connecting → idle` when readiness returns ([Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window). The `sdkConnectTimeoutSeconds` watchdog bounds the re-warm with its clock anchored at the pod's observed entry into `sdk_connecting`; the initial warm anchors at pod start, and a re-warm clock anchored at pod start would already have expired on any pod that has served a task. `task_ready` is sent at the next task's dispatch, after that task's workspace is materialized.
```

On application, the re-warm readiness signal and the re-anchored watchdog clock are code changes flagged in the Files-touched table. The shipped initial-warm signal cannot detect a re-warm: the `lenny.dev/sandbox-ready` gate is one-shot (`pkg/controller/sandbox/controller.go:523-556` flips it on container readiness and never resets it, and no shipped container readiness probe reflects SDK-warm state), `DecideSDKWarm` returns `sdk_connecting → idle` on Pod readiness alone (`pkg/controller/sandbox/lifecycle/lifecycle.go:177-186`), and the shipped watchdog clock is the pod's running time since `pod.Status.StartTime` (`pkg/controller/sandbox/sdkwarm.go:110-125`), which would retire any re-warming pod on its first reconcile.

**9.3.2 §6.2 state listing.** Insert after the line `task_cleanup ──→ failed            (onCleanupFailure: fail — pod terminated)` (line 156):

```markdown
  task_cleanup ──→ failed            (task_cleanup watchdog: taskCleanupTimeoutSeconds elapsed without a ReportTaskCleanup outcome — adapter death or undeliverable report; pod terminated and replaced)
```

Insert after the `Pre-attach terminal collapse` block (line 139), before the `Task-mode state transitions (from attached):` block (line 141), as a new blank-line-separated group:

```markdown
Pod reclamation drain transitions (pod-level sources of draining → terminated):
  claimed ──→ draining               (claim-time abort: the session-mode release drain yield ([§4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window) for an early release of a pod still in claimed (for example a resumed pod released while still in claimed), or the [§4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) orphan loop draining the pod after a gateway crash between the claim and the setup chain)
  attached ──→ draining              (the session-mode release drain yield when the terminal disposition has no modeled edge from attached (for example a session expiring while attached; [§4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window), or [§4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) orphan-loop recovery of a pod stranded in attached by a gateway crash)
  completed ──→ draining             (exclusive-pod reclamation after the terminal write: the session-mode release drain yield ([§4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window) or the [§4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) orphan-loop recovery)
  cancelled ──→ draining             (same triggers as completed → draining)
  expired ──→ draining               (same triggers as completed → draining)
  failed ──→ draining                (failed-pod reclamation: the session-mode release drain yield, the gateway yield that follows the task-mode onCleanupFailure: fail write ([§4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window), the task_cleanup watchdog's follow-on write (below), and the [§4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) orphan-loop recovery)
```

These edges exist in the implemented transition table (`pkg/sandbox/state/state.go:243-244,258-261`), whose comments record them as undocumented extensions of the §6.2 cleanup model (`pkg/sandbox/state/state.go:235-241,246-257`), and the session-terminal, `attached`, and `claimed` drains are all exercised by the shipped session-mode release path: the terminal write is guarded by `state.IsValid` and the drain is unconditional (`pkg/gateway/podsession/binder.go:1465-1492`; the drain doc comment at `binder.go:763-767` names "a terminal disposition, attached, or an early claimed/idle release"), so a release whose disposition has no modeled edge from the pod's current phase (for example a session expiring while `attached`, since `ValidTransitions` defines no `{Attached, Expired}` edge and the production expiry path maps `StateExpired` into `Release`, `pkg/gateway/sessionserver/usage.go:53-54` and `pkg/gateway/executor/pod.go:299-300`, or a resumed pod released while still in `claimed`) drains the pod directly from a live gateway. The driver, watchdog, and §4.6.1 recovery texts staged in this proposal assign each of these edges a specified writer, so the listing gains them; without the lines, §4.6.1 and the §6.2 paragraphs would cite edges the listing does not define, and the listing is the normative machine (`IsValid` rejects any absent edge with "not a valid transition per spec §6.2", `pkg/sandbox/state/state.go:274`).

**9.3.3 §6.2, new paragraphs.** Insert between the "Host-node schedulability precondition" paragraph (line 181) and the "preConnect re-warm on scrub_warning" paragraph (line 183):

```markdown
**Task-mode disposition driver.** The gateway resolves the `task_cleanup` branch above exactly once per `task_id`, when the adapter's `ReportTaskCleanup` RPC arrives ([Section 4.7](04_system-components.md#47-runtime-adapter) Adapter → Gateway RPCs). The report is sent for every task whose pod enters `task_cleanup`, whether from `attached` (task completion) or from `cancelled` (cancellation acknowledged); the cancellation path runs the same between-task sequence without the `task_complete` exchange. The `task_cleanup` entry is itself a gateway write on the [Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) Apply path: the gateway writes `attached → task_cleanup` when its session-result path observes the task's terminal outcome (the runtime's terminal `{type: "response"}` frame, or process exit synthesized as a result; [Section 15.4.1](15_external-api-surface.md#1541-adapterbinary-protocol)), and `cancelled → task_cleanup` when the cancellation it initiated is acknowledged; the [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) between-task sequence runs pod-side while the pod sits in `task_cleanup`. The inputs are the reported scrub outcome, the pool's `taskPolicy`, the pod's cumulative counters from `Sandbox.status.taskState` advanced by this task's outcome (`tasksCompleted` advances on cancelled tasks as well; [Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window), the pod's uptime against `taskPolicy.maxPodUptimeSeconds` (computed at resolution time from the pod's start time on the API object, so it is replica-independent without externalization), and the `lenny.dev/host-schedulable` label read at the moment of the transition (host-node schedulability precondition above). The writes and metric emissions that the resolution produces are enumerated on the `ReportTaskCleanup` row in [Section 4.7](04_system-components.md#47-runtime-adapter); on every disposition the gateway also releases the per-task `SandboxClaim` (the claim enters `released` and is deleted per [Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries), so the next task's claim passes the `lenny-sandboxclaim-guard` webhook through the standard claim flow). On the `onCleanupFailure: fail` disposition the gateway writes `task_cleanup → failed` on the Apply path and follows it in the same pass with a `failed → draining` non-Apply yield over the reclamation edge in the transition listing above ([Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window), mirroring the session-mode release sequence, so the WarmPoolController drains and replaces the pod; a claim-less `failed` pod has no other reclamation writer. The claim release follows the phase writes of the resolution, so a gateway crash mid-resolution leaves either a pod still in `task_cleanup`, bounded by the `task_cleanup` watchdog below, or a stale session-less claim whose collection by the [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) orphan loop routes the pod by phase. The gateway resolves and writes the disposition only while `Sandbox.status.phase` is still `task_cleanup`, and records the resolution in `status.taskState` (`lastTaskId`, `lastDisposition`, `lastScrubWarning`) in the same Apply that advances the counters. Retries are answered from that record: a report whose `task_id` equals `status.taskState.lastTaskId` receives the recorded `lastDisposition` and `lastScrubWarning` with no phase write and no metric emission, on any gateway replica and regardless of the phase the pod has since reached (`idle`, `sdk_connecting`, `draining`, `failed`, or a later phase). If a report with no recorded resolution arrives after the `task_cleanup` watchdog (below) has written `failed`, the gateway performs no phase write, responds with `disposition: retire`, and releases the `SandboxClaim` if it still exists; the pod's failure and replacement are accounted once, on the watchdog path, and the late report emits no scrub or retirement metric.

**`task_cleanup` watchdog.** A pod that hangs in `task_cleanup` (the adapter died mid-scrub, or the cleanup report is permanently undeliverable) would otherwise hold a tenant-pinned pool slot indefinitely. The WarmPoolController applies a per-pod timeout, `taskCleanupTimeoutSeconds` (default: the pool's `taskPolicy.cleanupTimeoutSeconds` plus 120 seconds; configurable per pool in the `scalingPolicy` block, following the `sdkConnectTimeoutSeconds` precedent in [Section 6.1](#61-what-a-pre-warmed-pod-looks-like)). The timer starts at the gateway's `task_cleanup` entry write (driver paragraph above), so an adapter that dies at any point in the between-task sequence is bounded. If the pod has not left `task_cleanup` within the timeout, the WarmPoolController transitions it to `failed` and then to `draining`, two sequential non-Apply status updates in the same pass, each over an edge in the transition listing above (`task_cleanup → failed`, then the `failed → draining` reclamation edge; the same two-write pattern as the [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) mid-bind recovery). The first update lands against the gateway's field-manager claim on `status.phase`, strips the claim on exactly the field it writes, and takes ownership (the standard WarmPoolController phase-write mechanism; [Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) "Gateway status window"). The expiry increments `lenny_warmpool_task_cleanup_timeout_total` (counter, labeled by `pool`; [Section 16.1](16_observability.md#161-metrics)), and the drain provisions a replacement from the warm pool. The watchdog completes the reclamation itself because the orphan loop's pod recovery is coupled to claim collection: claim age is measured from `metadata.creationTimestamp` at task dispatch and the backing session is terminal from the `task_cleanup` entry, so for any task that runs longer than `claimOrphanTimeout` minus `taskCleanupTimeoutSeconds` the loop collects the session-less claim while the pod is still in `task_cleanup`, a no-write phase for the loop, and a later `failed` write without the follow-on `draining` would strand the pod with no claim left to trigger recovery. Alert `TaskCleanupTimeout` (Warning) fires on a sustained rate ([Section 16.5](16_observability.md#165-alerting-rules-and-slos)); the counter distinguishes watchdog-fired replacements from `onCleanupFailure: fail` retirements. The watchdog performs no claim write: on the no-report path the per-task `SandboxClaim` is session-less and the orphan loop ([Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) collects it, because the loop's claim-deletion half is phase-independent. A mid-scrub pod is never returned to `idle` by any other mechanism: the orphan loop's pod-recovery half excludes `task_cleanup` ([Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)), so the watchdog is the sole bound on `task_cleanup`.
```

### 9.4 `spec/12_storage-architecture.md`

**9.4.1 `agent_pod_state` table schema (lines 457-469).** Add two columns after `execution_mode`:

```sql
    tasks_completed     INT     NOT NULL DEFAULT 0,  -- task mode: cumulative count of tasks that entered task_cleanup (mirrors Sandbox.status.taskState.tasksCompleted)
    scrub_failure_count INT     NOT NULL DEFAULT 0,  -- task mode: cumulative failed-scrub count (mirrors Sandbox.status.taskState.scrubFailureCount)
```

**9.4.2 Mirror contract.** Append to the paragraph following the table (line 476):

```markdown
The `tasks_completed` and `scrub_failure_count` columns mirror the gateway-written `Sandbox.status.taskState` counters ([Section 4.6.3](04_system-components.md#463-crd-field-ownership-and-write-boundaries) gateway status window) and are maintained by the WarmPoolController on the same transition mirror as `state`. They are read-optimized mirrors; the CRD status is authoritative for the task-cleanup disposition ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine)). The `lastTaskId`, `lastDisposition`, and `lastScrubWarning` fields of `status.taskState` are not mirrored; they serve only `ReportTaskCleanup` retry recall ([Section 6.2](06_warm-pod-model.md#62-pod-state-machine) "Task-mode disposition driver").
```

### 9.5 `spec/15_external-api-surface.md`

**9.5.1 §15.1, new paragraph.** Insert after the session-lifecycle route table (line 610), before the paragraph beginning `**State-mutating endpoint preconditions.**`:

```markdown
**Task-mode submission.** Work for a task-mode pool ([Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) `executionMode: task`) is submitted through the same session endpoints: `POST /v1/sessions` (or `POST /v1/sessions/start`) against a task-mode pool creates one task, and the creation response carries `sessionIsolationLevel.executionMode: task` ([Section 7.1](07_session-lifecycle.md#71-normal-flow)). Pod reuse between tasks is a pool-internal behavior governed by the [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes) between-task lifecycle and integration-level rules and the [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` disposition; the REST surface exposes it through the `sessionIsolationLevel` fields `podReuse`, `scrubPolicy`, and `residualStateWarning` ([Section 7.1](07_session-lifecycle.md#71-normal-flow)). The v1 REST surface defines no separate task-submission route. The tasks-suffixed paths in [Section 21](21_planned-post-v1.md#21-planned--post-v1) (`POST /a2a/{runtime}/tasks`, `GET /a2a/{runtime}/tasks/{id}/stream`, `POST /ap/v1/agent/tasks`) are post-v1 `ExternalProtocolAdapter` routes: the A2A adapter creates a Lenny session per task ([Section 21.1](21_planned-post-v1.md#21-planned--post-v1)), and the Agent Protocol adapter follows the same adapter mechanism ([Section 21.3](21_planned-post-v1.md#21-planned--post-v1)).
```

**9.5.2 §15.4 artifact list intro (line 1422).** Replace "The runtime adapter contract is published as three machine-readable artifacts committed to the repository" with:

```markdown
The runtime adapter contract is published as machine-readable artifacts committed to the repository
```

**9.5.3 §15.4 artifact list, JSONL bullet (line 1425).** Replace the parenthetical message list with:

```markdown
- **`schemas/lenny-adapter-jsonl.schema.json`** — JSON Schema (Draft 2020-12) for every adapter↔binary stdin/stdout message defined in [Section 15.4.1](#1541-adapterbinary-protocol) (`message`, `tool_result`, `heartbeat`, `shutdown`, `response`, `tool_call`, `heartbeat_ack`, `status`, and `set_tracing_context`). Open-string `type` fields are modeled via `anyOf` with pass-through for unknown types per the canonical type registry contract.
```

**9.5.4 §15.4 artifact list, new bullet.** Insert after the JSONL bullet:

```markdown
- **`schemas/lifecycle-events.schema.json`** — JSON Schema (Draft 2020-12) for the envelopes carried on the [Section 4.7](04_system-components.md#47-runtime-adapter) adapter↔runtime lifecycle channel, including `lifecycle_capabilities`, `checkpoint_request`, `task_complete`, `task_ready`, `task_complete_acknowledged`, and the other lifecycle messages, delivered in Phase 2.8 ([Section 18.8](18_build-sequence.md#188-phase-28--streaming-echo-runtime)).
```

The bullet attributes the channel rather than the envelope set to §4.7: the schema also defines a `files_updated` envelope (`schemas/lifecycle-events.schema.json:17`, emitted by `pkg/adapter/lifecyclechannel.go` `SignalFilesUpdated`) that no §4.7 row defines. Adding that row to §4.7 is a separate reconciliation item outside this proposal's scope; the wording above keeps the §15.4 artifact-prose invariant (`spec/15_external-api-surface.md:1428`) intact when the bullet lands.

**9.5.5 §15.4.1, task-mode between-task signaling paragraph (line 1461).** Replace the paragraph with:

```markdown
**Task mode between-task signaling:** Adapter sends `{type: "task_complete", taskId: "..."}` on the lifecycle channel after a task completes. The runtime releases task-specific resources and replies with `{type: "task_complete_acknowledged", taskId: "..."}`. After deployer-defined `cleanupCommands` and Lenny scrub complete and the gateway dispatches the next task (workspace materialized, manifest regenerated), the adapter sends `{type: "task_ready", taskId: "..."}` with the new task's ID. The runtime re-reads the adapter manifest (regenerated per task) and the next `{type: "message"}` on stdin is the start of the new task. This is distinct from `terminate`, which always means process exit.
```

### 9.6 `spec/16_observability.md`

**9.6.1 §16.1 metric catalog.** Insert after the "SDK-warm connect timeouts" row (line 262):

```markdown
| **Task-Cleanup Watchdog**                                                                                                                                                                                              |                 |
| Task-cleanup watchdog expiries (`lenny_warmpool_task_cleanup_timeout_total`, counter labeled by `pool` — increments when a task-mode pod leaves `task_cleanup` via the `task_cleanup` watchdog (`taskCleanupTimeoutSeconds` elapsed without a `ReportTaskCleanup` outcome); distinguishes watchdog-fired pod replacements from `onCleanupFailure: fail` retirements; sustained rate triggers the `TaskCleanupTimeout` warning alert ([Section 16.5](#165-alerting-rules-and-slos)); see [Section 6.2](06_warm-pod-model.md#62-pod-state-machine))   | Counter         |
```

**9.6.2 §16.5 alert table.** Insert after the `SDKConnectTimeout` row (line 491):

```markdown
| `TaskCleanupTimeout`             | `lenny_warmpool_task_cleanup_timeout_total` rate > 0.1/min for > 5 min on a given pool; adapters are systematically failing to deliver `ReportTaskCleanup` (adapter deaths mid-scrub or undeliverable reports) and the `task_cleanup` watchdog is replacing the pods. See [Section 6.2](06_warm-pod-model.md#62-pod-state-machine) `task_cleanup` watchdog.            | Warning  |
```

**9.6.3 §16.1 `lenny_task_reuse_count` row (line 124).** In the row's parenthetical, replace the clause "Observed per-pod at task completion;" with:

```markdown
Observed once per pod at its retiring `task_cleanup` resolution (`task_cleanup → draining` or `task_cleanup → failed`; cancelled tasks count toward the observed value; [Section 4.7](04_system-components.md#47-runtime-adapter) `ReportTaskCleanup`);
```

Without this edit the row would anchor the observation to task completion while the staged §4.7 row (Section 9.1.1) emits the observation at the retiring `ReportTaskCleanup` resolution, which can fall on the cancellation path, where no task completion occurs. The observation is scoped to retiring resolutions rather than every resolution because the counter is cumulative: a per-resolution observation would feed the samples 1 through N from an N-task pod into the histogram and bias the pool p50 toward N/2, contradicting the unedited §5.2 derivation `mode_factor = avg_tasks_per_pod_lifetime` from the histogram p50 with convergence toward `maxTasksPerPod` (`spec/05_runtime-registry-and-pool-model.md:549,571`), the surviving `histogram_quantile(0.50, ...)` clause in the same §16.1 row, the shipped retirement-time emitter (`ObserveTaskReuseCount`, `pkg/gateway/gatewaymetrics/gatewaymetrics.go:3047-3056`), and the PoolScalingController reader (`pkg/controller/poolscaling/controller.go:255-264`). The retiring-resolution scope keeps all of those unchanged while fixing the cancelled-task gaps in the pre-edit wording: a cancelled final task triggers the observation, and cancelled tasks count toward the observed value.

### 9.7 `schemas/lenny-adapter.proto`

**9.7.1 `service GatewayControl` (line 209).** In the service doc comment, replace the sentence "The service exists because a few §8 control operations originate at the pod and need a gateway decision." with:

```proto
// The service exists because a few control operations (§8 budget
// extension, the §9.1 platform-tool and §9.3 connector-tool surfaces,
// and §5.2/§6.2 task-cleanup reporting) originate at the pod and need
// a gateway decision.
```

Add after the `ExtendLease` RPC (line 216):

```proto
  // ReportTaskCleanup is the adapter's once-per-task report of the §5.2
  // between-task cleanup outcome (step-0 credential purge,
  // cleanupCommands, Lenny scrub, and the step-7 guest restart
  // re-verification when applicable; the task_complete exchange precedes
  // the sequence on the completion path and is absent on the §6.2
  // cancellation path). The gateway resolves the §6.2 task_cleanup
  // disposition exactly once per task_id (the RPC is idempotent on
  // retry), writes the pod phase, annotation, and task counters,
  // releases the per-task SandboxClaim, and emits the §5.2 scrub and
  // retirement metrics. The response tells the adapter whether to
  // re-warm the SDK, prepare for termination, or idle until the next
  // claim. The adapter retries an undeliverable report with backoff
  // (initial 1s, cap 30s).
  // spec: §4.7 Adapter → Gateway RPCs; §5.2; §6.2.
  rpc ReportTaskCleanup(ReportTaskCleanupRequest) returns (ReportTaskCleanupResponse) {}
```

**9.7.2 Messages and enums.** Add after `ExtendLeaseResponse` (line 1168):

```proto
// ReportTaskCleanupRequest carries the adapter's §5.2 between-task
// cleanup outcome for one finished task (completed or cancelled).
message ReportTaskCleanupRequest {
  string session_id = 1;
  string task_id = 2;
  ScrubResult scrub_result = 3;
  // failed_step names the scrub step that failed, using the
  // pkg/adapter/scrub StepName vocabulary (for example
  // "remove_workspace" or "verify"). Empty when scrub_result is
  // SCRUB_RESULT_SUCCEEDED.
  string failed_step = 4;
  // cleanup_commands_exit is the exit code of the deployer-defined
  // cleanupCommands run (§5.2). Zero on success.
  int32 cleanup_commands_exit = 5;
  // vm_restart reports the §5.2 step-7 guest VM restart outcome.
  // VM_RESTART_RESULT_SKIPPED when the pool does not run the restart.
  VmRestartResult vm_restart = 6;
}

enum ScrubResult {
  SCRUB_RESULT_UNSPECIFIED = 0;
  SCRUB_RESULT_SUCCEEDED = 1;
  SCRUB_RESULT_FAILED = 2;
}

enum VmRestartResult {
  VM_RESTART_RESULT_UNSPECIFIED = 0;
  VM_RESTART_RESULT_SKIPPED = 1;
  VM_RESTART_RESULT_SUCCEEDED = 2;
  VM_RESTART_RESULT_FAILED = 3;
}

// ReportTaskCleanupResponse carries the gateway-resolved §6.2
// task_cleanup disposition.
message ReportTaskCleanupResponse {
  TaskDisposition disposition = 1;
  // scrub_warning is true when the pod carries the §5.2 scrub_warning
  // annotation after this task (onCleanupFailure: warn with a failed
  // scrub below maxScrubFailures).
  bool scrub_warning = 2;
}

enum TaskDisposition {
  TASK_DISPOSITION_UNSPECIFIED = 0;
  // TASK_DISPOSITION_REUSE: the pod returns to idle and awaits the next
  // claim (non-preConnect pools).
  TASK_DISPOSITION_REUSE = 1;
  // TASK_DISPOSITION_REWARM_SDK: the adapter re-establishes SDK-warm
  // state through sdk_connecting before the pod returns to idle
  // (preConnect pools, §6.1).
  TASK_DISPOSITION_REWARM_SDK = 2;
  // TASK_DISPOSITION_RETIRE: the pod is retired; the adapter prepares
  // for termination.
  TASK_DISPOSITION_RETIRE = 3;
}
```

**9.7.3 Stale taxonomy comment (lines 1320-1324).** Replace the comment above `LifecycleChannelRequest` with:

```proto
// Opaque lifecycle event envelope. The control-event taxonomy carried on
// this stream is the §4.7 adapter→gateway control-event table
// (RATE_LIMITED, AUTH_EXPIRED, PROVIDER_UNAVAILABLE, LEASE_REJECTED,
// CheckpointBarrierAck, AdapterTerminating, FINAL_USAGE_REPORT); the
// wire definitions live in pkg/adapter/controlchannel.go. Kept opaque
// here so the proto schema does not need to evolve every time a control
// event is added. Request and response carry the same payload but are
// typed separately per proto-RPC convention.
```

On application, regenerate `pkg/proto/adapter/v1/*.pb.go` via `buf generate`.

### 9.8 `schemas/lenny-adapter-jsonl.schema.json`

Remove the `task_complete`, `task_complete_acknowledged`, and `task_ready` entries from the top-level `oneOf` (lines 19-21) and delete the corresponding `$defs` (lines 228-262). Replace the top-level `description` (line 5) with:

```json
"description": "Newline-delimited JSON exchanged on stdin/stdout between the adapter sidecar and the agent binary inside a Lenny pod. The adapter receives lines on stdin (inbound) and reads lines from stdout (outbound). Each line is a single JSON object discriminated by `type`. The adapter↔runtime lifecycle channel (checkpoint, interrupt, credentials_rotated, task_complete, task_ready, and the other lifecycle messages) is a separate Unix-socket stream defined in schemas/lifecycle-events.schema.json, and the gateway↔adapter control surface is the gRPC API in lenny-adapter.proto. See spec/15_external-api-surface.md §15.4."
```

### 9.9 `schemas/lifecycle-events.schema.json`

Replace the top-level `description` (line 5) with:

```json
"description": "JSON envelopes exchanged on the §4.7 adapter↔runtime lifecycle channel, a Unix socket distinct from the stdin/stdout JSONL message channel (schemas/lenny-adapter-jsonl.schema.json). Field names are camelCase to match the §4.7 message-schema table and the adapter wire (pkg/adapter/lifecyclechannel.go). The task_complete, task_ready, and task_complete_acknowledged envelopes here are the only wire definition of the task-lifecycle signals; the adapter→gateway task-cleanup report is the ReportTaskCleanup RPC in schemas/lenny-adapter.proto. See spec/04_system-components.md §4.7 and spec/15_external-api-surface.md §15.4."
```

If the `ReportTaskCleanup` RPC (Section 9.7.1) is rejected in review, drop the sentence beginning "the adapter→gateway task-cleanup report"; the rest of the description and the deletions in Section 9.8 stand independently.

Replace the `task_ready` `$defs` entry's `description` (line 204), which reproduces the pre-edit §4.7 row wording that Section 9.1.3 replaces, with:

```json
"description": "Adapter → runtime, task-mode lifecycle signal. Sent at the dispatch of the next task, after scrub has completed and the new task's workspace is materialized. Carries the new task's ID. The runtime re-reads the adapter manifest (regenerated per task) and prepares for the next message."
```

Section 9.5.4 places this schema on the §15.4 artifact list under the artifact-prose reconciliation invariant (`spec/15_external-api-surface.md:1428`); without the def edit the schema would be the only surviving site with the post-scrub timing, contradicting the rewritten §15.4.1 paragraph (Section 9.5.5).

Replace the `task_complete` `$defs` entry's `description` (line 193), whose closing sentence ("The runtime does not exit.") is the surviving form of the persistent-runtime claim that Section 9.1.2 corrects, with:

```json
"description": "Adapter → runtime, task-mode lifecycle signal. The current task is finished; the runtime releases task-specific resources and replies with task_complete_acknowledged. The frame itself does not terminate the runtime process; scrub step 1 kills it along with all other task processes, and the runtime serving the next task is spawned at dispatch or re-established by the preConnect SDK re-warm."
```

Without this edit the def would be the only surviving site supporting the persistent-runtime reading once Section 9.1.2 lands, under the same artifact-prose invariant.

### 9.10 `schemas/examples/`

Delete `schemas/examples/jsonl.task_complete.json`, `schemas/examples/jsonl.task_complete_acknowledged.json`, and `schemas/examples/jsonl.task_ready.json`. The lifecycle-channel examples (`schemas/examples/lifecycle.task_complete.json` and peers) are unchanged.

### 9.11 `tests/tier0_static/schemas_test.go`

Remove the `schemas/examples/jsonl.task_complete.json`, `schemas/examples/jsonl.task_complete_acknowledged.json`, and `schemas/examples/jsonl.task_ready.json` entries from the example list in `TestAdapterJSONLExamplesValidate` (lines 189-191).

### 9.12 Docs follow-up (not staged here)

The docs sites below go stale on application and are reconciled in the standard docs-sync pass:

- `docs/runtime-author-guide/adapter-contract.md:659` describes the JSONL schema as including lifecycle frames; the Section 9.8 rescope removes them.
- `docs/runtime-author-guide/adapter-contract.md:654-660` carries the "Canonical artifacts" table, a mirror of the §15.4 artifact list on the page `publishing.md:335` points to as the full list. After Sections 9.5.2 through 9.5.4 land, the spec list gains `schemas/lifecycle-events.schema.json`, and the line-659 JSONL-row rescope leaves the lifecycle envelopes with no artifact row on the page, so the table's opening artifact count and its row set both go stale. The table gains a `lifecycle-events.schema.json` row (purpose: the JSON envelopes on the adapter↔runtime lifecycle channel; canonical URL `https://schemas.lenny.dev/adapter/v1/lifecycle-events.schema.json`, following the existing rows), and the line-654 sentence "The adapter protocol is defined by three published schema artifacts" drops the count, matching the Section 9.5.2 count removal in the spec source.
- `docs/runtime-author-guide/adapter-contract.md:543` mirrors the pre-edit §4.7 `task_ready` row ("Scrub complete, new workspace materialized"); Section 9.1.3 moves the frame to dispatch time.
- `docs/runtime-author-guide/adapter-contract.md:77-81` carries the Adapter-to-Gateway RPCs table, a mirror of the §4.7 Adapter → Gateway RPCs table whose only row is `ExtendLease`, on a page that declares itself the complete protocol reference (`adapter-contract.md:10`); the table gains a `ReportTaskCleanup` row summarizing the staged Section 9.1.1 row.
- `docs/runtime-author-guide/lifecycle.md:369-374` and `lifecycle.md:396-402` give the between-task sequence as scrub followed directly by `task_ready`, with no dispatch step, no `ReportTaskCleanup`, and no disposition; both numbered sequences gain the corrected ordering.
- `docs/reference/metrics.md` declares itself the complete reference for all emitted Prometheus metrics (`docs/reference/metrics.md:11`) and mirrors the precedent pair (`lenny_warmpool_sdk_connect_timeout_total` at `:138`, the `SDKConnectTimeout` alert row at `:511`); it gains a metric row for `lenny_warmpool_task_cleanup_timeout_total` and an alert-rules row for `TaskCleanupTimeout`.
- `docs/runbooks/index.md` declares that every alert in the metrics reference maps to a runbook (`docs/runbooks/index.md:11,28-29`); it gains a Warning-alert map row for `TaskCleanupTimeout`, and a new `docs/runbooks/task-cleanup-timeout.md` runbook accompanies it, following the dedicated `docs/runbooks/sdk-connect-timeout.md` precedent (`docs/runbooks/index.md:90`).
- `docs/reference/state-machines.md:221` lists `task_cleanup → failed` with the single trigger `onCleanupFailure: fail`; the row gains the Section 9.3.2 watchdog trigger. The same file's transition tables omit every Section 9.3.2 pod-reclamation drain edge (`claimed → draining`, `attached → draining`, `failed → draining`, and the session-terminal `completed`/`cancelled`/`expired` → `draining`); the tables gain rows mirroring the staged listing lines and their full trigger sets (the live-gateway release drain yields as well as the orphan-loop recovery).
- `docs/reference/configuration.md` declares itself the complete reference for all Helm `values.yaml` fields (`docs/reference/configuration.md:11`) and lists the sibling `pool.scalingPolicy.sdkConnectTimeoutSeconds` (`:102`); it gains a `pool.scalingPolicy.taskCleanupTimeoutSeconds` row.

The task-mode-reuse diagram and its ASCII fallback (`docs/runtime-author-guide/lifecycle.md:357-365`) show only the pod phases of the reuse path and remain valid. `docs/runtime-author-guide/publishing.md:335` needs no reconciliation: it already scopes the JSONL schema to stdin/stdout frames, and the word lifecycle does not occur in that file.

## 10. Non-goals

- Adding a `/v1/tasks` REST route or any new client-facing ingestion endpoint. Task ingestion stays on the session API.
- Adding gateway→adapter frames to the `LifecycleChannel` control stream, or any RPC that triggers the between-task loop. The loop start remains adapter-observed per `spec/15_external-api-surface.md:1889-1891` and `spec/06_warm-pod-model.md:142`.
- Re-specifying the scrub steps, the adapter↔runtime unix-socket frame exchange, or the §6.2 branch-table semantics. `pkg/adapter/scrub`, `pkg/adapter/lifecyclechannel`, and `pkg/sandbox/taskcleanup` are built and unit-tested and are the integration contract the driver consumes.
- Changing the Standard- and Basic-level task-mode path (no reuse, shutdown per task, `spec/05_runtime-registry-and-pool-model.md:420`). It is unaffected by the new surface.
- A2A or Agent Protocol task routes (`spec/21_planned-post-v1.md`) are post-v1 by definition.
- New metric names beyond the `task_cleanup` watchdog counter. §5.2 defines `lenny_task_scrub_failure_total` (kept off the §16.1 catalog by design, preserving the catalog-parity test), §16.1 catalogs `lenny_task_pod_scrub_failure_count` and `lenny_task_pod_retirement_total`, and all three carry gateway-attributed emission in §5.2; §16.1 also defines the `lenny_task_reuse_count` histogram, whose observation timing Section 9.6.3 restages from task completion to the pod's retiring `task_cleanup` resolution (`task_cleanup → draining` or `task_cleanup → failed`); the staged §4.7 `ReportTaskCleanup` row (Section 9.1.1) enumerates the emission of all of these at resolution. The watchdog adds `lenny_warmpool_task_cleanup_timeout_total` and the `TaskCleanupTimeout` alert (Section 9.6) because no existing §16.1 metric distinguishes a watchdog-fired replacement from an `onCleanupFailure: fail` retirement; the `sdk_connecting` watchdog's counter-and-alert pattern is the precedent.
- `maxTaskRetries` spec changes. The §6.2 crash-retry policy (`spec/06_warm-pod-model.md:185-192`) is complete; the driver implements it as written.
- Changes to the tier-12 load scenarios. They already submit through the `/v1/sessions` routes; only the BUILD-GAPS ledger citation is stale.

## 11. Testing

- **Static (tier 0):** the updated `TestAdapterJSONLExamplesValidate` list passes; a negative assertion confirms the JSONL schema rejects a `{"type": "task_complete"}` frame (the envelope now lives only in `lifecycle-events.schema.json`); `buf` breaking-change rules pass on `lenny-adapter.proto` (the RPC, messages, and enums are additive); the §16.1 catalog parity test gains `lenny_warmpool_task_cleanup_timeout_total`, and the §16.5 alert parity transcription (`spec165WarningAlerts` in `pkg/alerting/rules/rules_test.go:252`) gains `TaskCleanupTimeout`, without which `TestCatalogHasNoUnspecifiedAlerts` (`pkg/alerting/rules/rules_test.go:334-351`) rejects the new catalog rule.
- **Unit:** `ReportTaskCleanup` handler idempotency per `task_id` (a second report returns the disposition recorded in `Sandbox.status.taskState` with no second phase write or metric emission, including after the pod has left `task_cleanup` and when the retry lands on a different replica); a cancelled task's report resolves through the same path and advances `tasksCompleted`; the late-report path (no recorded resolution after the watchdog has failed and drained the pod) returns `retire` with no phase write; disposition mapping for `reuse`, `rewarm_sdk`, and `retire` against the §6.2 branch table via `taskcleanup.Decide`; counter advance reads and writes `Sandbox.status.taskState` rather than process memory.
- **envtest (tier 2):** the gateway SSA Apply of `status.taskState` under the `lenny-gateway` field manager preserves `phase`, `activeSlots`, and `tenantId`; each non-Apply phase yield (the session-mode `draining`, the task-mode `idle`, `sdk_connecting`, and `failed → draining`, and the concurrent-mode last-slot `slot_active → idle`) strips the gateway's Apply claim on `status.phase` only; the WarmPoolController's follow-on transitions (`draining → terminated`, `sdk_connecting → idle`, and the drain of a pooled `idle` concurrent-mode pod after the converted `ReleaseSlot` yield) land as non-Apply `Updates` against the yield's update-operation owner; every such case seeds the production managed-fields state (a forced gateway Apply claim, then the yield `Update`, then the WarmPoolController transition `Update`) rather than seeding under the WarmPoolController's own Apply manager; a negative case asserts that a non-forcing SSA Apply changing `status.phase` against the yield's update-operation owner is rejected with a conflict; the WarmPoolController's broader status Apply with `status.phase` omitted (the `status.podName`, `status.nodeName`, `status.podIP`, and `status.observedGeneration` Apply) lands against the yield's update-operation owner of `status.phase` without conflict and leaves the phase value untouched; the watchdog's `task_cleanup → failed` non-Apply update succeeds while the gateway holds the carve-out claim.
- **Integration:** the between-task loop end-to-end against a fake adapter: task completes, report delivered, the `rewarm_sdk` path re-warms through `sdk_connecting` and the WarmPoolController returns the pod to `idle`, the `reuse` path returns to `idle`, the `retire` path drains, and the `onCleanupFailure: fail` path writes `failed` and then yields `draining` so the pod is drained and replaced; the per-task `SandboxClaim` is released on every disposition, after the phase writes, and the next task claims through the standard flow; adapter report retry while the gateway is unavailable; watchdog fires on adapter death, increments `lenny_warmpool_task_cleanup_timeout_total`, writes `failed` and then `draining` in the same pass, and the pod is replaced without returning to `idle`, with the stale claim collected by the orphan loop, including the long-task ordering in which the orphan loop collects the claim before the watchdog fires.
- **Chart:** assert the gateway role grants `get`/`update`/`patch` on `sandboxes/status`, `get`/`list`/`watch`/`patch` on `sandboxes`, `get`/`list`/`watch`/`patch` on `pods`, and `get`/`list`/`watch`/`create`/`delete` on `sandboxclaims`, and the controller role grants `get`/`update`/`patch` on `sandboxes/status` (all already rendered; the assertions pin the spec-recorded grants); the orphan-GC binding gate and phase routing have WPC test coverage for a `task_cleanup` pod with a stale claim (claim deleted, phase untouched), for a re-claimed pod holding a second live `SandboxClaim` alongside the collected stale claim (claim deleted, phase untouched in every phase from `claimed` through `attached`: the binding gate), for an `sdk_connecting` pod with a stale released-task claim (claim deleted, phase untouched), for a `claimed` pod whose `status.phase` carries a foreign `lenny-gateway` Apply claim (the non-Apply `draining` `Update` lands and strips the claim), for a mid-bind pod in `receiving_uploads`, `finalizing_workspace`, `running_setup`, or `starting_session` (claim deleted, phase written to `failed` and then `draining` in two sequential writes), for an `attached` or `failed` pod (claim deleted, phase written to `draining`), for a session-terminal pod in `completed`, `cancelled`, or `expired` (claim deleted, phase written to `draining`), and for a `slot_active` pod with a stale per-slot claim and a live sibling slot (claim deleted, phase untouched, and `status.activeSlots` untouched: the pod stays `slot_active` at its reduced slot count, with no idle return, until the `maxPodUptimeSeconds` drain), with every routed write asserted legal via `pkg/sandbox/state` `IsValid`; the tier-2 observability `catalog_crosscheck` test passes against the regenerated `charts/lenny/files/alerting-rules.yaml`, which carries the `TaskCleanupTimeout` rule.
- **Mirror:** the WarmPoolController mirror writes `tasks_completed` and `scrub_failure_count` to `agent_pod_state` on the `task_cleanup` transitions.

## 12. Findings closed on application

F-5.2.30 is unblocked rather than closed. The recorded rule-B blocker ("Re-attempt once §15/§4.7 define the gateway-side task-control surface", `BUILD-GAPS.md:3715`) is satisfied by Sections 9.1.1, 9.3.3, and 9.5.1. On application, amend the finding's deliverable list: drop the `/v1/tasks` ingestion deliverable (the route is a phantom; §15.1 now states the session-endpoint submission path), and correct the stale citation of `tests/tier7_load_cloud/task_mode_test.go` to the tier-12 scenarios, which already submit through the `/v1/sessions` routes.

## 13. Resolved in adversarial review

Adversarial review rounds populate this section. The drafting-stage challenge revisions are already folded into the staged text above: the disposition-enum alignment in the §5.2 lifecycle sentence, the retention of the two normative §5.2 clauses, the Apply-path correction for `failed` in the gateway status window, the completion of the carve-out field set, the `update` verb in the gateway RBAC grant, the "never force-conflict" scoping, the late-report and orphan-GC rules around the watchdog, the single-normative-site slimming of the §6.2 driver paragraph, the §15.1 pod-reuse hedge and the Agent Protocol hedge, and the determinate dependents of the JSONL envelope removal (examples, tier-0 test, proto comment, and §15.4 artifact bullets; Section 8).

### Round 1 (2026-06-09)

One entry per confirmed finding; each states the defect and the revision.

1. **Non-retiring dispositions had no `SandboxClaim` release or ownership-return path (High).** The staged driver released the claim only on retiring dispositions, the status window defined the non-Apply yield only for `draining`, and no text named the writer of `sdk_connecting → idle` after the between-task re-warm. Sections 9.1.4, 9.1.5, 9.1.7, and 9.3.3 now release the claim on every disposition, route `task_cleanup → idle` and `task_cleanup → sdk_connecting` through the same phase-only non-Apply yield as `draining`, and name the WarmPoolController as the `sdk_connecting → idle` writer.
2. **Cancelled tasks fell outside the `ReportTaskCleanup` trigger (Medium).** §6.2 routes `cancelled → task_cleanup` through the normal cleanup rules, but the staged trigger covered only completed tasks. Sections 3, 9.1.1, 9.1.5, 9.2.1, 9.3.3, and the proto comments (9.7.1 and 9.7.2) now cover every `task_cleanup` entry, and the staged text states that `tasksCompleted` advances on cancelled tasks.
3. **The orphan-GC phase gate made stale `SandboxClaim`s for non-`claimed` pods uncollectable (Medium).** Gating the whole delete-and-return-to-idle path on phase `claimed` disabled claim deletion for `failed` and terminal phases, which `pkg/gateway/podsession/binder.go:750-755` depends on, and leaked the claim permanently on the watchdog's no-report path. Section 9.1.8 now keeps claim deletion phase-independent and gates only the return-to-idle half; Section 9.3.3 names the orphan loop as the claim collector on the no-report path.
4. **The proposal claimed `publishing.md:335` describes the JSONL schema as including lifecycle frames (Low).** The line already scopes the schema to stdin/stdout frames, and the word lifecycle does not occur in the file. Sections 8 and 9.12 and the Files-touched table now drop `publishing.md` and state why it needs no reconciliation.
5. **The decision bullet paraphrased the `GatewayControl` doc comment without its §8 scoping (Low).** The comment scopes the service to §8 control operations, and `ReportTaskCleanup` is a §5.2/§6.2 operation. The Section 2 bullet now quotes the comment verbatim, and Section 9.7.1 stages a one-line widening of the service comment.
6. **The staged RBAC sentence recorded a `Sandbox` main-resource grant narrower than the shipped chart (Low).** The sentence recorded `get`/`list` while the chart grants `get`/`list`/`watch`/`patch`. Section 9.1.7 now records `watch` for claim-path reads and `patch` for the §5.2 tenant-pinning label Apply.
7. **The staged §15.1 paragraph said pod reuse surfaces only through `podReuse` and `scrubPolicy` (Low).** §7.1 also defines `residualStateWarning`, a REST surfacing of between-task reuse. Sections 7 and 9.5.1 now enumerate all three `sessionIsolationLevel` fields.
8. **The watchdog paragraph said the non-Apply `failed` write strips the gateway's claim on all carve-out fields (Low).** A non-Apply write removes other managers' claims only on the fields it writes. Sections 6 and 9.3.3 now scope the strip to `status.phase`.
9. **Three citations were off by one or internally inconsistent (Low).** `spec/08_recursive-delegation.md:813` is corrected to `:814` for `TaskRecord.sessionId`, the chart `sandboxes/status` citation `129-131` is corrected to `130-132` in both occurrences, and the Section 8 JSONL `$defs` range `228-263` is corrected to `228-262`, matching Section 9.8.
10. **The staged RBAC sentence omitted the `patch` verb the built slot-claim path requires (Medium; same surface as entry 6).** `pkg/gateway/podclaim/slotclaimer.go:589-600` SSA-applies the tenant-pinning label on the `Sandbox` main resource and the chart grants the verb. The Section 9.1.7 sentence and the Section 5 bullet now record the verb and its purpose.
11. **The §4.6.1 phase gate changed the shipped `ClaimGarbageCollector` without flagging the code delta (Medium).** Sections 6 and 9.1.8 now name `pkg/controller/warmpool/gc.go` (`ClaimGarbageCollector.returnToIdle`) as the shipped unconditional implementation and flag the `claimed`-only gate as a code change; the Files-touched table gains the row.
12. **Duplicate of entry 4 (Low).** Independently reported; resolved by the same edits.
13. **Duplicate of entry 7 (Low).** Independently reported; resolved by the same edits.
14. **Duplicate of entry 9, chart and `TaskRecord` items (Low).** Independently reported; resolved by the same corrections.
15. **The watchdog's non-Apply `failed` write contradicted the unedited WPC RBAC paragraph (Medium).** `spec/04_system-components.md:586` grants the WPC only `get`/`patch` on the `Sandbox` status subresource, while the shipped chart already grants `update`. New Section 9.1.9 stages the `update` clause with the watchdog rationale, and the Section 11 chart assertion now covers the controller role.
16. **No mechanism deleted the per-task `SandboxClaim` on the watchdog path, and the GC gate was broader than its target (Medium; same surface as entry 3).** Resolved by the Section 9.1.8 split and the Section 9.3.3 sentence naming the orphan loop as the no-report claim collector.
17. **The idempotent-retry promise had no recall mechanism on the success path (Medium).** A retried report after a successful resolution can arrive on another replica with the phase no longer `task_cleanup`, and nothing recorded the resolved disposition. Sections 3, 5, 9.1.1, 9.1.5, and 9.3.3 now persist `lastTaskId`, `lastDisposition`, and `lastScrubWarning` in `Sandbox.status.taskState` in the final counter Apply and define the retry response for every post-resolution phase.
18. **The §6.1 edit instruction would have deleted the `sdkWarmBlockingPaths` sentences it claimed to retain (Medium).** The literal replacement span engulfed the per-task demotion text. Section 9.3.1 now uses two spans: replace the cell opening through "...along with all other task processes." and separately delete the standalone "The SDK is re-warmed after the next scrub." sentence.
19. **The watchdog had no observable signal, and the cited accounting path named no existing metric (Low).** New Section 9.6 stages `lenny_warmpool_task_cleanup_timeout_total` and the `TaskCleanupTimeout` warning alert, mirroring the `sdk_connecting` watchdog precedent; Sections 6, 9.3.3, 10, and 11 are updated accordingly.
20. **The docs follow-up missed runtime-author-guide sites carrying the corrected between-task ordering (Low).** Section 9.12 and the Files-touched table now list `lifecycle.md:369-374`, `lifecycle.md:396-402`, and `adapter-contract.md:543` alongside the JSONL rescope, and note that the task-mode-reuse diagram remains valid.
21. **C-change labels were used without a defining enumeration (Low).** All C-labels are replaced with direct section references.
22. **The `Sandbox.status.taskState` CRD and Go-type dependents were unflagged (Low).** Section 5 now carries an accompanying-implementation note parallel to the `taskCleanupTimeoutSeconds` note, and the Files-touched table lists `pkg/apis/lenny/v1alpha1/sandbox_types.go` and `charts/lenny/crds/lenny.dev_sandboxes.yaml`.
23. **The staged §15.4 bullet attributed every lifecycle envelope to §4.7 over a schema containing `files_updated` (Low).** Section 9.5.4 now attributes the channel rather than the envelope set to §4.7 and flags the missing `files_updated` row as a separate reconciliation item.
24. **The Scope bullet did not name the blocked finding (Low).** The Scope bullet now ends with "Unblocks F-5.2.30."
25. **A Section 6 bullet opened with a sentence fragment (Low).** "Declared the single normative site for resolution semantics." is now a complete sentence.
26. **The staged watchdog name omitted backticks on the state name (Low).** All staged and prose occurrences now read `` `task_cleanup` watchdog ``, matching the `sdk_connecting` watchdog convention at `spec/06_warm-pod-model.md:69`.

### Round 2 (2026-06-09)

One entry per confirmed finding; each states the defect and the revision.

1. **The "never force-conflict" scoping named only the draining yield as the gateway window's return path (Medium).** Round-1 entry 1 corrected Sections 9.1.4, 9.1.5, 9.1.7, and 9.3.3 to the plural non-Apply phase yields but missed the Section 9.1.6 staged sentence and the Section 5 scoping bullet, which still named the `draining` yield as the sole return path, leaving the `idle` and `sdk_connecting` yields outside the sanctioned scoping under a strict reading of §4.6.3. Both sites now enumerate the session-mode `draining` hand-back and the task-mode disposition writes (`task_cleanup → idle`, `task_cleanup → sdk_connecting`, and the retiring `task_cleanup → draining`).
2. **The §4.6.1 return-to-idle path had no write mechanism that composes with the gateway's field-manager claim (Medium).** In the gateway-crashed-before-persist scenario no yield runs, so the dead gateway's SSA Apply claim on `status.phase` persists, and the shipped `returnToIdle` non-forcing Apply under `retryOnConflictSSA` would conflict on every attempt and give up. Section 9.1.8 now specifies the return-to-idle write as a non-Apply status `Update` (the watchdog's phase-only yield-stripping mechanism, covered by the Section 9.1.9 `update` verb), the application note records the second code change, and the Files-touched `gc.go` row gains the Apply-to-`Update` conversion.
3. **All BUILD-GAPS.md line citations were stale by six lines (Low).** Commit 022ab3c8 (the proposal-0003 staging commit) shifted the ledger after the citations were taken. The citations are refreshed: `:3683` to `:3689`, `:3689` to `:3695`, and `:3709` to `:3715` in both occurrences.
4. **The single-normative write-and-metric enumeration omitted `lenny_task_reuse_count` (Low).** §16.1 defines the histogram as observed per-pod at task completion, which is the `ReportTaskCleanup` resolution point, but the staged §4.7 row enumerated only the §5.2 scrub and retirement metrics, leaving the histogram's emission site unattributed, and the Non-goals inventory omitted it. The Section 9.1.1 row now includes the per-pod observation from the advanced `tasksCompleted` counter, and the Non-goals bullet names the histogram.
5. **The Section 6 orphan-GC justification misdescribed the built SSA behavior (Medium; same surface as entry 2).** The bullet said an ungated GC "would flip a mid-scrub pod to idle, racing the watchdog with a conflicting outcome", but against a gateway-held `status.phase` claim the shipped non-forcing Apply conflicts deterministically and fails after five attempts. The bullet now describes the stuck-conflict behavior, the crash-scenario composition failure, and the staged non-Apply `Update`; resolved together with entry 2.
6. **Duplicate of entry 3 (Low).** Independently reported; resolved by the same renumbering.
7. **The Files-touched table omitted flagged implementation dependents (Low).** Rows are added for `pkg/apis/lenny/v1alpha1/sandboxwarmpool_types.go` plus the regenerated `charts/lenny/crds/lenny.dev_sandboxwarmpools.yaml` (`taskCleanupTimeoutSeconds`), for a new numbered `migrations/` pair carrying the `agent_pod_state` columns, and for `pkg/observability/metrics/catalog.go`, `pkg/observability/metrics/catalog_test.go`, and `pkg/alerting/rules/rules.go` carrying the §16.1 counter entry, the parity-test transcription, and the §16.5 alert rule.
8. **The lifecycle-events `task_ready` `$defs` description kept the pre-edit post-scrub timing (Low).** Section 9.9 staged only the top-level description while Section 9.5.4 placed the schema under the §15.4 artifact-prose invariant, so application would have created a new artifact-prose discrepancy. Section 9.9 now restages the def description with dispatch-time wording, and the Files-touched row covers the def edit.
9. **Duplicate of entry 3 (Low).** Independently reported; resolved by the same renumbering.
10. **The staged gateway RBAC sentence re-recorded the Pods and SandboxClaim grants narrower than the shipped chart (Low).** The replaced sentence carried `get`/`patch` on Pods and `create`/`get`/`delete` on `SandboxClaim` while the chart grants `get`/`list`/`watch`/`patch` and `get`/`list`/`watch`/`create`/`delete`, the same defect class round-1 entries 6 and 10 fixed on the `Sandbox` clauses. Section 9.1.7 now records the shipped verb sets (naming claim-path pod-IP resolution for the adapter dial), the Section 5 bullet states the reconciliation, and the Section 11 chart assertions pin the Pods and SandboxClaim grants.
11. **The driver paragraph's input enumeration omitted pod uptime (Low).** The §6.2 branch table conditions on `maxPodUptimeSeconds` and the shipped driver holds `BootTime` in gateway memory beside the externalized counters, but the single normative input list named neither uptime nor its post-externalization source. Section 9.3.3 now lists the uptime input computed at resolution time from the pod's start time on the API object, and Section 5 states why it needs no externalization.
12. **The Status bullet used a state outside the prescribed lifecycle (Low).** "Revised after 1 adversarial review round" replaced the machinery-written "Draft for review." state mid-loop, obscuring whether the review converged. The bullet now leads with "Draft for review." and carries the revision note as a trailing clause, matching the proposal-0003 precedent of a leading lifecycle state with trailing history.
13. **Headings 3 and 4 left `ReportTaskCleanup` and `task_ready` unbackticked (Low).** Both headings now backtick the identifiers, matching heading 6 and the body prose.
14. **The Section 6 intro announced two paragraphs and one listing line over a three-bullet list whose third item is a §4.6.1 edit (Low).** The intro is reworded without the count and now covers the §4.6.1 clause.
15. **Section 9.12 counted "three runtime-author-guide sites" over four listed locations (Low).** The count is dropped per the doc-style rule on explicit counts.
16. **A Non-goals bullet ended with the fragment "Post-v1 by definition." (Low).** The justification is folded into a complete sentence.

### Round 3 (2026-06-09)

One entry per confirmed finding; each states the defect and the revision.

1. **The staged driver paragraph cited the §4.6.3 `released` claim phase for dispositions outside its unedited enumeration (Medium).** The `released` bullet at `spec/04_system-components.md:593` restricts the phase to a referenced pod in `idle` or `draining`/`terminated`, while the `rewarm_sdk` disposition releases the claim toward `sdk_connecting` and the `onCleanupFailure: fail` and late-report paths release a claim referencing a `failed` pod. New Section 9.1.10 stages the task-mode extension of the bullet, and the Files-touched spec/04 row covers it.
2. **The Non-goals bullet attributed `lenny_task_scrub_failure_total` to §16.1 (Low).** The aggregate counter is defined only in §5.2 and is kept off the §16.1 catalog to preserve the catalog-parity test. The bullet now carries per-section attribution and records the off-catalog status.
3. **The staged status window contradicted the shipped concurrent-mode last-slot release (Medium).** `ReleaseSlot` writes `slot_active → idle` via SSA Apply with `ForceOwnership` (`pkg/gateway/podclaim/slotclaimer.go:681-694`), leaving a live gateway claim on `status.phase` on a pooled `idle` concurrent-mode pod, against the staged "sole phase writer on pooled pods" conclusion, and the WarmPoolController's non-forcing drain Apply on such a pod would conflict deterministically and be abandoned. Sections 5, 9.1.5, 9.1.6, and 11 now route the last-slot release through the same non-Apply phase yield as the other pool returns, and the `ReleaseSlot` conversion is flagged as a code change in the Files-touched table.
4. **The staged window enumeration omitted the shipped `suspended` Apply write (Low).** The binder Applies `suspended` under the gateway field manager (`pkg/gateway/podsession/binder.go:1537`) and the pod stays bound during suspension, but the staged coverage read "claimed through attached" plus the terminal dispositions. Section 9.1.5 now includes the `suspended` hold.
5. **The Files-touched table omitted `pkg/admission/ownership`, the code mirror of the rewritten §4.6.3 table (Low).** The package doc and matrix record `Sandbox.status.*` as WPC-owned with no gateway entry, and its test asserts the gateway `status.phase` write is a violation. The table now carries the row for the carve-out entries and the updated package doc.
6. **The widened `GatewayControl` comment still enumerated three operation classes over a six-RPC service (Low).** The staged comment omitted `CallPlatformTool` and the three §9.3 connector RPCs, reproducing the defect class of round-1 entry 5. Section 9.7.1 now names the §9.1 platform-tool and §9.3 connector-tool surfaces, and the Section 2 bullet names the RPC groups that strain the §8 scoping.
7. **The problem statement classified the mixed-wording `task_ready` sites inconsistently with the cited texts (Low).** `spec/04:702` and the `lifecycle-events` def open with the post-scrub clause, the dispatch-time marker "with the new task's ID" sits at `spec/15:1461`, and Sections 1 and 2 cited the `lifecycle-events` def as dispatch-time evidence while Section 9.9 treated the same def as pre-edit post-scrub wording. Sections 1 and 2 now state that every site mixes the post-scrub timing clause with new-task content and attribute each marker to its actual site.
8. **The carve-out field citation range excluded the `Phase` declaration it was cited for (Low).** `sandbox_types.go:102-114` starts at the `ActiveSlots` comment; the range is corrected to `:95-114`, covering the three field declarations with their comments.
9. **The staged `failed_step` examples named values outside the built scrub vocabulary (Low).** "step_2" and "verification" do not occur in the `pkg/adapter/scrub` `StepName` constants (step 2 is `remove_workspace`; the verification step is `verify`). The Section 9.7.2 comment now defines the wire vocabulary as the `StepName` strings, with `remove_workspace` and `verify` as the examples.
10. **No staged text specified the trigger or writer for entering `task_cleanup` (Medium).** The driver precondition, the retry recall, and the watchdog all depend on the entry write, but no edit stated the gateway-observable signal that causes it; under a report-arrival reading a pod whose adapter died mid-scrub would sit in `attached` forever and the watchdog would never fire. Section 9.3.3 now states the entry write (`attached → task_cleanup` on the terminal outcome observed by the session-result path, `cancelled → task_cleanup` on the cancellation acknowledgement, both on the Apply path), Section 9.1.5 cross-references it, Section 6 records it in the driver bullet, and the watchdog timer is anchored to the entry write.
11. **The Files-touched table omitted the regenerated alert-rule artifacts (Low).** Adding `TaskCleanupTimeout` to `pkg/alerting/rules/rules.go` without regenerating `charts/lenny/files/alerting-rules.yaml` and `docs/alerting/rules.yaml` leaves the shipped chart's PrometheusRule and the deployer-facing export missing the alert and the tier-2 `catalog_crosscheck` test red. The table now carries the regenerated pair, and the Section 11 chart bullet covers the tier-2 parity.
12. **The `lifecycle-events` `task_complete` def kept the persistent-runtime wording (Low).** The def's closing "The runtime does not exit." is the surviving form of the §4.7 claim Section 9.1.2 corrects, on a schema Section 9.5.4 places under the §15.4 artifact-prose invariant. Section 9.9 now restages the def description with the corrected per-task process model, and the Files-touched row covers it.
13. **Open decision 1 closed with a verbless fragment (Low).** "Sign-off or redirection requested." is now a complete sentence.
14. **"drives `draining → terminated`" used a vague causal verb, including in staged spec text (Low).** The occurrences in the Section 5 bullet, the Section 9.1.5 staged paragraph, and the Section 11 envtest bullet now read "writes", matching the parallel transitions in the same sentences.
15. **The H1 title left `ReportTaskCleanup` unbackticked (Low).** The title now backticks the identifier, completing the round-2 entry-13 fix; the filename slug is the canonical workflow output and is unchanged.

### Pass 4 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The orphan-GC `claimed`-only gate mischaracterized the crash window and stranded mid-bind pods (High).** The create-and-start path runs the pod claim and the full bind before the session row is persisted (`pkg/gateway/sessionserver/start.go:478-523`; `pkg/gateway/podsession/binder.go:604-708`), so the gateway-crashed-before-persist window spans `claimed` through `attached` plus a landed `failed` write, and the shipped `returnToIdle` (`pkg/controller/warmpool/gc.go:215`) was the only mechanism recovering those pods. Sections 6, 9.1.8, 9.3.3, and 11 now route the recovery by phase (`claimed → idle`; `receiving_uploads` through `attached` and `failed` → `draining` for replacement; `task_cleanup` excluded), correct the window characterization, and the Files-touched `gc.go` row records the phase-routed rewrite.
2. **The docs reference mirrors for the new alert, metric, watchdog trigger, and pool field were absent from the edit lists (Medium).** `docs/reference/metrics.md:11`, `docs/runbooks/index.md:11,28-29`, `docs/reference/state-machines.md:221`, and `docs/reference/configuration.md:11` carry completeness contracts or mirror the changed inventories, and none is generated. Section 9.12 and the Files-touched table now list the metric and alert rows, a `TaskCleanupTimeout` runbook map row plus a new `docs/runbooks/task-cleanup-timeout.md` following the `sdk-connect-timeout` precedent, the watchdog trigger on the state-machine table, and the `taskCleanupTimeoutSeconds` configuration row.
3. **The non-Apply phase yield did not return `status.phase` ownership to the WarmPoolController (High).** Per SSA semantics a non-Apply `Update` transfers ownership of the changed field to the updater's own update-operation manager, so the WarmPoolController's next non-forcing Apply (`draining → terminated`, `sdk_connecting → idle`, the post-yield drain) would conflict deterministically and be abandoned by `retryOnConflictSSA` (`pkg/controller/warmpool/controller_test.go:44-47` concedes the cross-strategy conflict). Sections 5, 9.1.4, 9.1.5, 9.1.6, 9.1.8, 9.3.3, and 11 now specify the WarmPoolController's own `status.phase` transitions as the same non-Apply status `Update` under the Section 9.1.9 `update` verb (an `Update` is never rejected with a managed-fields conflict, lands against any owner, and takes ownership in turn, and the gateway's next claim re-takes the field with its forced Apply); this extends the mechanism the proposal already staged for the watchdog and orphan-GC writes instead of adding a second yield protocol or a force-takeover the §4.6.3 prohibition bars. The envtest bullet now seeds the production managed-fields state (gateway Apply claim, yield `Update`, WarmPoolController transition `Update`) and asserts the Apply-conflict negative case, and the conversion of the shipped Apply-based transitions (`pkg/controller/sandbox/controller.go:610-622`; `pkg/controller/warmpool/pod_reconciler.go:447-476`) is flagged in the Files-touched table.
4. **The WarmPoolController could not observe between-task re-warm completion with the named mechanism (High).** The initial-warm signal is one-shot: the `lenny.dev/sandbox-ready` gate never resets (`pkg/controller/sandbox/controller.go:523-556`), `DecideSDKWarm` flips `sdk_connecting → idle` on Pod readiness alone (`pkg/controller/sandbox/lifecycle/lifecycle.go:177-186`), and the watchdog clock anchors at `pod.Status.StartTime` (`pkg/controller/sandbox/sdkwarm.go:110-125`), which would retire any re-warming pod on its first reconcile. Sections 5, 9.1.5, 9.2.1, and 9.3.1 now specify the re-warm completion signal (the adapter surfaces SDK-warm state through its container readiness probe, so the scrub-step-1 kill drops Pod readiness and the reconnect restores it) and the re-warm watchdog clock anchored at the observed `sdk_connecting` entry, and the Files-touched table flags the accompanying code changes.
5. **The staged §4.6.3 ownership-table row enumerated only two of the three yields (Low).** The 9.1.4 row parenthetical omitted the concurrent-mode last-slot `slot_active → idle` release that Sections 9.1.5 and 9.1.6 enumerate, the defect class round-2 entry 1 ruled on. The row now carries the full yield set.
6. **`tasksCompleted` advancing on cancelled tasks contradicted the unedited §5.2 completed-task definitions and the staged §12.4.1 comment (Medium).** `spec/05_runtime-registry-and-pool-model.md:410,451` define the `maxTasksPerPod` counter over completed tasks, and the staged column comment repeated the completed-task wording. New Sections 9.2.4 and 9.2.5 redefine the retirement counter as advancing for every task that enters `task_cleanup`, the Section 9.4.1 comment now matches, and the advance-on-cancellation semantics from round-1 entry 2 are retained because the counter measures pod wear.
7. **`pkg/alerting/rules/rules_test.go` was a missed dependent of the `TaskCleanupTimeout` rule (Medium).** `TestCatalogHasNoUnspecifiedAlerts` rejects any catalog rule absent from the §16.5 transcription (`pkg/alerting/rules/rules_test.go:334-351`), so the staged `rules.go` addition alone leaves a red test, the defect class round-2 entry 7 fixed on the metrics side. The Files-touched alerting row and the Section 11 static bullet now cover the `spec165WarningAlerts` transcription.

### Pass 5 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The §5.2 completed-task `maxTasksPerPod` definition survived in the shipped SandboxTemplate CRD artifacts (Medium).** Sections 9.2.4 and 9.2.5 redefine the retirement counter over every `task_cleanup` entry, but the completed-task wording is mirrored in the `MaxTasksPerPod` field comment (`pkg/apis/lenny/v1alpha1/sandboxtemplate_types.go:69`) and in the CRD schemas generated from it (`charts/lenny/crds/lenny.dev_sandboxtemplates.yaml:282`; `pkg/embedded/crds/lenny.dev_sandboxtemplates.yaml:268`), none of which appeared in an edit list, the defect class of round-2 entry 7. Section 9.2.5 now flags the mirror, and the Files-touched table carries the type and CRD row alongside the existing Sandbox and SandboxWarmPool pairs.
2. **The orphan-GC phase routing omitted the release-window terminal phases and `slot_active`, stranding crash-parked pods with no recovery writer (High).** The GC treats a terminal session as inactive (`pkg/controller/warmpool/gc.go:61-67`; `cmd/lenny-controller/main.go:649-653`) and the release path writes the terminal phase before the drain yield (`pkg/gateway/podsession/binder.go:1471-1492`), so a gateway crash in that window leaves a session-less claim referencing a pod in `completed`, `cancelled`, or `expired`, phases the staged routing assigned to no group and no other mechanism recovers (`pkg/controller/sandbox/lifecycle/lifecycle.go:103-107`; `spec/10_gateway-internals.md:51`); the staged crash-window characterization was also falsified by the clause's own deletion-half sentence acknowledging terminal-phase stale claims. Sections 6, 9.1.8, and 11, the Section 9.1.8 application note, and the Files-touched `gc.go` row now describe both crash windows, route the session-terminal phases to `draining` over the existing reclamation edges (`pkg/sandbox/state/state.go:258-261`), add `slot_active` to the no-write set (sibling slots may still be live, so the replaced unconditional flip would have zeroed a multi-slot pod), and record that a task-mode pod stranded in `cancelled` before the `task_cleanup` entry write recovers through the same terminal route, since the watchdog timer starts only at that write.
3. **The staged gateway-status-window enumeration omitted the shipped `expired` terminal Apply write (Low).** An expired session maps to the `expired` phase through the same release path as the other terminal dispositions (`pkg/gateway/sessionserver/usage.go:53-54`; `pkg/gateway/executor/pod.go:284,299-300`; `pkg/gateway/podsession/binder.go:1471-1492`), but the Section 9.1.5 staged paragraph and the Section 5 bullet enumerated only `completed`, `failed`, and `cancelled`, the defect class of round-3 entry 4. Both sites now include `expired`.

### Pass 6 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The §4.6.1 edit retained the unconditional return-to-idle sentence the inserted phase routing contradicts (Medium).** Section 9.1.8 staged the phase-routed recovery as a pure insertion after the sentence "If no active session exists, the claim is deleted and the underlying `Sandbox` pod is transitioned back to `idle`, returning it to the warm pool." (`spec/04_system-components.md:479`), so the applied paragraph would assert the unconditional idle return in one sentence and the contradicting phase routing in the next, and the staged §6.2 watchdog claim that the orphan loop's pod-recovery half excludes `task_cleanup` would be falsified by the surviving sentence. Section 9.1.8 is now a replace-then-insert (the sentence becomes a phase-routed lead-in), the Section 6 bullet and the Files-touched spec/04 row record the replacement, and the application note verifies the retained follow-on crash-scenario sentence against the routed recovery.
2. **The unedited §16.1 `lenny_task_reuse_count` row kept completion-only observation semantics (Medium).** The row at `spec/16_observability.md:124` reads "Observed per-pod at task completion" while the staged §4.7 `ReportTaskCleanup` row emits the observation at every resolution, including the cancellation path, the predicate-drift class of pass-4 entry 6 and pass-5 entry 1. New Section 9.6.3 restages the row's timing clause to the `task_cleanup` resolution semantics, the spec/16 Files-touched row covers it, and the Non-goals bullet's description of the §16.1 definition now matches.
3. **The docs Adapter-to-Gateway RPCs table was a missed mirror of the §4.7 table gaining the `ReportTaskCleanup` row (Low).** `docs/runtime-author-guide/adapter-contract.md:77-81` transcribes the §4.7 Adapter → Gateway RPCs table (sole row `ExtendLease`) on a page declaring itself the complete protocol reference (`adapter-contract.md:10`), the mirror class of pass-4 entry 2. Section 9.12 and the Files-touched docs row now list the `ReportTaskCleanup` row addition.
4. **The Files-touched table omitted the `pkg/embedded/crds` copies of the regenerated Sandbox and SandboxWarmPool CRD manifests (Low).** `pkg/embedded/crds/crds.go:3-7` records the embedded manifests as manual copies of `charts/lenny/crds/`, `make generate` regenerates only the chart copies, and the SandboxTemplate row already lists its embedded copy per pass-5 entry 1, leaving the two rows internally inconsistent. Both rows now carry `pkg/embedded/crds/lenny.dev_sandboxes.yaml` and `pkg/embedded/crds/lenny.dev_sandboxwarmpools.yaml`.

### Pass 7 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The orphan-GC recovery routing prescribed state-machine edges that neither the §6.2 listing nor `pkg/sandbox/state` defines (High).** The staged §4.6.1 clause routed a `claimed` pod to `idle` and the mid-bind phases `receiving_uploads`, `finalizing_workspace`, `running_setup`, and `starting_session` to `draining`, but `ValidTransitions` contains no `{Claimed, Idle}` or mid-bind `→ Draining` edge (`pkg/sandbox/state/state.go:164-181,242-244`), `IsValid` rejects any absent edge (`pkg/sandbox/state/state.go:266-275`, enforced by the binder at `pkg/gateway/podsession/binder.go:744-748`), and the existing `{Claimed, Draining}` edge's comment documents the claim-crash recovery as draining rather than returning to `idle` (`pkg/sandbox/state/state.go:235-243`). Sections 6, 9.1.8, 11, and the Files-touched `gc.go` row now reroute the recovery onto existing edges instead of staging new ones, the option that adds no state-machine surface and matches the documented abort-edge semantics: `claimed → draining` through the claim-time abort edge, the mid-bind phases through their pre-attached failure edges (`→ failed`) and then the `failed → draining` reclamation edge in two sequential writes, and `attached`, `failed`, and the session-terminal phases to `draining` directly (`pkg/sandbox/state/state.go:244,258-261`); the chart-test bullet asserts every routed write legal via `state.IsValid`.

### Pass 8 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The docs Canonical artifacts table was a missed mirror of the §15.4 artifact list gaining the lifecycle-events schema (Low).** `docs/runtime-author-guide/adapter-contract.md:654-660` opens "The adapter protocol is defined by three published schema artifacts" and lists the same three artifacts as the pre-edit §15.4 list, on the page `publishing.md:335` points to as the full list, the mirror class of pass-4 entry 2 and pass-6 entry 3. After Sections 9.5.2 through 9.5.4 land, the spec list carries `schemas/lifecycle-events.schema.json` and the Section 9.8 rescope removes lifecycle frames from the JSONL row, leaving the table's count wrong and the lifecycle envelopes without an artifact row. Section 9.12 and the Files-touched docs row now list the `lifecycle-events.schema.json` table row and the line-654 count removal.
2. **The per-resolution `lenny_task_reuse_count` observation contradicted the unedited §5.2 p50 `mode_factor` derivation and the shipped retirement-time emitter (Medium).** Observing the cumulative `tasksCompleted` counter at every `task_cleanup` resolution feeds the samples 1 through N from an N-task pod into the histogram, so the pool p50 converges toward N/2 rather than toward `maxTasksPerPod` as `spec/05_runtime-registry-and-pool-model.md:549,571` and the surviving `histogram_quantile(0.50, ...)` clause of the §16.1 row require, and the shipped `ObserveTaskReuseCount` (`pkg/gateway/gatewaymetrics/gatewaymetrics.go:3047-3056`) and PoolScalingController reader (`pkg/controller/poolscaling/controller.go:255-264`) encode retirement-time semantics, the predicate-drift class of pass-4 entry 6 and pass-6 entry 2. Sections 9.1.1, 9.6.3, the Non-goals bullet, and the Files-touched spec/16 row now scope the observation to the dispositions that retire the pod (`task_cleanup → draining` or `task_cleanup → failed`), observing the final `tasksCompleted` value once per pod with cancelled tasks counted, which keeps the cancelled-task accounting fix while leaving the §5.2 derivation, the docs scaling mirror, and the shipped emitter and reader unchanged.

### Pass 9 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **Claim-less `failed` pods had no reclamation writer, leaving `failed → draining` unreachable on both the fail-disposition and the watchdog paths (High).** The staged design assigned `failed → draining` exclusively to the claim-deletion-coupled §4.6.1 recovery, but the fail disposition deletes the per-task claim at resolution, and on the watchdog path the orphan loop collects the session-less claim while the pod is still in `task_cleanup` (a no-write phase) for any task longer than `claimOrphanTimeout` minus `taskCleanupTimeoutSeconds`, so in both orderings no writer ever drained the `failed` pod and it leaked indefinitely, the stranding class of pass-5 entry 2 and pass-7 entry 1; no other writer exists (`pkg/controller/sandbox/lifecycle/lifecycle.go:103-107`; the `patchSandboxDraining` callers, `pkg/controller/warmpool/pod_reconciler.go:337,399` and `pkg/controller/warmpool/controller.go:664`, are never keyed on `failed`). Sections 5, 6, 9.1.4, 9.1.5, 9.1.6, 9.1.8, 9.1.9, 9.1.10, 9.3.3, and 11 now have the writer of `failed` complete the reclamation itself: the gateway follows the fail disposition's `task_cleanup → failed` Apply with a `failed → draining` non-Apply yield in the same pass (mirroring the session-mode release sequence, `pkg/gateway/podsession/binder.go:1471-1492`), the watchdog writes `failed` and then `draining` as two sequential non-Apply updates over existing edges (`pkg/sandbox/state/state.go:224,259`; the §4.6.1 mid-bind two-write pattern), the claim release is ordered after the resolution's phase writes so a crash mid-resolution leaves either a watchdog-bounded `task_cleanup` pod or a stale claim whose collection routes the pod, and the §4.6.1 routing covers a pod stranded in `failed` by a crash before the yield; the yield enumerations, the `released` bullet, and the test bullets carry the same predicate, and the alternative WarmPoolController-reclaims-`failed` sentence is replaced with the adopted rationale. This option was chosen over a WarmPoolController sweep for claim-less `failed` pods because it extends the existing yield and two-write patterns instead of adding a new reconcile predicate.
2. **The orphan-GC pod-recovery routing would drain a healthy reused task-mode pod and left `sdk_connecting` unrouted (High).** Task mode breaks the one-claim-per-pod-lifetime assumption: the pod returns to the pool and is re-claimed (`pkg/sandbox/state/state.go:164`) while a prior task's claim can survive a gateway crash between the disposition's phase writes and the claim release, the GC sweep filters on claim age and `SessionActive` only (`pkg/controller/warmpool/gc.go:154-171`) and recovers `claim.Spec.SandboxRef` unconditionally (`gc.go:189-199`), so a collection landing after the re-claim would write a live pod in `claimed` through `attached` to `draining`, and a pod in `sdk_connecting` (reachable when the crash falls between the `rewarm_sdk` yield and the claim deletion) appeared in neither the routed nor the no-write enumeration. Sections 6, 9.1.8, and 11 and the Files-touched `gc.go` row now gate the pod-recovery half on the collected claim being the pod's current binding (skipped when `Sandbox.status.sessionId`, written at claim time and cleared at release per `pkg/podregistry/crd.go:177,224`, is non-empty and differs from `claim.Spec.SessionID`, or when another live `SandboxClaim` references the pod), and add `sdk_connecting` to the no-write set (the stale claim belongs to a released task, the re-warm is bounded by the re-anchored `sdkConnectTimeoutSeconds` watchdog, and the WarmPoolController returns the pod to `idle` on completion); the chart-test bullet gains the binding-gate and `sdk_connecting` cases.

### Pass 10 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The orphan-GC binding gate keyed on a `Sandbox.status.sessionId` write that no production code performs, no edit staged, and the staged §4.6.3 carve-out forbids (Medium).** The only `Status.SessionID` writers, `pkg/podregistry/crd.go:177,224`, belong to a package no production binary constructs (its only importers are test files), the production claim path patches only `phase`, `activeSlots`, and `tenantId` (`pkg/gateway/podclaim/gatewaystatus.go:47-56`; `pkg/gateway/podclaim/claimer.go:103-117`) and no release path clears the field, so the gate's first prong could never fire, the staged §4.6.1 "written at claim time and cleared at release" sentence was false of the implementation, the Section 11 test case was unconstructible by production writes, and the staged §4.6.3 carve-out (`status.phase`, `status.activeSlots`, `status.tenantId`, and `status.taskState.*`) sanctioned no gateway writer for the maintenance the prong required. The prong is dropped rather than implemented: Sections 6, 9.1.8, 11, and the Files-touched `gc.go` row now gate the recovery solely on another live `SandboxClaim` referencing the pod, which suffices because the claim path creates the `SandboxClaim` before its first phase Apply (`pkg/gateway/podclaim/claimer.go:103-114`), so a re-claimed pod carries the later task's live claim in every recovery-write phase; this keeps the carve-out enumeration, the ownership-package entries, and the yield protocol unchanged instead of staging a new field-maintenance code path across every claim and release site. Functional impact was contained because the surviving prong already protected the live-pod scenario.
2. **The staged text attributed the recovery and reclamation drain edges (`claimed`/`attached`/`failed`/terminal → `draining`) to the §6.2 state machine, which defines none of them, and staged no §6.2 listing additions (Medium).** Every `──→ draining` line in the §6.2 listing sources from `task_cleanup`, `idle`, or `slot_active` (`spec/06_warm-pod-model.md:149-153,157,165-167`); the edges exist only in `ValidTransitions`, whose comments record them as undocumented extensions of the §6.2 cleanup model (`pkg/sandbox/state/state.go:235-241,246-257`), and the listing is the normative machine (`IsValid` rejects absent edges "per spec §6.2", `pkg/sandbox/state/state.go:274`), so the applied spec would have cited edges a reader following the cross-reference could not find while the watchdog's listing-present edge got a staged line and the listing-absent edges the same paragraphs rely on got none. Section 9.3.2 now stages the pod-reclamation drain group into the §6.2 listing (`claimed → draining` claim-time abort, `attached → draining`, `completed`/`cancelled`/`expired` → `draining`, and `failed → draining` with its specified triggers), the consistent fix under the proposal's own edge-existence criterion for the watchdog line; Sections 5, 6, 9.1.5, 9.1.8, and 9.3.3 drop the "already defines"/"existing edge" attributions in favor of the staged listing, Section 9.12 and the Files-touched docs row mirror the rows into `docs/reference/state-machines.md`, and the Files-touched spec/06 row records the listing additions.

### Pass 11 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The staged §6.2 drain-listing lines attributed `claimed → draining` and `attached → draining` to orphan-loop crash recovery alone (Medium).** The shipped session-mode release exercises both edges from a live gateway in routine operation: `Release` guards the terminal-phase write with `state.IsValid` and drains unconditionally (`pkg/gateway/podsession/binder.go:1465-1492`; the drain doc comment at `binder.go:763-767` names "a terminal disposition, attached, or an early claimed/idle release"), so a disposition with no modeled edge from the pod's current phase (a session expiring while `attached`, since `ValidTransitions` defines no `{Attached, Expired}` edge, `pkg/sandbox/state/state.go:183-187,208,244`, or a resumed pod released while still in `claimed`) skips the terminal write and the gateway writes the drain directly, while the sibling staged lines for the terminal drains already carried the dual triggers, the writer-attribution class of pass-5 entry 3. Section 9.3.2 now gives both lines the dual release-drain-yield and orphan-loop triggers, the Section 9.1.8 staged clause and the Section 6 release-window sentence condition the terminal-write ordering on the disposition having a valid edge from the pod's current phase (a crash before the yield on the skipped-terminal path leaves the pod in `attached` or `claimed`, which the routing already drains), the Section 9.3.2 justification records the live-gateway exercise of the `claimed` and `attached` drains, and the Section 9.12 state-machines mirror carries the full trigger sets.
2. **The `slot_active` stale-claim rationale asserted an idle return the crash ordering forecloses (Medium).** `ReleaseSlot` deletes the `SandboxClaim` before the Redis `Counter.Release` and the `status.activeSlots` decrement (`pkg/gateway/podclaim/slotclaimer.go:647-688`), so every ordering that leaves a stale per-slot claim has already skipped the dead slot's count release; the orphan GC deletes only the claim, the last-slot `slot_active → idle` yield fires only when the decrement reaches zero (`slotclaimer.go:682-684`), the next reservation mirrors the leaked Redis count back into status (`slotclaimer.go:509`), and rehydration reseeds only after a Redis restart (`pkg/gateway/slotcounter/slotcounter.go:14-21`), so the pod never returns to `idle` through the yield. The Section 6 bullet and the staged §4.6.1 sentence now state the actual outcome (the pod stays `slot_active` at reduced capacity, fully unclaimable for `maxConcurrent: 1` pools, until the §6.2 `slot_active → draining` `maxPodUptimeSeconds` drain reclaims it, `spec/06_warm-pod-model.md:166`, with the Redis-restart rehydration as the only count resync), and the Section 11 `slot_active` chart-test case asserts the no-idle-return outcome. Stating the bounded outcome was chosen over staging a count-reconciliation step on stale-claim collection because the residual capacity loss is uptime-bounded and the reconciliation would add a new recompute path across the GC and the slot counter that no staged behavior needs.
3. **The staged §4.6.3 ownership rewrite left `Sandbox.status.conditions` WarmPoolController-exclusive while the shipped gateway writes it from the claim window (Medium).** The release path writes the `Terminated` condition between the terminal-phase Apply and the drain yield (`pkg/gateway/podsession/binder.go:1479-1484`), the suspend path writes `Suspended` after the `suspended` Apply (`binder.go:1547-1552`), and the WarmPoolController writes `OrphanClaimReclaimed` (`pkg/controller/warmpool/gc.go:243-248`), all through `pkg/sandbox/condition` under per-condition field managers (`lenny-sandbox-cond-<type>`, `pkg/sandbox/condition/condition.go:82-84`) whose package doc names both writers (`condition.go:3-7`), and §6.2 makes `.status.conditions` part of the authoritative state (`spec/06_warm-pod-model.md:305`), so the staged WPC-exclusive remainder row and its `pkg/admission/ownership` mirror would classify shipped production writes as violations, the spec-contradicts-code class of round-3 entry 3. Section 9.1.4 now stages a third shared `status.conditions` row, Section 9.1.5 records the gateway's claim-window condition writes and the per-type managers as a surface outside the carve-out and the phase-yield protocol, the Section 5 bullet carries the code evidence, and the Files-touched ownership and spec/04 rows cover the shared entry.

### Pass 12 (2026-06-09, automated)

One entry per confirmed finding; each states the defect and the revision.

1. **The claim that the WarmPoolController's broader status Apply shares `status.phase` ownership with the gateway's update-operation owner without conflict was false, so the non-phase status updates would not land on any pooled pod whose phase the gateway last wrote through a yield (High).** The broader status Apply (`buildSandboxStatusPatch`, `pkg/controller/sandbox/controller.go:641-692`) carries `status.phase` unconditionally (`controller.go:686` sets `patch.Status.Phase = want.Phase`, and the patch is built whenever `status.phase`, `status.podName`, `status.nodeName`, `status.podIP`, or `status.observedGeneration` changes, `controller.go:669-674`), so every non-phase update (`status.podIP`, `status.nodeName`, `status.observedGeneration`) re-asserts management of `status.phase`; SSA conflict detection on a non-forcing Apply fires on asserting management of another manager's field regardless of whether the value differs, so after a gateway yield transfers `status.phase` to the gateway's update-operation manager (`pkg/gateway/podsession/binder.go:788`), the broader Apply conflicts and is abandoned by `retryOnConflictSSA` after five attempts (`pkg/controller/warmpool/controller.go:48`; `pkg/controller/warmpool/controller_test.go:44-47` concedes the cross-strategy conflict), so the broader update never lands. The claim also contradicted the same paragraph's statement two sentences earlier that manager identity includes the operation. Section 5, Section 9.1.5, and the Files-touched `controller.go` row now stage the broader Apply to omit `status.phase` (it carries the field only when `decision.Action == lifecycle.ActionSetPhase`, the case the non-Apply `Update` conversion already handles), so the broader Apply asserts management of no field the gateway's update-operation manager holds and lands its non-phase fields against any phase owner without conflict; this extends the omit-from-Apply pattern the SSA model already implies rather than adding a second takeover, and the Section 11 envtest bullet gains a case asserting the broader Apply with `status.phase` omitted lands against the gateway's update-operation owner without conflict and leaves the phase value untouched.

## 14. Open decisions for review

1. **Counter persistence store.** The draft puts `tasksCompleted`, `scrubFailureCount`, and the retry-recall resolution record (`lastTaskId`, `lastDisposition`, `lastScrubWarning`) on `Sandbox.status.taskState` (gateway-written inside the Section 9.1.5 status window, with the counters WPC-mirrored to `agent_pod_state`), because §6.2 makes `Sandbox.status` the authoritative pod state store (`spec/06_warm-pod-model.md:305`) and the gateway already reads the `Sandbox` at claim time. Two alternatives have equal spec precedent: a pod annotation (the `scrub_warning` precedent, `spec/05_runtime-registry-and-pool-model.md:446`) or a pod-scoped Redis key with blocking Postgres rehydration (the `active_slots` precedent, `spec/12_storage-architecture.md:189-191`). Sign-off on the store choice, or redirection to one of the alternatives, is requested.
2. **Scope of the §4.6.3 retro-documentation.** Section 9.1.5 records the gateway `status.phase` field-manager window that `pkg/gateway/podclaim` already implements for the session and slot paths, because the task disposition writes cannot be specified against the current WPC-exclusive table. If review prefers, that retro-documentation can split into its own reconciliation proposal, with this proposal reduced to the task-mode additions that depend on it.
3. **`ReportTaskCleanup` failure posture.** The draft has the adapter retry indefinitely with backoff while the WPC `task_cleanup` watchdog bounds the pod's wait by failing and replacing it. An alternative is a bounded adapter retry budget after which the adapter self-reports `AdapterTerminating`; the draft avoids it because the watchdog already covers adapter death and a second bound adds a config knob without removing the watchdog's necessity.

## 15. Files touched on application

| File | Change |
|------|--------|
| `spec/04_system-components.md` | §4.7 `ReportTaskCleanup` row; §4.7 `task_complete` and `task_ready` row notes; §4.6.3 ownership-table carve-out and shared `status.conditions` rows, gateway status window paragraph, "never force-conflict" scoping, gateway RBAC grant sentence, WarmPoolController RBAC `update` clause, and `SandboxClaim.status.phase` `released` bullet task-mode extension; §4.6.1 orphan-GC sentence replacement and binding-gated, phase-routed recovery clause |
| `spec/05_runtime-registry-and-pool-model.md` | Line 415 between-task lifecycle; line 419 Full-level bullet; line 446 `warn` attribution; line 410 `maxTasksPerPod` comment and line 451 task-count-limit bullet (counter advances on every `task_cleanup` entry) |
| `spec/06_warm-pod-model.md` | §6.1 preConnect task-mode row; §6.2 transition-listing additions (the watchdog trigger line and the pod-reclamation drain group: `claimed`, `attached`, `failed`, and the session-terminal phases → `draining`), task-mode disposition driver paragraph, and `task_cleanup` watchdog paragraph |
| `spec/12_storage-architecture.md` | `agent_pod_state` task counter columns and mirror-contract sentences |
| `spec/15_external-api-surface.md` | §15.1 task-mode submission paragraph; §15.4 artifact list intro, JSONL bullet rescope, and lifecycle-events bullet; §15.4.1 between-task signaling timing |
| `spec/16_observability.md` | §16.1 `lenny_warmpool_task_cleanup_timeout_total` counter row; §16.1 `lenny_task_reuse_count` row timing clause restaged to the retiring-resolution semantics; §16.5 `TaskCleanupTimeout` alert row |
| `schemas/lenny-adapter.proto` | `ReportTaskCleanup` RPC, request/response messages, `ScrubResult`/`VmRestartResult`/`TaskDisposition` enums; widened `GatewayControl` service comment; corrected `LifecycleChannelRequest` taxonomy comment |
| `schemas/lenny-adapter-jsonl.schema.json` | Removed `task_complete`, `task_complete_acknowledged`, and `task_ready` defs and `oneOf` entries; rewritten description |
| `schemas/lifecycle-events.schema.json` | Rewritten top-level description (sole wire definition of the task envelopes); `task_ready` `$defs` description moved to dispatch-time wording; `task_complete` `$defs` description corrected to the per-task process model |
| `schemas/examples/jsonl.task_complete.json`, `schemas/examples/jsonl.task_complete_acknowledged.json`, `schemas/examples/jsonl.task_ready.json` | Deleted |
| `tests/tier0_static/schemas_test.go` | Removed the deleted example entries from `TestAdapterJSONLExamplesValidate` |
| `pkg/proto/adapter/v1/*.pb.go` | Regenerated from the proto |
| `pkg/controller/warmpool/gc.go` | Binding-gated, phase-routed recovery in `ClaimGarbageCollector.returnToIdle` (the recovery write is skipped when another live `SandboxClaim` references the pod; `claimed → draining` through the claim-time abort edge; `receiving_uploads`, `finalizing_workspace`, `running_setup`, and `starting_session` → `failed` → `draining` through the pre-attached failure and reclamation edges; `attached`, `failed`, and the session-terminal phases `completed`, `cancelled`, and `expired` → `draining`; no write for `task_cleanup`, `sdk_connecting`, `slot_active`, `idle`, `draining`, and `terminated`; every routed write is an existing `pkg/sandbox/state` `ValidTransitions` edge), and conversion of its write from a non-forcing SSA Apply to a non-Apply status `Update` that lands against a crashed gateway's `status.phase` claim (code changes accompanying the §4.6.1 clause; claim deletion in `reclaim` stays phase-independent) |
| `pkg/controller/sandbox/controller.go`, `pkg/controller/warmpool/pod_reconciler.go` | WarmPoolController `status.phase` transitions convert from non-forcing SSA Apply to the non-Apply status `Update`; the broader status Apply (`buildSandboxStatusPatch`) omits `status.phase` (carrying it only when `decision.Action == lifecycle.ActionSetPhase`, which the non-Apply `Update` conversion handles) so it asserts management of no field the gateway's update-operation manager holds (code changes accompanying the §4.6.3 status window; Section 9.1.5) |
| `pkg/controller/sandbox/sdkwarm.go`, `pkg/controller/sandbox/lifecycle/lifecycle.go`, `pkg/controller/sandbox/podspec/podspec.go`, `pkg/adapter` (SDK-warm readiness endpoint) | Between-task re-warm readiness: the adapter container readiness probe surfaces SDK-warm state, and the `sdk_connecting` watchdog clock anchors at the observed `sdk_connecting` entry for re-warms (code changes accompanying the §6.1 task-mode row; Section 9.3.1) |
| `pkg/gateway/podclaim/slotclaimer.go`, `pkg/gateway/podclaim/gatewaystatus.go` | `ReleaseSlot` last-slot conversion: the final Apply zeroes `status.activeSlots` while the phase is still `slot_active`, and the `slot_active → idle` write converts from SSA Apply with `ForceOwnership` to the non-Apply phase yield; the `ApplyGatewayPhase` doc comment is updated to record the yield set and the `suspended` hold (code changes accompanying the §4.6.3 status window; Section 9.1.5) |
| `pkg/admission/ownership/ownership.go`, `pkg/admission/ownership/ownership_test.go` | Gateway claim-window carve-out entries for `Sandbox` `status.phase`, `status.activeSlots`, `status.tenantId`, and `status.taskState.*`, plus a shared `Sandbox` `status.conditions` entry for the per-condition `lenny-sandbox-cond-<type>` managers (gateway `Suspended` and `Terminated`, WarmPoolController `OrphanClaimReclaimed`), with the package doc updated to mirror the rewritten §4.6.3 table (accompany the implementation; Section 9.1.4) |
| `pkg/apis/lenny/v1alpha1/sandbox_types.go`, `charts/lenny/crds/lenny.dev_sandboxes.yaml`, `pkg/embedded/crds/lenny.dev_sandboxes.yaml` | `Sandbox.status.taskState` typed fields, plus the two regenerated CRD schema copies (the embedded copy is a manual mirror of the chart copy per `pkg/embedded/crds/crds.go:3-7`; accompany the implementation; Section 5) |
| `pkg/apis/lenny/v1alpha1/sandboxwarmpool_types.go`, `charts/lenny/crds/lenny.dev_sandboxwarmpools.yaml`, `pkg/embedded/crds/lenny.dev_sandboxwarmpools.yaml` | `taskCleanupTimeoutSeconds` field in `ScalePolicy`, following the `sdkConnectTimeoutSeconds` placement, plus the two regenerated CRD schema copies (accompany the implementation; Section 6) |
| `pkg/apis/lenny/v1alpha1/sandboxtemplate_types.go`, `charts/lenny/crds/lenny.dev_sandboxtemplates.yaml`, `pkg/embedded/crds/lenny.dev_sandboxtemplates.yaml` | `MaxTasksPerPod` field comment reworded from the completed-task definition to the cumulative completed-or-cancelled semantics of Section 9.2.5, plus the two regenerated CRD schemas that carry the comment as the field description (accompany the implementation; Sections 9.2.4 and 9.2.5) |
| `migrations/` (new numbered pair) | `agent_pod_state` `tasks_completed` and `scrub_failure_count` columns (accompany the implementation; Section 9.4.1) |
| `pkg/observability/metrics/catalog.go`, `pkg/observability/metrics/catalog_test.go`, `pkg/alerting/rules/rules.go`, `pkg/alerting/rules/rules_test.go` | `lenny_warmpool_task_cleanup_timeout_total` catalog entry and §16.1 parity-test transcription; `TaskCleanupTimeout` alert rule and its §16.5 parity-test transcription (`spec165WarningAlerts`), following the `SDKConnectTimeout` precedent (accompany the implementation; Sections 9.6 and 11) |
| `charts/lenny/files/alerting-rules.yaml`, `docs/alerting/rules.yaml` | Regenerated via `cmd/gen-alerting-rules` to carry the `TaskCleanupTimeout` rule; the tier-2 observability `catalog_crosscheck` test fails while the chart copy is stale relative to the catalog (accompany the implementation; Section 9.6.2) |
| `docs/runtime-author-guide/adapter-contract.md`, `docs/runtime-author-guide/lifecycle.md` | Docs-sync follow-up (Section 9.12): JSONL-schema description rescope, a `lifecycle-events.schema.json` row in the Canonical artifacts table with the line-654 artifact count removed, `task_ready` row at dispatch time, a `ReportTaskCleanup` row in the Adapter-to-Gateway RPCs table, and corrected between-task sequences |
| `docs/reference/metrics.md`, `docs/runbooks/index.md`, `docs/runbooks/task-cleanup-timeout.md` (new), `docs/reference/state-machines.md`, `docs/reference/configuration.md` | Docs-sync follow-up (Section 9.12): `lenny_warmpool_task_cleanup_timeout_total` metric row and `TaskCleanupTimeout` alert row, runbook map row plus the new runbook, the watchdog trigger on the `task_cleanup → failed` row and the Section 9.3.2 pod-reclamation drain-edge rows, and the `pool.scalingPolicy.taskCleanupTimeoutSeconds` field row |
| `BUILD-GAPS.md` | F-5.2.30 unblocked; deliverable list amended; stale citations corrected |
