# Review log: Concurrent finalize calls both prepare the session's pod

## Standing context

Changelog (compaction pass 1, 2026-09-23): This pass lifted the durable residue of every ledger entry from spec rounds 1 to 3, reconcile.1, f1.human-decisions, and non-spec rounds 1 to 5. It deleted the following superseded claims. The consume-before-ready move (non-spec rounds 2 and 3) was reverted by non-spec.3.fix-G1.1. The "Gap-2 ambiguous ready commit answers 409 currentState=ready" outcome (non-spec.1.review-performance.1 and non-spec.1.review-docs-alignment.1) was replaced by the 500 of sub-branch (iii). The "Gap-2 lost branch skips revoke" warnings (non-spec.1 reliability and security) were fixed by sub-branch (ii). The Redis-tracker premise (non-spec.1.review-performance.1) was corrected by non-spec.3.fix-design-G1.1. The closed-writer-list warnings were settled once SPEC-1 became writer-agnostic. Both DEFERRED entries were closed by reconcile.1. The 409-versus-410 OPEN was closed by f1.human-decisions. Two disagreements are carried as UNVERIFIED lines under Open. The section is about 150 lines and meets the 200-line target.

### Settled

- **SPEC-1 trigger**: state-based ("any request or gateway process other than this finalize call moves the session to a terminal state while finalization is in progress"), with no writer list (spec.1.fix-G1.1, spec.1.fix-design-G1.1).
- **SPEC-1 precedence clause**: "in place of any setup-failure error the call's own prepare phase returns". The main clause makes the 409 unconditional, so the clause is illustrative and does not narrow the rule (spec.2.review-mechanism.1, spec.2.review-reliability.1).
- **SPEC-1 anchor**: the verbatim last sentence of the §15.1 finalize row Notes cell, "A deterministic non-zero setup command surfaces here as the non-retryable `SETUP_COMMAND_FAILED` (422).", at spec/15_external-api-surface.md:646. The sentence is unique in the file, and no other open proposal edits that cell (0081 edits only the error-catalog row).
- **SPEC-1 references nothing else**: the round-2 rewrite dropped the §6.2 link and names no writer, so the sentence carries no anchor, heading, identifier, or reserved N3 phrase.
- **SPEC-1 is the single normative home** for the overtaken-finalize outcome. No other spec site states a finalize response for that case (spec.1/spec.3 review-single-source).
- **SPEC-1 needs no companion spec edit**: §6.2:290 defers to the §15.1 finalize note. §7.2:208, the SETUP_COMMAND_FAILED catalog row (15:1136), §29.2 step 19 (29:258), the §29 forwarding row (29:549), the §15.4 INVALID_STATE_TRANSITION row (15:1011), and the §15.2 finalize_workspace row (15:1329) are generic or state the surfacing endpoint only.
- **SPEC-1 adds no identifier**: it reuses INVALID_STATE_TRANSITION and details.currentState from the §15.1 preamble (15:641), which still requires details.allowedStates.
- **§15.1 neighbour rows**: preamble 409 rule at 15:641; terminate row at 15:649 admits `finalizing` and "aborts the in-progress setup"; DELETE row at 15:653 moves any non-terminal state to `cancelled`.
- **§6.2 watchdog**: the §6.2 heading is at spec/06_warm-pod-model.md:78, and the FINALIZE_TIMEOUT rule (default 600 s, `failed`/FINALIZE_TIMEOUT) is at 06:243. The spec key is `gateway.maxFinalizingTimeoutSeconds` (spec/11:226).
- **From `finalizing`** only `ready` (written by the admitted finalize call) or a terminal state is reachable, so "not finalizing at the exit write" means terminal once the entry guard holds, except in Gap-2 sub-branch (iii).
- **transitionReady is the sole writer of `ready`** in non-test pkg/ and cmd/ code (sessionserver.go:3030).
- **No non-terminal writer leaves `finalizing`**: legalReportTransition admits `finalizing` only toward `failed`, applyFailure no-ops on pre-running rows (failure.go:147-153, :360-385), and treerecovery targets delegation nodes.
- **`created` is written only by row constructors** (create.go:244, derive.go:388, replay.go:170, delegation/service.go:1419).
- **Terminal writers from `finalizing`**: terminate, DELETE, the watchdog, the orphan session reconciler (spec/10:59, orphansession.go:71), admin force-terminate (session_admin.go:41-72), tenant suspend (tenant_suspend.go:173-192), user full-revoke/erasure cancel (spec/11:270, admin/users.go:457-461), and tenant disable (spec/12:913). All run in the gateway.
- **Only gateway code writes terminal session states**; outside pkg/gateway the constants appear only in pkg/api/v1/session and read-only consumers.
- **Every terminal writer reclaims** through recordSessionCompleted / OnSessionTerminal to terminalReclaimPreRunning (usage.go:446, :581-605). The watchdog reaches it through the TerminalHook wired at cmd/lenny-gateway/controlserver.go:237.
- **Watchdog sweep is guarded**: it re-checks `r.State != st` inside Update, lists rows by state, and measures age from UpdatedAt (watchdog.go:598-633). It recovers an orphaned `finalizing` row after a crash or ambiguous entry commit.
- **Orphan-claim GC backstop**: the leader-only ClaimGarbageCollector (pkg/controller/warmpool/gc.go:274-320) collects leaked claims.
- **terminalReclaimPreRunning** skips the by-name reclaim when a live podRegistry BindResult exists or podRegistry is nil. Finalize never calls podRegistry.Put (only start.go does).
- **ReclaimClaimed** calls releaseCredentials (only b.Credentials.ReleaseSession, binder.go:1263-1268) before DeleteClaim, and returns the pod to the pool with no drain. failPhase drains (retires) the Sandbox instead (binder.go:1072-1107).
- **DeleteClaim has no owner check**; it deletes claim-<podName> by name (podclaim/claimer.go:310-333).
- **ReleaseSession is idempotent** on both credassign Service and Client; a double revoke never over-decrements (credassign.go:400-432, client.go:337-345).
- **Lease store per replica**: credleasestore's default backend is in-memory and per-replica, so a terminal writer on another replica cannot release a lease this replica's Prepare minted (credleasestore.go:9-13). See the UNVERIFIED line on the Postgres backend.
- **credassign Client.Release** issues RevokeCredentials to the Token Service and drops the local lease; an unreleased lease stays usable (client.go:299-315).
- **Store errors survive**: pgtenant.InTx and memstore.Update return the mutate error unwrapped, so errors.As(*session.PreconditionError) works on both stores and a refused guard leaves the row unchanged.
- **No store retries a mutation**: pgstore.Update runs one InTx under SELECT ... FOR UPDATE with no retry loop, and no decorator wraps sessionstore.Store, so a guard never misreads its own commit as a lost race.
- **No write-rate cost**: the guards run inside existing locked Updates; a refused entry call writes nothing; no etcd, Redis, or watch traffic is added.
- **failSession / emitTerminalLifecycle** does not call recordSessionCompleted or release a lease, so failFinalizing followed by reclaimFinalizedPod does not double-reclaim (start.go:3414-3430, lifecycle.go:258-290).
- **Client surface**: GET /v1/sessions/{id} carries `failureClass` only. `failureReason` is absent from spec/15 and spec/07 and appears only on the admin payload in code. FINALIZE_TIMEOUT is not a failureClass value.
- **OpenAPI path**: the served document is pkg/gateway/externalapi/openapi/openapi.json (the orchestrator lens text naming pkg/gateway/openapi/ is wrong). Its finalize operation already declares 409 "Invalid state transition", so no OpenAPI edit is needed.
- **MCP parity**: lenny/finalize_workspace is a postByID proxy onto REST /finalize (client_tools.go:102), so handler changes and DOCS-1(b) cover MCP. §15.2 "Semantic equivalence" and "Shared error taxonomy" (15:1418-1422) carry the 409 without a spec edit.
- **SDKs need no edit**: go, python, and typescript treat INVALID_STATE_TRANSITION generically.
- **handleFinalize is the only caller** of transitionFinalizing, transitionReady, and prepareAtFinalize. The start.go:2634 Prepare call belongs to create-and-start and gives 0081's Prepare-arm short-circuit no second trigger; the 0082 Impacts row stands.
- **Docs surfaces**: only docs/api/rest.md:100 (finalize "Key error codes", before `### POST /v1/sessions/{id}/start` at :102) and docs/api/mcp.md:349 (the "Session not in `created` state" row) state a finalize error outcome. Both are in DOCS-1. No tier-11 test pins either string.
- **Other docs stay true**: client-guide/session-lifecycle.md (:174-199, :523-532), reference/state-machines.md:105-106, error-handling.md:58, and error-catalog.md need no change. INTERNAL_ERROR guidance is at error-handling.md:96 and error-catalog.md:142.
- **No schema, chart, metric, alert, or audit surface** names finalize outcomes; session.finalize_workspace in spec/16:348 is a span name.
- **DOCS-1 writer wording**: DOCS-1(a) lists writers as "for example" and includes administrator force-terminate; it glosses maxFinalizingTimeoutSeconds as "the gateway's finalizing timeout".
- **DOCS-1 carries no lease sentence**: no session-endpoint section of the REST reference states lease behavior, and "releases any credential lease the call assigned" is false for a Token-Service-only lease. The table in CODE-3 is the only statement of lease disposition.
- **CODE-3 rule label** is **handleFinalize rules**; ", in handler order" was dropped because rule 3 runs after rule 4. The rules are not renumbered.
- **CODE-3 rules list is the single home** for the lost-write response. Its lead-in carries the "no lost branch reclaims" sentence and the DeleteClaim no-owner-check rationale. The Design exit-guard bullet (non-spec :14), summary Decisions and Watch-out lines, and Edge cases point at it.
- **CODE-3 rule 4 Gap-2 split** on the failFinalizing result: (i) nil or store error: reclaim when prep != nil, 500; (ii) lost and session.IsTerminal(pe.CurrentState): revokeFinalizeLease when prep != nil, no reclaim, 409 via writePreconditionError; (iii) lost and non-terminal (own ambiguous ready commit): no revoke, no reclaim, existing 500.
- **CODE-3 rule 3**: ConsumeDigest stays after the ready write, as in the tree, and its failure branch becomes log-and-continue (log.Printf), so no failure write lands on a `ready` row and the 410 "successful FinalizeWorkspace" definitions stay true.
- **Upload-token tracker**: MemoryTracker (cmd/lenny-gateway/stores.go:385) is the only ConsumedTracker; MarkConsumed returns nil or ErrConsumed only. The "Production wires this to Redis" comment describes nothing in the tree.
- **Token verification sites**: only /upload verifies the upload token (upload.go:302, :824-828), before its `created` precondition, so a consumed token answers 410 ahead of 409 there. /upload passes no capabilities to session.Validate. Mid-session uploads use /upload-to-session with no token.
- **handleFinalize validates no upload token**: recorded in summary "Defects in the shipped tree" (f1.human-decisions). TEST-GAPS.md T-7.1.4 assumes /finalize enforces 401/403/410; BUILD-GAPS.md F-7.1.20 wrongly says /finalize returns UPLOAD_TOKEN_EXPIRED. The summary now carries no open decisions.
- **Upload digest and expiry** are captured from the entry-write result before the ready write (sessionserver.go:3160-3162).
- **uploadAborts/uploadLimits.closeSession** run only on the finalize success tail; the terminal-path leak predates 0082 and the Edge-case bullet accepts it.
- **Tier 1 reaches prep != nil**: package sessionserver_test's pod-bind fixture (podBindRuntime :63, podBindClient :92, podBindAdapterDialer :187, podBindBinder :202, readyTransitionFailStore :1113-1136, recordingLeaseAssigner :1145-1162) runs Prepare to success over bufconn. TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 (:1175, Runtime assignment :1179) proves it.
- **Pod-bind fixture is envtest-backed** (skips without assets); the unit tier already runs envtest-backed pkg/ tests, so labelling TEST-1 tier 1 is correct.
- **TEST-1 file split**: finalize_race_test.go (package sessionserver_test, every hookStore case on the pod-bind fixture) plus finalize_race_internal_test.go (unit cases on unexported symbols only).
- **revokeFinalizeLease pin**: the external ready-write-loss case with a direct-write terminal commit fails when the call is removed. TEST-3 Case B asserts exactly two ReleaseSession entries.
- **TEST-3 Case B counts hold**: handleDelete from `finalizing` runs terminalReclaimPreRunning to ReclaimClaimed once (fixture wires PodRegistry as eager_claim_lifecycle_test.go:275-276 does), giving one claim-sbx-1 Delete and one release, plus revokeFinalizeLease for the second.
- **Recorders count calls**: recordingLeaseAssigner (tier 1) and recordingAssigner (tier 4) append on every ReleaseSession, including no-ops.
- **Fixture names**: the sessionserver-internal assigner is reclaimRecordingAssigner (terminal_reclaim_internal_test.go:42-49); tier 4 has recordingAssigner, eagerCluster, and eagerAdapterDialer (eager_claim_lifecycle_test.go:103-138, :191); tier 7a has raceAttempts and newRaceStart (racestart_testsupport_test.go:29, :50).
- **Build tags**: tier-4 files need `//go:build integration`; tier-7a files need `load_local`.
- **adapter.NewGRPCServer** already takes variadic grpc.ServerOption (pkg/adapter/transport.go:27); eagerAdapterDialer has several tier-4 callers, so TEST-3's extension must stay variadic.
- **Failing-consume test hook**: uploadtoken.NewVerifier(ring, tracker, clock) accepts an injected ConsumedTracker (uploadtoken.go:322); Options exposes Events, LifecycleAuditSink, InteractionAuditSink, and UploadTokenVerifier.
- **Rule-3 test**: TEST-1 "Consume after the ready write" (tracker records `ready`, returns ErrConsumed, expects 200) pins rule 3. No existing test asserts the old consume-failure 500.
- **failFinalizing committed tail** is pinned by one unit case in finalize_race_internal_test.go (nil error, `failed`, one status_change, one session_complete, one session.failed audit), using sseEventsOfType, captureLifecycleAudit, and auditSessionFailed (lifecycle_internal_test.go:290-312).
- **Plan-parse branch (rule 1)** is reachable only through a malformed stored WorkspacePlan with an empty finalize body; /create validates plans, so a test overwrites the row plan through the wrapped inner store after /create (finalize.go:62-63, :331-340; start.go:1218-1224).
- **Rule 1 test pair**: "Plan-parse failure, committed" (added by non-spec.5.fix-G1.1) pins reclaim-after-commit; the lost case pins no-reclaim and write-first.
- **Prepare-failure injection** in the pod-bind fixture uses a setup command that exits non-zero, as TestFinalizeFailsSessionAndReclaimsPodOnSetupError_spec_7_5 does (start_pod_test.go:773-866).
- **Store.Update doc sentence** "does NOT validate the transition" is at sessionstore.go:940-944; the handleFinalize entry Update closure names its parameter `r` (sessionserver.go:3096).
- **Checklist**: S1 is the single spec step (tiers 0 and 11) and precedes every code step; each deliverable (SPEC-1, CODE-1 to CODE-3, DOCS-1, TEST-1 to TEST-4) appears in exactly one step; Depends-on points backward only.
- **Idempotency-Key overlap**: overlapping same-key calls get 409 IDEMPOTENCY_KEY_IN_FLIGHT from the middleware (idempotency.go:351-371); §11.5 is silent on in-flight keys.
- **Performance, Kubernetes, and security lenses** found nothing in any round: the change adds no store write, CRD status, finalizer, webhook, or controller on the request path, and removes no control.

### Traps

- **Do not reintroduce a closed writer list.** SPEC-1, DOCS-1, the Design bullet, and the edge cases must not present terminate, DELETE, and the watchdog as the complete set. A closed list sent the orphan-reconciler case (pod dies mid-Prepare) to §7.1:208's SESSION_CREATION_FAILED rule while CODE-3 returns 409. Any list outside SPEC-1 says "for example".
- **Do not point clients at failureReason.** It is not on GET /v1/sessions/{id}, and the spec does not define it on the admin envelope either, so redirecting to GET /v1/admin/sessions/{id} creates a new unbacked claim. MISTAKE: earlier text told DOCS-1 readers to "read the session to learn why it ended"; both claims were removed.
- **MISTAKE: "at tier 1 prep is always nil".** Earlier rounds put this in the summary Watch-out and the TEST-1 fixture bullet. It left every prep != nil lost branch (rules 3, 4, Gap-2) untested and made TEST-3 Case B's "contains the session ID" the only lease pin, which passes with revokeFinalizeLease removed. The claim holds only for an internal-package test with no adapter; the external pod-bind fixture reaches prep != nil.
- **A lost exit write does not always mean a terminal writer.** The Gap-2 failure write can lose to the call's own committed `ready`. Do not state "lost means terminal" or a uniform per-branch revoke anywhere except CODE-3's rules; revoking in (iii) would strip a live session's credential.
- **Rejected Gap-2 (iii) outcomes.** Answering (iii) with 409 currentState=ready (reports the call's own success as a conflict and needs new SPEC-1/DOCS-1 text) or with 200 through a replayed success tail (re-read plus duplicate status and audit emission) was tried and withdrawn. Both are recorded in summary Non-goals. Do not reopen either without a spec change.
- **MISTAKE: consume before ready.** An earlier round moved ConsumeDigest ahead of the ready write to keep `finalizing` the only failure from-state. That consumed the token on finalizes whose ready write then lost or failed, turning a later /upload 409 into 410 UPLOAD_TOKEN_CONSUMED and falsifying the §7.1/§15.1 and docs 410 definitions. Non-spec round 3 reverted it. Do not move the consume before the ready write and do not re-add a consume failure branch (failSession, failFinalizing, or reclaim) after it.
- **Rejected consume alternatives.** Consume-before-ready plus an Edge-cases row and reworded 410 definitions (adds SPEC-2/DOCS-2 for an avoidable consequence), an Edge-cases row alone (leaves the 410 definition false), and a two-from-state failure write (already in summary Non-goals) were all rejected.
- **Do not renumber CODE-3 rules.** "Rule 4" is cited at several TEST sites; the label changed instead.
- **TEST-1 hooks commit terminal states by direct write to the inner store.** A DELETE through handleDelete runs recordSessionCompleted, which emits the terminal lifecycle and runs terminalReclaimPreRunning (ReleaseSession plus claim delete) on the same recording assigner and fake client, so the handler's own revoke and reclaim cannot be isolated.
- **Do not select the hooked Update by position.** applyFinalizePrepareResult adds an Update only when prep != nil, so an Nth-Update hook fires on the wrong write. Select by the State the mutation writes when probed on a copy, as readyTransitionFailStore does (start_pod_test.go:1123-1136).
- **Weak release assertions.** The terminal ReclaimClaimed calls ReleaseSession before DeleteClaim, so "released contains the session ID" after a DELETE passes with revokeFinalizeLease removed. Use exact counts or commit the terminal write before AssignCredentials.
- **Zero-emission assertions are one-sided.** TEST-1's lost-path "zero terminal lifecycle emissions" also passes if failFinalizing never calls afterFailed; only the committed-path unit case (exactly one emission) distinguishes them.
- **TEST-1 expected values follow CODE-3.** Each case cites its rule by number and does not restate it; if a rule changes, assert the new outcome.
- **finalize_race_internal_test.go target bullet** is a pointer to the cases list. Do not reintroduce a case enumeration or count there; an added case would falsify it.
- **The existing Gap-2 test must keep passing.** TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1: readyTransitionFailStore rejects only a mutation yielding `ready`, so failFinalizing commits `failed`, sub-branch (i) reclaims once, and the test asserts 500, `failed`, and one ReleaseSession.
- **TEST-3 "zero DemoteSDK" is trivial.** The fixture pool is not preConnect; PrepareWorkspace==1 is what pins the entry guard.
- **TEST-3 Case A needs a Get hook.** "Blocks until call 2 has completed its pre-lock Get" requires the tier-4 hook store to observe Get, while TEST-1's hookStore hooks only Update. Implementation judgment, not filed.
- **Stale handleFinalize doc comment.** Its "Gap 2 ... or in the single-use upload-token consume ... reclaims the pod and revokes the lease" paragraph (sessionserver.go:~3048-3056) is false after rule 3. CODE-2 stages only citation repair and rule 3 rewrites only the inline block comment. Code comment, below the finding bar; the implementor drops the consume clause with the CODE-2 rewrite.
- **Pre-existing prepare-failure DeleteClaim hazard.** On an overtaken call, rule 2 still reaches failPhase's drain or prepareAtFinalize's reclaimFinalizedPod (pool, credential, and delivery-isolation exits) before failFinalizing checks the row, so the no-owner-check hazard persists on that path. CODE-3's "never reclaims" is true only of the handler's own reclaim. It predates 0082, is a sub-case of the "terminate does not abort Prepare" shipped-tree defect, and is not filed against 0082. The fix idiom is a Delete with a UID precondition.
- **Pre-existing pod-reuse isolation gap.** Terminate does not abort a running Prepare on another replica, so ReclaimClaimed returns the pod to the pool while the overtaken Prepare keeps running (PrepareWorkspace, RunSetup, AssignCredentials). spec/13:30 relies on "each session's lease is released before the next session begins". SPEC-1 is neutral on abort. A later proposal implementing the abort cites 13:30.
- **Do not document non-abort in docs.** §15.1 says terminate aborts setup; documenting the code's non-abort would contradict the spec.
- **Do not cite §7.2 for `finalizing` transitions.** The §7.2 edge list has no `created → finalizing` or `finalizing → *` edges (07:160-200); its only pre-running edge is created → failed.
- **§7.1 step 23 is a normal-flow step.** "Release credential lease back to pool" (07:52) follows step 22's completion; it does not state lease release for every terminal transition, although the Design prose reads it that way.
- **§4.9 credential mismatch is precedence, not contradiction.** 04:1240 says a mismatch at /finalize releases the pod and returns CREDENTIAL_POOL_EXHAUSTED; SPEC-1 overrides it for an overtaken call. Do not refile without new evidence.
- **"setup-failure error" breadth.** The prepare phase returns non-setup errors (CREDENTIAL_POOL_EXHAUSTED, POOL_WARMING, INVALID_POOL_PROXY_DIALECT, credential-delivery-isolation 422, RUNTIME_LEVEL_UNDERPERFORMS, plan-parse and consume 500s), and CODE-3 answers 409 for all of them when the failure write is lost. Rounds 2 and 3 judged the SPEC-1 wording adequate because the main clause is unconditional; keep any qualifier as broad as CODE-3 rule 2.
- **§29 step 19 is happy-path prose.** "Finalize returns to the client only once the session is ready" (29:258, also 07:35-36) is already qualified by existing 422 returns; it is not a SPEC-1 edit site.
- **Stale orphansession comment.** orphansession.go:370 says the path "only fails post-attached (running) sessions", but its eligibility map includes `finalizing` (:71). Out of scope for 0082.
- **Rule-1 reorder crash window.** Failing before reclaiming opens a narrow window where the claim waits for orphan-claim GC. Not filed because GC backstops it.
- **Postgres failover under (i).** Rule 4 (i) can reclaim a pod whose ready write committed when failFinalizing also returns a transport error. This is shipped behavior and no worse after the change.
- **Ambiguous entry commit and client disconnect** leave `finalizing` for the watchdog (500 or orphaned row); both predate 0082 and are not undocumented outcomes.
- **Pre-existing 409-versus-410 gap.** §7.1 (07:58-60, :446) says finalize validates the uploadToken and a consumed token returns 410, but handleFinalize validates none, so a post-success finalize gets 409. Recorded as a shipped-tree defect; do not reopen as a 0082 decision.

### Open

- **Explicit lease-release text**: whether a lease assigned after a terminal transition needs explicit spec text beyond §7.1 step 23 and 07:216/224 (spec.1.review-security.1, UNVERIFIED).
- **Prepare-failure successor-claim follow-up**: whether the DeleteClaim/drain hazard on the overtaken prepare-failure path should be recorded as a follow-up finding, possibly with 0081's reclaim work (non-spec.1.review-fresh.1 OPEN; non-spec.5.review-fresh.1 UNVERIFIED).
- **Double-store-error reclaim**: when the Gap-2 ready write and failFinalizing both hit store errors (rule 4 (i), and rule 1), the handler reclaims while the row stays `finalizing`, and the later watchdog terminal write issues a by-name DeleteClaim; pre-existing, belongs in a separate proposal (non-spec.2.review-reliability.1).
- **Summary line numbers**: the shipped-tree-defects bullet cites sessionserver.go :3167 and :3190 for the ready write and consume; non-spec.3.fix-G1.1 saw ConsumeDigest at :3176 while non-spec.3.review-citations.1 resolved :3190. Re-verify (UNVERIFIED).
- **credleasestore Postgres backend**: non-spec.3.review-security.1 says credleasestore also has a Postgres-backed implementation, so "in-memory and per-replica" in CODE-3 rule 4 (ii) describes one backend; the revoke goes through credassign's local lease map either way. Earlier entries state only the in-memory backend (UNVERIFIED).
- **/upload and midSessionUpload**: non-spec.5.review-performance.1 says upload.go also admits `running` when midSessionUpload is set; non-spec.5 mechanism, reliability, and security say /upload passes no capabilities, so only `created` is admitted. Not material to rule 3 because ErrConsumed means already consumed (UNVERIFIED).

### Deferred

No unclosed DEFERRED entries. Both earlier entries (spec.1.review-client-surface.1 on the DOCS-1 writer list, spec.1.review-mechanism.1 on non-spec line 14 and DOCS-1) were closed by reconcile.1, which found the text already writer-agnostic.

## Ledger

### [non-spec.6.review-test-coverage.1]
FACT: Round-6 diff is one added TEST-1 bullet ("Plan-parse failure, committed", non-spec-changes.md:217). Its citations hold: ParseStored at pkg/workspaceplan/plan.go:311-312; empty body returns hasPlan=false at finalize.go:62-63; storedWorkspacePlan re-parses row plan at start.go:1218-1224; plan-parse branch reclaims then failSession then 500 at sessionserver.go:3114-3128; failSession's committed write emits through lifecycleAudit (lifecycle.go:268-270, Options.LifecycleAuditSink sessionserver.go:988). — EVIDENCE: as cited
USEFUL [Standing context "Rule 1 test pair"]: confirmed the committed/lost pair jointly pins reclaim-after-commit and no-reclaim-when-lost; no further rule-1 test needed.

### [non-spec.7.fix-G1.1]
DECISION: CODE-3 rule 2's lost branch calls revokeFinalizeLease(id) unconditionally before writePreconditionError — BECAUSE failPhase releases only when leaseAssigned, which Prepare sets only after assignCredentials returns nil, so a partial assignment leaves leases under the session — ALTERNATIVES: fixing it in binder.go (duplicates 0081 CODE-4); revoking on rule 2's committed branch too (pre-existing leak, 0081 CODE-4's remedy, out of scope); narrowing DOCS-1 (weakens the contract).
WATCHOUT: do not gate the rule-2 revoke on prep != nil; prepareAtFinalize returns nil prep on every error, so a gated call never runs — EVIDENCE: pkg/gateway/sessionserver/finalize.go:280-283
FACT: leaseAssigned is set after assignCredentials succeeds and failPhase releases only under it — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:948-953, :1072-1076
FACT: the pod-bind fixture has no AssignCredentials failure hook; recordingLeaseAssigner has fields assigns and released and its AssignProto never fails — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1145-1162
OPEN: rule 2's committed branch still leaks a partially assigned lease until 0081 CODE-4 lands; neither the summary's shipped-tree defects nor the 0081 Impacts row records that dependency (design marked it separate-finding).

### [non-spec.7.fix-G2.1]
DECISION: The rule-1 malformed-plan seed now lives once, as the TEST-1 fixture bullet **Malformed stored plan**; both plan-parse cases open with "Seed the **Malformed stored plan** and send finalize with an empty body." — BECAUSE the lost case had no seed and could not reach rule 1 — ALTERNATIVES: copying the seed into the lost bullet (two drifting copies) or a forward pointer to the sibling case (points at a peer, not the setup's home).
DECISION: TEST-3 Case A gets a precondition: fixture wires blobstore.NewMemoryStore on binder.Blobs and Options.Blobs; Case A uploads one blob and both finalize calls carry an uploadFile plan naming its uploadRef. "Exactly one PrepareWorkspace" is kept — BECAUSE binder.go:1323-1330 issues PrepareWorkspace only for a non-empty upload set and that count is the only non-trivial entry-guard pin — ALTERNATIVES: relaxing to "at most one" (loses the pin), pinning RunSetup instead (unverified unconditional), blob wiring inside eagerCluster (it returns only a client.Client).
WATCHOUT: Any TEST-3 counter assertion on PrepareWorkspace needs an upload and an uploadFile plan; eagerCluster wires no blob store. — EVIDENCE: tests/tier4_integration/eager_claim_lifecycle_test.go:237-238, :282, :316-318, :339-345
CORRECTS [Open "Plan-parse lost-case seed"]: closed by this round; removed from ### Open.

### [non-spec.7.fix-design-G1.1]
DECISION: CODE-3 rule 2's lost branch calls revokeFinalizeLease(id), with no prep gate, before writePreconditionError — BECAUSE Binder.Prepare sets leaseAssigned only after assignCredentials returns nil, so a partial assign (an earlier AssignProto/MintProto succeeded, then a later one or the cl.AssignCredentials RPC failed) leaves leases under the session ID that failPhase never releases, and the terminal writer's ReleaseSession either ran before the mint or ran on another replica — ALTERNATIVES: (a) fixing the binder (leaseAssigned before the call, or unconditional release in failPhase) duplicates 0081 CODE-4's attempt-scoped release of minted lease identifiers and edits a file 0082 does not touch; (b) narrowing DOCS-1 / the summary Goal to exclude the partial-assign case weakens the contract and leaves a leak on a path this proposal defines; (c) revoking on rule 2's committed branch too closes a pre-existing leak that 0081 CODE-4 owns.
FACT: prep is nil whenever prepareAtFinalize returns an error (finalize.go:280-283), and assignCredentials is Prepare's last fallible step (binder.go:948-957), so a lease minted by this call and still held after a rule-2 error can only come from a partial assign. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:861-869, :948-953, :1072-1076, :1224-1256
FACT: the tier-1 recordingLeaseAssigner fixture has one pool and never fails AssignProto (start_pod_test.go:1145-1162), so a partial-assign case needs the in-process adapter's AssignCredentials RPC to fail after AssignProto recorded the lease. EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1150-1162
CORRECTS [Standing context, "DOCS-1 lease sentence"]: the line "holds for rules 1 to 4(ii)" was false for rule 2's lost branch when setup failed inside assignCredentials. It holds only once rule 2's lost branch revokes.
OPEN: rule 2's committed branch (failFinalizing commits `failed`) also leaks a partially assigned lease. This predates 0082, and 0081 CODE-4 ("the credential-assignment failure arm releases those by identifier") is the remedy. Nobody has recorded it in 0082's summary "Defects in the shipped tree" or in the 0081 Impacts row.

### [non-spec.7.fix-design-G2.1]
DECISION: The malformed-plan seed moves out of the "Plan-parse failure, committed" bullet into one named bullet in TEST-1's "Fixture of finalize_race_test.go" (**Malformed stored plan**); both plan-parse cases cite it. BECAUSE the seed is fixture setup shared by two cases, and copying it into the lost bullet would state it twice — ALTERNATIVES: copy the seed into the lost bullet (duplicate that drifts); "same seed as the case below" forward pointer (the lost case precedes its seed's home).
DECISION: TEST-3 Case A gets reachable PrepareWorkspace by wiring a blob store in the TEST-3 Fixture (binder.Blobs and Options.Blobs) and, in Case A, one upload plus an uploadFile workspacePlan on both calls, citing TestEagerClaimLifecycleCreateUploadFinalizeStart. BECAUSE binder.go:1323-1330 issues PrepareWorkspace only when uploads is non-empty — ALTERNATIVES: drop the PrepareWorkspace==1 assertion (removes the only non-trivial entry-guard pin, review-log.md:93); pin RunSetup==1 via a setup command instead (unverified whether RunSetup is unconditional; the upload path is already exercised by the lifecycle test).
FACT: eagerCluster wires no blob store; TestEagerClaimLifecycleCreateUploadFinalizeStart sets binder.Blobs and Options.Blobs in its own body. — EVIDENCE: tests/tier4_integration/eager_claim_lifecycle_test.go:218, :234-240, :282, :317, :339-345
WATCHOUT: Any TEST-3 case that asserts a PrepareWorkspace count needs an upload-bearing plan; an empty or upload-free plan issues zero PrepareWorkspace. — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1323-1330

### [non-spec.7.review-applicability.1]
FACT: The only round-6 change is the TEST-1 "Plan-parse failure, committed" bullet. Its citations resolve: ParseStored at pkg/workspaceplan/plan.go:312, the empty-body early return at pkg/gateway/sessionserver/finalize.go:62-63, storedWorkspacePlan at pkg/gateway/sessionserver/start.go:1218-1224, and storedWorkspacePlanForFinalize falling back to the row plan at finalize.go:331-340. EVIDENCE: those lines
FACT: The docs anchor "**Key error codes:** `RESOURCE_NOT_FOUND` (404), `INVALID_STATE_TRANSITION` (409)." occurs 6 times in docs/api/rest.md (:100, :115, :124, :133, :242, :248). DOCS-1(a) is unambiguous only because it scopes the anchor to the finalize section (:100, before `### POST /v1/sessions/{id}/start` at :102). Keep that section scoping if DOCS-1 is reworded. EVIDENCE: docs/api/rest.md:94-102
FACT: The checklist lanes spec, code, test, and docs are all valid handlers. EVIDENCE: .claude/skills/implement-proposal/SKILL.md:16-17
USEFUL [Standing context: Checklist]: Confirmed again. Each deliverable appears in exactly one step, Depends-on points backward only, and S1 precedes every code step. The applicability lens found nothing in round 7.

### [non-spec.7.review-citations.1]

FACT: The round-6 "Plan-parse failure, committed" case cites hold: ParseStored at pkg/workspaceplan/plan.go:312; the empty-body early return of resolveFinalizePlan at pkg/gateway/sessionserver/finalize.go:62-64; storedWorkspacePlan's ParseStored call at pkg/gateway/sessionserver/start.go:1218-1224. With an empty body, nothing parses the stored plan before storedWorkspacePlanForFinalize (finalize.go:331-340). EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3089-3120
FACT: Every other file:line citation in non-spec-changes.md and summary.md still resolves as of this round: uploadtoken.go:190-193 (MarkConsumed contract), finalize.go:384 (log.Printf), upload.go:307-315 and :824-828, credleasestore.go:9-10, sessionserver.go:2958/3030/3168/3190, usage.go:428/:446, binder.go:1097-1102, lifecycle_internal_test.go:289-312, start_pod_test.go fixture ranges, and the 0081 summary/non-spec line ranges. EVIDENCE: see the cited lines
WATCHOUT: CODE-2's citation repair names transitionFinalizing and transitionReady, but their current doc comments carry no "§4.3" text (sessionserver.go:3019-3030); only handleFinalize's comment does. This is harmless and does not meet the bar. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3019-3063
USEFUL [Standing context]: the Settled list of fixture names and line anchors let me confirm the citations without re-deriving them.

### [non-spec.7.review-client-surface.1]
FACT: Client-visible finalize outcomes after 0082 are all existing codes: 404 RESOURCE_NOT_FOUND (entry Update ErrNotFound, previously 500), 409 INVALID_STATE_TRANSITION with details.currentState/allowedStates (rendered by writePreconditionError), and the rule-3 consume failure becoming 200 instead of 500. No spec, doc, or test states the old consume-failure 500. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3316-3330, :3190-3198; spec/07_session-lifecycle.md:60
FACT: AllowedStates(EndpointFinalize, nil) yields [created]; finalize has no gated states, so TEST assertions allowedStates=[created] match the wire. — EVIDENCE: pkg/api/v1/session/session.go:288, :421-434
FACT: Client SDKs (go, python, typescript) route finalize through a generic transition call and have no finalize-specific error or retry branch. — EVIDENCE: sdks/client/python/lenny/client.py:269-276
USEFUL [Standing context "OpenAPI path", "MCP parity", "Docs surfaces"]: re-verified; openapi.json finalize op declares 409; docs/api/mcp.md:349 and docs/api/rest.md:100 are the only finalize error-outcome sites. No client-surface finding this round.

### [non-spec.7.review-docs-alignment.1]
- DECISION: no docs-alignment findings in round 7 — BECAUSE the only r5->r7 change is the TEST-1 "Plan-parse failure, committed" case (no docs surface), and DOCS-1 anchors still resolve verbatim (docs/api/rest.md:99, docs/api/mcp.md:349) — ALTERNATIVES: filing the Gap-2 (iii) "500 on a ready session" outcome as undocumented was rejected as a close variant of the already-fixed ambiguous-ready finding, and Standing context already settles error-handling.md:96 as needing no change.
- FACT: DOCS-1's lease sentence also holds for rule 2 (lost prepare failure), because Binder.failPhase calls releaseCredentials when leaseAssigned — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1072-1078

### [non-spec.7.review-edit-sites.1]
FACT: Round-6 delta is one added TEST-1 case ("Plan-parse failure, committed"); its citations hold: ParseStored at pkg/workspaceplan/plan.go:312, empty-body early return at pkg/gateway/sessionserver/finalize.go:62-63, storedWorkspacePlan parse at start.go:1218-1224. It adds no identifier and no docs/spec/schema/chart surface. — EVIDENCE: non-spec-changes.md:217
FACT: `maxFinalizingTimeoutSeconds` and FINALIZE_TIMEOUT appear nowhere in docs/, charts/, or schemas/, so DOCS-1(a) is the first docs mention and no companion docs row exists to drift. — EVIDENCE: grep over docs/ charts/ schemas/ (no hits)
FACT: spec/15:683-687 (derive_failure reachability table) lists /finalize → 409 for a terminal row; it is unrelated to SPEC-1 and needs no edit. — EVIDENCE: spec/15_external-api-surface.md:687
USEFUL [Standing context "Docs surfaces"]: the list of finalize error-outcome docs sites (rest.md:100, mcp.md:349) re-verified complete this round; mcp.md:296 is the upload tool row, not finalize.

### [non-spec.7.review-fresh.1]
DECISION: Filed one finding: the TEST-1 "Plan-parse failure after a terminal write" bullet (non-spec-changes.md:216) names no malformed-plan seed, so the plan-parse branch never runs, the hook probe for `failed` never matches, and the case sees 200/`ready` — BECAUSE the Open "Plan-parse lost-case seed" entry was never closed and the round-6 committed case (:217) seeds only itself — ALTERNATIVES: re-filing the rule 4 (ii) "in-memory and per-replica" rationale (credleasestore.go:9-10 does say that; production uses credleasepg when pgPool is set, cmd/lenny-gateway/stores.go:1759-1774) rejected because the revoke behavior is unchanged; the rule-2 partial-lease gap is the security lens's finding.
FACT: writePreconditionError renders any *PreconditionError as 409 INVALID_STATE_TRANSITION with currentState/allowedStates, and a non-precondition error as 500 (pkg/gateway/sessionserver/sessionserver.go:3316-3330; PreconditionError.Code is a constant 409, pkg/api/v1/session/session.go:384-388). — EVIDENCE: as cited
FACT: handleDelete is Get, Validate, then an unconditional Update to `cancelled` followed by recordSessionCompleted; it never deletes the row, so TEST-4's finalize 409 set {finalizing, ready, cancelled} is complete. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:2925-2958
USEFUL [Standing context "Plan-parse branch (rule 1)"]: gave the seeding mechanism directly.

### [non-spec.7.review-kubernetes.1]
- FACT: The only r5-to-r7 change is the TEST-1 "Plan-parse failure, committed" bullet; it adds no CRD status, finalizer, webhook, watch, or controller on the request path. The committed branch's claim-sbx-1 deletion goes through reclaimFinalizedPod -> podBinder.ReclaimClaimed, which the gateway already performs today — EVIDENCE: pkg/gateway/sessionserver/finalize.go:379-387, pkg/gateway/sessionserver/sessionserver.go:3114-3126
- USEFUL [Settled: "Performance, Kubernetes, and security lenses found nothing"]: still holds in round 7; lost branches skip DeleteClaim, so the proposal introduces no second writer to a claim object.

### [non-spec.7.review-mechanism.1]
FACT: The binder calls PrepareWorkspace only when the plan yields at least one upload (`if len(uploads) > 0`), so a finalize with no plan or a plan with no uploadFile/uploadArchive/gitClone source makes zero PrepareWorkspace RPCs. A test that counts PrepareWorkspace must stage an upload and send a plan naming it, with binder.Blobs and Options.Blobs wired as the lifecycle test body does (eagerCluster does not). — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1323-1330; tests/tier4_integration/eager_claim_lifecycle_test.go:234-238, :277-280, :315-345
DECISION: filed TEST-3 Case A "exactly one PrepareWorkspace" as unreachable without an unstated upload-and-plan precondition — BECAUSE on correct code with two plain keyless calls the counter reads 0 and the assertion fails — ALTERNATIVES: drop the counter (weakens the pin the Traps section relies on); rejected in favour of stating the precondition.
DECISION: filed the OPEN "Plan-parse lost-case seed" (non-spec.5.fix-G1.1) as a finding — BECAUSE the lost case's `failed` probe never matches when the stored plan parses, so the hook stays unarmed and the case sees 200/`ready` — ALTERNATIVES: leave as OPEN; rejected because the committed case added in round 6 shows the seed the lost case omits.
USEFUL [Standing context "TEST-3 Case B counts hold", "Watchdog sweep is guarded", "transitionReady is the sole writer"]: re-verified; saved tracing the watchdog TerminalHook and the ready writers.

### [non-spec.7.review-performance.1]
FACT: Round-7 diff is only the TEST-1 "Plan-parse failure, committed" bullet; it adds no store write, watch, or RPC. Performance lens again finds no write amplification: entry and exit guards run inside the existing single locked Update, failFinalizing replaces failSession one-for-one, rule 3 removes a write (consume failure no longer fails/reclaims). Postgres failover path is unchanged (rule 4 (i) reclaim + 500, row left `finalizing` for the §6.2 watchdog). — EVIDENCE: non-spec-changes.md CODE-2, CODE-3 rules 1-4
FACT (resolves the Open "credleasestore Postgres backend" UNVERIFIED): production wires the Postgres-backed credleasestore/pgstore whenever pgPool != nil, and the in-memory store only otherwise; credassign Client.ReleaseSession and Service.ReleaseSession both iterate leases.LeasesBySession on that shared store. So CODE-3 rule 4 (ii)'s rationale "the lease store is in-memory and per-replica ... a terminal writer on another replica cannot release this lease" is false for a Postgres deployment; the revoke is still needed for the AssignCredentials-after-terminal-revoke ordering and is idempotent, so the action stays correct. Not filed as a finding (rationale only; no behavior or test changes). — EVIDENCE: cmd/lenny-gateway/stores.go:1759-1768, pkg/gateway/credentials/credassign/client.go:337-344, credassign.go:400-409, credleasestore.go:9-10 (package doc describes only the in-memory Store; :25-29 names the pgstore alternative)

### [non-spec.7.review-reliability.1]
FACT: The production gateway uses the Postgres-backed credential-lease store whenever pgPool is set (cmd/lenny-gateway/stores.go:1759-1774, credleasepg.New). The in-memory per-replica Store is the fallback only. With the shared store, a Client-mode terminal writer on another replica can find and revoke this replica's lease through LeasesBySession (credassign/client.go:337-345). CODE-3 rule 4 (ii)'s "in-memory and per-replica" rationale therefore describes only the fallback. The revoke stays necessary for the "ReleaseSession ran before AssignCredentials completed" case and is idempotent (Client.Release no-ops on an unknown lease, client.go:299-302), so the behavior is correct. The rationale is incomplete, which is below the finding bar. — EVIDENCE: cmd/lenny-gateway/stores.go:1755-1774
CORRECTS [Standing context "Lease store per replica" and Open "credleasestore Postgres backend"]: The Postgres backend exists and is the production default, so the UNVERIFIED line is resolved as true.
FACT: ClaimGarbageCollector classifies claims by claim binding state and age, and it never reads session state (pkg/controller/warmpool/gc.go:274-322). Rule 1's failFinalizing-then-reclaim reorder therefore opens no GC race on a claim whose session just went `failed`. The only exposure is the crash window already listed in Traps. — EVIDENCE: pkg/controller/warmpool/gc.go:274-322
FACT: DeleteClaim tolerates NotFound (podclaim/claimer.go:328), so a double reclaim of the same claim name is harmless unless the pod has been recycled to a successor, which is the no-owner-check hazard. — EVIDENCE: pkg/gateway/podlifecycle/podclaim/claimer.go:321-333
DECISION: Filed no findings this round. BECAUSE I traced rules 1 to 4 (i/ii/iii), the entry CAS, and TEST-1 to TEST-4 through ambiguous commit, Postgres failover, client disconnect, and watchdog overtake. Every residual hazard either predates 0082 (and is recorded in summary defects or Traps) or is backstopped by the watchdog or orphan-claim GC. ALTERNATIVES: I did not file the rule 4 (ii) Postgres-backend rationale, because its revoke behavior is unaffected.

### [non-spec.7.review-security.1]
FACT: Binder.Prepare sets leaseAssigned=true only after assignCredentials returns nil, so a failure inside assignCredentials (a later provider's AssignProto/MintProto error after an earlier provider's lease was minted, or a failed cl.AssignCredentials RPC after every lease was minted) reaches failPhase with leaseAssigned=false and releases nothing. The minted leases stay recorded under the session ID. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:861-869, :948-953, :1072-1076, :1222-1256; pkg/gateway/credentials/credassign/credassign.go:400-409
WATCHOUT: CODE-3 rule 2's lost branch calls no revokeFinalizeLease, so an overtaken finalize whose Prepare failed inside assignCredentials leaves a lease nobody releases (the terminal writer's ReleaseSession ran earlier or on another replica). This falsifies DOCS-1's "releases any credential lease the call assigned" and the summary Goal on post-terminal-revoke leases. The partial-assign leak on the committed rule-2 branch predates 0082. EVIDENCE: non-spec-changes.md:157, :179; summary.md:42
DECISION: Filed the rule-2 lease gap as the only security finding — BECAUSE it is the one lost branch where a lease can be minted after the terminal revoke and not released — ALTERNATIVES: re-filing the successor-claim DeleteClaim/drain hazard on prepare failure (pre-existing, already refuted/recorded); rule-3 log-and-continue (ErrConsumed means the digest is already invalidated, and /upload is gated on `created`, so no control regresses).

### [non-spec.7.review-single-source.1]
FACT: The round-6 addition "Plan-parse failure, committed" (non-spec-changes.md:217) asserts rule 1's committed outcome and cites the rule; it does not copy the rule. Its trigger path is correct: with an empty finalize body resolveFinalizePlan returns hasPlan=false (finalize.go:62-63), and storedWorkspacePlan calls ParseStored on the row plan (start.go:1218-1224). The plan-parse branch today reclaims first and then calls failSession (sessionserver.go:3114-3123). CODE-3 rule 1 reverses that order. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3114-3123
DECISION: No single-source findings in round 7 — BECAUSE each rule has one stating site: SPEC-1 for the overtaken-call outcome, CODE-3 "handleFinalize rules" for the lost-write responses, CODE-1 for the exit predicate, and CODE-2 for the entry check. The other sites cite the rule, give its rationale (the DeleteClaim no-owner check, the per-replica lease store), or assert its outcome in a test. DOCS-1 restates SPEC-1 in reader vocabulary, and the documentation rules allow that one restatement. — ALTERNATIVES: I rejected filing the TEST-1 case bullets as copies of CODE-3. Each case bullet gives concrete observables (claim-sbx-1, the released list, SSE, audit) under a single rule citation, and it does not restate the rule's branch conditions.

### [non-spec.7.review-test-coverage.1]
- FACT: Every CODE-3 branch now has a pinning case: rule 1 lost + committed, rule 2 lost (+ existing TestFinalizeFailsSessionAndReclaimsPodOnSetupError_spec_7_5 for committed), rule 3 consume-after-ready, rule 4 lost (3 terminal states), Gap-2 (i) existing test, (ii) and (iii) new cases; entry guard at tiers 1, 2, 4, 7a. EVIDENCE: non-spec-changes.md:211-223, start_pod_test.go:773-866, :1175-1266
- FACT: Entry-Update non-precondition store error keeps today's 500 (current branch writes INTERNAL_ERROR 500; writePreconditionError falls through to 500), so no new test is owed. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3102-3105
- DECISION: did not file tier 3 or tier 9 omission — BECAUSE summary.md:61 excludes tier 3 with reason (no envelope change; tests/tier3_contract/rest_sessions/sessions_test.go:250-260 already pins the finalize 409 envelope), and the lease-release precedent (Gap-2 revoke) is pinned at tier 1/4 with recording assigners, not tier 9 — ALTERNATIVES: filing tier 9 for revokeFinalizeLease; judged below the "plainly reaches" bar.
- USEFUL [Standing context "Plan-parse branch (rule 1)"]: saved re-deriving how the rule-1 branch is reachable (finalize.go:62-63 empty body keeps stored plan).

### [operator.redesign-r7]
DECISION: CODE-3 gains a table, **handleFinalize exit dispositions**, as the single home of each exit's lease releaser, pod reclaimer, and status (unchanged, changed by 0082, pre-existing and fixed here, or pre-existing and recorded), with the rationale for each disposition in the notes beneath it. Rules 1 to 4, the revokeFinalizeLease doc comment, the Design exit-guard bullet, the summary, and Edge cases cite the table by heading. — BECAUSE the non-spec loop repaired the lease and pod disposition one branch per round (rounds 1, 3, and 7) and left the prep gating inconsistent across rules — ALTERNATIVES: another per-branch repair round (rejected: it had produced the inconsistency); a per-rule disposition paragraph (rejected: it states each disposition next to its rule and leaves no single place to check that every minted lease has a releaser).
DECISION: revokeFinalizeLease runs with no prep gate on every lost exit write whose locked state is terminal: rule 1 lost, rule 2 lost, rule 4 lost, and Gap-2 sub-branch (ii). Sub-branch (iii) still neither revokes nor reclaims. — BECAUSE prepareAtFinalize returns a nil result on every error, so prep does not show whether a lease was minted, and ReleaseSession is keyed by the session identifier and does nothing when the session holds no lease — ALTERNATIVES: keeping the prep != nil gate on rule 4 and (ii) (rejected: correct only by coincidence there, and inconsistent with rule 2); returning a "lease minted" flag from prepareAtFinalize (rejected: Binder.Prepare reports no partial mint, and the flag would add a return value for a call that is idempotent anyway).
DECISION: CODE-3 rule 2 calls revokeFinalizeLease after failFinalizing whatever failFinalizing returns, including a committed failure write and a store error. TEST-1 gains the case "Credential-assignment failure, committed", and the rule-1 lost case now asserts released equal to exactly [id]. — BECAUSE on a partial assignCredentials the minted leases stay recorded under the session, failPhase skips its release because leaseAssigned is false, and failFinalizing's tail (afterFailed) and the tree's failSession release nothing, so the lease has no releaser in the tree; the revoke is safe because the entry guard admits one Prepare per session and only handleFinalize writes `ready` — ALTERNATIVES: recording the leak under the summary's shipped-tree defects and leaving it to 0081 CODE-4's attempt-scoped release (rejected: 0081 is a draft with no landing order relative to 0082, and the table would carry a minted lease with no releaser); fixing failPhase in binder.go (rejected: duplicates 0081 CODE-4 and edits a file 0082 does not otherwise touch). The two remedies compose, and the summary's 0081 Impacts row states it.
DECISION: The rule 4 (ii) sentence calling the lease store "in-memory and per-replica" is removed. The table's note "The terminal reclaim can miss a lease this call minted" states the reason that holds: the terminal reclaim's ReleaseSession enumerates LeasesBySession once, at the terminal transition, and a lease the still-running Prepare mints afterwards is outside that enumeration. — BECAUSE production wires the Postgres-backed lease store whenever a Postgres pool is configured and shares it with both credassign implementations, so the replica is not the cause — ALTERNATIVES: keeping the per-replica sentence with a fallback qualifier (rejected: it names a dev-mode condition as the reason).
DECISION: The overtaken-prepare DeleteClaim hazard (binder failPhase drain, Prepare reconnect ReclaimClaimed, and prepareAtFinalize's pre-Prepare reclaimFinalizedPod) and the failure-write store-error double reclaim move into the summary's Defects in the shipped tree that this proposal does not stage, as **Overtaken prepare failure** and **Failure-write store error**. The summary Watch-out on podclaim.DeleteClaim now scopes the no-reclaim guarantee to handleFinalize's own reclaimFinalizedPod. — BECAUSE the Watch-out stated a guarantee the binder violates on the overtaken prepare-failure path, and the table needs a recorded home for every pod hazard it leaves in place — ALTERNATIVES: staging a UID-preconditioned claim Delete (rejected: it changes podclaim for every caller and belongs with 0081's reclaim work); leaving both as review-log Traps and Opens (rejected: a reader of the summary never sees them).
FACT: Binder.failPhase releases leases only under `if leaseAssigned`, and Binder.Prepare sets leaseAssigned = true only after assignCredentials returns nil; assignCredentials is Prepare's last fallible step, so no Prepare failure reaches failPhase with leaseAssigned true, and Binder.Launch passes the literal true. — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go Binder.failPhase (:1073), Binder.Prepare (:954), Binder.Launch (:998)
FACT: Binder.Prepare's reconnect failure calls ReclaimClaimed, which releases the session's leases and deletes the claim; every later Prepare failure calls the reclaim closure, which calls failPhase and then drain (DeleteClaim). — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go Binder.Prepare (:853), Binder.ReclaimClaimed (:1097), Binder.drain
FACT: assignCredentials mints each pool lease through Credentials.AssignProto and each user lease through UserCredentials.MintProto into a local map before the adapter AssignCredentials RPC; each mint records the lease in the lease store at once (Client.recordAssignedLease, Service.Assign's s.leases.Put). — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go Binder.assignCredentials; pkg/gateway/credentials/credassign/client.go recordAssignedLease; pkg/gateway/credentials/credassign/credassign.go:337
FACT: failSession, afterFailed's parts archiveSettledChild and emitTerminalLifecycle release no lease and delete no claim; emitTerminalLifecycle emits status_change, session_complete, the lifecycle audit event, the retention roll, metrics, and the messaging drain only. — EVIDENCE: pkg/gateway/sessionserver/start.go failSession; pkg/gateway/sessionserver/lifecycle.go emitTerminalLifecycle; pkg/gateway/sessionserver/usage.go archiveSettledChild
FACT: The credleasestore package doc still says the store is in-memory and per-replica, but its LeaseStore interface names the Postgres-backed pgstore as an alternative, and cmd/lenny-gateway/stores.go replaces the in-memory store with credleasepg.New whenever pgPool is non-nil and passes the same llmLeases to credassign.NewClient and credassign.New. — EVIDENCE: pkg/gateway/credentials/credleasestore/credleasestore.go (package doc, LeaseStore); cmd/lenny-gateway/stores.go (:1764, :1822, :1833)
FACT: credassign Client.ReleaseSession and Service.ReleaseSession both walk leases.LeasesBySession for the session at call time and release each lease; Client.Release and Service.releaseLocked return on an unknown lease. — EVIDENCE: pkg/gateway/credentials/credassign/client.go ReleaseSession (:337), Release; pkg/gateway/credentials/credassign/credassign.go ReleaseSession (:400), releaseLocked
FACT: terminalReclaimPreRunning runs Binder.ReclaimClaimed once, from recordSessionCompleted, for a session whose from-state is pre-running and that has no live BindResult; finalize never registers a BindResult. — EVIDENCE: pkg/gateway/sessionserver/usage.go terminalReclaimPreRunning, recordSessionCompleted
FACT: prepareAtFinalize calls reclaimFinalizedPod on its pool-resolution, credential-pool-resolution, and credential-delivery-isolation exits, and returns a nil result on every error. — EVIDENCE: pkg/gateway/sessionserver/finalize.go prepareAtFinalize (:231, :251, :271)
FACT: The only existing finalize test that asserts an exact ReleaseSession count is TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 (released equal to [sess-fin-gap2]); Gap-2 sub-branch (i) adds no revoke, so it still passes. — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1250
FACT: 0081 CODE-4 adds an unconditional attempt-scoped release of the leases assignCredentials minted on Binder.Prepare's credential-assignment failure arm, which runs before handleFinalize's revoke. — EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.non-spec-changes.md (CODE-4, Binder.Prepare credential-assignment arm)
CORRECTS [Standing context Settled "Lease store per replica"]: the per-replica backend is the fallback; the reason a terminal writer's revoke misses a lease is the mint-after-release ordering, stated in the table's notes.
CORRECTS [Standing context Settled "CODE-3 rule 4 Gap-2 split"]: sub-branch (ii) revokes with no prep gate.
CORRECTS [Standing context Settled "CODE-3 rules list is the single home"]: the lost-write response stays in the rules, and the lease and pod disposition with its DeleteClaim rationale moves to the exit-dispositions table; the rules' lead-in cites the table.
CORRECTS [Standing context Settled "DOCS-1 lease sentence"]: the sentence now holds on every lost exit write whose locked state is terminal.
CORRECTS [Standing context Traps "A lost exit write does not always mean a terminal writer"]: the uniform revoke now applies to every lost write whose locked state is terminal, and the table is its single statement; sub-branch (iii) remains the exception.
CORRECTS [Standing context Traps "Pre-existing prepare-failure DeleteClaim hazard"]: moved to the summary's shipped-tree defects as **Overtaken prepare failure**.
CORRECTS [Standing context Traps "Postgres failover under (i)"]: recorded in the summary's shipped-tree defects as **Failure-write store error**.
CORRECTS [Standing context Open "Prepare-failure successor-claim follow-up"]: closed; recorded as **Overtaken prepare failure**.
CORRECTS [Standing context Open "Double-store-error reclaim"]: closed; recorded as **Failure-write store error**.
CORRECTS [Standing context Open "credleasestore Postgres backend"]: closed as true; the rule 4 (ii) rationale that depended on it is replaced.
CORRECTS [non-spec.7.fix-G1.1 ALTERNATIVES and OPEN; non-spec.7.fix-design-G1.1 ALTERNATIVES (c) and OPEN]: rule 2's committed branch now revokes, so the partial-assignment leak on that branch is fixed here and no longer depends on 0081 CODE-4.
CORRECTS [non-spec.7.review-performance.1 and non-spec.7.review-reliability.1 "not filed" rationale decisions]: the rule 4 (ii) rationale they judged below the bar is replaced.
CORRECTS [non-spec.7.review-security.1 WATCHOUT]: rule 2's lost and committed branches both revoke.
FACT: A concurrent-workspace row's PodAssignment is the pod ClaimSlot reserved a slot on, and every slot holder on that pod shares the one per-pod claim that SlotClaimer creates on a fresh pod and reuses otherwise. Binder.ReclaimClaimed deletes that claim by name and never calls ReleaseSlotReservation; the plan-parse reclaim, prepareAtFinalize's pool-resolution reclaim, and terminalReclaimPreRunning all reach it with no concurrency check. — EVIDENCE: pkg/gateway/sessionserver/create.go (PodAssignment from createClaim.SandboxName); pkg/gateway/sessionserver/start.go (ClaimSlot for MaxConcurrentSessions > 1); pkg/gateway/podlifecycle/podclaim/slotclaimer.go (CreateClaim on freshPod, else existing); pkg/gateway/podlifecycle/podsession/binder.go ReclaimClaimed; pkg/gateway/sessionserver/usage.go terminalReclaimPreRunning
FACT: credassign.Client.assign returns an error without recording the lease when the Token Service returns more than one lease, when credentialLeaseFromProto fails, or when recordAssignedLease's Put fails, each after the server committed; a timeout after a server commit also leaves an unrecorded lease. — EVIDENCE: pkg/gateway/credentials/credassign/client.go assign
FACT: The tree's Gap-2 branch reclaims (when prep != nil) before failSession, so CODE-3 rule 4 (i) reorders it, as rule 1 does. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go handleFinalize, Gap-2 branch after the finalizing → ready Update
FACT: A non-committing entry Update error returns 500 today and through writePreconditionError under CODE-2, and leaves the row `created`. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go handleFinalize, created → finalizing Update error branch
DECISION: Recorded "Shared-pod reclaim" as a shipped-tree defect, corrected the table preamble so a concurrent-workspace pool no longer reads as a no-op, and marked the rows a concurrent-workspace row can reach (rule 1, rule 2 before Prepare, ready write lost, Gap-2 (i) and (ii)) "Pre-existing, recorded (shared-pod reclaim)". — BECAUSE the preamble listed concurrent-workspace pools only as minting no lease, which hid the by-name delete of a shared claim — ALTERNATIVES: staging a slot-aware reclaim (rejected: it changes ReclaimClaimed and the terminal path for every caller, outside the race this proposal fixes).
DECISION: The table's lease column and the summary Goal now cover leases recorded in the lease store, and "Token-Service-only lease" is a shipped-tree defect. — BECAUSE a lease the Token Service minted but the gateway never recorded is invisible to LeasesBySession, so no releaser in the table can reach it — ALTERNATIVES: staging a compensating RevokeCredentials in Client.assign (rejected: it belongs to the credassign client and to every assignment path).
CORRECTS [operator.redesign-r7, table row Gap 2 (i)]: relabelled "Changed by 0082: the reclaim moves after the failure write", matching rule 1; the rule-1 store-error row carries the same label.
CORRECTS [operator.redesign-r7, summary "Failure-write store error"]: split by branch; on the plan-parse branch and in sub-branch (i) the handler reclaims after the failure write.
CORRECTS [operator.redesign-r7, summary Decisions, Watch-out, and Non-goals]: the rule-2 revoke decision, the DeleteClaim Watch-out, and the prep-gate Non-goal now cite the table or its named note rather than restating them.
CORRECTS [operator.redesign-r7, table preamble]: it now names the non-committing entry Update store error among the pre-mint exits, and the revokeFinalizeLease doc comment states its own reason (session-keyed, no claim delete because DeleteClaim has no owner check) instead of pointing at the proposal. The table cells are complete sentences, and the status value "Unchanged" reads "The disposition is unchanged".
CORRECTS [TEST-1 target bullet]: prep != nil holds only in a case whose Prepare succeeds, because prepareAtFinalize returns a nil result on every error.
CORRECTS [summary shipped-tree defect "handleFinalize validates no upload token"]: its spec line-number citations now name §7.1 Normal Flow, §7.4 Upload Safety, and §15.1 REST API with the paragraphs they cite.
DECISION: Left the summary Non-goals unchanged on the reported duplicate bullet. — BECAUSE a line-by-line comparison of the Non-goals list finds no repeated bullet, and the bullets at the cited position (the transitionguard.go helper and the new §7.1 paragraph) reject different alternatives — ALTERNATIVES: merging the §7.1-paragraph Non-goal with the Decision on the single SPEC-1 sentence (rejected: one records the decision and the other the rejected alternative).

### [operator.docs-1-cleanup]
DECISION: DOCS-1(a) drops the sentence "the gateway releases any credential lease the call assigned" and rewords the state sentence to say the session stays in the state it ended in and `details.currentState` names that state. — BECAUSE the lease clause overclaims for a lease the Token Service minted and the gateway never recorded (summary, **Token-Service-only lease**), no other session-endpoint section of the REST reference states lease behavior, and a client cannot observe the release; the old state sentence said the session keeps its state "in `details.currentState`", which is a response field rather than session state — ALTERNATIVES: narrowing the clause to "any lease the gateway recorded" (rejected: it puts an internal lease-store distinction on a client page); keeping the clause and fixing the Token Service defect here (rejected: the summary scopes that fix to a follow-up proposal).
CORRECTS [Standing context Settled "DOCS-1 lease sentence"; operator.redesign-r7 CORRECTS of the same entry]: DOCS-1 no longer states lease release.
