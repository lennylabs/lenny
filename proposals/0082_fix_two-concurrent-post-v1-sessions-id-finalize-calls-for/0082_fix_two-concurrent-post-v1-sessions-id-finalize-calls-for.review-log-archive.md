# Review log archive — 0082_fix_two-concurrent-post-v1-sessions-id-finalize-calls-for

Entries the compaction pass has already curated. Its residue is
in the review log's standing context; this file is the record, for a
human chasing a citation. No agent reads it.

### [spec.1.fix-G1.1]
DECISION: SPEC-1's trigger is state-based ("any request or gateway process other than this finalize call moves the session to a terminal state") and carries precedence over the call's own setup-failure error — BECAUSE the CODE-1 guard refuses any non-`finalizing` row whichever writer moved it, and spec/07_session-lifecycle.md:208 would otherwise send a transient setup failure to the retryable fallback — ALTERNATIVES: a "for example" writer list inside SPEC-1 (rejected: a partial enumeration in the normative home goes stale); a longer closed list (same defect).
WATCHOUT: Terminal writers from `finalizing` include the orphan session reconciler, admin force-terminate, tenant suspend, user full-revoke, and tenant disable, beyond terminate, DELETE, and the watchdog. Do not reintroduce a closed writer list in SPEC-1 or any site that presents one as complete — EVIDENCE: spec/10_gateway-internals.md:59; pkg/gateway/session/orphansession/orphansession.go:71; cmd/lenny-gateway/session_admin.go:48-62
WATCHOUT: The client session response carries `failureClass` only; `failureReason` (for example FINALIZE_TIMEOUT) is not on GET /v1/sessions/{id} and is spec-undefined on the admin envelope too. Do not point clients at it — EVIDENCE: spec/15_external-api-surface.md:674; pkg/gateway/externalapi/admin/sessions.go:63
MISTAKE: Earlier text claimed the terminal cause was readable via GET /v1/sessions/{id} as failureReason and told DOCS-1 readers to "read the session to learn why it ended"; both removed.

### [spec.1.fix-design-G1.1]
DECISION: SPEC-1's trigger becomes state-based ("any request or gateway process other than this finalize call moves the session to a terminal state while finalization is in progress") with no enumeration of writers, plus a precedence clause ("in place of any setup-failure error the call's own prepare phase returns") — BECAUSE the finalizingPrecondition guard is state-based, and every writer list goes stale — ALTERNATIVES: the reviewer's "for example" list (rejected: another partial list of writers inside the normative home, and the next round files it as incomplete again).
FACT: From `finalizing`, §15.1 and §7.2 allow only `ready` (written by the admitted finalize call) or a terminal state, so "not finalizing at the exit write" means "terminal" once the entry guard holds — EVIDENCE: spec/15_external-api-surface.md:646,649; pkg/api/v1/session/session.go:114
FACT: Every terminal writer that can take a `finalizing` row (terminate, DELETE, force-terminate cmd/lenny-gateway/session_admin.go:68-72, orphan reconciler cmd/lenny-gateway/workers.go:1139, budget terminator main.go:272) runs sessionserver.OnSessionTerminal -> terminalReclaimPreRunning (pkg/gateway/sessionserver/usage.go:446,581), so CODE-3's "lost exit write never reclaims" holds for all of them, beyond the three the proposal named.
FACT: The client session response carries `failureClass` only. `failureReason` is absent from spec/15 and spec/07 entirely and is served only on the admin payload in code — EVIDENCE: spec/15_external-api-surface.md:674; pkg/gateway/sessionserver/sessionserver.go:3364; pkg/gateway/externalapi/admin/sessions.go:63
WATCHOUT: Do not "fix" the failureReason claim by pointing clients at GET /v1/admin/sessions/{id}. The spec does not define failureReason on the admin envelope either, so that would be a new unbacked claim. Delete the sentence.
WATCHOUT: orphansession.go:370 comment says the orphan path "only fails post-attached (running) sessions", but its eligibility map includes `finalizing` (orphansession.go:71). This is a stale tree comment and out of scope for 0082 — EVIDENCE: pkg/gateway/session/orphansession/orphansession.go:71,367-371

### [spec.1.review-applicability.1]
FACT: SPEC-1 anchor resolves uniquely: the finalize row of the §15.1 "State-mutating endpoint preconditions" table ends with "...non-retryable `SETUP_COMMAND_FAILED` (422). |" — EVIDENCE: spec/15_external-api-surface.md:646 (preamble at :641, heading `### 15.1 REST API` at :614)
FACT: The link target `06_warm-pod-model.md#62-pod-state-machine` exists (heading `### 6.2 Pod State Machine`) and the FINALIZE_TIMEOUT watchdog rule SPEC-1 relies on is stated there — EVIDENCE: spec/06_warm-pod-model.md:78, :243
FACT: §15.1 terminate/DELETE rows admit `finalizing` (spec/15_external-api-surface.md:649, :654); §7.1 step 23 is "Release credential lease back to pool" (spec/07_session-lifecycle.md:52); §29 finalize forwarding row (spec/29_communication-scenarios.md:549) is unaffected by SPEC-1.
DECISION: no applicability findings on the spec staging — BECAUSE the single append creates no artifact, references only existing anchors, and the named gates (scripts/specshift/name, citation resolver under scripts/specshift/citation) exist — ALTERNATIVES: considered §7.1:60 "410 UPLOAD_TOKEN_CONSUMED" vs 409 for a post-success second finalize; pre-existing and outside SPEC-1's overtaken-call scope, not filed.

### [spec.1.review-citations.1]
FACT: SPEC-1's anchor text is exact. The finalize row's Notes cell ends "A deterministic non-zero setup command surfaces here as the non-retryable `SETUP_COMMAND_FAILED` (422)." EVIDENCE: spec/15_external-api-surface.md:646. The preamble 409 rule is at :641, the terminate row admitting `finalizing` is at :649, and the DELETE row is at :653.
FACT: The §6.2 FINALIZE_TIMEOUT watchdog text sits in the maxSessionAge table inside §6.2, so the anchor `06_warm-pod-model.md#62-pod-state-machine` resolves. EVIDENCE: spec/06_warm-pod-model.md:78 (heading), :243.
WATCHOUT: §7.1 step 23 ("Release credential lease back to pool") is part of the normal-completion sequence after step 22 marks the session completed. It does not state lease release for every terminal transition, although the Design prose reads it that way. I did not file this because the claim is Design rationale and no staged spec text depends on it. EVIDENCE: spec/07_session-lifecycle.md:51-52
WATCHOUT: §7.1 says a finalize request with a consumed upload token returns 410 UPLOAD_TOKEN_CONSUMED, and it says the token is validated on every finalize. That is a pre-existing spec-to-code gap: the proposal's problem statement says handleFinalize does not validate the token. After the first call succeeds, a later finalize could receive either 410 or 409, and no spec text orders the two checks. I did not file it because the gap predates this proposal. EVIDENCE: spec/07_session-lifecycle.md (uploadToken "Single-use invalidation" bullet)
FACT: §7.2 lists no pre-running edges except created → failed for derive-failure rows, so "no edge out of a terminal state" is vacuously true. EVIDENCE: spec/07_session-lifecycle.md:165

### [spec.1.review-client-surface.1]
FACT: Terminal writers that can overtake a `finalizing` session include more than terminate, DELETE, and the watchdog: admin force-terminate (§24.11, POST /v1/admin/sessions/{id}/force-terminate, -> `failed`, FailureReason FORCE_TERMINATED) and tenant suspend drain, which calls the same ForceTerminate. EVIDENCE: cmd/lenny-gateway/session_admin.go:41-66; pkg/gateway/externalapi/admin/tenant_suspend.go:173-192; spec/24_lenny-ctl-command-reference.md:137; spec/15_external-api-surface.md:848
FACT: GET /v1/sessions/{id} exposes `failureClass` only; `failureReason` is not in the §15.1 session response, the OpenAPI Session schema, or toResponse. FINALIZE_TIMEOUT is not a failureClass enum value. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3364-3365; pkg/gateway/externalapi/openapi/openapi.json:126-128; spec/15_external-api-surface.md:674
FACT: The OpenAPI document is at pkg/gateway/externalapi/openapi/openapi.json, not pkg/gateway/openapi/openapi.json as the orchestrator lens text says.
WATCHOUT: MCP parity needs no spec edit. §15.2 "Semantic equivalence" and "Shared error taxonomy" (spec/15_external-api-surface.md:1418-1422) already carry the REST 409 to finalize_workspace. EVIDENCE: spec/15_external-api-surface.md:1418
DEFERRED [non-spec-changes.md DOCS-1]: if SPEC-1 widens its writer list to include admin force-terminate, the DOCS-1 rest.md paragraph (non-spec-changes.md:171) that names only terminate, DELETE, and the finalizing timeout must widen the same way.

### [spec.1.review-docs-alignment.1]
FACT: SPEC-1's anchor sentence ("A deterministic non-zero setup command surfaces here as the non-retryable `SETUP_COMMAND_FAILED` (422).") is the verbatim end of the finalize row's Notes cell, and the §6.2 anchor `06_warm-pod-model.md#62-pod-state-machine` resolves to "### 6.2 Pod State Machine". EVIDENCE: spec/15_external-api-surface.md:646, spec/06_warm-pod-model.md:78
FACT: No other spec site states a finalize-specific overtaken outcome; the §15.4 INVALID_STATE_TRANSITION catalog row is generic and the §15.2 finalize_workspace tool row carries no error text, so SPEC-1 needs no companion spec edit. EVIDENCE: spec/15_external-api-surface.md:1011, spec/15_external-api-surface.md:1329
WATCHOUT: §7.1 says finalize requests carry the uploadToken and a consumed token returns 410 UPLOAD_TOKEN_CONSUMED, but handleFinalize validates no token at all; a second finalize after the winner reaches `ready` therefore gets 409 in code while the spec leaves 409-vs-410 precedence unstated. Pre-existing and not changed by 0082; not filed. EVIDENCE: spec/07_session-lifecycle.md:60, spec/07_session-lifecycle.md:446, pkg/gateway/sessionserver/sessionserver.go:3068-3080
OPEN: whether the 409-vs-410 precedence for a post-consume finalize belongs in a follow-up finding alongside the other shipped-tree defects.

### [spec.1.review-edit-sites.1]
FACT: terminate, DELETE, and the finalizing watchdog are not the only writers that can make a `finalizing` session terminal. The orphan session reconciler lists `finalizing` by name and forces it to `failed`/`orphan_pod_terminated`. Admin force-terminate, the §11.4 user full-revoke cancellation, and the §12.8 tenant-disable session termination also exist. — EVIDENCE: spec/10_gateway-internals.md:59; spec/15_external-api-surface.md:888; spec/11_policy-and-controls.md:270; spec/12_storage-architecture.md:913
WATCHOUT: any sentence (SPEC-1, DOCS-1 rest.md paragraph, the Design/edge-case bullets) that lists which terminal writers can overtake finalize must say "any terminal writer, for example ...". A closed list of three leaves the orphan-reconciler case without a stated outcome. — EVIDENCE: spec-changes.md SPEC-1 block
UNVERIFIED: §7.1 says a consumed uploadToken returns 410 UPLOAD_TOKEN_CONSUMED, and the proposal expects 409 with currentState=ready for an overlapping loser that arrives after the winner's ready write. Nobody has checked the order in which the finalize route checks the token and the state. Non-spec reviewers should check the middleware. — EVIDENCE: spec/07_session-lifecycle.md:60

### [spec.1.review-fresh.1]
FACT: A finalizing session has terminal writers beyond terminate, DELETE, and the watchdog: admin force-terminate (and tenant suspend, which calls it) writes failed/FORCE_TERMINATED under the row lock, and the §10.1 orphan session reconciler forces finalizing -> failed. — EVIDENCE: cmd/lenny-gateway/session_admin.go:41-66; pkg/gateway/externalapi/admin/tenant_suspend.go:192; spec/10_gateway-internals.md:59; spec/24_lenny-ctl-command-reference.md:137
WATCHOUT: Overlapping same-Idempotency-Key calls are not replayed while the first is in flight; the middleware answers 409 IDEMPOTENCY_KEY_IN_FLIGHT, and §11.5 is silent on in-flight keys (code only). — EVIDENCE: pkg/gateway/middleware/idempotency/idempotency.go:351-371; spec/11_policy-and-controls.md:283
WATCHOUT: The spec says a finalize carrying a consumed upload token gets 410 UPLOAD_TOKEN_CONSUMED (§7.1 single-use invalidation, §7.4 upload authorization), which competes with the preamble 409 for a loser that finds `ready`; handleFinalize does not validate the token today. — EVIDENCE: spec/07_session-lifecycle.md:60, :446; spec/15_external-api-surface.md:1123
FACT: The SPEC-1 anchor sentence matches spec/15_external-api-surface.md:646 verbatim; no other open proposal edits that Notes cell (0081 edits only the error-catalog row). — EVIDENCE: spec/15_external-api-surface.md:646

### [spec.1.review-kubernetes.1]
FACT: The staged spec edit (SPEC-1, one sentence in the §15.1 finalize row Notes cell) touches only Postgres-backed session state; no CRD, status subresource, finalizer, or admission webhook is involved, so the Kubernetes-idiom lens has no surface on it — EVIDENCE: 0082 spec-changes.md:26-30; spec/15_external-api-surface.md:646
FACT: The maxFinalizingTimeoutSeconds watchdog is a gateway goroutine rather than a controller reconcile, so SPEC-1 naming it as a terminal writer puts no controller on the request path — EVIDENCE: spec/18_build-sequence.md:223; spec/06_warm-pod-model.md:243

### [spec.1.review-mechanism.1]
FACT: More writers than terminate, DELETE, and the finalizing watchdog can move a `finalizing` row to a terminal state. The spec's §10.1.4 orphan session reconciler lists `finalizing` and forces `failed` with `orphan_pod_terminated`. §15.1 also has admin force-terminate. In code, the extra writers are pkg/gateway/session/orphansession/orphansession.go:71,346 and pkg/gateway/externalapi/admin/users.go:457-461 (the user-erasure cancel). — EVIDENCE: spec/10_gateway-internals.md:59, spec/15_external-api-surface.md:888
WATCHOUT: SPEC-1 lists three terminal writers as a closed set. CODE-3's finalizingPrecondition refuses on any non-`finalizing` state, whatever wrote it. A fix should state the rule by resulting state ("another writer moves the session to a terminal state") and give the writers only as examples. — EVIDENCE: spec-changes.md SPEC-1 block; non-spec-changes.md:14
DEFERRED [non-spec-changes.md]: line 14 ("another writer (terminate, DELETE, or the watchdog)") and DOCS-1 text at line 171 list only three writers. They must follow SPEC-1 if SPEC-1 is generalized to any terminal writer.

### [spec.1.review-performance.1]
DECISION: no performance or failure-mode finding on the staged spec edits — BECAUSE SPEC-1 is one response-outcome sentence in the §15.1 finalize row Notes cell; it adds no store write, no watch, no lock surface, and no counter. The guarded Updates it implies reuse the existing per-session row lock (no hot key beyond one row per session) and run the pod RPCs outside the lock — ALTERNATIVES: filing Postgres-failover or ambiguous-commit outcomes against SPEC-1 was rejected, because the sentence only governs the terminal-writer-overtook case and a store error keeps its existing 5xx path (non-spec concern).
FACT: SPEC-1 anchors resolve: finalize precondition row is spec/15_external-api-surface.md:646 and ends with the quoted SETUP_COMMAND_FAILED sentence; §6.2 watchdog text (default 600s, failed/FINALIZE_TIMEOUT) is spec/06_warm-pod-model.md:243 under heading "6.2 Pod State Machine" (line 78) — EVIDENCE: spec/15_external-api-surface.md:646, spec/06_warm-pod-model.md:243

### [spec.1.review-reliability.1]
FACT: More terminal writers reach `finalizing` than the three SPEC-1 names (terminate, DELETE, watchdog). The §10.1 orphan session reconciler lists `finalizing` in its eligible set and writes `failed`/`orphan_pod_terminated`. Admin force-terminate writes `failed` from any state. Tenant deletion step 2 force-terminates sessions. EVIDENCE: spec/10_gateway-internals.md:59; pkg/gateway/session/orphansession/orphansession.go:67-73; spec/24_lenny-ctl-command-reference.md:137; spec/15_external-api-surface.md:888; spec/12_storage-architecture.md:913
WATCHOUT: If SPEC-1 lists only some terminal writers, a pod that dies mid-Prepare (the orphan reconciler case) falls back to §7.1's rule, where a transient setup failure returns SESSION_CREATION_FAILED at finalize. CODE-3 returns 409 in that case, so the spec and the code disagree. Word the SPEC-1 trigger as "any terminal state" and give the writers only as examples. EVIDENCE: spec/07_session-lifecycle.md:208
FACT: The watchdog's finalizing sweep is itself a compare-and-swap inside Update (`if r.State != st { return nil }`), and it uses UpdatedAt as the state-entry time. EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:609-627,1351-1357

### [spec.1.review-security.1]
FACT: SPEC-1's anchor cell and the neighbouring rows are verified in place: the finalize row is at spec/15_external-api-surface.md:646, the terminate row at :649 (its text "aborts the in-progress setup ... releases the pod"), and the DELETE row at :653. The preamble's 409 rule is at :641. The watchdog -> failed/FINALIZE_TIMEOUT rule is at spec/06_warm-pod-model.md:243 under "### 6.2 Pod State Machine" (:78). EVIDENCE: the lines cited.
WATCHOUT: under the security lens, the live exposure in this area is pre-existing and not introduced by SPEC-1. Terminate does not abort a running Prepare on another replica, so the loser's AssignCredentials can land after the terminal writer has released the pod. spec/13_security-model.md:30 relies on "each session's lease is released before the next session begins" for recycled pods. The proposal records this under "Defects in the shipped tree" (the terminate-abort bullet), and SPEC-1 is neutral on abort, so it does not contradict the terminate row. A later proposal that implements the abort should cite 13:30. EVIDENCE: spec/13_security-model.md:30, spec/15_external-api-surface.md:649
UNVERIFIED: the design paragraph says "§7.1 step 23 releases the credential lease on a terminal transition" and treats that as the spec basis for CODE-3's release of a lease assigned after the terminal write. Step 23 (spec/07_session-lifecycle.md:52) is a normal-flow step. The idempotent terminal-handling text at 07:216/224 is the closest general statement. I judged this adequate and filed nothing. A human should decide whether a lease assigned after a terminal transition needs explicit text.

### [spec.1.review-single-source.1]
FACT: The overtaken-finalize outcome (409 with terminal currentState) is stated nowhere in spec/ outside SPEC-1; §6.2 watchdog row, §7.1, §7.2, and §29 contain no finalize-response text, so SPEC-1 is the single normative home. EVIDENCE: spec/06_warm-pod-model.md:243, spec/15_external-api-surface.md:646
FACT: The derive_failure reachability table in §15.1 lists /finalize 409 on a terminal-from-birth row; it is a different rule (never admitted) from SPEC-1's admitted-then-overtaken call, so it is not a copy. EVIDENCE: spec/15_external-api-surface.md:685
DECISION: no single-source findings on the spec staging — BECAUSE SPEC-1 cites §6.2 for the watchdog and mentions terminal retention in a clause rather than restating §7.2 — ALTERNATIVES: filing DOCS-1 as a copy rejected (reader-facing restatement allowed, and out of this loop's scope)

### [spec.2.review-client-surface.1]
FACT: The served OpenAPI document is pkg/gateway/externalapi/openapi/openapi.json (not pkg/gateway/openapi/). Its finalize operation already declares a 409 "Invalid state transition" response, so SPEC-1 adds no new status code, field, or error code to any client representation. EVIDENCE: pkg/gateway/externalapi/openapi/openapi.json:750-757
WATCHOUT: SPEC-1's closing clause "in place of any setup-failure error the call's own prepare phase returns" is narrower than CODE-3, which returns the 409 for every lost failure write. That includes plan-parse (500), consume (500), and prepare-phase credential errors (CREDENTIAL_POOL_EXHAUSTED via mapFinalizeCredentialMismatch). The main clause is unconditional, so I judged this a wording matter and did not file it. EVIDENCE: pkg/gateway/sessionserver/finalize.go:246-260; non-spec-changes.md:154-158

### [spec.2.review-edit-sites.1]
DECISION: no edit-site finding on the round-2 SPEC-1 rewrite — BECAUSE the new "in place of any setup-failure error" clause qualifies only the finalize row; spec/06_warm-pod-model.md:290 defers finalize failures to "the §15.1 finalize precondition note", and the error catalog row (spec/15_external-api-surface.md:1136) says "Surfaced wherever the gateway runs setup commands", which stays true — ALTERNATIVES: filing spec/07_session-lifecycle.md:208 (lists SETUP_COMMAND_FAILED at finalize with no superseded-call exception) as a missed site; rejected, since that sentence routes pre-attached failures to endpoints and does not cover a session another writer has already ended.
FACT: the SETUP_COMMAND_FAILED-at-finalize statements in spec/ are at 15:646 (the SPEC-1 home row), 15:1136 (error catalog), 07:208 (§7.1 pre-attached visibility), and 06:290 (§6.2, which defers to the 15.1 note). docs/reference/error-catalog.md:129 mirrors 15:1136 — EVIDENCE: grep -rn SETUP_COMMAND_FAILED spec/ docs/

### [spec.2.review-fresh.1]
- WATCHOUT: SPEC-1's precedence clause names "setup-failure error", but the finalize prepare phase returns many non-setup errors (CREDENTIAL_POOL_EXHAUSTED, POOL_WARMING, INVALID_POOL_PROXY_DIALECT, credential-delivery-isolation 422, RUNTIME_LEVEL_UNDERPERFORMS, retryable fallback). CODE-3 rule 2 answers 409 for all of them when the failure write is lost. Keep the SPEC-1 qualifier as broad as CODE-3 rule 2 — EVIDENCE: pkg/gateway/sessionserver/start.go:87-127 (writePodClaimError cases); spec/15_external-api-surface.md:646 (row separates credential failure from setup-command failure)
- FACT: round-2 diff touched only the SPEC-1 sentence, the first Edge-cases bullet, summary line "answers as SPEC-1 states", CODE-1 comment, and DOCS-1(a) paragraph — EVIDENCE: diff against scratchpad/cp-snap/0082/spec-r1-prefix

### [spec.2.review-mechanism.1]
FACT: every writer that can end a `finalizing` session (terminate, DELETE, admin force-terminate, watchdog, orphan session reconciler) runs in the gateway, so SPEC-1's "any request or gateway process" covers all of them — EVIDENCE: spec/10_gateway-internals.md:59; pkg/gateway/session/orphansession/orphansession.go
DECISION: did not file on the trailing clause "in place of any setup-failure error" being narrower than CODE-3's supersession (which also covers credential errors, the plan-parse 500 and the token-consume 500) — BECAUSE the main clause of SPEC-1 mandates 409 unconditionally for an overtaken call, so the trailing clause is illustrative and cannot license a different response — ALTERNATIVES: filing it as predicate drift; rejected as below the materiality bar

### [spec.2.review-reliability.1]
FACT: Only gateway code writes terminal session states (completed/failed/cancelled/expired); outside pkg/gateway the constants appear only in pkg/api/v1/session and in read-only consumers. So SPEC-1's "any request or gateway process" covers every terminal writer. EVIDENCE: grep of StateFailed|StateCancelled|StateCompleted|StateExpired over pkg/ excluding pkg/gateway and tests
DECISION: no reliability findings in round 2 — BECAUSE the diff only widens SPEC-1's writer set and drops the failureReason claim; the widened sentence still makes the 409 unconditional for a superseded call, and the "in place of any setup-failure error" clause does not narrow it — ALTERNATIVES: filing the narrow reading of "setup-failure error" against a credential or SESSION_CREATION_FAILED prepare error was rejected as wording, because the main clause already fixes the response

### [spec.3.review-applicability.1]
FACT: The round-2 SPEC-1 rewrite dropped the §6.2 link and names no writer, so the staged sentence references no anchor, heading, or identifier. Its only anchor, the finalize row's closing sentence, still matches spec/15_external-api-surface.md:646 verbatim and nowhere else in the file — EVIDENCE: spec/15_external-api-surface.md:646
DECISION: no applicability findings on the spec staging — BECAUSE SPEC-1 is a single append that creates no artifact, the sentence contains no reserved N3 phrase for the naming lint, and checklist S1 (tiers 0 and 11, no Depends-on) comes before every code step — ALTERNATIVES: considered whether "setup-failure error" narrows the trigger (already refuted in round 2, not re-filed).
USEFUL [spec.1.review-applicability.1]: its anchor-uniqueness check still holds after the round-2 rewrite. Only the link fact is obsolete.

### [spec.3.review-citations.1]
FACT: SPEC-1 anchor sentence "A deterministic non-zero setup command surfaces here as the non-retryable `SETUP_COMMAND_FAILED` (422)." is verbatim the last sentence of the finalize row Notes cell — EVIDENCE: spec/15_external-api-surface.md:646
FACT: Design citations all verified: preamble 409 with currentState+allowedStates (spec/15_external-api-surface.md:641), terminate row admits finalizing -> completed (:649), DELETE any non-terminal -> cancelled (:653), FINALIZE_TIMEOUT in §6.2 finalizing row (spec/06_warm-pod-model.md:243), §7.1 step 23 lease release (spec/07_session-lifecycle.md:52), no out-edge from a terminal state in §7.2.
WATCHOUT: §7.2 edge enumeration lists no `created → finalizing` or `finalizing → *` edges at all (grep finds none); do not cite §7.2 for finalizing transitions — EVIDENCE: spec/07_session-lifecycle.md:160-200

### [spec.3.review-client-surface.1]
FACT: the OpenAPI document lives at pkg/gateway/externalapi/openapi/openapi.json, not pkg/gateway/openapi/openapi.json as the orchestrator's standing note says. EVIDENCE: find output; there is no file at pkg/gateway/openapi/openapi.json
DECISION: no spec-lane client-surface finding. BECAUSE SPEC-1 adds no new code, field, or enum value: it reuses INVALID_STATE_TRANSITION (spec/15_external-api-surface.md:1011, "not valid for the current resource state"), and the preamble at :641 still requires details.allowedStates, so SPEC-1 naming only currentState narrows nothing. The MCP finalize_workspace tool (:1329) takes REST error parity from §15.2 without a spec edit. ALTERNATIVES: filing the OpenAPI finalize 409 response or the docs/api/mcp.md:296 INVALID_STATE_TRANSITION description was rejected, because both are non-spec remedies that the next loop owns.

### [spec.3.review-docs-alignment.1]
FACT: The SPEC-1 anchor sentence ("A deterministic non-zero setup command surfaces here as the non-retryable `SETUP_COMMAND_FAILED` (422).") is the final sentence of the finalize row Notes cell today — EVIDENCE: spec/15_external-api-surface.md:646
FACT: The MCP counterpart `finalize_workspace` (spec/15:1329) carries no per-tool error text in spec, so SPEC-1 needs no MCP-side spec twin; docs/api/mcp.md is covered by DOCS-1(b) — EVIDENCE: spec/15_external-api-surface.md:1329
DECISION: no spec-scoped docs-alignment findings in round 3 — BECAUSE both edge-case rows resolve to landing spec text (SPEC-1 and the §15.1 preamble), and the SETUP_COMMAND_FAILED catalog row (spec/15:1136) says where the code is surfaced, not that it always is, so SPEC-1's override does not contradict it — ALTERNATIVES: filing the catalog row as an unstaged site; rejected as wording.

### [spec.3.review-edit-sites.1]
FACT: SPEC-1 adds no identifier (no field, code, metric, alert, flag); it reuses INVALID_STATE_TRANSITION and details.currentState from the §15.1 preamble. The edit-site sweep therefore reduces to spec sentences that state finalize's response or its failure handling — EVIDENCE: spec/15_external-api-surface.md:641, :646
FACT: The other spec sites that describe finalize errors defer to the §15.1 finalize row, so SPEC-1 carries through to them: §6.2 client visibility ("per the §15.1 finalize precondition note", spec/06_warm-pod-model.md:290), §29.2 step 19 ("Finalize returns to the client only once the session is ready", spec/29_communication-scenarios.md:258), and the §29 forwarding row (spec/29_communication-scenarios.md:549). None of them states an unconditional error for the overtaken case — EVIDENCE: the cited lines
WATCHOUT: §4.9 says an assignment mismatch at /finalize "releases the claimed pod back to the warm pool and returns CREDENTIAL_POOL_EXHAUSTED" (spec/04_system-components.md:1240). That sentence covers the general case, and SPEC-1's "in place of any setup-failure error" clause overrides it for an overtaken call. I judged this precedence and not a contradiction. Do not refile it without new evidence — EVIDENCE: spec/04_system-components.md:1240

### [spec.3.review-fresh.1]
FACT: SPEC-1 anchor text matches the tree; the finalize row's Notes cell ends with "...non-retryable `SETUP_COMMAND_FAILED` (422). |" — EVIDENCE: spec/15_external-api-surface.md:646
FACT: The only other spec sites naming finalize error outcomes defer to or are generic relative to the §15.1 finalize note: §6.2 pre-attached "Client visibility" says finalize returns errors "per the §15.1 finalize precondition note" (spec/06_warm-pod-model.md:290); the §7.2 pre-attached paragraph (spec/07_session-lifecycle.md:208) and the SETUP_COMMAND_FAILED catalog row (spec/15_external-api-surface.md:1136) name which endpoint surfaces a failure, and SPEC-1's "in place of" clause governs precedence, so neither needs an edit.
FACT: §29 off-holder table (spec/29_communication-scenarios.md:545-549) handles terminate/DELETE in `finalizing` locally (no coordinator) and does not state a finalize response on overtaking, so SPEC-1 creates no conflict there.
FACT: The finalizing watchdog is a separate goroutine (pkg/gateway/runtime/watchdog/watchdog.go:48), so it falls under SPEC-1's "request or gateway process other than this finalize call".
DECISION: returned no findings for round 3 — BECAUSE the reworded SPEC-1 sentence is consistent with §15.1 preamble, terminate/DELETE rows, §6.2:243, §6.2:290, and §29 — ALTERNATIVES: filing §7.2:208 as a missed edit site, rejected because it states the surfacing endpoint, not precedence over a concurrent terminal writer.

### [spec.3.review-kubernetes.1]
FACT: Every writer that can end a `finalizing` session writes the Postgres session row from a gateway process: terminate, DELETE, the watchdog, and the orphan session reconciler, which runs leader-only inside the gateway. SPEC-1's "any request or gateway process" wording therefore covers them all, and no controller writes session state. EVIDENCE: spec/10_gateway-internals.md:59
DECISION: The Kubernetes-idiom lens has no finding on SPEC-1 — BECAUSE the staged edit is one REST-response sentence in the §15.1 finalize row, and the mechanism is a Postgres row lock (sessionstore.Update). It touches no CRD status, field manager, finalizer, webhook, or controller reconcile on the request path — ALTERNATIVES: flagging the non-spec podclaim.DeleteClaim no-owner-check note was rejected because it is out of scope for the spec loop and the design avoids calling it.

### [spec.3.review-mechanism.1]
FACT: The FINALIZE_TIMEOUT watchdog is a separate goroutine (pkg/gateway/runtime/watchdog/watchdog.go:48, :1383), so SPEC-1's "any request or gateway process other than this finalize call" covers it; the finalize call's own failure write is the only terminal write the clause excludes. EVIDENCE: spec/06_warm-pod-model.md:243
FACT: The §15.1 terminate row already says the gateway "aborts the in-progress setup" for `finalizing` (spec/15_external-api-surface.md:649). An aborted prepare error followed by a lost failure write falls under SPEC-1's 409. The terminate row and SPEC-1 agree. EVIDENCE: spec/15_external-api-surface.md:646,649,653
DECISION: I filed no finding on the round-2 SPEC-1 rewrite — BECAUSE the writer-agnostic clause matches the exit-guard mechanism (finalizing-only precondition). It leaves the window after the ready write outside "in progress", and the §7.1 step 23 (spec/07_session-lifecycle.md:52) and §6.2 citations in the Design section resolve — ALTERNATIVES: I considered filing the lack of a `finalizing → cancelled/completed` edge in the §7.2 list, but it predates this proposal and SPEC-1 does not touch it.

### [spec.3.review-performance.1]
DECISION: no performance or failure-mode findings on the staged spec edits — BECAUSE SPEC-1 is one response-contract sentence in the §15.1 finalize Notes cell and adds no store write, watch, key, or lease; the guards reuse the existing locked sessionstore Update, so per-session write count is unchanged — ALTERNATIVES: filing Postgres-failover or gateway-crash-mid-finalize behavior, rejected because the spec text is silent there today, SPEC-1 leaves it unchanged, and any remedy would be code rather than a staged spec edit
FACT: the §15.1 finalize row that SPEC-1 appends to is at spec/15_external-api-surface.md:646 as of this round — EVIDENCE: spec/15_external-api-surface.md:646

### [spec.3.review-reliability.1]
DECISION: no reliability findings in round 3 — BECAUSE the spec staging is unchanged since round 2 (the diff against spec-r1-prefix is the round-2 SPEC-1 rewrite, the edge-case bullet, and the non-spec DOCS-1/CODE-1 propagation), and SPEC-1 adds no retry, lease, store write, or recovery path of its own. A finalize handler that crashes mid-prepare leaves the row `finalizing` until the §6.2 watchdog fails it, and that recovery predates this proposal — ALTERNATIVES: filing gateway-crash or Postgres-failover outcomes against SPEC-1; rejected because SPEC-1 governs only the overtaken-by-terminal-writer case, and store errors keep their existing path
USEFUL [spec.2.review-reliability.1]: the fact that only gateway code writes terminal states settled "any request or gateway process" coverage without re-grepping

### [spec.3.review-security.1]
FACT: SPEC-1 changes only the finalize call's response when a terminal writer overtakes it; it adds no path that writes a pod, lease, or claim, so no established control (pod reuse, lease release, RBAC) is touched by the spec staging. Lease release on terminal is already normative at §7.1 step 23 — EVIDENCE: spec/07_session-lifecycle.md:52
FACT: Pre-running terminal reclaim deletes the claim by name and revokes the session-keyed lease; a finalize that assigns a lease after that reclaim is covered by CODE-3's revoke, not by any spec text — EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605
FACT: SPEC-1 anchor sentence ("A deterministic non-zero setup command surfaces here ...") exists verbatim at the end of the finalize Notes cell — EVIDENCE: spec/15_external-api-surface.md:646

### [spec.3.review-single-source.1]
FACT: The overtaken-finalize outcome (terminal writer wins, finalize returns 409 with terminal currentState) is stated nowhere in spec/ before SPEC-1; grep of spec/ for FINALIZE_TIMEOUT, overtaken or superseded finalize finds only the §6.2 watchdog row, §11.3 default and §18 watchdog deliverable, none of which state the finalize call's response — EVIDENCE: spec/06_warm-pod-model.md:243, spec/15_external-api-surface.md:646
DECISION: No single-source finding on the spec staging — BECAUSE SPEC-1 is the only stating site in the staged spec edits, and the Design and Edge-case bullets cite the preamble, terminate/DELETE rows, §6.2 and §7.1 step 23 by name rather than restating them — ALTERNATIVES: filing the Design bullets as copies was rejected because they name and cite their homes
WATCHOUT: Any restatement of the SPEC-1 outcome in non-spec CODE-3 rules or TEST-1 cases is a non-spec-loop matter; the spec home is already SPEC-1 — EVIDENCE: proposals/0082_.../0082_...spec-changes.md:29

### [reconcile.1]
FACT: The deliverable index and the implementation checklist match the converged staging: SPEC-1 in spec-changes.md; CODE-1, CODE-2, CODE-3, DOCS-1, and TEST-1 through TEST-4 in non-spec-changes.md. No schema, chart, or migration deliverable is staged. The checklist already names each deliverable once, leads with the single spec step S1, keeps one lane per step, and every code step depends on S1, so it was left unchanged. The index gained full paths for CODE-3 and the optional `eager_claim_lifecycle_test.go` site for TEST-3.
CORRECTS [spec.1.review-client-surface.1]: closed the DEFERRED DOCS-1 entry. SPEC-1 is now writer-agnostic, and the DOCS-1 rest.md paragraph (non-spec-changes.md DOCS-1 (a)) already lists its writers as "for example" and includes administrator force-terminate, so the statement is no longer false. No edit was needed.
CORRECTS [spec.1.review-mechanism.1]: closed the DEFERRED entry on non-spec-changes.md line 14 and the DOCS-1 text. The Design exit-guard bullet now reads "another writer already moved the row to a terminal state" with no writer list, the CODE-1 comment gives its writers as "for example", and DOCS-1 (a) does the same. No edit was needed.
FACT: The unclosed OPEN in [spec.1.review-docs-alignment.1] (409-versus-410 precedence for a post-consume finalize) is a scope decision. It is now carried in the summary's "Open decisions for human to make" with no recommendation, because the spec loop derived none.

### [f1.human-decisions]
DECISION: Resolved the open decision on 409-versus-410 precedence for a post-consume finalize by recording the gap. summary.md "Defects in the shipped tree that this proposal does not stage" gained the bullet "handleFinalize validates no upload token.", and the entry left "Open decisions for human to make", which now reads "This proposal carries no open decisions." — BECAUSE §7.1 and §7.4 require token validation on finalize (spec/07_session-lifecycle.md:58-60, :446), handleFinalize verifies no token (pkg/gateway/sessionserver/sessionserver.go:3065-3083, :3190), only upload.go:824-828 verifies it, and no BUILD-GAPS finding records the gap.
FACT: TEST-GAPS.md T-7.1.4 (OPEN) assumes /finalize enforces 401/403/410 and would fail against the current handler; BUILD-GAPS.md F-7.1.20 wrongly states /finalize returns UPLOAD_TOKEN_EXPIRED. The new bullet cross-references both.

### [nonspec.1.fix.1]
CORRECTS [nonspec.1.review.1] "TEST-1 fixture citation ranges do not contain podBindClient": non-spec-changes.md TEST-1 Targets, first bullet, now cites `start_pod_test.go:63-210`, which spans `podBindRuntime` (:63), `podBindClient` (:92), and `podBindWarmPool` through `podBindBinder` (:160-202). The helper list gained `podBindRuntime`, and the bullet states that each adapter-backed case assigns `&podBindRuntime{}` to the adapter's `Runtime`, as `TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1` does at :1179 (the review cited :1178). The `:1142-1162` and `:1175-1266` ranges were re-verified and are unchanged.
FACT: This pass opens the non-spec round 1 fixer subsection, because the review log held no earlier subsection for that round.
FACT: Citer sweep over `podBind`, `recordingLeaseAssigner`, and `start_pod_test.go` in every proposal file found no other site that lists the fixture's helpers or cites the `:160-210` range. summary.md "Watch out for" names "TEST-1's `finalize_race_test.go` fixture" without listing its parts, so it stays true.

### [non-spec.1.fix-G1.1]
DECISION: CODE-3 rule 4's Gap-2 failFinalizing result splits three ways: (i) nil or store error -> reclaim when prep != nil, 500; (ii) lost with session.IsTerminal(pe.CurrentState) -> revokeFinalizeLease when prep != nil, no reclaim, 409; (iii) lost with non-terminal state (own ambiguous ready commit) -> no revoke, no reclaim, existing 500 — BECAUSE the lease store is per-replica so only this handler can release a lease assigned after the terminal writer's ReleaseSession, and revoking in (iii) strips a live session's credential — ALTERNATIVES: see WATCHOUTs below.
WATCHOUT: Rejected answering Gap-2 sub-branch (iii) with 409 currentState=ready (reports the call's own success as a conflict and needs new SPEC-1/DOCS-1 text) or with 200 via a replayed success tail (re-read plus duplicate status/audit emission from a recovery branch). Recorded in summary Non-goals. Do not reopen either without a spec change — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3167-3181
WATCHOUT: A lost exit write does not always mean a terminal writer. The Gap-2 failure write can lose to the call's own committed `ready`. Do not reintroduce "lost means terminal" or a uniform per-branch revoke statement at any site other than CODE-3's "handleFinalize rules, in handler order" — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3030 (transitionReady is the only writer of ready)
FACT: The credential lease store is in-memory and per-replica; a terminal writer's ReleaseSession on another replica cannot release a lease issued here — EVIDENCE: pkg/gateway/credentials/credleasestore/credleasestore.go:9-10
FACT: The lost-write response now has one home: CODE-3's rules list, headed by a lead-in sentence carrying the DeleteClaim no-owner-check rationale. The Design exit-guard bullet, summary Decisions and Watch-out lines, and the edge-case bullets only point at it.
UNVERIFIED: the design says G2's TEST-1 cases (c) and (d) pin Gap-2 sub-branches (ii) and (iii) at tier 1 with prep != nil. This fix added no test. The next reviewer should confirm those cases exist after G2 lands and that they assert the lease and claim outcomes.

### [non-spec.1.fix-G2.1]
FACT: Tier 1 reaches prep != nil. The external-package pod-bind fixture (podBindAdapterDialer over bufconn plus podBindBinder and recordingLeaseAssigner) runs Prepare to success and records AssignProto and ReleaseSession — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:187-210, :1142-1162, :1175-1266
DECISION: TEST-1 split into finalize_race_test.go (package sessionserver_test, every hookStore case on the pod-bind fixture) and finalize_race_internal_test.go (finalizingPrecondition and failSession unit cases only) — BECAUSE only the external fixture makes lease and claim assertions non-vacuous — ALTERNATIVES: tier-4 Cases C/D (needs envtest for cluster-free behavior); adding to start_pod_test.go (mixes concerns)
WATCHOUT: TEST-1 hooks must commit terminal states by direct write to the inner store. A DELETE through handleDelete runs recordSessionCompleted, which emits the terminal lifecycle and runs terminalReclaimPreRunning (ReleaseSession plus claim delete) on the same recording assigner and fake client, so the handler's own revoke/reclaim cannot be isolated — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:2958; usage.go:428, :446; podsession/binder.go:1097-1102
WATCHOUT: Do not select the hooked Update by position. applyFinalizePrepareResult adds an Update only when prep != nil, so an Nth-Update hook fires on the wrong write in the adapter fixture. Select by the probed written State plus occurrence, as readyTransitionFailStore does — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1123-1136; sessionserver.go:3151-3153
MISTAKE: Earlier rounds stated "at tier 1 prep is always nil" in the summary and TEST-1, which left every prep != nil lost branch (rules 3, 4, Gap-2) untested and made TEST-3 Case B's permissive "contains the session ID" the only lease pin, which passes with revokeFinalizeLease removed.
FACT: Prepare failure inside the pod-bind fixture is injected with a setup command that exits non-zero (TestFinalizeFailsSessionAndReclaimsPodOnSetupError_spec_7_5); the old "no matching pool" injection cannot work there because /create claims the pod from the pool — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:773-866

### [non-spec.1.fix-design-G1.1]
DECISION: CODE-3 rule 4's Gap-2 lost branch splits on session.IsTerminal(pe.CurrentState): terminal -> revokeFinalizeLease when prep != nil, no reclaim, 409 via writePreconditionError; non-terminal (own ready commit landed, possibly moved on by /start) -> no revoke, no reclaim, existing 500 — BECAUSE only this branch can find a non-terminal row (transitionReady is the sole ready writer) and 500 matches the ambiguous-entry-commit treatment, so no new client-visible 409-with-ready outcome and no SPEC-1/DOCS-1 change — ALTERNATIVES: 409 currentState=ready (reports own success as conflict; needs spec/docs text), success tail + 200 (re-read + replayed tail from a recovery branch).
DECISION: CODE-3's rules list is the single home for the lost-write response; its lead-in carries the one "no lost branch reclaims" sentence and the DeleteClaim rationale. Design bullet (non-spec :14), summary :11/:19/:20/:30 and Edge case :288 become pointers.
FACT: tier-1 CAN run Prepare to success: pkg/gateway/sessionserver/start_pod_test.go:1175-1188 (package sessionserver_test) wires an in-process adapter plus recordingLeaseAssigner, and TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 asserts the Gap-2 revoke at tier 1 — EVIDENCE: start_pod_test.go:1175-1262
WATCHOUT: summary.md Watch-out "At tier 1, Prepare cannot succeed without an adapter, so prep is always nil" and non-spec TEST-1 fixture bullet repeat that claim; it is false for the external test package. Not fixed in G1 (separate finding). G1 put the lease assertions in TEST-3 to avoid depending on it.
WATCHOUT: TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 (start_pod_test.go:1175) must keep passing: unraced Gap-2 -> failFinalizing commits -> reclaim + 500 + failed. It pins rule 4's committed sub-branch.

### [non-spec.1.fix-design-G2.1]
FACT: tier 1 reaches prep != nil. Package sessionserver_test (no build tag) runs Binder.Prepare to success through an in-process adapter over bufconn, and records AssignProto and ReleaseSession. EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:187-210 (podBindAdapterDialer, podBindBinder), :1142-1162 (recordingLeaseAssigner), :1175-1266 (TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1)
FACT: Options exposes Events, LifecycleAuditSink, InteractionAuditSink, and UploadTokenVerifier, so an external-package test can observe SSE, audit, and lifecycle output and can inject a failing consume through uploadtoken.NewVerifier(ring, failingTracker, clock). EVIDENCE: pkg/gateway/sessionserver/sessionserver.go Options; pkg/uploadtoken/uploadtoken.go:189-193,322,363-367
WATCHOUT: a hook keyed on "the Nth Update" shifts between fixtures, because applyFinalizePrepareResult issues its own store Update only when prep != nil. Select the hooked Update by the State its mutation writes when probed on a copy, as readyTransitionFailStore does. EVIDENCE: pkg/gateway/sessionserver/finalize.go:351-353; start_pod_test.go:1123-1135
WATCHOUT: committing DELETE through handleDelete runs recordSessionCompleted, which emits the terminal lifecycle and, for a `finalizing` row with PodAssignment, calls ReclaimClaimed (ReleaseSession plus DeleteClaim). A test that checks what the finalize handler alone did has to commit the terminal state by a direct write to the inner store. EVIDENCE: sessionserver.go:2958; usage.go:428,446,581-596; binder.go:1097-1102
DECISION: TEST-1 is split into an external file, finalize_race_test.go (package sessionserver_test), and an internal file. The external file holds every hook case in one hookStore and uses the podBind adapter fixture. The internal file keeps only the unit cases on unexported symbols. BECAUSE: the lost-branch revoke and reclaim assertions pass vacuously when prep is nil, and one hookStore avoids a second copy. ALTERNATIVES: add the external cases and keep hookStore in the internal file as well, which defines two hook stores; export hookStore via export_test.go, which is unnecessary because no case needs internals.
DECISION: revokeFinalizeLease is pinned by the external ready-write-loss case, where a direct-write terminal commit means no terminal reclaim releases anything. Removing the call leaves released empty. TEST-3 Case B is tightened to exactly two ReleaseSession entries. ALTERNATIVES: a new TEST-3 Case C that commits the terminal state inside AssignProto was rejected, because the direct-write tier-1 case already fails when the call is removed.
WATCHOUT: the expected values in the new TEST-1 exit cases (409 or 500, revoke, reclaim) are whatever CODE-3 rules 3 and 4 give after G1. If G1 changed a rule, assert G1's outcome. The case text cites each rule by number and does not restate it.

### [non-spec.1.review-applicability.1]
FACT: Every non-spec anchor resolves in the current tree: handleFinalize entry Update closure names its param `r` (pkg/gateway/sessionserver/sessionserver.go:3096); failSession at start.go:3414; reclaimFinalizedPod at finalize.go:379; Store.Update doc sentence "does NOT validate the transition" at sessionstore.go:941-944; rest.md finalize "Key error codes" line at docs/api/rest.md:100 precedes `### POST /v1/sessions/{id}/start` at :102; mcp.md INVALID_STATE_TRANSITION row at docs/api/mcp.md:349. — EVIDENCE: as cited
FACT: Test fixtures the proposal reuses exist: eagerCluster/recordingAssigner(assignCount, released)/eagerAdapterDialer (tests/tier4_integration/eager_claim_lifecycle_test.go:103-138,191); adapter.NewGRPCServer takes variadic grpc.ServerOption (pkg/adapter/transport.go:27); raceAttempts/newRaceStart (tests/tier7a_load_local/racestart_testsupport_test.go:29,50); TestSessionStoreContract carries `// spec: 12.2.1` (tests/tier2_component/stores/sessionstore_test.go:82-89); the sessionserver-internal assigner is named reclaimRecordingAssigner, not recordingAssigner (terminal_reclaim_internal_test.go:49). — EVIDENCE: as cited
FACT: uploadVerifier is concrete *uploadtoken.Verifier, but its ConsumedTracker is an interface (pkg/uploadtoken/uploadtoken.go:315,363-368), so TEST-1's failing-consume case is buildable with a failing tracker. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:138
WATCHOUT: terminalReclaimPreRunning skips the claim DELETE when the session holds a podRegistry BindResult or the server has no podRegistry (pkg/gateway/sessionserver/usage.go:585-595); TEST-3 Case B's "exactly one Delete of claim-sbx-1" depends on the fixture wiring PodRegistry as eager_claim_lifecycle_test.go:275 does. — EVIDENCE: usage.go:585
DECISION: no applicability findings in round 1 of the non-spec loop — BECAUSE checklist lanes are all recognised (implement-proposal SKILL.md:16-18), spec step leads, every deliverable appears in exactly one step, Depends-on only point backwards, and CODE-1 (the only helper CODE-3 needs) lands in the same step — ALTERNATIVES: considered filing the handleFinalize doc comment going stale after the consume move (code comment, below bar) and TEST-3 Case A needing a Get hook the hookStore description omits (implementor judgment).

### [non-spec.1.review-citations.1]
FACT: Every finalize terminal writer outside handleFinalize (DELETE, terminate, watchdog, admin force-terminate, orphan session reconciler) reaches terminalReclaimPreRunning through recordSessionCompleted / OnSessionTerminal, so the "terminal writer already released the pod" premise holds for all of them — EVIDENCE: pkg/gateway/sessionserver/usage.go:301, :446; pkg/gateway/runtime/watchdog/watchdog.go:1274-1277; pkg/gateway/session/orphansession/orphansession.go:368
FACT: The watchdog's finalizing sweep is already a guarded write (r.State != st returns nil), which is what TEST-1's "watchdog-style guarded write" mimics — EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:613-621
FACT: pgstore.Update returns the mutate error through pgtenant.InTx unwrapped and normalizeMiss only rewrites ErrNoRows/22P02, so errors.As(*session.PreconditionError) survives on both stores — EVIDENCE: pkg/gateway/session/sessionstore/pgstore/pgstore.go:472-474, :1494-1506; pkg/gateway/storage/pgtenant/pgtenant.go:95-96
FACT: MCP finalize_workspace is a postByID proxy onto REST /finalize, so the CODE-2/3 fix and DOCS-1(b) cover MCP with no separate code site — EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102
FACT: Citation audit (round non-spec.1) verified every file:line and fixture name in summary, spec-changes, non-spec-changes, and checklist (0081 summary.md:1170-1182 and :149-153, 0081 non-spec :1197-1200 and :2032-2034, spec/07:58-60 and :446, spec/15:641/646/649/653, upload.go:824-828, reclaimRecordingAssigner, recordingAssigner.assignCount/released, eagerAdapterDialer, adapter.NewGRPCServer opts, newRaceStart/raceAttempts, TestSessionStoreContract spec 12.2.1). No false citation found.

### [non-spec.1.review-client-surface.1]
FACT: The served OpenAPI document is pkg/gateway/externalapi/openapi/openapi.json, not pkg/gateway/openapi/openapi.json as the orchestrator lens text says; its /v1/sessions/{id}/finalize entry already lists a 409 "Invalid state transition" response, so the overtaken-call 409 needs no OpenAPI edit. EVIDENCE: pkg/gateway/externalapi/openapi/openapi.json (paths./v1/sessions/{id}/finalize.post.responses)
FACT: The MCP tool lenny/finalize_workspace is a pure loopback proxy to POST /v1/sessions/{id}/finalize, so every handleFinalize change reaches the MCP surface with no separate code site. EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:60-71, :102
FACT: Client SDKs (go, python, typescript) treat INVALID_STATE_TRANSITION generically and carry details.currentState/allowedStates; none branches on finalize specifically, so no SDK edit is needed. EVIDENCE: sdks/client/go/lenny/errors.go:58-59, sdks/client/python/lenny/errors.py:79-80, sdks/client/typescript/src/errors.ts:66
FACT: handleFinalize is the only caller of transitionFinalizing/transitionReady/prepareAtFinalize; the one-shot POST /v1/sessions/start does not share this path. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3098, :3128, :3168
FACT: DOCS-1 anchors resolve: docs/api/rest.md:100 key-error-codes line precedes ### POST /v1/sessions/{id}/start at :102; docs/api/mcp.md:349 carries the finalize_workspace INVALID_STATE_TRANSITION row "Session not in `created` state".

### [non-spec.1.review-docs-alignment.1]
FACT: The MCP `lenny/finalize_workspace` tool is a thin proxy that POSTs to the REST `/finalize` handler, so DOCS-1(b) on docs/api/mcp.md describes the same guarded handler. EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102
FACT: docs/ never mentions `maxFinalizingTimeoutSeconds` or `FINALIZE_TIMEOUT` outside DOCS-1. The only docs that describe the finalize error surface are docs/api/rest.md (finalize section, around :93-100) and docs/api/mcp.md (finalize_workspace table, around :345-350). docs/client-guide/session-lifecycle.md section "c. Finalize Workspace" lists no errors. EVIDENCE: grep -rn maxFinalizingTimeoutSeconds docs/
WATCHOUT: Under CODE-3 rule 4, when the ready write hits an ambiguous commit, failFinalizing loses to `ready`, so the call returns 409 with currentState=ready and skips the SSE status_change and the session.finalize_workspace audit row. The non-spec Edge cases section lists only the ambiguous ENTRY commit. EVIDENCE: non-spec-changes.md:157, :286
DECISION: I filed no finding for the "Terminate does not abort Prepare" edge case — BECAUSE spec §15.1 says terminate aborts setup and the gap between that and the code predates this proposal; documenting non-abort in docs/ would contradict the spec (guardrail 1) — ALTERNATIVES: a docs edit, which I rejected.

### [non-spec.1.review-edit-sites.1]
FACT: The proposal adds no metric, alert, flag, Helm value, schema field, or error code; the only reader-facing surfaces that name the finalize INVALID_STATE_TRANSITION outcome are docs/api/rest.md (finalize section, "Key error codes" line) and docs/api/mcp.md (finalize_workspace error table, "Session not in `created` state"), both staged in DOCS-1. — EVIDENCE: docs/api/rest.md:99, docs/api/mcp.md:349
FACT: MCP finalize_workspace proxies to the REST /finalize handler, so DOCS-1(b) needs no separate MCP code change. — EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102
FACT: Swept docs/client-guide/session-lifecycle.md, docs/reference/state-machines.md, docs/reference/error-catalog.md, docs/client-guide/error-handling.md, SDK example pages, spec/29 forwarding table finalize row, and §7.1 steps 11-13; none becomes wrong after SPEC-1/DOCS-1. "finalize returns only once the session is ready" (spec/07_session-lifecycle.md:35-36, spec/29_communication-scenarios.md:258) is happy-path prose that setup errors already bypass today, so it is pre-existing rather than an unstaged site.
WATCHOUT: TEST-1 names the tier-1 assigner "recordingAssigner", but the sessionserver internal fixture type is reclaimRecordingAssigner; the tier-4 package has recordingAssigner. — EVIDENCE: pkg/gateway/sessionserver/terminal_reclaim_internal_test.go:42, tests/tier4_integration/eager_claim_lifecycle_test.go:103

### [non-spec.1.review-fresh.1]
FACT: A DELETE driven through handleDelete from `finalizing` runs recordSessionCompleted, which emits the terminal lifecycle (usage.go:428) and, when podRegistry is wired and PodAssignment is set, terminalReclaimPreRunning deletes claim-<pod> and calls ReleaseSession (usage.go:581-596, binder.go:1097-1104). A direct store write of `cancelled` does neither. Any TEST-1/TEST-3 assertion about claim presence, ReleaseSession counts, or lifecycle emission counts depends on which of the two the hook uses — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:2958; pkg/gateway/sessionserver/terminal_reclaim_internal_test.go:50-53 (reclaimTestServer wires podRegistry)
FACT: prepareAtFinalize's pre-Prepare failure exits and the binder's failPhase reclaim/drain the pod BEFORE handleFinalize reaches its failure write (finalize.go:231,251,271; binder.go:1072-1082). So a terminal writer that commits mid-Prepare, followed by a prepare failure, still gets a second DeleteClaim/drain after the terminal release. CODE-3's "lost exit write never reclaims" is true only of the handler's own reclaim — EVIDENCE: pkg/gateway/sessionserver/finalize.go:223-231
OPEN: The successor-claim hazard the proposal uses to reject reclaim-after-loss (DeleteClaim has no owner check) also applies to the prepare-failure path above, which 0082 leaves as is and does not record under shipped-tree defects. Not filed as a finding (pre-existing, outside the stated goals); a human may want it recorded as a follow-up.
FACT: uploadVerifier is the concrete *uploadtoken.Verifier; ConsumeDigest fails only through its tracker's MarkConsumed (pkg/uploadtoken/uploadtoken.go:363-367), so a tier-1 consume-failure case needs a real tracker that rejects the digest (for example, one that has already consumed it).

### [non-spec.1.review-kubernetes.1]
DECISION: No Kubernetes-idiom finding on the non-spec staging — BECAUSE CODE-1 to CODE-3 move Postgres session-row writes under the sessionstore.Update row lock, and they add no CRD status write, field manager, finalizer, webhook, or controller reconcile on the request path. The only apiserver effect is SandboxClaim DELETE, which the gateway already performs (pkg/gateway/podlifecycle/podclaim/claimer.go:310-331) — ALTERNATIVES: filing the unguarded failPhase/prepareAtFinalize DeleteClaim on an overtaken prepare failure was rejected because it predates the proposal and the proposal's handler-side claim ("the handler never reclaims") is literally true.
FACT: TEST-3 Case B's "exactly one Delete of claim-sbx-1" holds. The DELETE handler calls recordSessionCompleted with fromState=finalizing (pkg/gateway/sessionserver/sessionserver.go:2941,2958), and terminalReclaimPreRunning then runs ReclaimClaimed because finalize registers no BindResult and the eager fixture wires PodRegistry. EVIDENCE: pkg/gateway/sessionserver/usage.go:581-604; tests/tier4_integration/eager_claim_lifecycle_test.go:276
FACT: The finalizing watchdog's failed write is itself guarded (`if r.State != st { return nil }`) and reaches the terminal pod release through the TerminalHook with fromState. EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:613-632
WATCHOUT: An overtaken prepare failure still deletes claim-<pod> by name inside the binder (failPhase → drain → DeleteClaim) or prepareAtFinalize (reclaimFinalizedPod on the pre-Prepare exits). This carries the same no-owner-check hazard the proposal cites for skipping the handler reclaim. The fix idiom is a Delete with a UID precondition, and it belongs to a follow-up finding rather than 0082. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1072-1085,1200-1202; pkg/gateway/sessionserver/finalize.go:224-232

### [non-spec.1.review-mechanism.1]
FACT: Tier 1 can run a full finalize Prepare with prep != nil and a real lease assignment: TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 serves adapter.New over bufconn (podBindAdapterDialer) against a fake-client binder and asserts assigner.assigns and assigner.released — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:187-200, :1164-1260
CORRECTS the proposal's TEST-1 fixture bullet and summary Watch-out ("At tier 1, Prepare cannot succeed without an adapter"): false; the tier-1 harness already exists (external test package sessionserver_test).
WATCHOUT: CODE-3 rule 4's Gap-2 branch treats every lost failFinalizing alike ("do not reclaim"), but the lost state may be terminal (lease assigned after the terminal revoke must be revoked) or `ready` (ambiguous own commit, lease must stay). Branch on pe.CurrentState — EVIDENCE: non-spec-changes.md:157; pkg/gateway/podlifecycle/podsession/binder.go:1097-1106
FACT: failSession -> emitTerminalLifecycle does not call recordSessionCompleted/terminalReclaimPreRunning, so failFinalizing followed by reclaimFinalizedPod does not double-reclaim — EVIDENCE: pkg/gateway/sessionserver/start.go:3414-3430; pkg/gateway/sessionserver/lifecycle.go:258
FACT: the existing Gap-2 test's readyTransitionFailStore probes mutate on a copy and fails only a write producing `ready`, so it keeps passing with failFinalizing — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1118-1136

### [non-spec.1.review-performance.1]
FACT: The entry and exit guards add no store writes. The loser's refused Update runs the same SELECT ... FOR UPDATE the shipped unconditional Update ran and then rolls back, and a lost exit write now writes nothing where the shipped code wrote `failed`. The net Postgres write rate per finalize is unchanged or lower, with no etcd or Redis traffic added. — EVIDENCE: pkg/gateway/session/sessionstore/pgstore/pgstore.go:459-473, pkg/gateway/storage/pgtenant/pgtenant.go:95-97
FACT: Every terminal writer that can overtake a finalizing row reclaims the pod through OnSessionTerminal → terminalReclaimPreRunning. That covers the watchdog (Tick → recordCompleted) and the orphan reconciler. This supports CODE-3's rule that a lost exit write revokes the lease and does not reclaim the pod. — EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:613-631,1274-1278; pkg/gateway/session/orphansession/orphansession.go:342-368; pkg/gateway/sessionserver/usage.go:581-607
FACT: With an ambiguous Postgres commit, CODE-3 rule 4's Gap-2 branch ends in one of three states. If the ready write committed, the session stays `ready` and the call returns 409 with currentState=ready. If failFinalizing commits, the pod is reclaimed and the call returns 500. If failFinalizing also errors, the pod is reclaimed and the call returns 500, which matches the shipped behavior. None of these three outcomes is worse than the shipped behavior, where failSession overwrote `ready` with `failed`. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3151-3168
DECISION: no performance or failure-mode findings — BECAUSE the change adds no writes, no watches, and no new store dependency, and the upload-token consume move (Redis tracker) fails the session exactly as the shipped post-ready consume did — ALTERNATIVES: the rule-1 reorder (fail before reclaim) opens a crash window in which the claim waits for §4.6.1 orphan-claim GC; I did not file it because GC backstops the claim and the window is narrow.

### [non-spec.1.review-reliability.1]
FACT: The gateway credential lease store is in-memory and per-replica by default, so a terminal writer on another replica cannot revoke a lease this replica's Prepare minted. ReleaseSession works only on the replica that assigned the lease. EVIDENCE: pkg/gateway/credentials/credleasestore/credleasestore.go:9-13, cmd/lenny-gateway/stores.go:1759, pkg/gateway/credentials/credassign/client.go:337-344
FACT: ReleaseSession is idempotent (it iterates LeasesBySession, and releaseLocked is a no-op for an unknown lease), so a double revoke never over-decrements. EVIDENCE: pkg/gateway/credentials/credassign/credassign.go:400-423
WATCHOUT: CODE-3 rule 4's Gap-2 branch (a non-precondition ready-write error, followed by a lost failFinalizing) returns 409 with no revoke. When the loss comes from a terminal writer, the lease this call assigned leaks. The revoke has to be conditional on ferr.CurrentState being terminal rather than `ready`. EVIDENCE: non-spec-changes.md:157
WATCHOUT: prepareAtFinalize's pre-Prepare exits call reclaimFinalizedPod (DeleteClaim, no owner check) and Binder.failPhase drains the sandbox, regardless of row state. The successor-claim hazard therefore persists on the prepare-failure path. This predates the proposal (terminate does not abort Prepare) and was not filed. EVIDENCE: pkg/gateway/sessionserver/finalize.go:224-266, pkg/gateway/podlifecycle/podsession/binder.go:1072-1082
FACT: The existing TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 (readyTransitionFailStore) still passes under rule 4's reorder, where failFinalizing runs before the reclaim. It uses envtest plus an in-process adapter, so prep != nil there. EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1113-1265

### [non-spec.1.review-security.1]
FACT: Binder.ReclaimClaimed revokes the session lease (releaseCredentials) before DeleteClaim, so every reclaimFinalizedPod call in handleFinalize is also the lease revoke; dropping a reclaim drops the revoke unless revokeFinalizeLease replaces it — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1097-1105
FACT: credassign Client.Release issues RevokeCredentials to the Token Service and drops the local lease so the proxy stops resolving the bearer; an unreleased lease stays usable — EVIDENCE: pkg/gateway/credentials/credassign/client.go:299-315
WATCHOUT: CODE-3 rule 4's Gap-2 sub-branch (ready Update returns a non-precondition error, then failFinalizing is lost) skips both reclaim and revoke; when the lost state is terminal rather than `ready`, the lease AssignCredentials issued after the terminal writer's revoke is never released. Today's code reclaims unconditionally there — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3170-3181; non-spec-changes.md:157
DECISION: filed one design-defect plus the missing tier-1/4 case for it — BECAUSE it regresses the Goal "A lease that AssignCredentials issued after a terminal revoke is released" — ALTERNATIVES: filing pod-recycling-during-running-Prepare rejected, it predates the proposal and is recorded as a shipped-tree defect
CORRECTS [summary.md Watch out / non-spec TEST-1 fixture]: tier 1 CAN reach prep != nil. TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 runs a real in-process adapter over bufconn (podBindAdapterDialer) plus recordingLeaseAssigner and asserts ReleaseSession at tier 1 — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:187-200, :1167-1252. Its readyTransitionFailStore is also the ready-uncommitted fixture a Gap-2-lost case needs.
WATCHOUT: TEST-3 Case B commits DELETE after AssignCredentials, so the terminal ReclaimClaimed alone puts the session ID in assigner.released; the assertion cannot tell whether revokeFinalizeLease ran. Pin it by committing the terminal write before AssignCredentials (e.g., hook on the AssignProto call) and asserting a release that follows the assign — EVIDENCE: non-spec-changes.md:258-261

### [non-spec.1.review-single-source.1]
FACT: The exit-loss handling cascade has one full statement, CODE-3 "handleFinalize rules, in handler order" (non-spec-changes.md:152-157). The Design exit-guard bullet (non-spec-changes.md:14) and summary Decisions (summary.md:19) restate it as "lost exit write -> revoke lease only, never reclaim", and that restatement disagrees with rules 1, 2, and the Gap-2 sub-branch of rule 4. The Gap-2 sub-branch also admits a lost write that finds `ready` (an ambiguous self-commit), so "lost means a terminal writer" is not true for every branch. EVIDENCE: non-spec-changes.md:14, :157
DECISION: Filed this under (g)+(c), with the remedy of reducing the Design bullet and the summary Decision to a citation of CODE-3's rules — BECAUSE the two sites give different answers today — ALTERNATIVES: re-synchronising the bullet was rejected because it keeps two copies.
FACT: SPEC-1 restates no existing §15.1 text. The terminate row (spec/15:649) and the DELETE row (:653) state the transitions only, and the derive_failure table (:678-690) covers a different kind of row. DOCS-1 is the single permitted reader-vocabulary restatement. EVIDENCE: spec/15_external-api-surface.md:646-653

### [non-spec.1.review-test-coverage.1]
FACT: A tier-1 finalize test with a successful Prepare (prep != nil) already exists: in-process adapter via podBindAdapterDialer, recordingLeaseAssigner, asserts AssignProto and ReleaseSession counts — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1186-1251 (package sessionserver_test, external). The summary "Watch out for" claim that prep is always nil at tier 1 and a tier-1 ReleaseSession assertion cannot pass is false; it holds only for an internal-package test with no adapter.
WATCHOUT: The terminal writer's ReclaimClaimed calls ReleaseSession before DeleteClaim, so any test that only checks "released contains the session ID" after a DELETE passes with revokeFinalizeLease removed. Pinning the late-assign revoke needs the terminal write to land before AssignCredentials (e.g. an AssignCredentials interceptor issuing DELETE) or an exact released count of 2 — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1097-1103; pkg/gateway/sessionserver/usage.go:596; tests/tier4_integration/eager_claim_lifecycle_test.go:121-123 (released appends every call).
FACT: Existing Gap-2 test TestFinalize...(sess-fin-gap2) uses readyTransitionFailStore and asserts 500 + failed + one ReleaseSession; CODE-3 rule 4's non-lost branch keeps it green — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1217-1266.

### [non-spec.2.review-docs-alignment.1]
DECISION: no docs finding on CODE-3 rule 4 sub-branch (iii) (ambiguous ready commit answers 500 with the row `ready`) — BECAUSE the DOCS-1 overtaken-call paragraph scopes itself to a session that ended, and the generic INTERNAL_ERROR guidance (retry) already leads a client to a 409 currentState=ready retry answer, so no docs page is left wrong — ALTERNATIVES: requiring a finalize-specific 500 sentence in docs/api/rest.md; rejected as below the materiality bar.
FACT: docs/api/rest.md finalize section says nothing about 500; INTERNAL_ERROR guidance lives at docs/client-guide/error-handling.md:96 and docs/reference/error-catalog.md:142 — EVIDENCE: those lines.
FACT: DOCS-1's "the gateway releases any credential lease the call assigned" stays true across rules 1-4(ii) after the round-1 fix; rule 4(iii) is non-terminal and outside the paragraph's scope — EVIDENCE: non-spec-changes.md:156-164, :179.

### [non-spec.2.review-fresh.1]
FACT: The start_pod_test.go pod-bind fixture reaches prep != nil at tier 1 (in-process adapter, recordingLeaseAssigner at :1145-1162, Gap-2 test at :1175-1266 asserts released==[id]). TEST-1's move to package sessionserver_test is sound. — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1175-1266
FACT: No non-terminal writer moves a row off `finalizing` except transitionReady. The failure-report path makes no transition for pre-running states, and failure.go:376-385 admits finalizing only toward `failed`. So CODE-3 rule 4's claim that only sub-branch (iii) can find a non-terminal row holds. — EVIDENCE: pkg/gateway/sessionserver/failure.go:147-154
FACT: prepareAtFinalize registers no podRegistry binding, so a DELETE during `finalizing` goes through terminalReclaimPreRunning -> ReclaimClaimed, which releases the lease once (binder.go:1101). TEST-3 Case B's "exactly two released entries" holds. — EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605
FACT: The upload digest and expiry are captured from the entry-write result before the ready write (sessionserver.go:3160-3162), so moving the consume ahead of the ready write (CODE-3 rule 3) needs no new data source. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3160-3190

### [non-spec.2.review-mechanism.1]
FACT: The TEST-1 external fixture reaches prep != nil: TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 wires adapter.New + podBindAdapterDialer + recordingLeaseAssigner on binder.Credentials, and its readyTransitionFailStore probe still selects the guarded ready mutation on a finalizing row — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1118-1136, :1175-1266
FACT: The handler-driven DELETE of a finalizing row releases the lease exactly once (ReclaimClaimed -> releaseCredentials) when the podRegistry holds no BindResult, so TEST-3 Case B's "exactly two released entries" holds with revokeFinalizeLease as the second — EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605, pkg/gateway/podlifecycle/podsession/binder.go:1097-1102
FACT: No writer other than transitionReady moves a row off finalizing to a non-terminal state (EndpointTerminate/Delete write terminal; the watchdog writes failed), so CODE-3 rule 4 sub-branch (iii) is reachable only through an ambiguous ready commit — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3030, pkg/api/v1/session/session.go:288-303

### [non-spec.2.review-reliability.1]
FACT: Only terminal writers can move a row off `finalizing`: legalReportTransition admits finalizing only toward `failed`, treerecovery's running write targets orphaned delegation nodes via resume, and transitionReady is the sole `ready` writer. This backs CODE-3 rule 4's "only this branch can find a non-terminal row". — EVIDENCE: pkg/gateway/sessionserver/failure.go:360-385, pkg/gateway/sessionserver/sessionserver.go:3030
FACT: TEST-3 Case B "exactly two released entries" holds: handleDelete from finalizing runs terminalReclaimPreRunning -> ReclaimClaimed -> releaseCredentials (or the executor Release path, which also calls releaseCredentials once), and Binder.Prepare's success path calls no ReleaseSession. — EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605, pkg/gateway/podlifecycle/podsession/binder.go:1097-1102, :1963
OPEN: Pre-existing, not introduced by 0082. When the Gap-2 ready write and failFinalizing both hit non-precondition store errors (CODE-3 rule 4 sub-branch (i), and rule 1), the handler reclaims the pod while the row stays `finalizing`. The later §6.2 watchdog terminal write then runs terminalReclaimPreRunning -> DeleteClaim(claim-<pod>) with no owner check, which is the successor-claim hazard 0082 cites. This is out of scope for the finalize race and belongs in a separate proposal.

### [non-spec.2.review-security.1]
FACT: transitionReady is the sole non-test writer of session.StateReady in pkg/ and cmd/, so CODE-3 rule 4 sub-branch (iii) is the only branch that can find a non-terminal row after a lost exit write — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3030
FACT: credleasestore is in-memory and per-replica, which grounds the rule 4 (ii) revoke on a terminal-writer loss — EVIDENCE: pkg/gateway/credentials/credleasestore/credleasestore.go:9-10
FACT: Binder.ReclaimClaimed releases the lease before DeleteClaim, so every terminal pre-running reclaim adds one ReleaseSession entry; TEST-3 Case B "exactly two entries" depends on this — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1097-1102
DECISION: no security findings on the round-2 diff — BECAUSE the (iii) ambiguous-ready branch keeps pod and lease for a session that is legitimately ready, the upload token is consumed before the ready write so it cannot be replayed, and uploads are admitted only in created — ALTERNATIVES: filing the pre-existing rule 2 binder-side reclaim after a terminal writer as a pod-reuse regression; rejected because that text sits outside this round's diff and was examined last round

### [non-spec.2.review-single-source.1]
FACT: After round-1 fixes, the lost-exit-write cascade (no reclaim, lease revoke when prep != nil, 409 vs 500 per Gap-2 sub-branch) has one stating site: CODE-3 "handleFinalize rules, in handler order" (non-spec-changes.md:152-164). The Design "Exit guards" bullet, summary Decision/Watch-out lines, and the "Terminate from finalizing" and "Ambiguous ready commit" edge cases now cite it rather than restate it. EVIDENCE: non-spec-changes.md:14, :305, :307; summary.md:19, :30
DECISION: No (g) filed on TEST-1 per-case assertions. BECAUSE they translate each CODE-3 rule outcome into fixture observables (claim-sbx-1, recordingLeaseAssigner.released), which the test obligation requires, and every case agrees with its rule today. ALTERNATIVES: filing them as copies was rejected, because reducing them would leave the tests unspecified under (f).

### [non-spec.2.review-test-coverage.1]
FACT: The pod-bind fixture in pkg/gateway/sessionserver/start_pod_test.go (package sessionserver_test) reaches prep != nil at tier 1 through an in-process adapter over bufconn; TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 proves it by asserting one AssignProto and one ReleaseSession. — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1175-1266
FACT: The ReleaseSession recorders (recordingLeaseAssigner at tier 1, recordingAssigner at tier 4) append on every call, including no-op releases. The TEST-1 "released == [id]" assertions and TEST-3 Case B "exactly two entries" therefore depend only on how many times ReleaseSession is called. — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1160-1162; tests/tier4_integration/eager_claim_lifecycle_test.go:121-125; pkg/gateway/podlifecycle/podsession/binder.go:1097-1102
DECISION: I filed no findings for round 2. BECAUSE each new CODE-3 branch has a TEST-1 case, and each case asserts the revoke or reclaim that distinguishes it: rule 1 lost, rule 2 lost, rule 3 committed and lost, rule 4 lost, and Gap-2 (ii) and (iii). Gap-2 (i) is pinned by the existing Gap-2 test, and the committed consume case pins the move of the consume ahead of the ready write. — ALTERNATIVES: I considered a finding that the plan-parse case does not say how a malformed plan is seeded. I rejected it because that text did not change this round, and a direct inner-store write can seed the plan.

### [non-spec.3.fix-G1.1]
DECISION: CODE-3 rule 3 keeps the upload-token consume after the rule-4 ready write and drops its failure branch (log via log.Printf, continue) — BECAUSE the token is then consumed only after `ready` commits, so the §7.1/§15.1 410 "successful FinalizeWorkspace" definitions stay true with no spec or docs edit, and no failure write lands on a `ready` row — ALTERNATIVES: consume-before-ready plus an Edge-cases row and reworded 410 definitions (adds SPEC-2/DOCS-2 for a removable consequence); admit `finalizing` or `ready` in the failure write (already rejected in summary Non-goals).
MISTAKE: an earlier round moved the consume ahead of the ready write to keep `finalizing` the only failure from-state; that left the token consumed on finalizes whose ready write then lost or failed, turning a later /upload 409 into 410 UPLOAD_TOKEN_CONSUMED. Reverted this round; summary Non-goals now records why.
WATCHOUT: do not re-add a consume failure branch (failSession/failFinalizing/reclaim) after the ready write, and do not move the consume before it. ConsumeDigest's only error is ErrConsumed for an already-invalidated digest — EVIDENCE: pkg/uploadtoken/uploadtoken.go:190-193, :212-220 (MemoryTracker is the sole implementation, cmd/lenny-gateway/stores.go:385; the "Production wires this to Redis" doc comment describes nothing in the tree).
WATCHOUT: CODE-3's rule label is now **handleFinalize rules** (the "in handler order" qualifier is gone because rule 3 runs after rule 4). The rules are deliberately not renumbered; rule 4 is cited across TEST-1, TEST-3, and Edge cases.
FACT: /upload verifies the token before its `created` precondition, so a consumed token answers 410 ahead of 409 — EVIDENCE: pkg/gateway/sessionserver/upload.go:301-315.
UNVERIFIED: summary.md shipped-tree-defects bullet cites sessionserver.go :3167 and :3190 for the ready write and consume; the tree today has the ready Update at ~:3167 and ConsumeDigest at :3176. Adjudicated not-a-site this round; a later round should re-verify those line numbers.

### [non-spec.3.fix-G2.1]
DECISION: pin failFinalizing's committed tail with a unit case in finalize_race_internal_test.go (nil error, `failed`, one status_change, one session_complete, one session.failed audit) — BECAUSE it covers every CODE-3 rule that routes through failFinalizing, and the handler-level "Prepare failure after a terminal write" case already pins the lost direction (zero emissions) — ALTERNATIVES: asserting emissions in a handler exit case covers only one rule's route.
FACT: the pattern and helpers (sseEventsOfType, captureLifecycleAudit, auditSessionFailed) live in the same package's test code — EVIDENCE: pkg/gateway/sessionserver/lifecycle_internal_test.go:290-312
WATCHOUT: the TEST-1 target bullet for finalize_race_internal_test.go is now a pointer to the cases list; do not reintroduce a case enumeration or count there — EVIDENCE: non-spec-changes.md TEST-1 Targets, second bullet

### [non-spec.3.fix-design-G1.1]
DECISION: close the consume-before-ready finding by reverting the move rather than documenting its consequence. CODE-3 rule 3 keeps ConsumeDigest after the ready write (where the tree has it) and replaces its fail-and-reclaim branch with log-and-continue, so no failure write follows the ready write and `finalizing` stays the only from-state — BECAUSE the ConsumedTracker contract admits only nil or ErrConsumed (a digest already invalidated), the only tracker is MemoryTracker, CODE-2 lets exactly one call per session reach the consume, and /upload admits only `created`, so an unconsumed token mints nothing — ALTERNATIVES: Edge-cases row plus staged §7.1/§15.1 and docs rewording of the 410 (rejected: grows the spec change, falsifies summary line 24, and documents a consequence the design can avoid); Edge-cases row only (rejected: the 410 definition "successful FinalizeWorkspace" is then false); a two-from-state failure write (already rejected at summary Non-goals).
FACT: ConsumeDigest can fail only through ConsumedTracker.MarkConsumed, whose contract returns nil or ErrConsumed; the only implementation is the in-memory MemoryTracker wired at cmd/lenny-gateway/stores.go:385. The "Production wires this to Redis" doc comment has no implementation or spec basis. — EVIDENCE: pkg/uploadtoken/uploadtoken.go:185-220, :363-368
CORRECTS [review-log DECISION "no performance or failure-mode findings" (line 267)]: no Redis tracker exists; the consume failure mode is ErrConsumed from the in-memory tracker only.
CORRECTS [review-log DECISION "no security findings on the round-2 diff" (line 319)]: after G1 the token is consumed after the ready write again; replay is still impossible because /upload's precondition admits only `created` (pkg/gateway/sessionserver/upload.go:307-315), which is what the security conclusion rests on.
WATCHOUT: the label "handleFinalize rules, in handler order" is cited by name at non-spec-changes.md:14. G1 drops ", in handler order" because rule 3 now runs after rule 4; do not renumber the rules instead, since "rule 4" is cited at six TEST sites and by G2.

### [non-spec.3.fix-design-G2.1]
DECISION: pin failFinalizing's committed tail with one new unit case in finalize_race_internal_test.go that calls failFinalizing on a seeded `finalizing` memstore row and asserts nil error, State=failed, one status_change, one session_complete, and one session.failed audit event — BECAUSE it pins the function itself whichever CODE-3 rule calls it, and the lost direction is already pinned by TEST-1's "Prepare failure after a terminal write" zero-emission assertion — ALTERNATIVES: adding an emission assertion to the handler-level "Consume failure, committed" case (rejected: it pins only one caller route and duplicates what the unit case gives).
FACT: the helpers the new case needs (sseEventsOfType, captureLifecycleAudit, auditSessionFailed) already live in package sessionserver test code — EVIDENCE: pkg/gateway/sessionserver/lifecycle_internal_test.go:290-312
WATCHOUT: TEST-1's file-target bullet for finalize_race_internal_test.go enumerated its cases ("holds only the finalizingPrecondition and failSession unit cases"); any case added there falsifies it. Reword it to a pointer that lists no cases — EVIDENCE: non-spec-changes.md:201

### [non-spec.3.review-applicability.1]
FACT: All TEST-1 fixture names resolve in pkg/gateway/sessionserver/start_pod_test.go (package sessionserver_test): podBindRuntime :63, podBindClient :92, podBindAdapterDialer :187, podBindBinder :202, readyTransitionFailStore :1118-1136, recordingLeaseAssigner :1145-1162, Gap-2 test :1175. Existing Gap-2 test still passes under the guarded ready write plus failFinalizing, because its probe runs on a finalizing row. — EVIDENCE: start_pod_test.go:1123-1136, :1218-1267
FACT: Plan-parse failure (CODE-3 rule 1) is only reachable on a no-body finalize whose stored row.WorkspacePlan is unparseable (resolveFinalizePlan returns hasPlan=false for an empty body). A TEST-1 case must seed the bad plan directly into the inner memstore. — EVIDENCE: pkg/gateway/sessionserver/finalize.go:62-64, :331-339
FACT: Tier-4 files need //go:build integration because eagerCluster/recordingAssigner live under that tag, and tier-7a files need load_local. — EVIDENCE: tests/tier4_integration/eager_claim_lifecycle_test.go:3; tests/tier7a_load_local/racestart_testsupport_test.go
WATCHOUT: TEST-3 Case A's "blocks until call 2 has completed its pre-lock Get" requires the tier-4 hook store to observe Get as well as Update, while TEST-1's hookStore hooks only Update. I judged this to be ordinary implementation judgment and did not file it. — EVIDENCE: non-spec-changes.md:263, :269
DECISION: returned no findings — BECAUSE every anchor (spec/15:646 final sentence, docs/api/rest.md:100 inside the finalize section, docs/api/mcp.md:349), every cited symbol (session.Validate/AllowedStates/PreconditionError, CredentialAssigner.ReleaseSession, adapter.NewGRPCServer opts, uploadtoken.ConsumedTracker.MarkConsumed, Options.Events/LifecycleAuditSink), and the S1-S8 checklist (one lane per step, spec first, deps backward only, every deliverable in exactly one step) all hold — ALTERNATIVES: filing the stale handleFinalize doc-comment text about consume-after-ready (code comment, outside criterion d)

### [non-spec.3.review-citations.1]
FACT: Every file:line citation in non-spec-changes.md and summary.md resolved as claimed at this round: start_pod_test.go:63-210/:1113-1136/:1142-1162/:1175-1266/:1179; sessionserver.go:2958 (handleDelete -> recordSessionCompleted), :3030 (transitionReady, sole writer of StateReady in pkg/), :3065-3083, :3167, :3190; usage.go:428/:446; binder.go:1097-1102; credleasestore.go:9-10; upload.go:824-828; spec/07:58-60 and :446; spec/15:641; 0081 summary.md:149-153 and :1170-1182; 0081 non-spec-changes.md:1197-1200 and :2032-2034. EVIDENCE: the cited lines themselves.
FACT: Binder.releaseCredentials (binder.go:1263-1268) is only b.Credentials.ReleaseSession, so revokeFinalizeLease calling podBinder.Credentials.ReleaseSession directly releases exactly what ReclaimClaimed's revoke releases. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1263
FACT: pgtenant.InTx returns fn's error unwrapped (pgtenant.go:95-96) and memstore.Update returns mutate's error unwrapped (memstore.go:139-140), so the Design's errors.As claim holds on both stores.
FACT: `created` is written only by row constructors (create.go:244, derive.go:388, replay.go:170, delegation/service.go:1419); no Update sets StateCreated. EVIDENCE: grep StateCreated pkg/

### [non-spec.3.review-client-surface.1]
FACT: The OpenAPI document lives at pkg/gateway/externalapi/openapi/openapi.json, not pkg/gateway/openapi/openapi.json. The finalize path already declares a 409 "Invalid state transition" response, so the overtaken-call 409 needs no OpenAPI edit. EVIDENCE: pkg/gateway/externalapi/openapi/openapi.json paths["/v1/sessions/{id}/finalize"].post.responses
FACT: MCP lenny/finalize_workspace sends the call through the REST /finalize route via the shared SessionService, so handleFinalize's guards cover the MCP surface too. No separate MCP finalize path exists. EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102, pkg/gateway/mcpfabric/mcptools/mcptools.go:342-350
FACT: The client SDKs (go/python/typescript) wrap finalize as a generic transition and enumerate no per-endpoint error codes. The client-guide pages state the 409 rule generically. Neither needs a DOCS-1 companion edit. EVIDENCE: sdks/client/go/lenny/client.go:217-220, docs/client-guide/session-lifecycle.md:532
FACT: The DOCS-1(a) anchor line matches docs/api/rest.md:100, and the DOCS-1(b) cell matches docs/api/mcp.md:349. The SPEC-1 anchor sentence ends spec/15_external-api-surface.md:646.

### [non-spec.3.review-docs-alignment.1]
FACT: /upload verifies the upload token BEFORE the §15.1 state precondition, so a consumed token answers 410 UPLOAD_TOKEN_CONSUMED even on a terminal row — EVIDENCE: pkg/gateway/sessionserver/upload.go:301-315, pkg/uploadtoken/uploadtoken.go:341-343
WATCHOUT: CODE-3 rule 3 moves ConsumeDigest ahead of the ready write, so a finalize whose ready write then loses (rule 4 lost, Gap-2 (i)/(ii)) leaves the token consumed on a session whose finalize never succeeded; spec §7.1 single-use bullet (spec/07_session-lifecycle.md:60), §15.1 catalog row (spec/15_external-api-surface.md:1123), docs/reference/error-catalog.md:106 and docs/client-guide/error-handling.md:125 all describe 410 as following a successful finalize — EVIDENCE: non-spec-changes.md:158
FACT: MCP finalize_workspace is a thin postByID onto /finalize, so DOCS-1(b) mirrors the REST outcome correctly — EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102
FACT: DOCS-1 anchors resolve uniquely: docs/api/rest.md:100 (inside the finalize section) and docs/api/mcp.md:349 (only "Session not in `created` state" row)
DECISION: did not file the ambiguous-entry-commit or client-disconnect rows as undocumented — BECAUSE both outcomes (500 / orphaned finalizing until watchdog) predate the proposal (sessionserver.go:3097-3105) — ALTERNATIVES: filing as undocumented accepted outcomes; rejected as not introduced here

### [non-spec.3.review-edit-sites.1]
FACT: The only docs surfaces that state a per-endpoint finalize error outcome are docs/api/rest.md (finalize section, "Key error codes" line) and docs/api/mcp.md (finalize_workspace error table, INVALID_STATE_TRANSITION row). DOCS-1 covers both; client-guide/session-lifecycle.md:532 and error-handling.md:58 state only the generic 409 envelope and stay true. EVIDENCE: docs/api/rest.md:100, docs/api/mcp.md:349
FACT: The MCP finalize_workspace tool proxies to the same REST /finalize handler, so the guarded handler covers MCP too. EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102
FACT: No schema, chart, metric, alert, or audit-event surface names finalize outcomes; session.finalize_workspace in spec/16 is a span name, not an audit catalog row. EVIDENCE: spec/16_observability.md:348
WATCHOUT: spec/29_communication-scenarios.md step 19 ("Finalize returns to the client only once the session is ready") is happy-path scenario prose that existing 422 failure returns already qualify; it is not an edit site for SPEC-1. EVIDENCE: spec/29_communication-scenarios.md:258
WATCHOUT: eagerAdapterDialer has 6 callers across tier4 files (e.g. credential_delivery_gate_test.go:138); TEST-3 "extend in place" must keep the signature compatible (variadic), since adapter.NewGRPCServer already takes ...grpc.ServerOption. EVIDENCE: pkg/adapter/transport.go:27

### [non-spec.3.review-fresh.1]
FACT: The start_pod_test.go pod-bind fixture TEST-1 reuses is envtest-backed (podBindClient -> podBindEnvtestClient -> envtest.Start, which skips when assets are absent). The repo's unit tier already runs envtest-backed pkg/ tests (cmd/lenny-test/timeouts.go tierUnitTimeout comment), so labelling TEST-1 tier 1 matches harness practice and is not a tier-mislabel finding. EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:92-95, tests/testinfra/envtest/envtest.go:115-117, cmd/lenny-test/timeouts.go:47-56
FACT: Binder.releaseCredentials releases only b.Credentials (no UserCredentials), so revokeFinalizeLease's Credentials-only ReleaseSession matches what ReclaimClaimed revokes. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1097-1101, :1263-1268
FACT: transitionReady is the only writer of StateReady in non-test pkg code, and no pkg writer moves a finalizing row to a non-terminal state, so CODE-3 rule 4 sub-branch (iii) is the only branch that can see a non-terminal locked row. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3030; pkg/gateway/sessionserver/failure.go:147-153
DECISION: returned no findings for round 3 — BECAUSE the round-2 rewrites (rule 4 sub-branches, TEST-1 split into finalize_race_test.go plus internal file, TEST-3 Case B exact-two release count) checked out against the code and cited lines — ALTERNATIVES: filing TEST-1 as tier 2 (rejected, see FACT above)

### [non-spec.3.review-kubernetes.1]
FACT: The proposal adds no Kubernetes write. Its only k8s-visible effect is removing SandboxClaim deletes on lost exit branches; revokeFinalizeLease is in-memory (Credentials.ReleaseSession). No CRD status, finalizer, webhook, or controller reconcile sits on the finalize request path. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1097-1105
FACT: The FINALIZE_TIMEOUT watchdog reaches the same terminal reclaim as the REST DELETE and terminate handlers through TerminalHook.OnSessionTerminal(fromState=finalizing), so the premise that the terminal writer has already released the pod holds for the watchdog as well. EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:612-633, :1274-1278
WATCHOUT: On an overtaken call, CODE-3 rule 2 (prepare failure) still reaches the binder's failPhase drain or prepareAtFinalize's reclaimFinalizedPod BEFORE failFinalizing runs, so the no-owner-check DeleteClaim/drain hazard the proposal names still exists on that path. This is pre-existing and the proposal states it ("has already reclaimed"). It is not a defect this proposal introduces. A later owner-check fix (a DeleteOptions UID precondition on claim-<podName>) would cover it. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:867, :1072-1082; pkg/gateway/sessionserver/finalize.go:231, :251, :271
FACT: adapter.NewGRPCServer already takes variadic grpc.ServerOption, so TEST-3's interceptor extension needs no production change. EVIDENCE: pkg/adapter/transport.go:27

### [non-spec.3.review-mechanism.1]
FACT: The finalizing watchdog forces `failed`/FINALIZE_TIMEOUT through its own guarded Update and then calls the TerminalHook, which cmd/lenny-gateway wires to the session server, so the watchdog path reaches recordSessionCompleted -> terminalReclaimPreRunning like DELETE and terminate do. The CODE-3 "terminal writer already reclaimed" premise therefore holds for all three terminal writers. EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:613-631, :1274-1278; cmd/lenny-gateway/controlserver.go:237; pkg/gateway/sessionserver/usage.go:446
FACT: Only terminate and DELETE admit `finalizing` in the precondition table, and both write terminal states, so CODE-3's claim that only a terminal writer can move a row off `finalizing` holds. transitionReady is the only writer of `ready`. EVIDENCE: pkg/api/v1/session/session.go:280-330; pkg/gateway/sessionserver/sessionserver.go:3030
FACT: The Binder.Prepare call at start.go:2634 (prepareAndLaunch) is reached only from the combined create-and-start path, where the session is created inside the same call, so it gives 0081's Prepare-arm short-circuit no second concurrent trigger. The 0082 Impacts row stands. EVIDENCE: pkg/gateway/sessionserver/start.go:2371-2383
FACT: A no-body finalize skips plan parsing in resolveFinalizePlan, so a corrupt stored WorkspacePlan reaches the rule-1 plan-parse failure. The TEST-1 rule-1 case can be reached. EVIDENCE: pkg/gateway/sessionserver/finalize.go:60-63, :331-340
WATCHOUT: TEST-3's fixture pool is not preConnect, so the "zero DemoteSDK" assertion passes trivially. The PrepareWorkspace==1 count is what actually pins the entry guard. I did not report this because it is not a defect. EVIDENCE: tests/tier4_integration/eager_claim_lifecycle_test.go:157-165

### [non-spec.3.review-performance.1]
FACT: The proposal adds no store write per finalize. The entry guard and exit guards run inside the existing Update mutations, and moving the upload-token consume ahead of the ready write swaps the order of two existing calls. The per-finalize write count (Get, entry Update, optional applyFinalizePrepareResult Update, ready Update) is unchanged, and the change adds no etcd writes, watches, or new locks. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3065-3252
FACT: The finalizing watchdog is store-backed rather than per-replica. It lists rows by state, measures age from UpdatedAt, and re-checks the state inside its Update mutation, so it recovers an orphaned `finalizing` row after a replica crash or an ambiguous entry commit. EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:598-627
FACT: A double ReleaseSession is safe because both implementations release by looking up leases for the session and do nothing when none is found. The Service path decrements the active-session counter only for a lease it still holds. EVIDENCE: pkg/gateway/credentials/credassign/credassign.go:400-430, pkg/gateway/credentials/credassign/client.go:337-344
FACT: pgstore.Update returns the mutate error directly from inside the FOR UPDATE transaction, and memstore.Update returns it before writing, so a refused guard leaves the row unchanged on both stores. EVIDENCE: pkg/gateway/session/sessionstore/pgstore/pgstore.go:461-473, pkg/gateway/session/sessionstore/memstore/memstore.go:139-141

### [non-spec.3.review-reliability.1]
FACT: No decorator wraps sessionstore.Store in production; pgstore.Update runs mutate under SELECT ... FOR UPDATE inside pgtenant.InTx with no retry loop, and InTx returns fn's error verbatim, so a guarded mutate cannot be re-run after an ambiguous commit and misread its own commit as a lost race. EVIDENCE: pkg/gateway/storage/pgtenant/pgtenant.go:76-103; pkg/gateway/session/sessionstore/pgstore/pgstore.go:464-474
FACT: credassign ReleaseSession (both Service and Client) iterates the local per-replica lease store by session and is idempotent, so the double revoke (terminal ReclaimClaimed plus revokeFinalizeLease) cannot double-decrement a pool counter. EVIDENCE: pkg/gateway/credentials/credassign/credassign.go:400-432; pkg/gateway/credentials/credassign/client.go:337-345
FACT: Every writer that can move a `finalizing` row writes a terminal state (watchdog Tick, orphansession.failOrphan, terminate/DELETE, admin); failure.go treats a pre-running report as a no-op. This backs CODE-3 rule 4's "only (iii) finds a non-terminal row". EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:612-622; pkg/gateway/session/orphansession/orphansession.go:342-350; pkg/gateway/sessionserver/failure.go:147-153
FACT: The watchdog's finalizing sweep reaches recordSessionCompleted through the terminal hook wired in cmd/lenny-gateway/controlserver.go:237, and that path reclaims the pod by name via terminalReclaimPreRunning. The ambiguous-entry-commit fallback that the proposal delegates to the watchdog therefore works. EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605
WATCHOUT: The prepare-failure path (CODE-3 rule 2) still issues a DeleteClaim with no owner check after an overtaking terminal writer, through Binder.Prepare's reconnect-failure ReclaimClaimed and prepareAtFinalize's pre-Prepare reclaimFinalizedPod calls. It is a sub-case of the recorded "terminate does not abort Prepare" defect, and it predates this proposal. It was judged below the bar here. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:844-856; pkg/gateway/sessionserver/finalize.go:224-265
DECISION: Returned no findings under the reliability lens — BECAUSE every rule 1-4 branch re-traced for crash, cancelled-context, and ambiguous commit either commits a terminal state or leaves `finalizing` for the watchdog. Claim leaks are backstopped by the leader-only ClaimGarbageCollector (pkg/controller/warmpool/gc.go:274-320) — ALTERNATIVES: filing the rule-2 DeleteClaim residual. Rejected because it predates the proposal, it is covered by a recorded shipped-tree defect, and no remedy is available in the non-spec staging.

### [non-spec.3.review-security.1]
DECISION: security lens returned no findings — BECAUSE every lost exit branch only narrows DeleteClaim calls and adds a session-keyed lease revoke; no control is removed or feature-gated, and no security bound is sourced from a pod self-report — ALTERNATIVES: filing tier-9 omission for the lease revoke (rejected: revoke is pinned at tiers 1 and 4, and lease-accounting release is not credential delivery); filing pod-return-to-pool after a mid-Prepare terminal reclaim (rejected: pre-existing, unchanged by this proposal)
FACT: Binder.ReclaimClaimed revokes the session lease and deletes claim-<podName>, which returns the pod to the pool through the occupancy projection with no drain; failPhase instead drains (retires) the pod — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1072-1107
FACT: prepareAtFinalize's pre-Prepare exits (pool resolution, credential resolution, delivery-isolation check) call reclaimFinalizedPod themselves, so CODE-3 rule 2's "already reclaimed" includes a by-name DeleteClaim the proposal does not guard against a prior terminal reclaim; pre-existing — EVIDENCE: pkg/gateway/sessionserver/finalize.go:224-266
FACT: every terminal writer that can overtake finalize (REST terminate/DELETE, watchdog via TerminalHook, orphan reconciler) routes through the terminal pipeline, so a lost finalize that skips the reclaim does not leak the claim beyond the orphan-claim GC backstop — EVIDENCE: pkg/gateway/runtime/watchdog/watchdog.go:612-633,1274-1278; pkg/gateway/session/orphansession/orphansession.go:341-371
FACT: uploadAborts.closeSession and uploadLimits.closeSession are called only on the finalize success tail; no terminal path closes them — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3205,3209
WATCHOUT: credleasestore has a Postgres-backed pgstore implementation in addition to the in-memory one, so "the lease store is in-memory and per-replica" (CODE-3 rule 4 (ii)) describes one backend; the revoke goes through credassign's local lease map either way — EVIDENCE: pkg/gateway/credentials/credleasestore/credleasestore.go:9-12,24-28

### [non-spec.3.review-single-source.1]
FACT: After the round-2 fix, the lost-exit-write cascade has one home: non-spec-changes.md "handleFinalize rules, in handler order" (CODE-3 rules 1-4, with 4(i)-(iii)). The Design bullet, summary Decisions, and Edge cases cite it; the Edge-case "Ambiguous ready commit" lists consequences of 4(iii) only and agrees with it. EVIDENCE: non-spec-changes.md:14, :152-164, :305; summary.md:73
FACT: emitTerminalLifecycle issues no lease release, so the TEST-1 "Consume failure, committed" assertion released==[id] comes from reclaimFinalizedPod alone. EVIDENCE: pkg/gateway/sessionserver/lifecycle.go:258-290, finalize.go:379-387
DECISION: returned no findings under the single-source lens — BECAUSE every restatement found is rationale (summary Non-goals), a reader-vocabulary doc restatement (DOCS-1), or a test assertion that matches its cited CODE-3 rule — ALTERNATIVES: filing TEST-1 per-case assertions as copies of CODE-3 rules; rejected because earlier rounds required concrete assertions to pin revokeFinalizeLease, and each case cites its rule.

### [non-spec.3.review-test-coverage.1]
FACT: failSession's terminal tail is pinned only by TestFailSessionEmitsTerminalLifecycle_spec_7_2_2, which calls failSession directly. No existing finalize handler test asserts terminal lifecycle signals after a finalize failure. TestFinalizeFailsSessionAndReclaimsPodOnSetupError_spec_7_5 checks state and claim only. EVIDENCE: pkg/gateway/sessionserver/lifecycle_internal_test.go:290-312; pkg/gateway/sessionserver/start_pod_test.go:773-866
WATCHOUT: TEST-1's lost-path assertions of "zero terminal lifecycle emissions" also pass when failFinalizing never calls afterFailed. Only a committed-path assertion of exactly one emission distinguishes the two. EVIDENCE: non-spec-changes.md:215, :217
FACT: ReclaimClaimed calls releaseCredentials(sessionID) before DeleteClaim, so every committed reclaim adds one entry to recordingLeaseAssigner.released. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1097-1105
FACT: The plan-parse branch (rule 1) is reachable only through a malformed stored WorkspacePlan with no finalize body. resolveFinalizePlan validates a body plan and rejects a body when the row already has a plan. A test has to seed the bad plan directly into the store. EVIDENCE: pkg/gateway/sessionserver/finalize.go:62-75, :331-340

### [non-spec.4.review-docs-alignment.1]
FACT: The r3->r4 diff reverts CODE-3 rule 3: the upload-token consume stays after the ready write and has no failure branch (log only). No docs/ page describes the old consume-failure 500 or failed-session path, and docs/client-guide/session-lifecycle.md:199, docs/reference/error-catalog.md:106 and docs/client-guide/error-handling.md:125 (token consumed by a successful finalize) stay true under the revert. So the revert needs no DOCS edit. EVIDENCE: pkg/uploadtoken/uploadtoken.go:190-193 (MarkConsumed admits only ErrConsumed); MemoryTracker is the only implementation (uploadtoken.go:212)
FACT: The stale "rules 1-3" and "in handler order" sites were checked across the proposal directory. Only non-spec-changes.md:164 named them, and it now says "rules 1 and 2". The summary Non-goals line 23 and Alternatives lines 51-52 match the new rule 3.

### [non-spec.4.review-test-coverage.1]
FACT: Round-4 diff reverses the consume move: CODE-3 rule 3 now keeps ConsumeDigest after the ready write with no failure branch (log only); TEST-1 "Consume after the ready write" (tracker records `ready`, returns ErrConsumed, expects 200) pins it. No existing test asserts the old consume-failure 500 path, so no test disposition is needed. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3189-3198; grep for "could not be invalidated" in *_test.go returns nothing
FACT: uploadtoken.NewVerifier accepts an injected ConsumedTracker, so the TEST-1 stub tracker is constructible. — EVIDENCE: pkg/uploadtoken/uploadtoken.go:322
USEFUL: the failFinalizing emission case cites helpers that exist as stated. — EVIDENCE: pkg/gateway/sessionserver/lifecycle_internal_test.go:290-312

### [non-spec.5.fix-G1.1]
DECISION: added TEST-1 case "Plan-parse failure, committed" (CODE-3 rule 1) seeding a malformed WorkspacePlan through the wrapped inner store after /create, empty finalize body, no hook — BECAUSE the only rule-1 case (lost) passes when the reclaim is dropped — ALTERNATIVES: hook callback on the `failed` Update to pin ordering (the lost case already pins it); internal memstore case (cannot observe claim-sbx-1).
FACT: the plan-parse branch is reachable only through a malformed stored plan: /create validates plans, and an empty finalize body sets hasPlan=false so storedWorkspacePlan parses the row — EVIDENCE: pkg/gateway/sessionserver/finalize.go:62-63, pkg/gateway/sessionserver/start.go:1218-1224, pkg/gateway/sessionserver/sessionserver.go:3113-3125
OPEN: the lost-case bullet "Plan-parse failure after a terminal write" has no seed step for a malformed stored plan, so as written it cannot reach rule 1.

### [non-spec.5.fix-design-G1.1]
DECISION: add one TEST-1 case "Plan-parse failure, committed (CODE-3 rule 1)" in finalize_race_test.go, placed directly after the lost-case bullet — BECAUSE the lost case alone passes when the reclaim is dropped; the two cases together pin rule 1 (lost case pins no-reclaim-and-write-first, committed case pins reclaim-after-commit), so no ordering callback is needed — ALTERNATIVES: a hook callback on the `failed` Update observing claim presence (redundant with the lost case); an internal-test case (cannot observe claim-sbx-1 without the pod-bind fixture).
FACT: the plan-parse branch is reachable only through a stored WorkspacePlan that workspaceplan.ParseStored rejects with an empty finalize body; /create validates plans, so a test must overwrite the row's WorkspacePlan through the wrapped inner store after /create — EVIDENCE: pkg/gateway/sessionserver/start.go:1218-1224, pkg/gateway/sessionserver/sessionserver.go:3113-3125, pkg/gateway/sessionserver/finalize.go:62-63
OPEN: the existing lost-case bullet ("Plan-parse failure after a terminal write") never says how the case reaches the plan-parse branch (no malformed-plan seed); a later round may file that as a separate finding.

### [non-spec.5.review-applicability.1]
FACT: The only ConsumedTracker implementation in pkg/ and cmd/ is MemoryTracker; its MarkConsumed returns only ErrConsumed, so rule 3's "no failure branch" rests on a single-impl contract — EVIDENCE: pkg/uploadtoken/uploadtoken.go:186-219, :363-368
FACT: No existing test pins the old consume-failure behavior (failSession + 500), so rule 3 breaks no test — EVIDENCE: grep "could not be invalidated" hits only pkg/gateway/sessionserver/sessionserver.go:3196
FACT: readyTransitionFailStore's probe still selects the guarded ready mutation (the row is finalizing at probe time), so TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 keeps pinning rule 4 (i) — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1123-1135
WATCHOUT: the handleFinalize doc comment's "Gap 2 ... or in the single-use upload-token consume ... reclaims the pod and revokes the lease" paragraph becomes false under rule 3. CODE-2 (S2) repairs only citations in that comment, and rule 3 rewrites only the inline block comment — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3052-3055
FACT: every DOCS-1, SPEC-1, and Store.Update anchor resolves uniquely in its stated section — EVIDENCE: docs/api/rest.md:93-102, docs/api/mcp.md:349, spec/15_external-api-surface.md:646, pkg/gateway/session/sessionstore/sessionstore.go:940-944

### [non-spec.5.review-citations.1]
FACT: Every file:line citation in the round-4 rule-3 rewrite resolves: MarkConsumed contract (ErrConsumed only) at pkg/uploadtoken/uploadtoken.go:190-193, ConsumeDigest delegates to tracker only at :363-368, MemoryTracker is the sole MarkConsumed implementation in pkg/ and cmd/, log.Printf at pkg/gateway/sessionserver/finalize.go:384, upload admitted only via session.Validate(EndpointUpload) with nil capabilities at pkg/gateway/sessionserver/upload.go:307-315. — EVIDENCE: grep "MarkConsumed(" over non-test Go
FACT: TEST-1 fixture ranges still hold: podBind* helpers 63-210, recordingLeaseAssigner 1145-1162, TestFinalizePostCredentialWriteFailureRevokesLease 1175 (Runtime assignment :1179), readyTransitionFailStore 1113-1136; TestFailSessionEmitsTerminalLifecycle at lifecycle_internal_test.go:290-312. — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go
WATCHOUT: handleFinalize's doc comment (sessionserver.go:3051-3054) still says a consume failure after AssignCredentials reclaims the pod; rule 3 rewrites only the consume block's inline comment. A code comment, not a spec/docs site, so not a finding under the bar; the implementor should drop that clause when applying CODE-2's comment rewrite. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3051-3054

### [non-spec.5.review-client-surface.1]
FACT: The MCP `lenny/finalize_workspace` tool is a thin proxy that dispatches `POST /v1/sessions/{id}/finalize` through the REST route, so a handleFinalize change reaches MCP with no separate code site. EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:60-72, :102
FACT: The served OpenAPI document is at pkg/gateway/externalapi/openapi/openapi.json (not pkg/gateway/openapi/). Its finalize operation already lists 409 "Invalid state transition", so the overtaken-call 409 needs no OpenAPI edit.
FACT: The client SDKs (go/python/typescript) call finalize through a generic transition helper and have no finalize-specific error mapping. EVIDENCE: sdks/client/go/lenny/client.go:217-220, sdks/client/typescript/src/client.ts:285-286
FACT: Today the upload-token consume already runs after the ready write, and its failure branch calls failSession, which writes `failed` over `ready`. CODE-3 rule 3 removes that branch, and the only ConsumedTracker implementation in the tree is MemoryTracker. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3190-3200, pkg/uploadtoken/uploadtoken.go:212

### [non-spec.5.review-docs-alignment.1]
FACT: DOCS-1 anchors resolve today: the finalize "Key error codes" line is docs/api/rest.md:100 and the finalize_workspace INVALID_STATE_TRANSITION row is docs/api/mcp.md:349. No tier-11 test pins either string. — EVIDENCE: grep over tests/tier11_docs for finalize_workspace or "Key error codes" returns nothing
FACT: The CODE-3 rule-3 rewrite (consume stays after the ready write, and a failure is logged) leaves every docs statement about the token true. MarkConsumed fails only with ErrConsumed (pkg/uploadtoken/uploadtoken.go:190-193), so the token is consumed on every path, and docs/client-guide/session-lifecycle.md:199 and docs/reference/error-catalog.md:106 stay correct. — EVIDENCE: pkg/uploadtoken/uploadtoken.go:212-219
DECISION: no docs finding this round — BECAUSE the only changed client outcomes are the overtaken-call 409 (DOCS-1 covers REST and MCP) and the refused-overlap 409 (the existing precondition lines cover it) — ALTERNATIVES: filing the ambiguous ready-commit 500-on-ready as undocumented was rejected as a re-litigation of the round-fixed "Ambiguous ready-write commit" entry, whose response is the existing 500
WATCHOUT: `maxFinalizingTimeoutSeconds` appears nowhere in docs/ before DOCS-1. The spec key is `gateway.maxFinalizingTimeoutSeconds` (spec/11_policy-and-controls.md:226). DOCS-1 glosses it as "the gateway's finalizing timeout", so it is not a defect. — EVIDENCE: grep -rn maxFinalizingTimeout docs returns nothing

### [non-spec.5.review-edit-sites.1]
FACT: The MCP tool `lenny/finalize_workspace` proxies to REST `/finalize`, so DOCS-1's mcp.md row edit describes the same handler behavior as the rest.md paragraph — EVIDENCE: pkg/gateway/mcpfabric/mcptools/client_tools.go:102
FACT: No spec/, docs/, schemas/, or charts/ surface describes the old consume-failure behavior (session failed plus 500 "upload token could not be invalidated"), so CODE-3 rule 3's switch to log-only needs no doc edit; only one ConsumedTracker implementation exists (MemoryTracker), which returns only ErrConsumed — EVIDENCE: pkg/uploadtoken/uploadtoken.go:189-193, :212; grep of docs/ and spec/ for "invalidat" finds no site
FACT: Nothing under schemas/ covers the finalize REST responses (no OpenAPI); the only finalize hits are the adapter proto and chart RBAC/alert files, and none of them change — EVIDENCE: grep -rli finalize schemas charts
FACT: The finalize sites in client docs (docs/client-guide/session-lifecycle.md:174-199 and :523-532, and docs/reference/state-machines.md:105-106) state only the precondition and the happy path, and they stay true after SPEC-1 — EVIDENCE: those lines

### [non-spec.5.review-fresh.1]
FACT: the only ConsumedTracker implementation in the tree is MemoryTracker; its MarkConsumed returns nil or ErrConsumed only, so CODE-3 rule 3's "fails only through ErrConsumed" holds today (no Redis tracker exists despite the interface comment) — EVIDENCE: pkg/uploadtoken/uploadtoken.go:185-220, grep "MarkConsumed(" finds no other impl
FACT: no existing test pins the old consume-failure branch (500 + failed + reclaim), so rule 3's removal breaks no test — EVIDENCE: grep of *_test.go for "could not be invalidated"/ConsumedTracker finds only upload_token_ttl_internal_test.go and uploadtoken_test.go, neither exercising finalize
FACT: mid-session uploads go through /upload-to-session with no upload token, so rule 3's "a token left unconsumed mints no upload" holds even with capabilities.midSessionUpload — EVIDENCE: pkg/gateway/sessionserver/upload_to_session.go:70-99; /upload validates without capabilities at upload.go:307-315
FACT: TestFinalizePostCredentialWriteFailureRevokesLease_spec_7_1 still passes under failFinalizing: readyTransitionFailStore's probe only rejects a mutation that yields `ready`, so the failFinalizing Update passes through, commits `failed`, and sub-branch (i) reclaims once — EVIDENCE: pkg/gateway/sessionserver/start_pod_test.go:1123-1136, :1240-1266
WATCHOUT: after CODE-3, the handleFinalize doc comment's "Gap 2: a failure ... or in the single-use upload-token consume ... reclaims the pod and revokes the lease" paragraph becomes false; CODE-2 only stages citation repair for that comment. Code comment only, below the finding bar; an implementor should rewrite it — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3048-3051
UNVERIFIED: Binder.Prepare's failPhase still issues DeleteClaim when an overtaken call's prepare fails after the terminal writer released the pod (the same no-owner-check hazard the Non-goals cite); pre-existing and not claimed fixed by 0082. Belongs with 0081's reclaim work if anyone pursues it.

### [non-spec.5.review-kubernetes.1]
FACT: The proposal writes no CRD status, adds no finalizer, controller, or webhook; its only Kubernetes surface is the by-name SandboxClaim DELETE it avoids on lost exit writes. Lens returned no findings. — EVIDENCE: pkg/gateway/podlifecycle/podclaim/claimer.go:322-333 (DeleteClaim on claim-<podName>, no owner check), pkg/gateway/podlifecycle/podsession/binder.go:1097-1102 (ReclaimClaimed revokes lease then deletes claim)
WATCHOUT: Rule 2's prepare-failure reclaim is failPhase's drain of the Sandbox, not a claim DELETE, and it runs before failFinalizing checks the row; a terminal writer that already released the pod does not stop that drain. Pre-existing and outside this proposal's scope. — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1072-1082
FACT: terminalReclaimPreRunning skips the by-name reclaim when the session holds a live podRegistry BindResult; finalize never calls podRegistry.Put (only start.go does), so TEST-3 Case B's single claim-sbx-1 Delete expectation holds. — EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605, pkg/gateway/sessionserver/start.go:2936,4057

### [non-spec.5.review-mechanism.1]
FACT: The only ConsumedTracker implementation in the tree is uploadtoken.MemoryTracker; its MarkConsumed returns only nil or ErrConsumed, so CODE-3 rule 3's log-only consume branch fires only for an already-consumed digest. EVIDENCE: pkg/uploadtoken/uploadtoken.go:190-193, :203-212, :363-368
FACT: REST /upload passes no capabilities to session.Validate, so the midSessionUpload-gated `running` state is never admitted there; rule 3's "uploads are admitted only in created" holds for the token-verified path. EVIDENCE: pkg/gateway/sessionserver/upload.go:302-315, pkg/api/v1/session/session.go:282-287
FACT: terminalReclaimPreRunning needs a non-nil podRegistry and no live BindResult; both the tier-4 eager fixture and the tier-1 pod-bind fixture set PodRegistry, so TEST-3 Case B's two-release expectation is reachable. EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605, tests/tier4_integration/eager_claim_lifecycle_test.go:276
WATCHOUT: After rule 3 lands, the handleFinalize doc comment still says a consume failure reclaims the pod and revokes the lease ("Gap 2: ... or in the single-use upload-token consume"). CODE-2 rewrites that comment only for citation repair; the implementor should drop the consume clause too. Code comment only, below the finding bar. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3053-3056

### [non-spec.5.review-performance.1]
FACT: The design adds no store write. The entry and exit guards run inside the existing locked Update mutation, which is pure in-memory work, so row-lock hold time does not change. A refused entry call now writes nothing, where the shipped code wrote `finalizing` and the WorkspacePlan. It adds no etcd write, no watch, and no Redis key. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3168-3171 (the unguarded ready Update today)
FACT: Production wires the upload ConsumedTracker as the in-memory MemoryTracker. Its only error is ErrConsumed, so CODE-3 rule 3's log-and-continue branch has no Redis-reset failure mode. EVIDENCE: cmd/lenny-gateway/stores.go:385-386, pkg/uploadtoken/uploadtoken.go:211-219
WATCHOUT: Rule 3's clause "a token left unconsumed mints no upload because uploads are admitted only in created" is broader than upload.go, which also admits `running` when midSessionUpload is set. The scenario cannot occur, because an ErrConsumed result means the token is already consumed, so this is not material. EVIDENCE: pkg/gateway/sessionserver/upload.go:307-315
FACT: Under Postgres failover, rule 4 sub-branch (i) can reclaim a pod whose ready write actually committed, when failFinalizing also returns a transport error. This is shipped behavior ("as today") and no worse after the change.

### [non-spec.5.review-reliability.1]
FACT: Neither session store retries a mutation. pgstore.Update runs one pgtenant.InTx with no retry loop, and no decorator implements sessionstore.Store. So an ambiguous commit can never come back as a PreconditionError against the call's own write, and CODE-3 rule 4's lost branch never revokes a lease from a `ready` session. EVIDENCE: pkg/gateway/storage/pgtenant/pgtenant.go:76-102, pkg/gateway/session/sessionstore/pgstore/pgstore.go:404-464
FACT: Every terminal writer that can overtake a finalizing row reaches recordSessionCompleted and then terminalReclaimPreRunning. The writers are the watchdog (via WithTerminalHook, cmd/lenny-gateway/controlserver.go:237), the orphan session reconciler (orphansession.go:368), and DELETE/terminate. Finalize never calls podRegistry.Put, so the by-name ReclaimClaimed path runs. This backs the rule that lost branches skip reclaim. EVIDENCE: pkg/gateway/sessionserver/usage.go:581-605
FACT: The upload path passes no capabilities to session.Validate, so uploads are admitted only in `created` even though the upload.go:307-309 comment mentions midSessionUpload. Rule 3's rationale holds in code. EVIDENCE: pkg/gateway/sessionserver/upload.go:310-313, pkg/api/v1/session/session.go:282-287
WATCHOUT: The handleFinalize doc comment (sessionserver.go:3052-3055) still states that a failure in the upload-token consume reclaims the pod and revokes the lease. Rule 3 rewrites only the block comment. This is stale after CODE-3, but it is a code comment, so it falls outside the finding bar. The implementor should fix it together with the CODE-2 doc-comment rewrite. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3052-3055
WATCHOUT: uploadLimits.closeSession is called only from the finalize success tail, so DELETE/terminal paths never drop sessionBytes. The leak predates this proposal, and the proposal's Edge-case bullet accepts it for the lost branches. EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3205-3209, upload_limits.go:166-179

### [non-spec.5.review-security.1]
FACT: production wires uploadtoken.NewMemoryTracker (not Redis despite the interface comment), whose MarkConsumed returns only nil or ErrConsumed, so CODE-3 rule 3's log-and-continue drops no fail-closed branch that is reachable today. EVIDENCE: cmd/lenny-gateway/stores.go:385-386, pkg/uploadtoken/uploadtoken.go:189-220
FACT: the upload token is verified only on /upload (upload.go:302, :828), and upload admission passes no capabilities to Validate, so an unconsumed token after `ready` mints nothing. EVIDENCE: pkg/gateway/sessionserver/upload.go:302-315
FACT: every non-finalize writer that moves a row off `finalizing` writes a terminal state (the watchdog, the orphan reconciler at orphansession.go:346, terminate, and DELETE), and applyFailure no-ops on pre-running rows, so revoking the lease on a plain lost ready write cannot revoke a live session's lease. EVIDENCE: pkg/gateway/sessionserver/failure.go:147-153
WATCHOUT: this proposal does not fix, and documents as a shipped-tree defect, a pre-existing isolation gap: ReclaimClaimed returns the pod to the pool while an overtaken Prepare keeps running (PrepareWorkspace, RunSetup, AssignCredentials) against it, and prepareAtFinalize/failPhase can still drain or DeleteClaim after a terminal writer. Do not file it against 0082; it belongs to the "terminate does not abort in-progress setup" follow-up. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1072-1102

### [non-spec.5.review-single-source.1]
FACT: After the round-4 rewrite of CODE-3 rule 3 (consume stays after the ready write, failure branch replaced by a log), no stale "consume ahead of the ready write" text remains in summary, non-spec, or spec-changes; summary Decision line 23 and Non-goal line 51 cite rule 3 by number rather than restating it. — EVIDENCE: proposals/0082_*/0082_*.summary.md:23,51; non-spec-changes.md:158
FACT: The exit-loss cascade has one stating site (non-spec-changes.md CODE-3 "handleFinalize rules"); Design bullet, summary Decisions, and Edge cases cite it. The DeleteClaim-no-owner-check reason repeats across rule preamble, revokeFinalizeLease comment, summary Watch-out and Non-goal, but each is rationale, not a copy of the rule.
DECISION: Did not file TEST-1 per-case assertions as copies of CODE-3 rules — BECAUSE each case is one case per rule/sub-branch and its assertions are the concrete observables a test must name; all five agree with rules 1-4 today — ALTERNATIVES: filing under (g) rejected as over-reach against the (f) requirement for concrete assertions.
FACT: MemoryTracker is the only ConsumedTracker implementation in pkg/cmd, so rule 3's "admits one error, ErrConsumed" holds for the tree even though the interface comment says production wires Redis. — EVIDENCE: pkg/uploadtoken/uploadtoken.go:183-193,212

### [non-spec.5.review-test-coverage.1]
FACT: No test in the tree exercises handleFinalize's plan-parse failure branch (grep for "could not be parsed" and storedWorkspacePlanForFinalize in *_test.go returns nothing). To reach it, seed a memstore row whose WorkspacePlan is malformed and send finalize with no body. resolveFinalizePlan returns hasPlan=false for an empty body without reading the row plan, so storedWorkspacePlan(row) is the parser that fails. — EVIDENCE: pkg/gateway/sessionserver/finalize.go:62-63, :331-340; pkg/gateway/sessionserver/start.go:1218-1224
DECISION: filed the missing committed-branch test for CODE-3 rule 1 — BECAUSE rule 1 reorders failFinalizing ahead of reclaimFinalizedPod and makes the reclaim conditional, and the only listed rule-1 case (lost) passes when the reclaim is dropped entirely — ALTERNATIVES: tier-3 contract test for the new 409 (rejected: the envelope is existing and tier-1 cases assert details), tier-9 (already rejected at review-log line 422)
FACT: The rule-3 rewrite (consume stays after ready, log-and-continue) is pinned by the TEST-1 "Consume after the ready write" case, and no existing test asserts the old consume-failure 500 (grep "could not be invalidated" in tests returns nothing), so no test disposition is needed. — EVIDENCE: pkg/gateway/sessionserver/sessionserver.go:3189-3198
