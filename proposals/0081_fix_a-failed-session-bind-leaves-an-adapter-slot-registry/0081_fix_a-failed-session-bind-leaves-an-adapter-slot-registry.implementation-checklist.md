## Implementation checklist

Every spec step leads. Each implementation step carries the tests for the tiers its line
names, per `## Testing` in the non-spec changes file.

- [ ] **S1 · spec** — SPEC-5. §4.7.1 gains the bind-epoch block after the RPC tables, stating what the adapter mints, which bind-sequence responses report it, that no bind-sequence request carries one, that it is not a coordination generation, and the caller rules; §15.4 gains the published bind-epoch contract and the slot-identifier reclaim-hold contract after the SDK-warm demotion contract, each pointing at its normative home and each carrying its own non-conformance statement. SPEC-5 leads because the §4.7 `Shutdown` row, §5.2's reclaim-hold paragraph and §7.1's obligation each point at the §4.7.1 block; the §4.7.1 block's own pointer at §5.2's reclaim hold resolves when S4 lands, which is the one citation this order cannot put first.
      Tiers 0, 11. Depends on: —
- [ ] **S2 · spec** — SPEC-1. §4.1's scope sentence and the §4.7 `Shutdown` row state the slot release and the runtime teardown as two teardowns with two preconditions, state the epoch comparison and the three outcomes §4.7.1 defines, and state the no-op answer for a session the adapter holds no entry for. §29.4's session-end step 13 restates the graceful-shutdown signal's co-tenancy condition and cites §4.7.
      Tiers 0, 11. Depends on: S1
- [ ] **S3 · spec** — SPEC-2. §7.1 gains the pod-side reclaim obligation as a paragraph of its own after the atomicity paragraph, covering the creation finalize block, the §15.1 start transition and the §7.3 re-attach, with the obligation's begin and end boundary, the rule that the compensation carries the bind epoch the attempt holds and never re-dials, its `leaked` disposition, the exclusive-pod case and the tree hazard; §7.2's mid-resume snapshot-close sequence loses its premise sentence, gains one sentence in step 2 and a replaced step 3; §5.2's slot-retry `**Max retries:**` bullet states the placement constraint; §7.3's resume flow, §6.2's mid-resume cancel bullet and §4.7.9 step 5 point at §7.1.
      Tiers 0, 11. Depends on: S1
- [ ] **S4 · spec** — SPEC-3. §5.2's `**Slot cleanup:**` action list gains the slot's credential directory and the §4.9 timer cancellation; its `**Scrub model.**` paragraph covers the cleanup of a bind abandoned or failed after its slot enters `receiving_uploads` and before it reaches `running`, states that the cleanup reports no outcome, and states one cleanup-outcome report per session release; and a `**Slot-identifier reclaim hold.**` paragraph states the hold from the deregistering critical section until the cleanup finishes, the transient refusal it imposes, and what bounds it.
      Tiers 0, 11. Depends on: S1
- [ ] **S5 · spec** — SPEC-4. §6.2 gains the `receiving_uploads → slot_cleanup` edge and the pre-`running` cleanup paragraph, which cites the §5.2 sentences S4 lands. Tier 11 is deferred to S6 and tier 1 to S7, because the reader-facing table and the transcribed edge list are the two artifacts those gates reconcile against this block.
      Tiers 0. Depends on: S4
- [ ] **S6 · docs** — DOCS-1, DOCS-2. The per-slot sub-state table in the state-machine reference gains the row matching the new §6.2 edge. The adapter-contract reference's `Shutdown` row states the two teardowns with their two preconditions, the withheld cleanup-outcome report, the drain gate's own condition, the bind-epoch precondition and the clean-exit answer; the page gains the bind-epoch and reclaim-hold block after its scrub-responsibilities paragraph; and its `DemoteSDK` row states that the demotion drops the adapter's registry entry. The shipped `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` is extended in place with the substrings that pin them.
      Tiers 0, 11. Depends on: S2, S5
- [ ] **S7 · code** — CODE-3. `ValidTransitions()` and its doc comment in `pkg/sandbox/slotstate` gain the new edge, and `TestValidTransitions_spec_6_2` moves to the seven-edge set with a positive and a negative `IsValid` assertion.
      Tiers 0, 1. Depends on: S5
- [ ] **S8 · schema** — SCHEMA-1. `schemas/lenny-adapter.proto` gains the `SlotReclaimOutcome` enum and the nine additive fields, `make generate-proto` runs, and the regenerated `pkg/proto/adapter/v1` package lands in the same commit. The two `WIRED` rows are added to the `EXPLICIT` list in `scripts/seed-claim-register.py` and `tests/claim-map.json` is regenerated from it by `python3 scripts/seed-claim-register.py --out tests/claim-map.json` in the same commit, so both generated artifacts in this step are produced rather than hand-written. The shipped closed-field-set gate `TestShutdownMessagePostRemovalDescriptor_spec_4_1` moves with the proto in the same commit: its `wantReq` gains `7: "expected_bind_epoch"`, its `ShutdownResponse` `assertFieldSet` call gains `3: "slot_reclaim"`, and its doc comment and `// diagnosis:` name the fence and the outcome. The edit takes rule S-2's second window, whose precondition is that every in-flight `pkg/adapter` handler edit has merged, so this step precedes every step below that opens a handler file.
      Tiers 0, 3. Depends on: S1, S2
- [ ] **S9 · code** — CODE-6. `pkg/adapter/bindepoch.go` lands the epoch counter and its lazy seed, `slotState.epoch`, the reclaim-hold side table and the single `reclaimSlotLocked` helper that every deregister-then-destroy site takes it through (`Shutdown`, `releaseSessionSlot`, and the §10.1.4 hold termination, whose `heldSession` gains the carried release), the refusal sentinel, and the two shared resolve helpers; `ensureSlotStateLocked` gains the hold refusal and mints the epoch on its entry-creating branch alone, `ensureSlotPaths` and `resolvePrepareStagingDir` widen to return the epoch, the five resolve sites move to the shared helper, the seven handlers report the entry's epoch on their responses, and the adapter client gains the per-connection epoch latch with its clear on `DemoteSDK` and on a `RECLAIMED` reclaim, `BindEpoch`, and `ShutdownReclaim`. Its tier-3 work is the response-side epoch cases and the caller-latch case in the new `tests/tier3_contract/adapter_bind_epoch/`, and `tests/spec-map.json` gains that directory entry here. Its tier-1 work is the cases in the new `pkg/adapter/bindepoch_test.go` that CODE-6 alone makes pass; the `Shutdown`-outcome and `Shutdown`-hold cases in that file land with CODE-1 and the start-confirmation cases with CODE-2.
      Tiers 0, 1, 3. Depends on: S1, S8
- [ ] **S10 · code** — CODE-1. The adapter's `Shutdown` refuses a reclaim naming a bind epoch the entry does not carry, answering `SLOT_RECLAIM_OUTCOME_ABSENT` or `SLOT_RECLAIM_OUTCOME_SUPERSEDED` and removing nothing, takes the reclaim hold by routing its deregistration through CODE-6's `reclaimSlotLocked` helper and deferring the release that helper returns, releases the slot for any entry the call removed, runs the runtime teardown only for a session whose start the adapter has admitted, and files a cleanup-outcome report only for a session the shared runtime process was given, reading that last predicate through the new `runtimeHoldsLocked` accessor in `pkg/adapter/runtimegeneration.go`. Its tier-3 work is the two `Shutdown` outcome cases, which are properties of this deliverable alone, its tier-1 work adds the `Shutdown`-outcome and `Shutdown`-hold cases to `pkg/adapter/bindepoch_test.go`, and `tests/spec-map.json` gains the tier-7a and tier-9 entries for the two files this step creates. `slotAddressCaseFiles` in `tests/tier0_static/spec_map_slot_address_registration_test.go` gains both paths in sorted position in the same commit that creates them, because the credit-inventory gate reports any `slot*_test.go` file the inventory omits and this step runs tier 0.
      Tiers 0, 1, 3, 7a, 9. Depends on: S2, S4, S9
- [ ] **S11 · code** — CODE-2. `noteRuntimeStarted` takes the bind epoch its own claim observed and confirms the registry still holds an entry bound to this session at that epoch, and `StartSession` and `Resume` take the session back off the runtime and refuse the start when it does not, reporting no cleanup outcome. Both orderings are covered: the reclaim landing after the claim leaves no entry, and the reclaim landing before it leaves an entry at a different epoch, which is the ABA case. Its tier-1 work includes re-fixturing the shipped callers that record without a bound entry, and the two start-confirmation cases in `pkg/adapter/bindepoch_test.go`.
      Tiers 0, 1, 7a. Depends on: S3, S10
- [ ] **S12 · code** — CODE-4. The gateway sends the compensating `Shutdown` at every post-connection bind failure and at a failed `Resume`, on the still-open connection and a detached context, carrying the bind epoch the connection latched, and never re-dialling. It maps the answer outcome-first (RPC error leaked; `SUPERSEDED` or `ABSENT` not leaked whatever `exited_cleanly` reports; `RECLAIMED` and not clean leaked; otherwise not leaked), releases the session's credential leases on the bind paths, and carries the disposition through `ReleaseSlotReservation` and, on the §7.3 re-attach, through the `*SlotBindError` that `Binder.Resume`'s failure branch now returns in its error chain, which is what S13's third caller reads. `tests/spec-map.json` gains here the whole-file credits under sections 7.1 and 4.7 for `tests/tier4_integration/concurrent_workspace_test.go`, the file this step's tier-4 case lands in, because `TestSlotAddressCasesAreCreditedToEverySectionTheyAnnotate` checks a file the map registers as a whole against the union of the sections cited anywhere in it and this step runs tier 0.
      Tiers 0, 1, 4, 7a. Depends on: S3, S4, S10, S11
- [ ] **S13 · code** — CODE-5. One `accountSlotFailure` helper, whose request-derived parameters narrow to `pool string, maxConcurrentSessions int32`, serves every bind path the §7.1 obligation binds — `applySlotRetryPolicy`, `bindConcurrentSlot`'s reserved branch, and `resumeOnPod`'s `podBinder.Resume` failure branch — so the create-time reserved path and the §7.3 re-attach both reach the §5.2 threshold, and the retry after an incomplete reclaim carries `ExcludePods` so `ClaimSlot` places it on a different pod; `isTransientPodClaimError` gains the `codes.Aborted` arm, so a §7.3 resume refused by the reclaim hold or rolled back by CODE-2 reverts the row to `awaiting_client_action` rather than being demoted to terminal `failed`. `tests/spec-map.json` gains here the per-case entries for the cases this step creates in files the map already registers case by case: the four `applySlotRetryPolicy` cases in `pkg/gateway/sessionserver/slotretry_test.go` and the datastore-crossing case in `tests/tier4_integration/recycle_scrub_path_test.go`, each under sections 5.2, 6.2 and 7.1, because `TestSlotAddressCasesAreCreditedToEverySectionTheyAnnotate` checks such a file case by case and this step runs tier 0.
      Tiers 0, 1, 2, 4. Depends on: S12
- [ ] **S14 · code** — CONF-1. `tests/tier10_conformance/bind_epoch_conformance_test.go` lands the four properties of §15.4's published bind-epoch contract: the epoch is minted per entry and strictly increases, one entry reports one epoch on every response, a superseded reclaim performs nothing, and a zero epoch is the unconditional teardown. `tests/spec-map.json` gains that file's entry here.
      Tiers 0, 10. Depends on: S1, S10
