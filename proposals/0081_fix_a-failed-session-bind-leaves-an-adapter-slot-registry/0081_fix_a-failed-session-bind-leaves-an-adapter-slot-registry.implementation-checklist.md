## Implementation checklist

Every spec step leads. Each implementation step carries the tests for the tiers its line
names, per `## Testing` in the non-spec changes file. Every step runs tier 0, registers its
tests in `tests/spec-map.json` under the sections its `// spec:` annotations cite, carries a
`// diagnosis:` comment at tier 2 and above, and cites the specification by heading. A step
that changes the wire or adds a normative claim adds its `tests/claim-map.json` rows by
editing the `EXPLICIT` list in `scripts/seed-claim-register.py` and regenerating from it in
the same commit.

One ordering rule governs the code lane beyond the dependency lines below: the gateway learns
to send the new fields before the adapter begins requiring them. S13 mints and carries the
attempt token and sets `unconditional_teardown` at every non-compensating `Shutdown` caller;
S14 and S16 then refuse a request that carries neither. Landing them in the other order
refuses every bind and every teardown in the window between the two steps.

- [ ] **S1 · spec** — SPEC-5. §4.7.1 gains the bind-attempt block after the RPC tables: the caller mints one opaque token per bind attempt before that attempt's first RPC, the adapter treats it as opaque and never parses or orders it, the adapter writes it onto the entry it creates and never onto an entry it resolved in either direction, and the adapter resolves or creates the entry, stamps the token, and performs the comparison as one indivisible step under the lock that guards the registry. The block states admission and teardown as two named, ordered cascades with one table of which request carries which field; it is the single statement of both and no other carrier restates a rule's condition. §15.4 is re-cut to name the rule set, state the conformance criterion, and state the three non-conformances that are not any single rule's condition: a wrong evaluation order, a separable step, and a gRPC error for an outcome the reclaim-outcome rule reports. The slot-identifier reclaim-hold block is not rewritten. §15.1 takes no new catalog row for either adapter error code, because the gateway consumes both refusals as adapter-contract codes and renders neither into the REST error envelope; §15.1's `SETUP_COMMAND_FAILED` row instead has its cause sentence, its retryability sentence and its setup-output sentence replaced, so the row states both deterministic `FAILED_PRECONDITION` failures the adapter answers to the setup-command request and qualifies its setup-output remedy to the cause that ran a command. The row's exclusion sentence is keyed on the gRPC code and stands unedited. §6.2's pre-attached `**Client visibility:**` bullet has its second clause re-keyed the same way, on the request the adapter answered as well as on the gRPC code. SPEC-5 leads because the §4.7 `Shutdown` row, §5.2's reclaim-hold paragraph and §7.1's obligation each point at the §4.7.1 block. Three staged citations forward-reference S4: the §4.7.1 block's own pointer at §5.2's reclaim hold, §7.1's assertion that §5.2 states the hold, and §15.4's reclaim-hold block. Each resolves when S4 lands, and the applied spec is correct once all four spec steps have landed.
      Tiers 0, 11. Depends on: —
- [ ] **S2 · spec** — SPEC-1. §4.1's `ShutdownRequest` justification is restated per message, naming the requests that carry `bind_attempt` and the one that carries `unconditional_teardown`, and stating that both are bare scalars with no wire presence so §4.1's closing rule that no operation is selected by a field's presence standing in for a scope is untouched. The §4.7 `Shutdown` row states the slot release and the runtime teardown as two teardowns with two preconditions, names the outcomes §4.7.1 defines rather than counting them, states the two-field precondition and its `INVALID_ARGUMENT` answer, and points at §4.7.1 for the named rules that decide both teardowns and for the `absent` answer a session the adapter holds no entry for is given. §29.4's session-end step 13 restates the graceful-shutdown signal's co-tenancy condition and cites §4.7.
      Tiers 0, 11. Depends on: S1.
- [ ] **S3 · spec** — SPEC-2. §7.1 gains the pod-side reclaim obligation as one paragraph inserted after the atomicity paragraph, which is not itself edited. Its scope is the §15.1 start transition onto a slot on a pod serving concurrent sessions, whether that slot was reserved at creation or placed by §5.2's slot retry policy, together with the §7.3 re-attach onto a replacement pod. It carries the obligation's begin and end boundary, the rule that the compensation carries the token its own attempt minted, its `leaked` disposition, the exclusive-pod case and the tree hazard. The connection sentence loses its correctness load: the attempt holds its own token independent of any connection, so a compensation on a fresh connection is fenced exactly as one on the connection the failed stage still holds, and the open connection is used when it is available because it costs nothing. §7.2's mid-resume snapshot-close sequence loses its premise sentence, gains one sentence in step 2 and a replaced step 3; §7.3's resume flow, §6.2's mid-resume cancel bullet and §4.7.9 step 5 point at §7.1. §5.2's slot-retry `**Max retries:**` bullet is left as it stands, because the placement constraint it carried came out with the pod-exclusion mechanism.
      Tiers 0, 11. Depends on: S1.
- [ ] **S4 · spec** — SPEC-3. §5.2's `**Slot cleanup:**` action list gains the slot's credential directory and the §4.9 timer cancellation, and the same bullet's leaked-outcome sentence gains the exception for a reclaim that closes the session cleanly and fails only in removing the slot's slot tree; its `**Scrub model.**` paragraph covers the cleanup of a bind abandoned or failed after its slot enters `receiving_uploads` and before it reaches `running`, states that the cleanup reports no outcome, and states one cleanup-outcome report per session release; and a `**Slot-identifier reclaim hold.**` paragraph states the hold from the deregistering critical section until the cleanup finishes, the transient refusal it imposes, and what bounds it. The paragraph's cross-reference points at §4.7.1's bind-attempt block.
      Tiers 0, 11. Depends on: S1.
- [ ] **S5 · spec** — SPEC-4. Two fence edits, two prose edits in §6.2 and one bullet pair in §4.6.1. The per-slot sub-state fence gains the `receiving_uploads → slot_cleanup` edge, and the prose after the fence gains the pre-`running` cleanup paragraph, which cites the §5.2 sentences S4 lands. Every claim-deletion statement of the occupancy projection is re-keyed on the phase the pod projects at the claim DELETE: the fence's `claimed ──→ draining` trigger list, three clauses of §6.2's projection prose, and both claim-deletion bullets of §4.6.1's occupancy-projection list, whose false closing sentence is deleted rather than reworded. Tier 11 is deferred to S7 and tier 1 to S8, because the reader-facing table and the transcribed edge list are the two artifacts those gates reconcile against this block.
      Tiers 0. Depends on: S4.
- [ ] **S6 · spec** — SPEC-6. §16.1's metric catalog gains a row for each counter S10 emits: a compensating `Shutdown` answered `superseded`, which is a gateway series, and a `Shutdown` that met an entry carrying no token, which is an adapter series and whose row records the adapter metrics endpoint's standing deferral. Neither row states an expected value or an expected rate.
      Tiers 0, 11. Depends on: S1.
- [ ] **S7 · docs** — DOCS-1, DOCS-2, DOCS-3. The per-slot sub-state table in the state-machine reference gains the row matching the new §6.2 edge. The adapter-contract reference's `Shutdown` row states the two teardowns with their two preconditions, the withheld cleanup-outcome report, the drain gate's own condition, the two-field precondition and the clean-exit answer; and its `DemoteSDK` row states that the demotion drops the adapter's registry entry. The published error catalog takes no new row; its `SETUP_COMMAND_FAILED` row takes the four sentence replacements and the replaced remedy cell that mirror SPEC-5's §15.1 edits, including the retryable-fallback sentence re-keyed off the cause class, and no gate holds that page to §15.1. The shipped `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` is extended in place with the substrings that pin them.
      Tiers 0, 11. Depends on: S1, S2, S5.
- [ ] **S8 · code** — CODE-3. `ValidTransitions()` and its doc comment in `pkg/sandbox/slotstate` gain the new edge, and `TestValidTransitions_spec_6_2` moves to the seven-edge set with a positive and a negative `IsValid` assertion.
      Tiers 0, 1. Depends on: S5.
- [ ] **S9 · schema** — SCHEMA-1. `schemas/lenny-adapter.proto` gains `ERROR_CODE_SLOT_BIND_ALREADY_STARTED` and `ERROR_CODE_SLOT_BIND_ATTEMPT_SUPERSEDED`; `bind_attempt` on `PrepareWorkspaceRequest` field 5, `FinalizeWorkspaceRequest` field 6, `RunSetupRequest` field 5, `AssignCredentialsRequest` field 4, `ResumeRequest` field 16 and `ShutdownRequest` field 7; `mid_session` on `PrepareWorkspaceRequest` field 6; `unconditional_teardown` on `ShutdownRequest` field 8; and the `SlotReclaimOutcome` enum with `ShutdownResponse.slot_reclaim` at field 3. No response reports a bind attempt. `make generate-proto` runs and the regenerated `pkg/proto/adapter/v1` package lands in the same commit, as do the two `WIRED` rows added to the `EXPLICIT` list in `scripts/seed-claim-register.py` and the regenerated `tests/claim-map.json`. The shipped closed-field-set gate `TestShutdownMessagePostRemovalDescriptor_spec_4_1` moves in the same commit: its `wantReq` gains `7: "bind_attempt"` and `8: "unconditional_teardown"`, and its `ShutdownResponse` `assertFieldSet` call gains `3: "slot_reclaim"`. This is the only schema step. It takes rule S-2's second proto window, whose precondition is that every in-flight `pkg/adapter` handler edit has merged, so it precedes every step below that opens a handler file, and rule S-2 admits no third window.
      Tiers 0, 1, 3, 11. Depends on: S1, S2, S3, S4, S5.
- [ ] **S10 · code** — CODE-9. The counters and their catalog entries: a compensating `Shutdown` answered `superseded`, meaning the adapter held an entry the compensation was not addressed to, which states no expected value; and a `Shutdown` that met an entry carrying no token, which is zero on the bind paths and non-zero when an abandoned attempt's late `StartSession` left an entry carrying no token behind. `ShutdownResponse` carries no started signal, and the tier-3 closed-field-set gate belongs to S9. The sites are `pkg/observability/metrics/catalog.go` and the `spec161Metrics` list in its `catalog_test.go` for the superseded series alone, `pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go` and `gatewaymetrics.go` for the gateway collector and its accessor, `pkg/adapter/metrics.go` for the adapter series, `docs/reference/metrics.md`, the `SlotReclaim` hook on `Binder` beside `SlotFailure` with its `noteCompensationOutcome` forwarder in `slotbinder.go` beside `recordSlotFailure`, the hook's wiring in `cmd/lenny-gateway/metricsbackfill.go` beside the `SlotFailure` wiring, and the new `tests/tier11_docs/slot_compensation_metric_reference_test.go`, which takes a `slotAddressCaseFiles` row and a `tests/spec-map.json` §16.1 credit in this step. The adapter series takes no `catalog.go` row and no `spec161Metrics` entry, because `spec161Metrics` is reconciled both ways against `MetricCatalog()` and the §16.1 rows carrying the adapter scrape deferral are absent from both; it is gated instead by the shipped `tests/tier11_docs/adapter_metric_catalog_test.go`, which is not edited. Separating a staging failure from a finalize failure needs a new stage constant, because both record `slotFailureWorkspacePrep` today. This step's tier-1 work is the catalog transcription and the collectors; the assertions that each counter fires belong to S16 and S19, where the call sites land.
      Tiers 0, 1, 11. Depends on: S6, S9. The forwarder's parameter is the `SlotReclaimOutcome` the schema step mints, so this step cannot compile before S9 lands.
- [ ] **S11 · code** — CODE-7. `pkg/gateway/runtime/adapterclient` gains a translation from the gRPC status detail to two sentinel errors matched with `errors.Is`, following the `RunSetup` detail-reading pattern that is the gateway's only status-detail reader today. Without it the reclaim closures, the compensation and the outcome mapping have no way to tell a refusal from a failure, because `adapterv1.Error` carries no reason field and every gateway consumer of a bind failure matches on Go types.
      Tiers 0, 1, 3. Depends on: S1, S9.
- [ ] **S12 · code** — CODE-8. `Binder.Prepare`'s reclaim closure and `Binder.Launch`'s take the failing error as a parameter, every `reclaim()` call site in both functions passes the error it is about to return, and the closures skip `failPhase` on either typed refusal so the call site returns the refusal and the pod is not drained. `failPhase` gates only `releaseCredentials` on `leaseAssigned` and drains on every call, so a refusal that reaches it retires a pod that is serving the session correctly. This step blocks production use of the gates S14 lands and is the reason they cannot go first.
      Tiers 0, 1, 2. Depends on: S1, S11.
- [ ] **S13 · code** — CODE-4. The gateway mints an opaque `crypto/rand` token once per bind attempt at `materializeSlot`, `Binder.Prepare` and `Binder.Resume`, and carries it on each request that takes it. `Binder.Launch` mints none: it runs in a separate HTTP request from the `Prepare` that created the entry and issues only `ConfigureWorkspace` or `StartSession`, neither of which carries a token. Every non-compensating `Shutdown` caller sets `unconditional_teardown = true`, and the field is set inside `Client.Shutdown` and `Client.ShutdownRecycle` rather than at any call site, so the shipped callers (`Binder.ReleaseSlot`'s session-end teardown and its recycle disposition, `Binder.shutdownAdapter`'s two, and the §11.4 revoke fan-out) are carried without being edited. The mint site records that `crypto/rand.Read` panics rather than erroring on the pinned Go version and writes no error branch, so a later reader does not add one.
      Tiers 0, 1, 2, 3, 4. Depends on: S1, S2, S9, S11.
- [ ] **S14 · code** — CODE-6. `slotState` gains `bindAttempt`, and `ensureSlotStateLocked` and `ensureSlotPaths` take a resolve descriptor carrying what the caller asserts: the attempt token, whether the caller may create, and whether it may resolve a started entry. Under `s.mu`, before anything is written, it evaluates §4.7.1's cascade in that section's order, from the mid-session-create rule through the admit rule, with the reclaim hold ahead of the map lookup and the pairing rule already taken at each handler's entry by `validateBindFields`. Each `case` arm carries its rule's name in its comment, so the arm and the spec statement are one text. The three call sites pass what their own RPCs assert, which is what puts the claim path behind the same predicate as the workspace path. `FinalizeWorkspace` reads `mid_session` before it resolves rather than long after it, `PrepareWorkspace` latches both fields from the frame that resolves the slot identifier and reads neither again for the rest of the call, refusing no later frame on either field, and `RunSetup` and `AssignCredentials` are never mid-session. The existing untyped `Unavailable` refusal on the already-started claim becomes the typed form. CODE-6 adds the reclaim hold, the `reclaimSlotLocked` helper every deregister-then-destroy site is routed through, the refusal sentinel and the shared resolve helpers, all of them new to the tree, and carries no counter, no per-entry mint and no response reporting. The mid-session exemption rests on the gateway admitting a §7.4 upload only for a session with a live binding in the replica's `podRegistry`: confirm that guard reads what it appears to read before this step lands.
      Tiers 0, 1, 2, 3, 7a, 9, 10. Depends on: S1, S4, S9, S11, S12, S13.
- [ ] **S15 · code** — CODE-6. A per-slot guard, a capacity-one channel used as a semaphore, spans every section that writes or destroys a slot's tree outside `s.mu`, from the resolve that hands out its paths through the end of that path work, and a section whose only path work runs inside `s.mu`, and a section that only reads the tree, takes none. CODE-6's derivation table is the statement of which sections those are, and it puts `FinalizeWorkspace` and `RunSetup` across the materialization, `PrepareWorkspace` from the frame that resolves the identifier to the end of the call, `Resume` from ahead of its claim across the checkpoint restore, `releaseSessionSlot` and the §10.1.4 `terminateHeldSession` across their destructive section, and `Shutdown` on the arm that destroys alone. The gates are a fence at the resolve because they sit inside its critical section, but `ensureSlotPaths` and `checkpointRootsForSession` each release `s.mu` and return paths, so the work that follows runs unlocked against paths handed out earlier. A retry of an attempt's own RPC, and a matching `Shutdown` racing a `FinalizeWorkspace`, both reach it, and the reclaim hold reaches neither because it is tested at admission. The guard has two hand-out forms: the raw one the destructive sections take, and the hold-checking one every guarded admission RPC takes at its entry, which refuses a held identifier with the transient sentinel rather than letting the caller block. `s.mu` is never held while a guard is acquired, no path holds two guards, a section that acquires a guard after releasing `s.mu` re-reads what its decision depends on, and a guard entry lives for the life of the pod's slot identifier. `releaseSessionSlot` splits into a guard-acquiring form and an under-guard form, because `Resume` calls it from inside its own guard and a second acquisition blocks on the capacity-one channel rather than re-entering it. The guard-acquiring form takes a `context.Context` first parameter, so the seven production call sites in `session.go` and `sdkwarm.go`, the two internal test call sites and the exported `ReleaseSlotForTest` seam all thread one. Neither hand-out form waits past its caller's context; a destructive section whose acquisition does not complete removes unguarded and logs a `slot_guard_not_acquired` warning naming the slot identifier and the caller.
      Tiers 0, 1, 7a. Depends on: S1, S14.
- [ ] **S16 · code** — CODE-1. The adapter's `Shutdown` evaluates §4.7.1's teardown cascade in that section's order, from the teardown-pairing rule through the attempt-match rule. The pairing check is taken on the request's fields before the registry is read, and the comparisons that follow are one function evaluated under `s.mu`: first as a fast path that answers `absent` and `superseded` with no slot guard taken, because an arm that removes nothing touches no path, and again after the removing arm has released `s.mu`, taken the raw slot guard and re-taken `s.mu`, which is the evaluation `deregisterSlotLocked` runs inside, so an entry the call refuses to release is never removed and restored and no reclaim waits behind a materialization to report that it removed nothing. The step also carries the mechanical `UnconditionalTeardown: true` sweep over every in-tree `adapterv1.ShutdownRequest` literal that is not a bind-attempt case, because the precondition lands here and refuses a request carrying neither field before the registry is read; the set is the one `grep -rn "ShutdownRequest{" --include=*_test.go pkg/ tests/` names. The attempt-mismatch rule's entry-carries-no-token arm answers `superseded` so the rule fails closed, and it carries a counter. The arm is reachable on a conforming adapter, because `StartSession` and `ConfigureWorkspace` carry no token and create an entry carrying none when they resolve none, which §4.7.1 records as an accepted residue of the token contract. The call takes the reclaim hold by routing its deregistration through `reclaimSlotLocked` and deferring the release that helper returns, and releases the slot for any entry it removed.
      Tiers 0, 1, 2, 3, 4, 7a, 9, 10. Depends on: S1, S2, S4, S9, S10, S11, S13, S14.
- [ ] **S17 · code** — CODE-1. The `if bound` gate splits. The slot tree removal and the scrub report run whenever the call removed an entry, because the on-disk tree belongs to the slot identifier that the workspace-preparation RPCs create without binding. The usage flush, the pod-global drain and `Runtime.Close` stay behind the binding, because the runtime and the pod-global drain belong to a session that actually started, and the cleanup-outcome report is filed only for a session the shared runtime process was given, read through the `runtimeHoldsLocked` accessor. The response's `exited_cleanly` reports the tree-removal error for a reclaim the shared runtime process was never given, so this step also lands the nil-defaulted `Server.removeSlotTreeFn` seam and the `Server.removeSlotTreeVia` method the reclaim path calls, together with the tier-1 cases that drive the injected failure on both sides of that gate.
      Tiers 0, 1, 2, 4. Depends on: S2, S4, S16.
- [ ] **S18 · code** — CODE-2. `noteRuntimeStarted` takes the attempt token its own claim observed and confirms the registry still holds an entry bound to this session carrying that token; `StartSession`, `Resume` and the SDK-warm `ConfigureWorkspace` take the session back off the runtime and refuse the start when it does not, reporting no cleanup outcome, the SDK-warm site by taking the runtime half of the §6.1 demotion, calling the runtime's own `SDKWarmRuntime.DemoteSDK` and clearing `s.sdkConnected` under `s.mu` so a bare `Runtime.Close` does not leave that flag set; it removes no registry entry and no slot tree, so it routes through neither `releaseSessionSlot` nor the `DemoteSDK` RPC handler, and it answers `codes.Aborted` rather than the `codes.Internal` its sibling arms answer. Both orderings are covered: a reclaim landing after the claim leaves no entry, and one landing before it leaves an entry carrying a different token. The counter-wrap arm the epoch needed is gone, because a random token cannot repeat, so the case reduces to the entry having been replaced. Its tier-1 work includes re-fixturing the shipped callers that record without a bound entry.
      Tiers 0, 1, 7a. Depends on: S3, S14, S16.
- [ ] **S19 · code** — CODE-4. The gateway sends the compensating `Shutdown` at every post-connection bind failure and at a failed `Resume`, on a detached context, carrying the token its own attempt minted. It suppresses the compensation entirely when the failing stage was refused with either typed code, because a refusal means another attempt owns the entry. It computes the leaked disposition from the RPC error and the clean-exit flag alone, which the teardown rules make sufficient because every rule that removes no entry answers a clean exit; an outcome this build does not recognize is judged on the same two fields, so an unknown answer reporting a clean exit does not retire a healthy pod. It releases the lease identifiers this attempt's own `assignCredentials` minted rather than walking the session's leases, so a release after a successor has assigned strips nothing; the call stays unconditional and outside the `errors.As` guard, because a stage error that is not a slot bind error must still return minted leases. `Binder.Prepare` needs no compensation, because every `Prepare` failure drains the pod and the entry dies with it. The disposition travels through the `*SlotBindError` that `Binder.Resume`'s failure branch returns in its error chain, which is what S20's third caller reads; `ReleaseSlotReservation` gains a `leaked bool` parameter that every call site in CODE-4's table supplies, so `Binder.ReleaseSlot` is no longer the only producer of `leaked=true`.
      Tiers 0, 1, 2, 4, 8, 9. Depends on: S1, S3, S9, S10, S11, S13, S16.
- [ ] **S20 · code** — CODE-5. One `accountSlotFailure` helper, whose request-derived parameters narrow to `pool string, maxConcurrentSessions int32`, serves every bind path the §7.1 obligation binds (`applySlotRetryPolicy`, `bindConcurrentSlot`'s reserved branch, and `resumeOnPod`'s `podBinder.Resume` failure branch), so the create-time reserved path and the §7.3 re-attach both reach the §5.2 threshold. `isTransientPodClaimError` gains the `codes.Aborted` arm, which serves the reclaim hold's refusal, the superseded refusal and the rollback alike, so a §7.3 resume refused on any of them reverts the row to `awaiting_client_action` rather than being demoted to terminal `failed`. `tests/spec-map.json` gains the per-case entries for the cases this step creates in files the map registers case by case.
      Tiers 0, 1, 2, 4. Depends on: S3, S4, S19.
- [ ] **S21 · code** — CODE-6, CODE-1. Deterministic tier-1 cases for the adoption orderings. `Shutdown` performs only the empty-session-id check before it takes `s.mu`, so an internal adapter test holding the lock parks a compensation at the barrier with no production seam, following the existing hold-state and one-session-only cases. Cover every row of the case walk, and cover explicitly a resume landing on a leaked entry another attempt stamped, together with that resume's own lost-response compensation, because those are the two rows a comparison placed in the workspace handlers rather than at the resolve fails.
      Tiers 0, 1, 7a. Depends on: S1, S14, S16.
- [ ] **S22 · code** — CONF-1. The conformance battery is re-cut across two tiers. The battery is cut one case per named §4.7.1 rule, and the step is done when every rule in that section's set has a case at tier 3 and at tier 10. They land in a new `tests/tier3_contract/adapter_bind_attempt/`, following `adapter_generation_fence/`'s two forms: a descriptor gate for the new field numbers and types, and bufconn cases driving `adapter.New` over gRPC for the handler behaviour. The in-process battery lands at tier 10 beside `recycle_scrub_conformance_test.go`. The staged one-entry-one-epoch property is withdrawn rather than adapted, because under the token it would pass a defective adapter and fail a conforming one. `tests/spec-map.json` gains both entries, and the absence of a harness that can drive a third-party adapter registers as a `tests/claim-map.json` row with status `ABSENT`, following the rows the coordination-generation fence already carries.
      Tiers 0, 3, 10. Depends on: S1, S14, S16.
