# Review log: A failed session bind leaves a stale adapter slot registry entry

## Standing context

**Changelog (compaction pass 21).** Pass 20 lifted the ledger through the `non-spec.12` rounds and
the `f4.summary-cleanup` firing. The unlifted tail this pass read is the whole `non-spec.13` window:
its applicability, mechanism and test-coverage shards, which are the three entries the round
boundary archives whole, with their ids, as soon as this pass returns. This file still carries no
archive and no third section, so `## Standing context` and `## Ledger` are the whole live record.

Lifted from that tail: thirteen Settled entries (the checklist's clean mechanical state and the
three brief-named contradictions that are FIXED, CONF-1's sixteen rules against its two batteries,
the round-13 citation sweep, the three exhaustive call-site enumerations, the live gateway citation
set, the single closed field set over a message SCHEMA-1 opens, `stageWorkspace` as the one shared
`PrepareWorkspace` sender, the always-populated session id on every `PrepareWorkspace` frame,
DOCS-3's ungated target page, the complete tier map of `## Testing`, the per-symbol case coverage
with its three renamed survivors, and the absent `cmd/lenny-gateway` metric-hook precedent), four
Traps (the two `deregisterSlot` test callers, `Server.Resume`'s pre-start extraction ordering,
`st.started` set at `resume.go:50`, and the orchestrator brief's two standing falsehoods), three
Open items and two Deferred entries kept whole.

Retired and deleted. The caller's instruction was to retire every Open and UNVERIFIED entry whose
subject the 2026-09-16 revision deleted. No entry in `### Open` has such a subject, for the fourth
pass running: `grep -ci epoch` on `spec-changes.md` and `grep -c ExcludePods` on every staged file
both still return 0, and passes 17 through 20 retired every epoch- and exclusion-named Open item.
One survivor was found outside that section and retired: the UNVERIFIED clause riding inside the
Settled `ClaimSlot` pass-1 entry asked whether held occupancy makes a pod pass 1's own candidate,
which is a question about the placement exclusion, so it goes with the mechanism. Three epoch-named
Deferred entries were retired the same way, each because a token-era sibling carrying the same
correction stands: the seven epoch-reporting handlers and the `StartSessionResponse.bind_epoch`
half, the epoch-worded `ConfigureWorkspace` idempotent-repeat arm, and the `expected_bind_epoch`
gate quotation. Three further Deferred entries were retired as duplicates whose own successors carry
them whole. Nothing else was deleted.

Did NOT reach 1615 lines, and the section is longer than it was. Nothing was dropped to get there.
What remains is `MISTAKE`, unclosed `DEFERRED` and `OPEN`, which this pass may not drop, plus the
refuted-family index in Traps, which shards in this window again recorded as the part of this file
that saved them a round.

Two reading rules still govern. First, an entry using the words "bind epoch", `expected_bind_epoch`,
"epochless", `ExcludePod(s)` or a step number below S22 describes a withdrawn design or a superseded
sequence: its TREE facts survive and its DESIGN claims do not, and the Settled "DESIGN GENERATION"
entry states what replaced them. Second, the snapshot trap fired in this window too, so `diff -rq`
across the whole snapshot chain, newest to oldest, is the first command of any round. At round 13
both `non-spec-r13` and `non-spec-r13-start` were byte-identical to the live proposal and the newest
snapshot that actually differs is `non-spec-r12-prefix`.

The window's structural lesson has changed shape. Round 13 is the first round in which all three
lenses came back with nothing above the bar, and the single finding filed, `deregisterSlot`'s
unaccounted call sites, sits inside an enumeration the deliverable otherwise completes. The text has
stopped moving, which is what makes the snapshot check the cheapest first move rather than a
formality, and it is why the remaining cost of this proposal now sits in `### Deferred` rather than
in new findings.

### Settled

- **Adapter predicate nesting.** Entry present (`s.slots[id]`) ⊃ bound (`st.sessionID != ""`, set by both `assignCredentialsSlot` and `claimSessionSlotUnderLock`) ⊃ started (`st.started`, set only in `claimSessionSlotUnderLock`) ⊃ `runtimeLive` (set only by `noteRuntimeStarted`). EVIDENCE: pkg/adapter/slotcreds.go:32-34; slotsession.go:87-88; runtimegeneration.go:35-48; session.go:111,156,163.
- **`st.started` precedes `Runtime.Start`, and it has THREE setters.** `claimSessionSlotUnderLock` sets it at slotsession.go:88 and has three production callers: `StartSession` (session.go:111), `Resume` (resume.go:50) and SDK-warm `ConfigureWorkspace` (sdkwarm.go:217). All three also reach `noteRuntimeStarted` (session.go:163, resume.go:144, sdkwarm.go:261, the last guarded on `fresh`). On the start path `Runtime.Start` runs at :156 and `noteRuntimeStarted` at :163, and the gap spans `sessionConnectors`, `writeSessionManifest`, `startPlatformMCP` and `startConnectorMCPServers`, so it is a wide window rather than a hairline race. Pass 1 named only the `StartSession` caller, and the one-caller reading misled a fix round into naming `StartSession` in a spec predicate.
- **"The pod's shared runtime process has been given" is bound vocabulary.** It denotes `runtimeLive`/`runtimeCohort` membership in spec/04:962, spec/15:1734, spec/28:1194,1242,1735, spec/29:308,1521, docs/runtime-author-guide/integration-levels.md:99 and platform-tools.md:23, and pkg/adapter/runtimegeneration.go:8-25 defines it verbatim.
- **`runtimeLive`'s writers are exactly two.** `noteRuntimeStartedLocked` and `noteRuntimeClosed`; there is no third. EVIDENCE: pkg/adapter/runtimegeneration.go:36-49,:58-68.
- **DECISION: two predicates, not one.** The runtime teardown gates on `st.started` (over-approximates, fails closed toward closing); the cleanup-outcome report gates on `runtimeLive` membership through a new `runtimeHoldsLocked` accessor (fails closed toward not counting). CODE-1 is the first caller of that accessor.
- **DECISION: the teardown precondition names no RPC and is stated in one place.** §4.7's `Shutdown` row is the single home and reads "a session whose start the adapter has admitted", plus one sentence saying which RPC in that table starts a session depends on the pod's session mode and on whether the session is new or resumed. §4.1 points at §4.7 and restates nothing; every other site says "a start". The round-1 wording "a session whose `StartSession` the adapter has admitted" was wrong for a resumed session and for an SDK-warm pod, and enumerating the three RPCs was rejected as a closed enumeration repeated at six sites that goes stale on a fourth. The "has been given" phrase survives only where it denotes cohort membership (§6.2 `running`, the served-session count, §4.7 item 7).
- **DECISION: "the moment the adapter admits the start", not "the claim that admits the start".** `claim` in this specification denotes the `SandboxClaim` CRD, so the design's prescribed wording would have collided.
- **`RecordSessionScrub` has no per-session dedup.** It discards the session identifier and calls `IncrementSessionsServed` unconditionally before branching on `leaked`, then drives `maxSessionsPerPod` retirement off the post-increment count. Two reports for one session are two served sessions plus one bogus leak-ledger entry. EVIDENCE: pkg/gateway/mcpfabric/delegationtree/leasecontrol/scrubreport_server.go:451-481.
- **`ReportSessionScrub` is one channel carrying two things.** `IncrementSessionsServed` and `DrainLedger.RecordSessionScrub(..., leaked)`. Withholding the report withholds both, so suppressing it to protect the served-session count also suppresses the leak signal.
- **DECISION: the refused `StartSession` reports no outcome.** CODE-2's second `reportSessionScrub` call and the `errStartRaceReclaimed` sentinel are deleted. The adapter is the only place the at-most-one invariant can hold.
- **DECISION: one reporting rule, one home.** The withheld-report rule and the one-report-per-session-release rule are stated once, appended to spec/05 §5.2's `**Scrub model.**` paragraph (spec/05:453), which opens "uniform across session-mode configurations". SPEC-2 and SPEC-4 cite it rather than restating it.
- **§5.2's `**Slot cleanup:**` bullet is concurrency-scoped.** The bullet at spec/05:545 sits under the heading `**Slot failure and cleanup (`maxConcurrentSessions > 1`).**` at :542, so anything landed in it does not exist for a single-session pod. So is the `**Slot retry policy**` block at :553.
- **§5.2's action list is three actions.** Workspace directory, process group, `slotId`. It names neither the per-slot credential file nor the §4.9 expiry timers, while §5.2:455 ninety lines earlier already presupposes the wider list, and the code does both (`slotlayout.RemoveTree` sweeps `CredentialsDir` at tree.go:58-68; `deregisterSlotLocked` cancels every armed timer at slotsession.go:174-188). The code is ahead of the spec here.
- **DECISION: widen §5.2's action-list sentence (SPEC-3's first anchor)** rather than enumerate the slot release's actions in the §4.7 `Shutdown` row. One canonical list survives and §5.2 stops being incomplete for the ordinary session end too. No code deliverable changes, because both actions are shipped behaviour.
- **§6.2's per-slot fence is two blocks, and the split is load-bearing.** `running ──→ failed` and `slot_cleanup ──→ leaked` sit under "Per-slot sub-states scoped to concurrent occupancy" (spec/06:146-148); the other four sit under "a pod of either concurrency" (:150-156). docs/reference/state-machines.md:251 mirrors the scoping and a tier-11 test pins the pair. `pkg/sandbox/slotstate` ignores the split and holds all six edges in one set.
- **`{Running, SlotCleanup}` is already in `ValidTransitions()`,** so a session the runtime was given needs no new edge and CODE-3 stays the single added edge. EVIDENCE: pkg/sandbox/slotstate/slotstate.go:105-113.
- **The per-slot sub-state machine is a contract model.** Nothing in production drives `slotstate.Registry.Transition`; only `MarkLeaked`, `MarkReleased` and `ForgetPod` are called. A missing edge cannot break a runtime path today. EVIDENCE: pkg/sandbox/slotstate/registry.go:12-20; pkg/gateway/sessionserver/start.go:2833-2856.
- **§6.2's `receiving_uploads ──→ running` annotation** reads "workspace ready, session dispatched to runtime with its session identifier". DECISION: the staged new-edge annotation is left mirroring that vocabulary; rewording it would cascade into DOCS-1's row and CODE-3's doc comment.
- **DECISION: the `running` boundary is the runtime having been given the session,** with a start still in flight named on the `receiving_uploads` side, stated identically in the SPEC-4 fence annotation, the SPEC-4 prose, the Design paragraph, CODE-3's doc comment and DOCS-1's table row. CLOSED: the four sites now agree, after the fix that deleted the start-handover sense of "acknowledge" and then deleted the attribution clause; see the Settled entry on the closed `running` boundary. The fence annotation is still untouched, so the neighbouring entry "the staged new-edge annotation is left mirroring that vocabulary" also still holds.
- **DECISION: the `leaked` disposition is scoped to the pod class that offers it.** §7.1, the Design paragraph and the edge-case bullet now assign an unacknowledged reclaim the `leaked` sub-state and the §5.2 replacement trigger only for a pod serving concurrent sessions; on an exclusive pod the failed attempt releases the pod's claim and the pod retires under §6.2's pre-attached failure disposition.
- **DECISION: the reclaim obligation is its own §7.1 paragraph.** Lifted out of the "Atomicity of session creation (steps 2–8)" paragraph into a paragraph immediately after it, re-scoped to any gateway bind attempt (creation finalize block, §15.1 start, §7.3 re-attach), with §7.3's resume flow, §6.2's mid-resume cancel bullet and §5.2's `**Max retries:**` parenthetical as one-sentence pointers.
- **Two sections own retryability, each with a closed enumeration.** §5.2 "Non-retryable failure categories" (spec/05:555-560, three reasons, plus the "Client error on exhaustion" two-way disjunction and the "Fresh workspace guarantee") and §6.2 "Pre-attached failure retry policy" (spec/06:283-290, two reasons). spec/15:1136 adds "Any other setup-window failure ... is recovered with a fresh pod". Any future rule that makes a class non-retryable must edit all of them.
- **`ClaimSlot` already has the sentinel an exclusion needs.** It returns `ErrNoConcurrentSlot` (mapped to `WARM_POOL_EXHAUSTED` with `details.reason: "concurrent_slots_exhausted"`) whenever the pool list is non-empty and no candidate is placeable, so an exclusion that empties the candidate set needs no new sentinel and no new reason value. EVIDENCE: podclaim/slotclaimer.go:516-522; podsession/slotbinder.go:430-441.
- **The exclusion is one `continue` per pass.** `ClaimSlot` has exactly two candidate loops (pass 1 same-tenant claimed pods, pass 2 fresh idle pods), each already using `continue`-based skips, and `expiredByUptime` is the existing read-only skip precedent. EVIDENCE: podclaim/slotclaimer.go:336-357,:416-478.
- **The placement-filter test tier is 2, not 1.** The precedent pair `TestClaimSlotSkipsOverUptimeClaimedPod_spec_6_2` / `...IdlePod...` is in the envtest-backed slotclaimer_test.go; maxpoduptime_test.go in the same package is fake-client tier 1 and is not the precedent to copy.
- **`applySlotRetryPolicy` takes the shipped `req podsession.SlotBindRequest` by value,** and CODE-5 changes it to `req *podsession.SlotBindRequest` so the exclusion survives a `queue` pool's re-entry of the same closure; the request value still belongs to one client request, so the exclusion is scoped to that request's remaining attempts. EVIDENCE: pkg/gateway/sessionserver/start.go:2807-2884; pkg/gateway/sessionserver/queue.go:143-146,:205.
- **`SlotID == SessionID` on every path** and the per-slot tree is `/workspace/slots/{sessionId}/`, so a §5.2 retry of the same session landing on the same pod is the same adapter registry key and the same on-disk tree. §5.2's "Fresh workspace guarantee" silently assumes a distinct slot and does not hold there.
- **`SlotClaimer.ReleaseSlot(leaked=true)` returns early:** no Redis decrement, no claim DELETE, no recycle signal, no occupancy-zero disposition. So "releases the slot reservation" and "the leaked disposition holds occupancy" are compatible rather than contradictory, and a leaked release never reaches the `DeleteClaim` that retires the pod today. EVIDENCE: podclaim/slotclaimer.go:830-843,:882-885.
- **Today's `ReleaseSlotReservation` hard-codes `ReleaseSlot(ctx, sandboxName, false, false)`,** so a failed bind on a pod with no co-tenant deletes the claim and the pod retires. That is the safety valve the problem statement relies on, and CODE-4/CODE-5 remove it in exactly the failure case. EVIDENCE: podsession/slotbinder.go:493-503.
- **The `leaked` disposition already exists end to end.** `Binder.ReleaseSlot` sets `leaked = err != nil || !cleanly` on the adapter `Shutdown`, and the gateway (not the adapter) emits `lenny_adapter_leaked_slots` and already calls `slots.MarkLeaked` from `applySlotRetryPolicy`. Nothing new is needed for §6.2's "leaked holds occupancy". EVIDENCE: podsession/slotbinder.go:536-559; gatewaymetrics_credential.go:219-223.
- **The §5.2 leak/failure ledger is replica-local.** `Tracker.leaked` and `Tracker.events` are in-process maps with `UnhealthyThreshold = (maxConcurrent+1)/2`, backed by neither Redis nor Postgres. EVIDENCE: pkg/gateway/runtime/slothealth/slothealth.go:56-67,:215-221.
- **The churn change starts at concurrency 3.** At `maxConcurrentSessions: 2` the windowed `RecordFailure` alone reaches `ceil(2/2)=1` and drains the pod inside `applySlotRetryPolicy` before the retry iteration, so the persistent-leak disposition changes pod churn only at 3 and above.
- **`Shutdown` is unfenced.** `ShutdownRequest` carries `coordination_generation` (schemas/lenny-adapter.proto:1630-1635) and the adapter validates it only in `CoordinatorFence` and `CheckpointBarrier`. The compensating `Shutdown` is therefore not refused by the §10.1 fence, and the generation is per-session-coordinator anyway, so it does not advance between two attempts of the same session on one replica and cannot fence a lagging `Shutdown`. Closes an earlier UNVERIFIED.
- **`Shutdown` is also one of the few adapter handlers with no `checkSessionBound` guard** (pkg/adapter/session.go:227 versus attach.go:41, lifecycle.go:30, usage.go:266), so SPEC-1's "answers with a clean-exit response" sentence is reachable today and needs no fence carve-out.
- **The shipped `Shutdown` handler gates the whole teardown on `bound := removed && st.sessionID != ""`,** and gates only the §15.4.2 drain signal additionally on `!boundRemains` ("some surviving entry has a non-empty sessionID"). EVIDENCE: pkg/adapter/session.go:238-241,259.
- **`ShutdownResponse` is `{exited_cleanly, exit_code}`,** the adapter sets `ExitedCleanly: closeErr == nil` from the runtime close alone and discards the tree-removal error (`_ = removeSlotTree(st)`), and `exited_cleanly` is defined nowhere in `spec/` or `docs/`. SPEC-1's clean-exit answer needs no proto field and no SDK change; programme rule S-2 bars widening the response with one.
- **No per-slot sub-state is client-visible.** SPEC-4's new edge has exactly two parallel representations outside spec/06: docs/reference/state-machines.md's per-slot table (DOCS-1) and `pkg/sandbox/slotstate` (CODE-3). There is no OpenAPI, SDK or CRD mirror. EVIDENCE: pkg/api/v1/session/session.go:9-13; spec/15:672.
- **`stageWorkspace` sends `PrepareWorkspace` only under `if len(uploads) > 0`,** so an upload-free plan reaches `FinalizeWorkspace` as the first adapter RPC. EVIDENCE: podsession/binder.go:1323-1330.
- **The occupancy-zero recycle `Shutdown` is a second RPC reusing the just-released session's identifier,** so on that path it names a session the adapter holds no entry for and its clause three runs the whole-pod scrub regardless. Any blanket "a request naming a session the adapter holds no entry for removes nothing" rule must except it. CORRECTED (`non-spec.12.fix-G3.1`): this is the CONCURRENT sender only. See the Settled entry on the two recycle-carrying senders; the session-mode sender's recycle request is the session's only teardown and answers `reclaimed` on the removing arm, so a universal "a recycle-carrying `Shutdown` always answers `absent`" is false.
- **The adapter's `Resume` reaches `Runtime.Start` and `noteRuntimeStarted` too,** so a lost or cancelled resume leaves the same class-3 residue as a lost `StartSession`. EVIDENCE: pkg/adapter/resume.go:50,:140-144.
- **The `SandboxClaim` a leaked reclaim leaves `bound` is not undeletable.** §4.6.1 orphan-GC predicate 1 drains a `bound`/`recycling` claim older than `claimOrphanTimeout` whose pod no active session references, so a level-triggered backstop exists.
- **Ownership is clean.** The gateway owns `SandboxClaim.spec` and `.status` and routes the unhealthy-threshold drain through the `lenny.dev/drain-request` annotation rather than writing `Sandbox.status`. No component writes another's status subresource and no finalizer or SSA force-ownership is involved.
- **The anchor sweep is done, and the LIVE count is FOURTEEN sites across six files.** CORRECTED (`spec.8.review-applicability.1`): this entry read "sixteen" and listed spec/05:555 (`**Max retries:**`) among them; `grep -n "Max retries" spec-changes.md` returns nothing, because that edit was withdrawn with the pod-exclusion mechanism. Two independent lenses re-ran the fenced-block sweep at round 8 and every "text to replace" block still occurs exactly once repo-wide and every replacement block zero times. The rest of this entry stands as the site list. Rounds 2 to 6 minted the whole §7.2 block, §7.3's list tail, §6.2's `resuming → cancelled` clause, §5.2's `**Max retries:**` and `**Slot cleanup:**` sentences, and the §29.4 step-13 tail. Every "text to replace" block matches the tree byte for byte and occurs exactly once, and every "replace it with" block occurs zero times: spec/04:157 (§4.1 third sentence), :686 (§4.7 `Shutdown` row opening), :854 (§4.7.9 step 5); spec/05:453 (`**Scrub model.**`), :545 (`**Slot cleanup:**` action list); spec/06:150-153 (fence heading and the `receiving_uploads ──→ running` entry), :156-158 (fence close before `**\`reserved\` hold semantics.**`), :234 (`resuming → cancelled` clause); spec/07:23 (atomicity parenthetical) and :24 (the continuation line the new paragraph inserts before), :210 (§7.2 preamble premise), :213 (§7.2 step 2), :214 (§7.2 step 3), :414 (§7.3 list tail); spec/29:706-711 (§29.4 step 13). Roughly twenty lenses have now re-derived this mechanically, several of them by script over the fenced blocks, and every one came back clean; the count they report varies between twelve and sixteen only because some count fenced blocks and others count sites. Do not re-run it unless spec/ moves, and extend this entry when a fix round mints a new anchor rather than leaving the next reviewer to notice the list is stale. The §29.4 anchor is the one exception and it is non-unique INSIDE §29.4 as well as repo-wide: the same string closes step 6 at spec/29:645 and step 13 at :711, so the instruction resolves only through its "In §29.4's numbered step 13" scoping.
- **Every markdown anchor the staged text mints resolves.** `#47-runtime-adapter` (spec/04:657), `#479-startup-sequence-for-type-agent-runtimes` (:848), `#49-credential-leasing-service` (:1099), `#52-pool-configuration-and-execution-modes` (spec/05:365), `#62-pod-state-machine` (spec/06:78), `#71-normal-flow` (spec/07:3), `#72-interactive-session-model` (:115), `#73-retry-and-resume` (:378), `#151-rest-api` (spec/15:614), `#1542-rpc-lifecycle-state-machine` (:1686). No anchor is minted that spec/ does not already use.
- **§7.1's atomicity paragraph lives inside the fenced code block spanning spec/07:5-54.** Pre-existing, and not a defect of this proposal.
- **USEFUL (hand-off notes).** G1 and G2 each reported which sentences inside the SPEC-2 block they had already rewritten, which is what let G3 relocate and re-scope the block as a unit with their edits intact and append to the "Spec files touched" spec/05 entry rather than rewrite it, preserving SPEC-3's two anchors. It also let a later group discover that the design's line numbers had drifted and anchor on quoted strings instead of re-reading the file. Keep doing this.
- **Three RPCs admit a start, and §4.7's table carries all three.** `StartSession` (pod-warm, spec/04:672), `ConfigureWorkspace` (SDK-warm, :673) and `Resume` (:684) are all rows in the §4.7 Gateway→Adapter table, so the staged row's "which RPC in this table starts a session depends on the pod's session mode and on whether the session is new or resumed" resolves inside the table it names. spec/07:32-34 says SDK-warm pods skip `StartSession` outright, which is why naming that RPC in a predicate excludes every preConnect and resumed session.
- **The §7.3 re-attach claims from idle inventory.** `Binder.Resume` → `b.connect` → `podclaim.Claimer.Claim`, which takes only a Sandbox at `Status.Phase == Idle` (plus a same-tenant `reserved` rebind) and then CREATEs the deterministic per-pod claim; `ErrNoIdlePod` falls through to the §4.6.1 Postgres fallback, which re-reads the live Sandbox and refuses anything past idle. It never calls `ClaimSlot`. A pod holding a leaked slot keeps its `bound` claim, because `ReleaseSlot(leaked=true)` early-returns before `DeleteClaim`, so it is outside that candidate set with no `ExcludePod` needed. Nine lenses derived this independently; it closes the pass-1 OPEN "Resume path and the exclusion". EVIDENCE: podsession/binder.go:1590-1606,:1737-1768; podclaim/claimer.go:118-160.
- **That exclusion does not depend on Redis.** The reclaiming pod's per-pod `SandboxClaim` lives in etcd, so the §7.3 exclusion survives a Redis reset and the §12.4 rehydration hole does not reach it.
- **`ClaimSlot` pass 1 scans same-tenant CLAIMED pods with free capacity; pass 2 scans idle pods.** On the §5.2 bind path a leaked slot's held occupancy therefore filters nothing. Do not generalise the idle-inventory argument to the bind path. The UNVERIFIED that sat here, asking whether held occupancy makes the pod pass 1's own candidate, is RETIRED: it was a question about the placement exclusion, and no staged deliverable names that mechanism.
- **The three attempt kinds do not map onto three code paths, and `bindConcurrentSlot`'s reserved branch is a CONJUNCTION.** The guard is `if row.PodAssignment != "" && !session.IsRecovery(row.State)` (start.go:2595), so only a NON-RECOVERY row carrying a `PodAssignment` takes `BindReservedSlot` with no retry loop. A §7.3 resume-rebuild and a slotless row both reach `applySlotRetryPolicy` through `bindConcurrentSlot`, and so does a RECOVERY-state row (`resume_pending`, `resuming`, `awaiting_client_action`; pkg/api/v1/session/session.go:176-186) carrying a stale dead-pod `PodAssignment`, which falls through to :2606-2609 → `bindSlotWithRetry`. Each request builds a fresh `SlotBindRequest`, so `ExcludePods` never crosses a request boundary. CORRECTED (`non-spec-recheck-3.1.fix-G1.1`, FILED against summary.md:400 and fixed there): five earlier rounds wrote the unqualified "a row carrying a `PodAssignment` goes through `BindReservedSlot` and never enters `applySlotRetryPolicy`", citing the span `start.go:2594-2604`, which straddles the guard and the fallthrough. Cite `:2595` and `:2606-2609` separately. The conclusion those rounds drew is unchanged, because a recovery-state row is policy-placed and the staged placement exclusion covers it.
- **`applySlotRetryPolicy` wraps `binder.BindSlot` only,** and branches on `*podsession.SlotBindError`, so it governs BIND failures rather than only adapter-reported failures of a running slot. `BindReservedSlot` (start.go:2596) and `ClaimSlot` (:2148) are called directly and never traverse it. `maxSlotRetries == 1` bounds one invocation to two attempts, but a `queue` pool re-enters the whole closure with the budget starting over (queue.go:143-146,:205), so a single-valued exclusion field is NOT sufficient: a second unacknowledged reclaim on a second pod would replace the first name. CODE-5 stages `ExcludePods []string` and appends.
- **`BindReservedSlot` re-runs the whole `materializeSlot` sequence** (stage → finalize → setup → credentials → `StartSession`) against the persisted `row.PodAssignment` and `row.ID`, so a retried §15.1 start reaches the same pod, the same slot id and the same on-disk tree. Its failure keeps the row `ready` and returns a retryable `STARTING_FAILED` with a `Retry-After`, and nothing in `pkg/gateway/sessionserver` clears `PodAssignment`.
- **A create-time-reserved slot exists only on a concurrent pool.** `bindConcurrentSlot` is the sole caller of `BindReservedSlot` and is reachable only under `match.MaxConcurrentSessions > 1`; the exclusive create-time binding goes through `Binder.Launch`'s reconnect instead.
- **The slot routes are concurrency-gated end to end.** Every one sits behind `match.MaxConcurrentSessions > 1` (start.go:2139, :2351, :2465), and the exclusive path goes through `failPhase`, which drains the pod. So `ReleaseSlot(leaked=true)`'s early return can never falsify §7.1's "the pod retires" sentence, and §7.1's exclusive branch is grounded in the pool setting rather than in momentary occupancy.
- **`Launch`'s reconnect failure returns the pod to the pool rather than draining it,** through `ReclaimClaimed` (binder.go:977-991), but that failure precedes the attempt's first session RPC, so §7.1 owes no reclaim there. `ReclaimClaimed` sends no `Shutdown` at all: it deletes the per-pod claim and revokes the §4.9 lease, leaving the adapter's slot entry untouched on a concurrent pod.
- **`SlotClaimer.ReleaseSlot`'s `recycle` parameter, not `leaked`, patches the claim `bound → recycling`,** arms the missing-report timeout and returns `recycled`. `ReleaseSlotReservation` passes `recycle=false`, so a failed bind that drives occupancy to zero DELETEs the claim and the pod retires rather than recycling. That bounds the pass-1 "recycle boundary on a failed bind" question.
- **`Binder.Resume`'s failure branch releases nothing on an exclusive pool.** `reserveResumeSlot` returns `""` when `MaxConcurrentSessions <= 1` and `releaseResumeSlot` no-ops on an empty slot id; `resumeOnPod` returns the error with no rollback, and `holdOrFailOnResumeError` touches only the session row. A failed §7.3 re-attach therefore leaves the replacement pod `claimed` until §4.6.1 orphan GC.
- **§7.2 owns the mid-resume close sequence.** "Mid-resume terminal transitions — snapshot-close semantics" (spec/07:210-216) is the five-step sequence BOTH §6.2 `resuming` bullets defer to, and its step 3 carried the premise this proposal overturns ("no runtime was started on it, so no scrub beyond the pool's default post-session scrub is required"). SPEC-2 stages three §7.2 edits.
- **§7.2's sequence is only partly implemented.** Steps 4 and 5 exist (`bumpCoordinationGenerationOnSnapshotClose`, `recordSessionCompleted`); steps 1 and 3 have no implementation on the DELETE path. Correcting step 3's text therefore creates no code obligation and needs no code deliverable.
- **Neither §6.2 `resuming` bullet enumerates §7.2's sequence.** Both omit step 4's `coordination_generation` bump, which is why the sibling bullet's "abort / skip-seal / release-replacement-pod / run-terminal-handling" gloss is a pointer rather than a list that must grow, and why the `resuming → completed` bullet needs no edit when the reclaim joins the sequence.
- **The mid-resume reclaim rides the aborted attempt's own still-open connection,** under a detached context, before `cl.Close()` and the release. That is why it belongs at step 3 rather than step 1, and it is what makes "sends on the connection that attempt still holds" implementable.
- **§6.2's pod fence retires a failed pod.** `claimed ──→ draining (terminal claim disposition released or failed)`, `failed ──→ draining`, `draining ──→ terminated`, with no `failed → idle` edge, and spec/05:455 says a session that ends in failure always retires its pod. The prose at spec/06:283 ("marked `failed` and released back to the pool") is the loose statement and the fence governs.
- **A `leaked` slot cannot wedge a pod's retirement.** `draining ──→ terminated` is triggered by "pod replacement provisioned from warm pool" and is not gated on occupancy reaching zero, so occupancy held forever delays nothing. §4.6.1 orphan GC drains such a claim rather than returning the pod to idle.
- **DECISION: §7.1's obligation window ends when the attempt SUCCEEDS,** rather than when it "has the session running on the pod". `running` is bound vocabulary pinned by SPEC-4, so the old clause ended the obligation exactly where the next sentence says it must not.
- **DECISION: §7.1 states no cleanup-outcome report rule at all.** Its closing sentence was deleted outright rather than scoped. The report rule, its pre-`running` exception, its complement and the one-report-per-release rule are all SPEC-3's, appended to §5.2's `**Scrub model.**` paragraph, which the other sections cite.
- **DECISION: SPEC-3's §5.2 append states the complement.** "A cleanup that reclaims a slot the pod's shared runtime process was given is a session release like any other and reports its outcome." It is the spec statement of the staged code predicate `live := removed && s.runtimeHoldsLocked(sessionID)`, and it exists because stating only when the report is withheld left the positive half to implication.
- **DECISION: the SPEC-3 range is one canonical phrase,** "abandoned or fails after its slot enters `receiving_uploads` and before it reaches `running`", written identically into the staged §5.2 append, the Design rationale, and summary.md's What-changes bullet and SPEC-3 deliverable-index line. SPEC-4 adds an edge only out of `receiving_uploads`, and the `slot_assigned` case is deliberately left open.
- **DECISION: §7.2 takes three edits.** The preamble's premise sentence is deleted whole and the reason for skipping the live seal moves into step 2, which owns the decision; step 2 gains one sentence saying the aborted re-attach's pod-side state is reclaimed in step 3 rather than sealed; step 3 is replaced.
- **DECISION: the create-time-reserved retry bullet is keyed on acknowledgement.** The "bounded on both sides" claim was deleted and both arms re-keyed on whether the adapter acknowledged the reclaim, because the adapter's refusal is `st.started` while the gateway's `leaked` is `err != nil || !cleanly`, which made the old second arm exactly backwards and the old first arm false for every pre-start stage. The bullet's charter is to RECORD a residue no layer governs, and a record needs no bound.
- **DECISION: SPEC-1 stages a third block, on spec/29.** One sentence appended to §29.4 session-end step 13 restates the graceful-shutdown signal's co-tenancy condition and cites §4.7, and `spec/29_communication-scenarios.md` joins "Spec files touched". §29.4 step 12 is left unedited.
- **The drain gate and the runtime close have different preconditions in shipped code.** `Shutdown` sends the CH-RUNTIMEOPS drain only under `if !boundRemains`, then closes the runtime unconditionally for a bound entry. No `spec/` text stated the drain gate before this proposal: it lives at docs/reference/adapter-contract.md:75 and in code, which is why applying SPEC-1 creates a new spec-internal contradiction rather than exposing an old one.
- **`terminate` is the only §29 site that emits the graceful-shutdown signal.** §29.7 (drain) and §29.9 (eviction) carry checkpoint frames, so SPEC-1's single §29 site is the complete §29 mirror for the new condition.
- **The `terminate` frame carries `type`, `deadlineMs` and `reason` and no session field,** on the per-pod abstract socket `@lenny-runtime-ops`, so "the signal is pod-global and names no session" is accurate. The Basic/Standard stdin `shutdown` frame is also pod-global (§28.5.3's CH-MSGSOCK card excludes `heartbeat` and `shutdown` from the session-addressed set), so the claim holds at every integration level.
- **`ShutdownRequest.reason` is a plain `string` with no enumeration,** so the code lane's `"slot_bind_failed"` mints no wire value and needs no proto or SDK edit. The closed four-value enum belongs to the intra-pod `terminate` frame only, and `drainReason` normalises anything unrecognised to `session_complete`.
- **§29's preamble subordinates a trace on DISAGREEMENT and says nothing about omission.** So §29.2's silence on the finalize-block failure branch and §29.6 step 4's `RESUME_FAILED` branch are omissions rather than edit sites. It is also not a licence to leave a trace stale: this proposal edited step 13 rather than relying on the resolution rule, so an argument that "§29 is only a restatement" has to explain why step 13 was edited and a sibling was not.
- **§29.2's atomicity restatement covers its own steps 2-10,** which end at the `session_id` return, and the first pod-side RPC in that trace is step 15 `PrepareWorkspace`. So the widened §7.1 parenthetical quantifies over an empty set there and spec/29:200 stays accurate unedited.
- **`spec/16_observability.md` needs no edit and can orphan no alert.** There is no metric or alert row for leaked slots, per-slot cleanup or session scrub; the rows that touch the surface (`lenny_gateway_pod_retirement_total` :12, `lenny_slot_failure_total` :14, `lenny_slot_pod_replacement_total` :15, `lenny_pod_session_reuse_count` :128) all stay true, and `WarmPoolExhausted` keys on the `lenny_warmpool_idle_pods` gauge rather than on the client-visible error code.
- **`ReportSessionScrub` has no missing-report timeout,** unlike `ReportPodScrub`, so a withheld report trips no gateway-side watchdog and produces no spurious retire. It is also emitted once with no retry, so the at-most-one invariant is not defeated by redelivery even though `RecordSessionScrub` has no dedup key.
- **`slotlayout.EnsureTree` creates five directories per slot and `RemoveTree` removes four trees** (slot root, `/sessions/{id}`, `/artifacts/{id}`, `CredentialsDir`). §6.4 names three of them and defers removal to §5.2; neither §5.2's whole-pod scrub steps 1-6 nor step 0 sweeps `/sessions/` or `/artifacts/`, so the per-slot cleanup is the only spec'd reclaim for them.
- **SPEC-3's two added actions are shipped and correctly qualified.** `RemoveTree` sweeps `p.CredentialsDir` and `deregisterSlotLocked` cancels every armed provider timer before deleting the entry. §4.9's timer is direct-delivery-mode only (spec/04:1169), so the qualifier is exact and the clause is vacuous rather than wrong in proxy mode. §5.2:455 and scrub step 0 already presuppose the wider list, so the widening is a catch-up rather than a new obligation.
- **§4.7.9 step 5 is `PrepareWorkspace → FinalizeWorkspace → RunSetup → AssignCredentials → StartSession`.** The connect stage sits outside it: `connectSlot` does only `resolveSandbox`, `DialAdapter` and `NegotiateVersion`, which is pod-scoped and creates no adapter entry. So "a bind abandoned at the connect stage owes no reclaim" is sound, and the narrowed SPEC-3 range and §7.1's "first pod-side RPC" name the same boundary.
- **On the create-time-reserved path every stage that can fail before `StartSession` is pre-start,** so `st.started` is false for each and `claimSessionSlotUnderLock` admits the retry. The adapter refuses a repeat start on `st.started` alone, never on entry presence and never on the binding.
- **`Shutdown`'s entry removal and its tree removal are two moments.** `deregisterSlotLocked` runs under `s.mu`; `removeSlotTree` runs afterwards outside the lock, synchronously inside the handler before the response is built. Because `compensateFailedSlotBind` blocks on `cl.Shutdown`, an ACKNOWLEDGED reclaim has finished its tree removal by the time the gateway returns the error to the client.
- **The `Shutdown` handler performs no context-expiry check,** so a reclaim whose gateway-side deadline has already lapsed still executes destructively when it reaches the handler: it removes the entry, cancels the timers, closes the runtime for a started entry and removes the tree.
- **The leak disposition is keyed on the gateway's acknowledgement,** `leaked = err != nil || !cleanly`, rather than on what the adapter did with the entry, so an entry the adapter removed and acknowledged is released. `applySlotRetryPolicy` sets `ExcludePod` under `sbe.Leaked || relErr != nil`, a third arm no spec sentence names.
- **`SocketRuntimeProcess.Start` has no acknowledgement step.** It adds the session to the active set and returns, and it returns nil immediately when `p.connected` with no ctx check, so a racing `StartSession` on a cancelled context still starts the session. That is what makes both orderings of the reclaim-versus-claim race reachable rather than theoretical, and it is why "whose session the runtime has not yet acknowledged" names a handshake the platform does not have.
- **`ensureSlotStateLocked` calls `slotlayout.EnsureTree` BEFORE registering `s.slots[slotID]`,** and `EnsureTree` has no rollback, so a mid-way failure leaves directories on disk with no registry entry, which the staged §4.7 no-op rule then certifies as clean. Judged inert: the directories are empty and `MkdirAll` is idempotent on the retry.
- **A retry inherits the slot TREE and nothing else.** `assignCredentialsSlot` rewrites `st.creds` and the on-disk credential file wholesale from the request's lease set, `reconcileSlotExpiryTimersLocked` cancels timers for absent providers and re-arms the rest keyed on lease id, and `onSlotLeaseExpired` re-reads the slot state and returns unless the captured lease id still matches.
- **The retried start runs on occupancy the counter no longer counts.** `BindReservedSlot` releases the create-time reservation on failure through `ReleaseSlotReservation` → `ReleaseSlot(..., false, false)`, a real `active_slots` decrement, while the retry reconnects to the persisted binding and re-reserves nothing. Pre-existing and outside this proposal.
- **The reclaim's deadline is bounded.** The code lane wraps `context.WithoutCancel(ctx)` in `slotCleanupBudget`, a `WithTimeout` of `max(cleanupTimeoutSeconds / maxConcurrentSessions, 5)` with a 5s floor, and SPEC-1 plus SPEC-3 carry §5.2's formula across the concurrency boundary. §7.1's "states no deadline" reads as unbounded only in isolation.
- **The CRD cleanup-timeout rule is concurrency-gated in code.** `poolstore.validateSessionPolicy` puts the `cleanupTimeoutSeconds >= maxConcurrentSessions*5` check inside `if sp.MaxConcurrentSessions > 1`, so any spec sentence generalising that bullet across the concurrency boundary generalises an admission rule the code does not apply at concurrency 1.
- **The occupancy-zero concurrent release sends TWO RPCs.** `Binder.ReleaseSlot` sends the per-slot `Shutdown`, then a second `ShutdownRecycle` reusing the just-released session id over the same connection; the exclusive release sends one `ShutdownRecycle` for a session the adapter does hold. That asymmetry is why SPEC-1's no-op sentence reads differently on the two paths.
- **`SlotID == SessionID` IS stated in `spec/`, at spec/05:395** ("identified by the identifier of the session bound to it"), mirrored at docs/reference/glossary.md:371 and pinned by `tests/tier11_docs/slot_definition_glossary_reconciliation_test.go:85-104`; the code sets `SlotID: req.SessionID` at podclaim/slotclaimer.go:760. SPEC-2's §7.1 sentence restates an existing §5.2 definition. CORRECTED: an earlier form of this entry said the identity was stated nowhere and called §7.1 the first spec statement of it, so an argument shaped "§7.1 mints a new claim" is arguing from the stale form.
- **A pod-side reclaim destroys no client-retrievable setup output.** `GET /v1/sessions/{id}/setup-output` is served from the Postgres session row, which is what makes the §15.1 `SETUP_COMMAND_FAILED` promise survive a reclaim on the RunSetup stage.
- **The adapter manifest is pod-global,** at `/run/lenny/adapter-manifest.json` rather than under `/run/lenny/slots/{sessionId}/`, so SPEC-3's credential-directory removal cannot take it.
- **§7.1's three-kind parenthetical is exhaustive over the gateway's bind attempts.** `POST /v1/sessions/{id}/resume` routes through `resume_pending → running`, the same §7.3 flow, and a podless `suspended` session resuming is routed there too; `POST /v1/sessions/start` is the creation path. A fourth kind would be an unowned-residue finding, and there is none.
- **A concurrent pool has no `ready` gap.** `prepareAtFinalize` returns nil for `MaxConcurrentSessions > 1`, so the slot is materialised and launched together at `/start`, and the bound-but-never-started teardown class (READY_TIMEOUT, terminate-at-`ready`) can occur only on an exclusive pod, which spec/05:455 retires anyway.
- **No gate hard-fails on the staged spec text.** The tier-0 citation resolver and the line-citation ratchet target the retired `§X.Y line(s) L` form only and the staged text carries none; `per_slot_substate_scope_doc_reconciliation_test.go` pins four general edges by substring and the two scoped ones by exclusion, so SPEC-4's added edge passes; `recycle_scrub_trigger_consistency_test.go` reads the §4.7 row remainder SPEC-1 leaves untouched; `credential_path_literal_sweep_test.go` sweeps the retired pod-global path rather than the per-slot one; `code_blocks_test.go` parses only language-tagged fences and both the §7.1 and §6.2 fences are untagged.
- **The staged §5.2 sentences have no mirror outside spec/05.** Greps for "always assigned to a **new slot**", "fully saturated or unhealthy", "kills any processes owned by" and "releases the `slotId`" each return a single site.
- **The §5.2 slot retry policy runs on one path:** a row with no live `PodAssignment`, which is the §7.3 resume-rebuild or a slotless row. The create-time reservation and the `/start` reconnect to a create-reserved slot have no retry budget: `classifySlotBindFailure` answers the §5.2 `SLOT_FAILED` envelope, and a create-time `ErrNoConcurrentSlot` maps to `SESSION_CREATION_FAILED` rather than `WARM_POOL_EXHAUSTED`.
- **§5.2's non-retryable list includes `workspace_validation`,** a bind-time (FinalizeWorkspace) failure, so the policy demonstrably spans bind failures.
- **The snapshot diffs have been empty for most rounds.** `scratchpad/cp-snap/0081/spec-rN` was byte-identical to the live proposal in rounds 2, 4, 5 and 7, so "read the changed sections hardest" had no target. The real deltas are r2→r3 (the §7.2 block, the §15.1 create-time edge case, the racing-start rewrite, the `**Max retries:**` re-scoping and the `StartSession` → "start" sweep), r3→r4 (§7.1's "ends when that attempt succeeds", the §7.2 preamble deletion plus the step-2 sentence plus the step-3 rewording, the create-time-reserved bullet rewrite and SPEC-3's complement sentence) and r5→r6 (the §29.4 step-13 block). Diff against the named earlier snapshot rather than the current one; several lenses lost time on an empty diff. This has since become the rule rather than the exception: in every spec-recheck and non-spec-recheck round the snapshot the brief named was byte-identical to the live proposal, because it is taken at the round's own start. Run `diff -rq` over the whole `scratchpad/cp-snap/0081` directory first, take the newest snapshot that differs, and prefer the `-prefix` or `-start` form of the previous round's name. Later real deltas: `spec-r8` → the spec-recheck rounds (the §4.1 rationale, the §29.4 step-12 rationale, the blob-store edge-case rewrite and SPEC-3's leak-accounting clause), `spec-recheck-r1-prefix` → r2 (the clause's concurrency split), `spec-recheck-r4` → the open-decisions firing (summary only), and `non-spec-recheck-r1-prefix` → r2 (the CODE-2 `Resume` extension, the credential-lease relocation, the reserved-branch accounting and the two-arm tier-7a case).
- **CODE-1's response gate is `live`, not `started`.** It reads `ExitedCleanly: closeErr == nil && (live || treeErr == nil)` with `live := removed && s.runtimeHoldsLocked(sessionID)`, which is the same `running` boundary SPEC-3 and SPEC-4 install, so the tree-removal error is surfaced across the whole pre-`running` range including a start still in flight. Any lens checking that sentence against the earlier `(started || treeErr == nil)` form is reading a stale snapshot.
- **`started && !live` is unreachable at an ordinary session end,** which is what makes the `live` gate safe for the session-end classification. Every path that admits a start reaches `noteRuntimeStarted` unconditionally on success, and the two `noteRuntimeClosed` callers outside `Shutdown` deregister the entry in the same pass, so a later `Shutdown` has `removed == false`. EVIDENCE: pkg/adapter/session.go:156-163; resume.go:140-144; sdkwarm.go:217,:261,:296-298; holdstate.go:228-252.
- **`exited_cleanly` is read on the CONCURRENT slot-release path only.** `Binder.ReleaseSlot` computes `leaked = err != nil || !cleanly`, while session-mode `Binder.Release` calls `shutdownAdapter` best-effort and never reads the response (binder.go:1957-2008). CODE-1's widened `exited_cleanly` therefore cannot mark a slot `leaked` on a `maxConcurrentSessions: 1` pod.
- **DECISION: §5.2's leak-accounting sentence is concurrency-split.** On a concurrent pod a cleanup that does not complete is `leaked`, holds its occupancy, counts toward the whole-pod replacement trigger and moves `lenny_adapter_leaked_slots`; on a one-session pod the disposition is §7.1's claim release and retirement. Concurrency is the ONLY qualifier the sentence carries; the start-state and bind-path qualifiers are made true in the code lane instead.
- **DECISION: `accountSlotFailure` gains a THIRD caller** on `resumeOnPod`'s `podBinder.Resume` failure branch, rather than the §5.2 sentence being scoped down to the paths the gateway's accounting happens to reach. Its parameters narrow from `req podsession.SlotBindRequest` to `pool string, maxConcurrentSessions int32`, which changes both existing call sites, and the new caller is guarded on a non-empty resume slot id, which is exactly `MaxConcurrentSessions > 1`.
- **The §7.3 accounting gap was architectural, and it is now answered.** `pkg/gateway/podlifecycle/podsession` holds zero references to `slothealth`, `slotstate` or any leak gauge, and `Binder.Resume`'s failure branch returned a plain `fmt.Errorf`, so the disposition could not cross the package boundary at all. CODE-4 folds a `*SlotBindError` into that chain (transparent to `isTransientPodClaimError`, because `Unwrap` returns the cause) and CODE-5 accounts it.
- **Only the checkpoint-restore branch missed the accounting.** `resumeOnPod`'s snapshotless resume-rebuild goes `startOnPod` → `bindConcurrentSlot` → `applySlotRetryPolicy` and always reached it. Say "the checkpoint-restore re-attach", never "the resume path".
- **`applySlotRetryPolicy` is the only shipped producer of `MarkLeaked`, the leak gauge and `RecordLeak`.** `ReportSessionScrub(leaked=true)` feeds `drainLedger.RecordLeak` and never touches the gauge, so `lenny_adapter_leaked_slots` is the retry-policy path's signal rather than the general leaked-slot signal spec/05:545 and spec/06:160 describe.
- **`accountSlotFailure` is the only route a pre-`running` leak has to the §5.2 trigger.** The whole-pod replacement trigger has exactly two evaluation sites, `applySlotRetryPolicy` and `drainLedger.RecordLeak`, and SPEC-3 withholds the report that drives the second. Any future sentence asserting leak accounting on a pre-`running` reclaim must name a path that reaches `accountSlotFailure`.
- **DECISION: the credential-lease release moves out of `compensateFailedSlotBind` into the `materializeSlot` wrapper's error branch,** unconditional and outside the `errors.As` guard, so the compensation is pod-side only. `Binder.Resume` mints no §4.9 lease, so the shared form returned a live session's leases to their pool on a retryable failure.
- **No `materializeSlot` failure branch releases a credential lease today.** Its five stages do `cl.Close()` + `recordSlotFailure` + return, and `releaseCredentials`'s only callers are `failPhase`, `ReclaimClaimed`, `Release` and `ReleaseSlot`. CODE-4's wrapper release is a genuine new reclaim with no double-release site, and `ReleaseSession` is a documented no-op for a session holding none.
- **`releaseCredentials` is destructive rather than idempotent.** `Service.ReleaseSession` removes each lease from the store and decrements the credential's active counter, and nothing re-mints on a §7.3 retry.
- **DECISION: CODE-2's rollback covers the adapter's `Resume` as well as `StartSession`.** resume.go:50/:140/:144 is the identical claim → `Runtime.Start` → record sequence, and the staged §7.1 edge bullet is written RPC-neutrally, so the symmetric rollback needs no spec edit. `sdkwarm.go:261` is genuinely unraceable: its only gateway caller is `Binder.Launch`, whose failure runs `failPhase` and retires the pod.
- **`noteRuntimeStarted`'s caller list is exhaustive and its `bool` return breaks no build.** Three production sites (session.go:163, resume.go:144, sdkwarm.go:261) and six test sites (export_test.go:45, usage_test.go:233, adapterevents_test.go:95 and :184, podmcp_arming_internal_test.go:84/:185/:230). A bare call statement stays legal Go, so the `_ =` rewrites are cosmetic.
- **Exactly one shipped test goes red under CODE-2's guard.** `TestAdapterEventsEmitsControlEvents_spec_4_7` builds `New("served")` with nothing in `s.slots` and asserts the `soleSession` stamp. Both `adapterevents_test.go` sites take `bindSessionForTest`; `TestEmitFinalUsageOnShutdownPath_spec_4_7` stays green because `emitFinalUsage` passes the session id explicitly and the `soleSession` fallback never runs.
- **CODE-1's drain move withholds a frame that is sent today, and §15.4.2 owes none.** The shipped handler gates the teardown on `bound` (session.go:243) and the drain additionally on `!boundRemains` (:259), so a bound-but-unstarted reclaim currently gets a `terminate` frame. §15.4.2 is an adapter-PROCESS machine whose DRAINING obligation presupposes a runtime that was given a session, so nothing is lost. It is pinned by a no-frame assertion in the tier-1 "Bound but unstarted" case, whose no-other-bound-entry condition is load-bearing.
- **Every shipped adapter `Shutdown` test drives a started entry.** `claimSessionForTest`/`ClaimSessionForTest` binds, starts AND records (export_test.go:40-52), and every tier-7a drain-gate target goes through `startDrainSession`. That is why CODE-1 breaks no shipped test, and why every bound-but-unstarted assertion is new rather than a regression guard.
- **§29.4 is scoped to a started session.** Its `**Preconditions.**` paragraph (spec/29:586-591) scopes the WHOLE trace, interrupt and session-end alike, to a completed §29.2 startup, "so the runtime is running", with the interrupt path stated as an addition. Step 12 stays true under SPEC-1's started gate and needs no edit. Four lenses read the paragraph cold and reached the same reading; this settles the round-7 disagreement.
- **The creation finalize block engages the binder only on an EXCLUSIVE pool.** `prepareAtFinalize` returns nil for `MaxConcurrentSessions > 1`, so CODE-5's three accounting callers cover §7.1's three attempt kinds with no gap. A lens counting callers against §7.1's parenthetical will think one is missing; it is not.
- **`reserveResumeSlot` lands a real Redis increment** (`SlotClaimer.ReserveSlotOnPod`), so the §7.3 leaked hold is a held increment rather than a notional one. On an exclusive pool it returns `""` and `releaseResumeSlot` no-ops.
- **`ReleaseSlotReservation` hard-codes `recycle=false`,** so `bound → recycling` is unreachable on the failed-bind release path with either disposition (`WriteRecyclingStatus` sits inside `if recycle {`). The distinguishing counterfactual for a `leaked=true` release is the claim DELETE at occupancy zero.
- **`SlotClaimer.ReleaseSlot` has THREE dispositions, not two:** sibling-slot retained (:845-847), the recycle patch plus missing-report timeout inside `if recycle` (:850-878), and delete-and-retire at occupancy zero (:881-885). The recycling one is the disposition in which a bricked pod is handed to the next session.
- **`maxConcurrentSessions: 1` with `recycle.enabled: true` is a first-class configuration (sequential reuse),** so "one session per pod" does not imply "one session per pod lifetime". §4.6.1 projects `idle` for a claim deleted on a recycling pod and `draining` on a non-recycling one, and `failPhase`'s `drain` is a bare `DeleteClaim`, so the two code comments describe the same call oppositely.
- **Retirement is never a gateway act at the release.** `accountSlotFailure`'s tail reaches `DrainSandbox`, which only stamps `lenny.dev/drain-request`; the WarmPoolController owns the `draining` write. A test that asserts "the pod retires" is asserting something no gateway-side fixture can see.
- **The tier-4 fixture can show neither a retirement nor a threshold crossing.** `tests/tier4_integration/recycle_scrub_path_test.go` seeds exactly ONE Sandbox (`sbx-r` at :184, shared by all seven cases), runs no WarmPoolController, asserts the drain annotation directly, and its two concurrent pools carry `MaxConcurrentSessions: 4` against `UnhealthyThreshold = (n+1)/2`. Its `MaxConcurrentSessions: 1` line at :259 is the exclusive fixture, so a grep of the first hit misleads.
- **`UnhealthyThreshold` is `(maxConcurrent+1)/2`, which is 1 at concurrency 1 AND at 2.** Any argument that the trigger cannot fire at concurrency 1 is about §5.2's heading scope, never about the code.
- **Tier 7a and tier 4 are external test packages.** `tests/tier7a_load_local` compiles as `package tier7a_load_local_test` under the `load_local` tag, so `runtimeLive`, `runtimeIdleLocked` and `s.slots` are unreachable from it, and `pkg/adapter/export_test.go` does not help because a `_test.go` file's exported methods link only into its own package's test binary. The only exported cohort read is `SoleSessionID()`, which returns "" for an empty cohort and for a cohort of two alike.
- **`runtimeLive` is a pod-level cohort.** `noteRuntimeClosed` early-returns for a non-member and deletes only the named session, and `runtimeIdleLocked` is literally `len(s.runtimeLive) == 0`, so a started co-tenant keeps it non-empty. Any prose asserting "runtimeLive is empty" after a per-session teardown on a co-tenanted pod is false by construction.
- **The tier-7a race case parks the start inside `Runtime.Start`, which admits ONE ordering.** `noteRuntimeStarted` runs only after `Runtime.Start` returns, so while the start is parked the reclaim can only land before the record, which is the ordering CODE-2's guard exists for. The park fixture already exists as `gatedRuntime` (podmcp_arming_handoff_test.go:43-101) and there is a shipped two-RPC rendezvous precedent at podmcp_once_per_pod_start_race_test.go:239-256.
- **No gate reconciles the per-slot fence against its mirrors.** `per_slot_substate_scope_doc_reconciliation_test.go` holds two tests that never meet: one reads spec/06 and spec/07 against a fixed four-entry `generalSlotEdges` list, the other reads the doc's state NAMES and section placement only. Nothing compares `slotstate.ValidTransitions()` to spec/06 either. DOCS-1 now stages one new `generalSlotEdges` entry, which gates both the positive and the negative loop, plus one doc-row substring asserted as the `` `receiving_uploads` | `slot_cleanup` `` pair.
- **No tier-11 gate catches the `docs/reference/adapter-contract.md:75` drift.** `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` asserts four substrings that all survive SPEC-1, so the false row passes silently. The rest of the shipped tier-11 battery also stays green on the whole staging, re-derived by four lenses.
- **The §10.1 coordinator hold cannot arm in production.** `enterHoldState`'s only production caller is `onCoordinatorChannelClosed`, reached only from the deferred close of the adapter's own `AdapterEvents` handler, and no gateway code opens `AdapterEvents`. The hold allowlist excludes `Shutdown`, so the staged edge case is a conditional fail-closed statement, inert until remediation step R12 ships the control-stream consumer.
- **The adapter implements no process-group kill.** `removeSlotTree` is one line and greps for `Setpgid`, `syscall.Kill` and `pgid` over `pkg/adapter/*.go` return nothing, so §5.2's action list is ahead of the code on that clause before and after SPEC-3. Pre-existing; do not re-derive it as a finding.
- **`schemas/lenny-adapter.proto` already carries SPEC-3's widened cleanup list.** `SESSION_SCRUB_OUTCOME_RELEASED` names the slot's runtime, credential timers and directory tree, so SPEC-3's first anchor brings spec INTO line with the shipped proto. Do not file it as a proto mirror gap.
- **`onSlotLeaseExpired` bails when the slot is no longer registered,** so a stale §4.9 direct-mode timer cannot scrub a credential file whose `RemoveTree` failed. The entry deletion is what defeats §4.9's MUST, and it is shipped on every teardown path.
- **The §5.2 whole-pod scrub sweeps the racing-start orphan.** Step 1 is `kill -9 -1` as the sandbox user and step 2 is `rm -rf /workspace/slots/*` at the occupancy-zero recycle boundary, and recycling pods are tenant-pinned, so the "orphan survives into another tenant's session" escalation does not hold.
- **`PoolMatch.CleanupTimeoutSeconds` is not zero on a non-recycling pool,** despite its own doc comment saying so twice, because `poolPolicyMirror.PoolPolicy` assigns it outside the `if r := sp.Recycle` block. `slotCleanupBudget` therefore reproduces §5.2's `max(cleanupTimeoutSeconds / maxConcurrentSessions, 5)` on every concurrent pool.
- **A `Stage: "resume"` on `SlotBindError` mints no metric label value,** because `lenny_slot_failure_total` is emitted by explicit `recordSlotFailure` calls and `Stage` only refines `Reason()`. Adding a `*SlotBindError` to the resume error chain also changes no downstream classification: no classifier matches the type and `Unwrap` returns the cause.
- **`Binder.Resume`'s failure emits no `lenny_slot_failure_total`.** That counter comes only from `recordSlotFailure` inside `materializeSlot`, so a §7.3 re-attach can now move the leak gauge, trip the threshold and increment `lenny_slot_pod_replacement_total` with no slot-failure series on that pod at all.
- **The staged lane ADDS a producer of the unmodelled `slot_assigned → leaked` transition.** CODE-4 retires `BindReservedSlot`'s release-error swallow and CODE-5's reserved branch passes `sbe.Leaked` into `accountSlotFailure`. The scope call stands on the surviving ground: the hole predates the proposal, `MarkLeaked` writes the terminal with no `IsValid` call and `Registry.Assign`/`Transition` have no production caller, so nothing raises `InvalidTransitionError`, and closing it means an edge out of `slot_assigned` that SPEC-4 deliberately does not add.
- **DECISION: open decision 7 (the Redis rehydration hole) is resolved as "its own proposal".** It left `## Open decisions` and became a row under `## Defects in the shipped tree that this proposal does not stage`. The consequential form is shipped and common, because `leaked = err != nil || !cleanly` fires on every ordinary session end; the single rebuild source is restated at four carriers (spec/05:551, spec/12:191, spec/12:219, spec/10:62); and spec/05:551's index predicate is already wrong against migration 0080, which creates a wider one.
- **DECISION: open decision 12 is resolved as "no sentence is added".** SPEC-4's `**Pre-`running` slot cleanup.**` paragraph already states that a session the runtime has been given is a slot in `running`, so the proposition has a home and no staged sentence is left false.
- **DECISION: three out-of-scope defects stand.** The socket listener a last close retires, §6.2's missing terminal for a bind abandoned at the connect stage, and the coordinator hold that cannot arm each keep their summary row; none opens a decision and none is staged.
- **The impacts table now names 0073, 0075, 0078 and 0080.** SPEC-1's §4.1 sentence is 0073's own text (commit f37e867b8); 0075 reserved that paragraph as the ground its D3 rests on and the ground survives the edit; 0079 was withdrawn from the joint 0078/0079 row because its own fixed decision shortens the residue's life rather than widening the exposure, and the two proposals share no spec anchor and no code file.
- **`Binder.releaseCredentials` covers the compensation's whole obligation on the bind paths.** The concurrent bind path performs no §4.9 revoke on failure today, so a `StartSession` failure after `assignSlotCredentials` strands the lease's active-session slot; CODE-4's wrapper closes exactly that.
- **The tier-1 and tier-2 fixtures the staging names all exist.** `slotPod`, `slotTreeProbe`, `probeRuntime`, `startRuntimeOps`, `recordingSessionScrubReporter`, `assignOne`, `fakeExpiryClock`, `concurrentAdapter`, `recordingShutdownAdapter`, `fakeAssigner` (which already records every `ReleaseSession`), the envtest placement-filter precedent pair, and `pkg/gateway/sessionserver/slotretry_test.go`. Closes the pass-2 Open on tier-1 adapter fixtures: `export_test.go:45` is already a `noteRuntimeStarted` seam. Re-verified at round 4 with line numbers: `slotPod` (slotsession_test.go:61), `slotTreeProbe` (:77), `probeRuntime` (:33), `startRuntimeOps` (runtimeops_test.go:83), `recordingSessionScrubReporter` (sessionscrub_emit_test.go:20), `assignOne` (credexpiry_test.go:105), `fakeExpiryClock` (credexpiry_test.go:48), `bindSessionForTest` (usage_test.go:350), `concurrentAdapter` (podsession/slotbinder_test.go:71), `fakeAssigner.released` (podsession/binder_test.go:302), `TestClaimSlotSkipsOverUptime{Claimed,Idle}Pod_spec_6_2` (podclaim/slotclaimer_test.go:648,:682).
- **The round-4 snapshot is empty and the delta is one paragraph.** `non-spec-recheck-r4` and `-r4-start` are both byte-identical to the live proposal, and the only difference against `-r3` is the tier-7a paragraph rewrite at non-spec-changes.md:829-872; the test-coverage lens found the review log alone differing. The orchestrator's "the staging changed after this lane's last review converged" is not observable from the snapshot it hands you, so do not spend a round hunting a fix-stage delta in the staging text.
- **The checklist is clean as an execution sequence (do not re-derive).** Ten steps, ten staged deliverables, one deliverable per step, no deliverable named twice, no step naming an unstaged deliverable, every `Depends on` naming an earlier existing step, one lane per step (spec ×4 leading, docs ×1, code ×5), no checked box. A code step reaches a spec-lane dependency TRANSITIVELY rather than directly (S9 consumes SPEC-1's §4.7 no-op sentence and names S2, S3, S7, S8; S7 names S1, so S1 lands first), which is still checkable from the `Depends on` lines and is not a defect.
- **The gate state is green at every intermediate commit of the sequence.** Between S4 and S5 `per_slot_substate_scope_doc_reconciliation_test.go` neither requires nor forbids the new edge (`generalSlotEdges` is a fixed four-string slice at :32-37 driving a positive loop at :55 and a negative loop at :70); between S4 and S6 nothing reconciles `slotstate.ValidTransitions()` against spec/06; SPEC-1's row replacement keeps everything from "On the default disposition the pod is replaced." onward, which is what `TestRecycleTriggerConsistentAcrossSpec47And52_F5215` reads; `spec_28_register_writers_test.go:101` pins a §12.6 sentence this proposal does not touch.
- **Every code target in the non-spec staging exists with the stated signature.** `materializeSlot` (slotbinder.go:265) matches the staged wrapper's parameter list byte for byte and holds exactly the five `cl.Close()` calls the staging moves out; `b.releaseCredentials` at binder.go:1263; the five production `ReleaseSlotReservation` sites (start.go:2834, :3246, slotbinder.go:172, :217, binder.go:1714) plus the `slotBinder` interface at start.go:2727 and the two fakes (slotretry_test.go:52, slotretry_load_test.go:33); `connectSlot`'s `podclaim.SlotRequest` mapping at slotbinder.go:422-428 is the one line `ExcludePod` needs; `expiredByUptime` has its two candidate-pass sites at :433 and :491; `bindConcurrentSlot` holds `slotReq` at start.go:2594-2605; `resumeOnPod`'s Resume failure branch is start.go:4041-4043; types line up for `slotCleanupBudget` on both `SlotBindRequest` and `ResumeRequest`. Two lenses verified this mechanically; do not re-derive it.
- **`noteRuntimeStarted` has exactly the ten call sites CODE-2 enumerates and no eleventh,** `noteRuntimeClosed` the three production sites CODE-1's response paragraph names (holdstate.go:251, sdkwarm.go:297, session.go:267), `session.go:243` is `if bound {` and `:259` is `if !boundRemains {` (`:242` is `closeErr := error(nil)`, a mis-citation a fix pass already corrected), and `releaseSessionSlot` is slotsession.go:214-220.
- **The harness's tier is the test's DIRECTORY rather than its infrastructure.** Everything under `pkg/` runs in the unit tier even when it starts an envtest control plane, tier 2 is exactly `./tests/tier2_component/...`, and `validate-diagnosis` scans only `tests/tierN_*`, so nothing under `pkg/` is checked for the `// diagnosis:` annotation. This inverts a natural reading of `.claude/rules/test-coverage.md`. EVIDENCE: cmd/lenny-test/cmd_run.go:880-887,:1010; cmd_list_resolve.go:95; cmd/lenny-test/cmd_validate.go:120-140.
- **The tier-7a ordering argument holds mechanically.** Park inside `Runtime.Start` and release after the reclaim's `Shutdown` returns: the reclaim sees `st.started` true so the runtime teardown runs, `live` false so no report, then CODE-2's guard refuses and rolls back. No deadlock, because `gatedRuntime.park` releases `g.mu` before blocking and `Runtime.Start` is not called under `s.mu`. The co-tenanted variant's cohort assertion is observable from the external tier-7a package through `SoleSessionID()`, which returns the co-tenant's id in the correct state and `""` in the residue state because `runtimeCohort` goes 1→2.
- **CODE-1's move of `cancelPodMCPIfRuntimeIdle` into the `removed` branch is safe on every arm.** It is double-guarded by `runtimeIdleLocked()` and `mcpArmingHeldLocked()` (slotsession.go:238-260), and the one interesting arm, a session that claimed and armed with `Runtime.Start` still in flight, still holds a slot, so `mcpArmingHeldLocked()` is true and nothing is cancelled. No new hazard and no test is owed.
- **`ValidTransitions()` holds six edges today** (slotstate.go:106-113) and `TestValidTransitions_spec_6_2` mirrors them in a `want` map with a length fatal (slotstate_test.go:12-24), so the Testing section's "must move to seven edges" is arithmetically right; its `for e := range want { IsValid(...) }` loop already gives the positive assertion the section proposes to add, and the test's own `// spec:` comment ("exactly the six edges") needs one word changed.
- **DOCS-1's staged row and the staged tier-11 assertion match end to end.** The doc rows are spelled `` | `receiving_uploads` | `running` | ``, so the proposed `requireAllContain` substring matches DOCS-1's row verbatim, and adding `"receiving_uploads ──→ slot_cleanup"` to `generalSlotEdges` passes both the positive loop over the either-concurrency block and the negative loop over the concurrent-occupancy block, which carries only `running ──→ failed` and `slot_cleanup ──→ leaked`. The scoped header precedes the general header in spec/06, which the slice at :64 requires. Do not re-derive.
- **`tests/flake-budget.yaml` is a quarantine registry with `quarantined: []`,** rather than a register of stress-budgeted tests, so the new tier-7a case owes it no entry and it is not a missing edit site. EVIDENCE: tests/flake-budget.yaml:15-17.
- **The tier-4 `concurrent_workspace_test.go` is adapter-driven rather than gateway-driven.** It is `package tier4_integration_test` over bufconn with a real `adapter.Server`, a real `NewSocketRuntimeProcess` (:119), `echo-concurrent` (:307) and `maxConcurrentSessions: 2` at :96, and it holds no gateway binder and no pool object. The staged "on a `maxConcurrentSessions: 2` pool" is a gloss on the deployer configuration the flow assumes, and "after the compensation" means the case issues the adapter `Shutdown` itself. A reader taking it as gateway-driven will look for a binder that is not there.
- **The gateway tier-1 fixture inventory, corrected and landed.** `concurrentAdapter` (podsession/slotbinder_test.go:71-155) already declares `startErr`, `shutdownExitedCleanly` (default true) and `shutdownErr`, its `Shutdown` handler at :146-155 reads the latter two, it records no request, and it serves NegotiateVersion, FinalizeWorkspace, RunSetup, StartSession and Shutdown only. `recordingShutdownAdapter` (binder_test.go:1156-1173) carries per-request recording and `uncleanExit`, has no `shutdownErr` and no error arm, and implements `Shutdown` alone; same package, so `slotbinder_test.go` can borrow its recording pattern. Round 5 corrected the two false clauses at non-spec-changes.md:708-714. The genuinely absent fixture work is per-stage injection for the finalize, setup and credential-assignment stages, the `PrepareWorkspace` and `AssignCredentials` handlers, and request recording.
- **The files that create an unbound adapter entry call `Shutdown` nowhere.** `coordination_test.go`, `credentials_test.go`, `rotationgate_test.go`, `slotframe_test.go` and `tracingcontext_addressing_test.go` create an unbound entry or call `AssignCredentials` without starting, and `grep -n Shutdown` over them returns nothing, which is the other half of why CODE-1's report-gate move breaks no shipped adapter test.
- **DECISION: CODE-2's rollbacks answer `codes.Aborted`, not `codes.FailedPrecondition`.** `SlotBindError.Reason()` maps a `FailedPrecondition` at any stage but `workspace_prep` to `SlotReasonPolicyRejection`, which `NonRetryable()` reports true for, so the staged rollback turned a reclaim-versus-start race into a 422 `SLOT_FAILED{retryable:false, category:"policy_rejection"}` with no `Retry-After` and the retry budget unconsumed. `Aborted` falls through `Reason()`'s `default:` to `SlotReasonTransient` with zero gateway change. EVIDENCE: podsession/slotfailure.go:41-49,:84-102; sessionserver/start.go:2761-2780,:2873-2879.
- **`codes.Aborted` is free on this surface and is already the adapter's own vocabulary for a concurrency abort.** It appears in `pkg/adapter` only on the checkpoint op lock (checkpoint.go:115; oplock.go:36-40,:82) and nowhere on `StartSession` or `Resume`. `adapterclient.Client.StartSession` returns the gRPC error unmodified (client.go:142-143) and `slotErrCode` walks the chain with `errors.As` (slotfailure.go:106-114), so `materializeSlot`'s `%w` wrap does not hide it.
- **`Server.Resume` opens no span; `Server.StartSession` does.** resume.go:25-35 has no `tracing.NewTracer`; session.go:76-87 opens one and categorises through a deferred `tracing.RecordError`. The two rollbacks are therefore symmetric in every respect but the span line, and the `StartSession` arm sets `spanErr = tracing.CategorizeError(rollbackErr, tracing.CategoryTransient)`.
- **DECISION: CODE-2's rollback drops `releaseSessionSlot` and calls `cancelPodMCPIfRuntimeIdle()` directly, on both the `StartSession` and the `Resume` arm.** The guard has already established that the registry holds no entry bound to this session, so the deregister-and-`RemoveAll` half can only be a no-op or a destruction of somebody else's entry. `cancelPodMCPIfRuntimeIdle` is successor-safe through `mcpArmingHeldLocked` (slotsession.go:238-260). The identity-checked-deregister alternative was rejected as a pre-existing class fix wanting its own problem statement.
- **`st.sessionID` has exactly two production writers and neither ever clears it,** slotcreds.go:34 (`AssignCredentials`) and slotsession.go:87 (`claimSessionSlotUnderLock`), both setting it to the map key. So CODE-2's guard `!ok || st.sessionID != sessionID` is precisely "absent OR registered-but-unbound", and `st.sessionID != sessionID` is equivalent to `st.sessionID == ""` at every session-keyed call site.
- **The registered-but-unbound successor state is the ORDINARY state, not a rare interleaving.** `ensureSlotPaths` (slot.go:140-148) creates an entry with an empty `sessionID` for every `PrepareWorkspace`/`FinalizeWorkspace`/`RunSetup` (staging.go:134,:181,:337), so a §5.2 retry's workspace staging creates exactly the entry a lagging id-keyed release would delete.
- **DECISION: `slotretry_test.go`'s `req(pool, maxConcurrentSessions)` helper keeps its by-value return, and each `applySlotRetryPolicy` call site binds a local and passes its address.** The helper feeds two callees with different parameter modes: `applySlotRetryPolicy`, which CODE-5 changes to `*podsession.SlotBindRequest`, and `classifySlotBindFailure`, whose by-value signature (start.go:2761) no deliverable touches. No single return type leaves every call site unchanged.
- **The `req` helper's call-site inventory is twelve plus four.** Into `applySlotRetryPolicy` (the ones the pointer change edits): slotretry_test.go:81,:108,:137,:167,:208,:251,:294,:310,:344,:370,:421 and slotretry_load_test.go:86. Into `classifySlotBindFailure` (untouched): slotretry_test.go:442,:482,:499,:528. `bindSlotWithRetry` has no test caller; its only caller is `bindConcurrentSlot` at start.go:2608, staged as `&slotReq`. `classifySlotBindFailure`'s two production callers are start.go:2172 (inside `claimAtCreate`, declared :2085, its own create-time local) and :2602 (inside `bindConcurrentSlot`, declared :2594).
- **`maxSlotRetries` is a hard-coded `const = 1` at start.go:2720,** not read from `sessionPolicy.slotRetries`, so one `applySlotRetryPolicy` invocation is exactly two attempts. A single-valued exclusion would be sufficient WITHIN one invocation; the only bypass is the `queue`-pool re-entry, which is why `ExcludePods` is a slice carried by pointer.
- **The `queue` re-entry path, verified end to end.** `bindConcurrentSlot` captures `slotReq` in the `runWithQueue` closure (start.go:2606-2609); `applySlotRetryPolicy` is at :2807; `podclaim.ErrNoConcurrentSlot` is surfaced unwrapped (:2816-2819), classified as exhaustion (queue.go:103-107), routed to `waitInQueue` (:143-146) and the closure is re-invoked at :205. `waitInQueue` bounds the whole wait with `deadline := start.Add(waitBound)` (:155-205) and `yield` re-appends the ticket to the TAIL (:218-221,:258-278), so an excluded waiter round-robins rather than pinning the head and cannot stall unboundedly.
- **`validate-maps` is a TIER-0 check and it enforces per-file spec-map membership, not only existence and dangling paths.** `runValidateMaps` runs `validateTestFilesMapped`, which walks `componentAndAboveTierDirs()` and fails for any `_test.go` under `tests/tier{2,3,4,5,6,7a,7b,8,9,10,12}_*` that no `tests/spec-map.json` entry names; tier7a is mapped file by file with no directory glob. A NEW file under `tests/tier7a_load_local/` turns tier 0 red; a new test FUNCTION in a listed file costs nothing. `tests/tier11_docs` is absent from `componentAndAboveTierDirs()`, so the DOCS-1 half owes no entry. CORRECTED: three earlier entries recorded the ground for declining the spec-map item as "existence, version and dangling paths only", which is incomplete. EVIDENCE: cmd/lenny-test/cmd_run.go:761-770; cmd_validate.go:125-139,:716-792.
- **`s.podBinder` is a concrete `*podsession.Binder`, not an interface,** so `resumeOnPod`'s `podBinder.Resume` has no fake seam the way `applySlotRetryPolicy` has `slotBinder` (start.go:2725-2730). The package's own tests already build real `podsession.Binder` values against a fake or envtest client (start_preclaim_internal_test.go:655,:720,:779; terminal_reclaim_internal_test.go:52; start_pod_test.go:203), so the tier-1 resume accounting case is a fixture build rather than a fake swap. CORRECTED: no shipped test calls `resumeOnPod` directly. `start_pod_test.go` is `package sessionserver_test` and its `:1420` mentions the function only in a comment, so the case has to land in an internal file (`slotretry_test.go` or a new `*_internal_test.go`), and an external file can reach the path only through the HTTP resume handler.
- **`cl.ConfigureWorkspace` has exactly ONE production caller,** `Binder.Launch` at binder.go:1009, and `Binder.Launch` reaches only `StartSession` or `ConfigureWorkspace` (binder.go:967-976). So CODE-2's "no compensation races the SDK-warm record" holds, and there is no fourth start RPC on the exclusive path.
- **`slotBinder`'s only implementors are `*podsession.Binder` plus the two named fakes** (slotretry_test.go:52, slotretry_load_test.go:33), so CODE-4's signature change has no unlisted implementor.
- **`Registry.MarkLeaked` seeds an untracked slot at `Leaked` and never consults the edge list** (pkg/sandbox/slotstate/registry.go:99-116), so CODE-5's new callers cannot raise an invalid-transition error for a slot the gateway never `Assign`ed.
- **`deregisterSlotLocked` does NOT touch `runtimeLive`** (slotsession.go:174-189), which is what makes CODE-1's `live := removed && s.runtimeHoldsLocked(sessionID)` computable AFTER the deregistration inside the same critical section. A reviewer who assumes the deregistration clears cohort membership concludes `live` is always false and the report never fires.
- **The three leaked discriminators are one predicate across all three bind paths.** `compensateFailedSlotBind` returns `err != nil || !cleanly`; `applySlotRetryPolicy` accounts on `sbe.Leaked || relErr != nil`; `BindReservedSlot` folds its own release error into `sbe.Leaked` before the caller reads it; `Binder.Resume` folds `relErr` in before `resumeOnPod` reads it. The `leaked=true` release is a no-op inside `SlotClaimer.ReleaseSlot` (slotclaimer.go:830-836), so `relErr` is always nil on the leaked arm and the two-term discriminator never double-counts. CORRECTED for the `ExcludePods` narrowing: this entry is about ACCOUNTING and must not be read as covering PLACEMENT. The placement append on `applySlotRetryPolicy` is now `sbe.Leaked` alone, so accounting and placement are no longer one predicate on that path.
- **The `ExcludePods` pointer design has no aliasing or ordering hole.** `bindConcurrentSlot`'s `slotReq` is its own value parameter and `&slotReq` outlives every queue re-entry. Whether the append sits before or after the `reason.NonRetryable() || attempt == maxSlotRetries` return does not matter: a run reaching that return yields a `SlotFailedError`, which `isExhaustion` rejects, so no re-entry follows and an un-appended last-attempt pod is never re-placed.
- **`createClaimNeedsRollback` returns false for any `*SlotBindError`** (start.go:3211-3225), and under CODE-4 a leaked `BindReservedSlot` release performs no decrement, so the create-time reservation is deliberately held on the combined create-and-start path. It reads as a double-leak and is not one. It also cannot reach CODE-4's change: it is called only from the create path (start.go:833,:1026) with `startOnPod`'s error, while `resumeOnPod` (:3943) is reached only from treerecovery.go:29 and the resume handler at :3494.
- **`Credentials.ReleaseSession` DOES cover user-source leases.** `credassign.Service.ReleaseSession` iterates `s.leases.LeasesBySession([sessionID])` over the shared lease store, and the `UserCredentialAssigner` doc comment says so outright ("User leases share the lease store the pool assigner uses"). The reading that `releaseCredentials` touches only `b.Credentials` is right about the call and wrong about the consequence: `b.Credentials` is the store both minters write into. EVIDENCE: credentials/credassign/credassign.go:400-410; podsession/binder.go:335-343,:1263-1268.
- **`materializeSlot` today releases NO credential lease on any of its five failure branches** (slotbinder.go:264-325, five `cl.Close()` + return sites, none calling `releaseCredentials`), so CODE-4's unconditional wrapper release is a net reclaim rather than a re-arrangement. Each branch returns `b.slotBindError(...)`, so `errors.As` matches on every current stage and the wrapper-owns-the-close restructure loses no path.
- **A cancelled context cannot make `Runtime.Close` a no-op.** All three `RuntimeProcess.Close` implementations use ctx only to derive the SIGTERM grace: `SocketRuntimeProcess.Close` does `releaseActiveLocked` before any ctx read (socketruntime.go:435-446), `InProcessRuntime.Close` takes `_ context.Context` (embedded.go:188), `MCPRuntime.Close` ignores it until `resolveShutdownGrace` (mcpruntime.go:266-295,:312-324), which only uses a deadline when `time.Until(dl) > 0`. A cancelled ctx makes the close HARSHER, never absent. Closes the pass-4 Open on CODE-2's rollbacks running on the inbound context.
- **`adapterclient.Client` applies NO per-RPC timeout of its own;** every RPC rides the caller's ctx (client.go:133,:807). So a `StartSession` that fails on deadline means `applySlotRetryPolicy`'s own `ReleaseSlotReservation(ctx, …)` and its retry `BindSlot(ctx, …)` are already dead, which is why the compensation's `context.WithoutCancel` is the only thing that survives that case.
- **`SocketRuntimeProcess.Start` RELEASES `p.mu` before `p.accept(ctx, timeout)`** (socketruntime.go:183-203), so a compensating `Shutdown` does not block behind a parked start for the whole AcceptTimeout, and `Close` early-returns nil on `!p.connected` (:436-439). Consequence worth knowing: in that interleaving `closeErr == nil` and `live == false`, so `ExitedCleanly` is true, `Leaked` is false, and the `ExcludePods` exclusion does NOT fire, so the retry can return to the same pod. That is the recorded "rolled-back start can close a successor's runtime session" accepted failure mode reached by its most likely route.
- **`slotCleanupBudget`'s denominator is ≥ 1 at every reachable call site,** and `SlotBindRequest.CleanupTimeoutSeconds`'s own doc comment ("Empty/zero on a non-recycling concurrent pool") is stale: `slotBindRequest` sets it unconditionally from `match.CleanupTimeoutSeconds` (start.go:2567), which comes from `sessionPolicy.cleanupTimeoutSeconds` rather than from the recycle block. Bind path is gated on `match.MaxConcurrentSessions > 1` (start.go:2139,:2351,:2465); the resume path normalises through `maxConcurrentSessions()` (:3353-3358, applied at :4029) and `ResumeRequest`'s field doc says "normalized to a minimum of 1 by the caller". A test fake leaving the field zero would panic.
- **On an EXCLUSIVE pool reached through `Binder.Resume` the budget degenerates to the whole pool `cleanupTimeoutSeconds`** (`max(T/1, 5) == T`), so a pool at `cleanupTimeoutSeconds: 60` blocks the gateway request goroutine for up to 60s on a `context.WithoutCancel`. §5.2's CRD rule bounds the ratio only from below, and §16.5's creation SLO excludes the window (spec/16:626), so no stated budget is breached.
- **"per-session teardown" occurs EXACTLY ONCE in the whole `spec/`, `docs/`, `schemas/` corpus,** at spec/04:157, the sentence SPEC-1 replaces, and so do "per-slot teardown" and "whole-pod teardown". The §4.1 edit removes the term from the corpus and no second site needs to follow it.
- **`ReportSessionScrub`'s served-session effect is stated in `spec/` and need not be inferred from code.** The §4.7 row reads "The gateway increments `sessionsServed` on the pod's `agent_pod_state` row" (spec/04:692) and spec/12:494's column comment reads "gateway-written at each session release". SPEC-3's withheld-report rationale can cite spec rather than `scrubreport_server.go`.
- **spec/12:481 and :494 stay true under SPEC-3, and the word that makes them true is "also".** SPEC-3's append states the pre-`running` cleanup as something the per-slot cleanup ALSO runs for, so it is an additional trigger rather than a session release, while the complement sentence defines a post-`running` cleanup as "a session release like any other". Read the two sentences together before filing spec/12.
- **`lineContaining` is a case-sensitive FIRST-match scan, and `requireLine` inherits it.** tier11_docs/backup_status_enum_test.go:48-56; recycle_scrub_trigger_consistency_test.go:158-165. It does not require uniqueness, so neither SPEC-4's new fence edge nor its new prose paragraph can break the presence loops.
- **`generalBlock` in the per-slot tier-11 gate is `s62[index(generalHeader):]`, i.e. everything to the END of §6.2,** while `scopedBlock` is `s62[scopedHeader:generalHeader]` (per_slot_substate_scope_doc_reconciliation_test.go:53,:62-64). SPEC-4's new `**Pre-`running` slot cleanup.**` paragraph therefore sits inside `generalBlock`, which is harmless for both loops; it writes the edge with a single arrow (`receiving_uploads → slot_cleanup`) while the loops match on `──→`, and it does not contain "`leaked` slot semantics", so `TestLeakedSlotCountingLifetimeAgrees_F5231` still resolves to spec/06:160.
- **The shipped tier-3 and tier-10 per-slot-cleanup-outcome gates are green under CODE-1,** because every case drives a full `StartSession` first, so `live` is true and `ExitedCleanly` reduces to `closeErr == nil` exactly as today; for a session the adapter holds no entry for, `removed` is false and the answer is `true && (false || true)`, identical to the shipped `bound` gate. EVIDENCE: tier3_contract/gatewaycontrol_scrub/scrub_wire_test.go:135-152,:285,:318; tier10_conformance/recycle_scrub_conformance_test.go:175-197,:376-395.
- **`tests/tier11_docs/adapter_metric_catalog_test.go` cannot be reached by anything this proposal does:** its source of truth is a regex over `pkg/adapter/metrics.go` (:26,:53-60), and `lenny_adapter_leaked_slots` is registered gateway-side. SPEC-3 naming that gauge in §5.2 adds no catalog obligation.
- **No alert anywhere references any slot metric,** `grep 'slot'` over `pkg/alerting/rules/rules.go` returns only the §16.5 comment and unrelated `*Exhausted` names, `WarmPoolExhausted`/`WarmPoolLow` key on `lenny_warmpool_idle_pods`, and no runbook under `docs/runbooks/` mentions a slot except three unrelated hits (Redis hash slots, a Postgres replication slot, the credential path). So the alert half of the operational lens is structurally inert.
- **The `lenny_adapter_leaked_slots` gauge is wired in production and SPEC-3's clause is true on every path its antecedent binds.** `cmd/lenny-gateway/sessionsrv.go:378,:381-382` wires `SlotReplacement` and `SlotLeakGauge`, so neither callback is nil; `applySlotRetryPolicy`'s leaked arm is start.go:2846-2851; `BindReservedSlot` re-runs `materializeSlot` (slotbinder.go:255) so the reserved branch reaches the same discriminator; the resume branch is CODE-5's third caller. The gauge is absent from spec/16 and docs/reference/metrics.md, which is pre-existing.
- **`lenny_slot_pod_replacement_total` is registered with the single label `pool`** (gatewaymetrics_credential.go:211-216) while spec/16:15 and docs/reference/metrics.md:167 both state `pool, k8s_pod_name`. Live, pre-existing spec-versus-code divergence; CODE-5 only adds callers of the existing emitter.
- **The new-identifier sweep is complete and empty.** `grep -rn "ExcludePod\|slot_bind_failed\|runtimeHolds\|slotCleanupBudget\|accountSlotFailure\|materializeSlotStages\|compensateFailedSlotBind" spec/ docs/ schemas/ charts/` returns zero hits; every identifier the proposal mints is in-process Go. The only NAMES the staged spec text introduces are two bolded prose terms (**slot release**, **runtime teardown**), two bolded paragraph labels, one fence edge and one step-3 title, and each was greped clean. `charts/` carries none of the affected identifiers and `schemas/` carries exactly one affected file, `lenny-adapter.proto`, which programme rule S-2 bars.
- **The staged-anchor sweep now has a mechanical, repeatable form.** A ten-line python script over the 28 fenced blocks of `.spec-changes.md` against `spec/*.md` gives count 1 for blocks 0,2,5,8,10,12,14,16,18,20,22,24 (the anchors) and count 0 for the rest (replacements and insertions). Two independent lenses reproduced it with identical results. Re-run it after any spec landing rather than eyeballing anchors.
- **The per-slot vocabulary has no SDK, CRD or OpenAPI mirror,** and `pkg/api/v1/session/session.go:9-13` states the exclusion outright ("Internal-only states ... are never returned by the REST API"). Note the OpenAPI document is at `pkg/gateway/externalapi/openapi/openapi.json`, NOT the `pkg/gateway/openapi/` path some lens briefs give; `cleanupTimeoutSeconds` appears in no file under `charts/lenny/crds/` or `pkg/apis/`. The `receiving_uploads` hits in docs/runtime-author-guide/lifecycle.md and the two warm-path SVGs are the POD-level machine.
- **`schemas/lenny-adapter.proto` carries NO doc comment on `ShutdownResponse.exited_cleanly`** (:1665-1668 is a bare three-line message), so CODE-1's re-keying of that field falsifies no proto text. This narrows the standing proto Deferred to the `Shutdown` RPC comment at :203-206 plus the `ReportSessionScrub`/`SessionScrubOutcome` comments.
- **`docs/api/internal.md:95-160` describes the adapter RPCs under RETIRED names** (`StopSession`/`StopSessionRequest`/`StopSessionResponse.clean_exit`, and a `StartSessionResponse{success, error_code, error_message}` where the shipped proto has `refusal_reason` at :958-962). A client-surface lens grepping for `Shutdown` mirrors lands here; that block predates the proposal and is not an edit site for it.
- **There is a FOURTH `slot_assigned` reservation-leak site, and it is NOT a producer of `slot_assigned → leaked`.** `ClaimSlot`'s create-time connect-stage branch logs its `ReleaseSlotReservation` error and returns (slotbinder.go:168-177), and CODE-4's table deliberately passes `false` there, so a create-time claim-handshake failure whose release fails holds a Redis slot with no `MarkLeaked`, no gauge move and no threshold contribution. Shipped, outside §7.1 (no pod-side RPC issued). A future proposal closing the `slot_assigned` terminal should count it.
- **`bindReservedSlot`'s three connect-stage failures return a `SlotBindError` WITHOUT reaching `materializeSlot`** (slotbinder.go:230-254), so CODE-4's compensation never runs there and `sbe.Leaked` stays false until `BindReservedSlot`'s own release error sets it. `BindReservedSlot`'s release covers both that connect stage and every `materializeSlot` stage because it sits in the outer wrapper (:210-224), and the ordering is sound: `ReleaseSlotReservation` is passed the compensation's `sbe.Leaked` and only afterwards does the method raise it on a release error.
- **The orphan-GC backstop is real and level-triggered.** `ClaimGarbageCollector.classify` (pkg/controller/warmpool/gc.go:223-320) drains a `bound` claim once `Status.BindingStateTransitionTime` (falling back to `CreationTimestamp`) ages past `claimOrphanTimeout`, gated on `Sessions.PodHasActiveSession` returning false.
- **`expiredByUptime` is the worked precedent for `ExcludePods`, ownership assertion included.** It is skipped with one `continue` in each pass (pass 1 at :417-441, pass 2 at :483-500), after the per-pod claim GET, and its two envtest tests assert both the skip and that the gateway left `Sandbox.status.phase` untouched (`assertNotGatewayStatusOwned`, slotclaimer_test.go:648-716). Pass 1 reads the per-pod `SandboxClaim` and never `Sandbox.status.phase`; pass 2 reads `sb.Status.Phase != Idle` and then GETs the claim.
- **spec/05:551 and migration 0080 quote the same index differently.** The spec writes the predicate as `sessions(pod_assignment) WHERE state = 'active'`; `migrations/0080_sessions_active_by_pod_index.up.sql:19-21` QUOTES the spec as `sessions(pod_name) WHERE state = 'active'`. The summary's shipped-defect row quotes the spec correctly and the migration's quotation is the stale one.
- **§7.2 step 4 says the interrupted resume attempt "is *not* retried, so no new recovery generation is minted"** (spec/07:215), which is what closes the lagging-reclaim-destroys-a-successor hazard on the §7.2 path. The hazard survives only on the create-time-reserved §15.1 retry. §7.3 step 3c likewise bounds `maxResumeWindowSeconds` to "before pod is allocated" (spec/07:407-412), so the wall-clock timer cannot fire mid-replay.
- **"half-claimed" has exactly three sites in `spec/`:** spec/07:214 and spec/06:234, both in the edit list, and spec/07:221's "Pre-attach terminal collapse" bullet, which is about `resume_pending` where no pod exists and survives SPEC-2's step-3 rewrite unchanged.
- **`receiving_uploads` as a PER-SLOT state has exactly two spec sites (spec/06:151,:152) and one docs site** (docs/reference/state-machines.md:234-235, DOCS-1's target). Every other hit is the SESSION-model state of a different machine (spec/15:672, state-machines.md:149-151,:175-176, runtime-author-guide/lifecycle.md:39) and is not a SPEC-4 edit site. The sweep for missed §6.2 mirrors is finished.
- **Neither wire schema states WHEN the graceful-shutdown signal is sent, only what it means,** so SPEC-1's co-tenancy condition falsifies no schema: `lenny-adapter-jsonl.schema.json:109-115` and `runtime-ops-events.schema.json:174-179` describe the frame, and §28.5.3's `terminate` card plus its Timing and Degradation bullets bound `deadlineMs` rather than the trigger. §15.4.2's table describes DRAINING only, and spec/04 contains no "`terminate` frame" occurrence at all.
- **The specification's pod-self-report posture does not reach `exited_cleanly`.** It is written only about checkpoint chunk sizing (spec/11:37, spec/10:147, spec/13:196), the adapter is a platform-controlled container rather than the untrusted agent, and `leaked = err != nil || !cleanly` is shipped for every ordinary session end. A finding framed as "SPEC-3 sources the leak bound from an in-pod self-report" is refuted on all three grounds.
- **The staged edits touch NO §10.3 / §13.1 / §13.2 control,** verified by absence: no RBAC, NetworkPolicy, admission webhook, ServiceAccount, tenant-pin, `lenny-cred-readers` or ephemeral-container-guard text is added, removed or qualified, and there is no chart, podspec, schema or migration change. `ExcludePods` is a read-only skip that can only narrow a candidate set, so it cannot cross a tenant pin.
- **0079 shares NO spec anchor and no code file with 0081,** re-derived after the 0078 impacts row's rewrite deleted the sentence that recorded it. 0079's SPEC-1 replaces §4.7.9 step 7 (0081 appends to step 5), SPEC-2 is §4.7.10, SPEC-3 appends after §5.2 scrub step 6, SPEC-4 replaces §5.2's `**Recycling and integration levels**`, SPEC-5 is §5.2 sizing text, SPEC-6 is §6.1 and the §6.2 POD-level recycle edges; 0081's §5.2 anchors are `**Scrub model.**`, the `**Slot cleanup:**` list and `**Max retries:**`, and its §6.2 anchor is the PER-SLOT fence. The impacts table no longer carries a 0079 row, so this entry is the record.
- **BUILD-GAPS F-5.2.33 is open at BUILD-GAPS.md:4132 and its suggested resolution splits two ways:** part (a) drop `p.listener.Close()` (socketruntime.go:467), which is proposal 0078, and part (b) name the component that creates the successor runtime process on the sidecar model, which is proposal 0079. 0078's own fixed decision reads "After it lands, a recycling sidecar pod still serves one session, and it fails at the accept timeout rather than by dialing an address that no longer exists" (0078:46-48), which is why the 0078 impacts row withdrew its widened-exposure premise.
- **0080 §1.19's inventory is scoped to `CoordinatorFence` refusals,** so CODE-2's rollback status code does not enter it; the rollback deregisters nothing, and what changes `boundSlotState`/`checkSessionBound` membership is the reclaim's `Shutdown`. Choosing `Aborted` keeps a fourth class off the overloaded `FailedPrecondition` rather than adding one.
- **`tests/tier4_integration/concurrent_workspace_test.go` wires no `srv.Lifecycle`,** so `drainViaLifecycle` returns at its nil check (session.go:299-302) and no §15.4.2 frame is observable in that fixture at all; the only tier-4 file that wires one is `credential_lifecycle_test.go:170`, which builds `cmd/runtimes/streaming-echo` rather than `echo-concurrent`. `RuntimeOps.Terminate` additionally swallows `errLifecycleNotConnected`, so a nil `Lifecycle` is invisible.
- **The shipped tier-7a `TestConcurrentShutdownsSendOneDrainSignal_spec_6_4` pins BOTH arms of the `boundRemains` gate in one body** (shutdown_drain_gate_race_test.go:232-304): two started co-tenants tear down from a rendezvous, the frame must arrive before the last close, and the frame count must be one across the pair. That is why the tier-4 §15.4.2 clause was deleted rather than wired: it pinned nothing the tree does not already pin.
- **The tier-4 case's step order is forced rather than arbitrary.** Carol must bind and start before alice's `Shutdown`, because alice's is the last close, `releaseActiveLocked` empties the set and `Close` then shuts the never-rebound listener (socketruntime.go:435-467), after which carol cannot accept.
- **`pkg/gateway/sessionserver/slotretry_test.go` already carries both tables the `Aborted` coupling rows go into:** `TestSlotBindErrorReason_spec_5_2` (a `{stage, code, want}` table with a `{"session_start", codes.PermissionDenied, SlotReasonPolicyRejection}` row) and `TestClassifySlotBindFailurePassesTransientThrough_spec_5_2` (:383-407,:468-492). No new fixture and no new file is owed.
- **`slotlayout.EnsureTree` creates `/run/lenny/slots/{sessionId}` at `ensureSlotStateLocked` time, before any binding,** so the tier-1 "Registered but unbound" case's credential-directory assertion is achievable against a workspace-RPC-only entry. `slotlayout.RemoveTree` accumulates the removal error across all four trees including `CredentialsDir` and returns the first (tree.go:58-70), so SPEC-3's "a cleanup that does not complete is still accounted" holds for the credential half as well as the workspace half.
- **`slotretry_load_test.go` builds its `req(...)` INSIDE each of its 64 goroutines,** so the pointer change gives each goroutine its own request and the `ExcludePods` append cannot race. Sharing one pointer across them would be a `-race` failure on a shipped threshold test. The file is in "Files touched" and in no `## Testing` case, which is correct: signature adaptation, not new coverage.
- **`resumeOnPod`'s only unreclaimed abandon is the `fenceResumedPod` failure at start.go:4066-4068,** which returns with no `rollbackBinding`; the sibling `acquireCoordinationLease` failure at :4054-4057 DOES call `rollbackBinding`, which routes through `Binder.ReleaseSlot` and therefore sends the adapter `Shutdown`. Outside this proposal because §7.1's obligation ends when the attempt succeeds and `podBinder.Resume` had already returned.
- **`noteRuntimeStartedLocked` is called from exactly one place, `noteRuntimeStarted` (runtimegeneration.go:32),** so the tier-7a paragraph's "`noteRuntimeStarted` is the only writer of `runtimeLive`" is true as written even though the write physically happens in the `Locked` form, and it does not contradict the Settled entry naming two writers.
- **The whole-staging citation sweep is COMPLETE and clean; do not re-run it.** `[non-spec-recheck.5.review-citations.1]` opened, line by line, every citation in the non-spec staging and the summary across `pkg/adapter`, `pkg/gateway/podlifecycle`, `pkg/gateway/sessionserver`, `pkg/sandbox/slotstate`, the tier-3/4/7a/10/11 test files, `spec/04,05,06,07,28,29`, `schemas/`, the migration, and the cross-proposal references, and every one resolved. Four later lenses spot-checked subsets and found no drift.
- **DECISION: the compensating `Shutdown` splits its two bounds by SHRINKING the adapter's grace.** `compensateFailedSlotBind` hoists `budget := slotCleanupBudget(req.CleanupTimeoutSeconds, req.MaxConcurrentSessions)`, uses it for the `context.WithTimeout(context.WithoutCancel(ctx), budget)`, and passes `budget/2` as `cl.Shutdown`'s fourth argument. `slotCleanupBudget` keeps its signature. ALTERNATIVES rejected: doubling the RPC bound to twice the §5.2 figure (the most faithful reading of §5.2, whose figure is the adapter's enforcement number, but it doubles a worst case that already degenerates to the whole 60s `cleanupTimeoutSeconds` on an exclusive pool reached through `Binder.Resume`, inside a path holding the client's HTTP request); a named `slotCleanupGrace` helper or a package constant (one relation belongs in one place, and a bare constant is a non-spec default with no override); `slotCleanupBudget` returning `(budget, grace)` (equivalent and acceptable, but take it INSTEAD of the inline `budget/2`, never both); leaving `deadlineMs` at zero and widening the RPC bound alone (impossible, because with zero the adapter derives its grace FROM the RPC context's deadline).
- **With `deadlineMs: 0` the adapter's SIGTERM pivot and the gateway's give-up instant are the SAME instant.** `contextWithGraceDeadline(ctx, 0)` returns the inbound RPC context unchanged (session.go:327-332), and `resolveShutdownGrace` PREFERS that context's remaining deadline over the runtime-configured grace and over the package default (mcpruntime.go:311-324). A runtime that uses its grace was therefore ALWAYS reported unacknowledged, so a completed reclaim was recorded `Leaked`. That is the defect the split closes.
- **The strongest production-reachable argument for the split is the `terminate` frame, not the runtime close.** `Shutdown` feeds `req.GetDeadlineMs()` into `drainViaLifecycle` (session.go:260), `RuntimeOps.Terminate` serialises `DeadlineMs int32` with `omitempty`, and the frame REQUIRES `deadlineMs` with `minimum: 100`. `budget/2` is at least 2500ms because the budget carries a five-second floor, so this call site becomes schema-correct as a side effect. EVIDENCE: pkg/adapter/runtimeops.go:65,:486-490; schemas/runtime-ops-events.schema.json:180-183.
- **The §11.4 precedent is exact and is the shipped 2:1 relation.** `userTerminateRPCTimeout = 20s` bounds the call and the shorter `userTerminateDeadline = 10s` is the fourth argument, with a doc comment stating the reason ("exceeds userTerminateDeadline so the gateway observes the pod's graceful exit before giving up on the call"). EVIDENCE: cmd/lenny-gateway/user_revocation.go:45,:50,:55,:128-129.
- **DECISION: the §11.4 precedent is named in the staged Go doc comment by the constant names,** `userTerminateRPCTimeout` and `userTerminateDeadline`, rather than by a `cmd/lenny-gateway/user_revocation.go:47-55` file:line, because a line citation inside a shipped Go comment drifts for the same reason `code-best-practices.md` and channel-naming N8 bar spec line numbers.
- **`Client.Shutdown`'s fourth parameter is a `time.Duration`,** converted inside the client with `int32(deadline.Milliseconds())`, so `budget/2` is type- and unit-correct and cannot overflow int32 at any admissible `cleanupTimeoutSeconds`. A reviewer expecting a millisecond integer reads the staged call as a type error. EVIDENCE: pkg/gateway/runtime/adapterclient/client.go:807,:813-818.
- **A grace-expired close is reported as CLEAN.** `SocketRuntimeProcess.Close` returns `p.listener.Close()` on both the `cmd.Wait()` arm and the `time.After(grace)` kill arm, and `MCPRuntime.Close` returns nil on the kill arm; only a natural non-zero exit or a listener-close failure yields `closeErr != nil`. So shortening the window cannot manufacture a `leaked` classification, and the observable is "the RPC answered" rather than "the runtime exited gracefully". EVIDENCE: pkg/adapter/socketruntime.go:435-467; pkg/adapter/mcpruntime.go:294-305.
- **Only two of the three `RuntimeProcess.Close` implementations read the plumbed deadline.** `MCPRuntime.Close` and `SocketRuntimeProcess.Close` do, through `resolveShutdownGrace`; `InProcessRuntime.Close(_ context.Context, …)` discards it and blocks unbounded on `<-done`. `executor.SubprocessExecutor.Close` also ignores ctx and hard-codes a ten-second pivot, so it can already overrun a five-second budget. EVIDENCE: pkg/adapter/embedded.go:188-204; pkg/gateway/session/executor/subprocess.go:341-367.
- **`RuntimeOps.Terminate` does not block; it writes one frame and returns.** That is what keeps `drain + close` from consuming the whole RPC budget, so the handler's timed work is `Runtime.Close` at roughly `budget/2` plus `removeSlotTree`, comfortably inside `budget`. EVIDENCE: pkg/adapter/runtimeops.go:486-491; pkg/adapter/session.go:299-307,:327-332.
- **The adapter enforces NO per-slot cleanup timeout of its own.** `grep "cleanupTimeout\|CleanupTimeout" pkg/adapter/*.go` returns only the WHOLE-POD scrub's `CleanupTimeout` at podscrub.go:128, and `resolveShutdownGrace` applies no floor. So §5.2's "(minimum 5s enforced at runtime by the adapter)" has no implementation, a `deadline_ms` of 2500 is not clamped back up, and the split is not halving any figure the adapter enforces.
- **`slotCleanupBudget`'s divisor is safe in production and reachable in fixtures.** `SlotBindRequest` is built only under `match.MaxConcurrentSessions > 1`; `ResumeRequest` normalises through `maxConcurrentSessions()` at start.go:4029. CORRECTED: the earlier form of this entry said `binder_test.go:533` IS the `cl.Resume` failure branch CODE-4 puts the compensation on and warned of an integer-division panic there. It is not: :533 is `TestResumeReturnsErrNoIdlePodWhenPoolEmpty`, which fails at claim time before any adapter RPC, and all three `podsession.ResumeRequest{…}` literals in `binder_test.go` (:492, :533, :1650) are success or pre-RPC-failure paths. The branch CODE-4 actually changes is exercised by `resume_slot_reservation_test.go`, whose `resumeRequest(maxConcurrent)` helper sets `MaxConcurrentSessions` non-zero at every call site, so the divisor is safe there too. A fixer acting on the old form edits the wrong test. `CleanupTimeoutSeconds` is `int` while `MaxConcurrentSessions` is `int32`, so the body needs an explicit conversion.
- **`slotBindRequest` DOES populate `CleanupTimeoutSeconds`** (start.go:2566-2567), despite the neighbouring `exclusiveBindRequest` comment at :2524-2529 reading as if the concurrent request omitted it. A reviewer trusting that comment concludes the budget always falls to its 5s floor. `ResumeRequest` carries both inputs too, populated from `match.CleanupTimeoutSeconds` at start.go:4037.
- **`SlotBindRequest.CleanupTimeoutSeconds` lives at slotbinder.go:108.** The three `CleanupTimeoutSeconds int` hits in binder.go (:478, :520, :667) belong to `BindRequest`, `LaunchRequest` and `ResumeRequest`, so a package-scoped grep misleads about which struct owns which field.
- **`ReleaseSlotReservation` has exactly SIX production sites and CODE-4's table names all six:** slotbinder.go:172 (`ClaimSlot`'s connect-stage release), :217 (`BindReservedSlot`), binder.go:1714 (`releaseResumeSlot`), start.go:2727 (the `slotBinder` interface declaration), :2834 (`applySlotRetryPolicy`), :3246 (`rollbackClaim`). No seventh exists. It also IGNORES its `slotID` parameter today, so "the staged call sites thread the slot id" is not evidence of anything.
- **The reclaim gate has exactly THREE production entry points:** `Binder.BindSlot` (start.go:2810 through `applySlotRetryPolicy`), `Binder.BindReservedSlot` (:2596) and `Binder.Resume` (:4005), and `bindReservedSlot` funnels into `materializeSlot` too, so one wrapper covers both bind paths. Do not re-derive the "is a bind path missed" question.
- **`spec/18` needs no edit, and the question is closed.** Its phase-12c deliverable lines (spec/18:531-533, exit criteria :538) name "the per-session slot cleanup and whole-pod scrub split" and the slot-counter gate without enumerating a precondition, a report rule or a state-machine edge, so nothing SPEC-1 through SPEC-4 stages can make a phase deliverable stale. Three lenses derived it independently.
- **`validate-maps` compares FILE paths, never test names.** `validateTestFilesMapped` strips `::TestName` and `/...` from every spec-map entry and matches the walked `_test.go` path or any ancestor directory against that set, which is why `concurrent_workspace_test.go` (mapped as a bare path) and `recycle_scrub_path_test.go` (mapped only as `::Test…` entries) both pass. A new test FUNCTION in a listed file is genuinely free. EVIDENCE: cmd/lenny-test/cmd_validate.go:733-741,:768-776; tests/spec-map.json:552,:1040.
- **The gateway test files split across the package boundary, and the split decides where a case can live.** `slotretry_test.go`, `slotretry_load_test.go`, `queue_internal_test.go`, `start_preclaim_internal_test.go` and `terminal_reclaim_internal_test.go` are `package sessionserver`; `start_test.go` and `start_pod_test.go` are `package sessionserver_test`. `s.slotStates` is built unconditionally by `slotstate.NewRegistry()` at sessionserver.go:1873 with no `Options` field, so an external test cannot observe `MarkLeaked` and must discriminate through the injectable `SlotLeakGauge`/`SlotReplacement`/`SlotHealth` callbacks.
- **The adapter test files split the same way.** `socketruntime_test.go` is `package adapter_test`; `slotsession_test.go`, `export_test.go`, `usage_test.go`, `adapterevents_test.go` and `podmcp_arming_internal_test.go` are `package adapter`. The "Co-tenancy hazard" case is split correctly across that boundary, because the shipped `TestSocketRuntimeProcessCloseScopedToSlot_spec_5_2` already observes the connection behaviourally (EOF on the reader) rather than by reading `p.conn`.
- **`live && !started` is unreachable, and so is `live ⊄ started`.** `runtimeLive` membership is written only by `noteRuntimeStartedLocked`, which runs only after `claimSessionSlotUnderLock` set `st.started`, and `st.started` is never cleared; every deleting path either ran the `started` block (which calls `noteRuntimeClosed`) or ran pre-`Runtime.Start`. That is what makes CODE-1's `live` report gate safe in BOTH directions and gives the `if live` report a meaningful `closeErr`.
- **`deregisterSlotLocked` returns a NIL `*slotState` when `removed` is false,** so CODE-1's `started := removed && st.started` and `live := removed && s.runtimeHoldsLocked(sessionID)` are safe only because Go's `&&` short-circuits. EVIDENCE: pkg/adapter/slotsession.go:174-189.
- **CODE-1's staged clause two preserves the shipped ORDER exactly for a started session.** Shipped `if bound {}` runs emitFinalUsage → drain → Close → noteRuntimeClosed → removeSlotTree → cancelPodMCPIfRuntimeIdle → reportSessionScrub; staged runs `if started {…}` then `if removed {…}` then `if live {…}`, and `started ⊆ removed`, `live ⊆ removed`. An ordering finding stops here.
- **`removeSlotTree` cannot be widened into a cross-slot delete.** It takes `st.paths`, resolved once at `ensureSlotStateLocked` from the map KEY rather than from `st.sessionID`, and `ValidateSlotID` refuses an empty id before an entry can exist, so a registered-but-unbound entry has an empty `sessionID` and fully resolved paths. CODE-1's move of the removal from `bound` to `removed` cannot produce `RemoveAll("/workspace/slots")`. EVIDENCE: pkg/adapter/slot.go:112-126,:210-212; slotlayout/tree.go:59-71.
- **A §11.4 revoke cannot reach a bound-but-unstarted or a started-but-not-`runtimeLive` session at all.** `podTerminateFanOut.terminateLocal` iterates the per-replica `podsession.Registry` snapshot and skips any session with no binding, and a binding is registered only after the bind returns. So the only caller that reaches CODE-1's withheld branches is the new compensating `Shutdown`, where today nothing is sent. EVIDENCE: cmd/lenny-gateway/user_revocation.go:110-137. This also makes `emitFinalUsage`'s move to the `started` branch a no-op for every shipped caller.
- **`slotbinder.go:542` is the ONLY production reader of the `Shutdown` bool.** The other two adapter-RPC `Shutdown` call sites, binder.go:2043 and user_revocation.go:129, both discard it. That is the whole blast radius of CODE-1's re-keying of `exited_cleanly`, and `Binder.ReleaseSlot` also ignores the `ShutdownRecycle` response, so the occupancy-zero recycle edge is untouched.
- **`stageWorkspace` cannot fail on an upload-free plan,** so every reachable `stageWorkspace` failure carries uploads and `FinalizeWorkspace` (which calls `ensureSlotPaths` at the top of the handler, as `RunSetup` does) is the first RPC an upload-free plan reaches. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1284-1332,:1346-1354; pkg/adapter/staging.go:181,:337; slot.go:140-148.
- **`codes.Aborted` collides with nothing on this surface.** `grep -rn "codes.Aborted" pkg/ cmd/` returns only pkg/adapter/{checkpoint.go:115, oplock.go:36-40,:82}; no gRPC interceptor, service config or retry policy anywhere under `pkg/gateway/runtime/adapterclient` or `cmd/lenny-adapter` treats a status code specially; and the absence of any `retryPolicy`/`MaxAttempts` means a `Shutdown` that reached the handler is never re-sent, which keeps `RecordSessionScrub`'s missing dedup harmless on this path.
- **`isTransientPodClaimError` returns false for any bare gRPC status,** matching only `*PoolWarmingError`, `*CredentialAssignmentError`, `*SetupCommandFailure` and five sentinels (start.go:3648-3682). So the resume rollback's `Aborted` is inert there and a raced `Resume` demotes the row to `failed`, exactly as the adjacent shipped `codes.Internal` at resume.go:140 already does. The code choice adds no new class on that path.
- **`resumeOnPod`'s two branches are mutually exclusive** (the snapshotless rebuild returns at start.go:3977), so CODE-5's third `accountSlotFailure` caller cannot double-account with `applySlotRetryPolicy`'s. Every collaborator that caller needs is in scope: `match` (built at :3980-3984), `s.slotHealth`, `s.slotStates`, `s.slotReplacement`, `s.slotLeakGauge`, `s.podBinder`.
- **The `materializeSlot` wrapper's `errors.As(err, &sbe)` guard is TOTAL over today's stages.** All five stage failures return `b.slotBindError(…)`, a pointer, so no post-connection failure escapes the compensation and the unconditional `releaseCredentials` outside the guard is future-proofing rather than a live gap.
- **`ClaimSlot`'s terminal error selection is ordered `len(list.Items)==0 → ErrNoIdlePod`, else `sawTenantMismatch → ErrTenantMismatch`, else `ErrNoConcurrentSlot`,** and the `ExcludePods` skip sits AFTER the tenant check, so it empties the candidate set without touching `sawTenantMismatch` and cannot suppress an `ErrTenantMismatch` a fall-through depends on. EVIDENCE: podclaim/slotclaimer.go:428-441,:505-522.
- **`connectSlot` has NO Postgres fallback claim,** unlike the whole-pod `connect` (binder.go:1745-1755). `ClaimSlot` is the single placement route on the bind path, so `ExcludePods` honoured in its two candidate loops cannot be bypassed by a third route.
- **`runWithQueue` is a free generic function** (queue.go:134) rather than a `*Server` method, and `newPodClaimQueue(poll, clock)` is already driven that way at queue_edges_internal_test.go:42 and queue_internal_test.go:95, so the staged queue-re-entry tier-1 case can compose it directly over `applySlotRetryPolicy`.
- **Every `podsession.SlotBindRequest` and `podclaim.SlotRequest` composite literal in the tree is KEYED,** so adding `ExcludePods` breaks no build. There is also a NINTH `classifySlotBindFailure` call site the "twelve plus four" inventory does not name, `start_preclaim_internal_test.go:1019`, which passes a keyed literal by value and needs no edit.
- **Every import CODE-1, CODE-2 and CODE-4 need already exists at the target file.** session.go already imports `codes`, `status`, `time` and `pkg/observability/tracing`; resume.go already imports `codes` and `status`; slotbinder.go already imports `context`, `errors`, `log` and `time`. No import churn is an unlisted edit.
- **`claimSessionSlot` has exactly three production callers** (session.go:111, resume.go:50, sdkwarm.go:217), so `st.started` cannot be set by any path that is not one of the three start RPCs §4.7's staged row names. That is what makes "a session whose start the adapter has admitted" implementable as `st.started` with no residual class.
- **The startup and creation SLOs cannot be moved by the compensation.** `recordStartupDuration` has exactly two call sites and both sit after an `if err != nil { return }` (start.go:2401, :2673), so `lenny_session_startup_duration_seconds` samples successful starts only and is composed from the successful attempt's `BindTimings`; the burn-rate expressions divide by that histogram's count (alerting/rules/slo.go:214-242). §16.5's creation SLO is scoped to receipt through the `session_id` response and excludes the bind.
- **The served-session count reaches no observability surface.** `grep "sessions_served\|sessionsServed\|served-session\|served_session"` over spec/16, docs/reference/metrics.md, docs/runbooks/ and pkg/alerting/rules/rules.go returns ZERO hits. `lenny_pod_session_reuse_count` (spec/16:128) is the nearest row and SPEC-3 only stops it counting a session the pod never ran.
- **`MarkReleased` has NO production caller,** and `MarkLeaked`/`ForgetPod` have exactly one each, both in `applySlotRetryPolicy`. A second leak for the same session on a different pod keeps the first record's `pod` field (registry.go:99-115), so the second pod's gauge is published with the first pod's count. Pre-existing, metric-only, and the drain threshold is unaffected because `slothealth.Tracker.RecordLeak` is keyed by pod.
- **`lenny_adapter_leaked_slots` is registered `{pod_id, pool}` and is ZEROED rather than deleted** at the end of `applySlotRetryPolicy`'s drain arm (start.go:2869-2871). Pre-existing cardinality growth; CODE-5 only widens the set of pods reaching an existing emitter.
- **The Sandbox finalizer chain is bounded on both arms, and it is four files deep.** Every Sandbox carries `lenny.dev/session-cleanup`, held while `activeClaimReferences` finds a non-terminal `SandboxClaim` for it, which a `leaked=true` release leaves at `bound`. §4.6.1 orphan GC's `reclaimByDraining` drains the pod AND `Delete`s the claim, releasing the finalizer, gated on `PodHasActiveSession` being false. EVIDENCE: pkg/controller/sandbox/finalizer.go:60-84,:111-126; pkg/controller/warmpool/gc.go:222-248,:271-290,:359-368.
- **`writeBoundStatus` runs inside `reserveSlot`'s fresh-pod arm immediately after `CreateClaim`** (slotclaimer.go:689-712), so the tier-4 datastore-crossing case's "the per-pod `SandboxClaim` survives at `bound`" is assertable rather than an empty-phase claim.
- **`drainReason` is the ONLY reader of `ShutdownRequest.reason` repo-wide,** at session.go:260. The SDK's `sd.Reason` reads are the frame's reason rather than the RPC field, and the runtime SDKs pass the frame's reason through as an opaque string to `invokeTerminate(reason, deadlineMs)` in all three languages, so a new reason value costs no SDK edit.
- **`pkg/gateway/externalapi/openapi/openapi.json` carries no error-code enumeration at all,** so no gateway error-path change can ever be an OpenAPI edit site. Confirmed by greps for `WARM_POOL_EXHAUSTED`, `STARTING_FAILED` and `no_idle_pods` across its 4301 lines.
- **`applySlotRetryPolicy` has 12 test call sites, not 13** (slotretry_test.go 11 plus slotretry_load_test.go 1). The proposal states no count, so nothing is owed; recorded so a later round does not re-derive it.
- **`sdkwarm.go` has a SECOND writer that clears `s.sdkConnected` besides `DemoteSDK`,** `ShutdownDemoteSDK`'s timeout branch at :104. CODE-2's "which only `DemoteSDK` clears" is loose, and its conclusion is untouched because :104 is a process-shutdown path.
- **`p.addActiveLocked` is at socketruntime.go:184 and :220 and `return p.listener.Close()` at :467.** Hand-counting from a `sed` window is off by one here because of a wrapped comment; use `grep -n` on this file.
- **The whole summary structure was rebuilt and verified by the `f5` cleanup firing.** The required sections stand in the required order, `## Open decisions for human to make` carried entries 9 and 11 at that firing (it now carries 11, 16, 17 and 18 — see the correction below), `## Defects in the shipped tree that this proposal does not stage` carried eight entries then and carries thirteen now, and `## Deliverable index` is last. CORRECTED TWICE: the section carried nine rows with four for 0080, which `f1.summary-cleanup` read as a breach of its own one-row-per-proposal rule; the `f2.open-decisions.0080-consolidated` firing then merged the four into one, carrying every assessment verbatim under a bolded per-inventory-entry sub-label and adding the §1.12 claim-map arithmetic the four old rows did not carry. It now carries SIX rows, one per proposal or programme step (0080, 0073, 0075, 0078, R1b, R12). Any entry saying "nine rows" or "four 0080 rows" is reading a superseded pass. Nothing was relocated because nothing was misplaced.
- **The 0078 relationship, settled and re-verified.** 0078 keeps the pod's runtime listener bound across a session teardown, so it does not widen this exposure, and it is sequenced after this proposal for the test-file reason the impacts row records. The overlap in `tests/tier7a_load_local/` is package co-location rather than a file collision, and 0081 opens neither production file 0078 changes (`pkg/adapter/socketruntime.go`, `cmd/lenny-adapter/main.go`) nor DOCS-1's target (`docs/runtime-author-guide/lifecycle.md`). CORRECTED: problem-statement's "0078 exists to make pods survive across sessions, which widens exactly this exposure" was withdrawn and rewritten; the sequencing conclusion is unchanged.
- **The 0078 maintenance obligation is one third of one assertion.** 0078's CODE-1 kills only the LISTENER half of the staged co-tenancy-hazard assertion; its connection-close and child-kill halves still discriminate afterwards, because 0078 leaves the occupancy gate, the shared-connection close and the graced child reap untouched.
- **A hand amendment added a whole new mechanism late, and the earlier sweeps never saw it.** `grep -n "epoch"` over this review log returned zero hits when the `spec.1` lenses opened, so every sentence the bind-epoch amendment (commit 1344047d4) added is unreviewed by the seven sweeps that preceded it. Do not lean on an older "verified clean" verdict for any epoch or reclaim-hold text.
- **DECISION: "slot-identifier occupancy" is renamed the **slot-identifier reclaim hold** and re-windowed.** It starts in the critical section that deregisters the entry, ends when the cleanup finishes, and carries no `leaked` clause. BECAUSE "occupancy" is already bound to the pod's Redis slot-counter occupancy a `leaked` slot holds until pod termination (spec/06:160, spec/05:545), and a second sense of the word produced three findings at once. §5.2's append is `**Slot-identifier reclaim hold.**` (spec-changes.md:552), §6.2's sentence says "the adapter holds the slot's identifier" (:614), §15.4's companion block is `**Slot-identifier reclaim hold:**` (:662).
- **DECISION: §7.1 carries ONE leak predicate, "did not complete".** A reclaim did not complete when the adapter did not answer it, or answered `reclaimed` without reporting a clean exit; `superseded` and `absent` are completed reclaims and leave the slot unleaked. §5.2's `**Max retries:**` bullet and its commentary read "did not complete" / "incomplete reclaim" to match. That is exactly CODE-4's discriminator and SPEC-3's §5.2 clause, so spec, staged clause and shipped mapping are one statement.
- **DECISION: §7.1's retry-exclusion rationale is the reclaim hold rather than tree reuse.** The adapter admits no bind onto a held identifier, so a further attempt cannot reuse a tree the reclaim is deleting; it is refused transiently. CORRECTED (`spec.6.fix-G1.1`): the clause that charged such a refusal to the §5.2 retry budget is RETIRED, because the exclusion is exactly what stops a policy-placed retry ever reaching the hold. §7.1 and the Design now read "an attempt that meets the hold is one the retry policy did not place". §5.2's `**Max retries:**` bullet carries no reason of its own, so §7.1 is the only place the exclusion is motivated. CORRECTED AGAIN (`non-spec.2.fix-G1.1`, filed by `non-spec.2.review-reliability.2`): the exclusion reaches only a hold the RECLAIM opened. After CODE-6 routes `releaseSessionSlot` through `reclaimSlotLocked`, the abandoned attempt's OWN pre-`Runtime.Start` rollback opens a hold as well, and the gateway can see nothing that distinguishes it: the compensating `Shutdown` is exempt from the hold, finds no entry and answers `ABSENT`, which CODE-4 correctly reads as a completed reclaim, so the pod is not excluded and a policy-placed retry meets the live hold and spends its last attempt (`maxSlotRetries` is 1). The exclusion is therefore unwidenable without new wire surface and the residue is accepted. The three staged sites that stated the universal were FIXED by `non-spec.2.fix-reclaim-hold-callers`; see the Settled entry on those three sites for what they now say and for the UNVERIFIED beside it.
- **DECISION: the `- **§29.4.**` bullet was deleted from `## Spec sections deliberately untouched`.** SPEC-1 genuinely edits §29.4 step 13 and `## Spec files touched` names spec/29, so a section on both lists inverts the list's stated purpose. Nothing is lost: SPEC-1's own block carries the step-12 rationale in full, and the unconditional-form claim stands at spec-changes.md:242 (§4.7 row), :600 (§4.7.1 caller rules) and :622 (adapter rules).
- **The bind sequence is a closed set of SEVEN RPCs:** `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup`, `AssignCredentials`, `StartSession`, `ConfigureWorkspace`, `Resume`. Exactly seven adapter sites create-or-resolve through `ensureSlotStateLocked`: staging.go:134,:181,:337; slotcreds.go:26; slotsession.go:75 (from session.go:111); resume.go:50; sdkwarm.go:217.
- **"Creates or resolves a slot registry entry" is MUCH wider than the bind sequence, and must never scope the epoch.** `Attach` (attach.go:41), `Interrupt` and `SignalDeadline` (lifecycle.go:30,:80), `SendMessage` (session.go:186), `Checkpoint` (checkpoint.go:94), `CheckpointBarrier`/`CoordinatorFence` (coordination.go:116,:254), `ExportPaths` (exportpaths.go:45), `ReportUsage` (usage.go:266), `RotateCredentials`/`RevokeCredentials`/`ExtendCredentialLease` (slotcreds.go:70,:105,:125) all resolve the entry. Six lenses derived this independently; it is the ground of the filed §4.7.1-versus-§15.4 finding.
- **Each bind attempt dials its OWN adapter connection,** `b.DialAdapter(addr)` in `connectSlot` and `bindReservedSlot` with `cl.Close()` on every failure branch, so one attempt is one `adapterclient.Client` is one epoch latch and the caller-side echo is free. EVIDENCE: podsession/slotbinder.go:182,:237,:459.
- **`ensureSlotPaths` runs at the TOP of `PrepareWorkspace`, `FinalizeWorkspace` and `RunSetup` and creates the entry when absent** (slot.go:135-148; staging.go:134,:181,:337). Two consequences the amendment's text does not account for: an attempt's own second and third RPCs re-enter the entry-creating path, so a per-entry epoch is re-minted fresh if the entry was deregistered mid-attempt; and a hold that starts at entry creation lands in the MIDDLE of the bind sequence.
- **`Shutdown` appears in NO §28 register row and on no §28.5.1 contract card.** The gateway-to-pod channels on `LNK-POD-GRPC` are CH-ATTACH, CH-CHECKPOINT, CH-FENCE, CH-BARRIER and CH-PODHEALTH; the bind and teardown RPCs are carried by no registered channel. So the amendment's "adds no §28 row" claim holds, and SCHEMA-1's two claim-map rows anchored at `#2851-gateway-to-pod` point at a card that states nothing about the epoch.
- **§15.4 is the right home for the published contract.** Its own preamble names `schemas/lenny-adapter.proto` as the gateway↔adapter gRPC surface, so SPEC-5's placement is not a runtime-author/adapter-author mixup. Its two insertion points are real and unambiguous: spec/04:693 (after the Adapter→Gateway table, before `#### 4.7.2` at :695) and spec/15:1469 (after `**SDK-warm demotion contract:**`, before `#### 15.4.1` at :1471). The anchors it mints resolve: `#471-role-and-gateway-rpc-contract` (spec/04:659), `#1542-rpc-lifecycle-state-machine` (spec/15:1686), `#101-horizontal-scaling` (spec/10:3).
- **The specification DOES name gRPC status codes where the gateway's §5.2 category depends on them:** spec/07:208 (`FailedPrecondition`), spec/15:1136 ("every gRPC code other than `FailedPrecondition`"), spec/05:515 (`InvalidArgument`). An argument shaped "the spec never states gRPC codes" is false. UNVERIFIED: `spec.1.review-reliability.1` declined the same finding on the opposite ground (that §4.7 names no codes for any adapter RPC) and neither shard corrects the other; a round wanting to name a code for the hold refusal should settle which reading governs §15.4.
- **Retryability on the bind path is derived from the gRPC STATUS CODE alone,** and the two codes an adapter author would most naturally reach for both classify NON-retryable: `InvalidArgument` → `workspace_validation`, `PermissionDenied`/`FailedPrecondition` outside `workspace_prep` → `policy_rejection`. Everything else defaults transient. A third-party adapter picking either for the hold refusal produces a permanently dead session. EVIDENCE: podsession/slotfailure.go:41-47,:84-102.
- **The proto publishes a structured `Error` envelope with `Category{TRANSIENT,PERMANENT,POLICY}`** (schemas/lenny-adapter.proto:531-552) and NOTHING in the gateway's slot-bind path reads it. Do not assume "transient" in spec prose resolves onto that enum.
- **`claimSessionSlotUnderLock` refuses a second start on `st.started` with `codes.Unavailable`** (slotsession.go:80-85), so the epoch-inheritance hazard bites only when the predecessor failed BEFORE its start was admitted (finalize, setup, credential assignment). Do not dismiss it on the started case.
- **CODE-6's hold is an in-memory `reclaiming map[string]struct{}` taken at deregistration with an unconditional `defer s.releaseReclaim(sessionID)`,** so any spec sentence saying a `leaked` cleanup keeps the identifier held states something the code lane does not implement. EVIDENCE: non-spec-changes.md:171-181,:892-923.
- **The amendment adds no control-plane or data-plane write of any kind.** The epoch is an in-memory `int64` per registry entry and the hold an in-memory map key: no etcd write, no Postgres row, no Redis key, no informer, no watch, no metric series. SCHEMA-1 is one enum and nine scalar fields. The capacity half of a performance lens is structurally inert on the delta; its cost is a new fail-closed GATE, the first staged rule in this proposal that can refuse a bind the shipped adapter admits today.
- **§15.4.6's conformance categories exercise the RUNTIME BINARY over JSONL against a fake adapter** (spec/15:2038-2075), so they cannot host an adapter obligation and the proposal's reason for leaving them alone holds. §15.4.2's state machine (spec/15:1686-1706) is generic and is not falsified by the new pod-global graceful-signal condition.
- **§24.8's external-adapter compliance suite is schema-driven** ("assertions are generated from the published artifacts rather than hand-coded against prose", spec/24:114), so every prose obligation §15.4 publishes is ungenerable, including the pre-existing SDK-warm demotion contract. Pre-existing; do not file it against this proposal.
- **`sandboxclaim_guard.Decide` rejects any operation but `OpCreate`** and applies per-pod uniqueness over non-terminal claims, reading no phase transition, so neither the withheld DELETE nor the withheld `bound → recycling` patch can produce a rejected write. EVIDENCE: pkg/admission/sandboxclaim_guard/guard.go:127-150.
- **`TestRecycleTriggerCrossRefsResolve_F5215` additionally requires the §4.7 `Shutdown` row to carry the literal link `05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes`.** SPEC-1's replacement adds one of its own, so the link survives even if the retained tail's were reworded. A future edit that trims that tail turns both F5215 tests red.
- **`ShutdownRequest.coordination_generation` is field 6 and the proto comment claims a pod validates the generation on every gateway-to-pod RPC.** Only `CheckpointBarrier` and `CoordinatorFence` do. That matters for §7.2, whose step 4 bumps the generation to fence a stale coordinator: if `Shutdown` ever starts enforcing the gate, a fenced attempt's §7.1 compensation becomes unsendable. Nobody has staged anything here. EXTENDED: spec/10:30 already says "Pods validate the generation on every gateway→pod RPC", so a staged sentence asserting the check restates published spec and the code gap is pre-existing; SPEC-5 now makes it a spec-stated obligation on `Shutdown` rather than a code-lane possibility.
- **`AssignCredentialsResponse` is genuinely an EMPTY message,** `message AssignCredentialsResponse {}` at schemas/lenny-adapter.proto:1033, so SCHEMA-1's `bind_epoch = 1` is correct and free. The `session_id = 1 / leases = 2 / rotation_trigger = 3 / reserved 4 / coordination_generation = 5` field set an earlier entry attributed to it belongs to `RotateCredentialsRequest` (:1035-1053), the next message in the file. Eight lenses corrected this independently.
- **`DemoteSDK` removes the registry entry AND removes the tree, synchronously inside the RPC.** `Server.DemoteSDK` → `noteRuntimeClosed` + `releaseSessionSlot(anyRegisteredSession())` → `deregisterSlot` then `removeSlotTree` (sdkwarm.go:294-302; slotsession.go:214-220). That is what makes §4.7.1's caller-drop rule true and what makes §5.2's "a release that runs its cleanup inside the RPC that requested it ends the hold before that RPC answers" hold for the demotion fallback. Fifteen lenses have now derived it; do not re-derive.
- **The SHIPPED `DemoteSDK` call site is not the one §4.7:673 describes.** §4.7:673 has the gateway call `DemoteSDK` on a `ConfigureWorkspace` FAILURE and fall back to pod-warm; the tree calls it inside `Binder.Prepare` before any workspace RPC (binder.go:878-897), and `Binder.Launch`'s `ConfigureWorkspace` failure just reclaims and returns (:1008-1013). Pre-existing spec-versus-code divergence, outside 0081, and it makes the caller-drop rule vacuous on the shipped ordering.
- **Only `ResumeRequest` (field 14) and `ShutdownRequest` (field 6) carry `coordination_generation` among the epoch-relevant messages;** the other six bind-sequence requests carry none and no bind-sequence RESPONSE carries one. CORRECTED for the r6 revert: no bind-sequence REQUEST carries a bind epoch any more, so `ShutdownRequest` is the ONLY message carrying both and the correct §4.7.1 wording is the bare "as on `Shutdown`". Do not restore the "as on `Resume` and `Shutdown`" form an earlier pass recorded as the correction. Derive it with `awk '/^message /{m=$2} /coordination_generation = /{print NR": "m}'`.
- **`PrepareWorkspace` is the only client-streaming bind-sequence RPC, and the adapter resolves the slot exactly once per call.** `rpc PrepareWorkspace(stream PrepareWorkspaceRequest) returns (PrepareWorkspaceResponse)` (schemas/lenny-adapter.proto:41); `resolvePrepareStagingDir`'s `if stagingDir == ""` guard resolves from the first frame and later frames are checked only for a non-empty session id (staging.go:44-47,:66-83). All seven bind-sequence RPCs have unary responses, so "reports the entry's current epoch on its response" is well formed for each.
- **The §7.4 mid-session upload reuses `PrepareWorkspace` and `FinalizeWorkspace` on the LIVE binding.** `bind.Adapter` is `BindResult.Adapter`, the connection the successful bind dialled and the caller closes when the SESSION ends, and `upload_to_session.go:128,:134` calls both on it with `midSession=true`. §7.4's own prose never names those RPCs; the ground is the handler, so do not file "§7.4 does not say that".
- **The EXCLUSIVE bind spans TWO adapter connections.** `Binder.Prepare` runs PrepareWorkspace→AssignCredentials then `cl.Close()` (binder.go:956), and `Binder.Launch` re-dials through `b.reconnect` before `StartSession`/`ConfigureWorkspace` (:976-1013), so Launch's first bind RPC is necessarily epochless and re-mints. `prepareAtFinalize` returns nil for `MaxConcurrentSessions > 1`, so the split is the exclusive finalize/launch pair only; the concurrent `materializeSlot` keeps one `cl` open from `PrepareWorkspace` through `StartSession`.
- **Each bind attempt dials its OWN adapter client** (`DialAdapter` → `adapterclient.Dial` → `grpc.NewClient`, at slotbinder.go:237,:459 and binder.go:1152,:1776), so the per-connection epoch latch never multiplexes two sessions and cannot name a co-tenant's epoch on a concurrent pod. Check this first before building any epoch-crossing-attempts finding.
- **The agent podspec sets `RestartPolicy: Never`,** so an adapter process cannot restart in place under a live pod. That, and NOT the no-re-dial rule, is what closes the epoch-ABA question: `adapterclient.Dial` uses `grpc.NewClient`, whose `ClientConn` reconnects transparently, so "the connection the attempt already holds" would in fact survive a process restart. EVIDENCE: pkg/controller/sandbox/podspec/podspec.go:980; pkg/gateway/runtime/adapterclient/client.go:48-50,:72-79.
- **The adapter has THREE shipped bind-path refusals that are not the reclaim hold,** and none is stated in `spec/`: `codes.Unavailable` "pod is not idle: session %s is already bound on this pod" (slotsession.go:69-72), `codes.Unavailable` "session %s has already started on this pod" (:82-85), and the `codes.InvalidArgument` unresolvable-slot wrap (:76-78). §10.1's hold state is a fourth, rejecting every non-`CoordinatorFence` RPC with `UNAVAILABLE` (spec/10:57). Two of this proposal's edge cases lean on the started-session refusal surviving.
- **DECISION: the "This is the only rule under which the adapter refuses a request on the bind path" sentence was DELETED from §5.2** rather than narrowed, and its two pointer restatements in §4.7.1 and §15.4 were re-scoped to their own blocks. Importing a partial refusal list into normative §5.2 would mint normative surface nobody reviewed, because pod-not-idle and started-session have no spec statement anywhere.
- **`removeSlotTree` takes NO `context.Context` on either cleanup path** (`func removeSlotTree(st *slotState) error`, slot.go:208-212) and runs after the grace-bounded `Runtime.Close` (session.go:262-271; holdstate.go:200-201,:254 bound only the close). The `Shutdown` handler blocks on the removal before building its response, so no layer bounds the directory removal and the hold outlasts the close window by it.
- **The reclaim hold's bound is three-way and the removal is outside it.** The cleanup's close is bounded by the graceful window the reclaiming request carries, else by that request's own deadline, else, for a hold taken outside any request, by the termination window of the pass that runs the cleanup; the directory removal runs after the close under no deadline.
- **"A hold taken outside any request" has exactly one referent, and it is unreachable today.** `onHoldTimeout` (holdstate.go:189-205,:227-262) deregisters every started entry in one critical section through `deregisterStartedSessions` (slotsession.go:375-395) and then terminates them serially under one shared 10s context. The §10.1.4 coordinator hold cannot arm in production, so the clause is forward generality rather than a live bound.
- **`ABORTED` occurs nowhere else in `spec/`, `docs/` or `schemas/`,** so §15.4's status-code choice mints it, and there is no gRPC-status classification table on the adapter surface for it to contradict. The client-facing tables at spec/15:1023-1137 are REST error codes on a different surface.
- **DECISION: the shared-entry residue is ACCEPTED and unstaged, and NO lettered or numbered decision is minted for it.** The two "open decision D1's Option B" citations were false (no D1 and no Option A/B exists anywhere) and are replaced by a descriptive statement of what closing would cost: a per-ATTEMPT discriminator carried on every request that can create or resolve a slot entry, which this proposal does not stage. CORRECTED THREE TIMES: the section now carries entries 11, 16, 17 and 18, each keeping its original number and none renumbered; entries 9, 12, 13, 14 and 15 were deleted as resolved. Entry 17 is the re-put of entry 9 against the converged (worse) text and is the only entry carrying a recommendation. Any statement that the section carries "9 and 11", or "11 and 12", or "11, 15, 16, 17 and 18", or that entry 11 is the sole open decision, is reading a superseded pass. The preamble's own carried-out-of-the-log set is entries 16 through 18, rewritten when 15 left.
- **The hold is keyed on the slot identifier, which IS the session identifier,** so N concurrent cleanups on one pod refuse N distinct identifiers and never each other, no other session can collide with a held identifier, and no placement decision is affected. That identifier-scoping is what kills most availability findings against the hold. The proto states the identity too, in `ShutdownRequest`'s `reserved 4` comment (schemas/lenny-adapter.proto:1613-1619) — cite that rather than hunting spec prose.
- **spec/18 still needs no edit after SPEC-5.** Its adapter-conformance anchors are Phase 2/5 (`cmd/lenny-compliance`, the Basic battery over the RUNTIME binary, spec/18:121-122) and Phase 12c (:529-533, the cleanup/scrub split); neither enumerates an admission rule, an RPC field set or a §15.4 obligation, and CONF-1 lands in `tests/tier10_conformance`. Extends the pre-SPEC-5 Settled entry; three lenses derived it.
- **§28 carries no `Shutdown` row and no bind-sequence RPC row,** and §29.2/§29.4 label the bind and teardown RPCs "no register entry, the internal control API". So SCHEMA-1 widening eight messages adds no §28 row and the "deliberately untouched · §28's registers" bullet holds. §28.5.1's gateway-to-pod cards are exactly CH-ATTACH, CH-CHECKPOINT, CH-FENCE, CH-BARRIER and CH-PODHEALTH.
- **The adapter proto has no parallel client representation to keep in step.** No SDK, `docs/` or `spec/` file names `ShutdownRequest`, `AssignCredentialsResponse` or any bind-sequence response field, and there is no second copy of `lenny-adapter.proto` outside `schemas/` and `scripts/specshift/testdata/`. SCHEMA-1's nine fields and one enum mirror only `pkg/proto/adapter/v1`, which is generated. CORRECTED: this entry and the client-surface absence sweep beside it both wrote `pkg/gen/adapter/v1`, a directory that does not exist, so that arm of the sweep was vacuous; `schemas/buf.gen.yaml` pins `out: ../pkg/proto` and `go_package` is `.../pkg/proto/adapter/v1;adapterv1`.
- **`lenny_adapter_leaked_slots` IS named in `spec/` twice** — spec/05:545 and spec/06:160 — so SPEC-3's §5.2 mention mints no new metric surface. This corrects the natural misreading of the Settled entry that records the gauge as "absent from spec/16 and docs/reference/metrics.md", which is true of the inventories and is not a claim that `spec/` never names it.
- **The adapter registers no bind-refusal and no slot-admission metric.** `pkg/adapter/metrics.go:15-90` carries only the SO_PEERCRED pair, the LLM in-flight gauge, four credential-rotation series, the control-event counters and the tracing-frame drop counter, so §5.2's "no report and no counter names it" is true against the tree.
- **`examples/runtimes/echo/` DOES NOT EXIST in the tree.** spec/15:1466 calls it "A Go reference implementation of the adapter" while spec/15:1491 and :1812 describe it as a RUNTIME writing JSONL to stdout, and `examples/` is absent entirely. Pre-existing spec fiction; it cannot be an unstaged edit site, which moots the standing Open.
- **The gateway's `/finalize` block has a window in which §7.1's obligation cannot be discharged as written.** Its two "Gap 2" branches — the `finalizing → ready` store Update (sessionserver.go:3167-3181) and the upload-token `ConsumeDigest` (:3188-3197) — fail AFTER `Binder.Prepare` succeeded and CLOSED its connection, and both call `reclaimFinalizedPod` → `ReclaimClaimed`, which deletes the claim and revokes the lease and sends the adapter NO RPC. Staged §7.1 names the creation finalize block, mandates the reclaim "on the connection the failed stage still holds", and forbids re-dialling. FILED.
- **`isTransientPodClaimError` is a closed enumeration of sentinel types** (start.go:3648-3682) that a bare `codes.Aborted` matches on none of, so a §7.3 resume refused by the hold falls through to `s.failSession` and takes the row terminal.
- **The tier-11 §4.7 RPC-row gate harvests backticked first-column table rows.** `spec_47_rpc_row_naming_test.go:32-53` reads §4.7 with `specSection` and holds every `§4.7 table names <RPC>` Go comment to the row set it finds. SPEC-5 inserts prose rather than a table, so it is green; any future §4.7 or §4.7.1 edit that adds a markdown table with a backticked first column silently widens the accepted set.
- **In BOTH SPEC-5 blocks the admission-rules paragraph sits BELOW the RPC-set paragraph.** A cross-reference from the RPC-set paragraph reads "below", never "above". The §15.4 copy must NOT cross-reference "the caller rules below", because §15.4 carries no caller rules; do not re-synchronise the two sentences word for word.
- **DECISION: SPEC-4's §6.2 pre-`running` paragraph asserts no hold and no admission rule.** It points at §4.7.1 (which binds the adapter admits) and §5.2 (the hold, windowed from the deregistration), and says the `slot_cleanup` sub-state carries no admission rule of its own. Re-windowing §6.2's own sentence was rejected as a third normative copy; deleting it outright loses the protection that a reader of the fence alone does not read `slot_cleanup` as a label on a bindable slot.
- **`slot_cleanup` is entered at the gateway-observable failure, strictly earlier than the adapter's deregistration.** spec/06:154's trigger is "(session completes or fails)" and SPEC-4's new edge's is "the bind is abandoned or fails". The gap between that instant and the deregistration is exactly the interval in which §4.7.1 admission rule 1 REQUIRES a bind to be admitted, which is why any §6.2 sentence of the form "while a slot is in `slot_cleanup`, the adapter does X" claims X for an interval starting before any RPC reaches the adapter.
- **§6.2's per-slot sub-states are gateway-side.** spec/06:150 says "tracked per session, not as pod-level phase", so any staged sentence asking the ADAPTER to evaluate a predicate over them is unimplementable rather than merely mis-windowed. This is the structural reason the pointer form is right and a re-windowed form only accidentally right.
- **The reclaim hold is stated in exactly TWO places and the four sites agree word for word.** §5.2's `**Slot-identifier reclaim hold.**` and §15.4's companion block both window it from the deregistration of the registry entry to the cleanup finishing; §6.2 and §7.1 point at them. A later round that adds a third statement re-opens the two-start-points finding.
- **DECISION (`f1.open-decisions.13`): open decision 13 is resolved as staged and deleted.** The applied spec already answers it: §5.2's `**Fresh workspace guarantee**` stands as written and the retry policy is scoped to "the retries this policy places and no others". The question's `superseded` premise is false, because a release runs the per-slot cleanup that removes the tree, so the successor's workspace is materialized fresh. The genuinely-inherited case is adoption of a SURVIVING entry on the create-time-reserved path, which the policy does not place, and it is recorded as a shipped-defect row.
- **DECISION (`f1.open-decisions.14`): the compensating `Shutdown` is NOT exempt from §10.1's generation fence, and entry 14 is deleted.** §4.7.1 already states "Where both appear on one message, as on `Shutdown`, each is checked on its own terms", and a generation-stale rejection is simply a reclaim the adapter did not answer, which §7.1 already dispositions. §10.1's stale-replica rule tells such a replica to cancel its in-flight RPCs and not retry (spec/10:66-68), so an exemption would let a stale replica keep driving the pod. The unenforced per-RPC fence is recorded as a shipped-defect row naming spec/10:30 against pkg/adapter/coordination.go:120,:262.
- **The summary's structure was re-verified by the `f1.cleanup` firing and nothing was relocated.** Required sections in the required order, `## Deliverable index` last with fourteen deliverable lines, no `### Retired` block. The open-decisions preamble was corrected twice by that firing: "the numbering below does not start at 1", and it now records that entries 13 and 14 left with their answers staged and their residues recorded.
- **`## Decisions` names 0076 and 0080 deliberately and neither belongs in `## Impacts on other proposals`.** The 0076 mention is the ground of SCHEMA-1's second proto window and is word-for-word the R1b impacts row's claim; the 0080 mention is the ground of the file-collision discipline. Neither asserts anything about the other proposal's continued validity. A later pass that disagrees should merge into the existing row rather than add one.
- **The §5.2 append and the §15.4 insert each stage a fenced block containing a BLANK LINE,** so each lands as two markdown paragraphs even though the instruction says "Append to that paragraph". That is intended (the second paragraph is `**Slot-identifier reclaim hold.**`) and `## Spec files touched` names it. Do not "fix" the instruction by removing the blank line.
- **The `PrepareWorkspace` admission rule is per RPC CALL, not per frame.** Both blocks state that admission is evaluated once, at the frame from which the adapter first resolves the slot identifier, later frames are neither admitted nor refused on their own, and the single response reports the epoch admission fixed; §15.4 carries the matching non-conformance case for an adapter that resolves the identifier more than once within one call. The shipped adapter already does this, under `resolvePrepareStagingDir`'s `if stagingDir == ""` guard (staging.go:44-47,:66-83).
- **`coordination_generation` is declared on exactly fourteen REQUEST messages and on no response.** Derive it with `awk '/^message /{m=$2} /coordination_generation = /{print NR": "m}' schemas/lenny-adapter.proto`. It rides the forward RPCs the epoch deliberately does not fence, so the two field sets are nearly disjoint, and §4.7.1's both-fields sentence names `Shutdown` alone under the reverted per-entry design (see the correction on the `ResumeRequest`/`ShutdownRequest` entry above) rather than being illustrative.
- **`RotateCredentials`, `RevokeCredentials` and `ExtendCredentialLease` resolve through `slotStateLocked`, not `ensureSlotStateLocked`** (slotcreds.go:70-72,:103-107,:122-127), so they are outside the seven bind-sequence RPCs by construction and a revoke can never be fenced off by an epoch mismatch. Only `assignCredentialsSlot` among the credential RPCs takes the entry-creating path.
- **The whole-pod scrub survives every new fence.** Clause three of the shipped handler runs `startPodScrub` outside the `if bound` block (session.go:288-291), and the §4.7 row's unedited tail states the recycle disposition's scrub unconditionally, so neither an epoch mismatch, nor an `absent` answer, nor the reclaim hold can suppress the tenant-isolation scrub that precedes pod reuse.
- **There is a FOURTH `removeSlotTree` caller nobody's shard names:** the §10.1.4 coordinator-loss hold's self-termination pass (holdstate.go:229-254), which deregisters and cleans up outside any request. The staged §5.2 hold text already covers it, but a reviewer enumerating cleanup paths from `Shutdown` and `releaseSessionSlot` alone will miss it.
- **`isTransientPodClaimError` matches no bare gRPC status and `adapterclient` sets `CoordinationGeneration` only on `CheckpointBarrier`** (client.go:470), so `ShutdownRequest` and `ResumeRequest` always carry zero on the wire today. Both facts together are why the generation fence cannot refuse a compensation in the shipped tree.
- **The shipped adapter derives the scrub outcome from the runtime close alone,** `sessionScrubOutcome(closeErr)` mapping `closeErr == nil` to `RELEASED` with the tree-removal error discarded (sessionscrubreporter.go:38-43,:61-84; session.go:271,:279). So a pre-`running` cleanup would have reported `RELEASED` regardless, and withholding the report withholds only `IncrementSessionsServed`.
- **`grpc.NewClient`'s `ClientConn` reconnects transparently and `Alive()` reads exactly that** (adapterclient/client.go:48-50,:72-79), so "the connection the attempt already holds" survives a server restart. That is why the no-re-dial rule cannot carry the epoch-ABA case and `RestartPolicy: Never` must.
- **The two surviving "create or resolve a registry entry" phrases are the RECLAIM HOLD's admission predicate,** not the epoch-reporting predicate, and they are deliberately broader than the seven bind-sequence RPCs. Narrowing them to the bind sequence would admit `Attach`, `Interrupt`, `ReportUsage` and the rest mid-cleanup and open the hold. Do not "unify" the two predicates.
- **The proposal's line numbers have drifted roughly EIGHTY lines since the hand amendment (1344047d4 → 2aca9851b).** Any finding quoting a line number in the 500s for §5.2 or §6.2 prose is reading that snapshot. The current anchors: SPEC-1's §29.4 block at :334-360, the untouched list at :724-739, the "Spec files touched" §29.4 row at :760, the SPEC-5 blocks at :672-690 (§4.7.1) and :694-722 (§15.4), the §5.2 append at :600 and the hold paragraph at :602. Anchor on quoted strings, never on line numbers.
- **`tests/claim-map.json` is GENERATOR OUTPUT and a tier-0 gate holds it byte-identical.** `TestClaimRegisterIsReproducibleFromItsGenerator` re-runs `scripts/seed-claim-register.py --out` and diffs bytes, reporting per row that a committed row the generator does not emit "has no row source behind it". Rows are emitted `sorted(claims, key=lambda c: c["claim"])`, so no insertion position is authored; rows no status table carries go in the script's `EXPLICIT` list (:169, appended at :378); `SURFACE_OVERRIDES` cannot mint a row (`SystemExit` when it matches none). The generator reproduces the committed 76-row register exactly at HEAD. SCHEMA-1 stages two rows into the JSON and names the generator nowhere, so S8 turns tier 0 red. FILED. EVIDENCE: tests/tier0_static/claim_register_generator_test.go:19-31,:44,:74-122; scripts/seed-claim-register.py:169,:364-368,:378,:398.
- **The claim-register proto-agreement gate does NOT reach SCHEMA-1's rows.** CORRECTED (`non-spec.7.review-client-surface.1`, re-derived by six later lenses): `claim_register_proto_agreement_test.go` parses `Message.field` out of a row's `claim` ONLY when `strings.Contains(c.Surface, adapterProtoPath)` holds first (:67-79), and both staged rows put Go files in `surface`, so the proto-resolution arm is skipped rather than satisfied. Its other half is scoped to `coordination_generation` alone, which SCHEMA-1 adds none of. The earlier form of this entry said the gate forces the row and the proto edit into ONE commit; the same-commit requirement is real but its ground is the tier-3 `TestShutdownMessagePostRemovalDescriptor_spec_4_1` descriptor gate, not this one. A round reasoning from the old form chases an inert gate. Nothing in the staging changes: S8 already lands both. `claim_register_test.go:55-58` resolves `spec_anchor` against §28 headings and requires a WIRED row's surface to carry a path or symbol; `#2851-gateway-to-pod` resolves (spec/28:205) and both staged rows comply.
- **The generated proto package is `pkg/proto/adapter/v1`.** `pkg/gen` does not exist; `schemas/buf.gen.yaml` pins `out: ../pkg/proto` and `go_package` is `github.com/lennylabs/lenny/pkg/proto/adapter/v1;adapterv1`. The three proposal sites that said `pkg/gen/adapter/v1` were corrected by the third reconciliation pass.
- **The shipped tier-3 CLOSED field-set gate is `TestShutdownMessagePostRemovalDescriptor_spec_4_1`.** `assertFieldSet` errors on any declared field number missing from `want`, and the test pins `ShutdownRequest` to {1,2,3,5,6} and `ShutdownResponse` to {1,2}, so SCHEMA-1's field 7 and field 3 turn it red the moment the regenerated stubs land. It is the ONLY closed field-set gate over the nine messages SCHEMA-1 opens; `buf breaking` is silent on an additive edit and is the wrong gate to reason from. The fix belongs in the shipped file, in SCHEMA-1's own step, and its `// diagnosis:` needs the epoch fence and the reclaim outcome named. EVIDENCE: tests/tier3_contract/gatewaycontrol_scrub/shutdown_recycle_wire_test.go:202-238,:257-278.
- **`checkpoint_stream_wire_test.go` IS a closed pin too, and it does not matter.** `assertFields` (:92) iterates only its `want` slice, but twelve lines below, :151 pins `CheckpointStart` to exactly six fields and `assertOneof` (:77) pins a oneof's arm count. `grep -rn "Fields().Len()" tests/ pkg/` returns only those two sites plus `shutdown_recycle_wire_test.go:226`. Both `checkpoint_stream` pins are over messages SCHEMA-1 does not open. CORRECTS the r1 fix pass, which told an implementor that file can absorb an added field.
- **`validate-maps` maps four of this proposal's five test tiers FILE BY FILE.** Only `tests/tier4_integration` has a bare-directory entry (spec-map.json:3523), so a new tier-4 FILE needs no `validate-maps` entry (CORRECTED: that is true of `validate-maps` alone, and a new CASE in an existing file is not free — see the Settled entries on the second tier-0 gate); `tests/tier3_contract`, `tests/tier7a_load_local`, `tests/tier9_security` and `tests/tier10_conformance` carry only per-file entries plus deeper subdirectory globs (:1997, :2944, :4360). The four NEW test files 0081 stages therefore orphan and turn tier 0 red, and `tests/spec-map.json` is in no edit list. FILED; this closes and widens the two standing Opens that named only the tier-7a file. CORRECTS a note carried from proposal 0076 saying tier7a is outside the walk: it is inside `componentAndAboveTierDirs()`. EVIDENCE: cmd/lenny-test/cmd_validate.go:125-139,:716-793.
- **DECISION: each new spec-map entry lands in the checklist step that CREATES the file it maps** — S9 the tier-3 DIRECTORY entry `tests/tier3_contract/adapter_bind_epoch/...`, S10 the tier-7a and tier-9 file entries, S14 the tier-10 file entry. A trailing registration step (proposal 0073's S22 precedent) leaves tier 0 red on every intermediate commit from S9 to S13, which the checklist's own per-step tier rule forbids; the directory form keeps S10's added tier-3 file covered without a second entry. `tests/tier11_docs` is outside the walk, so the DOCS gates owe no entry.
- **DECISION: DOCS-2 exists, against `docs/reference/adapter-contract.md`, and folds into the EXISTING S6 docs step** with `Depends on:` widened from `S5` to `S2, S5`. A step of its own would renumber S7 through S14 and every `Depends on:` naming them for no separation the docs lane needs. This discharges the standing eleven-lens Deferred.
- **DECISION: DOCS-2 splits into a rewritten one-line `Shutdown` table row plus ONE prose block after `**Scrub responsibilities.**`.** The tier-11 gate reads the row with `lineContaining(page, "| `Shutdown` |")`, so the row must stay one physical line; the epoch contract and the reclaim hold are what §15.4 publishes to third-party authors and belong in prose. EVIDENCE: tests/tier11_docs/basic_level_echo_stamp_doc_reconciliation_test.go:294-311.
- **The `Shutdown` row at `adapter-contract.md:75` is now falsified in FOUR ways, three of them new since the pre-amendment loops read it.** Pre-amendment: the usage flush and runtime close move under `started`, and the `ReportSessionScrub` moves under `live`. New: CODE-1 moves `drainViaLifecycle` INSIDE the `if started {` block, so the drain signal needs `started && !boundRemains` where the row states only "leaves the pod holding no other bound session"; and a mismatched bind epoch makes the adapter perform NEITHER teardown. A DOCS-2 that covers only the two older halves is incomplete.
- **DECISION: `compensateFailedSlotBind` narrows to scalars,** `(ctx, cl, sessionID string, cleanupTimeoutSeconds int, maxConcurrentSessions int32, sandboxName, slotID string) bool`, because `Binder.Resume` holds a `podsession.ResumeRequest` and no `SlotBindRequest` is in scope there; `ResumeRequest` carries all three fields under the same names (binder.go:605,:645,:668). ALTERNATIVES rejected: a synthetic `SlotBindRequest` on the resume path (invents a conversion the proposal never sanctions), a shared `compensationParams` struct (a third parameter-carrying type for three scalars), and dropping `slotID` (the resume branch names a separately reserved slot id). The precedents are CODE-5's `accountSlotFailure` narrowing and CODE-4's own `slotCleanupBudget`. This closes the five-lens standing Open. The narrowed list is stated in exactly TWO prose sites and S12 keeps no signature at all: two prose copies of one signature is the drift that produced the finding.
- **DECISION: the §7.3 resume classification gap is closed by ONE arm, `case status.Code(err) == codes.Aborted: return true` in `isTransientPodClaimError`, owned by CODE-5.** The refusal crosses a process boundary, so only the status code survives: `errSlotReclaimInProgress` and `isSlotReclaimInProgress` are unexported and adapter-local, and no production file under `pkg/gateway` imports `pkg/adapter`. `status.Code` walks the wrap chain (grpc v1.80.0 `status.FromError` falls back to `errors.As` for `GRPCStatus()`), so the arm fires through the `*SlotBindError` CODE-4 makes `Binder.Resume` return. The function already carries one code-reading arm (`SetupCommandFailure`). Test home is `pkg/gateway/sessionserver/resume_setup_demotion_internal_test.go` (`package sessionserver`, `seedResumingRow` fixture at :23, a `holdOrFailOnResumeError` table at :48-110), never `start_test.go`, which is the external package. Only `pkg/adapter/checkpoint.go:115` answers `Aborted` today, so the arm widens nothing unintended.
- **`holdOrFailOnResumeError` and `writePodClaimError` are two different classifiers and only one was broken.** The wire envelope was already right: `writePodClaimError`'s default arm answers a retryable 503 `RESUME_FAILED` for any unrecognised cause (start.go:87,:208-219,:3517). Only the ROW-STATE classifier demoted the row terminal. `SlotBindError.Reason()` is a third classifier on a third path; a fix that touches one must say which.
- **`codes.Aborted` takes `Reason()`'s transient default at EVERY stage.** `Reason()` switches on the code first and only `FailedPrecondition`/`PermissionDenied` refine on `e.Stage`, so the staged start-stage and workspace-stage `Aborted` test rows cover the credential and setup stages too. EVIDENCE: podsession/slotfailure.go:84-101.
- **The credential stage's adapter error is NOT wrapped in `*CredentialAssignmentError`.** `assignSlotCredentials` returns that typed error only for a gateway-side lease-minting failure (`AssignProto`/`MintProto`); the `cl.AssignCredentials` RPC error returns bare and `materializeSlot` wraps it as a plain `*SlotBindError` at the `credential_assignment` stage. So a hold refusal on the credential path does not detour through the typed handling at start.go:89/:3653. EVIDENCE: podsession/slotbinder.go:369-403,:308-313.
- **`ensureSlotStateLocked` has exactly FIVE production resolve sites and the hold refusal reaches exactly the seven bind-sequence RPCs.** `ensureSlotPaths` (staging.go:134,:181,:337), `assignCredentialsSlot` (slotcreds.go:26) and `claimSessionSlotUnderLock` (slotsession.go:75). Every other entry-resolving handler goes through `boundSlotState`/`checkSessionBound`, and `RotateCredentials`/`RevokeCredentials`/`ExtendCredentialLease` through `slotStateLocked`. No sixth site. Re-derived by five lenses.
- **The SDK-warm one-session-only refusal runs BEFORE `ensureSlotStateLocked`** (slotsession.go:65-73), so putting the hold refusal at the top of `ensureSlotStateLocked` cannot reorder that control.
- **`deregisterSlot` (the `s.mu`-taking wrapper CODE-6 retires) has exactly two callers left and both are tests,** at `pkg/adapter/podmcp_arming_internal_test.go:88,:245`, a file already in the proposal's test list. Retiring it costs nothing else.
- **The `f1` firing resolved open decision 12 and deleted it: the reclaim hold ends when the cleanup pass that took it RETURNS, whether that cleanup succeeded or failed.** No staged text was edited, because the staging already states it twice in §5.2 and mirrors it in §15.4, and CODE-6 stages an unconditional `defer release()`. The two terminals that looked like they collided are different objects: `leaked` is the pod's Redis slot-counter occupancy held until pod termination, while the hold is adapter-local over the identifier, so a `leaked` slot keeps its occupancy without keeping its identifier held. Shipped behaviour already discards the tree-removal error outright (`_ = removeSlotTree(st)`).
- **The `f1` firing kept open decision 17 (the create-time-reserved retry's residue) with the human and rewrote it in place** so it is answerable in one sitting: harm first, then a low-confidence recommendation to accept as recorded, the ground, the three closures and why each lost. It is the only entry carrying a recommendation, and the section preamble was corrected to say so. Its "recorded under `## Decisions`" citation was FALSE — no such section exists in any file of this proposal; the record is `## Edge cases and accepted failure modes`.
- **The `f1` firing rewrote the 0080 §1.19 impacts row and the sentence it retired was wrong on BOTH halves.** "No bind-sequence RPC gains a refusal, so the class inventory grows by the reclaim hold's `ABORTED` refusal alone" is false: `errSlotReclaimInProgress` is raised at the top of `ensureSlotStateLocked`, which the bind-sequence and workspace RPCs are exactly what reach. The row now states that the hold adds one more producer of an arm §1.19 already enumerates (`boundSlotState` answers absent and unbound alike with one `codes.FailedPrecondition`, slotsession.go:274-283), that the hold's own refusal sits outside §1.19's inventory, and that the class SET is unchanged while membership moves.
- **Retuning `ceil(maxConcurrentSessions/2)` stays out of scope with NO row in the not-staged defects section,** because the proposal is silent on whether the value is right and a tuning question is not a defect it knowingly leaves. The Non-goals bullet is the record. Do not read the call as an assertion that the threshold is correct.
- **`resolvePrepareStagingDir` returns `(string, error)` and drops everything `ensureSlotPaths` gives it except `paths.Staging`** (staging.go:31,:116,:133-138), so any "epoch on the seven responses" work has to widen that helper too, not only `ensureSlotPaths`. FILED.
- **The compensation is synchronous inside `materializeSlot`,** so the gateway's own §5.2 retry can never race its own attempt's reclaim: `applySlotRetryPolicy` re-enters `BindSlot` only after `materializeSlot` returned. Every racing "retry" in the design and in the tier-4 late-reclaim arm is necessarily a fresh client request or a second replica.
- **`reclaimSlotLocked` inserts a hold only when it removed an entry,** and its `release` is a non-nil idempotent no-op otherwise, which is what stops a concurrent unfenced `Shutdown` (removed=false) from clearing another caller's hold on its deferred release. Subtle and correct as staged; do not "simplify" it.
- **The `budget/2` split's whole chain was verified end to end against the shipped adapter.** `contextWithGraceDeadline(parent, grace)` returns `WithTimeout(parent, grace)` for a positive grace and the parent unchanged for a non-positive one, and `resolveShutdownGrace` prefers that ctx's remaining time over the configured grace and the package default, so `ShutdownReclaim(rctx=budget, deadline=budget/2)` really does give `Runtime.Close` budget/2 and leave budget/2 of RPC deadline for the uninterruptible `removeSlotTree`. EVIDENCE: session.go:327-332; mcpruntime.go:308-323; socketruntime.go:457.
- **The §10.1.4 hold-termination pass cannot leak a reclaim hold, and it can hold several identifiers at once.** `deregisterStartedSessions` (slotsession.go:375-396) builds `members` only from entries it removed, under ONE `s.mu`, so CODE-6's `release func()` must be captured per member inside that loop; `onHoldTimeout` then runs `terminateHeldSession` for every member in a plain loop with no early break under a shared 10s context, and that function holds no `s.mu`, so a `defer release()` there cannot deadlock. The whole set is held from pass 1 until pass 2's last member returns. Nothing in the proposal says the hold set can carry several identifiers; it is correct, but a reader of CODE-6 alone will not expect it.
- **`heldSession` and `deregisterStartedSessions` live in `pkg/adapter/slotsession.go` (:306-313, :375), not `holdstate.go`,** even though CODE-6's prose attributes the latter to holdstate.go. The Files-touched list gets it right; do not spend a verifier pair on the prose.
- **CONF-1 says "Four properties, one per part of the §15.4 contract" while SPEC-5 lands TWO §15.4 blocks,** and all four properties are bind-epoch. The reclaim hold has no CONF-1 property, and neither does §15.4's `PrepareWorkspace` non-conformance clause.
- **`docs/reference/adapter-contract.md` is written for RUNTIME authors, not third-party ADAPTER authors** ("the protocol between the Lenny adapter sidecar and your runtime binary", :10; "Your runtime binary never sees them directly", :53). A finding demanding the full §15.4 bind-epoch and reclaim-hold contract be republished there is over-reach; the defensible scope is the `Shutdown` row's now-false statements.
- **The docs surface this proposal can falsify is small and now fully enumerated.** `grep -rln "Shutdown" docs/` returns three files and only `adapter-contract.md` describes the gateway→adapter RPC; the other two are the runtime-facing JSONL `shutdown` frame. The operator-narrative half is inert: no operator page or runbook enumerates leaked-slot or pod-replacement causes. SPEC-3's two added cleanup actions have no docs mirror. The new §6.2 edge reaches no SVG (`pod-warm-path.svg` and `sdk-warm-path.svg` carry `receiving_uploads` as a POD-level phase and no `slot_cleanup` node at all), so DOCS-1's table row is the whole docs surface for SPEC-4.
- **`spec/06:283`'s `**State storage:**` paragraph says the fine session-lifecycle states are NOT projected onto the CRD,** which is the structural reason no Kubernetes-idiom lens has purchase on the §6.2 fence edit. The Kubernetes lens has now returned zero findings on this proposal in eight consecutive runs.
- **`ClaimSlot` pass 1 filters on the per-pod claim, `claimstate.IsTerminal`, the tenant pin and `expiredByUptime`, with NO `sb.Status.Phase` read; only pass 2 reads the phase.** That is the idiomatically correct answer (do not block a synchronous bind on a level-triggered controller), so `ExcludePods` is right for the right reason. EVIDENCE: podclaim/slotclaimer.go:411-478.
- **`s.podBinder` already satisfies the unexported `slotBinder` interface,** so CODE-5's new `resumeOnPod` caller of `accountSlotFailure(ctx, s.podBinder, …)` compiles without widening it. EVIDENCE: start.go:2725-2737.
- **`RecordFailure` has exactly ONE production call site in the shipped tree, `applySlotRetryPolicy` (start.go:2854),** and CODE-5 adds two more. Any proposal sentence saying "whose only production call site is `applySlotRetryPolicy`" is a pre-amendment fact the amendment falsifies.
- **The §5.2 exclusion ("did not complete") and §7.1's "an attempt that meets the hold is one the retry policy did not place") are bridged by the handler blocking on `removeSlotTree`:** a COMPLETED reclaim's answer arrives only after the cleanup finished, so the hold is released before a retry can be placed. The one interleaving that breaks the bridge needs TWO `Shutdown`s for one session, which the gateway does not send. Under CODE-6 there is a second, narrower producer: an `ABSENT` answer can return while the adapter's OWN pre-`Runtime.Start` rollback still holds the identifier, reachable only when the gateway's context expired mid-handler. Below the bar; the outcome is the accepted transient refusal.
- **DECISION (`f1.open-decisions.15`): §7.1's exclusive-pod clause needs NO mid-resume carve-out, and entry 15 is deleted.** The staging is already correct: §7.1 closes with "The pre-attached disposition governs the pod; this reclaim governs the slot state on a pod that is released or reused rather than terminated", and SPEC-2's §7.2 step 3 states no pod outcome by design. No staged text changed.
- **§6.2's `resuming` failure-transitions subsection is self-declared authoritative, at spec/06:229, and :234 states the outcome.** ":229" declares it "the **authoritative enumeration** of every edge out of the session-model `resuming` state" and ":234" says the half-claimed replacement pod is released to the pool. An earlier design cited :230 for the first; it is :229.
- **A released half-claimed pod's residue is disposed of in BOTH pool configurations by shipped text.** Non-recycling: spec/06:80 projects a claim deleted on a `recycle.enabled: false` pod through `draining` to `terminated`. CORRECTED (`spec.8.fix-G2.1`): that spec/06:80 clause is the over-narrow one SPEC-4 now corrects, because the code retires such a pod on either recycle setting. The conclusion survives and the citation is now to the corrected clause. Recycling: spec/05:453 runs the whole-pod scrub whenever occupancy reaches zero before reuse, and `podsession/binder.go:498-501` (the `Recycle` field doc comment) records that trigger on the release path.
- **The occupancy-zero recycle boundary already waits for slot cleanup.** spec/06:175 reads "When occupancy reaches zero **and all slot cleanup has finished**, the pod leaves `claimed`", which kills the "the hold deregisters the entry, occupancy hits zero, and the whole-pod scrub races the per-slot cleanup" dress before it is built.
- **§15.4 now PUBLISHES the hold refusal's status code, and the older refutation is stale.** The staged block reads "refused with the gRPC status code `ABORTED`, which is the transient classification a caller retries on". `grep -rn ABORTED spec/ docs/ schemas/` is empty, so this is the corpus's first spec-level gRPC status on the adapter surface; the only §15.4 precedent is the `UNIMPLEMENTED` mention in the SDK-warm demotion paragraph (spec/15:1469). `codes.Aborted` is already live on the same service for the busy checkpoint op lock (checkpoint.go:115, oplock.go:37), on a disjoint RPC set, so there is no collision.
- **§7.2 orders the step-3 reclaim BEFORE step 4's `coordination_generation` bump** (spec/07:213 then :214), so the compensating `Shutdown` carries the generation the pod still holds and the bump cannot fence it.
- **A §7.4 mid-session upload refused by the hold surfaces to the client as HTTP 502 `UPSTREAM_ERROR`,** on both the `PrepareWorkspace` and the `FinalizeWorkspace` arm, so the staged "the transient classification a caller retries on" holds for that caller too. EVIDENCE: pkg/gateway/sessionserver/upload_to_session.go:70,:126-137.
- **`superseded` is already bound elsewhere and does not collide.** It is a `manifest_reason` enum value in the §10.1 partial-checkpoint model (spec/10:148,:157; spec/16:198; spec/29:969), a different enum on a different message.
- **"Registry entry" and "the adapter's slot registry" are already spec vocabulary** (spec/16:188, spec/28:861), so SPEC-5's heavy use of the term mints nothing undefined.
- **§28.7's wire-contract artifact register is one row per ARTIFACT under `schemas/`,** derived from the directory rather than per message or field (spec/28:1759-1765,:1781-1783), so SCHEMA-1's nine additive fields and one enum add no §28.7 row.
- **Four tier-0 proto gates were checked against SCHEMA-1 and none fires.** `claim_register_proto_agreement_test.go` inspects only rows whose `surface` contains `schemas/lenny-adapter.proto` and both staged rows name Go files; `adapter_proto_message_scope_test.go` keys on the `SessionId`/`session_id` pairing; `adapter_proto_generation_scope_test.go` counts `operationalFenceSentence` occurrences at twelve and no staged comment carries it; `adapter_session_address/session_address_wire_test.go` checks `session_id` presence and `reserved 4`. The one gate that DOES fire is the tier-3 `TestShutdownMessagePostRemovalDescriptor_spec_4_1`.
- **S10 declares `Tiers 0, 1, 3, 7a, 9`,** which is what closed the tier-3 step-ordering finding (the tier-3 suite is titled for SCHEMA-1, CODE-1 and CODE-6, and only S8 and S9 named tier 3 before). Any later edit that moves the two `Shutdown` outcome cases must move that tier 3 with them.
- **`recycle.maxSessionsPerPod` retirement is advanced by `sessionsServed`, which `RecordSessionScrub` increments,** so SPEC-3's withheld report genuinely does not advance pod retirement for a bind abandoned after `receiving_uploads`, even though that bind ran `RunSetup` and wrote a credential file. Judged "less strict rather than a control regression": the design states it and §5.2 scrub step 0's credential purge is the backstop. A reopening must argue from residue step 0 does not reach.
- **The threshold-retune scope call has a third ground, and its citations moved.** The shipped code CONFORMS to spec here (spec/06:160's rolling-window failures plus persistent leaks reaching `ceil(maxConcurrentSessions/2)` is what the clamped `(maxConcurrent+1)/2` computes), so a defects row would have no divergence to cite. Current citations: the Non-goals bullet at summary.md:315-316, the "Faster pod churn" bullet at non-spec-changes.md:1954-1963, `UnhealthyThreshold` at slothealth.go:215, `Tracker.Unhealthy` at :136, the sole production trigger at start.go:2858. The `:1764-1773`, `:214-220` and `:2857` citations in the earlier firings have drifted.

- **The `running` boundary contradiction is CLOSED, by a third route neither earlier option named.** The fix deleted the word "acknowledge(ment)" from the three START-handover sites, restated the boundary in the fence trigger's own vocabulary, and a later pass then deleted the attribution clause "which is the moment the trigger above names" as well, so SPEC-4's paragraph states the boundary on its own authority. The fence annotation at spec/06:152 and its mirror at docs/reference/state-machines.md:235 stay untouched, DOCS-1 keeps its single added row, and CODE-3's doc comment and DOCS-1's row already read arrival. The tree's boundary is `Runtime.Start` RETURNING followed by `noteRuntimeStarted`; there is no runtime-side acknowledgement of a start anywhere in `pkg/adapter` (the "runtime acknowledgement" hits in spec/04, spec/07 and spec/15 are the unrelated interrupt / CH-RUNTIMEOPS mechanism). Seven lenses across three rounds re-derived the old contradiction; do not reopen it. EVIDENCE: pkg/adapter/resume.go:140-144; runtimegeneration.go:36-44; server.go:348-351.
- **"Acknowledged" still carries a second, correct sense, and it must not be swept.** Every surviving use in the staging outside those three sites means "the ADAPTER did not answer the compensating `Shutdown`", which is load-bearing and untouched. A grep-and-replace over "acknowledg*" breaks the leak predicate.
- **Every gateway `PrepareWorkspace` call carries at least one upload frame, on BOTH callers.** `stageWorkspace` sends the RPC only inside `if len(uploads) > 0` (binder.go:1323-1330) and the §7.4 mid-session path is fed by `parseUploadToSession`, which 400s on `len(req.Files) == 0` (upload_to_session.go:183-186); `sendUpload` emits at least one frame per upload (adapterclient/client.go:267-286). So the upload-free stream that would report a zero epoch is unreachable from production, and the proposal's justification, which names only `stageWorkspace`, under-states a conclusion that holds on both. Eight lenses re-derived this; do not re-run it.
- **DECISION: the `ExcludePods` placement append reads `sbe.Leaked` alone; the accounting discriminator keeps `sbe.Leaked || relErr != nil`.** `relErr` is the gateway-side `ReleaseSlotReservation` failure, a Redis counter and `SandboxClaim` rollback that sends the pod no RPC and leaves no residue on it, so §5.2's placement rule, stated in pod-residue terms, does not reach it; `ClaimSlot` places only on a genuinely free slot. Both conditions still leak occupancy, so the ACCOUNTING composite is required and must not be collapsed. ALTERNATIVES rejected: widening the staged §5.2 sentence to name a failed reservation release (exports gateway bookkeeping into a reader-facing placement rule and cascades through the edge-case list), and recording the divergence as accepted (the divergence was the finding).
- **There is a SECOND tier-0 register a new test file must enter, and it has THREE derived rules, not two.** `tests/tier0_static/spec_map_slot_address_registration_test.go` holds a hand-written `slotAddressCaseFiles` inventory, and `TestTheCreditInventoryCarriesEveryAddressGuardCaseFile` fails for any `_test.go` under cmd/, migrations/, pkg/, scripts/, sdks/ or tests/ that matches rule 1, `slotSurfaceCallRE` = `slotstate\.|ClaimSlot\(|ReleaseSlot\(|ReserveSlotOnPod\(|claimAtCreate\(|BindReservedSlot\(` over the BODY; rule 2, `slotSubjectFileRE` = `(slot|one_session_only|sole_session)[^/]*_test\.go$` over the BASENAME (the `[^/]*` bars a slash, so this is a basename rule; a mid-window shard that read it as the whole repo-relative path was corrected by four later shards); or rule 3, a file under `tests/tier11_docs/` whose body mentions `TEST-GAPS.md` (:1180-1186, applied at :1217-1226). Both regexps are case-sensitive and anchored on the `(`, so the adapter's own `claimSessionSlot(`, `releaseSessionSlot(`, `reclaimSlotLocked(` and even `ReleaseSlotReservation(` do not match. An EXTRA inventory entry is never a gate failure — only omission is; `tests/tier10_conformance/recycle_scrub_conformance_test.go` sits in the inventory and matches no rule. The gate is exhaustive against the tree today, so an omission goes red on the first tier-0 run. EVIDENCE: :352, :1108-1131, :1168, :1173, :1180-1186, :1191, :1212-1230.
- **`tests/tier4_integration/concurrent_workspace_test.go` is ALREADY in `slotAddressCaseFiles`.** That is why S12 owes it whole-file spec-map credits under 7.1 and 4.7 rather than an inventory row. Do not re-file the inventory half. EVIDENCE: the tier-4 block of tests/tier0_static/spec_map_slot_address_registration_test.go.
- **Entering `slotAddressCaseFiles` is not free, and the entry must land in the SAME commit as the file.** `TestSlotAddressCasesAreCreditedToEverySectionTheyAnnotate` then demands a `tests/spec-map.json` credit under EVERY section the file's own `// spec:` annotation names, and `TestAddressCaseNamesAgreeWithTheirOwnCitations` demands the `_spec_X_Y` name suffix agree with that annotation. An inventory entry for a file that does not exist yet dies in `repoFileBytes`'s `t.Fatalf`, and an existing matching file with no entry fails the omission check, so splitting the two across commits is red in both directions. Of this proposal's new files only the two `slot*`-named ones match; `pkg/adapter/bindepoch_test.go`, `tests/tier3_contract/adapter_bind_epoch/` and `tests/tier10_conformance/bind_epoch_conformance_test.go` do not.
- **`pkg/adapter/bindepoch.go` and `pkg/adapter/bindepoch_test.go` owe no register edit.** `_test.go` files are outside the change-graph completeness domain (`changegraph.IsSourceFile` returns false), `pkg/adapter/` is already a coverage-baseline directory prefix, and `validateTestFilesMapped` walks only `componentAndAboveTierDirs()` (tier 2 and above), so a tier-1 package test under `pkg/` needs no spec-map entry. Three separate gate worries, all dead.
- **`tracing.Category` does not exist; the type is `tracing.ErrorCategory`.** The proposal said the former and it was corrected. The category is readable from an in-package tier-1 test with no new fixture: `tracing.RecordError` attaches it as the `error.category` span attribute (`AttrErrorCategory`), and `pkg/adapter/tracing_internal_test.go` already ships `installInternalSpanRecorder` and `endedSpanNamed` in package `adapter`.
- **The three `tracing.CategorizeError(..., tracing.CategoryPermanent)` stamps CODE-6 must displace are at staging.go:81, :183 and :339,** and all three handlers wire `spanErr` into a deferred `tracing.RecordError`. Asserting `slotResolveCategory` in isolation pins the helper and NOT that those three literal stamps moved, which is exactly the ship-it-broken path; the assertion belongs on the handler's recorded span. The other two resolve sites (slotsession.go:75-78, slotcreds.go:26-28) stamp no category at all, so the asymmetry is real and is named in the staging.
- **The tier-4 datastore-crossing case's trigger is an UNANSWERED compensating `Shutdown`, not one the adapter refuses.** After the amendment a refusal is `SUPERSEDED`/`ABSENT`, which CODE-4 maps to `leaked=false`, the opposite of both assertions the case exists for; the unanswered form takes the `err != nil` arm. The fixture needs no new harness: `recycleAdapterDialer` builds its client through `adapterclient.Dial`, which is variadic over `grpc.DialOption`, so a unary client interceptor keyed on the `Shutdown` RPC drops in.
- **`isTransientPodClaimError` has exactly ONE production caller, `holdOrFailOnResumeError` (start.go:3610), reached only from `handleResume`.** So CODE-5's `codes.Aborted` arm cannot widen any other classification, cannot touch the start path, and cannot be reached from `treerecovery.go`'s `resumeOnPod` caller. `status` and `codes` are already imported at start.go.
- **`status.Code` walks the wrap chain in this tree's grpc.** v1.80.0's `Code` → `Convert` → `FromError` falls back to `errors.As` for `GRPCStatus()`, verified in the module cache by four lenses, so the `codes.Aborted` arm fires through `fmt.Errorf("%w")` plus `*SlotBindError`. Do not re-derive it; do not reach for an `errors.Is` against the adapter-local sentinel, which is unexported and does not survive gRPC.
- **`recordSlotFailure` has exactly five call sites and all five are inside `materializeSlot`.** `pkg/gateway/podlifecycle/podsession/binder.go`, which holds `Binder.Resume`, has zero, so the §7.3 re-attach can move the leak gauge, trip the threshold and increment `lenny_slot_pod_replacement_total` with no `lenny_slot_failure_total` series on that pod at all. Any proposal sentence grounding a non-goal in "the counter already labels the stages at exactly these call sites" is false at the third compensation call site.
- **`resumeOnPod`'s two branches, with line numbers.** The snapshotless resume-rebuild ends at start.go:3978 and reaches `applySlotRetryPolicy` through `startOnPod` → `bindConcurrentSlot`; the checkpoint-restore branch begins at :3980, calls `s.podBinder.Resume` at :4005 and is followed immediately by `if err != nil { return "", err }` at :4041-4043 with NO retry loop. Any sentence giving the client-driven §7.3 resume a retry budget before `holdOrFailOnResumeError` is describing the other branch.
- **`releaseSessionSlot` calls NO `Runtime.Close`.** Its whole body is `deregisterSlot` → `removeSlotTree` → `cancelPodMCPIfRuntimeIdle` (slotsession.go:214-220). Only `Shutdown` (session.go:263) and the §10.1.4 `terminateHeldSession` (holdstate.go:243) close the runtime. It is also fully synchronous, which is what makes the hold it opens end before the requesting RPC answers.
- **`onHoldTimeout`'s pass 2 shares ONE 10-second context across every member, not ten seconds each,** and it loops with no early exit, so the holds pass 1 takes for all members are bounded in aggregate rather than multiplied by N. A future `continue` or `return` inside that loop would leak every skipped member's hold for the life of the pod.
- **`coordinatorHoldAllowedMethods` has FIVE entries, not three:** `CoordinatorFence`, `NegotiateVersion`, `AdapterEvents` and both `grpc.health.v1` methods (holdstate.go:52-58). `Shutdown` is genuinely refused there, so the accepted-failure-mode bullet naming "the fence, version negotiation, the event stream, and the health probes" is accurate.
- **`tests/claim-map.json` is exactly 76 rows today: 32 WIRED, 24 UNWIRED, 20 ABSENT.** Count it with python over the JSON rather than by grep. SCHEMA-1's two WIRED rows take the denominator to seventy-eight and the WIRED count to thirty-four; 0080 §1.12 and §1.18 both quote "seventy-six", and the consolidated impacts row names only §1.12's.
- **DOCS-2 verifies end to end against the tree and the gate.** Its anchors resolve (`adapter-contract.md:10` self-description, :64 `DemoteSDK` row, :75 `Shutdown` row, :84 `**Scrub responsibilities.**`), its replacement row carries all four substrings `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` requires plus the two the staged tier-11 extension adds, and the added block carries the lowercase "bind epoch" and "reclaim hold" the page-level assertions need. Its `DemoteSDK` claim is true in the tree. Three other tier-11 gates read that page with `requireNoneContain` and none of their forbidden strings appears in the staged text.
- **`Binder.DialAdapter` is an injected dialer (binder.go:59) and `adapterclient.Dial` is variadic over `grpc.DialOption`,** so any tier-4 or tier-3 case that needs to fail, delay or record an adapter RPC installs an interceptor without touching production wiring.
- **The widened-function test-caller surface is exactly seven files, and one grep finds them.** `grep -rn "ensureSlotPaths\|claimSessionSlot\|resolvePrepareStagingDir\|noteRuntimeStarted\|claimPodMCPStart" --include=*_test.go .` returns `pkg/adapter/{adapterevents,export,exportpaths,one_session_only,podmcp_arming_internal,slotsession,usage}_test.go` and nothing else; all five identifiers are unexported so no caller exists outside package `adapter`. The r2 fix added the two that were missing (`exportpaths_test.go`, `one_session_only_test.go`) to the `Tests:` bullet, which closes the standing Open. Note `socketruntime_test.go` is in that bullet for a different reason, so the bullet is not the widened-caller set.
- **`one_session_only_test.go` holds SEVEN `claimSessionSlot` call sites (:91, :94, :125, :159, :194, :209, :212), not nine.** Two shards wrote nine; the staged clause deliberately carries no count.
- **`podRegistry.Put` has exactly three production callers** (start.go:2936, :4057, cmd/lenny-gateway/coordination_seams.go:249), and the third is the coordinator-handoff re-adopt, which dials fresh and sends `CoordinatorFence` as its only RPC, so CODE-6's zero-latch carve-out is accurate.
- **Rule S-2's covered handler files are `session.go`, `lifecycle.go`, `checkpoint.go`, `coordination.go`, `credentials.go`, `slotcreds.go`, `attach.go`, `sdkwarm.go` plus the two renamed channel files.** This proposal opens exactly three of them, so "three of S-2's covered handler files" is right; `staging.go`, `slot.go`, `slotsession.go`, `resume.go`, `holdstate.go` and `runtimegeneration.go` are not covered. S-2's "every in-flight `pkg/adapter` handler edit must have merged first" reads inverted against S8 preceding S9-S11 and is not: putting S8 first is what guarantees none of this proposal's handler edits is in flight.
- **spec/04:1538 says `AssignCredentialsResponse` carries a `leaseToken` field, and the message is `{}`.** Already false before this proposal; SCHEMA-1 adding `bind_epoch = 1` neither creates nor worsens it. At least four lenses have met it beside the SCHEMA-1 table and had to rule it out.
- **`cleanupTimeoutSeconds` is not a CRD field anywhere in the tree,** despite §5.2 stating its floor as a "CRD validation rule" the `SandboxWarmPool` admission webhook enforces. `grep -rn cleanupTimeoutSeconds charts/ pkg/apis/` is empty. Pre-existing and unedited by SPEC-3.
- **The §5.2 reclaim hold's "or resolve" arm is VACUOUS for every RPC, and that is the fastest kill for the whole "the hold refuses X" family.** The hold begins at the critical section that DEREGISTERS the entry, so while it runs no entry exists and nothing can resolve one; the only reachable arm is creation, and the creation sites are exactly the bind sequence. It is why §15.4's "`Shutdown` is not held" is a clarification rather than an exception, and why `Attach`, `Interrupt`, `Checkpoint`, `ReportUsage` and the three credential RPCs are untouched. Refuted at least eight times.
- **§15.4's "Current protocol version: `1.0.0`" is not an obligation this proposal breaches.** Nothing in the spec requires a bump on an additive change, and `.claude/rules/code-best-practices.md` bars backward-compatibility handling outright because the platform is pre-deployment, so a deliberate semantic conformance break at an unchanged version string is not a finding. Recorded as looked-at rather than open.
- **Tier 9 and tier 10 both drive the exported `adapter.Server` directly in this repo,** with a fake runtime and fake scrub host ops rather than a runtime binary over JSONL, so CONF-1's filesystem assertions and the tier-9 credential-fence arms are observable at the tiers they are filed under, and parking inside `Runtime.Close` to assert an `ABORTED` refusal is mechanically available at tier 10.
- **There are TWO independent spec-map gates and the proposal now discharges both.** `validate-maps` (`cmd/lenny-test/cmd_validate.go:716`, `validateTestFilesMapped`) requires every `_test.go` under `componentAndAboveTierDirs()` to resolve to a spec-map entry, and a `dir/...` glob covers a whole directory. `TestSlotAddressCasesAreCreditedToEverySectionTheyAnnotate` (tests/tier0_static/spec_map_slot_address_registration_test.go:970) separately requires, for every file already in `slotAddressCaseFiles`, that `tests/spec-map.json` credit every section that file's own `// spec:` annotations name. The second gate is invisible from `validate-maps` and fires on a NEW CASE in an EXISTING file. Eight lenses across four rounds converged on this.
- **The credit gate's mode is per FILE, one row flips it, and a whole-file credit still counts in per-case mode.** `specMapCredits` (:824-858) puts a `path::TestName` row into `perCase` and every other covering entry into `wholeFile`; the gate takes the whole-file branch when `len(perCase) == 0` and the per-case branch otherwise, so a single per-case row switches every case in that file into per-case checking, forever. `creditsMissing` (:950-963) accepts `wholeFile[key] || perCase[fn][key]`, consulting `wholeFile` in BOTH branches, so the per-case branch is WEAKER than the whole-file branch and adding the first per-case row to a whole-file-credited file cannot turn tier 0 red. SETTLED (this closes the standing Open and the Trap that recorded the disagreement, re-derived by at least six shards): a whole-file credit DOES satisfy a case in a per-case-registered file, which is why `slotclaimer_test.go`'s §5.2-only tier-2 cases owe nothing even though that file carries nine per-case rows.
- **A `<dir>/...` entry counts as a whole-file credit.** `specMapEntryCoversFile` (:811-819) resolves directory globs, so a check written as `entry == path` misses them and manufactures false "this file has no credits" findings. The live globs that matter here: `pkg/adapter/...` under 4.7 only, `pkg/gateway/...` under 4.1, `pkg/gateway/sessionserver/...` under 11.4, `tests/tier4_integration/...` under 15.1.
- **The per-file credit census, with globs resolved.** `slotretry_test.go` whole {4.1, 11.4, 15.1} plus fifteen per-case rows; `slotclaimer_test.go` whole {4.1, 5.2} plus per-case; `recycle_scrub_path_test.go` whole {15.1} plus per-case; `slotbinder_test.go` whole {4.1, 4.6.3, 4.7, 5.2, 6.2, 7.1}, no per-case; `slotsession_test.go` whole {4.7 via the subtree, 4.9, 5.2, 6.1, 6.4, 15.4}, no per-case; `concurrent_workspace_test.go` whole {5.2, 6.4, 15.1, 28.5.3}, no per-case; `start_test.go` whole {4.1, 11.4} and outside the inventory. Two shards published different sets (`slotretry_test.go` as {15.1}, `slotbinder_test.go` without 4.1); that is the bare-path artifact rather than a disagreement, and the resolved sets above are the ones every later shard reproduced.
- **DECISION: the tier-4 concurrent-workspace case is `TestAbandonedBindLeavesNoSlotResidueOnTheSharedPod_spec_7_1`,** annotated `// spec: §7.1; §5.2; §4.7`, with the late-reclaim arm as a subtest of that same function and WHOLE-FILE spec-map credits for `tests/tier4_integration/concurrent_workspace_test.go` under 7.1 and 4.7 staged at S12. ALTERNATIVES rejected: per-case `path::TestName` rows (one such row flips the whole file into per-case mode and re-attributes the shipped cases and file-header annotations); folding the assertions into the shipped `TestConcurrentWorkspacePerSlotExecution_spec_5_2` (the only tier-4 case pinning §7.1's reclaim and §4.7's `SUPERSEDED` answer would then be credited to neither, and `lenny-test --spec 7.1` would not select it); copying the sibling's `§7.1; §5.2; §6.2` (this case asserts no §6.2 edge and does assert §4.7).
- **DECISION: the CODE-6 tier-1 contradiction was closed by deleting "plus cases in the two existing files below".** It contradicted the same subsection's "all of them land in the one new file" and had no resolvable referent; naming `slotsession_test.go` as a CODE-6 case home instead was rejected because that file carries no §7.1 credit.
- **DECISION: SPEC-3's leak-accounting sentence is scoped to the cleanup a reclaiming `Shutdown` performs,** with one added sentence naming the whole-pod scrub as where the handler-run cleanup's residue is accounted. The sentence was false only in naming one carrier for a cleanup that has two performers, and the second performer cannot be given a carrier without an adapter-side residue record plus a fourth reclaim outcome on the wire. ALTERNATIVES rejected: inverting CODE-4's `ABSENT` arm to leaked (`ABSENT` is the common answer on three branches and a leaked verdict drains a pod at `maxConcurrentSessions: 2`; it is also the inversion the hand amendment already undid); a fourth `SlotReclaimOutcome`; returning the error from `releaseSessionSlot` to its callers; deleting the sentence. CORRECTED in the same pass: the scrub does NOT unconditionally retire the pod on a failed verification, because `onScrubFailure` defaults to `warn`, which returns the pod to the pool with a `scrub_warning` annotation; only `fail` or reaching `maxScrubFailures` terminates it (spec/05:483,:484,:490). The staged text says "disposes of the pod under the `onScrubFailure` policy stated below".
- **The whole-pod scrub is a RECYCLING-pod mechanism,** running when occupancy reaches zero on a recycling pod before reuse (spec/05:453,:456). Any claim that the scrub bounds a residue owes the non-recycling half, where the pod retires at the same boundary. The staged edge-case bullet states both.
- **`releaseSessionSlot`'s production callers are the start-path rollbacks plus `DemoteSDK`:** session.go:133,:147,:157; resume.go:69,:73,:89,:107,:126,:134,:141; sdkwarm.go:236,:241,:251 and :298. The `sdkwarm.go:298` site is `DemoteSDK` rather than a rollback and is the odd one out. UNVERIFIED: one shard counts fourteen and another sixteen with neither correcting the other; the enumeration above is what both were reading, so a round that needs the count should recount rather than cite either.
- **DECISION: CODE-5's "Where it is read" bullet splits the two axes.** `BindSlot` stays by value, so the `ExcludePods` pointer threading touches no implementor of `slotBinder`; CODE-4's `leaked bool` widening of `ReleaseSlotReservation` DOES change the interface, both fakes take the fourth parameter at S12, and `fakeSlotBinder.released` widens from `[][2]string` to a record carrying pod, slot id and disposition. CODE-4's call-site table gained a row naming `slotretry_test.go`'s `fakeSlotBinder` and `slotretry_load_test.go`'s `concurrentSlotBinder`. This is the fix for the sentence four filings could not close; `concurrentSlotBinder` records nothing by design and needs the parameter and nothing else.
- **A live tier-11 gate reads the §4.7 `Shutdown` row and SPEC-1's replacement survives it.** `tests/tier11_docs/recycle_scrub_trigger_consistency_test.go:65,:75-100` requires that row to carry "recycle disposition", "ReportPodScrub", backticked `podId`, `cleanupCommands` and `cleanupTimeoutSeconds`, "does not block the response on the scrub", and the `05_runtime-registry-and-pool-model.md#52-...` link. SPEC-1 replaces only the row's opening and leaves the remainder standing, which is where all but the link live; the staged opening carries the link itself. S2's tier list already names tier 11. Nobody in the non-spec loop had recorded this gate.
- **`Binder.ClaimSlot` closes its connection after the §15.5 handshake and never reaches `materializeSlot`,** so CODE-4's call-site table is right to give its connect-stage release `false` and there is no fourth compensation site hiding there. EVIDENCE: slotbinder.go:128-134,:157-190.
- **The post-bind `registerBinding` failure is NOT a §7.1 reclaim gap.** `rollbackBinding` routes a slot-bearing result to `podBinder.ReleaseSlot`, which is the call that already sends the adapter its `Shutdown`, so the obligation's "ends when that attempt succeeds" boundary leaves no unreclaimed window. EVIDENCE: start.go:3166-3179.
- **`resumeOnPod`'s new `accountSlotFailure` caller cannot double-account.** The snapshotless resume-rebuild returns from the `startOnPod` branch (start.go:4020-4023), which already accounted through `applySlotRetryPolicy`; the new call sits on the checkpoint-restore `podBinder.Resume` branch alone, where `match` and every collaborator is in scope.
- **On a NON-recycling concurrent pool the occupancy-zero release retires the pod.** The claim DELETE plus §4.6.1's occupancy projection moves it claimed → draining, so the edge-case bullet's "a pod that does not recycle retires at that boundary and takes the residue with it" is true and the cross-tenant credential-residue finding it looks like does not exist. `Binder.ReleaseSlot`'s own doc comment ("the claim is deleted so the pod returns to the pool") is the loose wording that makes it look otherwise.
- **`AssignCredentialsResponse` is constructed inside `assignCredentialsSlot` (slotcreds.go:52),** not in the `AssignCredentials` handler (credentials.go:65), which only delegates. That is what lets the files-touched list name `slotcreds.go` for the epoch on that response, leave `credentials.go` unopened, and keep the S-2 covered-handler count at three.
- **`sectionsFromLines` reads `// spec:` annotation blocks ONLY** — never a bare `§X.Y` in prose, a `// diagnosis:` line or an assertion string (:679-693,:797-805). So DOCS-2's tier-11 extension adds no credit obligation even though it asserts §4.7.1 and §15.4 content on a file credited {4.1, 4.7, 5.2, 15.4.3, 28.5.3}. A later fix round that WIDENS that annotation turns tier 0 red.
- **`adapter_proto_generation_scope_test.go` counts EXACT occurrences of one sentence** (`operationalFenceSites = 12`) over the normalized proto text (:130-137,:186-190). A SCHEMA-1 field comment that copies a neighbouring field's generation sentence breaks the count and turns tier 0 red. The staged comments do not.
- **The golden-bytes case survives SCHEMA-1.** `TestShutdownRequestUnsetRecycleWireIdentical_spec_4_7` pins an exact byte string with everything but fields 1-3 unset, and proto3 emits nothing for a zero `int64`, so `expected_bind_epoch = 0` adds no bytes. EVIDENCE: tests/tier3_contract/gatewaycontrol_scrub/shutdown_recycle_wire_test.go:43-83.
- **§5.2's CRD webhook makes the 5s floor unreachable on an admissible concurrent pool.** It rejects any pool with `cleanupTimeoutSeconds < maxConcurrentSessions × 5`, so `slotCleanupBudget` is always `cleanupTimeoutSeconds / maxConcurrentSessions` and at least 5s, and larger on a generous pool. The proposal's repeated "an unset pool yields the 5s floor" framing understates the typical budget; at the spec's own canonical pool (`cleanupTimeoutSeconds: 60`) it is 15s at concurrency 4.
- **The per-pool claim FIFO is strictly head-of-line serialized.** Only the ticket at the head re-enters `attempt(ctx)`, and the next waiter is admitted by `yield` or `dequeue`, both of which run after `attempt` returns, so latency added inside a bind attempt is multiplied across every queued request for that pool. EVIDENCE: start.go:2606; queue.go:191,:205,:220,:240-280. This is a different mechanism from the refuted "`ExcludePods` head-of-line-blocks the pool FIFO" dress, which was about a waiter that yields to the tail.
- **`applySlotRetryPolicy`'s exhausted return is `SlotFailedError`, never an exhaustion sentinel,** so nothing downstream re-enters the queue. `runWithQueue` re-enters only on `ErrNoIdlePod`, `ErrNoConcurrentSlot` or `ErrTenantMismatch` (queue.go:103-107, gate at :143), and the refusal therefore ends the request on every pool mode including `queue`. EVIDENCE: start.go:2874-2881.
- **DECISION: the "what can meet the reclaim hold" finding was closed by prose at three sites** (the accepted-failure-mode bullet, CODE-5's "When it does not fire", summary.md's lagging-rollback residue) rather than by widening the placement predicate. ALTERNATIVES rejected: widening CODE-5's `ExcludePods` append to `ABSENT` (the common healthy answer, and CODE-4's outcome-first mapping exists to stop it reading as failure); a fourth `SlotReclaimOutcome` plus a second placement discriminator (cascades into SCHEMA-1, SPEC-5, CODE-1/4/5, tier 3 and tier 10 for a one-attempt cost); a new tier-1 case (already pinned by the conjunction of the `releaseSessionSlot`-hold case and the `Shutdown`-during-hold `ABSENT` case).
- **`reclaimSlotLocked` takes the hold only when the deregistration removed an entry,** so a compensating `Shutdown` landing BEFORE the rollback removes the entry itself, and the rollback that follows removes nothing and takes no hold. That third ordering is safe and must not be described as a hazard.
- **The real deltas of the non-spec window, recorded once.** r2 and r3 were byte-identical to r1. r3 → r4: the tier-4 case gained its name and annotation, S12's checklist line gained its spec-map sentence, the CODE-6 tier-1 preamble dropped "plus cases in the two existing files below", and the spec-map bullet gained the `concurrent_workspace_test.go` paragraph. r4 → r5: the third hold producer, six previously unnamed test functions, the tier-2 annotation narrowed from `§7.1; §5.2` to `§5.2`, and the rewritten `tests/spec-map.json` bullet. r5 → r6: the `f4` firing and the 0080 impacts row. r6 → r7: one cell of that row (the claim-register counts, 76 → 78 rows and 32 → 34 WIRED). Nothing else moved in seven rounds.
- **The §4.7 RPC tables live INSIDE §4.7.1.** The heading is spec/04:659, the Gateway→Adapter table runs :665-686, the Adapter→Gateway table opens :688 and closes before `#### 4.7.2` at :695. SPEC-1 (the `Shutdown` row opening) and SPEC-5 (the insert after the second table) therefore edit two anchors in one subsection without colliding. A reviewer who reads "§4.7" and "§4.7.1" as different sections files a false collision.
- **The gateway ALREADY classifies `codes.Aborted` as transient on the adapter surface,** in a shipped comment at start.go:228-230 ("Internal/Aborted/ResourceExhausted is a transient setup-window transport failure"), which is exactly what §15.4's "the transient classification a caller retries on" needs. The choice of `ABORTED` is a restatement rather than a mint on the gateway side.
- **The §10.1 coordinator hold publishes `UNAVAILABLE` with a `coordinator_hold` detail** at four §28 sites (spec/28:251,:304,:377,:1813), while §15.4's reclaim hold publishes `ABORTED`. Different holds, and nothing in `spec/` maps gRPC codes to categories, so the asymmetry is not a contradiction. A future reviewer will notice it; do not file it on that basis alone.
- **§5.2's whole-pod scrub, clause by clause, so SPEC-3's backstop sentence need not be re-derived.** Step 0 removes every `/run/lenny/slots/{sessionId}/credentials.json` (spec/05:461); step 2 is `rm -rf /workspace/slots/*` (:468); step 6 stat-verifies `/workspace/slots/`, `/tmp`, `/dev/shm` and `/run/lenny/slots/` and marks the scrub failed on residue (:471); `onScrubFailure` governs the failure (:481-483). All four sit BELOW the `**Scrub model.**` paragraph at :453, so SPEC-3's "stated below" pointers are literally true. More than a dozen shards verified this; do not re-run it.
- **The whole-pod scrub enumerates `/run/lenny/slots` children ON DISK rather than from the registry** (pkg/adapter/podscrub.go:132-137, whose own comment contemplates "a leaked slot whose registry entry is already gone"), so it reaches a credential residue whose entry the adapter has already deregistered. That is what makes SPEC-3's whole-pod-boundary accounting reachable rather than notional.
- **A pod that does not recycle retires at the occupancy-zero boundary in BOTH non-recycling modes.** The §5.2 configuration table gives `maxConcurrentSessions: 1, recycle.enabled: false` as "terminates when the session ends" (spec/05:424) and the bounded cohort as "terminates after the cohort drains" (:427); spec/06:80 projects a claim deleted on a non-recycling pod through `draining` to `terminated`, and spec/06:175 makes occupancy-zero wait for slot cleanup.
- **`buf`'s lint config is inline in the ROOT `buf.yaml`** (STANDARD plus `enum_zero_value_suffix: _UNSPECIFIED`), despite that file's own comment claiming the module config lives in `schemas/buf.yaml`, which does not exist. `SLOT_RECLAIM_OUTCOME_*` satisfies ENUM_VALUE_PREFIX and the zero-value suffix rule. EVIDENCE: buf.yaml:8-20.
- **The tier-0 change-graph completeness gate excludes `_test.go`, and `pkg/adapter/` is already a coverage-baseline prefix,** so neither `pkg/adapter/bindepoch.go` nor any new test file nor the new `tests/tier3_contract/adapter_bind_epoch/` directory owes a `tests/change-graph.json` key. EVIDENCE: cmd/lenny-test/cmd_validate.go:398-401; tests/registers/change-graph-coverage.yaml:43.
- **`tests/registers/identifier-senses.yaml` is keyed by file plus occurrence index of a RETIRED channel spelling,** so an insertion into an edited spec file could in principle shift its indices. It cannot here: spec/04, 05, 06, 07, 15 and 29 carry no retired spelling at all; the only live ones are the naming-table rows in spec/28:149-159 and spec/17:1530.
- **`tests/tier3_contract/` is organised as per-surface SUBDIRECTORIES** (`adapter_events/`, `adapter_generation_fence/`, …), which is why the spec-map bullet registers `tests/tier3_contract/adapter_bind_epoch/...` as a directory entry rather than a file. An agent expecting a flat file there will think the entry is malformed.
- **The three deregister-then-destroy sites are exactly three, and `ShutdownDemoteSDK` is not a fourth.** `Shutdown` (session.go:238), `releaseSessionSlot` (slotsession.go:215 via `deregisterSlot` at :192) and `deregisterStartedSessions` (slotsession.go:389, reached from holdstate.go:190, destroying at :254). `ShutdownDemoteSDK` (sdkwarm.go:69) delegates to `DemoteSDK`, whose release is `releaseSessionSlot` at sdkwarm.go:298.
- **`ensureSlotStateLocked` (slot.go:105-124) is the ONLY writer of `s.slots[...]` and `deregisterSlotLocked` the only `delete`,** verified with `grep -rn "s\.slots\[" pkg/adapter/*.go | grep -v _test` (four reads, one write, one delete). CODE-6's hold refusal at the top of that function therefore has no bypass.
- **`status.Code` reaches through BOTH wrappers.** grpc-go v1.80.0's `Code` → `Convert` → `FromError` falls back to `errors.As` for `GRPCStatus()`, and `SlotBindError.Unwrap` (slotfailure.go:74) and `SlotFailedError.Unwrap` (:143) both return the cause, so CODE-5's `codes.Aborted` arm fires on the bare status, through `*SlotBindError`, and through the `*SlotFailedError{Err: sbe.Err}` that `applySlotRetryPolicy` returns on its last attempt.
- **`isTransientPodClaimError`'s shipped internal table carries no `Aborted` row** (podclaimerror_internal_test.go:354-383), so CODE-5's new arm turns no shipped case red, and the only `codes.Aborted` producers under `pkg/` and `cmd/` outside tests are `pkg/adapter/checkpoint.go:115` and the `opLock` refusals it wraps (oplock.go:27-40), neither reachable from `resumeOnPod`.
- **`claimSessionSlot`'s `idempotentRepeat=true` has exactly ONE production caller,** `ConfigureWorkspace` (sdkwarm.go:217); `StartSession` (session.go:111) and `Resume` (resume.go:50) both pass false. So CODE-6's idempotent-repeat clause covers the whole early-return-before-epoch-capture hazard and no second handler needs it.
- **The reclaim hold cannot refuse a mandatory credential control, and this is the fastest kill for that whole family.** The hold refusal sits only in `ensureSlotStateLocked`, whose five production callers are staging.go:134/:181/:337, slotsession.go:75 and slotcreds.go:26. `RevokeCredentials` (slotcreds.go:125), `RotateCredentials` (:70), `ExtendCredentialLease` (:122), `CoordinatorFence` (coordination.go:116) and credentials.go:121 all resolve through the read-only `slotStateLocked`/`boundSlotState` and take no hold. A future change that moves a revoke or rotate site onto `ensureSlotStateLocked` turns this into a fail-open; that is the thing to re-check.
- **There is NO per-session missing-report timeout.** Every missing-report timer in the tree is the §5.2 whole-pod recycle-boundary timer armed at the `bound → recycling` patch (pkg/gateway/session/recycle/recycleboundary.go:24-74), so withholding `ReportSessionScrub` on the pre-`running` path leaves no timer to fire.
- **`slotstate.Registry.MarkLeaked` bypasses `IsValid`/`ValidTransitions` entirely** (registry.go:99-115), so the pre-`running` leaked disposition cannot be blocked by the edge list and the gauge moves with or without CODE-3's new edge. Do not file "the leak gauge will not move because `receiving_uploads → leaked` is illegal".
- **The adapter's only pod-wide entry count is the unexported `(*Server).slotCount`** (`len(s.slots)` under `s.mu`, slotsession.go:398-407), read solely by the §28.5.3 output demultiplexer (attach.go:70,:304-313,:314,:345,:357). No adapter response in `schemas/lenny-adapter.proto` carries a slot-count or occupancy field. That is what establishes there is no alternative rebuild source for pod occupancy, and it is the citation the Redis-rehydration row now uses.
- **DECISION (`non-spec-recheck-3.2.fix-design-G1.1`): the Redis-rehydration row KEEPS the adapter as its subject** and re-points its citation from `pkg/sandbox/slotstate/registry.go:8-12` to `pkg/adapter/slotsession.go:398-407`, adding that no gateway-to-adapter RPC returns a pod-wide occupancy count. ALTERNATIVES rejected: re-subjecting the clause to the gateway registry (true sentence, but it stops supporting the row's conclusion) and citing both components (hair on a row the proposal deliberately does not stage).
- **`pkg/adapter/slotsession.go` has no `log/slog` import today,** so CODE-6's "gains a `log/slog` import" for the `_ = removeSlotTree(st)` replacement at slotsession.go:217 is accurate.
- **`PodExecutor.Release` calls `registry.Remove` BEFORE `binder.ReleaseSlot`** (pkg/gateway/session/executor/pod.go:315,:320), which is the ordering the tier-7a upload arm's narrow window rests on.
- **`ExcludePods` exists nowhere under `pkg/` or `cmd/` today** (`grep -rn ExcludePods` returns nothing), so the absence claim survives the standing verify-by-reading-the-tree hazard. The bullet "`ExcludePods` is unchanged in substance and stays whole" (non-spec-changes.md:968) is a statement about the AMENDMENT not touching the deliverable, not a claim about the tree.
- **Every `podsession.SlotBindRequest` field is in-process only.** The struct carries no serialization tags (slotbinder.go:26-56), so `ExcludePods` reaches no wire format.
- **`bindConcurrentSlot`'s `slotReq` is a by-value PARAMETER captured by the `runWithQueue` closure** (start.go:2594, :2606-2609), re-verified this window, so CODE-5's `&slotReq` pointer threading genuinely survives a queue re-entry.
- **The claim-register validator's WIRED-row rules are three:** a non-empty `surface`, a surface that is not a bare line reference, and no `deferral_id`. The reachability gate (`adapterClientReachableRPCs`) is joined to the three credential rows alone. Both staged rows comply, and `#2851-gateway-to-pod` resolves to spec/28:205 and is already used by roughly thirty shipped rows. EVIDENCE: tests/tier0_static/claim_register_test.go:36-46, :222-307.
- **No gate anywhere hardcodes a claim-register row count;** the "seventy-six" figures live only in proposal 0080's prose (0080:163, :308). `tests/claim-map.json` is 76 rows, 32 WIRED / 24 UNWIRED / 20 ABSENT, so the summary's 78 / 34 arithmetic is right.
- **`cmd/lenny-compliance`'s proto reader is a regexp scoped to `enum ErrorCode`** (schemaassert.go:54-56), so SCHEMA-1's sibling `SlotReclaimOutcome` enum cannot perturb it.
- **The checklist re-simulated clean this window: fourteen steps, fifteen deliverables.** S6 carries DOCS-1 AND DOCS-2, both docs lane; every staged deliverable sits in exactly one step; every `Depends on` names an earlier existing step (S1 —; S2/S3/S4 S1; S5 S4; S6 S2,S5; S7 S5; S8 S1,S2; S9 S1,S8; S10 S2,S4,S9; S11 S3,S10; S12 S3,S4,S10,S11; S13 S12; S14 S1,S10); one lane per step; the spec block leads; no box is checked. `schema` is a valid lane.
- **The summary's file-collision bullet was widened** to name `server.go`, `staging.go`, `slotcreds.go` and `holdstate.go` alongside `slot.go`, `slotsession.go`, `session.go`, `runtimegeneration.go` and `bindepoch.go`, which is NINE, and the 0080 impacts row was rewritten to the full ELEVEN-file list that matches `## Files touched on application` (the two the bullet still lacks are `resume.go` and `sdkwarm.go`). The earlier "summary asserts slot.go untouched" drift is gone; the nine-versus-eleven disagreement stands and is the standing Open.
- **0080 §1.2 is discharged in its ENTRY-REMOVAL half only, and its third clause has an independent cause.** That clause, that a later session on the pod arms neither MCP server, survives without any failed bind: `claimSessionSlotUnderLock` inserts the claimant's own entry through `ensureSlotStateLocked` (slotsession.go:75) and then gates the arming on `len(s.slots) != 1` (:106-110), so two ordinary interleaved binds miss the arming. Re-derived directly at the `f2` firing. It is already a row under `## Defects in the shipped tree that this proposal does not stage`, so the impacts row points at it rather than restating it. Do not read the §1.2 discharge as covering the arming.
- **0080 §1.14 is proposal 0076's, so the impacts row owes it no assessment.** 0080 assigns §1.14 ("whether the adapter's hold state is partitioned per slot") to 0076 at proposals/0080_...md:196, and its subject is the §10.1 COORDINATOR hold rather than this proposal's reclaim hold, which shares only the English word. Closes the standing Open that read the omission as possibly accidental.
- **The consolidated 0080 row's claim-register sub-assessment covers §1.12 AND §1.18.** §1.18 quotes both the denominator and the `WIRED` count ("Of its seventy-six rows, thirty-two are `WIRED`", proposals/0080_...md:307-308), which the row's earlier text left unstated, and SCHEMA-1's two `WIRED` rows move both to seventy-eight and thirty-four. `ShutdownRequest.coordination_generation` sits in §1.18's `UNWIRED` R16 list and is a DIFFERENT field from `expected_bind_epoch`, so the two new rows reclassify no existing `UNWIRED` row.
- **The whole-staging citation sweep was re-run against the AMENDMENT text specifically and came back clean.** `[non-spec-recheck-3.3.review-citations.1]` re-opened every amendment-era citation in the non-spec staging and the summary — CODE-1, CODE-2, CODE-4, CODE-6, SCHEMA-1, DOCS-1/DOCS-2, the Testing section, the files-touched credit sets and the cross-proposal references — and every one resolved. This is the second independent confirmation of the pass-12 "sweep is COMPLETE" entry and it closes the residual worry that the hand amendment's own citations were unswept. Do not re-run it.
- **The three spec-lane sites carrying the refuted two-ground argument were FIXED.** `[non-spec.2.fix-reclaim-hold-callers]` edited `spec-changes.md` at all three: SPEC-1's staged §7.1 replacement block, the Design section's parallel sentence, and the accepted-failure-mode bullet. The staged §7.1 paragraph now says §5.2 keeps its retries off a pod whose reclaim did not complete and that a hold no reclaim opened is outside that constraint; the bullet names the three callers that reach the hold and prices the cost at one attempt out of a budget whose default is one retry. SPEC-2's `**Max retries:**` text was deliberately NOT changed, because the constraint it states is correct and only the three claims about its reach were wrong. UNVERIFIED: the standing Deferred said these three were spec-lane and unapplied; the two records disagree with no correction between them, and the newer one is kept. Whoever next opens `spec-changes.md` should confirm the three sites carry the scoped form before re-applying anything.
- **Four consecutive summary-cleanup firings re-verified the same structure and moved nothing.** `f1`, `f2`, `f6` and `f7` each re-read the eight required sections in order with `## Deliverable index` last, confirmed `**Fixed decisions.**` and `**What is fixed.**` are gone, confirmed no `### Retired` block exists inside `## Open decisions for human to make`, and confirmed the section carries 11, 16, 17 and 18 with identifiers verbatim. Entry 17 is the only one carrying a recommendation; 16 and 18 record explicitly that the loop derived none; 11 states none. A fifth structural firing over unmoved text is waste.

- **DESIGN GENERATION: the bind epoch is withdrawn and the mechanism is a gateway-minted per-attempt BIND ATTEMPT TOKEN.** The 2026-09-16 hand revision (commit e53e4800a) replaced it wholesale. The gateway mints one token per bind attempt from a cryptographically secure source (16 bytes hex in the code lane), the adapter stamps it on the entry it creates, compares it for equality and never mints one of its own, and no response reports a token. Every entry below that argues in epoch vocabulary is a record of the withdrawn design.
- **The token-carrying request set is five, and it is closed.** `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup`, `AssignCredentials` and `Resume` carry `bind_attempt`; `PrepareWorkspace` and `FinalizeWorkspace` are the two that also carry `mid_session`. `StartSession` and `ConfigureWorkspace` carry NEITHER field and are governed by the started-session rule alone. Every other RPC resolves an entry the session already holds, creates none, and is outside the admission rules.
- **`mid_session` already ships on `FinalizeWorkspaceRequest` (field 4) and does NOT exist on `PrepareWorkspaceRequest`,** whose field 4 is `reserved "slot_id"`. SCHEMA-1 adds the marker to `PrepareWorkspace` only, which is why §15.4's wire sentence names one message while §4.7.1 names two. More than a dozen shards have read this backwards off a single grep hit. EVIDENCE: schemas/lenny-adapter.proto:713-724, :682-696.
- **The SCHEMA-1 field numbers under the token design, verified free by many independent lenses.** `PrepareWorkspaceRequest` `bind_attempt`=5, `mid_session`=6 (1-3 used, 4 reserved); `FinalizeWorkspaceRequest` `bind_attempt`=6 (1-4 used, 5 reserved); `RunSetupRequest` 5 (4 reserved); `AssignCredentialsRequest` 4 (3 reserved); `ResumeRequest` 16 (6 and 15 reserved); `ShutdownRequest` `bind_attempt`=7 and `unconditional_teardown`=8 (1,2,3,5,6 used, 4 reserved); `ShutdownResponse` `slot_reclaim`=3. Do not re-derive these, and do not carry the retired nine-number epoch table forward.
- **`ShutdownResponse.slot_was_started` was minted and then DELETED from the whole staging,** with the `lenny_slot_compensation_removed_started_total` row that was its only consumer. `ShutdownResponse` gains `slot_reclaim` at field 3 and nothing else.
- **Two adapter `ErrorCode` values are minted, 28 and 29, and they take NO §15.1 row.** `SLOT_BIND_ATTEMPT_SUPERSEDED` is answered on `ABORTED` with `CATEGORY_TRANSIENT`; `SLOT_BIND_ALREADY_STARTED` on `FAILED_PRECONDITION` with `CATEGORY_PERMANENT`. The precedent is `ERROR_CODE_PROTOCOL_VERSION_INCOMPATIBLE = 27` (schemas/lenny-adapter.proto:585), which is published through §15.4.2 alone (spec/15:1699) and has no §15.1 catalog row. `spec/` carries no adapter-ErrorCode catalog anywhere.
- **§15.1's catalog is the CLIENT-facing `code` table with an HTTP Status column** (spec/15:1002,:1006). A row there asserts the code reaches a client. The gateway renders no adapter `ErrorCode` into a REST envelope on any staged path, so a row for either new code would be a category error. That is the ground on which the staged §15.1 sub-block and the first DOCS-3 were deleted.
- **The `Shutdown` selector is two fields and the request must ask for exactly one.** A request carrying a `bind_attempt`, and a request carrying `unconditional_teardown`, are the two legal forms; a request carrying neither, and a request carrying both, is answered `INVALID_ARGUMENT`. Every non-bind caller (the §11.4 revoke fan-out, the occupancy-zero recycle edge, `Binder.ReleaseSlot`, `Binder.shutdownAdapter`) must set the flag, which is a code-lane obligation worth a test.
- **The admission cascade has a published ORDER, and it is: the §5.2 reclaim hold, then field well-formedness, then the rules for a request that resolves no entry, then the attempt-identity rule, then the started-session rule.** The hold and the well-formedness check both PRECEDE the cascade rather than sit inside it. The staged code evaluates in exactly that order, and §15.4 says explicitly that its clause set is not an evaluation order and points at §4.7.1 for the one that is.
- **A request meeting both the differing-token condition and the started-entry condition is refused `SLOT_BIND_ATTEMPT_SUPERSEDED`,** so a stale bind identity stays transient rather than becoming the permanent `SLOT_BIND_ALREADY_STARTED`. The overlap is reachable and the two codes carry opposite client retryability, so the tie-break is load-bearing.
- **Field well-formedness is `validateBindFields(bindAttempt string, midSession bool) error` in `pkg/adapter/slot.go`,** returning `codes.InvalidArgument` when `(bindAttempt == "") != midSession`, unwrapped, called at the entry of the five handlers whose messages carry the fields. It does NOT route through `slotResolveError`, which would re-present it as a slot-resolve failure and hand `SlotBindError.Reason()` a workspace-validation reason.
- **`StartSession` and `ConfigureWorkspace` both CREATE the entry when none exists,** through `claimSessionSlot` → `claimSessionSlotUnderLock` → `ensureSlotStateLocked`, so either can mint an UNTOKENED entry. That entry is answered `superseded` by a named reclaim (fail-closed, because it may be a live successor's), is released only by the unconditional teardown or by the pod's retirement, and refuses every later bind attempt at that session on that pod. It is a recorded accepted residue with recovery routed to position 2 of the remediation plan. EVIDENCE: pkg/adapter/session.go:111; sdkwarm.go:217; slotsession.go:75.
- **`FinalizeWorkspace` is UNCONDITIONAL on both bind paths and precedes the start,** so on any correct ordering the entry is always created by a token-carrying RPC and the untokened entry needs an abandoned or late start to occur. `PrepareWorkspace` is the conditional one (uploads only). EVIDENCE: podsession/binder.go:921; slotbinder.go:291.
- **`SLOT_BIND_ALREADY_STARTED` on `RunSetup` is reachable, by the interleaving §4.7.1 itself describes.** Attempt B's `PrepareWorkspace` creates the entry under B's token; abandoned attempt A's late `StartSession` carries no token, is admitted because the entry has not started, and starts the session on B's entry; B's `RunSetup` then matches on token and falls to the phase gate. Do not dismiss the setup-window envelope mapping as unreachable.
- **`writeSetupCommandError` branches on the gRPC code ALONE.** `status.Code(cause) == codes.FailedPrecondition` gives 422 `SETUP_COMMAND_FAILED` with `details.reason: "setup_command_failed"` and no `Retry-After`; every other code, `Aborted` included, gives the retryable 503 fallback with `Retry-After`. Every `RunSetup` error is wrapped in `*SetupCommandFailure` on both bind paths, and `writePodClaimError` checks the typed setup case first. EVIDENCE: pkg/gateway/sessionserver/start.go:186-194,:217-249; podsession/binder.go:933-943; slotbinder.go:299-305.
- **DECISION: §15.1's `SETUP_COMMAND_FAILED` row is WIDENED rather than the gateway's envelope selection narrowed,** because narrowing the code is outside this proposal. §15.1's exclusion sentence ("every gRPC code other than `FailedPrecondition` ... is not this code") is keyed on the CODE and survives untouched. spec/06:290's `**Client visibility:**` clause is quantified over "any other setup-window failure" and IS falsified, so it is re-keyed on the gRPC code under a new SPEC-5 anchor. spec/07:208 is a one-way cause-to-code enumeration inside a parenthetical and stays true, merely incomplete.
- **DECISION: DOCS-3 is redefined.** It was deleted once (as the two published error-catalog rows) and re-minted by `spec.6.fix-G2` as the `docs/reference/error-catalog.md:129` `SETUP_COMMAND_FAILED` row edit, stating both `FAILED_PRECONDITION` causes and qualifying the setup-output remedy, in that page's own voice with no spec section numbers. It is named in checklist S7 and is defined NOWHERE in `non-spec-changes.md`; the docs loop owes the section.
- **DECISION: the post-start entry confirmation is a PUBLISHED obligation.** It is stated once in the SPEC-5 §4.7.1 block ("**A start confirms the entry is still its own.**") and mirrored as one §15.4 non-conformance clause, (j), enforced at tier 3 and tier 10. It is stated UNIVERSALLY for every request that starts a session, so CODE-2's SDK-warm site (sdkwarm.go:261) is cascaded to refuse through its own `releaseSessionSlot` plus the §6.1 `DemoteSDK` idiom rather than discarding the bool. This CLOSES the standing Open on whether a third-party adapter omitting the confirmation conforms: it does not.
- **DECISION: the `ExcludePod(s)` placement mechanism is WITHDRAWN WHOLE, and no placement rule survives anywhere in the staging.** `grep ExcludePod` over the staging returns nothing outside the review logs. What governs a retry meeting the reclaiming pod is the per-attempt token compare (a surviving entry) or the reclaim hold (a running cleanup), and both refusals are transient. §5.2's `**Max retries:**` bullet is left exactly as it stands.
- **§5.2's retry placement prefers the SAME pod.** "The retry is always assigned to a **new slot** on the same pod (if a slot is available) or on a different pod from the pool (if the original pod is fully saturated or unhealthy)" (spec/05:555). `applySlotRetryPolicy` re-submits the identical `req` with no exclusion and `maxSlotRetries == 1`, so a same-pod refusal is the request's last attempt.
- **`WARM_POOL_EXHAUSTED` is the claim-exhaustion code and is NOT §5.2's retry-budget exhaustion.** It is reached only through the `ErrNoConcurrentSlot`/`ErrNoIdlePod`/`ErrTenantMismatch` sentinels the retry loop returns unchanged; the budget exhaustion returns the structured `error.category` error of the `**Client error on exhaustion:**` bullet. Do not write `WARM_POOL_EXHAUSTED` into any retry-budget narrative.
- **DECISION: §7.1's reclaim obligation is scoped by whether the pod survives the failure, rather than by a list of call sites.** It binds the §15.1 start transition onto a slot on a pod serving concurrent sessions (reserved at creation or placed by §5.2's retry policy) and the §7.3 re-attach onto a replacement pod. `Binder.Prepare` and `Binder.Launch` drain the pod through `failPhase` and send no compensation at all, so the exclusive start discharges the obligation by retirement.
- **DECISION: SPEC-2's §7.1 atomicity-parenthetical edit is DELETED and the shipped parenthetical stands.** Session creation leaves no pod-side state to reclaim on either pool class: on an exclusive pool `Binder.Prepare`'s failure closure drains the pod, and on a concurrent pool `prepareAtFinalize` returns before any adapter RPC. SPEC-2's §7.1 block is now one inserted paragraph and no in-place edit.
- **The finalize gate is one line and it is the warrant for "no adapter RPC runs at finalize on a concurrent pool".** `finalize.go` returns `(nil, nil)` before `s.podBinder.Prepare` when `match.ExecutionMode == service || match.MaxConcurrentSessions > 1`. §7.1's own numbered flow reads as if steps 11-13 do pod I/O at finalize, and §15.1:646-647 carries the carve-out ("On a concurrent-workspace pool whose reserved slot materializes and runs setup at start"). Several lenses have lost a pass trying to falsify this.
- **`Binder.Prepare` runs at `/finalize` and `Binder.Launch` at `/start`, two independent HTTP requests, with nothing carrying in-process state between them.** `Launch` re-dials and issues only `ConfigureWorkspace` or `StartSession`. That is why neither can hold `Prepare`'s token, and it is the load-bearing reason the two starts carry none.
- **DECISION: the reclaim hold's bound is stated by chaining through §5.2's own `**Slot cleanup:**` bullet.** The two sentences arguing that the bullet's timeout is "not a second bound" and that the directory removal runs "under no deadline" are deleted. The hold lasts as long as the cleanup; on a pod serving concurrent sessions a cleanup that overruns the bullet's timeout is a failed cleanup, so the slot is `leaked` and its identifier stays held until the pod terminates. Ending the hold at the timeout was rejected as actively unsafe: releasing the identifier while `os.RemoveAll` runs readmits a successor onto a tree being deleted. CORRECTED in round 14: the identifier-hold half of this decision is RETIRED. SPEC-3's staged §5.2 text no longer carries the "so the slot is `leaked` and its identifier stays held until the pod terminates" clause; the hold's bound is the cleanup's return on every arm, success or failure, and the disposition of a cleanup that does not complete is the scrub-model paragraph's `leaked` clause alone, which §7.1 reads off the response's clean-exit answer. The identifier hold and the `leaked` occupancy are separate objects. The rejection of ending the hold at the timeout stands as written.
- **DECISION: §15.4's unconditional-teardown non-conformance clause is scoped to an adapter that holds an entry for the named session,** because the unscoped form declared the shipped occupancy-zero recycle `Shutdown` and the §11.4 revoke fan-out non-conforming. The no-entry case is answered `absent` by §4.7.1 and by the §4.7 row.
- **`superseded` covers TWO adapter states, not one:** an entry a different bind attempt created, and an entry carrying no token, the latter so the rule fails closed. Any definition of `superseded` as "a different bind attempt owns the identifier" is too narrow, which is why the §16.1 row now states the outcome by pointer to §4.7.1.
- **SPEC-6 stages three §16.1 rows and their content is settled.** `lenny_slot_compensation_superseded_total` (gateway, `pool` and `k8s_pod_name`, stating no expected value and no expected rate); `lenny_slot_compensation_outcome_unrecognized_total` (gateway, UNLABELLED, because its emitter is the free function `slotReclaimLeaked`, which holds neither the pool nor the pod name); `lenny_slot_shutdown_untokened_entry_total` (adapter-side, `k8s_pod_name`, with the not-scraped deferral).
- **Every metric row's expected-VALUE clause on this proposal has been wrong three times running.** "Zero on a conforming deployment", then "non-zero on a system that retries and is the count of retries the fence protected", then the same claim one step smaller. The convention §16.1 supports is to state what a series counts and stop; `lenny_slot_failure_total` and `lenny_warmpool_claims_total` carry no expectation at all.
- **The adapter DOES hold its own pod identity,** `os.Getenv("POD_NAME")` from the Downward API, cached as `s.podID` at construction, with a fail-closed withhold when it is unset. So an adapter-emitted series labelled `k8s_pod_name` is emittable. The `RecycleScrub` proto comment "The adapter, which holds no pod identity of its own" is about the claim-row key on the wire. At least eight lenses have had to settle this. EVIDENCE: pkg/adapter/server.go:36,:372-380; sessionscrubreporter.go:56-74.
- **The metric-catalog gates are one-directional and none is tripped by adding §16.1 rows alone.** `TestMetricCatalogIsCompleteAgainstSpec161` and `TestMetricCatalogHasNoUnspecifiedMetrics` compare `MetricCatalog()` against the HAND-MAINTAINED `spec161Metrics` slice in `catalog_test.go` rather than against spec/16; the alert cross-check runs alert-to-catalog; tier-11's `adapter_metric_catalog_test.go` runs registered-adapter-metric to both catalogs with an empty `specCatalogPending`. A §16.1 row with no registered metric fails nothing.
- **The gateway metric catalog is `pkg/observability/metrics/catalog.go` plus its `spec161Metrics` list, and an ADAPTER counter must be registered in `pkg/adapter/metrics.go` instead.** `pkg/gateway/observability/catalog.go`, which CODE-9 names twice, does not exist. An adapter counter registered gateway-side silently escapes the tier-11 adapter sweep.
- **§16.1 already carries adapter-process rows with a fixed house phrasing,** "emitted by the adapter process inside the agent pod and therefore outside the [§16.9](#169-prometheus-scrape-targets-and-crds) default scrape target set until a deployer wires an adapter scrape target" (spec/16:186-189). SPEC-6's third row paraphrases it without the §16.9 link. That is wording; at least six lenses have checked it and none filed it.
- **The adapter genuinely exposes no scrape endpoint.** `pkg/adapter/metrics.go` registers into `prometheus.DefaultRegisterer` and no `promhttp` handler exists under `pkg/adapter/` or `cmd/lenny-adapter/`; §16.9's canonical target set is gateway, controller, token service and `lenny-ops`. So SPEC-6's deferral claim is true, and agent-pod ingress is `allow-gateway-ingress` only, so nothing in §13.2 contradicts it.
- **`k8s_pod_name` is sanctioned and raises no cardinality question.** §16.1.1 is the single-source-of-truth table for attribute NAMES and carries it; `lenny_slot_failure_total` and `lenny_slot_pod_replacement_total` already pair it with `pool`; the forbidden list is `session_id`, `operation_id`, `root_session_id`, `credential_id`, `user_id`, `lease_id`, `request_id`. Its "Used on" column is descriptive and no gate parses it.
- **Both gateway leak ledgers share ONE `slothealth.Tracker` instance.** `cmd/lenny-gateway/sessiondeps.go:261` mints it and the same pointer reaches both `sessionsrv.go`'s `SlotHealth` (the `applySlotRetryPolicy`/`accountSlotFailure` path) and `newScrubReportService(..., Tracker: ...)` (the `drainLedger.RecordLeak` path). A finding shaped "the withheld report lands in a different ledger than §6.2's trigger evaluates" dies here. What genuinely differs is the GAUGE, which only the bind-failure path publishes.
- **The adapter today refuses a double start with `codes.Unavailable`, not `FailedPrecondition`,** so the staged `SLOT_BIND_ALREADY_STARTED` / `FAILED_PRECONDITION` / `CATEGORY_PERMANENT` answer is a deliberate retryability CHANGE on that path rather than a restatement of shipped behaviour. EVIDENCE: pkg/adapter/slotsession.go:78-85.
- **`ConfigureWorkspace` hard-codes `idempotentRepeat = true`,** so `allowStarted` is always true there, and §4.7.1's "unless it is a repeat `ConfigureWorkspace` for the session that started on that pod" is equivalent, because the entry is keyed by session identifier. spec/04:673 publishes the idempotency and is untouched. Do not file this as predicate drift.
- **The mid-session credential refresh path is `RotateCredentials`, not `AssignCredentials`,** so the started-session rule cannot reach proactive lease renewal (spec/04:1456 step 3). The checkpoint-restart rotation path (`Checkpoint` → replacement pod → `AssignCredentials` → `Resume`) is a fresh bind attempt on a new pod and is inside the token contract rather than against it. This was the strongest unstaged-site candidate and it died here.
- **`DemoteSDK` is called at exactly one site and it runs BEFORE any entry-creating RPC of the attempt,** at the top of `Binder.Prepare` (binder.go:882), ahead of `stageWorkspace` and `FinalizeWorkspace`, and it releases the registry entry synchronously inside its own RPC. So there is no post-demotion untokened-entry route and no post-demotion hold. A `ConfigureWorkspace` failure reclaims and returns; it does not demote and retry.
- **The §4.7 RPC tables physically sit INSIDE §4.7.1** (heading spec/04:659, `Shutdown` row :686, Adapter→Gateway table opening :688, `#### 4.7.2` at :695), and citing that row as "§4.7" is the shipped repo convention. A reviewer who reads "§4.7" and "§4.7.1" as different sections files a false collision.
- **The adapter `Shutdown` caller set in the tree is FOUR functions across five sites,** and the §4.7 commentary names three. `Binder.ReleaseSlot` (slotbinder.go:542) and its recycle arm (:574), `Binder.shutdownAdapter`'s retire arm (binder.go:2043) and its recycle arm (:2037), and `cmd/lenny-gateway/user_revocation.go:129`. The non-spec lane carries the full table, so nothing is orphaned; a fixer editing that commentary should add `Binder.shutdownAdapter`.
- **§28.4's claim register is the FILE `tests/claim-map.json`, seeded from `scripts/seed-claim-register.py`, rather than a table inside spec/28.** So the §15.4 commentary's promised `ABSENT` row obliges no spec/28 edit and does not contradict the "§28's registers deliberately untouched" bullet. At least six lenses have nearly filed the pair.
- **The anchor sweep under the token design is done and clean.** Every "text to replace" fenced block in `spec-changes.md` resolves exactly once across `spec/` and every "replace it with" block zero times, re-derived mechanically over all 28 fenced blocks by more than ten independent lenses. The §4.7.1 insertion window (spec/04:688-695), the §15.4 window (spec/15:1469-1471) and the §6.2 fence window (spec/06:150-156) are each unique.
- **The checklist is now TWENTY-TWO steps under the token design,** and every entry below that names S1-S14 with epoch-era contents is reading the superseded sequence. Known live step identities from this window: S1 SPEC-5, S3 SPEC-2's §7.1 paragraph, S6 SPEC-6, S7 DOCS-1/DOCS-2/DOCS-3, S9 SCHEMA-1, S13 CODE-6, S15 CODE-1, S17 CODE-2, S20 CODE-9, S22 CONF-1 plus the claim-register row.

- **ONE open decision remains, and it is entry 20.** The `f1.cleanup` firing deleted the
`**Resolved by this revision.**` block after checking its four entries: 11 (§7.1's trigger stays
"fails") and 16 (no upper clamp on the exclusive-pool resume budget) are answered by the staged §7.1
and §5.2 text, which carries the trigger and the `max(cleanupTimeoutSeconds / maxConcurrentSessions,
5)` formula unchanged; 17 (the shared-entry residue) is answered by the per-attempt token and its
first-writer-wins stamp in §4.7.1; 18 (the refusal's client-visible outcome) is answered by the
`ABORTED` classification of `SLOT_BIND_ATTEMPT_SUPERSEDED`. Entry 19 was deleted as not an open
decision; its finding, that `handleUploadToSession` refuses a mid-session upload unless
`s.podRegistry.Get(row.ID)` returns a binding with a non-nil `Adapter`, is what the staged §4.7.1
mid-session rules rest on and is pinned by a tier-4 case. Entry 20 (whether the two new `ErrorCode`
values take 28 and 29 or the Phase-2 range) keeps its identifier and text. Any entry saying the
section carries 11, 16, 17 and 18 is reading a superseded pass.
- **The summary's structure after the `f1.cleanup` firing.** Eight listed sections in order, `##
Summary` holding only its four labelled parts, `## Deliverable index` last with nineteen deliverable
lines, `**Accepted failure modes.**` moved verbatim to the end of `## Non-goals`, `## Revision
history` removed, and `## Defects in the shipped tree that this proposal does not stage` at
twenty-one entries. `## Goals`, that defects section and `## Impacts on other proposals` were not
edited, and every "recorded under the accepted failure modes" pointer still resolves because no
sentence in the moved block was reworded.
- **The 0078 relationship is THREE file collisions, all in test files.**
`tests/tier7a_load_local/shutdown_drain_gate_race_test.go` is in this proposal's files-touched list
and 0078 lists the same file against TEST-7 (0078:785). The four `adapterv1.ShutdownRequest`
literals at :269, :332, :373 and :453, inside `TestConcurrentShutdownsSendOneDrainSignal_spec_6_4`
(:232) and `TestShutdownDrainRacesAnIncomingSession_spec_6_4` (:316), each go red under the
two-field `Shutdown` selector unless edited, because those cases build the request against the
adapter server directly rather than through `Client.Shutdown`. The impacts row now says so and tells
TEST-7 to preserve `unconditional_teardown`; its earlier "package co-location rather than a file
collision" clause is retired.
- **DECISION: the pre-`running` cleanup trigger names its ACTORS rather than quantifying over bind
failures.** The §5.2 append, the §6.2 prose and the fence annotation now name the §7.1 pod-side
reclaim where its obligation reaches the attempt, and the adapter's own handler for a start that
fails, and state the disposition of the case neither reaches (a pre-attached bind on a pod serving
one session, whose registry entry and per-slot tree are reclaimed with the pod and which reports
nothing). ALTERNATIVES rejected: widening §7.1 to cover the pre-attached one-session bind (obliges a
`Shutdown` to a pod the pre-attached disposition already retires, and puts an adapter RPC on
`failPhase`, which issues none); giving the adapter a cleanup handler on the four
workspace/setup/credential RPCs (a new mechanism, and it collides with first-writer-wins entry
ownership); deleting the trigger sentence (leaves the report-withholding and one-report rules with
no named window).
- **`Binder.failPhase` is `releaseCredentials` plus `drain` and issues no adapter RPC**
(podsession/binder.go:1072-1082), so a pre-attached bind failing at `PrepareWorkspace`,
`FinalizeWorkspace`, `RunSetup` or `AssignCredentials` on a pod serving one session initiates no
per-slot cleanup on the pod at all. The only production sites that run a per-slot cleanup outside
the `Shutdown` handler are start paths: session.go:133,:147,:157; resume.go:69-141;
sdkwarm.go:236-298; holdstate.go:254. No handler for the four workspace/setup/credential RPCs
releases the slot.
- **DOCS-3 is `docs/reference/error-catalog.md:129`, four in-place replacements, and it adds NO
row.** It was deleted in rounds 4 and 5, re-staged by hand after the round-6 stop
(non-spec-changes.md:1838-1877), and it rewrites the `SETUP_COMMAND_FAILED` row's description
opening, retryability sentence and `details.reason` sentence and appends one clause to the remedy
cell, mirroring SPEC-5's widening of the §15.1 row in that page's own voice with no spec section
numbers. The two adapter `ErrorCode` values never appear on that client-facing page.
- **The four docs mirrors this proposal stages, each with the spec edit it follows.**
`docs/reference/metrics.md` ← §16.1 (CODE-9, non-spec-changes.md:1497);
`docs/reference/state-machines.md` ← §6.2's per-slot edge (DOCS-1, :1782);
`docs/reference/adapter-contract.md` ← the §4.7 `Shutdown` row plus the bind-attempt block and the
`DemoteSDK` row (DOCS-2, :1796); `docs/reference/error-catalog.md` ← the widened §15.1 row (DOCS-3,
:1838). The two spec-lane sentences that said otherwise (spec-changes.md:323-327 "this proposal's
docs deliverables do not reach it" and :932 "One file outside `spec/` moves with them") were
corrected in place; `grep -n "docs/reference\|outside \`spec/\`\|mirror"` over that file returns
only :323, :324, :327 and :932, so the fix was fully contained.
- **`SetupCommandFailure` is constructed at exactly two sites and both are the `RunSetup` call**
(podsession/slotbinder.go:304, binder.go:937), and `writeSetupCommandError` is the only branch onto
`SETUP_COMMAND_FAILED`, keyed on `status.Code(setupFail.Cause) == codes.FailedPrecondition`
(start.go:237-241). So "the setup window" in SPEC-5's §15.1 widening means the `RunSetup` RPC alone,
and a `FailedPrecondition` at the workspace or credential stage never reaches that envelope.
- **The anchor sweep was re-run under the token design and is clean; do not re-run it by hand.**
Every "reads, verbatim" fenced block in `spec-changes.md` occurs exactly once across `spec/*.md` and
every replacement block zero times (blocks at :337, :378, :472, :495, :507, :536, :555, :579, :599,
:620, :804, :816, :828, :851), and a python sweep of every `](...)` link against `spec/*.md`
headings returns zero bad anchors. The fourteen verbatim anchors, each unique in its own file:
spec/04:157, :686, :659-693, :854; spec/05:453, :545; spec/06:152, :157, :158, :234, :290;
spec/07:23, :210, :213, :214, :414; spec/15:1136, :1469, :1471; spec/16:14-15; spec/29:703-710,
:586-591, :697.
- **"started" versus "running" is NOT predicate drift, and the code settles it.** `st.started` is
set inside `claimSessionSlotUnderLock` at claim time, before `Runtime.Start` runs
(slotsession.go:78-87), so the §4.7 row's runtime-teardown precondition (taken at the moment of
admission) and §4.7.1's started-session rule key on the same bit, while §6.2's `running` sub-state
keys on the later runtime-given boundary. The two boundaries are deliberately different and the
staging is consistent on both.
- **The graceful-shutdown-signal gate is shipped verbatim,** `if !boundRemains {
s.drainViaLifecycle(...) }` with the same rationale clause (pkg/adapter/session.go:252-260), so the
§4.7 row's and §29.4 step 13's statement of it must not be filed as new behaviour.
- **The spec staging is swept clean of the withdrawn placement mechanism.** Greps for "different
pod", "WARM_POOL_EXHAUSTED", "placed by neither", "neither mechanism" and "whatever state the failed
stage" over `spec-changes.md` all return nothing, which APPLIES and retires the four standing
Deferred entries against that file. When sweeping for withdrawn-placement residue grep for those
phrases, never for `ExcludePod`.
- **RESOLVED: a pod serving one session has TWO exits, and SPEC-4's §6.2 paragraph is the single
home that states both.** The retiring exit is the one every failed bind takes: the release reaches
occupancy zero, the per-pod claim is deleted, and the occupancy projection drains a `claimed` pod
whose claim is gone rather than returning it to inventory. The return-to-pool exit is `reserved →
idle`, reached through the recycling binding state once the adapter's whole-pod scrub has been
reported, on either production path that writes `reserved` and on a failed scrub as well as a
successful one, because the default `onScrubFailure: warn` returns a failed-scrub pod to the pool
with a `scrub_warning` annotation. The scrub removes every per-slot workspace tree and credential
file the pod still holds and verifies their absence from disk, independently of the adapter's
registry; the registry entry and the armed lease-expiry timers are not reached by it. §7.1's staged
paragraph, SPEC-3's §5.2 append and the accepted-failure bullet each cite that paragraph and restate
nothing, and the `slot_cleanup → released` sole-exit half is answered by it too: on the pod class
that offers no `leaked` sub-state, a cleanup that runs completes and releases, and a bind neither
actor reclaims drives no edge at all. EVIDENCE: pkg/controller/warmpool/occupancy.go:126-140;
podsession/binder.go:867,:998,:1072-1082,:1200-1202; podclaim/bindingstate.go:182 with
recycle/recycleboundary.go:219-221 and recycle/scrubreporter_seams.go:886,917;
pkg/adapter/podscrub.go:132-183.
- **The threshold-retune scope call, with current citations.** The record is the Non-goals bullet at
summary.md:440-441, the problem statement's out-of-scope paragraph at problem-statement.md:141-142,
and the "Faster pod churn at `maxConcurrentSessions >= 3`" bullet at non-spec-changes.md:2501, which
CORRECTS the drifted `:1954-1963` citation. No staged deliverable touches the formula, its clamp or
the drain block (`UnhealthyThreshold` slothealth.go:215, `Tracker.Unhealthy` :136, sole production
trigger start.go:2858), and the shipped code conforms to spec/05:561 and spec/06:160, so a defects
row would have no divergence to cite and none is owed.
- **`tests/claim-map.json`'s `coordination_generation` fence rows carry `UNWIRED`, not `ABSENT`,** so
SPEC-5's "following the rows the `coordination_generation` fence already carries" is loose as a
status precedent. The `ABSENT` status the proposal picks is independently correct under §28.4's own
definitions, so this is below the bar; recorded so it is not re-derived. EVIDENCE:
tests/claim-map.json:47-54,75-82,83-90; spec/28:163-165.
- **DECISION: the `PrepareWorkspace` per-frame predicate is asymmetric and is stated as one default-value principle.** Neither `bind_attempt` nor `mid_session` carries presence on the wire, so a later frame leaving either at its proto3 default asserts nothing and repeats the resolving frame's assertion; only a non-empty `bind_attempt` differing from the resolving frame's, or `mid_session` true where the resolving frame's was false, is a disagreement. ALTERNATIVES rejected: dropping the per-frame comparison (loses the fail-closed refusal and leaves clause (i) unpinnable); obliging the sender to restate both fields per frame (the adapter cannot tell a repeated `false` from an omitted one, and §15.4 is published to third parties); giving `mid_session` explicit presence (new wire surface, and it splits the two forms of one field).
- **DECISION: the admission rules, well-formedness included, are decided on the RESOLVING frame's values.** Stated in the same paragraph as the per-frame principle, because applying the well-formedness pair per frame refuses every later frame of a mid-session upload before the disagreement rule is reached.
- **`sendUpload` sends no per-frame marker at all.** Each `PrepareWorkspaceRequest` frame is built from `SessionId`, `UploadRef` and `Chunk` alone, chunked at 64 KiB (`prepareWorkspaceChunkSize`), and one `PrepareWorkspace` call loops over the whole uploads map, so the resolving frame is the first frame of the first upload and every frame of every later upload is a later frame. EVIDENCE: pkg/gateway/runtime/adapterclient/client.go:240,:247-265,:269-284.
- **`mid_session` is a bare proto3 bool.** `bool mid_session = 4` on `FinalizeWorkspaceRequest`; omitted and false are the same bytes. SCHEMA-1 adds the same bare form to `PrepareWorkspaceRequest`. EVIDENCE: schemas/lenny-adapter.proto:724.
- **DECISION: the pre-attached failure's pod retirement is stated once and unconditionally, by DELETING the `recycle.enabled: false` qualifier** from §6.2's occupancy-projection `claimed ──→ draining` trigger and from the matching clause in the projection prose, rather than by adding a branch. SPEC-4 therefore has three §6.2 sites; DOCS-1 gains the mirrored `docs/reference/state-machines.md:138` sentence and CODE-4 the `drain` doc-comment correction. ALTERNATIVES rejected: rewording only the proposal's three repetitions (leaves §6.2 saying two things); narrowing the carve-out to non-recycling pools and giving the recycling one-session pod its own residue class (invents a third class and a compensation no code sends); restating the disposition in §7.1 and §5.2 instead of citing §6.2 (the three-voices state SPEC-4 exists to end).
- **The occupancy projection reads the pod's CURRENT PHASE, never the recycle setting.** `case state.Claimed: return state.Draining, true` and `case state.Reserved: return Idle`, with no recycle read on the no-claim path; the comment names a non-recycling release, the one-session-only invariant and an orphan-GC reclaim as the three producers. EVIDENCE: pkg/controller/warmpool/occupancy.go:118-140.
- **The pre-attached bind failure writes NO claim disposition.** `failPhase` calls `drain`, which is a bare `podclaim.DeleteClaim`, so any clause of the form "the pre-attached failure is treated as a failed session" is a fiction the tree does not satisfy. The `failed` disposition is written only at the recycle boundary and by the scrub reporter, four sites in all. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1072-1085,:1195-1203; pkg/gateway/session/recycle/recycleboundary.go:222-228; recycle/scrubreporter_seams.go:976.
- **No test pins the `recycle.enabled: false` deletion clause,** so the correction costs one docs sentence and no test rewrite. `tests/tier11_docs` reconciles §6.2 against `state-machines.md` for the per-slot scope and the recycle transitions only.
- **SCHEMA-1's field numbers are all free.** `ShutdownRequest` tops out at `coordination_generation = 6`, so 7 and 8 are free; `ShutdownResponse` holds 1 and 2, so 3 is free; `PrepareWorkspaceRequest` has 4 reserved, so 5 and 6 are free; `FinalizeWorkspaceRequest` has 5 reserved, so 6 is free; `ErrorCode` ends at 27, so 28 and 29 are next. EVIDENCE: schemas/lenny-adapter.proto:585,:706-731,:1635.
- **`ShutdownResponse` is `{exited_cleanly = 1, exit_code = 2}` and `slot_reclaim` is NEW at field 3.** There is no `slot_was_started` field anywhere in the staging, at field 4 or elsewhere; the design computes `ExitedCleanly: closeErr == nil && (live || treeErr == nil)` instead. EVIDENCE: schemas/lenny-adapter.proto:1665-1668; non-spec-changes.md:345-351,:1692-1702.
- **The adapter holds its own pod identity.** `podspec.PodNameEnvVar = "POD_NAME"` is rendered from the Downward API `fieldRef: metadata.name` in both deployment models and cached as `s.podID` at construction, so SPEC-6's adapter-side `k8s_pod_name` label is populatable. The proto comment "the adapter, which holds no pod identity of its own" is about the WIRE-delivered identity for `RecycleScrub.pod_id`. EVIDENCE: pkg/controller/sandbox/podspec/podspec.go:175,:904-918; pkg/adapter/server.go:36,:372-380; schemas/lenny-adapter.proto:1645-1649.
- **The §16.1↔code parity gate is `pkg/observability/metrics/catalog_test.go`, and it is asymmetric by lane.** `spec161Metrics` is a HAND transcription that OMITS the three adapter-emitted §16.1 rows, and the two tests are bidirectional. So an ADAPTER §16.1 row imposes no `catalog.go` obligation, while a GATEWAY §16.1 row imposes two (catalog.go plus the hand list). EVIDENCE: pkg/observability/metrics/catalog_test.go:15-155,:186-206; pkg/observability/metrics/catalog.go:271.
- **`tests/tier11_docs/adapter_metric_catalog_test.go` holds every name registered in `pkg/adapter/metrics.go` to BOTH `docs/reference/metrics.md` AND §16.1,** with two named escape lists that are currently empty of these names. So the adapter-side untokened counter needs its §16.1 row and its metrics.md row in the same commit as its registration. EVIDENCE: tests/tier11_docs/adapter_metric_catalog_test.go:33-49,:79-116.
- **`docs/reference/metrics.md` is hand-authored, not generated** (no generation header; the tier-11 gate reads it as a plain document), so CODE-9's mirror edit is an authoring-source edit. EVIDENCE: docs/reference/metrics.md:1-11; tests/tier11_docs/adapter_metric_catalog_test.go:84.
- **`tests/tier11_docs/runtime_page_metric_names_resolve_test.go` gates `docs/reference/adapter-contract.md` on naming only series that appear in spec/16,** so a backticked `lenny_*_total` on DOCS-2's page with no §16.1 row turns tier 11 red. EVIDENCE: that file :33-38,:50-60.
- **No `charts/` or `pkg/alerting/` surface names any slot or adapter-leak series,** so the new counters owe no alert row and no runbook page. `docs/operator-guide/observability.md` carries its own curated metric tables and lists no slot metric at all, so it is not a second mirror either.
- **The adapter `ErrorCode` enum is enumerated in NO spec or docs table.** A grep over `docs/` and `spec/` for `ERROR_CODE_`/`ErrorCode` returns one line, the REST envelope's `<ErrorCode>` placeholder, so the two new values mint no table row and the "§15.1 takes no new row" scope call is cheap. `PROTOCOL_VERSION_INCOMPATIBLE`'s three prose sites are narrative rather than a catalog, and it is `ErrorCode = 27` with no §15.1 row, which is the shipped precedent. EVIDENCE: spec/15:552,:1699; schemas/lenny-adapter.proto:585.
- **The entry-creating request set is exactly seven and there is no eighth.** `PrepareWorkspace`, `FinalizeWorkspace` and `RunSetup` reach `ensureSlotPaths` (staging.go:134,:181,:337); `AssignCredentials` reaches `ensureSlotStateLocked` (slotcreds.go:26); `StartSession`, `Resume` and `ConfigureWorkspace` reach `claimSessionSlot` (session.go:111, resume.go:50, sdkwarm.go:217). So §4.7.1's "every other RPC resolves an entry the session already holds, creates none" is true of the tree, and `rotate`/`extend`/`revokeCredentialsSlot` all use the resolve-only `slotStateLocked`.
- **`pkg/gateway/podlifecycle/podsession/binder.go:1607` is the ONLY non-test caller of the adapter `Resume` RPC in the whole gateway,** so no unmodelled resume path can issue a tokenless `Resume`. `Binder.Resume` issues exactly one entry-touching pod RPC.
- **The mint sites converge on two functions.** `Binder.BindSlot` (slotbinder.go:133) and `Binder.BindReservedSlot` (:254) both tail-call `materializeSlot` (:265), so one mint at the top of `materializeSlot` covers both concurrent paths; `Binder.Prepare` (binder.go:843) is the only caller of `b.assignCredentials` and issues `RunSetup` at :933, so one mint there covers the whole exclusive sequence.
- **The §10.1 hold-timeout termination's ten-second graceful window is SHIPPED, not an invented constant,** and it is a per-pod ceiling rather than a per-member one: `onHoldTimeout` builds ONE `context.WithTimeout(context.Background(), 10*time.Second)` shared across every deregistered member, and only the last member's close consumes it. §10.1's own prose names no window, so the staged §5.2 sentence is the first spec statement of it. EVIDENCE: pkg/adapter/holdstate.go:196-205,:228-253.
- **`releaseSessionSlot` reports nothing, and `reportSessionScrub` has exactly ONE caller in the whole adapter,** the `Shutdown` handler. So SPEC-3's "a cleanup the adapter runs inside the failed start's own handler reports nothing" restates shipped behaviour rather than creating an obligation, and the candidate "SPEC-3 removes a `leaked` signal the adapter produces today" is false. EVIDENCE: pkg/adapter/slotsession.go:214-220; pkg/adapter/sessionscrubreporter.go:61; pkg/adapter/session.go:279.
- **`handleUploadToSession` refuses with `TARGET_NOT_READY` unless `s.podRegistry.Get(row.ID)` returns a live binding on THIS replica,** so the §4.7.1 safety argument for the identity-free mid-session request is discharged against the tree, and the §7.4 bearer-credential posture closes the cross-session path independently. EVIDENCE: pkg/gateway/sessionserver/upload_to_session.go:110-121; spec/07:446.
- **"External adapters" are external PROTOCOL adapters (MCP, A2A, OpenAI-completions), not runtime adapters,** despite §24.8's row naming `schemas/lenny-adapter.proto` among its assertion sources. The staged §15.4 claim that the project has no harness for a third-party runtime adapter is therefore not contradicted by §24.8. Chased to the bottom; do not re-chase. EVIDENCE: spec/24:114; spec/15:885,:1452; pkg/gateway/externalapi/admin/external_adapters_test.go:132.
- **`Shutdown` appears NOWHERE in spec/28 and §28.4's claim register lives in `tests/claim-map.json` rather than in spec text,** so "§28 takes no row" holds and the claim-register staging needs no spec/28 edit.
- **The whole-pod scrub is filesystem-only and the adapter process survives pod reuse,** so nothing in the tree ever clears `s.slots` other than `deregisterSlotLocked` (the sole `delete(s.slots, ...)`). Any reasoning about what a recycled pod carries forward starts there. EVIDENCE: pkg/adapter/slotsession.go:180; pkg/adapter/podscrub.go:39-81.
- **No §16.5 SLO is breached by the synchronous compensating `Shutdown`.** The creation-latency SLO measures receipt through the `session_id` response and excludes workspace upload; on an exclusive pool the workspace RPCs run at finalize rather than inside that window; the startup SLOs are P95 over successful starts and only a post-retry start carries a previous attempt's compensation. EVIDENCE: spec/16:626-631.
- **A rolling update CANNOT strand a tokened entry; only a SIGKILL, crash or OOM can.** CORRECTS the standing Open: preStop stage 1 only flips readiness, stage 2 waits on in-flight checkpoints, stage 3 polls `active_streams` for the remainder of `terminationGracePeriodSeconds` (chart default 240s, budgeted 90+90+30 ≤ grace), and the process is not signalled until that deadline, so an in-flight bind of a few seconds runs to completion and sends its compensation. A coordinator handoff moves coordination of an existing session and does not move an in-flight request off the replica serving it. The accepted-failure bullet's crash-only framing is right. EVIDENCE: spec/10:113,:137,:174,:175.
- **A `Shutdown` carrying neither a graceful window nor a deadline IS bounded in the tree.** CORRECTS the standing Open: `resolveShutdownGrace` prefers the ctx deadline, then the runtime-configured grace, then the package default, so a `deadline_ms == 0` request with no ctx deadline still gets a finite grace. What the staged §5.2 sentence lacks is the fallback, which is documentation completeness of a class refuted twice in this loop; filing it needs a caller that sends both empty and a harm the package default does not absorb. EVIDENCE: pkg/adapter/mcpruntime.go:308-323; schemas/lenny-adapter.proto:1609-1612.
- **The `superseded` predicate is stated at FIVE sites and only TWO were corrected** when the untokened-entry residue was accepted. Wide: spec-changes.md:463 and :894. Narrow: :400 (twice), :741, :793. A future sweep must touch all four remaining spellings in one edit or the drift reappears at whichever it misses.
- **`docs/reference/error-catalog.md:129`'s exclusion sentence is keyed on the CAUSE CLASS** ("A non-deterministic setup-window failure (a crashed pod or a transport timeout) instead surfaces as the retryable ..."), where §15.1's is keyed on the gRPC code. DOCS-3 is therefore four description-cell sentence replacements plus one remedy-cell replacement, and "four" is correct; the earlier "three plus a remedy clause" is the stale count. That page is also the ONLY reader-facing mirror of the §15.1 widening; `docs/client-guide/error-handling.md` carries no such row.
- **The five other spec/15 sentences naming `SETUP_COMMAND_FAILED` (spec/15:625,:646,:647,:650,:720) are NOT edit sites.** Each says a deterministic non-zero setup command surfaces as the non-retryable code; none claims that is the code's only cause, so SPEC-5's widening falsifies none of them.
- **`SetupCommandFailure` is produced at exactly ONE gateway site per bind path, the `RunSetup` call,** so "the setup window" in the staged §15.1/§6.2 text means `RunSetup` alone. An `AssignCredentials` or `Resume` refused `FAILED_PRECONDITION` takes its own envelope and never reaches the client as `SETUP_COMMAND_FAILED`. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:299-305; binder.go:933-942.
- **§6.2's `**Client visibility:**` bullet has NO reader-facing mirror** (`grep -rln "pre-attached" docs/` is empty), so SPEC-5's edit there opens no docs edit site. Nor does any page mirror §7.2's snapshot-close sequence or §4.7.9's step-5 sequence. The four mirrors that do exist are all staged: state-machines.md (DOCS-1), adapter-contract.md (DOCS-2), error-catalog.md (DOCS-3), metrics.md (CODE-9).
- **`ConfigureWorkspace` sets `st.started = true`** through `claimSessionSlot(sessionID, true, true)`, so an SDK-warm entry is a started entry under §4.7.1's started-session rule. §4.7 publishes it as idempotent (spec/04:673), which is what makes SPEC-5's §15.4 exemption citation sound. EVIDENCE: pkg/adapter/sdkwarm.go:217; pkg/adapter/slotsession.go:80-88.
- **§11.3's timeout table carries no row for the §11.4 revoke's ten-second SIGTERM window,** so the reclaim hold's ten-second graceful window owes no §11.3 row by precedent. EVIDENCE: spec/11:198-245.
- **The §16.1/code label divergence is PRE-EXISTING and wider than previously recorded.** `lenny_slot_pod_replacement_total` registers `[]string{"pool"}` alone where §16.1 says `pool, k8s_pod_name`, and `lenny_adapter_leaked_slots` registers `pod_id` rather than the §16.1.1 `k8s_pod_name`. SPEC-6's new rows follow the §16.1.1 convention, which is correct; do not harmonise them onto `pod_id`. EVIDENCE: pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go:212-226; spec/16:15,:296.
- **The snapshot generation rule, restated for the token revision.** `spec-r8` is byte-identical to the live proposal; the real delta is against `spec-r7` (189 diff lines, where the new §6.2 text is). `spec-r9` EXISTS but is dated 2026-09-10 and is PRE-revision epoch content, so the highest-numbered snapshot is not the newest. Diff by mtime, never by number.
- **§4.6.1 is the THIRD home of the claim-deletion rule, and SPEC-4 now edits it.** Both §4.6.1 claim-deletion bullets are re-keyed on the projected phase and the bullet's false closing sentence ("the projection returns a pod from `claimed` to `idle` only on a recycling pool") is deleted, because `ProjectOccupancyPhase` has no `claimed → idle` arm on any pool. EVIDENCE: spec/04:415-416; pkg/controller/warmpool/occupancy.go:128-140.
- **DECISION: every claim-deletion statement is keyed on the PROJECTED PHASE and carries no recycle-setting qualifier** — `claimed` drains, `reserved` returns to idle — at all five sites (the §6.2 fence trigger, both clauses of §6.2's projection prose, both §4.6.1 bullets) plus the two proposal restatements. The controller's own doc comment states the rule in those words, so the fix transcribes shipped code rather than inventing anything.
- **The false sentence sits on the `draining`, then `terminated` bullet (spec/04:417), not the `claimed` bullet (spec/04:412).** The two bullets are adjacent and both mention `claimed` and `idle`, so a lens reading the proposal's prose alone mis-attributes the deletion. Read both spec bullets before judging which one a SPEC-4 statement describes.
- **§6.2's projection prose takes THREE clause replacements, not one.** The no-claim clause gains the warm-inventory qualifier; the recycling-pod clause becomes a claim deleted while the pod projects `reserved` with its scrub and any re-warm complete; the `recycle.enabled: false` clause becomes a claim deleted while the pod projects `claimed`, on a pool of either recycle setting. After the re-key the three clauses partition and no input gets two answers.
- **"warm-inventory phase" is pre-existing §4.6.1 vocabulary at spec/04:411, not a term SPEC-4 mints.** `ProjectOccupancyPhase` actually returns `("", false)` there, so the clause is loose in exactly the way the shipped bullet is; the looseness is mirrored rather than introduced, and nothing downstream needs a definition the proposal owes.
- **DECISION: the clean-exit gap is closed by ONE clause on §4.7.1's reclaim-outcome rule.** "A response reporting `absent` or `superseded` reports a clean exit; only a response reporting `reclaimed` can report an unclean exit." §15.4 republishes it, so a conforming third-party adapter cannot answer `absent` with `exited_cleanly: false` and leak the slot. "Clean exit" / `exited_cleanly` is otherwise undefined corpus-wide, so this is the corpus's first definition of the term.
- **DECISION (open decision 23, RESOLVED): §7.1's leak predicate keeps the WIDE form,** "one whose answer does not report a clean exit, whatever outcome that answer carries". On a conforming adapter it selects the same slots as the `reclaimed`-only form and differs only by failing closed against a non-conforming one, and it is what lets CODE-4's disposition read the RPC error and the clean-exit flag alone. The standing narrow-form DECISION is SUPERSEDED; the narrow form stays the recorded fallback if the code lane ever objects, and it has not.
- **DECISION: §4.7.1's Admission enumeration is narrowed from "create or resolve" to "create",** and its closing "Every other RPC ... is outside these rules" sentence is qualified in place with the reclaim hold's wider scope as §5.2 states it. The seven named RPCs are exactly the entry-CREATING set, so "create" is exact where "create or resolve" was false of them.
- **DECISION: §15.4's closing sentence "An RPC that resolves an entry the session already holds and creates none is reached by no rule in the set." is DELETED** with nothing in its place. The block's own premise is that §4.7.1's statement is not restated there, and that sentence was the one place it broke the premise, paraphrasing an RPC-identity-scoped rule as a property a §7.4 mid-session upload satisfies.
- **DECISION: §5.2's reclaim-hold carve-out is re-keyed to "a bind whose start the adapter never admitted".** The old wording ("a start that failed before the runtime was given the session") named the case the §4.7 row explicitly says IS torn down, because the runtime teardown's precondition is the adapter's ADMISSION of the start.
- **§4.7.1 is now SIXTEEN named rules and §15.4 lists all sixteen, one for one.** pairing, reclaim hold, mid-session-create, create-and-stamp, attempt identity, started-session, admit, stamp-once, start-confirmation, first-frame, teardown-pairing, no-entry, unconditional-teardown, attempt-mismatch, attempt-match, reclaim-outcome. At least six lenses checked the correspondence name by name; a rename or an addition must sweep both blocks in one edit.
- **The admission cascade has a published ORDER: the §5.2 reclaim hold, then field well-formedness, then the no-entry rule, then the attempt-identity rule, then the started-session rule.** The hold and the well-formedness check PRECEDE the cascade rather than sitting inside it, and §15.4 says explicitly that its clause set is not an evaluation order and points at §4.7.1 for the one that is.
- **DECISION: §15.1's `SETUP_COMMAND_FAILED` second cause is narrowed to THE SETUP-COMMAND REQUEST (`RunSetup`), not "a bind-sequence request".** `*podsession.SetupCommandFailure` is constructed at exactly two sites, both the `RunSetup` error arm, and `writeSetupCommandError` is its only reader. §6.2's `**Client visibility:**` clause is re-keyed on the request AND the gRPC code; §15.1's exclusion sentence is deliberately left as it stands.
- **A `FAILED_PRECONDITION` at a non-setup stage reaches one of TWO other client envelopes.** On the concurrent bind path it is a `SlotBindError` classified `policy_rejection`, rendered as 422 `SLOT_FAILED{retryable:false}` with no `Retry-After`; on the exclusive path it is a bare wrap and takes the retryable 503 fallback. Three envelopes, not two, and no staged text asserts a destination for the non-`RunSetup` refusals.
- **SPEC-6 stages TWO §16.1 rows, not three.** `lenny_slot_compensation_outcome_unrecognized_total` was removed repo-wide, correctly, because CODE-4 no longer branches on the outcome value and has no unrecognized-outcome arm to instrument. CORRECTS the standing "three rows" entry, which cost at least four lenses a grep each.
- **The adapter-side §16.1 row IS gated, in both directions.** `tests/tier11_docs/adapter_metric_catalog_test.go` holds every name registered in `pkg/adapter/metrics.go` to BOTH §16.1 and `docs/reference/metrics.md`, with `specCatalogPending` empty, so the registration and the two catalog rows must land together. The gateway counter's rows have no gate at all, so dropping them stays green.
- **`spec161Metrics` omits four shipped §16.1 adapter rows,** so an adapter §16.1 row with no `catalog.go` or `spec161Metrics` entry follows shipped precedent and turns nothing red: both catalog tests read the hand-transcribed list rather than spec/16. EVIDENCE: pkg/observability/metrics/catalog_test.go:15-40; spec/16:186-189.
- **`pkg/gateway/externalapi/errorclassify/errorclassify.go:459-477` is the §15.2 MCP/A2A mirror of the §15.1 catalog** (`"SETUP_COMMAND_FAILED": {CategoryPermanent, false}`). It is the one client-facing parallel representation earlier "no parallel representation" sweeps never named; SPEC-5 widens the row's CAUSE only and the new cause is permanent and non-retryable, so the table stays correct and is not an edit site.
- **DECISION: the docs-mirror trailer is an OBLIGATION statement rather than a completion claim.** It reads "Each reader-facing reference page that mirrors these sections moves with the section it mirrors. The edits the non-spec deliverables must carry are these.", so a `(DOCS-1)` parenthetical names the deliverable that OWES the edit. Deleting the paragraph, appending under the old "carries" framing, re-attributing to another id and adding a deferral carve-out were all rejected.
- **`docs/reference/state-machines.md:138` is the ONLY docs mirror of the §6.2 projection sentence,** and it carries all three replaced clauses; §4.6.1's parallel bullets have no docs mirror at all. No tier-11 gate holds that page against §6.2 or §4.6.1, so nothing catches the stale page at implementation time.
- **`docs/operator-guide/troubleshooting.md:41`'s `setup_command_failed` is the `error_type` label of `lenny_warmpool_warmup_failure_total`, not the REST envelope's `details.reason`.** The row sits under a warm-pool diagnosis block whose query is `rate(lenny_warmpool_warmup_failure_total[5m]) by (reason)`. The only client-facing docs mirror of §15.1's widening is `docs/reference/error-catalog.md:129` (DOCS-3), and `docs/client-guide/error-handling.md` carries no such row.
- **`PrepareWorkspace` is the only client-streaming RPC on this contract,** so the first-frame rule needs no sibling; and `PrepareWorkspaceRequest` is NOT a §4.1 stream envelope (it declares no `oneof`), so §4.1's envelope paragraph and its tier-0 gate do not reach the two new fields.
- **`idempotentRepeat: true` has exactly ONE production caller, `ConfigureWorkspace` (sdkwarm.go:217);** `StartSession` (session.go:111) and `Resume` (resume.go:50) both pass false. That is what makes the started-session rule's single `ConfigureWorkspace` exemption exactly right and a `StartSession`/`Resume` exemption wrong.
- **The reclaim hold does not exist in the shipped tree at all.** `errSlotReclaimInProgress`, `isSlotReclaimInProgress` and the string `slot_reclaim_in_progress` appear nowhere under `pkg/`, `tests/` or `schemas/`, so CODE-6 introduces the whole mechanism. The non-spec prose calling those symbols "unchanged from the shipped text" was corrected by the reconciliation pass.
- **`ShutdownRequest` fields 7 and 8 are free (1, 2, 3, 5 and 6 live, 4 reserved) and `ShutdownResponse` gains `slot_reclaim = 3` and nothing else.** There is no `slot_was_started` field anywhere in the staging or the tree; the orchestrator brief has asserted one for at least six rounds.
- **The reconciliation pass rebuilt the checklist and the deliverable index.** The SPEC lane stands S1=SPEC-5, S2=SPEC-1, S3=SPEC-2, S4=SPEC-3, S5=SPEC-4, S6=SPEC-6; every code step's `Depends on` gained its direct spec-step ids and S6 was dropped from S7's; and `## Deliverable index` carries TWENTY lines (SPEC-1 to SPEC-6, SCHEMA-1, CODE-1 to CODE-9, CONF-1, DOCS-1 to DOCS-3). The standing "nineteen lines" entry is stale.
- **`## Open decisions for human to make` now carries entries 20, 21, 22, 26 and 27.** 23, 24 and 25 were resolved and their blocks deleted; 21 and 27 gained a recommendation, a ground, alternatives and a confidence; 20, 22 and 26 carry the question and its ground alone. Any entry saying the section holds one decision, or 11/16/17/18, is reading a superseded pass.
- **DECISION (open decision 24, RESOLVED): the pairing rule and the mid-session-create rule take NO error category and NO `ErrorCode`, and §15.4's sentence stands unnarrowed.** Both are answered on a bare `status.Errorf` with no `WithDetails`, so there is no envelope for any section to state a category on; the teardown-pairing and start-confirmation refusals are the same shape. The proto comment claiming every RPC failure carries a structured envelope is pre-existing and contradicted by every bare status the shipped adapter already returns.
- **DECISION (open decision 25, RESOLVED): the `Shutdown` row's "states which teardown it asks for" phrasing STANDS.** The same sentence already carries the entry-selection sense, §4.7.1 states the correction four sentences below its own heading, and no staged artefact reads the phrase literally. The four-site wording sweep stays available as a copy-edit against the applied spec and gates nothing.
- **`## Impacts on other proposals` carries 0080, 0073, 0075, 0078, 0072, 0079, R1b and R12, one row each.** 0072 shares SEVEN files with this proposal, four of them colliding inside one section; 0079 shares five spec files with every collision anchor-distinct. Neither loses a deliverable's subject, and four of 0072's own citations have drifted against the tree, recorded in its row rather than corrected there.
- **The claim-DELETE-before-projection race is real, reachable and PRE-EXISTING.** `ProjectOccupancyPhase`'s no-claim arm keys on the phase the projection last wrote, so a claim created, bound and deleted inside a WarmPoolController failover window reconciles as no-claim plus `Current=idle` and hits `default: return "", false`: the unscrubbed pod stays in idle inventory carrying the failed bind's registry entry, tree, credential file and armed timers, and `ClaimSlot` re-offers it with no used-pod guard. It is open decision 27 and wants its own proposal; SPEC-4 makes the phase dependency explicit rather than introducing it.
- **`o.Current` is the phase the projection last WROTE, not "the phase the pod is in".** Any spec sentence of the form "the claim is deleted while the pod projects X" is an assertion about reconcile ordering rather than about the claim.
- **A demotion cannot leave a bind meeting the hold.** `DemoteSDK` runs `releaseSessionSlot` synchronously inside its own RPC, so the pod-warm bind sequence that follows resolves no entry and takes the create-and-stamp rule. EVIDENCE: pkg/adapter/sdkwarm.go:274-302.
- **The shipped `Shutdown` already answers a clean exit on the no-entry path,** returning `ExitedCleanly: closeErr == nil` with every destructive act inside `bound := removed && st.sessionID != ""`. So the staged reclaim-outcome rule records shipped behaviour on the `absent` arm rather than imposing new behaviour there.
- **`pkg/gateway/podlifecycle/podsession/one_session_only_test.go:34` cites a `credSessionID` stickiness backstop that no longer exists** (`grep credSessionID pkg/adapter` returns nothing). It is a stale comment rather than a shipped guard, and no reading should rest on it.
- **Every bind-sequence request already carries `coordination_generation`,** so the identity-free `mid_session` form of `PrepareWorkspace`/`FinalizeWorkspace` is still replica-fenced by §10.1 even though it asserts no attempt identity. That is a second fence beside the gateway-side admission guard the staging rests it on.

- **The production adapter-`Shutdown` caller set is FIVE sites behind ONE builder.** `Client.Shutdown` (client.go:807-809) and `Client.ShutdownRecycle` (:860-867) both funnel into the unexported `Client.shutdown` (:813-824), the only production constructor of `adapterv1.ShutdownRequest` in the tree. The five callers are slotbinder.go:542, :574, binder.go:2037, :2043 and cmd/lenny-gateway/user_revocation.go:129. There is NO crash-takeover and no coordinator-handoff `Shutdown`; `pkg/gateway/coordination/` contains no `Shutdown` string, and the re-adopt sends `CoordinatorFence` alone. CODE-4 sets `unconditional_teardown` in the builder, so no caller edit is owed. Derived independently by at least eight shards; do not re-derive.
- **There are TWO recycle-carrying `Shutdown` senders and they reach DIFFERENT arms.** The concurrent path (`Binder.ReleaseSlot`) sends a plain unconditional `Shutdown` at slotbinder.go:542 and then `ShutdownRecycle` at :574, so the second finds no entry and answers `absent`. The session-mode path (`Binder.Release`'s recycle branch, binder.go:1974 → :1994 → `shutdownAdapter(..., true)` → :2037) sends the recycle request as the session's ONLY teardown, so the entry is live and the outcome is `reclaimed` on the removing arm. That path is production-live: `exclusiveBindRequest` sets `Recycle: match.Recycle` (start.go:2520,:2561), so any exclusive pool with recycling reaches it on every non-failed release.
- **`startPodScrub` is asynchronous.** It spawns a goroutine and returns (podscrub.go:40-42), so firing clause three from the removing arm blocks nothing under the slot guard or the reclaim hold. It does run CONCURRENTLY with the per-slot cleanup those two protect, which no staged text orders.
- **DECISION: CODE-1's handler has ONE exit, `answerShutdown(outcome, exitedCleanly, untokened)`,** which counts the untokened row, runs clause three (`if rc := req.GetRecycle(); rc != nil { s.startPodScrub(rc) }`) and builds the response; the shipped trailing clause three at session.go:283-291 is DELETED in the same edit, or the scrub runs twice and `ReportPodScrub` double-sends. Two returns deliberately bypass the helper because they perform nothing: the two-field precondition's `INVALID_ARGUMENT` and the shipped empty-session-id check at session.go:228-230.
- **DECISION: the per-slot guard's scope is a PREDICATE plus a derivation table, never an RPC enumeration.** A section that does path work against the slot's tree outside `s.mu` acquires that slot's guard before the resolve that hands out its paths and holds it until that path work ends; a section whose path work runs inside `s.mu` takes none. The predicate selects `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup` and `Resume` on the admission side and the three destructive sections on the other. EVIDENCE for the exemptions: `assignCredentialsSlot` writes the credential file inside its own `s.mu` section (slotcreds.go:24-44); `ConfigureWorkspace`'s only write is `writeSessionManifest` under `Server.ManifestDir` (manifest.go:280-283, server.go:111-114), which `slotlayout.RemoveTree` does not touch (tree.go:58-69).
- **`Server.Resume` is the adapter's largest UNLOCKED path write,** which is why the enumeration that omitted it was wrong. `checkpointRootsForSession` copies the roots under `s.mu` and RELEASES it (slot.go:183-207); `restoreChunks` then fetches every chunk over the network and runs `workspace.ExtractTree` with no lock (resume.go:169-208, called at :106).
- **DECISION: the guard is `map[string]chan struct{}` (capacity-one semaphores) and both hand-out forms are context-bounded.** `lockSlotGuard(ctx, slotID) (func(), bool)` and `acquireSlotGuardForResolve(ctx, slotID) (func(), error)` both select the send against `ctx.Done()`. The expiry disposition is NOT uniform: a destructive section that cannot acquire proceeds unguarded and logs `slot_guard_not_acquired` (which is today's shipped behaviour, so no regression); an admission RPC that cannot acquire is refused with `ctx.Err()`, because proceeding unguarded is how a materialization re-creates a tree the reclaim just removed.
- **DECISION: `Shutdown` compares under `s.mu` FIRST, acquires the guard only on the arm that destroys, and RE-DECIDES under `s.mu` afterwards.** Every arm that removes nothing answers from the fast path and never waits, because the guard's holders are unbounded in wall time (`workspace.MaterializeWithPolicy`/`MaterializeOverlayWithPolicy` take no ctx at all, materialize.go:157,:203; `RunSetup` runs under a `setupPolicy.timeoutSeconds` spec/14:99 permits to be absent) while the reclaim's budget is `max(cleanupTimeoutSeconds / maxConcurrentSessions, 5)`s and a timed-out reclaim is charged a permanent leak. The re-decision is sound against every remover: the only writer that can remove an entry without taking the guard is §10.1.4 pass 1, which removes under `s.mu` alone, and the re-decision catches exactly that and answers `absent`.
- **The guard STRICTLY CONTAINS the reclaim hold on a single-pass reclaim.** `defer unlockSlot()` is registered before `defer release()` and Go runs defers LIFO, so `release()` runs first. Consequence: a guarded workspace RPC racing a reclaim BLOCKS rather than being refused, and the hold's refusal survives only for the unguarded entry-creating RPCs and, on the §10.1.4 two-pass path, in the window between `deregisterStartedSessions` (hold, no guard) and `terminateHeldSession` (guard). That is why the hold is tested at BOTH `acquireSlotGuardForResolve` and `ensureSlotStateLocked`: they are one predicate at two points, not two mechanisms.
- **DECISION: §10.1.4's ten-second budget is RE-SCOPED rather than divided.** `onHoldTimeout`'s single `context.WithTimeout(context.Background(), 10*time.Second)` becomes the pass's GUARD-ACQUISITION deadline alone, and `terminateHeldSession` mints its own ten-second close context after the acquisition returns, which `emitFinalUsage` and `Runtime.Close` run on. BECAUSE the shipped pass is a serial loop over pass 1's whole member set sharing one context (holdstate.go:196-205,:237,:249), so any wait charged to it is charged to every LATER member. SPEC-3's staged §5.2 sentence already bounds "the cleanup's close" per cleanup, so this is a mis-scoping correction rather than a new rule. ALTERNATIVES rejected: a per-member slice of the shared budget (invents arithmetic and still burns member 1's own grace); deleting the guard from `terminateHeldSession` (needs an exception to the guard predicate and re-opens the `ExtractTree`-versus-`removeSlotTree` race); one per-member context covering both acquisition and close (a parked holder still burns member 1's close grace).
- **DECISION: `releaseSessionSlot` splits into a guard-acquiring form and `releaseSessionSlotUnderGuard`,** because `Resume` holds the slot's non-re-entrant guard across its whole call and its seven rollback sites (resume.go:69,:73,:89,:107,:126,:134,:141) would otherwise self-deadlock on every `Resume` failure, which no happy-path test reaches. It also gains a `context.Context` first parameter; every production caller has a handler ctx in scope. Production callers: session.go:133,:147,:157 and sdkwarm.go:236,:241,:251,:298. `Server.ReleaseSlotForTest` (export_test.go:32-35) gains the ctx too and has five callers (integrationlevel_test.go:84, credexpiry_test.go:320, checkpoint_stream_test.go:1012, tracingcontext_addressing_test.go:425, one_session_only_test.go:77).
- **`releaseSessionSlot` calls NO `Runtime.Close`.** Its whole body is `deregisterSlot` → `removeSlotTree` → `cancelPodMCPIfRuntimeIdle` (slotsession.go:214-220). Only `Shutdown` and `terminateHeldSession` (holdstate.go:251-252) close a runtime on a cleanup path.
- **`ReleaseSlotReservation` gains a `leaked bool` and that adds no mechanism.** `podclaim.SlotClaimer.ReleaseSlot` already takes a fourth `leaked` argument; the shipped `ReleaseSlotReservation` hard-codes the literal `false` for it (slotbinder.go:502) while `Binder.ReleaseSlot` passes a computed one (:559). So `Binder.ReleaseSlot` was NEVER the only producer of `leaked=true`: `BindReservedSlot` sets `sbe.Leaked` and `applySlotRetryPolicy` passes it. Any text saying the parameter does not exist makes CODE-5's staged four-argument call fail to compile.
- **`Binder.Prepare` sets `leaseAssigned = true` at binder.go:954,** two statements after its LAST `reclaim()` site (:950) and before its only success return, so every `Prepare` reclaim sees the flag false and `failPhase`'s credential-release half is unreachable from `Prepare`. `:1053` is inside `Binder.Launch`'s `BindResult` literal, a different function. `Binder.Launch` passes the literal `true` at :998, so the refusal harm is `Launch`'s alone.
- **DECISION: CODE-8 is a closure SIGNATURE change, `reclaim := func(cause error)`, plus an enumerated call-site sweep.** Both reclaim closures are `func()` with no parameters and no returns (binder.go:866-869, :997-1000), and every call site declares its failing error in its own `if`/`else if`, so a guard written into the bodies does not compile in `Prepare` and silently never fires in `Launch`. The parameter is named `cause` rather than `err` because `Prepare` reuses a function-scope `err`. Call sites: :883, :918, :923, :935, :950 in `Prepare`; :1010, :1021, :1032 in `Launch`. The two reconnect-failure early returns call `ReclaimClaimed` instead and are OUTSIDE the sweep.
- **`Binder.Launch`'s only reachable refusal is `SLOT_BIND_ALREADY_STARTED`,** because `Launch` mints no token, so the identity rule cannot fire there. That refusal means a live started session owns the entry, which is why no credential release may sit on the refusal arm: `failPhase`'s release is the session-wide `LeasesBySession` walk and would strip the live session's leases.
- **The `Shutdown` two-field precondition is a wire-visible break on every in-tree test literal, and the compiler cannot see it.** The fields are proto scalars, so a skipped sweep produces no build error and six tiers go red together. The sweep set is `grep -rn "ShutdownRequest{" --include=*_test.go pkg/ tests/` — roughly sixty call sites across roughly twenty-six files in tiers 1, 2, 3, 4, 7a, 9 and 10 — and it is stated by the reproducing grep rather than enumerated, following the precedent CODE-6 set for its own widening. The sweep belongs to the CODE-1 step that introduces the precondition, not to the CODE-4 mint step, because production callers get the field from the builder.
- **`tests/tier11_docs/alert_catalog_crosscheck_test.go` DOES NOT EXIST and never did.** The only file of that basename is `pkg/observability/metrics/alert_catalog_crosscheck_test.go`, a unit-tier `package metrics_test` file whose two tests derive every set they compare from `rules.Catalog()`, `MetricCatalog()` and `AlertSupportCatalog()` at run time. It holds NO name inventory and reads neither `docs/reference/metrics.md` nor `spec/16`, so a counter with no alert referencing it owes it nothing. This CLOSES the standing Open asking whether CODE-9 owes it an entry; the citation must be DELETED rather than repointed.
- **The metric-gate topology, settled and re-derived by six shards.** The only gate holding a metric to BOTH `docs/reference/metrics.md` and §16.1 is `tests/tier11_docs/adapter_metric_catalog_test.go`, whose source of truth is a regex over `pkg/adapter/metrics.go` alone, with `undocumentedAdapterMetrics` and `specCatalogPending` as its only (currently empty) exemptions. `spec161Metrics` in `pkg/observability/metrics/catalog_test.go` is reconciled BOTH ways against `MetricCatalog()` and never against §16.1 prose, and it deliberately OMITS every adapter row carrying the §16.9 scrape deferral. So the ADAPTER series is gated on both catalogs with no test edit and must NOT enter `spec161Metrics`; the GATEWAY series has no docs gate anywhere in the tree.
- **A new `Binder` metric hook needs THREE files and the wiring site is the one nobody names.** Registration and struct field in `gatewaymetrics_credential.go`, the `Inc*` accessor in `gatewaymetrics.go`, and `w.podBinder.<Hook> = gwMetrics.Inc<X>` in `cmd/lenny-gateway/metricsbackfill.go:149`, which is the ONLY production wiring site for `SlotFailure` and the pattern the new hook follows. A nil hook is a silent no-op, so an unwired hook emits nothing while every tier stays green.
- **`pkg/adapter/metrics.go` declares NO methods on `*Server`** — every helper there is a package-level `incX(...)` — and no adapter counter carries a pod label today. CODE-1's staged `s.noteShutdownMetUntokenedEntry(sessionID)` is a `*Server` method, which is what lets it read `s.podID`, and no deliverable declares it.
- **The per-file `tests/spec-map.json` registration census, read off the map on 2026-09-18.** `binder_test.go`: NO whole-file credit outside 4.1 via the `pkg/gateway/...` directory entry, plus 31 per-case rows. `slotbinder_test.go`: whole {4.1(dir), 4.6.3, 4.7, 5.2, 6.2, 7.1}, no per-case. `client_test.go`: whole {4.1(dir), 4.4, 4.7, 4.9, 5.2, 7.2, 7.3, 7.5, 11.2, 11.3, 11.4, 13.2, 28.5.3}, no per-case. `slotretry_test.go`: whole {4.1(dir), 11.4(dir), 15.1} plus per-case. `slotretry_load_test.go`: directory credits plus per-case. `slotsession_test.go`: whole {4.7(dir), 4.9, 5.2, 6.1, 6.4, 15.4}, no per-case. `sdkwarm_test.go`: whole {4.7.9, 6.1} only. `holdstate_test.go`: whole {5.2, 6.4, 9.1, 10.1, 10.1.4, 11.2, 15.4, 15.4.3, 28.5.3} plus 4.7 by subtree, and NO 4.7.1. `concurrent_workspace_test.go`: whole {5.2, 6.4, 15.1(dir), 28.5.3}. Key the rule on each case's own `// spec:` annotation rather than on a section list.
- **Two of the gateway tier-1 files are OUTSIDE `slotAddressCaseFiles`,** so the per-case credit gate never reaches them: `pkg/gateway/sessionserver/resume_setup_demotion_internal_test.go` and `pkg/adapter/socketruntime_test.go` (the inventory carries `socketruntime_e2e_test.go` instead). Do not stage entries for them on that gate's strength.
- **Adding ROWS to an existing test function falls due for nothing.** CODE-5's `slotretry_test.go` work extends `TestSlotBindErrorReason_spec_5_2` and `TestClassifySlotBindFailurePassesTransientThrough_spec_5_2`, both already entered per-case under 5.2 with unchanged annotations. The credit gate fires on a new FUNCTION, not on a new row.
- **The checklist is TWENTY-TWO steps over TWENTY deliverables and its graph is clean.** Every deliverable appears in at least one step, every `Depends on` names a strictly earlier step, every box is unchecked, one lane per step, the six spec steps lead and S7 (docs) precedes every code and schema step. CODE-9 moved to S10 (ahead of its callers); CODE-1 is S16/S17/S21, CODE-4 S13/S19, CODE-6 S14/S15/S21, CONF-1 S22. The splits are deliberate and are not a "deliverable named by two steps" defect. No `S<n>` reference survives outside `implementation-checklist.md`, so the renumber-residue class is closed.
- **CONF-1 is SIXTEEN named §4.7.1 rules PLUS one case titled for the evaluation order,** and the tier-3 behavioural list covers all seventeen, walked one for one. The ordering case exists because §15.4 publishes mis-ordered application as a non-conformance that is not any single rule's condition. Two framing sentences that assert a rule-per-case battery with no exception were relaxed in the same edit; a fix that lands the case and leaves them is the half-fix the next round files.
- **CONF-1's negative-case list carries its own tier statement** ("Each appears at tier 3 and again at tier 10"), so a case inserted there lands at both tiers by construction. There is no separate tier-10 case list, only the battery paragraph. The tier-3 behavioural list is a near-duplicate restatement in a different voice, so a grep hit count of two for a case title is ONE case.
- **`status.Code` walks the error chain in this tree's grpc,** re-verified against `google.golang.org/grpc@v1.80.0` `status/status.go:96-126`, so CODE-5's `codes.Aborted` arm fires through CODE-4's `*SlotBindError` and CODE-7's double-`%w`. Do not re-derive.
- **The `ensureSlotStateLocked` chokepoint holds mechanically.** `s.slots[...]` is WRITTEN at exactly one site (slot.go:124); `ensureSlotStateLocked` has exactly three callers (slot.go:143 via `ensureSlotPaths`, slotsession.go:75, slotcreds.go:26) and `ensureSlotPaths` exactly three (staging.go:134,:181,:337). `rotateCredentialsSlot` reaches neither.
- **`writeSetupCommandError` branches on `status.Code(setupFail.Cause)` ALONE, and `details.reason` discriminates nothing.** `FailedPrecondition` gives 422 `SETUP_COMMAND_FAILED` with no `Retry-After`; every other code gives the retryable 503 fallback with `Retry-After`, and BOTH carry `details.reason: setup_command_failed`. The three discriminating assertions are the status code, the body `code` and the presence of the header. EVIDENCE: start.go:87, :236-249.
- **The production stage constant for `RunSetup` is `slotFailureSetup = "setup"`** (binder.go:291), used at slotbinder.go:302-304. The shipped precedent test `TestClassifiedSlotFailureKeepsSetupCommandEnvelope_spec_7_3` builds `Stage: "session_start"` and still lands 422, because the typed `*SetupCommandFailure` handler wins over stage-derived classification; a new case should use the production label anyway.
- **SCHEMA-1's `ABSENT` claim-register row carries `"deferral_id": "R8"`.** The tier-0 validator rejects a non-`WIRED` row with an empty deferral, a malformed step id, or an id the remediation plan does not declare; `### R8. Reciprocal host-conformance battery` is declared at gateway-runtime-comms-remediation.md:980 and is the shipped precedent for a conformance-harness gap. R8's own scope text is the reciprocal direction, so the staged note says which step closes it.
- **The summary's structure after the `f4` firing.** Eight sections in the required order, `## Summary` holding only its four labelled parts, `## Non-goals` ending with the relocated `**Accepted failure modes.**` block, `## Open decisions for human to make` carrying exactly 20, 21, 22, 26 and 27 (21 and 27 the only ones with a recommendation), `## Defects in the shipped tree that this proposal does not stage` at twenty-two entries, `## Impacts on other proposals` at one row each for 0080, 0073, 0075, 0078, 0072, 0079, R1b and R12, and `## Deliverable index` last with twenty lines. Nothing was moved and no entry renumbered.
- **`Server.slotGuards` growth is bounded and the question is closed.** `SlotID == SessionID`; a recycling pod retires at `recycle.maxSessionsPerPod` (required when recycling is enabled) and a non-recycling pod is terminated when its claim is deleted at occupancy zero (slotclaimer.go:884-890), so the table is one channel per session of one pod lifetime, order-of-kilobytes. Two rounds chased it to the end. Do not re-file it as unbounded.
- **DECISION (round 13, applicability): one finding filed and the surface is otherwise clean.** The unaccounted `deregisterSlot` call sites. The whole checklist (lanes, Depends-on, order, tier lists, deliverable coverage) and the whole created-artifact worklist were re-walked and everything else sits below the bar.
- **The checklist is internally consistent on every mechanical check, and the brief's three "known wrong" items are FIXED.** All 22 boxes unchecked; one lane per step (S1-S6 spec, S7 docs, S8+ code, S9 schema); every Depends-on names an EARLIER existing step; every deliverable (SPEC-1..6, DOCS-1..3, SCHEMA-1, CODE-1..9, CONF-1) appears in at least one step; the leading spec block is unbroken. The reconciliation pass rebuilt the checklist, so S1 saying §15.1 gains rows, S17 naming two CODE-2 confirmation sites and S7 describing DOCS-3 as two new rows are all repaired. Do not re-file them. EVIDENCE: implementation-checklist.md:17, :29, :51.
- **CONF-1's rule set and its two batteries line up one-for-one.** Sixteen rules at non-spec-changes.md:1932-1939; the tier-3 battery (:2885-2958) and the tier-10 list each carry a case per rule plus the evaluation-order case, seventeen in all. The earlier "CONF-1 promises an evaluation-order case and its battery contains none" defect is CLOSED.
- **The round-13 citation sweep came back clean and need not be re-run.** Verified byte-for-byte: slotbinder.go:265/:303-304/:542/:543/:574; binder.go:291/:843/:976/:1009/:1072-1082/:1590/:1994/:2037/:2043; user_revocation.go:129; staging.go:181/:239; adapterclient/client.go:333-341; slotretry_test.go:519-548; start.go:239-249; shutdown_recycle_wire_test.go:208/:259/:264-267; seed-claim-register.py:192 (R8) and its 13-row GENERATION_FENCE block; gateway-runtime-comms-remediation.md:980; catalog_test.go:188-211; adapter_metric_catalog_test.go:26/:34/:49/:56/:100/:110; spec_map_slot_address_registration_test.go:970-988/:1173/:1191; basic_level_echo_stamp_doc_reconciliation_test.go:294; adapter-contract.md:10/:18/:53/:64/:75; state-machines.md:138 (all three replaced clauses unique); error-catalog.md:129; and every SCHEMA-1 field-number basis cell. No false citation.
- **The three call-site enumerations the proposal claims are exhaustive ARE exhaustive.** `noteRuntimeStarted` has exactly the ten callers CODE-2 names (resume.go:144, sdkwarm.go:261, session.go:163, export_test.go:45, podmcp_arming_internal_test.go:84/:185/:230, usage_test.go:233, adapterevents_test.go:95/:184); `releaseSessionSlot` has exactly the seventeen CODE-6 names (session.go:133,:147,:157; sdkwarm.go:236,:241,:251,:298; resume.go seven; podmcp:144,:195; export_test.go:34); `ReleaseSlotForTest` has exactly the five named. That precision is what makes the `deregisterSlot` omission a gap rather than a style choice.
- **DECISION (round 13, mechanism): empty findings list.** Every flow traced origin-to-effect closes. The one borderline held was DOCS-2's replacement `Shutdown` row stating both "`superseded` ... so nothing was released" and, two sentences later, the general slot-release sentence; not filed, because the staged §4.7 row carries the identical general sentence and delegates it to §4.7.1's named rules, the general-narrowed-by-specific-in-the-same-block pattern is refuted four times over, and DOCS-2 itself argues nobody implements the contract from that page (non-spec-changes.md:2289-2296).
- **The proposal's gateway line citations are all live as of round 13.** binder.go:843/:976/:1590, :866-869 and :997-1000 (the two reclaim closures), :918/:923/:935/:950 and :1010/:1021/:1032 (every `reclaim()` call site), :954 (`leaseAssigned = true`), :1072-1082 (`failPhase`), :137 (`SlotFailure`), :1994/:2037/:2043; slotbinder.go:265/:287/:294/:353-361/:542/:574; metricsbackfill.go:149; client.go:807-824 and :860-867 (the single `shutdown` builder); start.go:3648-3681 and :4041.
- **`shutdown_recycle_wire_test.go:208` is the ONLY closed field set over any message SCHEMA-1 opens.** `grep -rn "ResumeRequest{}).ProtoReflect"` and its four siblings return nothing, and `checkpoint_stream_wire_test.go`'s only count assertion is over `CheckpointStart` (:151). EVIDENCE: tests/tier3_contract/checkpoint_stream/checkpoint_stream_wire_test.go:77,:151.
- **`stageWorkspace` is the single shared sender of `PrepareWorkspace` on BOTH bind paths, so the token threads through its signature.** `Client.PrepareWorkspace` (client.go:247) takes neither `midSession` nor a token today, and `stageWorkspace` (binder.go:1283) is called from binder.go:916 AND slotbinder.go:284, sending at :1327. Neither CODE-4's Targets nor Files-touched names `stageWorkspace`, though both name the files it lives in; that silence was judged below the bar and is NOT evidence the threading is missed. EVIDENCE: non-spec-changes.md:3550-3559.
- **Every `PrepareWorkspace` frame is required to carry a non-empty session id,** so "the first frame that carries one" is always frame 1 and the first-frame latch has no unresolved-frame case. EVIDENCE: pkg/adapter/staging.go:66-82.
- **DOCS-3's absence claim holds: nothing gates `docs/reference/error-catalog.md`.** `grep -rn error-catalog tests/ scripts/ cmd/ Makefile` returns nothing, and all four quoted "currently reads" sentences plus the remedy cell are verbatim at line 129.
- **DECISION (round 13, test coverage): empty findings list, over a proposal byte-identical to its r13 snapshot.** A deliverable-by-deliverable walk of `## Testing` (non-spec-changes.md:2381-3260) found every changed behaviour pinned at the tier `.claude/rules/test-coverage.md` routes it to.
- **The tier map of the staged `## Testing` section is complete against the checklist's per-step tier digits.** Tier 1 (bindattempt_test.go, bindattempt_orderings_test.go, slotsession_test.go, sdkwarm_test.go, socketruntime_test.go, holdstate_test.go, slotstate_test.go, the four gateway files); tier 2 (binder_envtest_test.go, two arms, S12/CODE-8); tier 3 (tests/tier3_contract/adapter_bind_attempt/, descriptor gate plus behavioural battery, S22); tier 4 (concurrent_workspace_test.go three arms, recycle_scrub_path_test.go one case); tier 7a (slot_bind_attempt_race_test.go, slot_reclaim_hold_race_test.go); tier 8 (compensation_loss_test.go, S19/CODE-4); tier 9 (slot_credential_reclaim_fence_test.go, three arms); tier 10 (slot_bind_attempt_conformance_test.go); tier 11 (two shipped gates extended in place plus slot_compensation_metric_reference_test.go). EVIDENCE: non-spec-changes.md:2381-3260; implementation-checklist.md:33-59.
- **Every new adapter or gateway symbol the staging introduces has at least one named case, including the three that return zero hits by name.** Hit directly: `reclaimSlotLocked`, `acquireSlotGuardForResolve`, `lockSlotGuard`, `releaseSessionSlotUnderGuard`, `removeSlotTreeFn`, `validateBindFields`, `ensureSlotStateLocked`, `accountSlotFailure`, `ReleaseSlotReservation`, `slotFailureWorkspaceFinalize`, `terminateHeldSession`, `runtimeHoldsLocked`, `noteRuntimeStarted`, `slotResolveError`, `ShutdownReclaim`. Covered under another name: `removeSlotTreeVia` (driven as `Server.removeSlotTreeFn`), `noteCompensationOutcome` (the "The counters" case asserting the recorded `(outcome, pool, podName)` triple) and `isTransientPodClaimError` (`TestHoldOrFailOnResumeErrorSlotRefusals_spec_7_3`'s three rows). EVIDENCE: non-spec-changes.md:2650-2660, :2808-2818, :2862-2870.
- **`cmd/lenny-gateway` has no shipped wiring test for any `podBinder` metric hook,** so CODE-9's own admission that "the tier-1 case below sets the hook directly and so cannot observe the omission" (non-spec-changes.md:1885-1890) matches the repo precedent and is not a missing-tier finding. EVIDENCE: cmd/lenny-gateway/metricsbackfill.go:141-161; cmd/lenny-gateway/scrub_report_wiring_test.go:284.

### Traps

- **Do not re-gate the cleanup-outcome report on `st.started`.** "Simplifying" the two predicates back into one reintroduces the double report through CODE-2's rollback and advances `sessions_served` for a session that never ran. The tier-1 "Claimed but not yet recorded" case exists to fail when that happens.
- **Do not gate the runtime teardown on `runtimeLive` either.** The tempting local fix for the mismatched sentence is to move pkg/adapter/session.go to `runtimeLive` so the code matches the spec text. It inverts the fail-closed direction: CODE-2's rollback fires only when `Runtime.Start` returns, so a start that hangs would leave the runtime serving the abandoned session forever. EVIDENCE: pkg/adapter/session.go:155-163.
- **Do not widen §6.2's fence to move `slot_cleanup ──→ leaked` into the either-concurrency block.** It is the obvious way to make a prose terminal claim true, and it changes shipped state-machine semantics, cascades into docs/reference/state-machines.md:251, and breaks the tier-11 matched-pair test. G1 removed the terminal claim from SPEC-4's prose instead, which is the smaller fix.
- **Do not edit spec/06:152 or docs/reference/state-machines.md:235.** The advice holds and the reason pass 1 recorded for it does not. The annotation reads "workspace ready, session dispatched to runtime with its session identifier", which is the runtime-has-been-given boundary (`runtimeLive`) rather than the admitted-start boundary, and SPEC-4's prose anchors to it rather than replacing it, which is what keeps DOCS-1 to the single added row it stages. An agent trusting the old gloss concludes SPEC-4's prose mis-cites the annotation when it does not. The residual "dispatched versus acknowledged" question is now CLOSED: the staged prose no longer attributes its boundary to the trigger at all. See the Settled entry on the closed `running` boundary.
- **Do not move the SPEC-2 block below §7.1's fence.** §7.1's atomicity paragraph physically sits inside the fenced flow listing, between the step-8 line and the line continuing it, so any paragraph inserted after it also lands inside the fence. The staged instruction says so explicitly. Below the fence it would sit among the `uploadToken` prose. EVIDENCE: spec/07:5,23,24,54.
- **Do not put a per-slot scrub rule in §5.2's `**Slot cleanup:**` bullet.** The bullet is under a `maxConcurrentSessions > 1` heading, so on a single-session pod §4.7's unqualified `ReportSessionScrub` row would govern instead. The concurrency-independent home is the `**Scrub model.**` paragraph at spec/05:453.
- **TRIED AND WITHDRAWN: the no-retry rule.** SPEC-2 originally said the failed attempt is not retried. Priced honestly that is roughly eleven mirrored exception clauses across §5.2, §6.2, §15.1, §29 and three error-catalog rows, plus a new `SlotReason` value (§5.2's "Client error on exhaustion" sets `error.category` from the failure reason, and an unacknowledged reclaim is a disposition rather than a reason), and it withdraws availability during exactly the partial pod outage that made the reclaim fail. The placement rule replaced it. Do not re-derive the retryability framing.
- **TRIED AND WITHDRAWN, and do not go the other way either: deleting the no-retry clause outright.** With the retry unconstrained and a lagging `Shutdown` still executing under the same identifier there are two unfenced outcomes. (1) The reclaim lands between the retry's `PrepareWorkspace` and its `StartSession`: `claimSessionSlot` re-creates an empty entry and tree and starts the session on it silently, and CODE-2's survived-the-start confirm does not catch it, because the entry it checks is the one its own claim just recreated. (2) The reclaim lands after the retry is running and tears down a healthy session. EVIDENCE: pkg/adapter/slotsession.go:74-90.
- **TRIED AND WITHDRAWN: fencing the lagging `Shutdown` by generation.** It looks like a free answer because `ShutdownRequest` already carries `coordination_generation`. The generation is per-session-coordinator and does not advance between two attempts of the same session on the same replica, and adding a per-attempt field is blocked by programme rule S-2, which reserves the single `schemas/lenny-adapter.proto` edit to step R1b.
- **MISTAKE: the proposal had already found and mispriced the placement exclusion.** non-spec-changes.md:574-579 rejected it as "the larger alternative ... not staged here", pricing only its code cost and never pricing the spec-contradiction surface the no-retry rule creates. Priced honestly it is four small Go edits and two spec clauses against roughly eleven spec and doc sites plus a new reason value. Do not re-derive that bullet's conclusion; the mispricing is the finding.
- **MISTAKE: the report rule was stated twice with opposite content.** SPEC-2 had the racing `StartSession` report the reclaim outcome while SPEC-3 forbade any report for the same reclaim. It cost a full round, and the cost was structural rather than verbal: each block stated its own version of one rule instead of citing a single statement of it. This is why the rule now lives in one paragraph that the others cite.
- **MISTAKE: a `leaked` `ReportSessionScrub` was used as a "this pod is bricked, retire it" side-channel** (non-spec-changes.md:189-195,:584-588). That is an accounting RPC repurposed as a health signal, and it cost the proposal a self-contradiction with SPEC-3 plus a double `sessions_served` advance. It was reached for because the ledger is not concurrency-gated (see the next trap), so a single `leaked` report really does retire the pod on the spot.
- **The leak ledger is not concurrency-gated.** `UnhealthyThreshold(1) == 1`, so one `leaked` report on a `maxConcurrentSessions: 1` pod stamps `lenny.dev/drain-request` immediately. EVIDENCE: pkg/gateway/session/recycle/scrubreporter_seams.go:184-197; slothealth.go:121-140.
- **The "bricked pod" premise is real but not distinctive to the racing rollback.** `SocketRuntimeProcess.Close` closes the listener whenever the released session was the last active one, and the listener is bound exactly once at adapter start. An ordinary session end reaches the same state through the same call, so retiring only on the racing path fixes no class. This is why deleting the report is safe.
- **`Binder.DrainSandbox` removes nothing from placement synchronously.** It only stamps the `lenny.dev/drain-request` annotation for the WarmPoolController, and `ClaimSlot`'s pass-1 scan of claimed pods reads the per-pod claim and never the Sandbox phase. Any claim that "at `maxConcurrentSessions` 2 the drained pod is no longer a candidate" is wrong. EVIDENCE: podsession/slotbinder.go:601; podclaim/slotclaimer.go:95-107,:417-469.
- **MISTAKE (nearly filed): the staged "not retried" clause does not ban every post-first-RPC retry.** It is grammatically governed by "A reclaim the adapter does not acknowledge", so it bans the retry only for the unacknowledged-reclaim class. Read the sentence's subject before filing on it. The parenthetical this entry used to carry, that `applySlotRetryPolicy` skips the retry when `sbe.Leaked`, is stale under the placement design: CODE-5 and its tier-1 case assert the retry STILL runs on a leaked disposition and only carries `ExcludePod`. Read the current CODE-5 text rather than any older gloss of the retry rule.
- **Dead end: "the adapter cannot see this leak".** `lenny_adapter_leaked_slots` is emitted by the gateway (gatewaymetrics_credential.go:219-223) despite the name and despite §6.2 calling it adapter health metadata, and `applySlotRetryPolicy` already marks a slot leaked without the adapter knowing. Withholding the adapter's report does not blind the gauge; CODE-5's `accountSlotFailure` feeds it from the gateway side. Two lenses chased this and both refuted themselves.
- **Dead end: "the withheld report contradicts §4.7 or §12".** §4.7's `ReportSessionScrub` row, spec/05:453 and spec/12:481 all scope their reporting rule to "a session release", and the proposal deliberately frames the pre-start reclaim as not one, so the whole family of findings gets refuted. What survives is the `leaked` terminal, which spec/06:160 requires to be counted with no scoping to session releases.
- **Dead end: "SPEC-3's insert does not reach an exclusive pool because §5.2's bullet is concurrency-scoped".** spec/05:395 and :453 already make slots and the per-slot cleanup uniform across concurrencies, and the shipped §4.7 `ReportSessionScrub` row generalises the per-slot cleanup to every session release with no concurrency qualifier (spec/04:692). The scoping ambiguity is pre-existing. Do not file it.
- **Dead end: "`releases the slot reservation afterwards` contradicts the leaked hold".** `ReleaseSlot(leaked=true)` skips the decrement and the occupancy-zero disposition, so the two are compatible. Three lenses checked this and dropped the finding.
- **Dead end: "the §7.1 rollback omits credential-lease release".** §7.1 step 23 is the existing spec basis and `failPhase` already cites it for the pre-attached failure path, so the code staging's `releaseCredentials` is anchored. EVIDENCE: podsession/binder.go:1065-1078,:1259-1268.
- **Do not read the reclaim as scoped away from pods that retire.** "The pre-attached disposition governs the pod; this reclaim governs the slot state on a pod that is released or reused rather than terminated" cannot mean that: on the concurrent path a failed bind whose slot was the pod's last occupant also retires the pod (`ReleaseSlot` deletes the claim at `remaining==0`), and the proposal compensates there. EVIDENCE: podclaim/slotclaimer.go:880-885.
- **`§15.4.2` is the repo's established citation for the CH-RUNTIMEOPS `terminate` signal,** even though the frame's own card is §28.5.3 and §15.4.2 only names the DRAINING state. Do not file it as a false citation; the tree already cites it that way. EVIDENCE: pkg/adapter/drain_test.go:12,:53.
- **The `slot_cleanup ──→ leaked` scoping asymmetry is pre-existing.** The same asymmetry already applies to the shipped `running ──→ slot_cleanup` edge, `slotstate` ignores the concurrency scoping entirely, and §5.2 and §6.2's leaked-semantics prose state the outcome unconditionally. Three lenses judged it below the bar. A later round asking why the leaked terminal is unreachable on a single-session pod should know it is structural.
- **The leaked-occupancy hold has no durable backing, and that is pre-existing.** The Redis `active_slots` counter's only rehydration source is `SessionStore.GetActiveSlotsByPod` over Postgres rows with `state='active'`, and a failed bind has no active row. The same hole already exists for the shipped leaked class. A later round wanting to close it should widen §5.2's rehydration text rather than §7.1.
- **Dead end, closed on the refute side: the `concurrent_slots_exhausted` gloss.** spec/05:549 reads "pods exist but all slots are full", which does not literally describe a pool whose only candidate was disqualified. It is ALREADY loose in the shipped tree: `ClaimSlot` returns `ErrNoConcurrentSlot` whenever the pool list is non-empty and no candidate is placeable, `ErrTenantMismatch` maps to the same reason, and candidates skipped by `expiredByUptime` do too. Adding one more skip reason does not falsify a gloss that already over-claims. Five lenses reached it and each refuted itself. EVIDENCE: podclaim/slotclaimer.go:505-522; podclaimerror_internal_test.go:87-88.
- **Citation ranges to normalise, not to file.** Several proposal citations give `SocketRuntimeProcess.Close` as `:441-467` and the listener bind as `:155-161`; the real spans are `:435-467` and `:156-161`. The cited ranges sit inside the real ones rather than being plainly false, so they were left alone. A future citation check should normalise them.
- **Settled, formerly disputed: "On the default disposition the pod is replaced." (spec/04:686) has exactly ONE site.** The pass-1 refutation asserted the sentence also appears verbatim at spec/29:696. It does not: `grep -rn` over spec/ and docs/ returns spec/04:686 alone, and §29 item 12 carries a differently-worded restatement ("the adapter closes the session runtime and the pod is replaced"). Two lenses confirmed this independently. The refutation's conclusion still stands, because the sentence is already loose today for any per-slot `Shutdown` on a co-tenanted pod, so the proposal does not introduce the looseness; its evidence does not. A reviewer arguing a missed §29 edit site from the two-site claim will find only one site. UNVERIFIED: whether the row wants "on the default disposition at the session's release" is still undecided.
- **Disputed, unresolved: is the "workspace-prep handler inserts the entry after the reclaim was answered" race filable?** `spec.1.review-security.1` says no, because both `resolvePrepareStagingDir` and `FinalizeWorkspace` call `ensureSlotPaths` at the top of the handler, before the slow work, so a client-side deadline almost always postdates the insert. `spec.1.review-reliability.1` says the handler runs `ensureSlotPaths` with no ctx check even on an expired deadline and a code-lane reviewer should look. Neither corrects the other; both agree the remedy would be an adapter-side tombstone, which the summary rejects as a non-goal.
- **MISTAKE, refuted at least eight times: "§7.1 says the exclusive pod retires while §6.2:283 and §7.2 step 3 release it back to the pool".** Every dress of this has been built and dropped: false citation, unreachable disposition, the residue riding a pooled pod back into inventory, and the tenant-isolation framing. The §6.2 fence retires a failed pod, spec/05:455 says so outright, and the original refutation quoted spec/06:234's release-to-pool clause and reasoned past it. A later round wanting it needs NEW evidence that a pooled replacement pod actually keeps the residue, which means reading the mid-resume terminal handler's release path in the gateway and showing it is not a drain. Filing it on the same evidence costs two verifiers and closes nothing.
- **MISTAKE, four separate occurrences: the bound/started drift.** Three fixers restated the runtime-teardown precondition as "every bound entry the call removed"; the §29.4 rationale that carried the third is now rewritten onto §29.4's own Preconditions paragraph and that finding is discharged, so do not re-file it. `bound ⊋ started`: `assignCredentialsSlot` binds at AssignCredentials while only `claimSessionSlotUnderLock` sets `started`. The fourth occurrence runs the other way: a sentence that names the predicate correctly but assumes the `exited_cleanly` signal is uniform across a range the predicate splits. Check every new sentence that names the runtime teardown against §4.7's "a session whose start the adapter has admitted", and every new sentence that reasons about `exited_cleanly` against the `started` boundary as well as the `bound` one. In each case the conclusion was right and only the stated ground was wrong, so the fix is the predicate phrase rather than the decision. Do not run the drift the other way either, which a round-4 lens nearly did against CODE-2: `claimSessionSlotUnderLock` sets `st.sessionID` AND `st.started` together (slotsession.go:87-88), so CODE-2's guard `!ok || st.sessionID != sessionID` is satisfied at every `noteRuntimeStarted` call site, SDK-warm (sdkwarm.go:261, on the `fresh` arm after the claim) and `Resume` (resume.go:50 then :144) included. `assignCredentialsSlot` binds EARLIER; it is not the only binder, and the guard drops no record.
- **MISTAKE that cost a round: a narrow conclusion generalised into blanket cover.** A WATCHOUT closed with "The same holds for item 13's `terminate` frame. Do not file it as a missed edit site." What it had actually established was only that item 13 is not falsified by the TWO-TEARDOWN SPLIT, because item 13 is scoped to a session that ran. The co-tenancy gate is a DIFFERENT sentence of SPEC-1 and bites exactly the session ends item 13 enumerates, so item 13 was a real edit site and round 5 had to find it again. When writing a "do not file" note, say which sentence of the change it clears.
- **TRIED AND WITHDRAWN, four alternatives to §5.2's placement rule.** A third carrier that clears the row's §4.6 pod binding (a new gateway store write, and `ClaimSlot` pass 1 still admits a same-tenant claimed pod with free capacity, so it does not even deliver a different pod); a durable per-row exclusion (changes the created-state pod binding from create through start); an adapter tombstone of answered reclaims (touches pkg/adapter/slot.go and slotsession.go, which a later campaign position rewrites, and summary.md:154 already prices it); and deleting the constraint outright, which the standing trap already bars.
- **Do not write that a retried start inherits the first attempt's credential file or its armed §4.9 timers.** Only the slot TREE is inherited. `assignCredentialsSlot` rewrites both wholesale from the request's lease set, and a stale timer no-ops against a re-created entry. A reviewer's suggested fix over-claimed at exactly this point, and landing it would have added two new false claims.
- **Do not write "the retried start re-binds cleanly" in the acknowledged arm.** The reservation was released on failure and the retry re-reserves nothing, so the retried session runs on occupancy the counter no longer counts. That is pre-existing and deliberately outside this proposal, but an arm that asserts cleanliness is next round's finding.
- **`running` is bound vocabulary and every state-word occurrence is backticked.** Do not introduce a second, unbackticked sense (for example "ending a session the gateway believes is running") next to a bullet whose backticked `running` an earlier group is protecting. Say "takes the session off the shared runtime process" instead.
- **"pre-`running`" collides with an established term at a different level:** the session states `created`, `ready`, `starting` and `finalizing`. Do not simplify §5.2's corrected sentence back to a bare "pre-`running`". Three surviving "pre-`running`" phrases in the proposal are LABELS for the rule, the edge or the paragraph rather than range assertions, and a grep-and-replace that rewrites them adds words and no content.
- **Do NOT touch the "A start that races the reclaim" bullet's closing sentence** ("Neither ordering reports a cleanup outcome, because the slot never reached `running`"). Round 3 adjudicated it true-as-scoped and four later lenses re-derived the temptation. It reads like the same defect as the deleted §7.1 sentence and is not. If a round does file on it, the correction is the predicate ("the reclaim finds no session the runtime holds"), never the conclusion.
- **Do NOT touch spec/07:220's "Pre-attach terminal collapse" paragraph.** No replacement pod is claimed on those edges, so "no live workspace or running runtime to seal" stays true, and the §7.2 preamble deletion sharpens the contrast rather than breaking it. Five lenses have swept it as a candidate matching site.
- **Do NOT extend the §29 co-tenancy carve-out to §29.9 item 4b.** That bullet records that the specification does not state at what point of the eviction path the `terminate` frame is sent, which is an ordering claim the co-tenancy gate does not answer.
- **The §29.4 step-13 anchor is NOT unique, and it now occurs FOUR times.** "([§15.4.3](...), §28.5.3)." occurs at spec/29:645, :711, :887 and :982; :711 is the intended site. CORRECTED: earlier passes recorded two sites and then three. The instruction resolves only because it scopes itself to "§29.4's numbered step 13". Do not drop that scoping if the block is reworded, and do not report the anchor as unique in a sweep.
- **Do not file the §5.2 retry-policy trigger reading.** "The policy is triggered by an adapter-emitted slot-failure event for a running session, so SPEC-2's placement constraint reaches no bind failure" is a real tension between §5.2:544-557 and §6.2:283-286, and it is pre-existing: the code settles it the other way (`applySlotRetryPolicy` branches on `*podsession.SlotBindError`) and §5.2's own non-retryable list includes a bind-time reason. Three lenses dropped it.
- **Do not file the cross-replica actor problem in §7.2.** Step 3's "the connection that attempt still holds" against step 4's "fencing any stale coordinator" looks like a mis-assigned actor. Step 1 ("cancel the in-flight restoration RPCs") already presumes the sequence's replica owns those RPCs, and the reclaim is in fact issued by the failing attempt's own goroutine, so step 3 inherits the assumption rather than introducing it. Four lenses.
- **Do not file either `leaked` gloss.** §6.2:148's `slot_cleanup ──→ leaked` annotation and §5.2:562's whole-pod replacement trigger both gloss `leaked` as "cleanup timeout exceeded", which does not describe a reclaim the adapter never answered. §5.2's own bullet already says "If cleanup fails, the slot is leaked", broader than either gloss, and the shipped code already sets `leaked = err != nil || !cleanly` on the ordinary session end. Three lenses.
- **Do not file the `slot_cleanup ──→ released` annotation.** "(slot workspace removed, processes killed, slot released)" is today a word-for-word mirror of §5.2's three-action list, and SPEC-3 widens that list to five, so the annotation becomes an abbreviated gloss of a longer list. An abbreviated annotation states nothing false, and the structurally identical §5.2/§6.4 enumeration finding was already refuted. Three lenses, one of them standing next to the row while editing DOCS-1.
- **Do not file §11.4 revoke step 3.** "The pod's runtime adapter initiates graceful shutdown (SIGTERM to agent, wait up to 10s, then SIGKILL)" sits under a list scoped to "all active sessions", which are `started` by construction, and it is already false for any per-slot `Shutdown` on a co-tenanted pod. Pre-existing looseness this proposal widens rather than creates. A round that widens the revoke's scope should re-check the line.
- **Do not file §4.1's retained second sentence as drift this proposal creates.** Before the edit, sentence 2 already said "per-slot teardown"/"whole-pod teardown" while sentence 3 said "per-session teardown"/"whole-pod scrub". The edit adds a naming mismatch to a pre-existing one. It stays the standing OPEN on vocabulary, and four lenses have declined it.
- **Do not file the §4.7 "the pod's session mode" term collision.** The specification binds "session mode" to `executionMode: session`, while the axis that selects the starting RPC is pod-warm versus SDK-warm, which the same §4.7 table already spells two rows above. Four lenses derived it and all four declined it as wording. The fix is two words and belongs outside the loop; a fifth derivation is waste.
- **Dead end, and here are its five sites: "the withheld report contradicts §4.7 or §12".** spec/04:692, spec/05:453, spec/12:481 and :494, schemas/lenny-adapter.proto:308-319 and docs/reference/adapter-contract.md:81 all scope their reporting rule to "a session release", and the proposal frames the pre-start reclaim as not one. Grepping `ReportSessionScrub` across spec/, docs/ and schemas/ lands on exactly this set. CORRECTED: spec/05:545 was in that list and does not belong there. Its `**Slot cleanup:**` bullet reads "On slot completion or failure ... The adapter reports each slot cleanup outcome (`released` or `leaked`)", which is a trigger rather than a session-release scoping. The dead-end verdict has survived many rounds anyway, so a round that wants to re-open the bullet-versus-scrub-model tension must argue from the bullet's OWN trigger and from SPEC-3's new pointer sentence (spec-changes.md:520), never from the "session release" framing.
- **MISTAKE nearly filed: the recycle-disposition `Shutdown` against §5.2:459.** "The adapter closes the ending session's runtime" is not falsified by SPEC-1's no-op sentence. The two-RPC split is an implementation fact; the spec still models one request carrying the disposition beside the teardown, so the applied spec is self-consistent and the divergence is pre-existing.
- **MISTAKE nearly filed twice: the withheld report as a residual-state relaxation.** Neither "a tenant drives N pre-`running` attempts onto one pod without advancing `recycle.maxSessionsPerPod`" nor "SPEC-3 relaxes the residual-state bound" clears the bar. Today's failed-bind path sends no `Shutdown` at all, so the counter does not advance there either, and the per-slot cleanup the proposal ADDS is what reclaims the residue. The whole-pod scrub and `maxScrubFailures` remain the backstops.
- **MISTAKE nearly filed: the reclaim as a deadlock sink.** "§7.2 step 1 stops the 300s `resuming` watchdog and step 3 then blocks on an unbounded adapter RPC, so `resuming` becomes the deadlock sink §6.2 forbids" does not survive: the code lane bounds the reclaim with `slotCleanupBudget` (5s floor), the Design paragraph names §5.2's figure, and §7.1's own leaked disposition presupposes the gateway gives up. Two lenses.
- **MISTAKE nearly filed: the replica-local leak ledger at multi-replica scale.** `slothealth.Tracker.leaked` and `.events` are in-process maps, so a pod may never reach `ceil(maxConcurrentSessions/2)` while its cluster-wide Redis occupancy stays held. The fact is true and the finding is a close variant of the already-refuted occupancy-hold family, whose refutation granted the other retirement routes (`recycle.maxSessionsPerPod`, `maxPodUptimeSeconds`) as the bound.
- **MISTAKE nearly filed: the create-time-reserved bullet's unscoped `leaked`.** A create-time-reserved slot exists only on a concurrent pool, so the bullet's unqualified `leaked` is correct on its own domain and is not the unscoped-`leaked` defect an earlier round fixed twice. Two lenses.
- **MISTAKE nearly filed: the tree-removal window on the acknowledged branch.** The round-4 bullet attaches "a reclaim whose tree removal is still running when the retry materializes the same tree" to the ACKNOWLEDGED branch, where the removal has completed before the gateway returns the error. It survives on the reading that a client which times out and retries `POST /start` concurrently with the still-in-flight compensation produces exactly that interleaving. Defensible; do not spend a round on it.
- **Read the §7.1 block's long line whole before editing any part of it.** Two sentences roughly 250 words apart on one physical line pull opposite ways, and the same false proposition was stated a second time in the Design copy with "first pod-side RPC" where the staged paragraph says "first such RPC", so a grep anchored on one wording misses the other. Rounds 1 and 2 both fixed this paragraph by deleting one restatement and both left the neighbouring one standing. The paragraph's disease is structural: it derives consequences from rules other sections own, so sweep the WHOLE paragraph rather than removing them one finding at a time.
- **§7.1's obligation has TWO bounds and every delegation names only the lower one.** Any future edit that hands a new path to §7.1 must state what happens on that path once the slot is `running`.
- **`context.WithoutCancel(ctx)` in CODE-4's compensation is load-bearing.** It is what makes the reclaim survive a cancelled resume context; a later edit that "simplifies" it back to the inbound ctx silently deletes the mid-resume cancel case.
- **Do NOT close the §7.2 gap by pointing at §7.3.** The §7.3 appended sentence binds "a step in this flow that fails", and a client, parent or admin cancel is an external terminal trigger rather than a failing step, which is exactly why the proposal edits the §6.2 cancel bullet separately.
- **The imprecisions that were verified and declined, so nobody re-verifies them.** "The row keeps its §4.6 pod binding" (§4.6 is "Pod Lifecycle Controllers" and owns the `SandboxClaim` binding, while the session row's create-time pod is persisted at §7.1 step 5, so the attribution is loose rather than false); the Design's "closes by stating that the client never receives a `session_id`" (that sentence sits mid-paragraph at spec/07:23 with more sentences after it); SPEC-2's §6.2 note "Neither bullet enumerates that sequence" (its load-bearing half, that neither names the step-4 bump, is true). Do not spend a verifier pair on any of them.
- **Round 3 deleted the only text that stated why a §7.3 re-attach cannot land on the reclaiming pod,** and the Design paragraph's "placed by neither mechanism" lost one of its two antecedents with it. The mechanism still holds in code, so this is a record gap rather than a coverage gap. A round asking "what keeps a re-attach off the reclaiming pod" should read the Settled entry on `Claimer.Claim` rather than reopen the question.
- **MISTAKE, a FOURTH occurrence of the unscoped `leaked` terminal, and the most expensive.** A fix round appended a sentence to §5.2's `**Scrub model.**` paragraph asserting the `leaked` disposition, the held occupancy and the whole-pod replacement trigger with no concurrency qualifier, inside the paragraph the proposal itself declares to hold "on a pod of either concurrency", against staged §7.1's explicit "stated for concurrent occupancy and do not apply there". Five lenses filed it in one round. The structural cause is that the scrub-model paragraph is deliberately concurrency-independent, so ANY sentence appended there that names `leaked`, the trigger or the gauge needs its own scope. It was fixed by splitting the sentence by pod concurrency; check every future append against this.
- **Do NOT collapse that finding into the standing "the `slot_cleanup ──→ leaked` scoping asymmetry is pre-existing" trap.** That trap is about shipped §5.2 and §6.2 prose being loose against the fence. The round-8 defect was a contradiction between two sentences THIS proposal stages, created by a fix and against the proposal's own recorded DECISION. A verifier that merges the two refutes a live finding with an unrelated refutation.
- **Do NOT add a bind-path or a start-state qualifier to §5.2's leak-accounting sentence.** The bind-path qualifier is precisely the carve-out by code path that §5.2's trigger forbids, and it would falsify three untouched shipped sentences (spec/05:545, spec/06:160, docs/reference/state-machines.md:251). The path question is answered in the code lane by the third `accountSlotFailure` caller; the start-state question is answered by CODE-1's `live` gate. CITATION RE-POINTED (the `f4` firing deleted the conformance-gap bullet this trap used to cite): the same no-carve-out argument now lives in CODE-5's `accountSlotFailure` doc comment ("which the trigger states with no carve-out by code path", non-spec-changes.md:515) and in its caller list at non-spec-changes.md:537-569.
- **Do NOT surface `treeErr` on the started path unconditionally to close a clean-exit gap.** It reclassifies ordinary session-end slots as `leaked` under `Binder.ReleaseSlot`'s `err != nil || !cleanly`, which is the §6.2 accounting change the proposal explicitly declines. The form that works, and that landed, is gating on the report predicate (`live`) rather than on the teardown predicate.
- **Do NOT simplify CODE-1's three predicates into fewer.** The report gate and the response gate are both `live`; the runtime teardown stays `st.started` and must, because CODE-2's rollback fires only when `Runtime.Start` returns, so a start that hangs under a `runtimeLive` teardown gate would leave the runtime serving an abandoned session forever. Three predicates, two of which are the same one, is the correct shape.
- **MISTAKE that cost a whole open-decisions firing: a scope call resting on shipped behaviour a staged deliverable removes.** An out-of-scope row argued "no staged deliverable adds a second producer" from `BindReservedSlot` swallowing its own reservation-release error, while the same proposal's CODE-4 retires exactly that swallow. The false ground then propagated into the summary, the SPEC-4 exclusion paragraph and two edge-case bullets. When adjudicating a scope call on this proposal, check the ground against the STAGED tree, not the shipped one.
- **MISTAKE, three rounds on one sentence: the tier-7a assertion list.** Round 1 wrote three universals ("`runtimeLive` is empty", "`runtimeIdleLocked` is true", "the runtime was closed exactly once") beside a co-tenanted variant that falsifies the first two and a `FailedPrecondition` invariant that is interleaving-scoped. Round 2 shrank the list and produced a smaller false universal. Round 3 deleted both sentences that described a distribution over interleavings and fixed one interleaving instead. The list itself was the defect: a shared assertion list beside per-variant and per-interleaving axes.
- **Do NOT reintroduce a "the runtime was closed exactly once" assertion, in any form.** As a call count it is two: the reclaim closes under the `started` gate and CODE-2's rollback closes again. As a process-teardown count it is zero on the co-tenanted variant, because `SocketRuntimeProcess.Close` early-returns on `!p.connected` and again while a started co-tenant keeps the active set non-empty. Whether the reclaim's close or the rollback's close tears the process down depends on where the park sits relative to `accept`, which is a fixture decision nothing has made. The effect is pinned instead by the shipped `TestSocketRuntimeProcessCloseScopedToSlot_spec_5_2` and by the tier-1 co-tenancy case.
- **TRIED AND WITHDRAWN: fixing the tier-7a case by admitting both interleavings.** Dropping the park and running the barrier form makes CODE-2's guard exercised only on the runs that happen to lose the race, which turns the case's own subject into a lottery under a stress budget, and every assertion becomes conditional on an arm the test can only identify by reading the outcome it is asserting.
- **CODE-1's at-most-one-report-across-interleavings rule is stated ONCE,** in the CODE-1 section beside the `live` gate that carries it. The test inventory points at it. Restating it in the tier-7a paragraph is exactly what produced the round-3 contradiction.
- **Do NOT add `noteRuntimeClosed` to CODE-2's rollback.** `noteRuntimeStarted` refused, so the session was never inserted; the call is a no-op and it cannot remove a co-tenant. Adding it to make a mis-scoped test assertion pass would be a code change answering a prose defect.
- **MISTAKE, the one-caller reading a second time: "`StartSession` is the only site the compensation can race".** CODE-4 compensates `Binder.Resume` two pages later, and the proposal's own text says the adapter's `Resume` leaves the identical residue. The two statements sat in one file for several rounds and cost a missing rollback, a missing tier-7a arm and an attribution error in the summary. Any future sentence naming `StartSession` alone on this surface must be checked against `resume.go:50,:140,:144` and `sdkwarm.go:217,:261`.
- **Do NOT put the credential-lease release back inside `compensateFailedSlotBind`,** and do not gate it there with a stage check, a `req.SlotID != ""` check or a nil-credentials check. Every such conditional makes the function's contract depend on which caller it has, which is the coupling that produced the finding. The release belongs to the wrapper that owns the stages that mint a lease.
- **Do NOT make the bind-path lease release conditional on `assignSlotCredentials` having SUCCEEDED.** It mints per provider in a loop and returns on the first error, so a failed credential-assignment stage can already hold minted leases. Unconditional in the wrapper is the fail-closed choice and `ReleaseSession` is a documented no-op for a session holding none.
- **The §7.2 step-2 anchor is a mid-line fragment.** The staged sentence lands BEFORE the `final_workspace_ref` sentence rather than at the end of step 2, because both sit on one physical line. It is deterministic and appliable, so it is not a finding, but an implementor expecting an append-to-end will be surprised, and a reviewer must not "fix" it by re-anchoring.
- **Dead end, and here is its resolution: `docs/reference/adapter-contract.md:75` is the site and `:81` is not.** The `Shutdown` row at :75 is the reader-facing mirror of the §4.7 row SPEC-1 rewrites and is falsified in four ways, only one of which depends on the contested "is a pre-start reclaim a session release" framing; frame any finding on the drain-gate and usage-flush halves. The four sibling sites (`adapter-contract.md:81`, `security-principles.md:33`, `execution-modes.md:68`, `multi-tenancy.md:72`) all say "at each session release", which SPEC-3 widens rather than falsifies. Earlier passes let the DEFERRED and the dead-end trap pull opposite ways on :81; they do not.
- **Dead end: "`SlotID == SessionID` falsifies §5.2's `new slot` and the Fresh workspace guarantee".** Three lenses built and dropped it. "New slot" reads as a new reservation, the placement rule keeps the hazard off every retry §5.2 places, and it is already false in the shipped tree, where no `Shutdown` is sent on a failed bind at all so the tree always survives. The staged change strictly improves it.
- **Do NOT file `spec/12:913` or `spec/15:1795` against SPEC-1's co-tenancy gate.** They are the same class as the §11.4 revoke trap: each is already false for a co-tenanted pod against the shipped `boundRemains` gate, and each states a delivery intent rather than a delivery guarantee. A round that widens the gate's scope re-checks all three together.
- **Do NOT file §5.2's `**Recycling and integration levels.**` sentence** ("recycling runs with no CH-RUNTIMEOPS exchange between sessions") against SPEC-1. `drainViaLifecycle` already fires under `!boundRemains` in the shipped adapter, so the sentence is loose today and SPEC-1 restates rather than widens.
- **Do NOT file the unbound co-tenant against the drain gate any more.** A registered-but-unbound co-tenant has an empty `sessionID` and so does not hold the signal off, but after CODE-1 the signal needs `started` AND no bound entry, and the residual case is an ordinary started session end, which is shipped and pre-existing.
- **The exclusive-pod retirement claim now has THREE staged sites:** §7.1's new paragraph, the accepted-failure-mode bullet, and §5.2's `**Scrub model.**` append. A future edit that adjusts the wording in one must sweep all three, and "Spec files touched" gives no hint that a pod-disposition claim now lives in §5.2.
- **The staged exclusive-pod carve-out fails on the sequential-reuse configuration.** `maxConcurrentSessions: 1` with `recycle.enabled: true` is supported, and §4.6.1 projects `idle` for a claim deleted on a recycling pod, so "the pod retires, so the reclaim's residue does not outlive the pod" is not universal. Do not read spec/06:283 as settling it either way; it asserts both horns. A round that wants this needs the §4.6.1 projection route rather than §7.3 reachability, and it must answer why the occupancy-zero whole-pod scrub does not sweep the residue anyway.
- **MISTAKE nearly filed, five performance dresses.** The SIGTERM→SIGKILL pivot the `deadlineMs: 0` compensation would hand `SocketRuntimeProcess.Close` is guarded on `cmd != nil` and no production wiring sets `SpawnPath`; the §16.5 creation SLO samples successful creates only, so neither the compensation's latency nor its held connection breaches a stated budget; `slothealth`'s never-evicted maps predate the change; and the reserved-branch drain composite is strictly better than today, because today's `leaked=false` release already deletes the claim on a solo-occupancy pod.
- **MISTAKE nearly filed: "the `ExcludePod` pass-2 exclusion is unreachable".** True that a leaked release leaves the claim, so the excluded pod is projected `claimed` and can never be a pass-2 idle candidate; pass 1 is the load-bearing one, because a pod at concurrency 4 holding one leaked slot still has free capacity. A read-only skip on a branch that cannot fire is defense in depth, and the fix would be deleting a harmless filter.
- **Do NOT read `pkg/sandbox/slotstate` as adapter-side.** It is the gateway's in-process registry (doc comment at registry.go:6-12, constructed at sessionserver.go:1873), and `grep -rn "slotstate\." pkg/adapter/` returns nothing. The summary's Redis-rehydration row used to cite it for a claim about the ADAPTER; five lenses left that unfiled, `non-spec-recheck-3.2.review-citations.1` FILED it as a component misattribution, and the fix landed: the row keeps the adapter as its subject and now cites `pkg/adapter/slotsession.go:398-407`. Do not re-derive the misattribution, and do not "fix" the row by re-subjecting it to the gateway.
- **Do NOT file the §4.9 timer cancellation as stranding a credential file.** `onSlotLeaseExpired` returns immediately when the slot is no longer in the registry, so leaving the timer armed would scrub nothing either. If a round ever wants the ordering closed, the edit is to condition the cancellation in SPEC-3's sentence, never to widen §4.9.
- **WATCHOUT: `pkg/gateway/podlifecycle/podsession/resume_slot_reservation_test.go` is named nowhere in the proposal.** It holds `TestResumeReleasesItsReservationWhenTheAdapterResumeFails_spec_5_2`, which drives a FAILING `Binder.Resume` against a real `adapter.New(...)` server and asserts the `active_slots` decrement and the claim disposition, which is exactly the branch CODE-4 changes. Traced and green: the adapter holds no entry for the session, so the compensating `Shutdown` answers `ExitedCleanly: true` under both the shipped gate and CODE-1's, `Leaked` is false and `releaseResumeSlot` decrements as today. Do not file it as a broken test; do re-check it if CODE-4's disposition wiring changes. EVIDENCE: that file, :211-261.
- **MISTAKE nearly filed, three dresses in one round: bookkeeping against the deliverable index.** summary.md:58's "the one new field is `ExcludePod`" against CODE-4's second in-process field `SlotBindError.Leaked`; CODE-4's index line naming three files while its own call-site table stages three edits in `pkg/gateway/sessionserver/start.go`; CODE-5's index line naming two files while its Targets name `podsession.SlotBindRequest`. All three are real and verifiable, all three are covered by the deliverable body and by `## Files touched on application (non-spec)`, and none changes what an implementor builds. This loop has now refuted five of that class on the materiality bar; price a bookkeeping candidate against it before spending a verifier pair.
- **MISTAKE nearly filed: S7's tier list omits tier 7a.** CODE-1 moves the `Shutdown` drain gate from `bound` to `started` and `tests/tier7a_load_local/shutdown_drain_gate_race_test.go` is the shipped suite pinning that gate, but every session those tests drive is started through `startDrainSession`, so the moved gate cannot reach them and `boundRemains` is untouched. S8 depends on S7 and runs 7a one step later anyway. EVIDENCE: non-spec-changes.md:874-884,:209-218.
- **MISTAKE nearly filed twice: "the per-stage compensation table omits the workspace-prep stage".** True that `materializeSlot`'s first failure branch is `stageWorkspace` (slotbinder.go:284-289, `slotFailureWorkspacePrep`) and that it is the stage producing the class-1 registered-but-unbound residue, but the compensation is stage-agnostic by construction (one wrapper), `FinalizeWorkspace` reaches `ensureSlotPaths` the same way and so produces the identical state, and the adapter half of that residue class is pinned at tier 1 by the "Registered but unbound" case. CORRECTED, and this is a filed finding rather than a note: the clause this entry used to close with, that the upload-free branch is the one stage failure sending no RPC at all, is false. `stageWorkspace` CANNOT fail on an upload-free plan (`rewriteExtractedSources` returns the plan unchanged, the blob-fetch loop is entered only for `uploadFileRefs`, and `PrepareWorkspace` is the last statement), so every reachable `stageWorkspace` failure carries uploads and Settled #65 is the correct statement. `[non-spec.2.review-mechanism.1]` filed it against non-spec-changes.md:493-498 and the tier-1 case at :847-850. Do NOT collapse the finding into the refuted "over-sending an obligation does not violate it" item, which GRANTED the premise this finding attacks. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1284-1332,:1346-1354,:1408-1420.
- **MISTAKE nearly filed: the tier labels on the `podsession` cases.** `podsession/binder_test.go:174-180` starts envtest while the sibling `slotbinder_test.go` cases are filed tier 1, and `slotbinder_test.go` carries `// diagnosis:` on 12 of its 17 tests where the fake-backed `sessionserver/slotretry_test.go` carries none on 15. No gate turns on it, for the reason in the Settled entry on directory-based tiers, and the tests get written and run either way.
- **MISTAKE nearly filed: `export_test.go` against the Testing section's "No export_test.go change".** non-spec-changes.md:639 contradicts :289 and :998 literally, but the sentence's own justification clause scopes it to "no NEW seam is needed for these cases", and an implementor builds S8 from CODE-2's scope, which names the file and the line.
- **MISTAKE nearly filed: the R1b "No impact" impacts row against the proto `Shutdown` comment.** After SPEC-1 the comment at schemas/lenny-adapter.proto:203-206 is further falsified and R1b is the only step allowed to open that file, but the comment is ALREADY stale for any per-slot `Shutdown` on a co-tenanted pod, so this is the same widening-of-pre-existing-looseness class as the §11.4 revoke trap. Checked in the same pass and NOT falsified: the `ReportSessionScrub` comment (:308-311) and `SessionScrubOutcome` (:436-438) both scope themselves to "every session release", which SPEC-3 widens. The correction that IS owed is the standing Deferred against the proto, which R1b carries.
- **MISTAKE nearly filed, and here is what kills it: "the `exited_cleanly` rule's `live ||` disjunct has no test in either direction".** True as stated, since no listed or shipped case drives a `Shutdown` whose `removeSlotTree` fails, but the exclusion paragraph (non-spec-changes.md:695-701) already records that arm as excluded for a portability reason (`removeSlotTree` calls `slotlayout.RemoveTree` with no injectable ops) that covers the started arm identically even though the paragraph names only the unstarted one. The only cheap discriminating assertion left is already enforced by the shipped `TestShutdownIsANoOpForAnAlreadyReleasedSession_spec_4_7` (session_test.go:469-473): inverting the disjunct to `live && treeErr == nil` turns its `ExitedCleanly` assertion red. The residual is one sentence of scope in the exclusion paragraph.
- **MISTAKE nearly filed: the tier-3 dismissal's attribution.** non-spec-changes.md:789-791 says the no-op clean-exit answer is pinned at tier 1 "by the adapter cases above" and end to end at tier 4 below, and neither is literally true, because no listed adapter case and neither tier-4 case drives `Shutdown` for a session the adapter holds NO entry for. The behaviour is covered anyway by the shipped `TestShutdownIsANoOpForAnAlreadyReleasedSession_spec_4_7`, and it is UNCHANGED by CODE-1 (`removed` false gives `closeErr == nil && (false || true)`, the same answer the shipped `bound` gate gives), so the attribution is loose and no changed behaviour is untested.
- **MISTAKE nearly filed: "tier 5 is reached, because CODE-5's reserved and resume callers newly drain a pod nothing drains today".** `DrainSandbox` itself is untouched, the new callers reach an unchanged annotation stamp whose WarmPoolController chain has its own shipped coverage, their own discriminators are asserted at tier 1, and the kube-visible consequence (the per-pod `SandboxClaim` surviving at `bound`) at tier 4. Same shape as the tier-8 item three rounds have declined.
- **Dead end: "withholding `emitFinalUsage` for a bound-but-unstarted entry strands a delegation-tree budget".** The §12.4 budget return runs from the archive path (`pkg/gateway/sessionserver/usage.go:648-668`, `returnTreeBudget`) rather than from the final usage report, and a never-started session's usage meter is empty anyway. Chased as an unlisted consequence of moving `emitFinalUsage` into the `started` block; there is none.
- **TRIED AND WITHDRAWN: CODE-2's rollback releasing the slot by session identifier.** The staged rollback called `s.releaseSessionSlot(sessionID)`, which re-reads `s.slots[sessionID]` at call time and `RemoveAll`s whatever tree it finds. Because the guard that fires the rollback has already established "absent OR registered-but-unbound", the call can only be a no-op or a destruction of a later attempt's entry and staged uploads, and the window is as wide as a `Runtime.Close`. It was CODE-2's own new instance of a shipped five-site pattern; the round removed the new instance only. Do not reinstate it, and do not reach for the identity-checked deregister here: that fixes a pre-existing class, changes `claimSessionSlot`/`deregisterSlot`/`deregisterSlotLocked` signatures inside a file a later campaign position rewrites, and wants its own problem statement.
- **The reverse-ordering sub-case does NOT reach CODE-2's rollback at all.** If a successor re-claims through `claimSessionSlot`, `st.sessionID == sessionID` and `noteRuntimeStarted` returns TRUE against the successor's entry, so the abandoned start records into `runtimeLive` and returns success. Any test written as "the rollback runs and the bound successor survives" is wrong on the arm it names; only the unbound arm discriminates. The staged tier-1 "The rollback destroys no successor" case says so explicitly.
- **MISTAKE that cost a finding, a design pass and a fix pass: "the pointer change costs no test edits".** Round 5 settled the `ExcludePods` pointer threading and asserted that returning `*podsession.SlotBindRequest` from `slotretry_test.go`'s `req` helper would leave every call site compiling. It would not: the helper feeds `classifySlotBindFailure` at four sites whose callee keeps a by-value parameter. The claim came from grepping ONE callee of the fixture helper rather than every caller of the helper, and it put a false compile-time claim into a staged deliverable. Before asserting a signature change is free, enumerate the helper's callers, not the callee's.
- **TRIED AND WITHDRAWN: making `req` return a pointer.** It compiles for the twelve `applySlotRetryPolicy` sites and breaks the four `classifySlotBindFailure(in, req(...))` sites in the same file. Also withdrawn: a second `reqPtr` fixture (two near-duplicate request fixtures in one file to avoid twelve one-line locals, and the local is what makes per-call ownership legible in the load test's goroutine), and widening the deliverable so `classifySlotBindFailure` takes a pointer (two production callers plus a composite-literal test caller, and a nil-dereference surface on an error-mapping helper).
- **MISTAKE: CODE-2's rollback answering `codes.FailedPrecondition`.** It made a transient reclaim-versus-start race a permanent 422 `SLOT_FAILED{retryable:false, category:"policy_rejection"}` with no `Retry-After`, and `applySlotRetryPolicy` skipped the retry on attempt 0, against the proposal's own "No failure class becomes non-retryable" and staged §7.1's "Nothing about the attempt's retryability changes". The fix is the status code, and it is the reason those two sentences need no rewrite. spec/15:1136 independently makes an adapter `FailedPrecondition` the deterministic-permanent code in the setup window.
- **Do NOT touch `podsession/slotfailure.go`'s `Reason()` switch to carve `slotFailureSessionStart` out.** It is on the files-touched list only for `SlotBindError.Leaked`. The carve-out widens to EVERY start-stage `FailedPrecondition`, including the genuinely deterministic "adapter is not configured with a runtime" at session.go:104, and it leaves the adapter still saying "precondition failed" for a concurrency abort. `codes.Unavailable` is also barred: `claimSessionSlotUnderLock` already mints it twice on the same claim path (slotsession.go:69-71,:82-84).
- **Do NOT mirror the `spanErr` line onto the `Resume` rollback, and do not change its category.** `Server.Resume` opens no span, and the `StartSession` arm's `tracing.CategorizeError(rollbackErr, tracing.CategoryTransient)` is the second carrier of the same transient classification the status code establishes. A future edit that categorises it Permanent re-derives the defect the round closed.
- **The rollback's status code is asserted verbatim in two test descriptions far from CODE-2** — the tier-1 "Start-versus-reclaim rollback, deterministic form" case and the tier-7a start-versus-reclaim case's per-arm sentence — plus the new gateway tier-1 classification bullet. Any future change to the code must grep the proposal for the code name rather than read CODE-2 alone.
- **TRIED AND WITHDRAWN: wiring a `RuntimeOps` into the tier-4 fixture to observe the §15.4.2 signal.** It duplicates roughly seventy lines of `startDrainPeer` across a package boundary (tier-7a's helper is `package tier7a_load_local_test` and not importable) and hard-codes a carol-before-alice ordering nothing else in the case needs, to re-observe a gate this proposal does not touch. Also withdrawn: adding a co-tenanted variant to the tier-1 "Bound but unstarted" case, which contradicts that case's own load-bearing "leave no other bound entry on the pod" condition, and moving the clause to tier 7a, whose section already says no new signal-frame case is added.
- **Do not "fix" the tier-4 carol clause to say the connection survives but not the listener.** `SocketRuntimeProcess.Start` takes the `p.connected` early return for a second session and never rebinds the listener, which reads like a defect. It is not: `Close` tears connection and listener down in the same last-close branch, so a surviving connection witnesses a surviving listener on the shipped tree. Weakening it would also falsify the 0078 impacts row, which asserts the listener half holds independently of 0078.
- **`startRuntimeOps` (runtimeops_test.go:83) is cheap and used by eight tier-1 adapter tests, which makes "just add a co-tenanted tier-1 drain case" look free.** It is not: it re-pins an UNCHANGED gate and collides with the tier-1 "Bound but unstarted" case's stated reason for excluding a co-tenant. The proposal's discipline for an untouched mechanism is to attribute it to the shipped test that owns it and add nothing.
- **`slotTreeProbe` probes the credential FILE (`paths.CredentialsFile`), not the credential directory** (slotsession_test.go:77-85), so a successor created through `ensureSlotPaths` alone has no credential file and a survival assertion on one there is unsatisfiable. The bound sub-case has to run `AssignCredentials` first.
- **The §5.2 append can silently redirect four tier-11 gates, and only case sensitivity is stopping it.** `lineContaining` returns the FIRST matching line, and SPEC-3 appends to the `**Scrub model.**` paragraph at spec/05:453, which precedes every §5.2 bullet a gate anchors on. The gates key on capitalised "Whole-pod replacement trigger", "Session count limit", "Uptime limit" and the retirement-logging line; the append writes "whole-pod replacement trigger stated below" and "the pod's served-session count" in lower case. A fix round that recases either phrase, or that adds any of those anchor strings to the append, turns `TestLeakedSlotCountingLifetimeAgrees_F5231` and its three siblings red by scoping them to the wrong line. The same first-match hazard applies to `requireLine(s72, "per-slot sub-states")`, whose single occurrence is spec/07:162.
- **A later edit that puts a scoped-block edge string into SPEC-4's new prose paragraph would silently pass the negative loop,** because `generalBlock` runs to the end of §6.2 while `scopedBlock` ends at the general header. The current text is safe (single arrow, no "`leaked` slot semantics" phrase), and it is safe by accident rather than by design.
- **`SlotBindError.Stage` is doc-commented as the `lenny_slot_failure_total` error_type label** (slotfailure.go:63-64), which reads as if CODE-4's `Stage: "resume"` mints a metric label value. It does not: `slotFailureConnect = "connect"` is the shipped precedent for a Stage that is explicitly not an error_type value (binder.go:286-299), and the counter is emitted only by the five `recordSlotFailure` calls inside `materializeSlot`. Do not file the label-cardinality candidate.
- **`fakeSlotBinder.BindSlot` discards its request** (`_ podsession.SlotBindRequest`, slotretry_test.go:25-49) while three staged tier-1 cases assert on the `ExcludePods` a `BindSlot` call carries, and non-spec-changes.md:813 says the queue case runs "over the shipped `fakeSlotBinder`". The fake needs a recorded-requests field. Not filed (one struct field, file already in the edit list); a fixer touching that paragraph should add the clause.
- **MISTAKE nearly filed: "the SDK-warm freshness arm leaves `started && !live` reachable, so CODE-1 withholds `ReportSessionScrub` for every SDK-warm session".** `fresh` is false at sdkwarm.go:221/:260 only when `claimSessionSlotUnderLock` short-circuits on `st.started` under `idempotentRepeat` (slotsession.go:80-86), that is, a REPEAT `ConfigureWorkspace` for a session whose first, fresh call already recorded. So `runtimeLive` holds it and `live` is true. The `fresh` guard is what makes the Settled `started && !live` claim hold, not what breaks it.
- **MISTAKE nearly filed twice: "`cancelPodMCPIfRuntimeIdle` moving into the `removed` branch cancels a co-tenant's armed MCP surface".** Double-guarded by `runtimeIdleLocked()` and `mcpArmingHeldLocked()` (slotsession.go:238-260), and the only arm it newly reaches is a reclaim of the session that armed the surface itself.
- **MISTAKE nearly filed, five mechanism dresses in one round.** (1) The design's race analysis misses the middle ordering where the reclaim's deregistration precedes the start's claim while its `removeSlotTree` runs after it — real, but the same window is written out in full in CODE-5's "When it does not fire" bullet. (2) CODE-4's unconditional `releaseCredentials` releases leases a snapshotless resume-rebuild may still hold — the rebuild re-mints on every attempt and the barred "do not make the lease release conditional" trap covers it. (3) CODE-3's doc-comment gloss drops the "or fails" conjunct — an abbreviated gloss in a Go comment states nothing false. (4) The compensation is sent for an upload-free `stageWorkspace` failure where no pod-side RPC was issued — over-sending an obligation does not violate it, and the staged §4.7 no-op sentence makes the extra send safe. (5) The §29.4 step-13 sentence scopes the co-tenancy gate to a pod serving concurrent sessions while §4.7 states it unconditionally — on a one-session pod a bound co-tenant cannot exist, so the scoped statement is vacuously equivalent.
- **MISTAKE nearly filed: "the placement constraint adds a `WARM_POOL_EXHAUSTED` cause `docs/operator-guide/troubleshooting.md` does not carry".** The table at :36-44 enumerates warmup-failure causes only and is ALREADY incomplete against the shipped tree, where `ErrTenantMismatch` and uptime-expired candidates both surface as `WARM_POOL_EXHAUSTED` and appear in no row. `docs/reference/error-catalog.md:143` is loose the same way. One more instance of a pre-existing enumeration gap does not clear the bar.
- **MISTAKE nearly filed: "spec/15:1136's `recovered with a fresh pod` contradicts the create-time-reserved edge-case bullet's `reconnects to the same pod`".** The bullet lands in no spec file, and the divergence is already shipped: `BindReservedSlot` reconnects from the persisted `row.PodAssignment` and nothing in `pkg/gateway/sessionserver` clears it, so spec/15:1136 and docs/reference/error-catalog.md:156 are already loose for the concurrent create-time-reserved `/start` path.
- **MISTAKE nearly filed: `docs/runtime-author-guide/lifecycle.md:319,:330` against SPEC-1's co-tenancy gate.** Same class as the §11.4 revoke, spec/12:913 and spec/15:1795 traps: the shipped `!boundRemains` gate at session.go:259 already makes it false on a co-tenanted pod, so SPEC-1 restates rather than widens, and the remedy is a docs edit.
- **MISTAKE nearly filed: "the §13.1 sequential-reuse credential-isolation guarantee is broken by the staged exclusive-pod carve-out".** Killed twice: the occupancy-zero whole-pod scrub sweeps the credential residue regardless (spec/05:461 step 0, :471 step 6), and CODE-4's unconditional `releaseCredentials` revokes the lease gateway-side. Same evidence base as the already-refuted exclusive-pod family.
- **MISTAKE nearly filed: "the reclaim's `Runtime.Close` on a co-tenanted pod takes the last-close branch and widens the pod-brick exposure to the failed-bind path".** The widening is real and it is availability rather than a security bound; an ordinary session end reaches the identical state through the identical call, and the pod-brick defect already has its own summary row and its own standing Open.
- **MISTAKE nearly filed, three capacity dresses in the round-2 performance lens.** (1) `ExcludePods` head-of-line-blocks the pool FIFO — `waitInQueue` yields the ticket to the TAIL, so an excluded waiter round-robins and merely burns its own `maxQueueWaitSeconds`. (2) The exclusion pushes retries from pass-1 packing onto pass-2 idle pods and drains warm-pool headroom at Tier 3 — `ExcludePods` names only the pods whose own reclaim went unacknowledged, `maxSlotRetries == 1`, and pass 1 still prefers any other same-tenant claimed pod with capacity. (3) A Redis outage turns the `relErr != nil` arm into a pool-wide drain storm — `applySlotRetryPolicy` already does `RecordLeak` plus `DrainSandbox` on that arm today, and the slot path is fail-closed under §12.4 anyway.
- **MISTAKE nearly filed: "`Binder.Resume` returning a `*SlotBindError` in its chain changes `isTransientPodClaimError`'s answer".** `isTransientPodClaimError` (start.go:3648-3682) matches only `*PoolWarmingError`, `*CredentialAssignmentError`, `*SetupCommandFailure` and five sentinels, and `SlotBindError.Unwrap` returns the cause (slotfailure.go:74-75), so every arm still matches through the new link. No classification moves and no test is owed.
- **MISTAKE nearly filed, four test-coverage dresses.** (1) "The `Resume` rollback has no deterministic tier-1 case" — the tier-7a case drives both arms from one rendezvous keyed on the RPC name, so it is listed; preferring a second framing is a preference between equivalent framings. (2) "The `slot_bind_failed` reason and the resulting `session_complete` frame have no assertion" — `drainReason` normalises and is untouched, so the frame's value set is unchanged. (3) The `isTransientPodClaimError` item above. (4) "SPEC-2's §7.2 step-3 reclaim has no listed test" — the §7.2 terminal-collapse handler is a separate HTTP request holding no adapter connection; the reclaim step 3 names is the one CODE-4's failure branch sends, which the staged resume case covers in all three arms.
- **Do not file the `sessionserver.go` collaborator doc-comment narrowing.** `slotReplacement` (:412-416) and the `SlotReplacement` option (:1658-1663) both say the counter increments "when the slot retry policy drains an unhealthy concurrent-mode pod", and after CODE-5 two of the three `accountSlotFailure` callers are not the slot retry policy. Real and verifiable, and it dies on the bar: a Go doc comment is none of the spec/docs/schemas/charts surfaces, the operator-facing descriptions stay true, and nothing an implementor builds changes. The absence of `sessionserver.go` from `## Files touched on application (non-spec)` is the same non-finding, not a second one.
- **Do not "reconcile" `pkg/adapter/session.go:133,:147,:157` with `:156` and `:163`.** The first three are the `releaseSessionSlot` pre-start branches; :156 is `s.Runtime.Start` and :163 is `s.noteRuntimeStarted`. Both citation sets are correct and they are different lines of the same function. Likewise `pkg/gateway/podlifecycle/podsession/slotbinder.go` has two near-identical connect sequences, `bindReservedSlot` at :230-254 (create-time-reserved) and the tail of `connectSlot` at :449-473 (fresh reservation); both use `slotFailureConnect` and the proposal cites each for different claims, so check which one a citation means before calling it drift.
- **Grepping spec prose for a quoted phrase fails SILENTLY when the phrase wraps across a source line.** "the adapter closes the session runtime" is at spec/29:697 but a one-line grep returns nothing, because "On the default disposition the" ends :696. Grep a short fragment that cannot straddle a wrap, or read the neighbourhood. Related: `ReleaseSlotReservation` is three-argument in the tree today and CODE-4 adds the fourth, so the shipped signature is not evidence that the proposal's four-argument call sites are wrong.
- **The whole-pod scrub is NOT a backstop on the failure path SPEC-3's new clause creates,** although the SPEC-3 rationale's "stays the backstop for anything a per-slot cleanup left behind" reads as though it were: a cleanup that does not complete leaves the slot `leaked`, `leaked` holds occupancy, and the whole-pod scrub fires at occupancy zero, so the backstop is unreachable in exactly the case that needs it. Not filed, because the same hole exists for every shipped leaked slot and before this proposal a failed bind left the credential file with no cleanup at all. A later round tempted by the tenant-isolation framing should stop here.
- **"Removes nothing" in SPEC-1's §4.7 no-op sentence looks like a fresh angle on the refuted "the no-op rule suppresses the whole-pod scrub" finding, and it is not.** The occupancy-zero recycle `Shutdown` both names a session the adapter holds no entry for and removes a great deal, but the refuter's ground (the row's untouched remainder and SPEC-1's §4.1 three-operation sentence both state the scrub gated on the disposition alone) covers the "removes nothing" conjunct identically. Close variant, barred.
- **`recycle.maxSessionsPerPod` and `maxPodUptimeSeconds` are both under `sessionPolicy.recycle`,** so the racing-start bullet's bound leaves a non-recycling concurrent pool apparently unbounded. The bullet UNDER-states: in the racing ordering the reclaim is acknowledged, the gateway releases with `leaked=false`, the Redis counter decrements, occupancy reaches zero and a non-recycling pod is retired by the claim DELETE at `remaining==0` (slotclaimer.go:881-885). Do not re-derive it as an unbounded residue.
- **Do NOT weaken summary.md's `ExcludePod(s)` sentences** (the 0080 §1.19 impacts row's placement clause and the CODE-5 line in the Summary's traps list). The queue-re-entry finding falsified them against the OLD single-valued design; the slice-plus-pointer fix makes them TRUE as written. Weakening both because both findings touched the same table cell would leave the proposal under-claiming a guarantee its own staged code provides.
- **Do NOT read the lagging-release summary row as a second copy of open decision 9, or as an obligation on CODE-2.** Decision 9's producer is the compensating `Shutdown` this proposal adds; the row's producer is the abandoned attempt's own shipped rollback with no compensation involved. Decision 9's "the exposure is created here rather than inherited" is scoped to the reclaim and was deliberately not edited. CODE-2 removes the fifth instance of the pattern without opening the four shipped ones, and reading the row as an obligation re-opens the class fix the round-5 design pass rejected.
- **Do NOT widen the 0080 §1.7 impacts row into §1.7's threshold or ledger question.** This proposal changes neither `slothealth.UnhealthyThreshold` nor `drainLedger.RecordLeak`, and its non-goals record that retuning `ceil(maxConcurrentSessions/2)` is out of scope. Adding "and the clamp still drains an exclusive pod" would restate a shipped-tree fact rather than an impact on 0080.
- **`TestValidTransitions_spec_6_2`'s doc comment says "exactly the six edges the spec enumerates"** (slotstate_test.go:10-11) and CODE-3's staged test change says nothing about it. It is a stale-comment nit, not a gate; the count lives in `len(want)`. Do not spend a finding on it.
- **Do NOT edit a ledger entry in place to correct it.** Ledger blocks are the historical record of what a round decided; the repository's mechanism for correcting one is a `CORRECTS` tag in a later round's shard, and the durable claim is corrected here in `## Standing context` instead. Two round-5 entries carrying the superseded "pointer helper costs no test edits" remedy were corrected exactly that way.
- **Do NOT justify the graceful-window split with `SocketRuntimeProcess.Close`'s SIGTERM wait.** In the shipped sidecar wiring `SpawnPath` is never set, so `cmd == nil` and `Close` returns `p.listener.Close()` with no wait at all. A round already filed and withdrew that dress. Justify with `MCPRuntime` (the §5.1 `type: mcp` runtime, which does read the plumbed deadline), the `terminate` frame's required `deadlineMs`, and the §11.4 precedent. EVIDENCE: `grep -rn "SpawnPath" cmd/ pkg/ --include=*.go` outside tests returns only the field declaration and its own doc.
- **MISTAKE: the finding that produced the split cited `InProcessRuntime.Close` blocking on `<-done` as evidence.** It is not evidence: that method's signature is `Close(_ context.Context, …)`, so it discards every deadline and no value of `deadlineMs` changes its behaviour. The same holds for `executor.SubprocessExecutor.Close`. A fixer who copies the finding's evidence list into the proposal lands two false claims. Cite `MCPRuntime` and `SocketRuntimeProcess` instead.
- **Do NOT "simplify" the split back to a single bound.** `contextWithGraceDeadline` returns the PARENT UNCHANGED for a non-positive grace, which is exactly what made the pre-split `deadlineMs: 0` set the close grace to the full remaining RPC budget and produced the confirmed "a completed reclaim can never be reported as cleanly reclaimed" defect. Collapsing the two bounds re-opens it.
- **Any change to the split must also touch the tier-1 per-stage assertion.** The per-stage compensation table asserts a positive `deadlineMs` equal to half the budget and strictly less than the RPC deadline the same pool configuration produces. Changing the call without changing that line leaves the staging self-contradictory, which is how the `deadlineMs: 0` form survived several rounds.
- **MISTAKE nearly filed, three dresses on the split.** (1) "`budget/2` at the 5s floor gives the adapter 2.5s, below §5.2's `minimum 5s enforced at runtime by the adapter`" dies on the reading: §5.2's minimum is on the SLOT CLEANUP timeout while `deadline_ms` bounds only `Runtime.Close`, the RPC deadline still carries the whole figure, and the adapter enforces no per-slot cleanup timeout at all. (2) "the reclaim's `terminate` frame carries a value no operator doc anticipates" dies because no spec or doc pins the value and every `deadlineMs` occurrence in `spec/` and `docs/` is the interrupt frame or an illustrative `10000`. (3) "`SocketRuntimeProcess.Close`'s doc comment names one gateway window while a second caller pins a pool-derived one" is a Go doc comment, killed by the standing collaborator-doc-comment trap.
- **MISTAKE nearly filed: "the residual headroom is the wrong half".** With `budget/2` pinned as the close grace, the adapter's remaining work (`emitFinalUsage`, the frame send, `removeSlotTree`'s four `os.RemoveAll`s, the response) has to fit in the other half, so the harm looks moved from the close to the tree removal. It dies twice over: before the split `Runtime.Close` could consume the WHOLE budget, so the split is strictly better; and on the main residue class, bound-but-unstarted, `started` is false, no close runs at all, and the whole budget goes to the tree removal.
- **MISTAKE nearly filed: "the split makes a slow runtime report unclean, inverting the defect it closed".** Neither runtime `Close` reports the SIGKILL pivot as an error, so `closeErr` is grace-independent and `ExitedCleanly` is unaffected by the halving. The classification for the slow band is identical before and after; what changes is that the fast band now reports a completed cleanup as completed.
- **MISTAKE nearly filed: "the rollback runs `Runtime.Close` on a cancelled inbound ctx, so the teardown is skipped".** Closed by the Settled entry on the three `Close` implementations and re-verified twice this window: a cancelled ctx makes the close harsher, never absent, and the active-set removal the rollback exists for always lands. Do not rebuild it.
- **MISTAKE nearly filed: "a successful retry's later release erases pod A's leak record".** `MarkReleased` has no production caller at all. The residual is real and cosmetic: a second leak for the same session on a different pod publishes the second pod's gauge with the first pod's count, and the drain threshold is keyed by pod and unaffected.
- **MISTAKE nearly filed: the phantom `SandboxClaim` and the stuck Sandbox finalizer.** "The `leaked` release leaves a non-terminal claim holding the Sandbox's session-cleanup finalizer, so the pod wedges in Terminating and trips `FinalizerStuck`." Two things kill it: orphan GC does not merely drain, it drains and then `Delete`s the claim, which releases the finalizer; and while a live co-tenant references the pod the finalizer is legitimately held, because `evaluate` gates on `PodHasActiveSession`. It is also pre-existing, since `leaked = err != nil || !cleanly` fires on every ordinary session end.
- **MISTAKE nearly filed: `DrainSandbox` from the new `resumeOnPod` caller as a gateway write into controller-owned state.** §4.6.3's Gateway ServiceAccount paragraph grants `get`/`patch` on agent Pods precisely for the `lenny.dev/drain-request` annotation, and `Binder.DrainSandbox`'s own doc comment records that routing as the §4.6.3-conformant form. A new caller of an already-blessed mechanism is not an idiom violation.
- **MISTAKE nearly filed twice: CODE-2's `Resume` rollback answering `Aborted` demotes the session terminally.** `isTransientPodClaimError` maps neither `Aborted` nor `FailedPrecondition` to transient, so the resume rollback's code looks load-bearing. It is inert: the rollback fires only after the gateway's own call has failed and issued the compensation, so the code goes to a caller that has already abandoned the RPC, and both candidate codes give the same classification there. The rationale's gloss ("for the classification reason the `StartSession` rollback records above") is inaccurate, because no §5.2 classifier runs on the resume path; the fix is one clause, never a code change.
- **MISTAKE nearly filed: "`Aborted` makes a start refused after a §11.4 revoke retryable, where `FailedPrecondition` would have been terminal".** In the SHIPPED tree `noteRuntimeStarted` has no return value and no rollback exists, so the racing start simply succeeds today. Aborted-plus-retry is strictly more conservative than what ships, and less strict than it could be is not a finding.
- **MISTAKE nearly filed: "CODE-1 widens `removeSlotTree` from `bound` to `removed`, so a lagging reclaim now deletes a SUCCESSOR's workspace and credential directory".** Real, and it is the exact hazard `ExcludePods` is staged for; the one path `ExcludePods` does not reach, a create-time-reserved `BindReservedSlot` retry returning to the same pod through `row.PodAssignment`, is already a recorded accepted failure mode.
- **MISTAKE nearly filed: "an empty session id collapses the path to the slots root and destroys every co-tenant's workspace".** Dies on `st.paths` being resolved from the map key at `ensureSlotStateLocked` rather than from `st.sessionID`, and on `ValidateSlotID` refusing an empty id before an entry can exist. This is the check worth doing on any future edit that moves the `bound` gate.
- **The honest security residual, and why it is not a finding.** A COMPROMISED adapter answering `exited_cleanly: true` on a reclaim it never ran now buys the Redis decrement, exemption from the persistent leak count, and exemption from `ExcludePods`. Each is more permissive than today only if today were stricter, and today is not: `ReleaseSlotReservation` hard-codes `leaked=false`, the reserved and resume paths reach no tracker, and no exclusion exists. A later round needs a path where the lie makes the gateway DO something it does not do today, rather than fail to do something new.
- **MISTAKE nearly filed: "the compensation blocks the client request goroutine for up to `cleanupTimeoutSeconds`".** Real and quantified (at the documented default of 60s a `maxConcurrentSessions: 2` pool gives a 30s block per failed bind, payable twice, and an exclusive pool reached through `Binder.Resume` degenerates to the full 60s), and it does not clear the bar: §16.5's creation SLO excludes the bind, and the startup P95's burn-rate expression divides by the startup histogram's count, which a sub-5% bind-failure rate cannot move.
- **MISTAKE nearly filed: "the third `accountSlotFailure` caller destroys a warm pod per failed §7.3 re-attach, where nothing is destroyed today".** It dies on the shipped baseline: a failed `Binder.Resume` already leaves the replacement pod's claim `bound` with no active session and §4.6.1 orphan GC drains it. The pod is lost either way, so this is a churn-timing change rather than net warm-pool consumption. Anyone re-deriving the Tier-3 correlated-resume-storm framing must answer this baseline first.
- **MISTAKE nearly filed: "the resume path's release runs on the dead caller context, so `Leaked` is true even when the reclaim succeeded".** The mechanism is real, and the caller's ctx being dead IS the design's own primary trigger. It dies on three grounds: the leak is HONEST, because the Redis increment genuinely was not released, so `leaked` is the correct disposition; the stranding is pre-existing at every release site; and the proposal states and tests the arm. What is left is "the release should be detached too", which is hardening on shipped code.
- **MISTAKE nearly filed: "§7.2 step 3's reclaim is ordered before a pod release that step 1's cancellation makes impossible".** Killed by the Settled fact that §7.2 steps 1 and 3 have no implementation on the DELETE path, so the deterministic-cancellation premise is not reachable in the tree and correcting step 3 creates no code obligation.
- **Do not file `non-spec-changes.md:442` ("No code under `pkg/`, `cmd/`, or `sdks/` reads the reason value").** It is literally self-contradictory two sentences after the same paragraph names `drainReason` as the reader, and three lenses have verified and declined it. `grep -rn "GetReason()" pkg/ cmd/ sdks/` settles it in one command: one non-test reader. The intended reading is "nothing branches on it beyond the normaliser", and a fixer touching that paragraph should narrow it to those words.
- **`recordingShutdownAdapter` records the request and NOTHING about the RPC context** (binder_test.go:1167), while the new tier-1 assertion pins `deadlineMs` "strictly less than the RPC deadline that same configuration produces". The fixture paragraph enumerates the fake's new capabilities and does not name capturing `ctx.Deadline()`. Three lenses declined it as test mechanics of the same class the material skeptic already refuted; a fixer already editing that paragraph should add the clause, and a reviewer should not spend a finding on it.
- **Do NOT file the reason-value or status-code tables in `docs/`.** `docs/reference/adapter-contract.md:210-216` gives per-value meanings for a `reason` set that already lists `"drain"`, which is not in the four-value enum, and the shipped §11.4 revoke already sends `"USER_REVOKED"` down the same normaliser; `docs/api/internal.md:490-500` is a gRPC status table carrying neither `ABORTED` nor `INVALID_ARGUMENT`, both of which the shipped adapter already returns; `docs/reference/adapter-contract.md:208` says "sends SIGTERM, then SIGKILL after 10 seconds" where `SocketRuntimeProcess.Close` SIGKILLs at the grace deadline with no SIGTERM. All three are pre-existing and untouched by this proposal.
- **`pkg/adapter/sessionscrubreporter.go:79-81` claims "the next release's idempotent re-report are the backstops" for a failed `ReportSessionScrub`.** That is false against `RecordSessionScrub`, which has no per-session dedup key. Pre-existing code comment in a file no deliverable opens; a later reliability lens will find it and should not spend a round on it.
- **A future edit that moves §6.2's scoped block BELOW the general header inverts both tier-11 slices silently,** because `scopedBlock` is `s62[scopedHeader:generalHeader]` and `generalBlock` runs to the end of the section. The current ordering (scoped at :146-148, general at :150-156) is what makes the `generalSlotEdges` addition satisfy the positive loop and the negative loop at once.
- **A future §5.2 gate must anchor BELOW SPEC-3's append.** The append writes the literal `**Slot cleanup:**` into the `**Scrub model.**` paragraph at spec/05:453, ninety lines above the real bullet at :545, so a `lineContaining(s52, "Slot cleanup")` gate would resolve to the append. No gate anchors on it today (`grep -rn "Slot cleanup" tests/ scripts/ pkg/ cmd/` returns only comment prose), which is the only reason it is safe.
- **MISTAKE that cost a whole design round: minting the bind epoch PER REGISTRY ENTRY.** The hand amendment did that and then concluded, in the create-time-reserved edge case and in the summary, that "neither the tree the retry staged nor the session it started can be taken down by the previous attempt's reclaim". With per-entry minting that conclusion is false in the one ordering the amendment exists for: `ensureSlotStateLocked` returns the existing entry, `claimSessionSlotUnderLock` gates only on `st.started`, so the successor inherits the predecessor's epoch and the fence compares EQUAL. Six lenses reached the same hole independently. The fix is per-admitted-attempt minting plus the caller echo; do not re-derive the per-entry form.
- **TRIED AND WITHDRAWN, five alternatives to the per-attempt epoch.** (a) Mint per entry and re-mint on the start claim with a latest-wins latch — a lost `StartSessionResponse` leaves the caller naming the pre-start epoch, so the real started-session residue is answered `superseded` and leaks silently. (b) Bump on every bind RPC with no request fields — same lost-response failure, and the lost response is the COMMON case, because the compensation exists precisely when the caller's context expired mid-RPC. (c) A gateway-minted attempt UUID — carries no order, so an earlier attempt's straggler is adopted back and fences the live successor out; the adapter's monotone counter is what supplies the ordering. (d) Refuse every epochless bind against an existing entry, needing no request fields — refuses the attempt's OWN second RPC, because `ensureSlotPaths` runs at the top of three staging handlers. (e) Accept the inheritance hole as a residue — it is a running session's process group killed.
- **MISTAKE: the §4.7 `Shutdown` row fenced the SLOT RELEASE alone and left the runtime teardown on its own precondition** ("a session whose start the adapter has admitted"), which a superseded successor satisfies. A conforming third-party adapter reading the row literally would flush usage, drain and close a live session's runtime on a mismatch. The staged Go was already right (CODE-1 returns before any teardown); the spec was the defect. The fence must be a precondition on the WHOLE request.
- **WATCHOUT: epoch rule 1 must exclude the cleanup interval, or it contradicts the reclaim hold.** During a cleanup the entry is DEREGISTERED, so "the adapter holds no entry" is true and an epochless bind would be admitted straight into the interval the hold exists to protect. Both staged blocks carry the "no cleanup for the identifier is running" clause; a reword that drops it re-opens the window.
- **WATCHOUT: do NOT make the superseded-attempt refusal permanent** to "stop the dead attempt retrying". The standing Trap on CODE-2's rollback records exactly that mistake: a permanent code turned a transient race into a non-retryable 422 and contradicted staged §7.1's "Nothing about the attempt's retryability changes". What stops a stray retry stealing a running session is the "epochless request against a started entry is refused" clause, not the error category.
- **WATCHOUT: "occupancy" deliberately SURVIVES in two senses and must not be swept to "hold".** The epoch's ownership sense ("the bind attempt that owns a slot identifier") and the pre-existing pod-level Redis slot-counter sense (spec-changes.md:51,:55,:137,:152; non-spec-changes.md:771,:778; summary.md's leaked-occupancy block) both stand. Only the adapter-local interval was renamed. A later round that mass-renames will re-merge the two mechanisms the rename just separated.
- **MISTAKE, the FIFTH occurrence of the unscoped `leaked` terminal, and it was the amendment's.** The new §5.2 hold paragraph closes with an unqualified "a cleanup that exceeds it leaves the slot `leaked`", one paragraph below the sentence pair that an earlier round split by concurrency to fix exactly this, and SPEC-4's :580 exclusivity sentence ("lasting until the cleanup reaches `released` or `leaked`") reintroduces the terminal claim G1 had deleted. Both filed. The pattern rule stands: ANY sentence appended to the concurrency-independent `**Scrub model.**` paragraph that names `leaked`, the trigger or the gauge needs its own scope, and the two sites live in different deliverables so a fixer treating them as one leaves one standing.
- **DELETED AND REPLACED, a MISTAKE that would have broken a correct staging: "`AssignCredentialsResponse` is not empty, so `bind_epoch = 1` is a hard field-number collision; the free number is 6".** It is FALSE. The message is `message AssignCredentialsResponse {}` and field 1 is free; the field set the old entry quoted is `RotateCredentialsRequest`'s. The artifact that produced it is reproducible and worth knowing: `awk '/^message AssignCredentialsResponse/,/^}/'` never terminates on a ONE-LINE message, so the range runs on and prints the NEXT message's body. Use `grep -n "message X"` then `sed -n` when checking a proto field number. Renumbering SCHEMA-1 to 6 on the strength of the old entry lands a defect; eight lenses filed the correction.
- **MISTAKE: spec-changes.md:178-181's reverse-ordering argument has the wrong subject.** It says "the START'S OWN claim re-creates the registry entry" and then fences it with "the epoch its own claim observed", which is the epoch that claim just minted, so the ordering it posits is not refused by the epoch and is past the hold by construction. summary.md:100-102 states the same case correctly ("a RACING claim re-creates the entry a start confirmation reads"). Filed; whoever fixes it must decide whether the bullet meant a SUCCESSOR's claim, in which case the abandoned-start-re-creates ordering is an unrecorded residue.
- **WATCHOUT: dropping the "which is why that refusal is absent from the **Non-retryable failure categories** list below" clause forces a matching edit** to the `## Spec sections deliberately untouched` bullet, which currently says the list is "named only to state that the reclaim-in-progress refusal is absent from the list". Leave the bullet saying the list is untouched and why, without the cross-reference.
- **The glob `*spec-changes.md` in this proposal directory matches BOTH `.spec-changes.md` and `.non-spec-changes.md`,** so `sed -n 'N,Mp' *spec-changes.md` concatenates them and every line number you read is wrong. Use the full filenames. This has bitten at least one round.
- **WATCHOUT: the §4.1 replacement justifies the epoch by analogy to a Kubernetes `DeleteOptions` precondition,** and the analogy is unsound on error semantics: a real precondition mismatch is a 409 Conflict, while a mismatched epoch here is a SUCCESSFUL `superseded` response. Judged below the bar because the sentence claims only the classification. A later lens will notice it; the reasoning that killed it is here so it is not re-litigated.
- **MISTAKE nearly filed: "SPEC-1 defines `reclaimed` as 'held the named entry and released the slot', so a cleanup that fails has no outcome value and the response reports `reclaimed` for a leaked slot".** §15.4 frames the enum as the epoch comparison's outcome and the leak discriminator stays `exited_cleanly`, so the drift is definitional at worst and a verifier reads "released the slot" as "performed the release".
- **MISTAKE nearly filed: "the new adapter-local hold is invisible in the pod's `/healthz` `leaked_slots` count and in the Redis occupancy §6.2:160 defines, so an operator sees free capacity on a pod that refuses binds".** Dies because the hold is per slot identifier and the identifier is the session identifier, so no other session can collide with it and no placement decision is affected.
- **MISTAKE nearly filed: "the epoch fence is unobservable, because the rollback's status code reaches a caller that has already given up".** It is observable on three routes: a §11.4 revoke racing a live `StartSession`, `Binder.Launch`'s exclusive `StartSession` at binder.go:1013 (the same adapter call site), and the staged tier-7a rendezvous. Do not file "the code is unobservable".
- **`pkg/adapter/session.go:163` is reached by BOTH the concurrent `materializeSlot` start stage and the exclusive `Binder.Launch`** (binder.go:1013, the `else if` arm), so CODE-2's rollback also fires on the exclusive path, where `failPhase` retires the pod. The proposal never claims otherwise and the outcome is fail-closed, but a reader who assumes "sdkwarm is the only exclusive site" misreads it; `sdkwarm.go:261` is the OTHER arm of the same if/else.
- **The "rollback destroys no successor" case discriminates on the UNBOUND arm only, and its closing sentence must say so.** In the bound sub-case the successor's `claimSessionSlot` sets `st.sessionID` under the same key, so CODE-2's confirmation succeeds and the rollback body never executes; a re-added `releaseSessionSlot` cannot run there. Only the unbound arm, where `ensureSlotStateLocked` leaves `st.sessionID` empty, turns red if a registry release is added back.
- **The "rolled-back start can close a successor's runtime session" failure mode has TWO open branches, not one.** Bounding it to "the branch on which `ExcludePods` does not fire" is refuted by the proposal's own create-time-reserved carve-out: `bindConcurrentSlot` routes a NON-RECOVERY row with a `PodAssignment` through `BindReservedSlot` and never enters `runWithQueue` or `applySlotRetryPolicy`, so the §5.2 placement constraint does not reach a §15.1 retry onto a create-time-reserved slot at any disposition. The two branches are the policy-placed retry after an acknowledged reclaim with a clean release, and the create-time-reserved retry the policy never places. CORRECTED: the guard is a conjunction (`row.PodAssignment != "" && !session.IsRecovery(row.State)`, start.go:2595), so the unqualified form of that sentence, which five rounds copied, is false for a recovery-state row; see the Settled entry on the three attempt kinds. The two-branch conclusion is unaffected, because a recovery-state row IS policy-placed and the exclusion covers it.
- **MISTAKE nearly filed: "the compensation's residual headroom is the wrong half".** With `budget/2` pinned as the close grace the adapter's remaining work has to fit in the other half, so the harm looks moved from the close to the tree removal. It dies twice: before the split `Runtime.Close` could consume the WHOLE budget, so the split is strictly better; and on the main residue class, bound-but-unstarted, `started` is false, no close runs, and the whole budget goes to the tree removal.
- **TRIED AND WITHDRAWN, and this is the one that must survive: the PER-ATTEMPT bind epoch with a request-side echo.** Round 5 minted the epoch per admitted bind attempt, put an `int64 bind_epoch` on all seven bind-sequence REQUESTS, gave §4.7.1 two admission rules (an epochless request admitted only when no cleanup is running and the entry's session has not started, mints and reports a fresh epoch; an epoch-carrying request admitted only at that epoch), and re-scoped the caller echo to the session's later requests on the reporting connection so the §7.4 mid-session upload stayed admissible. Round 6 reverted the whole thing to per-entry minting with no request field. Do not re-derive the per-attempt form as a fix for the inheritance hazard without answering why r6 withdrew it; do not "fix" SCHEMA-1 back to sixteen fields.
- **Do NOT re-file the per-entry inheritance hole as a fresh finding.** Under per-entry minting a retry that reaches the pod before the lagging reclaim's critical section RESOLVES the abandoned attempt's surviving entry and inherits its epoch, so the reclaim compares EQUAL and can tear down the retried session. The proposal now OWNS that in two accepted-failure-mode bullets. What is still filable is a place where the surrounding prose claims the fence closes it.
- **MISTAKE, twice, the same shape: a design reversal leaves behind every sentence a grep on the new vocabulary misses.** The r6 revert rewrote three sites to entry vocabulary (§4.7 row, §7.1's "On superseded" clause, §4.7.1) and left §7.1's `superseded` TRIGGER sentence and the Design in ATTEMPT vocabulary, which diverge exactly on the shared-entry case; the same pass rewrote the "retry meets the hold" edge case to say a policy-placed retry never meets it while leaving §7.1 asserting that it does and spends a policy attempt. When judging a fix here, grep the staged NORMATIVE blocks for the claim, never only the narrative.
- **MISTAKE: the r5 fix that re-based the epoch rewrote the edge-case bullet and left the staged §7.1 paragraph and the Design narrative asserting the opposite.** Same class as the earlier "two rule-stating sites still give the exclusion trigger as an unacknowledged reclaim". A fix round that changes the narrative and leaves the normative text behind is this proposal's most repeated failure.
- **MISTAKE that cost a round: the r1 fix that closed a too-wide-predicate finding overshot into a false universal.** It replaced §4.7.1's wide epoch-reporting predicate with a closed seven-item list PLUS "Every other RPC on this contract neither carries nor reports a bind epoch", which excluded `Shutdown` three paragraphs above the rule requiring the caller to echo an epoch on a `Shutdown`. Five lenses filed it in one round. A conforming third-party adapter following the §15.4 copy would have ignored the fence and torn down a successor's session. When narrowing a predicate, check the narrowing against every mechanism the section already fences.
- **Do NOT file the wide "create or resolve a registry entry" predicate as over-reaching.** §5.2 and §15.4 both use it, and it literally reaches `Attach`, `Interrupt`, `SendMessage`, `Checkpoint`, `CoordinatorFence`, `ExportPaths`, `ReportUsage` and the three credential RPCs while CODE-6 refuses only inside `ensureSlotStateLocked`. It dies because the hold starts at the DEREGISTRATION, so during the hold no entry exists and the "or resolve" half is vacuous: those RPCs find nothing and answer their own not-found, and only the creating half is reachable. Four lenses built it; the refuter's ground is that "create or resolve" is this change's own vocabulary for the bind-sequence RPCs.
- **Do NOT file the §7.1 "spends one of the attempts the retry policy allows" pair against "§5.2 keeps policy-placed retries off that pod".** Four lenses read it as consequence-then-mitigation and declined; round 7 reworded it anyway to "an attempt that meets the hold is one the retry policy did not place". Read the current text before filing.
- **Do NOT file the reclaim hold's bound as unenforced, in any dress.** The published bound is descriptive: the adapter enforces no per-slot cleanup timeout, CODE-6 releases the hold on a plain `defer`, and `removeSlotTree` takes no context. Three rounds refuted it on materiality, and the round-7 fix then went ONE STEP PAST the refutation by declaring the removal deadline-free, which is the reading the refutation rejected and which collides with the `**Slot cleanup:**` bullet the same block says stands verbatim. That collision is the only live form; argue it from the bullet, not from the absence of enforcement.
- **Do NOT file spec/10:30's generation validation against the adapter.** spec/10:30 says pods validate `coordination_generation` on every gateway→pod RPC and the proto comment repeats it, while the adapter validates it only in `CoordinatorFence` and `CheckpointBarrier`. Any staged sentence asserting the check restates published spec; the divergence is pre-existing and unowned. Six lenses reached it.
- **Do NOT file the §4.7 row's "precondition on the whole request" against the whole-pod scrub.** The row enumerates two consequences (slot release, runtime teardown) and leaves the recycle-disposition scrub unnamed, which reads as a granularity mismatch. It is unreachable: only a fenced compensation carries an epoch and it never sets the recycle disposition, and the occupancy-zero recycle is a separate epochless `ShutdownRecycle`. Five lenses; 2:1 against the outlier phrase.
- **Do NOT file "the §4.7 no-op sentence suppresses the occupancy-zero recycle scrub".** The sentence enumerates the two teardowns, the scrub is neither, the row's UNEDITED tail states the scrub unconditionally, and SPEC-1's §4.1 replacement keeps "runs the whole-pod scrub when the recycle disposition is set". Check the unedited tail before building this one.
- **Dead end: the `PrepareWorkspace` per-frame epoch mint.** Because the RPC is client-streaming and a bare `int64` is zero on later frames, an implementer applying the rules per MESSAGE would re-mint per chunk. It dies because the shipped adapter resolves the entry once from the first frame, the caller learns the epoch from the single response, and ownership lands on the last mint. Two lenses.
- **Dead end: "`ConfigureWorkspace` is documented idempotent while the admission rules refuse an epochless request onto a started entry".** It died even under the r5 design (within one attempt the caller echoed its epoch; across attempts `Binder.Launch`'s failure reclaims onto a fresh pod), and the r6 revert dissolves it entirely. Two lenses.
- **Dead end: "the `DemoteSDK` fallback bind meets the reclaim hold".** `releaseSessionSlot` runs synchronously inside `DemoteSDK`, so the hold it opens is over before the RPC answers; and §4.7:673 routes a FAILED `DemoteSDK` to pod failure and a replacement claim, so the fallback runs only after a successful return. Six lenses built and dropped it; the only surviving form is a third-party adapter that cleans up asynchronously.
- **The §5.2 reclaim-hold paragraph is ONE physical line carrying eleven independent normative sentences.** Read the whole line before editing any clause; a grep anchored on one sentence misses its neighbours. This is the same disease already recorded for §7.1's long line, and three findings in one round landed inside it.
- **MISTAKE nearly filed, the security family, five dresses in one round and four in another.** `superseded`/`absent` as pod self-reports exempting a slot from the leak accounting; a compromised adapter refusing every fenced teardown; the epoch as a guessable small integer; the exclusive-pod carve-out returning a recycling pod carrying credential residue; a late `StartSession` re-creating the entry and leaving an orphan holding live credentials. Each dies on the standing bar that the lie must make the gateway DO something it does not do today, on the pre-existing `leaked = err != nil || !cleanly` self-report, or on the cleanup having removed `CredentialsDir` and cancelled the §4.9 timers first.
- **MISTAKE nearly filed: "the hold refusal is accounted as an ordinary transient slot failure, so one refusal drains a healthy pod at `UnhealthyThreshold(2) == 1`".** The mechanism is real end to end through `accountSlotFailure` → `RecordFailure` → `DrainSandbox`. It dies because an ACKNOWLEDGED compensation finishes its cleanup and releases the hold before the gateway returns the error, so the client's retry meets the hold only in the client-timeout race, where the reclaim is usually unanswered and routes to `RecordLeak`/`ExcludePods` instead. Two rounds.
- **MISTAKE nearly filed: "§15.4's `Shutdown` epoch comparison is a TOCTOU".** An epochless successor can mint a fresh epoch between the compare and the deregistration. It dies on the staged wording: "performs the slot release and the runtime teardown only when the two are equal" is a condition on the PERFORMING, so an implementation whose entry changed epoch between compare and act is already non-conformant. Adding "atomically" is clarification.
- **Do NOT use the no-re-dial rule as the ground for the epoch-restart argument.** The rule does not carry the case: a `grpc.ClientConn` reconnects transparently, so the connection survives an adapter process restart. The ground is `RestartPolicy: Never` on the agent podspec. Two rounds recorded the wrong ground and the third caught it.
- **Do NOT "tighten" §4.7.1's two-connection clause into its strong reading.** "Both connections resolve one entry, so both observe one epoch" is true as a claim about the VALUE and false as a claim about both connections having observed it: on the exclusive path `Binder.Launch`'s fresh connection observes nothing when its only bind RPC fails, and its compensation is the unconditional form. The preceding clause already covers that case, and the exclusive Launch failure runs `failPhase` and retires the pod, so the unfenced form has no successor to destroy.
- **Do NOT file the started-session refusal gap inside this proposal.** spec/04:672's `StartSession` row states no refusal while `claimSessionSlotUnderLock` refuses a repeat start with `codes.Unavailable`, and two of this proposal's edge cases depend on that refusal surviving. It is a pre-existing spec gap 0081 neither causes nor repairs, and enumerating the refusals inside §5.2 or §15.4 would mint unreviewed normative surface. File it separately if anyone wants it.
- **Do NOT file a §15.4 completeness finding.** Three have been refuted on the ground that a semantically-stated requirement plus a correct first-party implementation is not a correctness defect: the missing wire spelling of an epochless `Shutdown` (since fixed anyway), the missing status code for the hold refusal, and the missing per-outcome response rule. A fourth lands on the same refutation.
- **Two sub-threshold citation drifts, recorded so nobody re-derives and then files them.** `spec-changes.md` cites spec/29:697 for step 12's "the adapter closes the session runtime", which is on :698; and it says §7.1's atomicity paragraph "closes by stating that the client never receives a `session_id`" when that sentence is mid-paragraph and the same document quotes the real closing sentence correctly elsewhere. Neither changes the argument it supports.
- **MISTAKE that cost SIX groups and produced no edit: findings generated against a stale snapshot.** All six `spec.1.fix-*` groups were handed findings written against the hand-amendment commit 1344047d4 while the tree stood at 2aca9851b, roughly eighty lines later. Three resolved to NO EDIT because the quoted text no longer exists anywhere (`grep -n "tables above"`, `"bounds the occupancy"`, `"Slot-identifier occupancy"`, `"On either incoming edge"` all return nothing outside the review logs), one was a no-op on a bullet already deleted, and two verifiers confirmed a finding against line numbers eighty lines stale before anyone checked the string. The rule: verify a finding's quoted STRING against the current file before designing a fix, and never trust its line number.
- **MISTAKE, the SIXTH filing of the unscoped `leaked` terminal and the SECOND against text already repaired.** Occurrences four and five are recorded above; the sixth was filed by two `spec.1` groups against spec-changes.md:522 and :580, both of which had been rewritten in the interval. The pattern rule still holds and the pair of sites is what is stale. Every remaining `leaked` mention in the staged spec (:51, :390, :600, :624, and the two accepted-failure bullets) has been swept and is concurrency-scoped; the `**Slot-identifier reclaim hold.**` paragraph deliberately names no `leaked` terminal at all.
- **MISTAKE: SPEC-5 round 1 described a field set the proto does not have.** It asserted the epoch and `coordination_generation` "travel on the same messages", on the section that is the normative gateway-adapter contract. Only `ResumeRequest` and `ShutdownRequest` carry both. CORRECTED AGAIN by the r6 revert: with no bind-sequence request carrying an epoch, `ShutdownRequest` is the only message carrying both, and the live form is "Where both appear on one message, as on `Shutdown`, each is checked on its own terms". Do not restore `Resume` to that sentence; the r5-era correction this entry recorded is the stale one.
- **MISTAKE: the round-1 fix that narrowed a too-wide predicate overshot into a false universal, and five lenses filed it in one round.** It replaced §4.7.1's wide epoch-reporting predicate with a closed seven-item list PLUS "Every other RPC on this contract neither carries nor reports a bind epoch", which excluded `Shutdown` three paragraphs above the rule requiring the caller to echo an epoch on a `Shutdown`. A conforming third-party adapter following the §15.4 copy would have ignored the fence and torn down a successor's session. When narrowing a predicate, check the narrowing against every mechanism the section already fences.
- **MISTAKE: the two-start-points defect was fixed at §5.2 and §15.4 and reintroduced at the §6.2 site.** An earlier round converged §5.2 and §15.4 on the deregistration and did not sweep §6.2, whose sentence keyed the hold on the DURATION of `slot_cleanup`, a state entered a whole gateway round trip earlier. The §6.2 sentence looked like a harmless restatement and its own rationale said it minted no edge, so a reader who checks only that passes over it. The defect is the quantifier, not the edge.
- **MISTAKE: the r7 fix went one step PAST the round-6 refutation.** The refuted finding said the hold's stated bound did not cover the tree removal; the refutation's ground was that the cleanup ends at or before the per-slot timeout. The fixer answered by declaring the removal deadline-free, which is the reading the refutation rejected, and it now collides with the `**Slot cleanup:**` bullet the same block says stands verbatim. That collision is the only live form of the family; argue it from the bullet, never from the absence of enforcement.
- **Do NOT re-run the anchor sweep by hand; it is a ten-line script and it has come back clean in every round of this window.** Extract the fenced blocks after `## Staged edits` and `str.count` each across `spec/*.md`: blocks 0,2,5,8,10,12,14,16,18,20,22,24 give count 1 and every replacement or insertion gives 0. At least ten independent lenses reproduced it with identical results.
- **spec/06 carries TWO near-identical per-slot sub-state heading lines inside one fence,** :146 "Per-slot sub-states scoped to concurrent occupancy (tracked per session):" and :150 "Per-slot sub-states (tracked per session, not as pod-level phase; a pod of either concurrency):". SPEC-4 quotes :150 verbatim so it is unambiguous, but a paraphrased anchor here would not be.
- **Dead end, six lenses: "the `DemoteSDK` fallback bind meets the reclaim hold".** `releaseSessionSlot` runs synchronously inside `DemoteSDK`, so the hold it opens is over before the RPC answers, and §4.7:673 routes a FAILED `DemoteSDK` to pod failure and a replacement claim, so the fallback runs only after a successful return. The only surviving form is a third-party adapter that cleans up asynchronously.
- **Dead end, two lenses: "`ConfigureWorkspace` is documented idempotent while the admission rules refuse an epochless request onto a started entry".** It died even under the r5 per-attempt design (within one attempt the caller echoed its epoch; across attempts `Binder.Launch`'s failure reclaims onto a fresh pod), and the r6 revert dissolves it entirely, because no bind-sequence request carries an epoch.
- **Dead end, two lenses: the `PrepareWorkspace` per-frame epoch mint.** Because the RPC is client-streaming and a bare `int64` is zero on later frames, an implementer applying the rules per MESSAGE would re-mint per chunk. It dies because the shipped adapter resolves the entry once from the first frame, the caller learns the epoch from the single response, and ownership lands on the last mint.
- **Do NOT file the §4.7 row's "precondition on the whole request" against the whole-pod scrub, in any of its dresses.** The row enumerates two consequences and leaves the recycle-disposition scrub unnamed, which reads as a granularity mismatch. It is unreachable: only a fenced compensation carries an epoch and it never sets the recycle disposition, the occupancy-zero recycle is a separate epochless RPC, and SPEC-1's §4.1 replacement keeps "runs the whole-pod scrub when the recycle disposition is set". Five lenses; 2:1 against the outlier phrase.
- **Do NOT "tighten" §4.7.1's two-connection clause into its strong reading.** "Both connections resolve one entry, so both observe one epoch" is true as a claim about the VALUE and false as a claim about both connections having observed it: on the exclusive path `Binder.Launch`'s fresh connection observes nothing when its only bind RPC fails, and its compensation is the unconditional form. The preceding clause already covers that case, and the exclusive Launch failure runs `failPhase` and retires the pod, so the unfenced form has no successor to destroy.
- **Do NOT file spec/10:30's generation validation against the adapter, and do NOT file §4.7.1's restatement of it.** spec/10:30 says pods validate the generation on every gateway→pod RPC and the proto comment repeats it, while the adapter validates it only in `CoordinatorFence` and `CheckpointBarrier`. Any staged sentence asserting the check restates published spec; the divergence is pre-existing and unowned, and the `f1` firing recorded it as a shipped-defect row. Six lenses reached it.
- **Do NOT file a §15.4 completeness finding.** Four have now been refuted on the ground that a semantically-stated requirement plus a correct first-party implementation is not a correctness defect: the missing wire spelling of an epochless `Shutdown` (fixed anyway), the missing status code for the hold refusal, the missing per-outcome response rule, and the zero-frame `PrepareWorkspace` response. A fifth lands on the same refutation.
- **Do NOT file the `concurrent_slots_exhausted` gloss against the placement exclusion, again.** spec/05:549 defines the reason as "pods exist but all slots are full" while the exclusion makes a not-full pod produce it, but `ClaimSlot` already returns `ErrNoConcurrentSlot` for every non-tenant-mismatch candidate it skips, and `ErrTenantMismatch` maps to the same reason. Six lenses across the window have now built and dropped this.
- **MISTAKE nearly filed, the security family, five dresses in one round and four in another and five again in a third.** `superseded`/`absent` as pod self-reports exempting a slot from the leak accounting; a compromised adapter refusing every fenced teardown; the epoch as a guessable small integer; the exclusive-pod carve-out returning a recycling pod carrying credential residue; a late `StartSession` re-creating the entry and leaving an orphan holding live credentials; the withheld report relaxing the `maxSessionsPerPod` residual bound; the epoch as a fail-open default because zero is permissive. Each dies on the standing bar that the lie must make the gateway DO something it does not do today, on the pre-existing `leaked = err != nil || !cleanly` self-report, or on the cleanup having removed `CredentialsDir` and cancelled the §4.9 timers first.
- **MISTAKE nearly filed, four capacity dresses in one round and four reliability dresses in another.** The hold refusal counted against the pod's unhealthy threshold; the §10.1 fence making the compensation unsendable after a handoff; the pre-`running` leak losing its occupancy on a Redis reset; the hold's accounting sentence appended to the concurrency-independent scrub-model paragraph; the hold refusing a §5.2-placed retry during the adapter's own cleanup; §5.2's Fresh workspace guarantee falsified by the hold; a replica lost mid-bind refusing an identifier for the pod's life. Each is either recorded as an accepted failure mode in the proposal's own edge cases, or is the pre-existing baseline, or is a barred close variant.
- **Do NOT conclude "nothing changed" from an empty diff against the snapshot the brief names.** In every round of this window `spec-rN` and `spec-rN-start` were byte-identical to the live proposal, because the snapshot is taken at the round's own start. Run `diff -rq` over the WHOLE `scratchpad/cp-snap/0081` directory first and diff against the newest snapshot that actually differs. Worse, that snapshot is sometimes two rounds back: at round 8 the newest differing snapshot was `spec-r6`, and the six real hunks were written in round 7's FIX stage after round 7's own lenses had run, so a round-7 shard saying "there is none" was true when written and misleading afterwards.

- **MISTAKE that nine lenses over seven sweeps repeated: checking proto field-number FREENESS and stopping there.** Every one of them verified that SCHEMA-1's nine numbers are unused, and none of them looked for a test that pins the field SET. `TestShutdownMessagePostRemovalDescriptor_spec_4_1` turns red on exactly those additive fields, and `buf breaking` is silent on an additive edit, which is the gate a reviewer reaches for and the wrong one. When a proposal adds a proto field, grep `tests/` for `Fields().Len()` and `assertFieldSet` over the messages it opens, not only the proto for the number.
- **MISTAKE, reproduced by at least eight lenses in one window: the `awk` range over a one-line proto message.** `awk '/^message X \{/,/^\}/'` and `sed -n "N,/^}/p"` both run straight past `message AssignCredentialsResponse {}` into the NEXT message and print `RotateCredentialsRequest`'s field set, which is how the retired "field 1 collides with `session_id = 1`" trap was manufactured. `grep -n "^message AssignCredentialsResponse "` with a trailing space also MISSES the line. Use `grep -n "^message X\b"` then `sed -n`, or a brace-balanced parse. Renumbering SCHEMA-1 on the strength of the old form lands a defect.
- **Do NOT hand-edit `tests/claim-map.json`.** It is generator output under a tier-0 byte-identity gate; a hand-added row turns tier 0 red on landing and the next seeding run drops it silently. The authoring site is the `EXPLICIT` list in `scripts/seed-claim-register.py`, and the generator must be re-run with `--out tests/claim-map.json` in the SAME commit. Every row in `EXPLICIT` carries a `note`; the validator does not require one, so that is convention rather than a gate.
- **MISTAKE in a finding's own `suggested_fix`: `errors.Is` against an adapter-local sentinel, from the gateway.** `errSlotReclaimInProgress` and `isSlotReclaimInProgress` are unexported and in `pkg/adapter`, which NO production file under `pkg/gateway` imports (only component tests do), and sentinel identity does not survive gRPC. A fixer taking that suggestion literally writes code that cannot compile, or worse a message-string match. The status code is the only signal that crosses the boundary.
- **Do NOT open an `Aborted` case in `SlotBindError.Reason()`** to close the resume classification gap. CODE-2's rollback relies on the same transient default, so an `Aborted` case there would make both the rollback and the hold refusal non-transient on the concurrent-bind path. The arm belongs in `isTransientPodClaimError`, which is a different classifier on a different path.
- **Do NOT put a test for `holdOrFailOnResumeError` or `isTransientPodClaimError` in `start_test.go`.** That file is `package sessionserver_test` and cannot see either unexported function. Two findings named it. The home is `resume_setup_demotion_internal_test.go`.
- **Do NOT delete the `ReportSessionScrub` mention from the `adapter-contract.md` `Shutdown` row.** `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` requires the row to CONTAIN "end-of-session teardown", "recycle disposition", "ReportSessionScrub" and "ReportPodScrub". DOCS-2 must QUALIFY the mention rather than remove it, and must keep the row on ONE physical line, because the gate reads it with `lineContaining(page, "| `Shutdown` |")`. The same four substrings are why the stale row ships green today: no gate catches its absence or a wrong fix.
- **DOCS-2's prose may not cite spec section numbers.** `.claude/rules/doc-content.md` bars `§4.7.1`/`§15.4` in reader-facing docs. The page already violates this once at :393; that is not licence.
- **Do NOT file `adapter-contract.md:64` (the `DemoteSDK` row) as its own finding.** §4.7.1's caller rule makes the demotion the act that drops the caller's epoch, which leaves a one-line RPC summary INCOMPLETE rather than false, and the row is a verbatim mirror of spec/04:674, which this proposal does not edit. Three lenses declined it on that ground. The :75 fix should still pick it up.
- **The checklist renumber left stale prose step references, and they are not findings.** `non-spec-changes.md:1668` still says "Between S4 and S6 the unedited file still passes" where SPEC-4 is now S5. Substantively harmless; a fixer already in that paragraph should correct it. Do not read a step number in non-spec prose as evidence of the current sequence.
- **MISTAKE nearly filed: S9's `Depends on: S1, S8` does not close over S4,** although CODE-6 implements SPEC-3's reclaim-hold paragraph, which lands at S4. Held back because S4 precedes S9 in the one stated execution sequence and §15.4's own hold block (SPEC-5, S1, which IS named) carries the wire-observable requirement CODE-6 implements. A round that wants this must argue from a pipeline that dispatches by `Depends on` rather than by listed order.
- **Do NOT re-file the wide "create or resolve a registry entry" predicate, in its security dress either.** The dresses tried this window: `RevokeCredentials` refused mid-cleanup with `ABORTED` so a mandatory §4.9 revocation is refused; `Attach`/`Interrupt`/`ReportUsage` answering `FailedPrecondition` during a hold where §15.4 calls a permanent status non-conformant; the §11.4 revoke `Shutdown` answered `absent`. All die on the same two grounds: during the hold no entry exists so the resolve half is vacuous, and `removeSlotTree` has already taken `CredentialsDir` while `deregisterSlotLocked` has already cancelled every armed timer, so a revoke has nothing left to revoke. Reopening it means attacking the "create or resolve is this change's own bind-sequence vocabulary" scoping directly.
- **MISTAKE, and the bar is DEAD: "do NOT file CODE-1's early return skipping clause three".** That trap rested on the retired per-entry-epoch design, where only a fenced compensation carried a non-zero epoch and never set the recycle disposition. Under the token, `shutdownReclaimOutcome`'s switch tests entry ABSENCE before the unconditional flag, so `unconditional_teardown` against an absent entry takes the non-removing early return — and that is exactly what the concurrent `ShutdownRecycle` always is, because `Binder.ReleaseSlot` already tore the entry down. The staged handler therefore dropped `startPodScrub` on a production path, the finding was CONFIRMED, and the fix is the single `answerShutdown` exit. Do not reuse the old bar, and do not trust the self-refutation that went with it ("the unconditional form does NOT early-return on an absent entry"), which is false against the staged switch order.
- **Do NOT file `compensateFailedSlotBind`'s `default: return false` as a fail-open.** `SLOT_RECLAIM_OUTCOME_UNSPECIFIED` plus `exited_cleanly: false` maps to not-leaked, where today's `err != nil || !cleanly` maps it to leaked, so it reads as inverting `slotbinder.go:533-543`'s stated fail-closed posture. It dies twice: CODE-1's handler sets an outcome on every return path so UNSPECIFIED is unreachable for the first-party adapter, and staged §7.1 enumerates exactly two incomplete-reclaim conditions so the default arm is spec-conformant. The only reacher is a hypothetical third-party adapter, a framing the material skeptic has refuted three times. A later round needs a first-party reachability argument.
- **The caller-latch clear diverges between the lanes, it is INERT, and it is now FILED.** §4.7.1 says a caller holds no epoch once it has issued any RPC that removes the entry, naming `DemoteSDK` and "a `Shutdown` answering `reclaimed`", while CODE-6 clears only on `DemoteSDK` and on `ShutdownReclaim` answering `RECLAIMED`; the plain `Client.Shutdown` and `ShutdownRecycle` keep their signatures, read no outcome and clear nothing. Five lenses reached it and declined; `non-spec.6.review-fresh.1` filed it as a staged-code-contradicts-staged-spec finding, which is the loop's own scope rule, so the inertness below is the reason it is cheap to fix rather than a reason to close it. The direction is fail-open, and it is unreachable because no gateway connection carries a bind sequence after an ordinary `Shutdown`: `BindResult.Adapter` is per attempt and is closed at session end (`defer result.Adapter.Close()`, slotbinder.go:544). Reopening it needs a path where a plain `Shutdown` is followed by a bind-sequence RPC or a compensation on the SAME connection.
- **MISTAKE nearly filed, the security family again, five more dresses in one window.** A lagging `AssignCredentials` re-creating the entry and writing `credentials.json` plus §4.9 timers for an abandoned session (same route as the recorded `StartSession` variant, killed by CODE-4's unconditional `releaseCredentials` and by the occupancy-zero scrub sweeping `/run/lenny/slots/`); the epoch fence stranding credential residue (every adapter deregistration path reaches a `removeSlotTree`, so `superseded` and `absent` both entail the predecessor's tree, credential directory and timers are already gone); `emitFinalUsage` narrowing from `bound` to `started` leaking a delegation-tree budget (best-effort with a stream-close fallback §8.3 tolerates, and no bound-but-unstarted session reaches a `Shutdown` in the shipped tree); the pod-self-reported `leaked` bound as a regression (the adapter's power to force "not leaked" is IDENTICAL on both sides of the change — before, `exited_cleanly`; after, `slot_reclaim` plus `exited_cleanly`); and the §4.7 no-op sentence as a mandatory-credential-purge bypass (killed by the row's unedited tail).
- **Do NOT file the resume-path latency family; it is DISCLOSED rather than open.** On an exclusive pool `Binder.Resume`'s compensation runs with `slotCleanupBudget` degenerating to the whole pool `cleanupTimeoutSeconds` (default 60, documented "Must be `> 0`" with no upper bound) inside the client's request. The proposal names and prices it, and the standing five-dresses trap already declined the family because §16.5 samples successful creates only. A future round must argue from a STATED budget it breaches, never from the wall clock.
- **The snapshot trap has a fourth and a fifth form.** Fourth: this window's `spec-rN` directories REUSE the names of the previous window's, so `spec-r3` … `spec-r9` under `scratchpad/cp-snap/0081.../` belong to a different document generation and ordering the listing by NAME is misleading — and mtime order is misleading too, because `spec-r4` sorts newer than `spec-r9`. Fifth: an empty diff can mean "the previous round produced no fix" rather than "you diffed the wrong snapshot", which is only distinguishable by `diff -rq` across the whole chain. In several rounds the newest differing snapshot was a `-prefix` directory, because the fix stage runs between the prefix snapshot and the next round's. Use `git diff` on the proposal file when the snapshots are ambiguous.
- **Do NOT read `spec/06:160`'s attribution of `lenny_adapter_leaked_slots` to the adapter as a premise.** The gauge is GATEWAY-registered and gateway-emitted (gatewaymetrics_credential.go:219-223) despite the name and despite the spec sentence. Five more lenses built a finding on "the adapter cannot know what the gateway observed" this window; the premise does not exist.
- **FILED and live: CODE-4's credential release is keyed on the SESSION, so it can destroy a concurrent successor attempt's leases, and two standing entries look like they refute it and do not.** The `materializeSlot` wrapper runs `compensateFailedSlotBind` first, blocking up to `slotCleanupBudget` on a `context.WithoutCancel`, and only then calls `b.releaseCredentials(req.SessionID)`, which iterates `LeasesBySession([sessionID])` and releases every lease under that key (credassign.go:400-410), while `assignSlotCredentials` mints per attempt under the same key (slotbinder.go:379). The design's own headline scenario, a client whose context expired retrying into the same slot, puts attempt 2 past `assignSlotCredentials` inside that window: the epoch fences the pod-side reclaim to `superseded` and the very next statement destroys the successor's leases anyway. Look-alike (1): the Trap "Do NOT make the bind-path lease release conditional on `assignSlotCredentials` having SUCCEEDED" bars a STAGE condition; this is the release's KEY, and releasing exactly the lease ids this attempt minted stays unconditional over a partial assign. Look-alike (2): the refuted "CODE-4's unconditional `releaseCredentials` releases leases a snapshotless resume-rebuild may still hold" died on "the rebuild re-mints on every attempt"; a concurrent second `/start` does not re-mint after the release. The proposal states the correct rule for the resume path and does not carry it to the bind path (non-spec-changes.md:701-704).
- **Do NOT cite "§15.4 names no status code for the hold refusal" as evidence of a gap.** Three earlier findings were refuted on that premise and a later fix CLOSED it: §15.4 now publishes `ABORTED` and the two non-conformance cases. Reading the refuted list as a map of what the text still lacks misleads in this direction; re-read the text. The same staleness runs the other way for anyone quoting the refutations of "the missing wire spelling of an epochless `Shutdown`".
- **The snapshot trap has a SIXTH form: `diff -rq` can report the spec staging as unchanged when it changed.** In one round `diff -ru <snapshot> <proposal dir>` listed only the checklist and the non-spec file, while a direct `diff -u` of the two `.spec-changes.md` files showed a real delta. Diff the single file explicitly rather than trusting the recursive listing, and remember that mtime order across `scratchpad/cp-snap/0081.../` is not round order (`spec-r4` sorts newer than `spec-r9`) and that this window reuses the previous window's directory names.
- **Do NOT move the §7.2 reclaim after step 4.** It sits at step 3, before the `coordination_generation` bump, and that ordering is the only reason a mid-resume compensation is not fenced by the bump it precedes. A later edit that reorders them makes every mid-resume reclaim fail the generation check and leak the slot.
- **The SCHEMA-1 window under `**Decisions.**` and the R1b row under `## Impacts on other proposals` both assert the S-2 second-window precondition.** They agree today and are deliberately not folded into each other: the decisions bullet carries it as this proposal's step-ordering constraint, the impacts row as an assertion about the programme step. An edit to one owes the same edit to the other.
- **MISTAKE nearly filed, three shapes against the reclaim hold's reach into non-bind RPCs.** `RevokeCredentials` answering `codes.FailedPrecondition` mid-hold where §15.4 calls a permanent status non-conformant; `extendCredentialLeaseSlot` returning an empty success; `Attach`/`Interrupt`/`ReportUsage` answering the same permanent code. All three die on the same two grounds already recorded for the wide predicate, and the natural remedy is a §15.4 wording tightening, which is spec-lane. Anyone reopening must attack the "create or resolve is this change's own bind-sequence vocabulary" scoping directly. EVIDENCE: pkg/adapter/slotcreds.go:102-146; slotsession.go:274-283.
- **Do NOT read the refuted-list entries about the shared-entry epoch inheritance as current.** Six of seven shapes one lens re-derived from the amendment text were refuted as STALE against a pre-`2aca9851b` snapshot rather than on their merits. Diff against the newest differing snapshot and grep the load-bearing quote before deriving anything from remembered text.
- **The standing context is where the answers are, and reading it end to end is the cheapest first move.** Six of seven candidates a fresh reader generates on this document are already in it, several as explicit "do not re-derive" entries, and four consecutive lens shards this window recorded that reading it first turned a full re-derivation into a spot check. The refuted-family index in Traps is the highest-value part; keep it at length and do not split the consolidated security-family and capacity-family entries back into individual lines.

- **MISTAKE, filed twice and still standing: "refuses" is overloaded after the amendment, and the Testing section was never swept.** Post-amendment a refusal is the `SUPERSEDED`/`ABSENT` answer, which CODE-4 maps to `leaked=false` and CODE-5 excludes from `ExcludePods`; pre-amendment "the adapter refuses" could only mean an errored RPC. The tier-4 datastore-crossing case carried the pre-amendment sense verbatim from commit 2811d55fe. Whoever edits that section must sweep every "refuse"/"refused" for the place "did not answer" is meant, and must NOT sweep the other twenty, which are the reclaim-hold and epoch refusals and are correct.
- **Do NOT name `releaseSessionSlot` as a `Runtime.Close` park site.** Two fix rounds wrote tier-1 hold bullets telling an implementor to park or panic `Runtime.Close` at `releaseSessionSlot`; its body reaches no such call. Only the §10.1.4 disjunct is writable, and `removeSlotTree` is `slotlayout.RemoveTree(st.paths)` with no injectable seam, so that row has no parkable destructive step either.
- **The snapshot trap, seventh and eighth forms.** Seventh: in several rounds the named snapshot was byte-identical to the working tree INCLUDING the review log, so `diff -rq` returned nothing at all and the real delta was the UNCOMMITTED working-tree change — use `git diff HEAD -- proposals/0081_*/` then. Eighth: the newest DIFFERING snapshot is often a `-prefix` directory, because the fix stage runs between the prefix snapshot and the next round's, and in one round the brief's named snapshot was two rounds stale. Run `diff -rq` across the whole `scratchpad/cp-snap/0081...` chain before budgeting a round on "read the delta first"; at least fifteen shards in this window opened by discovering the delta did not exist.
- **MISTAKE the credit-inventory gate punishes, and nobody named the gate for six rounds: creating a `slot*_test.go` file.** Two of this proposal's new files match it by basename and neither was registered until the r5 fix. The trap has a second edge that is CONTINGENT and easy to miss: rule 1 is a body match over the whole tree, so an EXISTING unregistered file becomes mandatory the moment an edit adds `slotstate.`, `ClaimSlot(`, `ReleaseSlot(`, `ReserveSlotOnPod(`, `claimAtCreate(` or `BindReservedSlot(` to it. `pkg/gateway/sessionserver/start_test.go` and `resume_setup_demotion_internal_test.go` are both outside the inventory at zero matches today and are both plausible homes for a `BindReservedSlot(` literal. Do not describe the name rule as a `slot*_test.go` prefix glob; the regexp matches `slot` anywhere in the basename.
- **The DOCS-2 audience-mismatch clause is FILED, and the older "do not file it" entry is superseded.** DOCS-2's rationale says the block reaches "the third-party adapter author the page is written for" (non-spec-changes.md:1320-1321, repeated at summary.md:694) and paraphrases the page's line 10 as "the complete reference for the gateway-to-adapter gRPC protocol" (:1294-1295). `docs/reference/adapter-contract.md` says it is the complete reference for the protocol between the adapter sidecar and "your runtime binary" (:10), calls the adapter container Lenny-managed (:18), and says of the gRPC section "Your runtime binary never sees them directly" (:53); it never names a third-party adapter author, and the proposal's own spec-changes.md:705 puts that audience on §15.4. Six lenses across three rounds derived it and declined it as rationale prose that never reaches `docs/`; `non-spec-recheck-3.3.review-citations.1` FILED it on the seventh derivation and its own WATCHOUT says this entry must not be read as refuting the filing, because the filing is about the false GROUND rather than about whether the block should land. The fix is one clause: "so the gateway-to-adapter contract this page covers states the epoch and the hold". WHAT SURVIVES as a bar: do NOT file the OPPOSITE finding, that the full §15.4 bind-epoch and reclaim-hold contract must be republished on that page. That is over-reach for a runtime-author-facing page, and the defensible scope is the `Shutdown` row's now-false statements.
- **Do NOT restate the Non-goals bullet's "adds accounting where there is none" half.** The bullet's operative claim, that no threshold changes, is exactly true, and the falsifier let the bullet stand for that reason. Its justification half is not the whole of CODE-5: on `applySlotRetryPolicy` the accounting already exists and its shipped discriminator is `relErr != nil` alone (start.go:2834-2854), and CODE-5 widens it to `sbe.Leaked || relErr != nil`, so an unacknowledged reclaim on that path moves from the windowed arm to the persistent-leak arm. That disposition change is stated correctly elsewhere in the proposal. A later pass rewriting the bullet should keep the threshold claim and drop the "where there is none" half rather than copying it forward.
- **MISTAKE, twice in one phase: a firing published a provenance statement about a file state that does not exist.** `f6` recorded "the 0080 row itself was not touched" and the summary recorded that the row "was applied at firing 5 of this phase and reversed afterwards ... so the row stands as it was written at firing 4". Both are false: the baseline `2f473c14e` carries firing 4's five-file version of the row while HEAD carries the eleven-file rewrite, so the rewrite was applied and never reversed, and open decision 19 rested on the false premise. The summary half was deleted with entry 19; the log half stands as a wrong line rather than being edited, per the no-editing-a-ledger-entry rule. Before asserting that a firing's edit was reversed, diff the named baseline against HEAD for that one block; a phase's own bookkeeping about itself is the least-checked prose in the proposal.
- **MISTAKE that cost a whole loop: a fix stage declared a lane converged without sweeping `### Open` for entries marked FILED.** CODE-4's session-keyed credential release was recorded FILED-and-live with a named remedy, the fix round after it did not apply it, and the next round's reliability and security lenses both re-derived and re-filed it. Deferred and FILED entries against files the current lane MAY edit are the cheapest findings in the pile; check them before anything else.
- **Do NOT re-derive the capacity family; it has now been declined by six performance passes over text that did not move.** The replica-local `slothealth` ledger against the durable Redis occupancy, the exclusive-pool resume budget degenerating to the whole `cleanupTimeoutSeconds`, the compensation pinning a client request goroutine, the blob-outage mass drain, and the correlated re-attach storm are all true, all recorded as Open items or accepted failure modes, and all declined on the shipped-baseline or open-decision ground. Filing in this lens needs a mechanism that is NEW in the staged text. One durability asymmetry is worth having stated once: `ReleaseSlot(leaked=true)` skips the counter decrement whose doc comment names the §12.4 Postgres fallback, so the withheld occupancy is DURABLE while the `slothealth` count meant to bound it is per-replica and in-memory — pre-existing sharding, but this proposal changes the RATE, because a `leaked` disposition becomes the routine answer to any unanswered compensating `Shutdown` rather than needing `ReleaseSlotReservation` to error.
- **MISTAKE, the `awk` proto-range artifact, now reproduced by more than FORTY lenses across three windows and STILL the single most repeated waste on this proposal.** Nearly every lens that checks SCHEMA-1's field numbers builds `AssignCredentialsResponse` a false field set by running an `awk '/^message X \{/,/^\}/'` range over the one-line `message AssignCredentialsResponse {}` at schemas/lenny-adapter.proto:1033, which never matches `^}` and falls through into `RotateCredentialsRequest` (:1035-1059, fields 1 session_id, 2 leases, 3 rotation_trigger, reserved 4, 5 coordination_generation). Every one of them then nearly files "field 1 collides with `session_id`", and one earlier window actually landed that as a Trap before it was retired. CORRECTED for the token design: the nine RESPONSE numbers this entry used to list belonged to the withdrawn epoch and are gone. The live field set is the token one in Settled (`PrepareWorkspaceRequest` 5 and 6, `FinalizeWorkspaceRequest` 6, `RunSetupRequest` 5, `AssignCredentialsRequest` 4, `ResumeRequest` 16, `ShutdownRequest` 7 and 8, `ShutdownResponse` 3), and it is settled; do not check it again. If you must, use `grep -n "^message X"` plus `sed -n '/^message X {$/,/^}$/p'`, or a brace-depth walker.
- **MISTAKE, and it corrects a claim earlier passes carried: "a new test FUNCTION in a listed file costs nothing".** That is true of `validate-maps` and false of the second tier-0 gate. Once a file is in `slotAddressCaseFiles`, `TestSlotAddressCasesAreCreditedToEverySectionTheyAnnotate` demands a `tests/spec-map.json` credit for every section the file's annotations name, per case when the map registers that file case by case. Three files this proposal adds cases to are registered case by case with narrow whole-file credit (`slotretry_test.go`, `slotclaimer_test.go`, `recycle_scrub_path_test.go`). Check the target file's own registration granularity before concluding a case is free.
- **Do NOT land a `§7.1`-annotated case in `pkg/adapter/slotsession_test.go`.** It IS in `slotAddressCaseFiles` and carries no 7.1 credit (whole-file 4.9, 5.2, 6.1, 6.4, 15.4 plus 4.7 by subtree), so such a case turns tier 0 red at S9 and forces further spec-map staging for no benefit. This is why the CODE-6 tier-1 cases all land in the one new file.
- **Do not re-open the credit question; it is settled.** One early shard wrote that a case-by-case-registered file must be fixed with per-case rows and that a whole-file entry is wrong. At least six later shards read `creditsMissing` (spec_map_slot_address_registration_test.go:950-963) and established that it consults `wholeFile` in BOTH branches, so a whole-file credit satisfies a case in a per-case-mode file. That is why `slotclaimer_test.go`'s §5.2-only tier-2 cases owe nothing and why S12 stages whole-file credits for `concurrent_workspace_test.go` rather than per-case rows. A round that "corrects" the staging on the old reading adds rows that flip a file's mode for no gain.
- **MISTAKE, the third reclaim-hold producer, and BOTH orderings survive it.** The two grounds CODE-5 gives for excluding the §5.2 retry are about a hold the RECLAIM opened; after CODE-6 the abandoned attempt's own `releaseSessionSlot` rollback opens one too and nothing reports it. Ordering (a) rollback-then-retry: the retry is refused by the hold and spends its last attempt. Ordering (b) retry-then-rollback: the retry created its entry and staged its tree first, so the lagging rollback deregisters the RETRY's entry and deletes the RETRY's tree. The hold closes neither. CODE-5's "When it does not fire" sentence describes (b) and stays TRUE; a fixer who replaces it wholesale withdraws a live hazard.
- **The standing refutation "`releaseSessionSlot` is fully synchronous, so the hold ends before the requesting RPC answers" is scoped to callers still WAITING.** On the abandoned-attempt path the gateway stopped waiting, which is the premise of the whole compensation design (`context.WithoutCancel`), so the hold outlives the request that opened it. Anyone re-checking the third-producer finding must not stop at that entry, and the same caveat applies to the two `DemoteSDK` dead-end entries, which are about a caller that does wait.
- **Do NOT refute the queue head-of-line amplification with the `ExcludePods` "yields to the TAIL" refutation.** They are different mechanisms: the refuted dress was about an excluded waiter re-queueing, while the filed one is that the FIFO admits only the head's `attempt(ctx)`, so a synchronous compensation inside `materializeSlot` blocks every queued request for that pool. At the spec's own defaults the arithmetic lands exactly on `maxQueueWaitSeconds` (60/4 = 15s budget, two attempts, 30s).
- **MISTAKE, a fourth occurrence: the fix stage did not sweep `### Traps` and `### Open` for entries marked FILED.** The CODE-5 `slotBinder`-interface sentence took four filings across three windows before a fix stage took it, and the standing entry naming that exact cost was already in the log when the fourth filing happened. Read the FILED entries first; they are the cheapest findings in the pile and they are the ones that recur.
- **Do NOT file `docs/api/internal.md`.** It is a hand-authored gRPC page already fictional against the shipped proto: it declares `rpc StopSession`, `rpc UploadFiles` and a `StartSessionResponse{success, error_code, error_message}` the proto does not have, and its status table lists no `ABORTED` while the shipped adapter already returns it. SCHEMA-1 neither creates nor worsens that. Five lenses reached it; it wants its own proposal.
- **Do NOT file the `lenny_slot_pod_replacement_total` label mismatch.** spec/16:128 labels it `pool` AND `k8s_pod_name` while the gateway hook is `slotReplacement func(pool string)`; CODE-5 keeps that signature and only adds callers, so the mismatch is pre-existing and neither created nor widened here.
- **Do NOT file the `concurrent_slots_exhausted` reuse, again, in its operational dress either.** `ClaimSlot`'s shipped `expiredByUptime` skip already returns `ErrNoConcurrentSlot` for a pod with free capacity, so spec/05:549's "all slots are full" gloss is already falsified in kind. Seven lenses across two windows have now built and dropped it; a filing needs a harm the uptime-skip precedent does not already carry.
- **Do not schedule a lens over text that has not moved.** The operational lens fired five times on this proposal for one filing, which was refuted; the performance lens fired roughly a dozen times with at least six over unmoved text. Run `diff -rq` across the whole snapshot chain first, and when only the review log differs, read `### Open` and the prior shards of that lens and stop.
- **Reading this file needs the Read tool, not `bash`.** Bash output over roughly 2KB is persisted to a tool-results file and only a preview is shown, and the Read tool refuses more than 25k tokens per call, so `## Standing context` comes back in passes of 100 to 180 lines. Budget for that before planning a round.
- **WATCHOUT: SPEC-4's §6.2 edge must keep exactly one space either side of `──→`.** The tier-11 check is a raw `strings.Contains` on `"receiving_uploads ──→ slot_cleanup"`, and the shipped fence uses `──→` (U+2500 U+2500 U+2192) with the trigger paren at column 41 and a 41-space continuation indent. Re-flowing the block breaks nothing mechanical except that string.
- **Do NOT copy a neighbouring field's generation comment into a SCHEMA-1 field.** `adapter_proto_generation_scope_test.go` counts exact occurrences of the operational-fence sentence and pins the count at twelve, so one copied comment turns tier 0 red.
- **WATCHOUT: `Server.reclaiming` needs a lazy-init guard the way `s.slots` has one.** Adapter tests build `Server` by struct literal and a write to a nil map panics. CODE-6 states the lazy seed for the epoch counter for exactly that reason and says nothing about the map; whoever writes `reclaimSlotLocked` must not forget it.
- **Do not let the `_ = removeSlotTree(st)` discard's replacement grow.** CODE-6 turns it into a `slog.Warn` (and `pkg/adapter/slotsession.go` gains a `log/slog` import it does not have today). That log line carries no contract and pins no test deliberately: the edge-case bullet is written as an accepted failure mode with an observable trace rather than a half-built accounting path. Promoting it to a metric or a counter re-opens the design the r5 fix closed.
- **The credit-inventory trap has a contingent second edge nobody has costed.** If an implementor lands the tier-1 "reserved bind path" case in `pkg/gateway/sessionserver/start_test.go`, its `BindReservedSlot(` literal matches `slotSurfaceCallRE` and pulls that whole shipped file into `slotAddressCaseFiles`; because it has no per-case rows it then enters the credit gate in WHOLE-FILE mode, demanding credits for every section every annotation in it names against whole-file credits of 4.1 and 11.4 only. That could be a large unbudgeted cascade. The proposal's general sentence covers it in principle and nobody has priced it. CONFIRMED this window: `start_test.go` is outside the inventory today at zero matches and its only spec-map credits are the `pkg/gateway/...` (4.1) and `pkg/gateway/sessionserver/...` (11.4) directory globs, while it already cites §15.1 and §4.4 — so the cascade is a credit failure on its PRE-EXISTING citations, not on the new case.

- **MISTAKE, the sentence five rounds copied: "a row carrying a `PodAssignment` goes through `BindReservedSlot` and never enters `applySlotRetryPolicy`".** The guard is a conjunction. A recovery-state row (`resume_pending`, `resuming`, `awaiting_client_action`) carrying a stale dead-pod `PodAssignment` — which is exactly the §7.3 resume-rebuild the function's own doc comment names at start.go:2585-2590 — falls through to :2606-2609 and into the retry policy. The cited span `start.go:2594-2604` is wrong on both ends and straddles the guard and the fallthrough. The sentence stood in summary.md's open decision 17 `Ground:`, was FILED by `non-spec-recheck-3.1.review-citations.1` and fixed by qualifying it with "in a non-recovery state". Two things to carry: the CONCLUSION was never wrong (a recovery row is policy-placed, so the exclusion covers it and the residue stays bounded to the create-time-reserved path), and an earlier verifier had established the same tree fact while refuting a DIFFERENT finding without noticing this sentence. When a verifier derives a tree fact that contradicts a sentence it is not looking at, say so.
- **WATCHOUT: the `release` closure `reclaimSlotLocked` returns must take `s.mu` ITSELF, and nothing staged says so.** CODE-1 writes `defer release()` while `s.mu` is still held and unlocks on the next line, and `terminateHeldSession` defers it with the lock never held, so a `*Locked`-style release deadlocks both sites and a bare `delete(s.reclaiming, id)` is a data race `-race` will find in the tier-7a hold suite. Five shards across two lanes derived this independently and none filed it, because it is ordinary implementation judgement; whoever lands S9 should state it in the helper's doc comment.
- **WATCHOUT: put the §10.1.4 per-member `defer release()` in the pass-2 LOOP, not only inside `terminateHeldSession`.** `deregisterStartedSessions` (pass 1) takes a hold on every started member under one `s.mu`, and `onHoldTimeout` (pass 2) destroys them one at a time under a single shared 10s context with no early exit. A panic inside an early member's `terminateHeldSession` leaks every remaining member's hold for the life of the pod, which is the exact failure CODE-1's own rationale gives as the reason its release is deferred. EVIDENCE: pkg/adapter/holdstate.go:190-205; slotsession.go:375-396.
- **The §10.1.4 N-identifier hold is not a capacity finding, and here is the refutation that kills it.** Pass 1 holds up to `maxConcurrentSessions` identifiers at once for as long as the whole 10s pass. In the SHIPPED tree that same window is strictly WORSE: pass 1 already deregisters the entry, so a rebind onto the identifier succeeds, and pass 2's `removeSlotTree(m.state)`, whose paths derive from the slot identifier, then deletes the FRESH session's tree. The hold closes that latent bug. A filer needs a harm that survives this; three lenses built the capacity dress and each dropped it.
- **WATCHOUT: do NOT "simplify" `nextBindEpochLocked`'s lazy `time.Now().UnixNano()` seed into a constructor seed.** Adapter tests build `Server` by struct literal, so a constructor-only seed leaves the field at zero, which is the wire's "unfenced" value — every fenced reclaim in the test suite would become unconditional and the suite would still pass green. CODE-6 gives that reason; a later round that reads the lazy seed as untidy will delete the property.
- **WATCHOUT: `Server.reclaiming` needs a lazy-init guard the way `s.slots` has one,** for the same struct-literal reason. CODE-6 states the lazy seed for the epoch counter and says nothing about the map; a write to a nil map panics.
- **Dead end, several dresses: the reclaim hold reaching a non-bind RPC.** `CoordinatorFence` answered `codes.FailedPrecondition` during a hold where §15.4 calls a permanent status non-conformant; `RevokeCredentials`/`Attach`/`Interrupt`/`ReportUsage` the same; `extendCredentialLeaseSlot` returning an empty success. All die on the adjudicated vacuous-resolve reading (during the hold no entry exists, so only the CREATING half of "create or resolve" is reachable) plus the Settled fact that every one of those handlers resolves through `slotStateLocked`/`boundSlotState` rather than `ensureSlotStateLocked`. Reopening means attacking the scoping reading directly.
- **Do NOT file §28:861's `status`-frame relay against the hold.** §28 says a `status` frame is relayed "on a pod whose registry entry for that session has already been released", which is exactly the hold's window, and SPEC-3's hold sentence refuses "any request that would create or resolve a registry entry". It dies because the hold is scoped to the gateway-adapter gRPC surface (§15.4 states the refusal as the gRPC status `ABORTED`), an intra-pod CH-RUNTIMEOPS frame is not a request on that surface, and §28 explicitly exempts `status` from live-binding confirmation. Anyone re-filing must argue that the word "request" reaches intra-pod frames, which §15.4 contradicts.
- **MISTAKE nearly filed, and it costs a full read of two files: "SPEC-3's whole-pod backstop overstates coverage, because `RemoveTree` removes FOUR trees and the scrub touches only two".** True that nothing in scrub steps 0-6 removes or verifies `/sessions/{sessionId}/` or `/artifacts/{sessionId}/`, which `RemoveTree` does remove and which §6.4 assigns to slot cleanup (spec/06:386). It dies because the staged sentence names exactly the two things the scrub does and claims no more, and the gap is pre-existing and equally true of any failed per-slot cleanup on the ordinary release path. A re-filer needs a harm the pre-existing framing does not already own.
- **MISTAKE nearly filed: the bounded-cohort credential exposure.** On a `maxConcurrentSessions: N, recycle.enabled: false` pool no whole-pod scrub ever runs, so a `credentials.json` left by a partially-failed in-handler cleanup stays readable by every co-tenant through the shared `lenny-cred-readers` group for the cohort's life. It dies on the staged clause's own scope: the non-recycling arm claims only that the residue "does not outlive the pod", which is true (the pod terminates after the cohort drains), and the exposure is shipped best-effort `removeSlotTree` behaviour the staging does not change.
- **MISTAKE nearly filed, twice, from opposite lanes: the `refusal_reason` graceful-refusal arms against §15.4's "reports zero on a bind-sequence response".** `ConfigureWorkspaceResponse.refusal_reason` and `StartSessionResponse.refusal_reason` exist for a refusal that resolves no entry and therefore has no epoch to report, which would make a third-party adapter non-conformant. `grep -rn "refusal_reason\|RefusalReason" pkg/adapter/*.go` returns nothing, so neither arm is reachable in the shipped adapter and both construction sites are the success arm. Same hypothetical-third-party class the loop has refuted repeatedly.
- **MISTAKE nearly filed: the staged §7.2 step-3 reclaim against the tree's mid-resume cancel path.** `handleDelete` (sessionserver.go:2922-2959) and `handleTransition` (:2985-3005) write the row state and bump the coordination generation and release no pod at all, so the obligation looks unimplementable. It dies because §7.2 step 3's "release the replacement pod" is ITSELF unimplemented today, so adding the reclaim to an unimplemented step makes the tree no less conforming. A filer must first show where the replacement pod IS released on that edge.
- **MISTAKE nearly filed: the tier-1 classifier rows omit a credential-assignment `codes.Aborted` row.** The prose says "the credential site's row is the one this pins" while only a workspace-stage and a start-stage row are staged. `Reason()` switches on the code first and refines on `e.Stage` only inside the `PermissionDenied, FailedPrecondition` arm, so `Aborted` takes the transient default at EVERY stage and a third row would assert the same branch. Additional coverage, below the bar.
- **The snapshot trap, ninth and tenth forms.** Ninth: the non-spec staging has now been byte-identical since `non-spec-r6`, so an empty diff against any of `non-spec-r7`, `-r8`, `non-spec-recheck-2-r1/-r2/-r3-start` means the file genuinely has not moved rather than that you diffed wrong; in more than twenty shards of this window the named snapshot was byte-identical to the working tree and the only differing files were `summary.md`, `status.md` and the review logs. Tenth: `diff -ru` over the whole directory dumps 30KB of unchanged status and review-log prose and costs a round, so run `diff -rq` per file FIRST, and when only the review log differs, read `### Open` and the prior shards of your own lens and stop. When the snapshots are all identical, the real delta is the uncommitted working tree: `git diff HEAD -- proposals/0081_*/`.
- **Do not schedule a lens over text that has not moved, and this window is the proof.** Every one of the twelve-plus shards in `spec-recheck-2.2`, `non-spec-recheck-2.1`, `non-spec-recheck-2.2` and `non-spec-recheck-3.1` opened by discovering there was no delta, and every one returned an empty findings list. The one filed defect of the whole window (the `bindConcurrentSlot` conjunction) came from a citation sweep of text that HAD moved. Run `diff -rq` across the snapshot chain before budgeting a round.
- **Reading this file needs the Read tool, not `bash`.** Bash output over roughly 2KB is persisted to a tool-results file and only a preview is shown, and the Read tool refuses more than 25k tokens per call, so `## Standing context` comes back in passes of 100 to 150 lines and the whole log takes roughly twenty-five calls. Budget for that before planning a round, and read `### Traps` and `### Open` first: they are where the answers are.

- **MISTAKE, the same sentence failing in BOTH directions: §4.7.1's "Every other RPC on this contract neither carries nor reports ..." closure.** Written outside the `Shutdown` block it excluded `Shutdown`, and five lenses filed it; moved INTO the `Shutdown` block its "other" resolved against `Shutdown` and excluded the five bind carriers. It is now deleted and the carriage rule lives in exactly one sentence at the enumeration site ("`StartSession` and `ConfigureWorkspace` carry none, and no other request on this contract carries one"). Do not reintroduce a free-standing "every other RPC" carriage sentence in any position.
- **Do NOT delete or "harmonize" the OTHER "Every other RPC" sentence.** §4.7.1's "Every other RPC on this contract resolves an entry the session already holds, creates none, and is outside these rules" is scoped to the ADMISSION rules, sits before `Shutdown` is introduced, and is correct for the pod-scoped RPCs. The DOCS-2 paragraph uses the same admission-scoped phrasing and is likewise correct.
- **MISTAKE, two rounds on one sentence: per-RPC reachability narration inside §4.7.1's Admission paragraph.** Round 1 wrote it as a universal (which refused every `StartSession`), round 3 narrowed it to a closed enumeration, and round 3's lens filed the enumeration. It is now DELETED rather than narrowed a third time, because every rule in the cascade is self-selecting by its own condition and a per-RPC reachability claim beside them has no job. A third enumeration, however qualified, is the same answer one step smaller.
- **MISTAKE: `ShutdownResponse.slot_was_started` was the code lane's intruder into a spec lane that never admitted it.** Its only consumer anywhere was one §16.1 counter whose stated purpose was false on both halves: §7.1 obliges the compensation exactly when the adapter may have admitted the start, so `reclaimed` over a started entry is the DESIGNED outcome, and a `reclaimed` answer already means the tokens compared equal, so the counter could not observe an identity-gate failure at all. Field and row both deleted. The operational question is already carried by `lenny_slot_failure_total{error_type=...}`.
- **Do NOT flip the `Shutdown` four-way compare so a named reclaim removes an untokened entry.** "Token versus empty = reclaimed" looks like the fix for the untokened residue. An untokened entry can be the one a SUCCESSOR's own `StartSession` created for a session that is now running, so removing it tears down a live session. `superseded` there is the fail-closed answer; only its old justification ("no compensated path produces an entry carrying no token") was false.
- **Do NOT reclassify the untokened-entry `superseded` as a reclaim that "did not complete"** to get the §5.2 whole-pod replacement trigger to collect it. The same untokened entry may belong to a session the pod is legitimately serving and the gateway cannot tell; replacing that pod destroys a live session.
- **`superseded` for a tokenless entry is CORRECT on the `Shutdown` path and WRONG on the bind path.** The bind-path identity refusal fires only when the request's non-empty token differs from the entry's non-empty token. Do not sweep the phrase "carries no token" out of the `Shutdown` sites, and do not import it into the bind-path gate.
- **Do NOT put the reclaim hold back inside the admission cascade as "rule 1".** §5.2's hold reaches "any request that would create or resolve a registry entry", which is a WIDER set than the requests §4.7.1's cascade governs; a `Checkpoint` resolves an entry and is refused by the hold while §4.7.1 says it is outside the rules entirely. State the hold by reference to §5.2 as a check that precedes the cascade and carries its own scope.
- **The hold's precedence over the no-entry rule is load-bearing, not decoration.** During a hold the entry is already deregistered, so a `mid_session` `PrepareWorkspace` resolves no entry and the no-entry rule would answer the PERMANENT `FAILED_PRECONDITION` where §5.2 promises a transient refusal the caller retries.
- **"Carries `bind_attempt`" means the message TYPE declares the field, never that the value is non-empty.** The fields are bare proto3 scalars with no presence. That reading is what lets the well-formedness condition exclude `StartSession`/`ConfigureWorkspace` with no carve-out clause. Any rewrite that loses it needs the carve-out back.
- **`ensureSlotStateLocked`'s rule switch has NO field-validity case and must not grow one.** The well-formedness gate sits above it, at each handler's entry. Anybody reading the switch alone concludes the create branch fires unconditionally on an absent entry, which is correct only because a malformed request never reaches the function.
- **WATCHOUT: `mid_session` is a bare proto3 bool on a CLIENT-STREAMING message, so "omitted" and "false" are the same bytes.** A later `PrepareWorkspace` frame carrying an empty `bind_attempt` where the first carried one is explicitly not a disagreement, but a later frame whose `mid_session` is false where the first was true IS a refusal. No spec or staged-code text states the sender's obligation to repeat `mid_session` on every frame; `sendUpload` builds every frame from scratch (adapterclient/client.go:269-284), and CODE-8 is the sender.
- **MISTAKE that cost most of a pass: deriving that `superseded` is unreachable on the retry path.** The chain (a retry meeting a surviving entry is refused and creates nothing, so the lagging reclaim always compares equal) assumes the abandoned attempt created an entry. The reachable path is the attempt whose first entry-creating RPC never reached the adapter: §7.1 still owes a reclaim, the successor creates a fresh entry under its own token, and the lagging reclaim meets it and is answered `superseded`. The proposal's own bullet already contemplates it.
- **MISTAKE nearly filed twice: "§4.1's retained second sentence becomes a false field inventory once the two new `ShutdownRequest` fields land".** It dies on the tree: `ShutdownRequest` ALREADY carries `reason` (2), `deadline_ms` (3) and `coordination_generation` (6) beside the address, so the clause was never an exhaustive field list. One lens lost a full verification round to it.
- **Do NOT file §5.2:459's recycle-`Shutdown` payload list as missing `unconditional_teardown`.** It enumerates the scrub payload (recycle disposition, `podId`, `cleanupCommands`, `cleanupTimeoutSeconds`) and already omits `session_id`, `reason`, `deadline_ms` and `coordination_generation`, all shipped. §4.7's row states the caller rule generically. A filer needs an argument that the sentence is read as the complete field set.
- **Do NOT file §11.4's revoke fan-out as a missing edit site.** §11.4 enumerates no `ShutdownRequest` fields, so no §11 sentence becomes false; the obligation on that caller to set `unconditional_teardown` is carried by the §4.7 row and by the code lane. The same reasoning covers §5.2's recycle boundary sentence.
- **Do NOT file the §15.1 `SETUP_COMMAND_FAILED` cause drift a second time.** It is now staged as a widening under SPEC-5, its exclusion sentence is untouched, spec/06:290 is re-keyed on the gRPC code, spec/07:208 is a one-way enumeration that stays true, and `docs/operator-guide/troubleshooting.md:41` was adjudicated not-a-site because the remedy stays right for the setup-command cause. The row ALSO over-claims before this proposal applies, because shipped `RunSetup` answers `FailedPrecondition` for "adapter is not configured with a workspace root".
- **Do NOT file the §16.1.1 "Used on" column as going stale.** §16.1.1 is the single source of truth for attribute NAMES, both labels are already in it, the column is descriptive, and nothing mechanical parses it. Three lenses have declined it.
- **Do NOT file the `k8s_pod_name` label on SPEC-6's adapter row as unemittable.** The adapter caches the Downward API `POD_NAME`. The earlier "the site cannot supply that label" fix was about the GATEWAY free function `slotReclaimLeaked`, which holds neither the pool nor the pod name, and it must not be generalised to the adapter. At least eight lenses have rebuilt this.
- **WATCHOUT: DOCS-3 is named in the checklist and defined nowhere in `non-spec-changes.md`.** One fix group deleted it and a later one re-minted it with a different subject. If two groups are told to define DOCS-3 in one round, the definitions collide; check what the checklist's S7 line currently means before writing.
- **Do NOT file a §15.4 completeness finding, now for the fifth time.** The refuted set is the missing wire spelling of the unconditional form, the missing status code for the hold refusal, the missing per-outcome response rule, the zero-frame `PrepareWorkspace` response, and the unnamed `ErrorCode` for the hold refusal. Each dies on the same ground: a semantically-stated requirement plus a correct first-party implementation is not a correctness defect.
- **The snapshot trap fired on EVERY round of this window, in both lanes.** `spec-r2` through `spec-r6` and `non-spec-recheck-3-r4`/`-r5` were each byte-identical to the working tree, so `diff -rq` returned nothing and "read the changed sections first" had no target. More than twenty shards opened by discovering it. Run `diff -rq` across the whole snapshot chain first, take the newest snapshot that actually differs, and when every snapshot is identical use `git diff HEAD -- proposals/0081_*/`.
- **MISTAKE that burned six fix groups in one round: findings written against the pre-revision text.** The hand revision moved roughly eighty lines and rewrote whole blocks, and three groups resolved to NO EDIT because the quoted string no longer existed anywhere. Verify a finding's quoted STRING against the current file before designing a fix, and never trust its line number.
- **MISTAKE, the parallel-rewrite seam: the hand revision edited three files at once and left the defects BETWEEN them.** SPEC-6 staged a metric depending on a `ShutdownResponse` field the spec staging never published; SPEC-2 kept a §5.2 placement edit whose mechanism the code lane had withdrawn; CODE-6's call-site table read a `req.GetBindAttempt()` that SCHEMA-1 does not add to `StartSessionRequest`. When reviewing after a multi-file revision, read ACROSS the spec and non-spec files rather than down either one.
- **MISTAKE: a scope call resting on the shipped tree when a staged deliverable moves it.** Recorded again this window in the §5.2 `**Max retries:**` residue and in the "only production call site is `applySlotRetryPolicy`" attribution. Check the ground against the STAGED tree.

- **Do NOT split the pre-`running` cleanup trigger by POD CLASS.** "On a pod serving concurrent
sessions", and an "except on a pod serving one session" carve-out, are false twice: a §7.3 re-attach
reclaim onto a one-session replacement pod IS inside the staged §7.1 scope and does drive
`receiving_uploads → slot_cleanup` there, and the adapter's failed-start cleanup runs on either pod
class. The correct split is by WHICH BIND PATH the §7.1 obligation reaches, and the only uncovered
case is a pre-attached bind on a one-session pod, where `Binder.Prepare`'s reclaim is `failPhase`. A
reviewer's suggested wording invites the wrong split.
- **Leave the matched strings byte-identical when rewording the fence annotation.** The arrow string
`receiving_uploads ──→ slot_cleanup`, the two backticked cell values in DOCS-1's table row and
CODE-3's `{ReceivingUploads, SlotCleanup}` entry are each matched by a staged assertion; the tier-11
gate reads neither the parenthetical annotation nor the row's third column, which is why only those
two were reworded.
- **Do NOT write that the published error catalog "gains rows" for the two new codes.** That is the
pre-revision wording and it is wrong in both directions: DOCS-3 adds no row, and the two adapter
`ErrorCode` values are gateway-to-adapter codes that never reach the client-facing page. The
checklist's S7 line still carries the wrong form, which is why the standing Deferred names it.
- **`.spec-changes.md` has no trailing newline.** A scripted replacement whose search string ends in
`\n` fails on the last paragraph of that file.
- **review-log.md:2260 and :2268 record a DECISION to delete DOCS-3 and a WATCHOUT not to touch
`docs/reference/error-catalog.md:129`. Both are RETIRED** by the 2026-09-16 hand revision and the
round-5 §15.1 widening. A later agent reading either cold will undo DOCS-3; re-deleting it loops the
proposal.
- **MISTAKE nearly filed, and it cost a third of a round: SPEC-5's §6.2 `**Client visibility:**`
replacement and its "any other gRPC code".** It reads as though "any other" has no antecedent, so
the falsified quantification looks like it survives the re-key. It does not: the clause's first half
names the deterministic non-zero setup-command exit surfacing as `SETUP_COMMAND_FAILED`, and §15.1's
own row states that cause's code, so "any other" resolves to "other than `FailedPrecondition`" and
yields the intended meaning. Do not re-file it.
- **Do NOT file "§7.1's carve-out reads as a universal that excludes a §7.3 re-attach onto a
one-session pod".** `spec.2.review-feasibility.1` adjudicated that the implication runs one way only
(outside the obligation → one-session pod, never the converse), so a re-attach onto an exclusive
replacement pod is inside the obligation and does send a reclaim. Frame any exclusive-pod finding on
the §15.1/start arm, which IS explicitly concurrency-scoped.
- **Declined, so nobody re-derives it: the edge-case bullet's "refused on the reclaim hold while the
cleanup runs and on the attempt token afterwards" states disjoint sub-cases rather than two phases of
one.** When the adapter executed the cleanup and only the response was lost, the entry is gone
afterwards and the retry SUCCEEDS; when the adapter never received the reclaim, no hold exists and
only the token refuses; when the cleanup itself failed, staged §5.2 holds the identifier until the
pod terminates, so there is no "afterwards" at all. It over-claims pessimism only and lands in no
applied spec text.
- **Declined: the reclaim is sent once, with no durable record and no re-drive,** so a gateway crash,
a preStop drain or a rolling update between abandoning an attempt and sending its compensation
leaves a tokened entry no live attempt can adopt. It is stated as an accepted failure mode with two
named out-of-scope recoveries, and the residue bullets are barred from reopening. What is still owed
to a human is the standing Open on a rolling update versus a crash.
- **Do NOT file the §15.4 "`Shutdown` is not held" carve-out against §5.2's and §4.7.1's wide hold
predicate, now for the seventh time.** They reconcile on the vacuous-resolve reading: during the
hold the entry is already deregistered, so a `Shutdown` resolves nothing and the outcome is `absent`
either way. If a later round wants it closed, the fix is one clause in the §5.2 hold paragraph
naming the `Shutdown` exemption, never a §15.4 edit.
- **`spec/07:23`'s atomicity paragraph does not close with "The client never receives a
`session_id`..."** — it closes "...regardless of the flag." — although `spec-changes.md:38-40` says
it does. The insertion instruction at :444-447 quotes the real ending, so the edit applies cleanly.
Below the bar; do not re-derive.
- **A format pass may not mint a recommendation, alternatives-and-why-each-lost, or a confidence for
open decision 20.** It carries its question and its ground only, and it is now the section's sole
entry. The next adjudication firing owes the other three.
- **The 0078 overlap in `shutdown_drain_gate_race_test.go` is a merge hazard, not a design
conflict.** The two proposals' edit regions inside that file are distinct: 0078 touches
`socketDrainPod`'s accept timeout and the sequenced leg's assertion, while this proposal sets a
request field on four `ShutdownRequest` literals. The corrected impacts row says so in those terms,
so a later reader does not read it as a design conflict.
- **MISTAKE: a recorded WATCHOUT on a normative predicate sat unfiled for six rounds and shipped into five paraphrases.** The `mid_session` zero-value hazard was written down as a WATCHOUT (a later frame whose `mid_session` is false where the first was true IS a refusal) and no round filed it, so the symmetric per-frame predicate refused every ordinary multi-chunk §7.4 mid-session upload for six rounds of staging. Its "CODE-8 is the sender" framing is what kept it out of the spec lane, although the spec half was filable on its own. A recorded WATCHOUT on a normative predicate is filed or refuted, never carried.
- **The `PrepareWorkspace` per-frame predicate lives at SEVEN sites and they must stay one statement.** Two are normative (the §4.7.1 `PrepareWorkspace is client-streaming` paragraph and the §15.4 clause beginning "An adapter does not conform when it resolves the slot identifier more than once"); five are paraphrases in `non-spec-changes.md` (CODE-6's staging.go description :1307, CONF-1 clause (i) :1554, CONF-1's negative-case walkthrough :1609, the gateway tier-1 case :1966, the tier-3 clause list :2276). A fix touching only the normative pair leaves a conformance battery encoding the withdrawn symmetric rule.
- **The zero-value hazard has a SECOND face: the well-formedness pair.** If "non-mid-session with an empty `bind_attempt` is `INVALID_ARGUMENT`" is applied per frame, every later frame of a mid-session upload (empty token, default-false marker) is ill-formed and the upload is refused by well-formedness before the disagreement rule is reached. The fix has to say in the same paragraph that the admission rules are decided on the resolving frame.
- **Do NOT touch the Recycle-edges group's `a failed session` clause at spec/06:114-118.** It is correct for its own group, whose preamble requires the claim to be patched `bound → recycling` at the recycle boundary first, and deleting it breaks the clean-recycle retirement path. The defect is the OTHER `claimed ──→ draining` entry, the one under "Occupancy projection".
- **spec/06:80 carries TWO claim-deletion clauses and only one is corrected.** "a claim deleted on a pod with `recycle.enabled: false`" is the over-narrow one. "a claim deleted on a recycling pod under its limits projects `idle`" describes the `reserved → idle` edge after the claim has been patched, matches `case state.Reserved` in occupancy.go, and is correct as written.
- **The tempting wrong fix for the recycle qualifier is an exception clause** reading "and on a recycle-enabled pool the pre-attached failure is treated as a failed session". No actor writes that disposition on the pre-attached path, so the clause is a fiction the tree does not satisfy. It is exactly the hair the loop exists to prevent.
- **MISTAKE: the round-7 rewrite replaced an unconditional statement with a two-branch one.** The settled entry stated the retirement unconditionally; `git diff c1ab102f3..096b39656` on spec-changes.md replaced it with a two-branch form that reads as a tightening of the citation and was in fact the introduction of a claim no actor produces. Cost: one deep finding and a whole design round. The staged text drifted from its own settled record; check a "tightening" against the standing entry it narrows.
- **The one-session carve-out is a SWEEP, not a single edit.** Three rounds have now each found one more un-narrowed universal left behind by it: the §5.2 append, the accepted-failure bullet, and §4.7.9 step 5's trailing "rather than leaving the pod holding the session's state". Before declaring the sweep done, grep spec-changes.md for every sentence asserting that a pre-`running` bind failure is reclaimed, cleaned up, or leaves no state on the pod, and check each against the carve-out at spec-changes.md:641,:704.
- **The carve-out is scoped by STAGE, not by pod class, and that is what rescues it from the standing "do not split by pod class" trap.** It covers only a bind that fails at the workspace-preparation, setup or credential-assignment stage. `Binder.Resume` issues NO `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup` or `AssignCredentials` — it calls `connect`, `reserveResumeSlot` (kube and Redis only) and then `cl.Resume` — so no §7.3 re-attach can fail at one of those three stages and the carve-out and the re-attach arm do not overlap. Two round-8 lenses each nearly filed the pod-class objection. Do not re-file it without first showing a re-attach RPC at one of those stages. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1590-1629.
- **MISTAKE: a fix round corrected the `superseded` gloss at the §16.1 row only,** leaving the same too-narrow gloss standing at the §4.7 row, §4.7.1's outcome paragraph and §15.4's republication, even though the round that filed it wrote "if a fixer touches one, it must touch all three". Filed again at round 8. EVIDENCE: spec-changes.md:400,:741,:793 (narrow) versus :463,:894 (wide).
- **Do NOT "fix" spec-changes.md:400 by making a named reclaim REMOVE an untokened entry.** An untokened entry may be a live successor's own `StartSession` entry. The fix is the outcome NAME only: widen the gloss to cover an entry no bind attempt owns, leaving "performs neither, removes nothing" exactly as staged.
- **UNVERIFIED (two round-8 shards disagree and neither corrects the other):** `spec.8.review-client-surface.2` records the `superseded` gloss drift as twice-refuted and says do not file it a third time, while `spec.8.review-mechanism.2` filed it as two substantiated findings on the ground that :400's defect is an enumeration HOLE and :741/:793's is a false rationale clause. The newer mechanism reading is kept; whoever settles it should start from whether the two shards are describing the same defect.
- **The snapshot trap fired again, in its twelfth recorded form.** `diff -rq spec-r8 <live>` reports only the review log differing, while the real r7→r8 delta is 189 lines against `spec-r7`. Compounding it, `spec-r9` exists and is dated 2026-09-10, so it is PRE-revision epoch content. Take the newest snapshot that DIFFERS, by mtime, and never the highest-numbered one.
- **MISTAKE: a resumed round re-executes lenses whose shard is already on disk.** The round-8 shard directory already held a file for every lens, and several lenses re-ran a pass whose honest answer was already recorded. Read your own shard first and diff the staging's mtime against the shard's before spending the round; if the staging has not moved, say so and re-confirm rather than re-derive. A resumed run must still show its own evidence, so re-run the two or three mechanical checks that carry the verdict.
- **"Admission" throughout the staged §4.7.1 and §15.4 blocks means the ADAPTER's own gRPC request admission, never a Kubernetes admission webhook.** Third recording of this trap.
- **`onHoldTimeout` deregisters EVERY started session in pass 1 and runs the cleanups sequentially in pass 2,** so under the reclaim hold ("from the critical section that deregisters the entry until the cleanup finishes") the hold on member N is open for the whole of members 1..N-1. At the top `maxConcurrentSessions` that is a long hold. It is NOT a regression, because every one of those sessions is being terminated anyway. EVIDENCE: pkg/adapter/holdstate.go:189-205.
- **The remedy for the two accepted-residue findings is ONE SENTENCE EACH in the staged SPEC-5 §4.7.1 block,** never a request to build remediation position 2 (a durable pending-reclaim record, a reaper, a tree generation, or a bind-scoped lock). A fixer who reads the finding as "close the residue" changes the design the loop settled. EVIDENCE: spec-changes.md:284-289,:309-314.
- **MISTAKE: a fix stage declared a lane converged without sweeping `### Open` for entries marked FILED.** Round 7 ran two fix groups plus a corrections pass and touched neither of the two accepted-residue findings, which the standing `### Open` still recorded as FILED with an unapplied remedy. Sweeping `### Open` for FILED-and-unapplied against the staging took ten minutes at round 8 and found both live. Do that sweep before declaring convergence.
- **The two new §16.1 rows for the GATEWAY counters have NO gate at all.** `adapter_metric_catalog_test.go` reads only `pkg/adapter/metrics.go`, and nothing reconciles the gateway catalog against §16.1 or metrics.md, so if CODE-9's metrics.md rows are dropped, tier 11 stays green.
- **DOCS-2 claims `docs/reference/adapter-contract.md` "publishes `ConfigureWorkspace` as idempotent". The page does not.** Its row reads only "Point a pre-connected session at the finalized `cwd` (SDK-warm mode). Timeout: 10s."; the idempotency claim lives at spec/04:673 alone. DOCS-2 must either add the idempotency clause to that row or drop the self-reference. A separate trap bars filing the neighbouring `DemoteSDK` row; this is a different row and a different defect. EVIDENCE: docs/reference/adapter-contract.md:63; non-spec-changes.md:1826,:1828.
- **`ConfigureWorkspace` DOES create a registry entry** (`claimSessionSlot` at sdkwarm.go:216), so §5.2's entry-creating request list is complete and correct. A lens that assumes `ConfigureWorkspace` only "points" an existing session at a cwd will wrongly file the list as over-broad.
- **CORRECTS the orchestrator brief: `ShutdownResponse` does not gain `slot_was_started`, and `slot_reclaim` is new rather than kept.** The brief for this run states "ShutdownResponse keeps slot_reclaim and gains slot_was_started (4)"; both halves are wrong against the staging. A lens checking the §15.4 wire enumeration against the brief will file a phantom omission. Three round-8 lenses each spent time on it.
- **The §5.2 `**Slot cleanup:**` pointer is the one seam in the append where two readings survive.** The bullet sits under the `maxConcurrentSessions > 1` heading and its own trigger is "On slot completion or failure"; SPEC-3's pointer carries the ACTION LIST across the concurrency boundary while SPEC-3's next sentences deny the bullet's trigger on a one-session pod. One round-8 lens read the pointer as scoped to the action list and declined; the standing dead-end entry explicitly leaves a re-open argued from the bullet's own trigger open. EVIDENCE: spec/05:543,:545; spec-changes.md:641.
- **`sessionPolicy` "Pod reuse" (`maxConcurrentSessions: 1` with `recycle.enabled: true`) is a REAL configuration,** so the recycling half of the one-session carve-out is not vacuous. A reviewer who assumes one-session pods never recycle will wrongly refute any finding that rests on it. EVIDENCE: spec/05:425.
- **Candidate DROPPED, do not re-file: a surviving residue entry on a reused single-session pod suppresses the next session's MCP arming.** `claimPodMCPStartLocked` arms the intra-pod MCP surface only when `len(s.slots) != 1` is false, and `slotCount()` is the quantity §28.5.3's resolve-or-reject rule reads, so one residue entry makes every unaddressed frame unresolvable for the life of the pod. The behaviour is real, shipped and unchanged by any staged deliverable, and the staged sentence asserts nothing false, so the remedy would be new spec text about a pre-existing residue class. EVIDENCE: pkg/adapter/slotsession.go:109-115,:401-407; pkg/adapter/podscrub.go:39-81.
- **Candidate DROPPED, do not re-file: the crash-stranded tokened entry is a failure mode less reliable than shipped.** Today a retry ADOPTS the stale entry (`ensureSlotStateLocked` returns the existing `*slotState`) and succeeds; under the staging every later attempt at that session on that pod is refused `SLOT_BIND_ATTEMPT_SUPERSEDED` until the pod is replaced. The mechanism is real and the adoption half is verifiable, but the proposal states it bullet for bullet as an accepted failure mode, its incidence is crash-only, and the accounting drains the pod at `maxConcurrentSessions: 2` on one refusal. EVIDENCE: pkg/adapter/slot.go:104-107; spec-changes.md:296-307.
- **Candidate DROPPED, and it cost the most time of any round-8 candidate: "SPEC-4's retiring exit is infeasible on a sequential-reuse pool."** The spec sentence it rests on is real (spec/06:80) and `failPhase`'s drain really is a bare `DeleteClaim`, but the shipped projection resolves a claim DELETE on a `claimed` pod to `Draining` unconditionally of recycle, and spec/05:455 plus §6.2's pod-crash paragraph both retire a failed session's pod regardless of recycle settings. Two rounds have now walked this to the end.
- **MISTAKE: the r8→r9 rewrite deleted the §4.7 row's clean-exit sentence and broadened §7.1's leak predicate in the same round.** Each change is defensible alone; together the broadened predicate lost its referent, and a conforming third-party adapter could answer `absent` with `exited_cleanly: false` and leak the slot. Cost: one design-defect finding, a round, and three stale back-references that still asserted the deleted sentence. When a fix deletes a sentence another section depends on, grep the whole proposal for the claim before landing it.
- **MISTAKE: an earlier round declared spec/06:80's `idle` clause "correct as written" and told the fixer to leave it.** It was correct only while its sibling still carried the `recycle.enabled: false` qualifier. Widening the sibling to "either recycle setting" made both clauses true at once for a claim deleted on a `maxConcurrentSessions: 1` + `recycle.enabled: true` pod under its limits, which is exactly what a failed bind produces, and one input got two answers. Widening one clause of a two-clause partition is not a local edit; both clauses move together.
- **MISTAKE: SPEC-4's own rationale said the claim-deletion rule was stated "twice".** That count is what kept eight rounds from looking in spec/04, where it is stated twice more. It is five sites across two sections. `grep -rn "one-session-only invariant" spec/` and `grep -rn "recycling pod under its limits" spec/ docs/` find every one in two commands, and the second also surfaces the docs mirror. Cost: one full round.
- **MISTAKE: a client-surface shard dropped the §15.1 "bind-sequence request" finding on the wrong ground,** writing "it dies on the scope word: the staged sentence says 'a setup-window request'". The sentence's SECOND disjunct was "a bind-sequence request", which §4.7.1 binds to six RPCs, five of which never reach `writeSetupCommandError`. The drop cost three rounds of the defect standing. Read every disjunct of a sentence before dropping a narrowing finding on one of them.
- **Do NOT file the §15.1 exclusion sentence's parenthetical as an incomplete partition.** After the narrowing, a `FAILED_PRECONDITION` answered to a non-setup-command bind request is not this code and stays the retryable fallback, which is the CORRECT disposition, so only the appositive gloss ("every gRPC code other than `FailedPrecondition`") is imprecise. Two lenses reached it and both declined; a filing needs a wrong OUTCOME rather than an imprecise gloss.
- **Do NOT file the docs-mirror trailer against DOCS-1's body, for the seventh time.** The paragraph states an obligation DOCS-1 has not discharged; both texts are correct as written, the spec lane may not author staged docs content, and the gap is closable only by the non-spec lane or the reconciliation pass. Six lanes have derived it independently and each burned two verifiers on a finding its own lane could not close.
- **WATCHOUT: two anchors in the staging are identifications rather than "reads, verbatim" blocks, and `grep -cF` returns 0 on both.** §4.6.1's deleted closing sentence is quoted with `§6.2` where the tree writes the full markdown link, and the fenced `claimed ──→ draining` replacement quotes no original at all, disambiguated only by "In the fenced `Occupancy projection` block" while four other `claimed ──→ draining` entries live in sibling groups of the same fence. Check the instruction's own wording before filing a byte mismatch.
- **MISTAKE, three times: re-deriving §4.6.1's preamble ("a level-triggered projection of claim existence, claim binding state, and the pool's `sessionPolicy`") as falsified by SPEC-4.** It survives on TWO arms and a refutation naming only one does not stop the next lens: `scrubProfile` in the same bullet list, and `recycle.maxPodUptimeSeconds` in the uptime-drain sentence at spec/04:418 with its matching `idle ──→ draining` fence entry at spec/06:98. A fourth lens reaching this candidate should stop at both.
- **Do NOT file `docs/reference/adapter-contract.md:81`'s "at each session release" under SPEC-3's withheld report.** Three more lenses re-derived it this window and each was saved by the standing dead-end entry: the staged §5.2 text says the pre-`running` cleanup "**also** runs", so it is an additional trigger beside every session release and the row stays true. The same reasoning covers `docs/reference/execution-modes.md:68` and `docs/operator-guide/security-principles.md:33`.
- **MISTAKE: the snapshot trap fired in EVERY round of this window, and findings were again written against stale text.** `spec-r9` through `spec-r15` were each byte-identical to the live proposal at their own round's start, so the real delta is always against an earlier snapshot and was sometimes two rounds back. Run `diff -rq` across the whole chain first, take the newest snapshot that DIFFERS, and when every one is identical use `git diff HEAD -- proposals/0081_*/`.
- **Do not schedule a lens over text that has not moved; this window is the proof.** The citation, client-surface, feasibility, fresh-holistic, operational, performance and reliability lenses each returned empty between three and five consecutive times over byte-identical owned text, and the one filed defect of the whole recheck came from a citation sweep of text that HAD moved. Re-dispatch a lens only when its own owned block changes.
- **WATCHOUT: the orchestrator brief has carried "`ShutdownResponse` keeps slot_reclaim and gains slot_was_started (4)" for at least six rounds, and both halves are false.** `slot_reclaim` is NEW at field 3 and `slot_was_started` was deleted from the whole staging. Its "SPEC-6 stages three counters" is stale the same way. At least six lenses have spent time on the phantom omission before the log corrected them.
- **MISTAKE: a fix round corrected the `superseded` gloss at one site and left the others.** The predicate is stated at five sites, wide at two and narrow at three, and the round that filed it had already written "if a fixer touches one, it must touch all three". Sweep every spelling in the same edit or the drift reappears at whichever one is missed.
- **Do NOT "fix" the narrow `superseded` gloss by making a named reclaim REMOVE an untokened entry.** An untokened entry can be a live successor's own `StartSession` entry, so removing it tears down a running session. `superseded` there is the fail-closed answer; the fix is the outcome NAME only, widening the gloss to cover an entry no bind attempt owns while leaving "performs neither, removes nothing" exactly as staged.
- **WATCHOUT: `pkg/gateway/observability/catalog.go` does not exist, and CODE-9 named it twice.** The gateway counters belong in `pkg/observability/metrics/catalog.go` plus `spec161Metrics` in its `catalog_test.go`; the collector sits with the other gateway slot series in `pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go`; and the adapter counter must be registered in `pkg/adapter/metrics.go` or the tier-11 adapter sweep never sees it. The reconciliation pass applied this; do not re-derive the wrong path from an older entry.
- **MISTAKE nearly filed, repeatedly: "SPEC-4's re-key introduces new pod churn" and "SPEC-4 removes the one-session-only control".** Both die on the controller. The projection already drains a `claimed` pod whose claim is deleted, on either recycle setting, so SPEC-4 corrects the spec toward the code, adds no replacement rate, and strictly tightens the scrub-before-idle invariant; the deleted §4.6.1 sentence was false rather than protective, and the replacement bullet carries the control.
- **WATCHOUT: `ProjectOccupancyPhase` returns `("", false)` for a claim-less pod in any warm-fill phase,** deliberately, so the occupancy projection never fights the warm-fill writer. Reading §4.6.1's "`idle` for a pod in a warm-inventory phase with no claim" as an assertion that the projection WRITES `idle` there is a misread: it is the steady-state value the Sandbox-to-Pod reconciler writes.
- **MISTAKE a resumed round repeats: re-executing lenses whose shard is already on disk.** One round's shard directory already held a file for every lens and several re-ran a pass whose honest answer was recorded. Read your own shard first and diff the staging's mtime against it; when the staging has not moved, re-run only the two or three mechanical checks that carry the verdict and say so.
- **WATCHOUT: `.spec-changes.md` has no trailing newline, and the glob `*spec-changes.md` matches BOTH staged-change files.** `sed -n 'N,Mp' *spec-changes.md` concatenates them so every line number you read is wrong, and `tail` makes the file look truncated mid-word. Use the full filenames.
- **Reading this file needs the Read tool in passes of 100 to 150 lines, and `### Traps` and `### Open` first.** Bash output above roughly 2KB is persisted rather than shown and the Read tool refuses more than 25k tokens per call, so the whole log is roughly twenty-five calls. The cost is dominated by the number of TURNS rather than the number of calls, because each turn re-sends the accumulated context: issue several Read calls in one turn.

- **MISTAKE, three rounds on ONE sentence: the per-slot guard's scope written as an RPC enumeration.** Round 8 stated it as a list of RPC names; the list omitted `Resume`, whose checkpoint extraction is the adapter's biggest unlocked tree write, and included `Shutdown` whole, which put the two arms that remove nothing behind an unbounded materialization. Round 9 found both. Adding `Resume` to the list is the same answer one step smaller, so the replacement is a stated PREDICATE plus a derivation table with a "path work outside `s.mu`" column, which is checkable against the tree while a list is not. The enumeration is the defect, not its contents.
- **MISTAKE, two more rounds on the same paragraph: bounding a WAIT and protecting the WORK AFTER the wait are different problems.** Round 10 answered "§10.1.4 pass 2 blocks on an uninterruptible guard" by making the acquisition context-bounded, which stopped the wait running past the budget but not CONSUMING it; round 11's suggested fix (slice the shared budget per member) repeats the kind. The answer that held decouples the two budgets. A related false universal, "one section's holder cannot delay another section past its own budget", was DELETED rather than qualified, because a caller-budget claim stated inside a paragraph about the guard HELPERS is false wherever two sections share a context, which §10.1.4 pass 2 does.
- **MISTAKE: the staged `ensureSlotStateLocked` create arm called `s.newSlotStateLocked(slotID)`, a symbol that exists nowhere in the tree or the proposal,** and assigned it one value. The shipped create branch does three things that block does not: it lazily seeds `s.slots` (which `New()` never initialises, and the adapter's own tests build `Server` by struct literal), it calls `resolveSlotPaths` — which is `slotlayout.Resolve` → `ValidateSlotID`, the fail-closed path-traversal guard `TestStartSessionRejectsAMalformedSessionAddress_spec_5_2` rides on — and it calls `slotlayout.EnsureTree`, and it RETURNS BOTH ERRORS. The fix is the shipped body inline under `case !ok:` plus the stamp; minting a helper adds a name two enumerations must then carry.
- **MISTAKE: a paragraph that announces itself as a correction is the least-checked prose in the file.** CODE-4 carried "Correcting a claim the shipped text carried: the disposition does not travel through `ReleaseSlotReservation`'s own parameter list" a hundred lines above its own staged four-argument signature, and the false half propagated into checklist S19, where an implementor executing it produces code that does not compile. The framing is what let it stand through several rounds. Grep for the thing a "Correcting a claim…" paragraph claims to correct before trusting it.
- **MISTAKE: the reconciliation pass rebuilt the checklist from the review log's `### Deferred` block VERBATIM, and one entry was already stale.** A DEFERRED written N rounds ago describes the deliverable as it was then; this one told an implementor to route the SDK-warm confirmation refusal through `releaseSessionSlot`, which the deliverable had since been rewritten to forbid, and which destroys a successor's entry and tree. Re-read the DELIVERABLE before landing a DEFERRED correction.
- **WATCHOUT: `DemoteSDK` names two different things and the bare name reads as the wrong one.** `SDKWarmRuntime.DemoteSDK` (sdkwarm.go:134) is the runtime method CODE-2's refusal arm calls; `Server.DemoteSDK` (sdkwarm.go:274) is the RPC handler, which deregisters the entry and removes the tree through `releaseSessionSlot` and must not be used there. Always qualify. The SDK-warm `ConfigureWorkspace` failure arm SHIPS as `s.releaseSessionSlot(sessionID)` guarded on `fresh` (sdkwarm.go:249-252), which is exactly the idiom the new refusal arm forbids, so copying the neighbouring arm is the natural wrong turn.
- **WATCHOUT: Go defers run LIFO, so a defer registered LATER runs FIRST.** A design note saying the guard unlock should be "deferred after `defer release()` so the guard outlives the destructive section" has it backwards: `defer unlockSlot()` must be registered BEFORE `defer release()`. This was written down wrong once and corrected in the same round.
- **WATCHOUT: dropping a guard-table entry never blocks a holder.** The holder keeps the old channel and the next acquirer mints a second one, so a drop voids the guard at exactly the moment it is needed. The table is pod-lifetime and deliberately never dropped; a refcount was rejected as a second mechanism with its own leak mode for a table the pod's served-session count already bounds. A finding's own closing sentence ("having `Shutdown` hold the guard across its destructive section closes this too") is wrong on its face — the deletion would happen while `Shutdown` itself holds the guard.
- **WATCHOUT: the expiry disposition must NOT be uniform across the two hand-out forms.** A destructive section that cannot obtain the guard proceeds and destroys unguarded, which is today's shipped behaviour; an admission RPC that cannot obtain it must be refused, because proceeding unguarded is how a materialization re-creates the tree a reclaim just removed. One rule for both is the tempting simplification and it reintroduces the original race.
- **WATCHOUT: `Server.reclaiming` and `Server.slotGuards` both need lazy-init guards.** Adapter tests build `Server` by struct literal and a write into a nil map panics. CODE-6 states the seed for `s.slots` and says nothing about either new map.
- **WATCHOUT: `pkg/gateway/podlifecycle/podsession/slotbinder.go:541` is a COMMENT line; the `Shutdown` call is at :542.** The off-by-one survived several rounds inside a staged code comment. `:574` (`ShutdownRecycle`) is exact.
- **Do NOT re-file the `slotFailureWorkspaceFinalize` reclassification, now for the fourth time.** `SlotBindError.Reason()`'s only stage-sensitive arm is `e.Stage == slotFailureWorkspacePrep` (slotfailure.go:90-99), so moving the new constant onto the `slotBindError` argument at slotbinder.go:295 would flip a finalize `FailedPrecondition` from transient to `SlotReasonPolicyRejection`, which `NonRetryable()` reports true for and which contradicts the staged "no failure class becomes non-retryable". But CODE-9 cites only the two `recordSlotFailure` lines (:287, :294) and calls the change "a measurement separating them", and the Files-touched entry does not list the finalize `slotBindError` branch, so a verifier refutes the finding on CODE-9's own scoping. The stage constants live in binder.go:290-294, NOT in slotfailure.go, which is why the Files-touched line looks alarming. What is filable is the AMBIGUITY of the scoping, judged below the bar three times.
- **Do NOT file the deleted "crash takeover and the coordinator-handoff teardown" caller row.** It named a site that does not exist, it was the one row of six carrying no file:line, and it was deleted; `Client.Shutdown` sets the field itself so the table is informational. Five shards derived its falsity independently.
- **Do NOT file `cmd/lenny-compliance` "imports no `adapterv1`".** It is literally false (`schemaassert_test.go:9` imports it) and immaterial: the claim is about the binary having no adapter under test, which holds.
- **Do NOT file the "No `export_test.go` change" tension.** The Testing section says it while CODE-2 stages an edit at `export_test.go:45` and the files-touched list names the file. The colon-explanation that follows scopes the sentence to "no NEW exported seam is needed". Two verification agents would burn on it.
- **Do NOT file `spec161Metrics`'s doc comment calling itself a complete §16.1 transcription.** It is not, deliberately: it omits every adapter row carrying the scrape deferral, which is what makes the adapter series' exclusion correct.
- **WATCHOUT: the orchestrator brief has been wrong in the same two ways for more than ten rounds.** "`ShutdownResponse` keeps `slot_reclaim` and gains `slot_was_started (4)`" is false on both halves — `slot_reclaim` is NEW at field 3 and `slot_was_started` was deleted from the whole staging; `grep -c slot_was_started` over the proposal returns 0 outside this log's own corrections. And from round 10 on, "the non-spec lane has had zero rounds since the token revision" is false. At least ten lenses have spent time on the phantom field.
- **The snapshot trap fired in EVERY round of this window as well.** `non-spec-r9` through `non-spec-r12` and their `-start` siblings were each byte-identical to the live proposal, so `diff -rq` returned only the review log. The real delta is always against the PREVIOUS round's `-start` or `-prefix` snapshot (r8→r9 was 1021 diff lines; r9→r10 was 734). Run `diff -rq` across the whole chain first, take the newest snapshot that actually DIFFERS, and when every one is identical use `git diff HEAD -- proposals/0081_*/`.
- **WATCHOUT: `grep` over these staged files fails silently when a claim wraps across a line break.** `grep -c "no such parameter"` returned 0 while the sentence was plainly in the file, because the wrap split it. Grep a short unwrapped fragment when hunting further copies of a claim.
- **WATCHOUT: Testing-section text repeats verbatim between the tier-3 behavioural list and the CONF-1 negative-case list.** A grep hit count of two for a case title is ONE case, and a fix that edits one list and not the other is a half-fix that reads as complete.
- **Deliverable HEADING file lists are a live drift surface.** CODE-2's heading omitted `pkg/adapter/sdkwarm.go` while its body staged a whole refusal arm there; CODE-6's omits `credentials.go`, `server.go` and `sdkwarm.go`; the same class was confirmed earlier against CODE-6's omission of `resume.go`. `## Files touched` is the authoritative list, and a fixer touching a heading should re-derive it from there rather than patching one name.
- **MISTAKE nearly filed, the capacity family, four more dresses this window.** `slotGuards` unbounded growth (bounded by `maxSessionsPerPod` and by the claim DELETE at occupancy zero); the synchronous compensation holding a gateway goroutine and an adapter connection past the client's deadline (the RPC is inherent to the fix, the budget is §5.2's own figure, and §11.4's shipped revoke holds the same relation at 20s); `k8s_pod_name` cardinality (the shipped sibling `lenny_slot_failure_total` already carries it); per-slot guards serializing across sessions (the key is the slot identifier, which equals the session identifier, so co-tenants never contend). A filing here needs a concrete top-tier exhaustion with numbers.
- **MISTAKE nearly filed, the security family, five more dresses this window.** A failed bind adding a permanent `slotGuards` entry while `sessions_served` does not advance (capacity hygiene, not a bound); CODE-1's narrowing of `ReportSessionScrub` from `bound` to `live` as a relaxation of `maxSessionsPerPod` (the tree sends no `Shutdown` on those paths today, so the narrowing preserves shipped counting); the untokened-entry occupancy under-count; the residual per-slot credential file on a leaked concurrent slot; the §4.9 timer-cancellation ordering. Each is on the refuted list or recorded as an accepted residue.
- **Do NOT file the tier-2 envtest case on a `lenny.dev/drain-request` stamp.** `Binder.Prepare`'s ordinary failure runs `failPhase` → `b.drain` → `podclaim.DeleteClaim` (binder.go:1072-1082, :1200-1202); the annotation has exactly one producer, `Binder.DrainSandbox` → `podclaim.StampDrainRequest`, whose only production caller is the §5.2 unhealthy-threshold branch in `applySlotRetryPolicy`. No `Prepare` failure of either kind ever stamps it, so both arms observe the same nothing. The re-keyed case asserts the per-pod `SandboxClaim` present on the typed refusal and deleted on the ordinary failure instead.
- **Do NOT file the in-`pkg/` envtest file placement as a misplacement.** `pkg/gateway/session/recycle/scrubreporter_seams_envtest_test.go`, `pkg/embedded/stack/*_envtest_test.go` and `cmd/lenny-gateway/statelessrouting_envtest_test.go` all carry no build tag and run under the unit tier, while the component tier runs only `./tests/tier2_component/...` and two siblings. "Tier 2" in this proposal names the KIND of test rather than the runner's target, exactly as those precedents do.
- **Do NOT re-derive the whole-client-surface absence sweep.** `grep -rnE 'bind_attempt|unconditional_teardown|slot_reclaim|SlotReclaimOutcome|SLOT_BIND_' spec/ docs/ schemas/ charts/ sdks/ pkg/gateway/externalapi/openapi/` returns zero lines, confirmed independently by at least four shards. There is no SDK, CRD, OpenAPI, JSONL-schema or `docs/api` mirror of the adapter gRPC surface, so SCHEMA-1's fields, its enum and its two error codes mirror to exactly three staged places: the regenerated `pkg/proto/adapter/v1`, the tier-3 descriptor gate and the claim register.
- **Do NOT re-verify SCHEMA-1's field numbers.** Every number and every "Basis" cell was checked message by message against the shipped proto by at least six independent shards across this window: `PrepareWorkspaceRequest` 1-3 + reserved 4; `FinalizeWorkspaceRequest` 1-4 (`mid_session` already at 4) + reserved 5; `RunSetupRequest` 1-3 + reserved 4; `AssignCredentialsRequest` 1-2 + reserved 3; `ResumeRequest` 1-5, 7-14 + reserved 6, 15; `ShutdownRequest` 1-3, 5, 6 + reserved 4; `ShutdownResponse` 1-2; `ErrorCode` ends at 27 and the 1000-1999 Phase-2 range is prose with no `reserved` statement.
- **Do NOT re-run the non-spec citation sweep.** Two shards resolved every `path:line` token in `non-spec-changes.md`, `spec-changes.md` and `summary.md` mechanically and by hand, and a third spot-checked eleven at random; every one resolved. The known exceptions are recorded individually: `binder.go:1053` (fixed to :954) and `slotbinder.go:541` (a comment line). Sub-citations written as bare `:NNN` continuations are NOT caught by the extracting regex and must be read from the surrounding prose; roughly a third of the load-bearing anchors are of that form.
- **A resumed round re-executes lenses whose shard is already on disk.** Read your own shard first and diff the staging's mtime against it; when the staging has not moved, re-run only the two or three mechanical checks that carry the verdict and say so.
- **WATCHOUT: `deregisterSlot` has TWO TEST callers besides the production one, and CODE-6 retires it.**
`pkg/adapter/podmcp_arming_internal_test.go:88` and `:245` call the `s.mu`-taking wrapper at
`pkg/adapter/slotsession.go:192`. CODE-6 says "`deregisterSlot` is retired into the helper"
(non-spec-changes.md:1330) and enumerates the SAME FILE's `releaseSessionSlot` sites at `:144` and
`:195` twelve lines later (:1652), so the omission sits inside an enumeration the deliverable
otherwise completes. Retiring the function breaks both lines at compile time and the replacement is
a judgment call: `reclaimSlotLocked` requires the caller to hold `s.mu` and returns a `release func()`
whose invocation opens and closes a hold, so routing them through it opens a hold nothing releases.
Semantic risk is LOW (the hold is per-slot-identifier and the follow-on at `:88` claims a DIFFERENT
identifier, "bob") and the safe replacement is an inline `s.mu.Lock(); s.deregisterSlotLocked(...)`,
but the edit is not mechanical and nothing staged states it.
- **WATCHOUT: `Server.Resume`'s ordering is NOT what the proposal's citation list suggests.**
`restoreChunks`/`ExtractTree` (resume.go:105, body at :169/:206) run BEFORE `Runtime.Start` (:140)
and `noteRuntimeStarted` (:144). A lens reasoning that the guard must outlive the start because the
extraction follows it will build a false race. EVIDENCE: pkg/adapter/resume.go:50,:105,:140,:144,:169,:206.
- **WATCHOUT: a `Resume` is already a §10.1.4 "started member" while it sits inside `workspace.ExtractTree`.**
`claimSessionSlotUnderLock` sets `st.started` at slotsession.go:87-88, which on the resume path is
resume.go:50, long before the restore and the start. Pass 1 can therefore deregister its entry
mid-restore. That interleaving is benign under the staged design (the rollbacks find nothing and
`noteRuntimeStarted` then refuses), but it is not the interleaving the proposal's §10.1.4 prose
walks through. EVIDENCE: pkg/adapter/slotsession.go:87; holdstate.go:190; slotsession.go:374-388.
- **WATCHOUT: the orchestrator brief was still wrong in the same two ways at round 13.**
It says "ShutdownResponse keeps slot_reclaim and gains slot_was_started (4)" and "the non-spec lane
has had zero rounds". Both are false and the ledger has corrected them at least a dozen times
(review-log.md:1241,:1267,:3368). `grep -rn slot_was_started` over the proposal returns hits only
inside this review log, and SCHEMA-1's nine-row field table ends at `ShutdownResponse.slot_reclaim = 3`
(non-spec-changes.md:2129-2139). Do not derive a missing-test or a missing-field finding from it.

### Open

- **CODE-1 comment phrasing** — UNVERIFIED: non-spec-changes.md:72-77 still carries "the runtime teardown runs for a session the pod's shared runtime process has been given" for a gate that reads `st.started`; confirm G2 landed the rewrite. See `spec.1.fix-G1.1`.
- **Tier-4 fixture extension** — OPEN: the staged tier-4 datastore-crossing case needs a second placeable Sandbox, a `MaxConcurrentSessions > 1` pool and a wired session server, none of which `recycle_scrub_path_test.go` has, and extending `recycleCluster` changes a fixture seven cases share. Decide between extending it and splitting the retry-re-binds half down to tier 1. See `non-spec-recheck.1.review-applicability.1` and `non-spec-recheck.1.review-feasibility.1`.
- **Socket runtime on a recycling pool** — OPEN: after any last `Runtime.Close` the listener is gone, so the next session's `Runtime.Start` accepts on a closed listener. Either socket runtimes never land on recycling pools or the recycle path is broken for them. Pre-existing; worth its own finding. See `spec.1.fix-design-G2.1`.
- **§6.2 fence versus §6.2 prose on `leaked` at concurrency 1** — OPEN (filed as a separate finding, not for this proposal): the fence scopes `slot_cleanup ──→ leaked` to concurrent occupancy while §6.2's own leaked-semantics paragraph, §5.2's `ceil(maxConcurrentSessions/2)` threshold at 1, and `slothealth.Tracker` all apply `leaked` at concurrency 1. See `spec.1.fix-design-G2.1`.
- **`terminate` frame reason value** — OPEN, re-scoped: under the design as it now stands a bound-but-unstarted reclaim runs no runtime teardown, no close and no frame, because §4.7 puts the §15.4.2 signal inside the teardown and CODE-1 moves the drain inside the `started` block. The `reason`-enum question survives only for a reclaim of a session whose start WAS admitted, where `drainReason` maps `"slot_bind_failed"` to `session_complete`. The wire stays valid; nobody has decided whether that is the intended value. Re-scope the item before spending a round on the enum. See `spec.7.review-operational.1`.
- **Mid-start `Shutdown` bricks the pod silently** — UNVERIFIED, and narrower than it reads: `Close(sessionID)` for a session not in `p.active` still takes the last-close teardown whenever `p.active` is empty and `p.connected` is true, closing the listener bound once at adapter start. Reaching `connected && active empty` needs the `Interrupt` path, which closes the conn without clearing `connected`. Pre-existing, reachable only after an Interrupt of the last active session, and the remedy is code-lane. See `spec.1.review-docs-alignment.1` and `spec.2.review-reliability.2`.
- **§4.1's retired vocabulary** — OPEN: SPEC-1's §4.1 replacement retires "per-session teardown" while the retained second sentence still says "The per-slot teardown and the whole-pod teardown are the same operation on the same address". After the split there are three named operations and the retained sentence names two the new sentence does not define. See `spec.1.review-fresh.1`.
- **`receiving_uploads` on an upload-free plan** — UNVERIFIED: if the §6.2 edge label "workspace materialization begins for this slot" is read as `PrepareWorkspace` rather than as entering the staging stage, a reclaim on the upload-free branch has no legal edge out of `slot_assigned` and SPEC-4 does not cover it. The charitable reading was assumed. UNVERIFIED and disputed: `spec-recheck.3.review-reliability.1` reads SPEC-4's own prose ("Every earlier stage of the §4.7.9 step-5 bind sequence ... leave the slot in `receiving_uploads`") as asserting the charitable reading outright and therefore closing the item, while the later `f2.open-decisions.out-of-scope-defects.slot-assigned-terminal` records it as still unsettled and rests its scope call on the charitable reading. Neither corrects the other. See `spec.1.review-kubernetes.1`.
- **§5.2's bullet at concurrency 1** — UNVERIFIED: whether §5.2's `**Slot failure and cleanup**` bullet is meant to govern the slot release on a `maxConcurrentSessions: 1` pod at all, given the staged §4.7 row defines the slot release as "the §5.2 slot cleanup" with no concurrency scoping. `spec.1.review-feasibility.1` argues it does not over-reach; `spec.1.review-performance.1` still wants it decided.
- **spec/05 versus spec/06 on failed-session pod retirement** — OPEN as a pre-existing wording tension, CLOSED for this proposal's sentence: spec/05:455 and :492 say a session that ends in failure or a crash always retires its pod, while spec/06:283 says the pod "is marked `failed` and released back to the pool (or terminated if unhealthy)". The §6.2 fence resolves it in favour of retirement, which is what makes the staged §7.1 citation hold. Predates this proposal, and a fix elsewhere should not deepen it. See `spec.1.review-security.1` and `spec.2.review-operational.1`.
- **`terminate` frame `deadlineMs`** — UNVERIFIED, pre-existing and code-lane, now NARROWED: the frame requires `deadlineMs` with `minimum: 100`, `RuntimeOps.Terminate` serialises it `omitempty`, and the two shipped gateway callers (`slotbinder.go:542`, `binder.go:2043`) still pass 0, so a Full-level runtime receives a frame missing a required field at an ordinary session end. The `budget/2` split answers it FOR THE COMPENSATION'S CALL SITE ONLY, which becomes the one `Shutdown` in the tree that satisfies the minimum; the item stands everywhere else. Whoever owns the tier-3 runtime-ops contract test should confirm. See `spec.2.review-client-surface.1` and `non-spec-recheck-2.2.fix-design-G1.1`.
- **`/sessions/` and `/artifacts/` in the widened action list** — UNVERIFIED: `RemoveTree` removes four trees and SPEC-3's widened §5.2 list names two of them plus the timers. The omission predates the proposal, so it was judged incompleteness rather than a defect, but a completeness lens may disagree. See `spec.2.review-citations.1`.
- **"incomplete enumeration" under §29's preamble rule** — OPEN: §29 item 12 gains no fourth `Shutdown` trigger from SPEC-2, and the preamble subordinates a trace only where it DISAGREES. Somebody should decide once whether an incomplete enumeration counts as disagreement and record the answer, because the question returns every round. See `spec.2.review-citations.1`.
- **Concurrent resume leaves a freshly claimed pod at occupancy 1** — OPEN, and the earlier narrowing was CORRECTED: the hold is bounded by §4.6.1 orphan GC at `claimOrphanTimeout`, not by the §5.2 replacement trigger. At `maxConcurrentSessions >= 3` the trigger never fires, because `UnhealthyThreshold` is `(maxConcurrent+1)/2`, `slothealth.Tracker` counts per pod, and each §7.3 retry claims a DIFFERENT fresh idle pod, so no replacement pod accumulates two leaks. `PodHasActiveSession` is false on a freshly claimed replacement pod (`bumpRecoveryGeneration` runs only on success, so `row.PodAssignment` still names the OLD pod), which is what makes predicate 1 fire. The proposal itself is not wrong here; its churn bullet scopes the drain claim to `maxConcurrentSessions: 2`. Whether `ClaimSlot` pass 1 treats such a pod as slot-bearing is still unverified. See `spec.2.review-performance.1` and `non-spec.2.review-kubernetes.1`.
- **Does CODE-4 owe a pod release on the resume path?** — UNVERIFIED: `Binder.Resume`'s error branch does `cl.Close()` plus a no-op `releaseResumeSlot`, so §7.1's "the failed attempt releases the pod's claim and the pod retires" is discharged on the §7.3 re-attach only by §6.2's general policy and never by a staged code path. Judged pre-existing and left unfiled; a code-lane reviewer should decide. See `spec.2.review-feasibility.1` and `spec.2.review-reliability.2`.
- **Whose view of "running" does §7.1 mean?** — OPEN: under a pod-state reading the obligation has ended before §7.2 step 3's reclaim is owed; under an attempt-completion reading it still holds. Round 4 narrowed the upper bound to "ends when that attempt succeeds", which favours the second reading without saying so. See `spec.3.review-fresh.1`.
- **Does §7.3 owe a release step at all?** — UNVERIFIED: the §7.3 appended sentence orders the reclaim "before the replacement pod is released", but §7.3's numbered flow states no replacement-pod release on any branch, and §6.2's three non-terminal `resuming` failure bullets state no disposition for the half-claimed pod either. Only §7.2 step 3 releases it. Four lenses judged it prose imprecision. See `spec.3.review-edit-sites.1` and `spec.5.review-client-surface.5`.
- **Do the other three `resuming` failure bullets need the reclaim clause?** — UNVERIFIED: pod crash, the 300s watchdog and non-retryable errors state no pod release today, so there is nothing for the clause to attach to, but §6.2 calls itself the authoritative enumeration of every edge out of `resuming`. Decide once. See `spec.7.review-mechanism.1`.
- **Transient over-assignment on the acknowledged racing-start ordering** — UNVERIFIED: the gateway releases the reservation with `leaked=false`, decrementing the counter for a slot a live session still occupies, so the pod can transiently carry `maxConcurrentSessions + 1` sessions, which is what §5.2's "Slot assignment atomicity" exists to prevent. The racing-start bullet records the residue but not this consequence. See `spec.5.review-reliability.1`.
- **§6.2:160's two claims about the leak count** — UNVERIFIED: it says the persistent count is the `lenny_adapter_leaked_slots` gauge "equivalently the leaked portion of the pod's Redis slot-counter occupancy" and that the ADAPTER exposes it. Both halves are already wrong against the tree before this proposal. The gauge half is a recorded dead end; whether the Redis-equivalence half is a separate spec finding is undecided and is not 0081's. See `spec.5.review-performance.2`.
- **Should the timer cancellation be conditioned on the credential removal?** — UNVERIFIED: `deregisterSlotLocked` cancels the §4.9 direct-mode timers at the top of `Shutdown` while `removeSlotTree` runs after the drain signal and `Runtime.Close`, so a cleanup whose credential removal fails leaves a direct-mode key with its enforcement point disarmed. Judged near-unreachable and pre-existing. A round wanting to close it should condition the cancellation in SPEC-3's sentence rather than widen §4.9. See `spec.5.review-security.1`.
- **Does the retry re-increment the Redis reservation?** — UNVERIFIED: `BindReservedSlot`'s release-on-failure leaves `PodAssignment` set, so a client retry may bind a reservation the counter no longer holds, while the edge-case bullet reads as though the reservation survives. Pre-existing and code-lane. See `spec.5.review-feasibility.1`.
- **§29.10's "Shared by the whole pod" list** — OPEN, pre-existing: it names neither the pod's shared runtime process nor CH-RUNTIMEOPS. Deliberately not pulled into the §29.4 edit, and a candidate finding for a later round. See `spec.5.fix-G1.1` and `spec.7.review-operational.1`.
- **Should `maxSessionsPerPod` count a bind that reached `RunSetup`?** — UNVERIFIED: §4.7.9 step 5 runs setup commands on the pod before `AssignCredentials`, so a bind abandoned at `ready` has executed code that primes the residual-state vectors §5.2 enumerates, and neither the tree nor the staged spec counts it. Pre-existing; a human or a later proposal should decide. See `spec.7.review-security.1`.
- **Churn from the third accounting caller** — OPEN: on the §7.3 re-attach the accounting is reached for the first time at every concurrency, so at `maxConcurrentSessions: 2` one clean restore failure now drains the freshly claimed replacement pod. The recorded fallback is to account only the leaked arm, stated as a scope in the helper's doc comment. Nobody has priced it against a resume storm. See `spec-recheck.1.fix-G1.1`.
- **Correlated re-attach churn at Tier 3** — OPEN: a whole-node loss puts every slot on those pods into `resume_pending`, and each failed re-attach now drains a fresh replacement pod that never hosted the failed session. The per-instance change is stated in writing; the aggregate against Tier 3's warm-pool headroom is not. A chaos-tier lens is better placed than the performance one. See `non-spec-recheck.1.review-performance.1`.
- **What disposition the exclusive-pod abandonment arm should name** — UNVERIFIED: §5.2 scrub step 6 marks the scrub failed if `/workspace/slots/` or `/run/lenny/slots/` is non-empty, which looks like the right pointer, but nobody has checked whether a half-completed cleanup's tree trips step 6 or is swept by steps 1-6 first. See `spec-recheck.3.review-applicability.1`.
- **Does §4.6.1 orphan GC reclaim a resume-path leaked slot?** — UNVERIFIED: it drains a `bound` claim whose pod no active session references, so it fires only while the pod carries no other session; once `ClaimSlot` pass 1 places a same-tenant session there the claim is referenced and the leak has no reclaim route. Check `claimOrphanTimeout`'s predicate against a pod at occupancy 1 with one live sibling. See `spec-recheck.1.review-performance.1`.
- **Does the resume-path slot failure owe an `error_type` value?** — OPEN: `lenny_slot_failure_total` has no series on that path, so a §7.3 re-attach can move the leak gauge and trip the threshold with no slot-failure signal. A new label value mints no §16 or metrics-reference edit, because both rows state the label without enumerating its values. Either answer is legal and the proposal states neither. See `non-spec-recheck.1.review-operational.1`.
- **The tier-7a park placement, and what the co-tenanted variant asserts** — OPEN: where the park sits relative to `SocketRuntimeProcess.Start`'s `accept` decides which of the two closes tears the process down, and the paragraph deliberately asserts nothing about it. The variant now names the pod-level cohort outcome instead. A later round either names a teardown assertion, which needs the placement settled and a real `SocketRuntimeProcess` behind the gate, or drops the variant. See `non-spec-recheck.3.fix-G1.1` and `non-spec-recheck.3.fix-design-G1.1`.
- **Is tier 8 reached?** — UNVERIFIED and declined by three rounds: the change is a failure and recovery path in the plain sense, but the tier-8 examples in `.claude/rules/test-coverage.md` are all infrastructure failure injection and the adapter-refusal and blob-outage cases are covered at tier 4. See `non-spec-recheck.3.review-test-coverage.1`.
- **`binder_test.go` is in no file list** — OPEN: it holds `fakeAssigner` and the `Binder.Resume` fixtures the staged resume case cites, and appears in neither the gateway-tests list nor "Files touched on application". Same package as `slotbinder_test.go`, so nothing fails to compile. See `non-spec-recheck.3.review-applicability.1`.
- **Does a §15.1 retry reach a pod already stamped for drain?** — UNVERIFIED: nothing in `bindReservedSlot` reads `Status.Phase`, so a retry pinned to `row.PodAssignment` may dial a draining pod, succeed and then die with it. The answer decides whether the withdrawn reserved-branch composite is an accepted limitation or a stuck-session bug. See `spec-recheck.3.review-performance.1`.
- **The `**Recycle lifecycle**` paragraph is in no edit list** — OPEN: spec/05:455 is the surface that governs a one-session pod that does NOT retire, and its only retire rule is the failure-or-crash one, which a `completed` terminate does not trigger. Any future sentence asserting "the pod retires" for an exclusive pod has to survive it. See `spec-recheck.3.review-edit-sites.1`.
- **The pod MCP surface a same-pod retry inherits** — UNVERIFIED: after an ACKNOWLEDGED reclaim, `mcpArmingHeldLocked` is true for any entry under the id including the retry's unbound workspace-prep entry, so the retry's `claimPodMCPStartLocked` returns `startMCP == false` (`s.mcpSession == sessionID`) and the retry inherits a surface serving the abandoned attempt's manifest nonce. Looks pre-existing and is outside both findings that raised it; somebody should check whether the nonce mismatch is real. See `non-spec-recheck.5.fix-design-G2.1` (slotsession.go:254-260,:288-292).
- **Nothing at any tier pins that the compensating `Shutdown` stays inside its budget** — OPEN: the budget is the only thing bounding the detached context, and on an exclusive pool reached through `Binder.Resume` it degenerates to the whole pool `cleanupTimeoutSeconds` (up to 60s). Recorded rather than filed, because a prior round refuted the "`slotCleanupBudget` has no listed test" finding as an extra case inside a tier the proposal already exercises. See `non-spec-recheck.5.review-performance.1` and `non-spec-recheck-2.1.review-performance.6`.
- **Can a workspace-prep handler write into the slot directory after `removeSlotTree`?** — UNVERIFIED: the top-of-handler `ensureSlotPaths` makes a post-reclaim ENTRY insert unlikely, but a slow `FinalizeWorkspace` or `RunSetup` still writing into the slot directory after the removal re-creates directories, leaving a tree with no entry. Judged below the bar (the whole-pod scrub's `rm -rf /workspace/slots/*` sweeps it and no residue predicate reads it), but nobody has traced the file-writing path. See `non-spec-recheck.5.review-reliability.1`.
- **CODE-2's replacement doc comment for `noteRuntimeStarted` drops the shipped idempotency sentence** — UNVERIFIED: the BEHAVIOUR is untouched, because the guard lives in `noteRuntimeStartedLocked` (runtimegeneration.go:40-43), which CODE-2 does not edit. Whoever lands CODE-2 should keep the sentence or move it onto the `Locked` form. See `non-spec-recheck.5.review-edit-sites.1`.
- **Do N8's retired spec-line citations reach proposal rationale prose?** — UNVERIFIED: `0081_....spec-changes.md`'s rationale cites spec line numbers, `.claude/rules/channel-naming.md` N8 retires that form, and its `proposals/` escape hatch is written against N3 rather than N8. Moot for the applied spec, since no staged spec text carries a line number. A human or a naming-lens pass should settle it once for the campaign. See `spec-recheck-2.2.review-citations.1`.
- **Does the create-time-reserved edge-case bullet's "reconnects to the same pod" survive §6.2 and §15.1?** — UNVERIFIED: it sits beside spec/06:288 ("Each retry claims a fresh pod") and spec/15:1136 ("recovered with a fresh pod per §6.2"). The reading that separates them (the gateway's internal pre-attached retry loop versus a fresh client call against a row that keeps its pod binding) was assumed rather than proved from the code. A code-lane reviewer with `bindConcurrentSlot`/`PodAssignment` open should settle it. See `spec-recheck-2.2.review-edit-sites.1`.
- **Does the residue's drain-suppression harm deserve any direct observation?** — UNVERIFIED: the round-2 design says no, on the ground that the harm IS the residue and the tier-4 case's first assertion states the residue directly while `boundRemains` is pinned on both arms by the shipped tier-7a test. A reviewer who disagrees should argue against the registry-assertion-is-the-pin claim rather than re-open the fixture wiring. See `non-spec-recheck-2.1.fix-design-G2.1`.
- **The file-collision decision and the 0080 impacts row disagree on the adapter file list** — OPEN, and it is the nine-versus-eleven item seen from the impacts side. The `**Decisions.**` file-collision bullet enumerates NINE `pkg/adapter` files; the 0080 row and `## Files touched on application` carry ELEVEN, adding `resume.go` and `sdkwarm.go`. The load-bearing half agrees at both sites, so the row's conclusion stands either way; a fixer correcting one site must sweep the other. Three firings have declined it as out of scope for a format pass. See `f3.open-decisions.cleanup` and `f7.summary-cleanup`.
- **Does the `spec-changes.md:98-102` blob-store clause still need repair?** — UNVERIFIED, and two entries disagree with no correction between them. The standing Deferred said the spec half stands uncorrected while the non-spec half was repaired; `non-spec-recheck-2.4.review-edit-sites.1` read `spec-changes.md:97-103` directly and reports the bullet ALREADY repaired to the windowed-counter wording, closing with "do not re-file it and do not re-apply it". The newer reading was kept and the Deferred retired. Whoever next opens `spec-changes.md` should confirm which is true before editing that bullet.
- **Does `SocketRuntimeProcess.Close` return within the grace it is handed?** — UNVERIFIED, and the whole margin argument for the `budget/2` split rests on it. `resolveShutdownGrace`'s wait loop was read as a contract rather than traced. A code-lane reviewer with socketruntime.go and mcpruntime.go open should confirm once and record it. See `non-spec.2.review-reliability.1`.
- **Is the `budget/2` window ever shorter than a Full-level runtime needs to quiesce?** — UNVERIFIED: the floor puts it at 2500ms and the reclaim is tearing down an abandoned session, so it was judged inert, but nobody has measured it against a real runtime. A conformance or tier-10 lens is better placed. See `non-spec.1.review-reliability.1`.
- **Does any §4.9 or §13 statement bound how long a failed attempt may hold a minted lease?** — UNVERIFIED: the compensation's ordering leaves the §4.9 leases live for up to the whole budget while the client's request is held. Judged not a security finding (the leases are returned unconditionally on the error path, so it fails closed) and not new in the delta. A credential-lifecycle lens should confirm. See `non-spec-recheck-2.4.review-security.1`.
- **Is S5, a `docs`-lane step, permitted to write `tests/tier11_docs/*.go`?** — UNVERIFIED: the proposal puts the tier-11 edits in DOCS-1's step and the checklist line says "Tiers 0, 11". If the implement-proposal write lease is lane-scoped by path rather than by tier, S5 cannot land its own gate. Someone who knows the lease mechanics should confirm. See `non-spec-recheck-2.4.review-docs-alignment.1`.
- **`SLOT_FAILED` appears in no `spec/` or `docs/` file** — OPEN and pre-existing: it is a client-visible 422 emitted at start.go:313 and pinned by a tier-3 test, and `docs/reference/error-catalog.md` and `docs/client-guide/error-handling.md` both lack a row for it. Outside 0081, which mints no new code, but a real documentation gap some proposal should own. See `non-spec-recheck-2.4.review-docs-alignment.1` and `non-spec.1.review-client-surface.1`.
- **`docs/reference/configuration.md:99` is now three effects behind on `cleanupTimeoutSeconds`** — OPEN: the row describes it as "Timeout for `cleanupCommands` plus the scrub-report grace" with the constraint "Must be `> 0`", already omitting the per-slot formula and the CRD rule `cleanupTimeoutSeconds >= maxConcurrentSessions × 5`, and CODE-4 adds a third effect (the gateway's give-up bound, half of it the adapter's graceful window). Not filed, because a docs edit would document a code-only choice the spec deliberately leaves unnumbered. See `non-spec.2.review-docs-alignment.1`.
- **Does `ClaimSlot` pass 1 admit a pod the WarmPoolController is already draining?** — UNVERIFIED and pre-existing: pass 1 reads the per-pod claim and never the Sandbox phase. Outside this proposal, and no lens here has been asked to judge it. A conformance lens after implementation should decide. See `non-spec-recheck-2.4.review-kubernetes.1`.
- **The unscoped `leaked` in the new hold paragraph and in SPEC-4's :580 sentence** — OPEN, FILED as two findings in two deliverables. See `spec.1.review-operational.1` and `spec.1.review-security.1`.
- **Is a second `POST /v1/sessions/{id}/start` for the same session admitted while the first is in flight?** — UNVERIFIED: it decides how wide the unfenced first-RPC window is. The finding was reached on the budget-expiry route instead. A code-lane reviewer with the `/start` precondition table open should confirm. See `spec.1.review-performance.1`.
- **The `/finalize` Gap-2 window cannot discharge §7.1's reclaim obligation** — OPEN, FILED: both branches fail after `Binder.Prepare` closed its connection and call `ReclaimClaimed`, which sends the adapter no RPC, while §7.1 names the creation finalize block, mandates the reclaim on the connection the failed stage still holds, and forbids re-dialling. The fixer must pick one of two legal answers and make §7.1, §4.7.1's caller rule and the accepted-failure-mode list agree: relax the no-re-dial rule for the epochless form, or record the window as an accepted residue. Keep the finding on the CONNECTION, never on the pod disposition. See `spec.7.review-mechanism.8`.
- **Does the successor that adopts a surviving entry inherit its still-armed §4.9 timers?** — UNVERIFIED: no cleanup and no `deregisterSlotLocked` runs on adoption. Same session and same tenant, so not an isolation issue, but a stale timer firing `AUTH_EXPIRED` at the successor is a correctness question for the code lane. See `spec.5.review-security.1`.
- **Does the hold's predicate want narrowing to the bind sequence?** — OPEN: §5.2 and §15.4 both refuse "any request that would create or resolve a registry entry" with a normative `ABORTED`, which is wider than the seven bind-sequence RPCs the design means. Declined four times on the vacuous-resolve reading. If a round wants it, the correction is to narrow both predicates to the enumerated seven, and the argument has to start from §4.7.2's Checkpoint/Interrupt operation lock, which QUEUES rather than refuses. See `spec.8.review-edit-sites.1`.
- **Does §28.3's `LNK-POD-GRPC` multiplicity reach unregistered gateway→adapter RPCs?** — UNVERIFIED: that row says "One connection per gateway replica per pod" while §4.7.1 states a bind attempt may span two connections and production dials a fresh client per connect. §29.2 and §29.4 place the bind and teardown RPCs outside the §28 register, which is why three rounds declined it. A channel-naming lens should settle it. See `spec.6.review-edit-sites.1` and `spec.8.review-fresh.1`.
- **spec/04:672's `StartSession` row states no refusal** — OPEN, pre-existing: `claimSessionSlotUnderLock` refuses a repeat start onto a started session with `codes.Unavailable`, and this proposal depends on that refusal twice. Worth its own finding in a later round; do not repair it inside 0081. See `spec.6.fix-design-G1.1`.
- **Retire the Kubernetes lens for the remaining spec rounds?** — OPEN: it has returned nothing on the spec staging in rounds 3, 5, 6, 7 and 8 against text that changed each round only on the gRPC surface. If a future amendment touches no CRD, status write, finalizer, webhook or reconcile loop, retiring it costs nothing and saves a round's latency. See `spec.8.review-kubernetes.1`.
- **Are the two shared-entry edge-case bullets one ordering stated twice?** — UNVERIFIED: `spec.6.fix-design-G3.1` deliberately did not restructure the section, and the two bullets describe near-identical orderings. If a later round merges them, apply the closing-cost sentence ONCE.
- **Does "termination window" name a quantity the specification defines?** — OPEN: §5.2's hold paragraph bounds a hold taken outside any request by "the termination window of the pass that runs the cleanup", and `grep -rn "termination window" spec/` returns zero hits, on a fail-closed gate. §15.4 points at §5.2 for the whole hold and inherits the undefined bound into the third-party contract. Declined as descriptive. See `spec.6.review-kubernetes.1`.
- **Is the hold refusal "accounted as an ordinary transient slot failure" on the resume path?** — UNVERIFIED: true for the create-time-reserved retry through `materializeSlot` → `recordSlotFailure`, and false for the client-driven §7.3 resume, which emits no `lenny_slot_failure_total` at all and reaches accounting only through CODE-5's new caller. The edge case names that resume as one of three things that can meet the hold. Turns on whether the sentence means the §5.2 classification or the metric. See `spec.6.review-operational.1`.
- **Who owns the per-slot sub-state, and when does `slot_cleanup` begin?** — UNVERIFIED: the r3 §6.2 rewrite rests entirely on "§6.2's `slot_cleanup` sub-state begins when the gateway observes the failure", and nothing in §6.2 states a tracker or a start instant, while spec/05:544 attributes the sibling `failed` marking to the ADAPTER. Not filed as a wrong-component attribution because §6.2 itself is silent and `pkg/sandbox/slotstate` is gateway-side. Settle it before the next edit rests on it. See `spec.3.review-citations.1`.
- **Is the §7.1 "spends one of the attempts" clause finally settled?** — UNVERIFIED and re-derived in six rounds: round 7 reworded it to "an attempt that meets the hold is one the retry policy did not place", and one lens still reads the pair as reachable only for a conforming adapter that answers `reclaimed` early while its cleanup runs. Read the CURRENT text before filing; four lenses have declined it as consequence-then-mitigation.
- **Should the hold's predicate be narrowed, and is `Shutdown`'s carve-out an exception?** — OPEN, two halves of one question: §5.2 and §15.4 refuse "any request that would create or resolve a registry entry" while §15.4 adds "`Shutdown` is not held". Both are reconcilable on the vacuous-resolve reading (during the hold no entry exists), and the correction, if a round wants one, is one clause in §5.2 rather than in §15.4. See `spec.6.review-edit-sites.1`.
- **Are the two `WIRED` claim-register rows owed at all?** — OPEN: §28.4 obliges a row only for a normative §28 statement, and the proposal itself argues `Shutdown` carries no §28 row. Dropping the rows is a legal alternative to editing the generator, and it is cheaper. A human or a later round should pick. See `non-spec.1.review-edit-sites.1`.
- **Is the tier-9 credential-fence suite correctly landed at S10 alone?** — OPEN: `tests/tier9_security/slot_credential_reclaim_fence_test.go` is attributed to "CODE-1 and CODE-6" and S9/CODE-6 carries no tier 9. Judged fine because CODE-1 is the later half and S10 follows S9; the same reasoning did NOT rescue the tier-3 suite, which was filed. See `non-spec.1.review-applicability.1`.
- **How far does the reclaim hold actually extend?** — UNVERIFIED: `s.reportSessionScrub(ctx, sessionID, closeErr)` sits above `defer release()` in CODE-1's body, so the hold spans an outbound gateway RPC that no staged sentence names among its bounds. Judged harmless because the hold is keyed on the ending session's own identifier. Whoever revisits the hold's bound should know its real extent includes the scrub report. See `non-spec.1.review-reliability.1`.
- **Does the credential-delivery hold refusal want a tier-9 arm of its own?** — UNVERIFIED: `assignCredentialsSlot` mid-cleanup is pinned at tier 1 (the five-resolve-site table) and at tier 7a, and the staged tier-9 file carries only the two epoch arms, while `.claude/rules/test-coverage.md` routes credential delivery to tier 9. Judged nice-to-have. See `non-spec.1.review-security.1`.
- **Does the adapter's `/healthz` `leaked_slots` count increment when the report is withheld?** — UNVERIFIED and still unanswered after two filings: spec/06:160 makes that count an ADAPTER health-metadata export and names it as one of two sources for the `ceil(maxConcurrentSessions/2)` drain trigger. If it does not increment, one of the two sources never sees these leaks. The staged text names the gateway-side path, so it is not this proposal's defect. See `spec.6.review-security.1` and `non-spec.1.review-security.1`.
- **summary.md's "Seven residues survive" count** — OPEN: the bullet then enumerates four plus two. Same class as the already-refuted "Four statements are needed" count, and `doc-style.md` would drop the count anyway. Whoever next edits that bullet should name the seventh or drop the count. See `non-spec.1.review-fresh.1`.
- **Does a mass §7.3 recovery storm cascade through the new `resumeOnPod` accounting caller?** — UNVERIFIED and unmodelled: each failed resume drains its replacement pod at `maxConcurrentSessions: 2`, and the replacements are themselves resumed onto. This sharpens the standing correlated-re-attach-churn item with the specific loop. A capacity reviewer with Tier 3 pod-churn numbers should. See `non-spec.1.review-performance.1`.
- **Do the two SCHEMA-1 claim rows need `note` fields?** — UNVERIFIED: every row in the generator's `EXPLICIT` list carries one and the validator checks only status, surface and anchor, so it is convention rather than a gate. Whoever moves the rows into the generator should match the siblings. See `non-spec.1.review-feasibility.1`.
- **Does DOCS-2 owe a positive tier-11 gate on its new row's content?** — OPEN: the existing gate is substring-presence only and would pass a stale row again the next time §4.7 moves, where DOCS-1 stages its own assertion. See `non-spec.1.review-operational.1`.
- **CODE-4's session-keyed credential release versus a concurrent successor attempt** — OPEN, FILED: the wrapper's `b.releaseCredentials(req.SessionID)` releases every lease under the session key after a compensation that can block for the whole budget, and a second `/start` attempt for the same session is past `assignSlotCredentials` inside that window. The remedy is to release the lease ids this attempt minted rather than the session's. See `non-spec.1.review-reliability.1` and the Trap that records its two look-alike refutations.
- **What §15.1 error a client sees when its own `/start` retry on a create-time-reserved slot meets the hold** — UNVERIFIED: the proposal lists §15.1's error catalog as deliberately untouched on the ground that the refusal mints no client-visible code, and nothing in either lane names the catalog row the gateway answers with or shows it is transient on that path. See `spec-recheck.1.review-client-surface.1`.
- **Does the widened leak criterion mass-retire pods on a transport blip?** — UNVERIFIED: an incomplete adapter reclaim now produces a PERSISTENT leak where the shipped path produced a windowed failure that decayed in five minutes, so a fleet-wide gateway-to-adapter blip retires one pod per event at `maxConcurrentSessions: 2`. Judged not a finding (the residue is genuinely unreclaimable and §6.2 prescribes the persistent disposition), and nobody has measured the replacement rate it implies. See `spec-recheck.1.review-performance.1`.

- **Which step routes `Shutdown` through `reclaimSlotLocked`?** — OPEN, FILED: the checklist S9 line and CODE-6's body say CODE-6 does it at S9; CODE-1/S10 and two Testing bullets say CODE-1 does it at S10. Whoever fixes it must pick one owner and make the S9 parenthetical read as a rule statement or drop `Shutdown` from it. See `non-spec-recheck.4.review-edit-sites.1`.
- **Does the hold releaser `reclaimSlotLocked` returns take `s.mu` itself?** — OPEN: CODE-1 registers `defer release()` while holding `s.mu` and runs it after the unlock, while `terminateHeldSession` defers it with the lock never held, so it must take the lock, and `reclaimSlotLocked`'s "callers hold `s.mu`" must not be read as covering `release`. Nothing staged says so. Whoever lands CODE-6 should state it in the doc comment. See `non-spec-recheck.1.review-reliability.1`.
- **Can the tier-4 interceptor be added without perturbing the six other `recycleAdapterDialer` call sites?** — UNVERIFIED: the proposal implies a per-case variant dialer and does not say so, and an implementor who edits the shared helper in place breaks the other six cases. See `non-spec-recheck.5.review-applicability.1`.
- **Three docs sentences against SPEC-3's withheld report** — UNVERIFIED and disputed, with no correction between the readings: one operational lens FILED `docs/reference/adapter-contract.md:81`, `docs/reference/execution-modes.md:68` and `docs/operator-guide/security-principles.md:33` as stating universally what SPEC-3 withholds, on the ground that :81 sits on the very page DOCS-2 opens and would contradict DOCS-2's own row; five other lenses declined the same sites because SPEC-3 places the pre-`running` cleanup OUTSIDE "session release" and the doc mirrors an unedited spec sentence. The newer FILED reading is kept here; the refutation is recorded in Traps. Somebody should settle whether DOCS-2 grows the exception clause or a DOCS-3 exists. See `non-spec-recheck.4.review-operational.1`.
- **Does DOCS-2's `DemoteSDK` row want a tier-11 pin of its own?** — UNVERIFIED: the staged extension pins the `Shutdown` row and the two page-wide strings and asserts nothing about the `DemoteSDK` row, so that half of DOCS-2 can be reverted silently. No gate reads that row today. See `non-spec.2.review-edit-sites.1`.
- **Does the tier-11 extension owe an annotation widening?** — UNVERIFIED: it adds assertions over §4.7.1, §5.2 and §15.4 content to a test whose `// spec:` annotation is `4.1, 4.7`, and the staged edit list does not name the annotation. No gate reads a tier-11 annotation, because `componentAndAboveTierDirs()` excludes `tests/tier11_docs`, so nothing turns red. See `non-spec.2.review-operational.1`.
- **Does the §10.1.4 self-termination path owe a scrub-report carve-out?** — UNVERIFIED: it deregisters STARTED sessions and deliberately files no `ReportSessionScrub`, which staged §5.2's complementary arm ("a cleanup that reclaims a slot the pod's shared runtime process was given is a session release like any other and reports its outcome") says it must. Not filed, because §5.2's SHIPPED scrub-model sentence already says the same and spec/10 carries no carve-out, so applying SPEC-3 does not change the truth value. Start from holdstate.go:214-218 rather than re-deriving. See `spec-recheck.2.review-reliability.1`.
- **The queue FIFO amplifies the synchronous compensating `Shutdown`** — OPEN, FILED: the compensation is uncancellable by construction, sits inside `materializeSlot`, and `materializeSlot` is reached from the queued attempt, so at the spec's documented defaults two attempts of 15s each equal the default `maxQueueWaitSeconds` for every waiter behind the head. See `non-spec.7.review-performance.1`.
- **Does the `DemoteSDK` release owe a cleanup-outcome report?** — OPEN: `sdkwarm.go:298` reclaims a slot the pod's shared runtime process was given (`ConfigureWorkspace` reaches `noteRuntimeStarted` on its fresh arm) and reports nothing, against SPEC-3's complement sentence. Either that sentence is over-broad for a tentatively-configured preConnect session or CODE-6 owes a report there. Deliberately out of scope for the r5 group. See `non-spec.5.fix-design-G1.1`.
- **How many production callers does `releaseSessionSlot` have?** — UNVERIFIED, and the ENUMERATION is agreed even though the count is not: session.go:133,:147,:157; resume.go:69,:73,:89,:107,:126,:134,:141; sdkwarm.go:236,:241,:251 (the start-path rollbacks) plus sdkwarm.go:298 (`DemoteSDK`, correctly excluded from the rollback list). Shards this window variously report fourteen, fifteen and sixteen over that same list. Cite the enumeration, never a number. See `non-spec.5.fix-design-G1.1`, `spec-recheck-2.1.review-citations.1` and `spec-recheck-2.1.review-reliability.1`.
- **Does gRPC keepalive shorten an unresponsive `ShutdownReclaim`?** — UNVERIFIED: if a keepalive timeout well below the budget exists in `pkg/gateway/runtime/adapterclient`, the queue-amplification finding's worst case shrinks; the loaded-adapter case, where the pod answers slowly rather than not at all, stands either way. See `non-spec.7.review-performance.1`.
- **Does `Server.reclaiming` need a lazy-init guard?** — UNVERIFIED: adapter tests build `Server` by struct literal and a write to a nil map panics; CODE-6 specifies the lazy seed for the epoch counter and says nothing about the map. Judged over-specification to state, so it is on the implementor. See `non-spec.7.review-feasibility.1`.
- **Can the tier-4 concurrent-workspace case assert its late-reclaim arm's `leaked=false`?** — UNVERIFIED: that file imports no gateway package and stands up no `podsession.Binder` or `SlotClaimer`, so the disposition it names has no observer there. The outcome mapping is pinned at tier 1, so an implementor can drop the clause or import the binder. See `non-spec.5.review-test-coverage.1`.
- **The tier-1 gateway cases name no file** — UNVERIFIED: if the "Accounting", "One `maxConcurrentSessions: 2`", "reserved bind path" and "resume path" cases land in `pkg/gateway/sessionserver/slotretry_test.go`, each needs its own per-case spec-map entry under 5.2/6.2/7.1, and S13 enumerates only the four `TestSlotRetry*` names. Whoever revisits should name the file rather than re-file the credit gap. See `non-spec.7.review-applicability.1`.
- **Does `terminal_reclaim_internal_test.go:157-168` contradict staged §6.2/§7.2?** — UNVERIFIED: `TestTerminalReclaimPreRunningSkipsResumingSession` asserts a `resuming` session is excluded from the BY-NAME CLAIM reclaim, which is `ReclaimClaimed` rather than the §7.1 adapter-side reclaim. A later lens re-deriving a test-disposition finding here should start from that distinction. See `non-spec.7.review-test-coverage.1`.
- **`start_test.go` is listed as a tier-1 home for CODE-4/CODE-5 but carries no assigned case** — UNVERIFIED: nothing breaks, since the file matches no derived inventory rule today, but nobody has established which staged case lands there. Name the case or drop the file from the list. See `non-spec-recheck-2.2.review-mechanism.1`.
- **Can §5.2's whole-pod scrub ever PASS step 6?** — UNVERIFIED and pre-existing: step 0 removes only `/run/lenny/slots/{sessionId}/credentials.json` and leaves the directory itself, which would keep `/run/lenny/slots/` non-empty for step 6's stat-check. Predates 0081 and is outside its staging; a future §5.2 reviewer should look. See `spec-recheck-2.1.review-feasibility.1`.
- **Does the summary's "Its production call sites are …" over-claim by omitting `sdkwarm.go:298`?** — UNVERIFIED: an earlier round deliberately removed the `DemoteSDK` release from that list because it is not a rollback, which leaves a universal phrasing over an incomplete set. Judged below the bar and unadjudicated. See `non-spec-recheck-3.2.review-citations.1`.
- **`summary.md:161-171`'s file-collision paragraph names nine adapter files where the authoritative lists name eleven** — OPEN, and this is the third window in which a summary enumeration has lagged the deliverables. `resume.go` and `sdkwarm.go` are missing and are genuinely opened. Declined repeatedly as narrative bookkeeping; a fixer touching that paragraph should bring it to eleven rather than adding a twelfth partial list. See `non-spec-recheck-2.1.review-edit-sites.1` and `non-spec-recheck-2.1.review-feasibility.1`.
- **DOCS-2 misattributes the audience and self-declared scope of `docs/reference/adapter-contract.md`** — OPEN, FILED on the seventh derivation after six declines: the rationale calls the page the third-party adapter author's and paraphrases its line 10 as the gateway-to-adapter reference, where the page is written for the runtime author. Rationale prose only, so nothing reaches `docs/`; one clause fixes it. See `non-spec-recheck-3.3.review-citations.1` and the Trap that records what survives of the older decline.

- **Does a bind attempt span `/finalize` to `/start`?** — OPEN: §4.7.1 calls the start "a later stage of the same binding" while `Binder.Launch` is a separate HTTP request holding no token. One reading needs the token persisted across two requests (nothing stages that); the other makes `Launch` a tokenless attempt, contradicting "the gateway mints a token for each attempt". A human must pick. See `spec.1.review-mechanism.1` and `spec.1.review-reliability.1`.
- **Does the headline claim over-state what the token closes?** — OPEN: the proposal says the token "closes the shared-entry residue", and the tokenless start is outside the fence. The owner should decide whether that is acceptable here or whether remediation position 2 must land first. See `spec.1.fix-design-G1.1`.
- **Open decision 20: do the two new `ErrorCode` values take 28 and 29 or the Phase-2 range?** — OPEN and for a human; it lost one of its stated reasons when the §15.1 rows were deleted, and its text was re-pointed to whoever owns the adapter's `ErrorCode` enum. See `spec.1.fix-G2.1`.
- **Are the two `WIRED` claim-register rows owed at all, and may an intermediate commit carry one whose reader is two steps away?** — OPEN: §28.4 obliges a row only for a normative §28 statement, and the proposal argues `Shutdown` carries no §28 row. Dropping them is a legal and cheaper alternative to editing the generator. See `non-spec.1.review-edit-sites.1` and `non-spec.5.review-applicability.1`.
- **Does CONF-1 owe more properties, and does the reclaim hold owe one?** — OPEN: CONF-1's clause set is lettered (a)-(j) and the tier-3 case list references the letters, so any relabelling is a four-site cascade; the hold has no property and neither does the `PrepareWorkspace` single-resolution clause. See `non-spec.1.review-test-coverage.1` and `spec.2.fix-G1.1`.
- **Does the `superseded` outcome have any producer besides the untokened residue and a compensation that lost a race?** — OPEN: nobody has enumerated the reachable orderings, and the corrected §16.1 row deliberately makes no claim that depends on the answer. See `spec.2.fix-design-G2.1`.
- **Is the only scrapeable signal for the late-start untokened residue the wrong one?** — OPEN: the discriminating series is adapter-side and unscraped, so an operator sees only the gateway counter whose §16.1 row calls the outcome benign. A question for the owner. See `spec.2.review-operational.1`.
- **Does `lenny_adapter_leaked_slots` now need a §16.1 row?** — OPEN: the staged §5.2 append is a SECOND spec site telling an operator to watch a gauge that has no §16.1 row, no `catalog.go` entry and no `docs/reference/metrics.md` row. The gap is pre-existing; a human may want it swept. See `spec.6.review-operational.1`.
- **Is the §5.2 whole-pod replacement trigger diluted across gateway replicas?** — OPEN: spec states it as a gateway-side count while `slothealth.Tracker` is replica-local and in-process, so at ~20 replicas no replica need reach `ceil(maxConcurrentSessions/2)`. Pre-existing; this proposal raises the RATE of leaked slots and extends the disposition to three bind paths. See `spec.2.review-performance.1` and `spec.6.review-performance.1`.
- **Does `recordSetupCommandFailed` emit an audit row or metric that §16.1 or §16.5 describes as setup-command-specific?** — UNVERIFIED: if it does, SPEC-6 may owe a sentence. spec/16 carries only the unrelated warm-pool warmup label. See `spec.6.review-fresh.1`.
- **Is the reclaim hold bounded on a `maxConcurrentSessions: 1` pod?** — UNVERIFIED: the bounding sentence leans on the `**Slot cleanup:**` bullet's timeout, which is scoped to `maxConcurrentSessions > 1`. One lens declined it on the ground that the held identifier is the session identifier and never recurs. See `spec.2.review-reliability.1` and `spec.6.review-reliability.1`.
- **Is `ensureSlotStateLocked`'s doc-comment claim that "the predicate here is the whole of the admission rule" still true?** — OPEN: once the handler-entry validator is named it is false and must be reworded, and a later round should check that nothing else in CODE-6 rests on the one-chokepoint claim. See `spec.3.fix-design-G1.1`.
- **Does §6.2's prose ("marked `failed` and released back to the pool") contradict the retirement reading §7.1 now leans on at a second site?** — UNVERIFIED and pre-existing: the §6.2 fence (`failed ──→ draining ──→ terminated`) agrees with the proposal. A lens owning §6.2/§7.1 prose consistency should decide whether the shipped clause needs a corrective edit. See `spec.1.fix-design-G3.1` and `spec.6.review-kubernetes.1`.
- **Are "registry entry" and "bound entry" defined anywhere?** — OPEN: both are load-bearing nouns of the staged contract and neither is defined in `spec/`; "registry entry" appears only at spec/16:188 and spec/28:861, and "bound entry" only in the sentence SPEC-1 deletes. A human may want one defining clause in §4.7.1. See `spec.5.review-fresh.1`.
- **Does the `bind_attempt`/`mid_session` well-formedness rule want an explicit evaluation position against the create rule?** — UNVERIFIED after the fix: it is now stated as a precondition that gates entry into the cascade, and one lens still reads the §4.7.1 commentary outside the staged block as calling the hold part of the cascade and omitting the gate. See `spec.3.review-security.1` and `spec.3.fix-G1.1`.
- **Is CONF-1 clause (j)'s tier-3 case buildable?** — UNVERIFIED: it needs a fake runtime that blocks inside `Start` until a second call has sent an unconditional `Shutdown`, and nobody has checked that the `adapter_generation_fence` bufconn fixture can be made to block or that a second concurrent call reaches `Shutdown` while `StartSession` holds no lock. See `spec.6.fix-G2.1`.
- **Is the `budget/2` graceful-window ratio a non-spec default needing an override?** — UNVERIFIED: `.claude/rules/code-best-practices.md` bars a default not fixed by the spec and not overridable by a flag or config value. Not an edit-site question; a rules-conformance lens should decide. See `non-spec-recheck-3.4.review-edit-sites.1`.
- **Does the `bind_attempt` well-formedness rule strand any existing sender?** — SETTLED NO for the five carriers (every sender is inside a minted-token path, and `credassign/client.go:194` is the token service's own `AssignCredentials` and must not be counted), but nobody has checked `cmd/lenny-compliance` or a tier-10 battery for a zero-frame `PrepareWorkspace`. See `spec.6.review-fresh.1`.

- **Two accepted residues live only in the proposal's own reasoning** — OPEN, FILED as two findings:
the staged §4.7.1 block records the untokened-entry residue in landing text and gives no parallel
sentence for the self-recreated entry after an unconditional teardown (spec-changes.md:280-289,
summary.md:411-416) or for the entry stranded by a gateway crash between abandoning the attempt and
sending the reclaim (:290-300, summary.md:424-427). The remedy is a sentence each beside the
untokened-residue sentence, never a request to build the position-2 mechanism. See
`spec.7.review-docs-alignment.1`.
- **Neither newly recorded residue has any observability** — OPEN: the untokened case gets
`lenny_slot_shutdown_untokened_entry_total`, while an entry stamped with a dead attempt's token
produces only repeated `SLOT_BIND_ATTEMPT_SUPERSEDED` refusals, which the gateway accounts as
ordinary transient slot failures, so nothing distinguishes a permanently stranded slot from a
retried one. Whether that needs a §16.1 row is for a later round or a human. See
`spec.7.review-docs-alignment.1`.
- **Does the one-session-pod residue deserve its own accepted-failure bullet?** — OPEN: the G1 fix
names a registry entry and per-slot tree surviving on a pod serving one session until the
pre-attached disposition retires it. That design says no, because the disposition is benign and the
added §5.2 sentence states it, beside the "§6.2 has no terminal for a bind abandoned at the connect
stage" bullet. See `spec.7.fix-design-G1.1`.
- **No listed summary section is named for accepted failure modes** — OPEN: the block now sits at the
end of `## Non-goals`, under a heading that does not announce it. Whoever owns the summary's
structure should decide whether the section list wants a part or a section of its own for residues
the design accepts, rather than leaving each format pass to choose a host. See `f1.cleanup`.
- **The deleted `## Revision history`** — OPEN: it recorded that on 2026-09-15 the proposal was
revised against the fifth determination in `scratchpad/attempt-fence-determination.md`, replacing
the adapter-minted bind epoch with the caller-minted per-attempt token after review found the epoch
self-defeating under its own latch rule; that the revision moved the comparison into the adapter's
resolve chokepoint and folded in the started-entry gate, the adapter-client typed-error translation
and the reclaim-closure short-circuit; that the remaining findings were pushed out as their own
`BUILD-GAPS.md` entries; and that the status was reset to Draft. This changelog and `status.md`
carry it. Whoever owns the summary should decide whether any of it belongs in a listed section. See
`f1.cleanup`.
- **Open decision 20 carries no recommendation, alternatives or confidence** — OPEN: it is the only
entry left in the section and the next adjudication firing owes all three. See `f1.cleanup`.
- **Is the token refusal observable end to end through `slotResolveError`?** — UNVERIFIED: the five
`codes.InvalidArgument` resolve wraps must preserve `ABORTED` to the gateway, and nobody has traced
it. Code-lane item; re-put in token vocabulary from the retired epoch form. See
`spec.6.review-fresh.1`.
- **Does §7.4's mid-session upload want a rule of its own?** — UNVERIFIED: the pair runs on the live
binding's connection with `mid_session` set, so the admission rules admit it, but the CONF-1 battery
still owes a case driving a `FinalizeWorkspace(mid_session=true)` onto a started session. See
`spec.5.fix-G1.1`.
- **A retry whose predecessor's start the adapter ADMITTED is refused until the reclaim clears** —
OPEN: if that reclaim is never answered, the identifier refuses every further attempt at that
session on that pod until the pod terminates. Recorded in the accepted failure modes; a human may
want a distinct client-visible outcome there. See `spec.1.fix-G1.1`.
- **SPEC-3's exclusive-pod arm over-reaches its antecedent** — OPEN: the clause's subject is a bind
"abandoned or fails" while §7.1's paragraph binds only "a gateway bind attempt that fails". A
READY_TIMEOUT or a revoke retires the pod anyway under spec/05:455; the surviving class is a client
terminate at `ready` on a recycling exclusive pool, where the session ends `completed` and the pod is
reused. See `spec-recheck.3.review-edit-sites.1` and `spec-recheck.3.review-applicability.1`.
- **Naming any new test file with `slot` in its basename pulls it into `slotAddressCaseFiles`** —
UNVERIFIED, and it survives whatever the epoch-named tier-3 directory is renamed to: such a file
then owes a `tests/spec-map.json` credit under every section its own `// spec:` annotation names,
and the staged directory entry covers `validate-maps` only. An implementor running tier 0 at that
step will be told. See `non-spec-recheck-3.1.review-applicability.1`.
- **Does "takes the session back off the shared runtime process" hold for every runtime implementation?** — UNVERIFIED: `InProcessRuntime.Close` and `MCPRuntime.Close` ignore the session identifier and tear the whole runtime down on any call, while the socket implementation's early return makes it harmless while a co-tenant is active. If a concurrent pod can ever run an `InProcessRuntime`, the undo tears down co-tenants. The remedy is in code, so the non-spec lane owns it. See `spec.8.review-feasibility.1`.
- **Does spec/07:208 need SPEC-5's treatment?** — OPEN: it is the third restatement of the setup-window-to-`SETUP_COMMAND_FAILED` mapping, after §15.1:1136 and §6.2:290, both edited. Declined because its setup-command clause states a sufficient condition rather than a definition and its fallback is narrowed to a transient transport failure. A later round that widens the refusal's reach should re-check it. See `spec.8.review-edit-sites.1`.
- **Neither newly recorded residue has any observability** — OPEN: the stranded-token case produces only repeated `SLOT_BIND_ATTEMPT_SUPERSEDED` refusals that the gateway accounts as ordinary transient slot failures, so nothing distinguishes a permanently stranded slot from a retried one. Whether it wants a §16.1 row is for a human. Carried unchanged from `spec.7.review-docs-alignment.1`. See `spec.8.review-docs-alignment.2`.
- **Does a compensating `Shutdown` answered `superseded` still run the whole-pod scrub?** — UNVERIFIED: staged §4.1 keeps "runs the whole-pod scrub when the recycle disposition is set" unconditioned on the token while staged §4.7 conditions only the two teardowns. Unreachable in this tree, because the recycle disposition is set only on the occupancy-zero release path and every such caller sets `unconditional_teardown`; nobody has checked whether a third-party gateway could construct one. See `spec.8.review-mechanism.1`.
- **Can the adapter populate `k8s_pod_name` on every pod?** — UNVERIFIED: the adapter caches `podID` from the Downward API at construction, and the shipped scrub reporter WITHHOLDS its report when that cache is empty rather than fabricating an identity. A counter labeled by an empty pod name is a live series with a blank label rather than a withheld one, so the code lane should decide whether the counter drops the label (matching every other adapter metric in §16.1) or repeats the withhold. See `spec.8.review-operational.1`.
- **Does a session whose create-time-reserved pod is drained by the accounting rebind onto another pod or end `failed`?** — UNVERIFIED: it decides whether the crash-stranding bullet's "until the pod is replaced" is a recovery or only an end. Same question as the standing "Does a §15.1 retry reach a pod already stamped for drain?". See `spec.8.review-performance.1`.
- **Two round-8 findings recorded in commit 096b39656 were never landed** — OPEN: the commit message records "the superseded outcome defined three ways" and "§4.7.9 step 5 contradicting the one-session carve-out", neither of which the fix pass could land before the rate limit. Both were re-filed by other round-8 lenses; a reader should confirm they stayed filed rather than assume it. See `spec.8.review-kubernetes.1`.
- **Does §15.4's "the status code and error category ... are each stated once, by §4.7.1" hold for every named rule?** — UNVERIFIED: four rules name a status and no category, judged vacuous because their answers carry no `adapterv1.Error`, and open decision 24 resolved it as "neither", but no published-contract-completeness lens has signed it off. See `spec.9.review-fresh.1` and the `f1.post-spec-loop` firing for decision 24.
- **Does §15.1's exclusion sentence want the narrowing §6.2's clause took?** — OPEN, filed at round 14 and adjudicated twice since as an imprecise gloss over a correct disposition. See `spec.14.review-applicability.1` and `spec.14.review-citations.1`.
- **Do §15.1's endpoint-precondition rows (spec/15:625,:646,:647,:650) want the widened cause too?** — UNVERIFIED: read as stating a sufficient condition per cause rather than the row's full cause set. See `spec.14.review-citations.1`.
- **Which client envelope does a started-session refusal at `AssignCredentials` or `Resume` reach on the single-session path?** — UNVERIFIED: only the workspace-prep transient path and the concurrent `policy_rejection` path were traced, and no staged text asserts a destination. See `spec.12.fix-G2.1` and `spec.12.fix-design-G2.1`.
- **Is the adapter's gateway-facing gRPC server reachable from the agent container, and does it require mTLS?** — UNVERIFIED after three rounds: if it is reachable unauthenticated, an in-pod agent could send an unconditional `Shutdown` for a co-tenant session. Pre-existing, so out of scope here, but it wants its own finding against §13.1/§13.2. See `spec.12.review-security.1` and `spec.14.review-security.1`.
- **Does `docs/reference/metrics.md` take a row for the untokened-entry counter as well as the superseded one?** — OPEN: the spec trailer and CODE-9's Files-touched line say both; CODE-9's body names one. See `spec.12.review-applicability.1` and `spec.14.review-docs-alignment.1`.
- **Does a gateway-side catalog sweep hold `lenny_slot_compensation_superseded_total` to both §16.1 and `docs/reference/metrics.md`?** — UNVERIFIED, nobody looked. See `spec-recheck.5.review-reliability.1`.
- **Does a recycle-carrying `Shutdown` that fails the pairing rule still run the whole-pod scrub?** — OPEN: staged §4.1 says the handler runs it whenever the recycle disposition is set, while the teardown-pairing rule says such a request "changes nothing". Unreachable from a conforming gateway and fail-closed either way. See `spec-recheck.5.review-security.1`.
- **Does §6.4's per-session tree enumeration want `/run/lenny/slots/{sessionId}/`?** — OPEN: spec/06:386 omits it while SPEC-3 names it in §5.2's action list and `slotlayout.RemoveTree` already removes it. Judged pre-existing and not falsified by SPEC-3. See `spec.12.review-citations.1`.
- **Does the summary's accepted-residue list carry a residue the spec staging's edge-case section does not?** — OPEN: the MCP-arming residue appears in summary.md alone. Three close variants were refuted as documentation completeness rather than correctness, so a filing must argue the edge-case-section-completeness rule. See `spec-recheck.5.review-docs-alignment.1`.
- **Should the Kubernetes, reliability, performance, operational, feasibility and citation lenses be retired for the remaining spec rounds?** — OPEN: each returned empty across three to five consecutive dispatches over unchanged owned text, and the reopening event for each is a fix round that edits its own block. See `spec-recheck.5.review-performance.1` and its sibling lens shards.
- **Does `docs/reference/state-machines.md:239`'s `slot_cleanup | released` row want SPEC-3's widened action list?** — OPEN: it glosses three actions where SPEC-3 widens to five, and DOCS-1 stages only the new edge row. See `spec-recheck.5.review-docs-alignment.1`.

- **Should `slotFailureWorkspaceFinalize` exist at all?** — OPEN: nothing outside CODE-9's closing paragraph, its files-touched entry and checklist S10 names it, and no spec or docs row enumerates `error_type` values, so deleting it would owe no test. Kept because CODE-9 states an operator rationale three rounds examined. See `non-spec.11.fix-G2.1` and `non-spec.11.fix-design-G2.1`.
- **Does CONF-1 owe a recycle-carrying row?** — OPEN: the unconditional-scrub rule is part of the published contract and is the thing our own staging got wrong, but pinning it needs a `ReportPodScrub` observer in the battery harness, which travels the GatewayControl link rather than the RPC response. See `non-spec.11.fix-G3.1` and `non-spec.11.fix-design-G3.1`.
- **Does the whole-pod scrub racing the per-slot cleanup on the removing arm want an ordering statement?** — OPEN: `answerShutdown` fires the asynchronous `startPodScrub` while the slot guard and the reclaim hold are still held and the per-slot tree removal is in flight, and no staged §5.2 or §4.7.1 text orders them. See `non-spec.12.fix-G3.1` and `non-spec.12.fix-design-G3.1`.
- **Is ten seconds the right guard-acquisition deadline for the §10.1.4 pass?** — OPEN: the re-scope keeps the shipped value with no separate justification, and the ordering is best-effort with a shipped fall-through, so a shorter dedicated budget may be better. See `non-spec.11.fix-design-G1.1`.
- **Does any tier-7a or tier-8 case assert a wall-clock bound on the WHOLE §10.1.4 pass?** — UNVERIFIED: the per-member close context moves the worst case from 10s to N×10s, N bounded by `maxConcurrentSessions`. Grep `tests/` for hold-timeout duration assertions before running those tiers. See `non-spec.11.fix-design-G1.1`.
- **Do `pkg/adapter/holdstate_test.go`'s fixtures expose a fake `Runtime` that can capture the context handed to `Close`?** — UNVERIFIED: the staged per-member close-budget case assumes one can be written or extended there. See `non-spec.11.fix-G1.1`.
- **Is either guard-acquisition EXPIRY branch driven by any listed test?** — UNVERIFIED: `slot_guard_not_acquired` appears only in staging prose and one staged call site, never in `## Testing`, so neither the destructive side's unguarded removal nor the admission side's `ctx.Err()` refusal is pinned at any tier. See `non-spec.11.review-mechanism.1` and `non-spec.11.review-test-coverage.1`.
- **Should the `SlotReclaim` hook be two-argument?** — UNVERIFIED: CODE-9 declares `SlotReclaim func(outcome, pool, podName string)` while SPEC-6's row gives the series only `pool` and `k8s_pod_name`, and the forwarder already filters to `superseded` alone, so the `outcome` argument is a constant the accessor discards. See `non-spec.11.review-applicability.1` and `non-spec.12.review-mechanism.1`.
- **Should CODE-4's `Targets:` list carry `upload_to_session.go` and `start.go`?** — UNVERIFIED: both are named in `## Files touched` and in CODE-4's prose and in no Targets list, and the `upload_to_session.go` edit is load-bearing, because once `validateBindFields` lands a §7.4 `PrepareWorkspace` carrying an empty token and `mid_session` false is `INVALID_ARGUMENT` and the whole mid-session upload route refuses. See `non-spec.12.review-applicability.1`.
- **What replaces `deregisterSlot` at its two test callers?** — UNVERIFIED: `pkg/adapter/podmcp_arming_internal_test.go:88,:245` call the function CODE-6 retires, and routing them through `reclaimSlotLocked` would open a reclaim hold nothing releases and make later binds in those tests refuse; the safe replacement is an inline `s.mu.Lock(); s.deregisterSlotLocked(...)`. The file is already in the test list, so this is mechanical and unstated. See `non-spec.12.review-applicability.1` and `non-spec.12.review-mechanism.1`.
- **Does the setup-envelope split want a tier-3 case?** — OPEN: `tests/tier3_contract/rest_sessions/slot_address_absence_test.go:445` exists in the tree and appears in neither the Testing section nor the Files-touched test list; round 12 deliberately left it out because no status code, header or body key is new. See `non-spec.12.fix-G1.1` and `non-spec.12.fix-design-G1.1`.
- **Does the §11.4 revoke fan-out have any test at all?** — UNVERIFIED: it routes through `Client.Shutdown` so the shared builder sets the field and the caller table needs no edit, but nobody looked for a test over the fan-out. See `non-spec.12.review-test-coverage.1`.
- **Should the `tests/tier3_contract/adapter_bind_attempt/` files be named?** — OPEN: the proposal says "one file" twice and names neither; nothing turns on it because the inventory regex matches basenames and the spec-map entry is a directory entry. See `non-spec.10.review-applicability.1`.
- **Has CODE-2's `Resume` confirmation refusal any production reacher left?** — UNVERIFIED: every staged destructive path takes the same guard `Resume` now holds for its whole call, so nothing can remove or replace the entry between `claimSessionSlot` and `noteRuntimeStarted`. Whoever owns CODE-2 should decide whether the arm is deliberate defense in depth. See `non-spec.10.review-mechanism.1`.
- **Does the bind-path half of the guarded-compensation residue want writing down?** — OPEN: the proposal records the residue for `Resume` alone, while a compensating `Shutdown` on the ordinary bind path blocks on a guarded materialization or the whole `setupPolicy.timeoutSeconds`, which is the design's primary path. See `non-spec.10.review-mechanism.1` and `non-spec.10.review-performance.1`.
- **Do the deliverable HEADING file lists want re-deriving from `## Files touched`?** — OPEN: CODE-6's heading omits `credentials.go`, `server.go` and `sdkwarm.go`, all three of which its own body and the files-touched list assign to it; the identical defect against `resume.go` was confirmed and fixed in an earlier round, so the class is live. See `non-spec.11.review-applicability.1` and `non-spec.12.review-applicability.1`.
- **Is the hold's "create or resolve" predicate wider than CODE-6's two test points?** — UNVERIFIED: the staged §5.2 and §15.4 blocks say the hold reaches every request that would create or resolve an entry, while CODE-6 tests it at `acquireSlotGuardForResolve` and `ensureSlotStateLocked`, which between them cover the seven bind-sequence RPCs. Judged vacuous, because the hold opens only after the entry is deregistered so nothing else CAN resolve one. A filing must defeat that vacuity first. See `non-spec.12.review-mechanism.1`.
- **Do S14 and S16's tier digits mean "run the tier" or "carry cases for it"?** — OPEN: they declare tiers 3 and 10 while every tier-3 and tier-10 case lands in files S22 creates, and S16/S17 declare tier 2 while the only tier-2 cases belong to S12's deliverable. See `non-spec.9.review-applicability.1`.
- **Does S10 leave an unexported method with no callers?** — UNVERIFIED: CODE-9 lands `noteCompensationOutcome` at S10 while its only caller lands at S16/S19, so `golangci-lint`'s `unused` could turn tier 0 red at that commit depending on `.golangci.yml`'s `run.tests`. See `non-spec.9.review-fresh.1`.
- **Does `pkg/gateway/podlifecycle/podsession/binder_envtest_test.go` owe a `slotAddressCaseFiles` row?** — UNVERIFIED: its name matches neither derived name rule, so it turns entirely on whether the implementor's fixture calls the slot claim surface. The proposal states the conditional rule rather than the file. See `non-spec.9.review-kubernetes.1`.
- **Is the tier-4 datastore-crossing case constructible in `recycle_scrub_path_test.go`?** — UNVERIFIED: that file's driver runs the session-mode recycle path while the compensation lives on the concurrent-slot path through `materializeSlot`; it has miniredis and so can carry a slot counter, but nobody has confirmed a concurrent-pool arm. See `non-spec.9.review-kubernetes.1`.
- **Does `client_test.go` hold the §15.4 credit CODE-7's cases cite?** — UNVERIFIED: the file is whole-file registered without 15.4, and nobody checked whether `creditKey` resolves a cited id to a declared ancestor. See `non-spec.8.review-kubernetes.1`.
- **Do 0081 and 0079 both touching `tests/tier10_conformance/recycle_scrub_conformance_test.go` owe an impacts row?** — OPEN: no row makes a claim the overlap falsifies today, so nothing is owed now; a later round widening either row should count it. See `non-spec.8.fix-G1.1` and `non-spec.8.fix-design-G1.1`.
- **Does any OTHER staged CODE deliverable introduce an early return between the precondition and clause three?** — UNVERIFIED: only CODE-1's handler body was checked. The next round should re-grep the staged Go blocks for `return &adapterv1.ShutdownResponse` and confirm there is exactly one construction site. See `non-spec.11.fix-design-G3.1`.
- **Does `fakeSDKWarmRuntime`'s construction helper build a server with a workspace base the successor's `AssignCredentials` can write into?** — UNVERIFIED: the staged SDK-warm tier-1 case assumes it. See `non-spec.9.fix-design-G2.1`.
- **Do any other staged `func()` closures need an error they cannot see?** — UNVERIFIED: the G3 round checked only `binder.go`'s two. See `non-spec.8.fix-design-G3.1`.
- **Does `pkg/gateway/podlifecycle/podsession/binder_envtest_test.go` exist as a reachable envtest home?** — UNVERIFIED: the package's existing envtest cases live in `binder_test.go` and friends, and nobody checked whether the harness is reachable from a new file. See `non-spec.8.review-citations.1`.
- **S16, S21 and S22 depend on S15 without naming it** — OPEN: all three consume the per-slot guard's hand-out forms `lockSlotGuard` / `acquireSlotGuardForResolve`, which S15 creates, and all three list `Depends-on: ... S14` without S15. The stated execution order (S15 before S16) makes it harmless today and the lens's checklist rule only bars a Depends-on naming a LATER or non-existent step, so it was not filed. If a future round reorders the code block, this breaks first. See `non-spec.13.review-applicability.1`.
- **summary.md:141 attributes CODE-5's `codes.Aborted` arm to the wrong function** — OPEN: it says "the accounting helper", while the deliverable puts the arm in `isTransientPodClaimError` and `accountSlotFailure` is the accounting helper. Nothing built from the proposal changes either way, and CODE-5 and SCHEMA-1 both name the right function; the reconciliation pass can tidy it in one word. See `non-spec.13.review-mechanism.1`.
- **The two new client-envelope cases are pinned at tier 1 rather than tier 3** — OPEN: the 422 `SETUP_COMMAND_FAILED` versus retryable 503 pair lands in `pkg/gateway/sessionserver/slotretry_test.go`, where `.claude/rules/test-coverage.md` routes an HTTP response contract to tier 3. Declined because the envelopes are unchanged and the shipped precedent for exactly this assertion, `TestClassifiedSlotFailureKeepsSetupCommandEnvelope_spec_7_3`, already sits at tier 1 in that same file. A lens wanting to re-open it should start from that precedent rather than from the tier table. EVIDENCE: non-spec-changes.md:2836-2860. See `non-spec.13.review-test-coverage.1`.

### Deferred

- DEFERRED [schemas/lenny-adapter.proto]: the `Shutdown` RPC comment ("asks the adapter to terminate the agent and release the pod ... Returns when the agent process has exited") and the `ReportSessionScrub` / `SessionScrubOutcome` comments ("on every session release") are the proto-side statement of the contract §15.4 says is "kept in sync with the prose". SPEC-1's two-teardown split, its no-op clean-exit answer, and SPEC-3's withheld report all falsify parts of them. GROUND CHANGED: the amendment retracted the S-2 reading this entry rested on, because SCHEMA-1 now opens `schemas/lenny-adapter.proto` under S-2's second window (summary.md:94-95) at S8. Two lenses nonetheless declined to file the comment repair here, on the ground that the `Shutdown` comment is ALREADY false today for a session the adapter holds no entry for and for a per-slot `Shutdown` on a co-tenanted pod, which is the pre-existing-looseness class prior rounds killed. So the correction is now LANDABLE here rather than barred, and whether to land it or leave it to R1b is a live choice rather than a constraint. The R1b attribution below is the older disposition. The `Shutdown` comment is already stale for a co-tenanted pod, so this is a widening rather than a new break. NARROWED: `ShutdownResponse` carries no doc comment at all (:1665-1668 is a bare three-line message), so CODE-1's re-keying of `exited_cleanly` owes nothing here and the entry is the three comments named below and no more. EVIDENCE: schemas/lenny-adapter.proto:203-206,:308-311,:436-438; spec/15:1456.
- DEFERRED [pkg/adapter/session.go, pkg/adapter/resume.go, a later proposal]: the shipped pre-`Runtime.Start` failure branches release the slot by session identifier alone (session.go:133,:147,:157; resume.go:69,:73,:89,:107,:126,:134,:141; the same pattern also stands at sdkwarm.go:236,:241,:251,:298). Under `SlotID == SessionID` a lagging one deletes a later attempt's entry and `RemoveAll`s the tree, uploads and credential directory that attempt staged, exactly as CODE-2's withdrawn rollback release would have. The claim that "`releaseSessionSlot` is correct against an entry a concurrent reclaim removed" is false for all of them. This proposal does not open those branches; the `f3` firing landed the record as a summary out-of-scope row, and the class fix (an identity-checked deregister threading the `*slotState` the claim returned into `deregisterSlotLocked`) wants its own problem statement.
- DEFERRED [proposals/0081_.../0081_....review-log-archive.md, the WATCHOUT at :3785]: it carries the same rule as the standing Trap "Do NOT add a bind-path or a start-state qualifier to §5.2's leak-accounting sentence" and cites its evidence as "summary.md, the `bindConcurrentSlot`'s-reserved-branch bullet under the recorded limits", which the `f4` firing deleted and which no longer resolves. Re-point it to CODE-5's `accountSlotFailure` doc comment (non-spec-changes.md:515) and its caller list (:537-569), which state the same no-carve-out argument. The standing-context half of this correction is applied; the archive is outside every current lane's editable set.
- DEFERRED [pkg/gateway/runtime/adapterclient/client.go, the `Client.Shutdown` doc comment at :797-798]: it says "A zero deadline lets the adapter apply its default grace period". That is false. When the caller's ctx carries a deadline, `resolveShutdownGrace` takes the ctx's remaining time in preference to both the runtime-configured grace and the package default, so a zero deadline yields the CALLER's own remaining budget; the adapter's default applies only when the caller set no deadline at all. OWNER CHANGED: `adapterclient` IS a target now — CODE-6 opens `pkg/gateway/runtime/adapterclient/client.go` for the epoch latch, `BindEpoch` and `ShutdownReclaim` — so the one-sentence repair can be staged under CODE-6 at S9 rather than left to whoever next opens the file. Derived independently by four shards across two windows and still unapplied.
- DEFERRED [spec/06_warm-pod-model.md:156 and docs/reference/state-machines.md:236]: the `slot_cleanup ──→ released` annotation and its docs mirror gloss three actions ("slot workspace removed, processes killed, slot released"), a word-for-word mirror of §5.2's shipped three-action list, while SPEC-3 widens that list to five (the credential directory and the §4.9 direct-mode expiry timers join it). Neither is false, because an abbreviated gloss states nothing wrong, which is the standing trap's reasoning for the identical §6.2:148 annotation, but a reader comparing either to §5.2 after this lands sees a shorter list. If DOCS-1 is ever widened beyond its single added row, this is the sibling row to widen with it.
- DEFERRED [docs/reference/error-catalog.md]: nothing, recorded deliberately. Under the placement-rule design, lines 129, 155 and 156 stay TRUE, because no failure class becomes non-retryable. Recorded explicitly so a later round does not re-file them: they are sites only under the rejected no-retry alternative.
- DEFERRED [proposals/0081_.../0081_....spec-changes.md "Spec files touched"]: the spec/07 half is CLOSED, and the line now reads "the section preamble's premise sentence deleted, a sentence added to step 2, and step 3 replaced". The spec/04 half stands: the list still describes the §4.7 row edit as "(first sentence plus one sentence)" when the replacement swaps two sentences for seven or eight, and the lead-in "Replace the `Shutdown` row's first sentence" is stale in the same way. Six lenses re-derived it and none filed it, because the operative quote-and-replacement pair is exact and no implementor can be misled; one lens argued the parenthetical describes the edit rather than the anchor block and is therefore defensible. A fixer touching the SPEC-1 §4.7 block should correct it in the same edit. A count in that list is a trap that re-arms on every later edit; prefer a named set, as the SPEC-3 entry now uses ("the per-slot cleanup pointer and the cleanup-outcome report rules appended"). The §5.2 entry now also understates: a pod-disposition claim lives in that append and the named set does not hint at it.
- DEFERRED [docs/, four sites that stay TRUE]: `docs/client-guide/session-lifecycle.md:416`, `docs/reference/adapter-contract.md:84`, `docs/runtime-author-guide/index.md:186` and `docs/runtime-author-guide/lifecycle.md:69` also describe the per-slot cleanup and are NOT falsified by SPEC-1 or SPEC-3. Recorded explicitly so the non-spec loop does not widen its docs edit list past the five sites that actually break. Also unchanged: `docs/runtime-author-guide/lifecycle.md:34-43` is a pod-level table in a different vocabulary with no cleanup edge, so SPEC-4 does not reach it.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, CONF-1's `PrepareWorkspace` coverage]: §15.4 publishes a non-conformance case for an adapter that "evaluates the admission rules per `PrepareWorkspace` frame, or mints more than one epoch for one call", and the battery has no case for it. What CONF-1 wants is one case asserting that every frame of a multi-frame `PrepareWorkspace` call is admitted exactly once and that the single response reports one non-zero epoch. This is what survives of the larger echo-property Deferred the r6 revert dissolved; do not restore the echo half.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:1719-1722, the hold-refusal accepted-failure bullet]: it says a §7.3 resume refused by the hold "costs the pod one windowed `RecordFailure` … whose only production call site is `applySlotRetryPolicy`". That is false against the STAGED tree: CODE-5 extracts `RecordFailure` into `accountSlotFailure` and gives it three callers, and the resume path reaches it ONLY through the new `resumeOnPod` caller — which is what makes the bullet's own conclusion true. What is true instead: the refusal costs the pod one windowed failure through `accountSlotFailure`'s third caller. The conclusion stands; only the parenthetical attribution is wrong. Same class as the standing MISTAKE about a scope call resting on shipped behaviour a staged deliverable removes.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:943-945, CODE-6's `deregisterSlotLocked` rationale]: it justifies keeping the signature "for the read-only caller that needs the deregistration without a destroy". After CODE-6 all three production callers go through `reclaimSlotLocked`, so no such caller exists and `deregisterSlotLocked` survives only as that helper's own body. What is true: the signature is kept because `reclaimSlotLocked` calls it, and there is no read-only caller. A fixer already in that paragraph should drop the clause. Sub-threshold on its own; recorded so it is not re-derived.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:1668, a step reference the renumber staled]: it reads "Between S4 and S6 the unedited file still passes" where SPEC-4 is now S5 under the resequenced checklist (S1=SPEC-5 … S5=SPEC-4). What is true: the interval is between S5 and S7. Substantively harmless, and it is the only stale step reference the renumber sweep missed; a fixer touching that paragraph should correct it.

- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, CODE-5's `Targets:` list]: it does not name `isTransientPodClaimError`, although the checklist S13 line, summary.md's index line and `## Files touched` all assign the `codes.Aborted` arm to CODE-5 and the arm is specified in CODE-5's own body. What is true instead: the `Targets:` list owes `pkg/gateway/sessionserver/start.go` `isTransientPodClaimError` beside its existing entries. Judged the same bookkeeping class the loop has refuted on materiality, so not filed; a fixer already in that paragraph should add it.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, the hold-refusal accepted-failure bullet]: this extends the standing entry above it rather than replacing it. The bullet still rests on "whose only production call site is `applySlotRetryPolicy`", which CODE-5 falsifies by giving `RecordFailure` three callers, and the r2 fix stage then BUILT A NEW CLAUSE ON TOP of the false one, adding a "once that retry budget is spent" premise that does not exist on the branch the sentence is about: `resumeOnPod`'s checkpoint-restore branch has no retry loop at all. What is true instead: the refusal costs the pod one windowed failure through `accountSlotFailure`'s third caller, and the client-driven §7.3 resume reaches `holdOrFailOnResumeError` on its FIRST error. The bullet's conclusion stands; the attribution and the premise are both wrong. FILED again this window.

- DEFERRED [proposals/0081_.../0081_....summary.md, the `**Decisions.**` file-collision bullet at :158-167]: it says the adapter edits land in `session.go`, `runtimegeneration.go`, `bindepoch.go`, `slotsession.go` and `slot.go`. After the amendment the proposal also opens `resume.go`, `sdkwarm.go`, `staging.go`, `slotcreds.go`, `holdstate.go` and `server.go` (non-spec-changes.md:2160-2192), and the 0080 §1.1 impacts row already names `resume.go` and `sdkwarm.go`, so the two sites disagree. What is true instead: the bullet owes the full file list, or a pointer to `## Files touched on application`. The `f4` firing recorded it and declined it as out of scope for a format pass; no implementor is misled, because the files-touched list and the deliverable index both carry the full set.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, the `tests/spec-map.json` bullet]: it says `tests/tier4_integration/concurrent_workspace_test.go` "is registered as a whole, under sections 5.2, 6.4 and 28.5.3". The map also gives it a 15.1 whole-file credit through the directory entry `tests/tier4_integration/...`, so the enumeration is four sections. What is true instead: the set is {5.2, 6.4, 15.1, 28.5.3}. The derived action (whole-file credits under 7.1 and 4.7 at S12) is unaffected, which is why it was not filed; a future reviewer recomputing the set will see four ids and should not file it either.

- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:216-221 and :1900-1905]: CODE-1's staged code comment says "`removeSlotTree` reaches the slot's credential directory, so widening it to `removed` is what reclaims `/run/lenny/slots/{sessionId}/credentials.json` for a registered-but-unbound entry", and the tier-9 arm says a regression there "leaves a written credential file and armed timers" for a registered-but-unbound entry. A registered-but-unbound entry can hold NEITHER: `assignCredentialsSlot` sets `st.sessionID` (making the entry bound) BEFORE it writes `credentials.json` and before `reconcileSlotExpiryTimersLocked` arms any timer, and no writer ever clears `sessionID`. What is true instead: the widening from `bound` to `removed` newly reclaims the slot's WORKSPACE TREE and its (empty) credential DIRECTORY for an unbound entry, while the credential FILE and the armed timers were already reclaimed for a bound-but-unstarted entry by the shipped `bound` gate. Not filed as precision-of-wording; the tier-9 arm is the half that would mislead an implementor into asserting an unsatisfiable state. EVIDENCE: pkg/adapter/slotcreds.go:31-33,:44,:50; pkg/adapter/session.go:239,:270.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, SPEC-3's first anchor]: the widened §5.2 `**Slot cleanup:**` action list adds the credential directory and the §4.9 timers but still names only "the slot's workspace directory", while `slotlayout.RemoveTree` removes FOUR trees (`/workspace/slots/{id}`, `/sessions/{id}`, `/artifacts/{id}`, `/run/lenny/slots/{id}`, tree.go:59-70) and spec/06:386 already routes the first three to §5.2. So §6.4's cross-reference resolves to a list missing `/sessions/{sessionId}/` and `/artifacts/{sessionId}/`. Not filed, because the omission pre-exists the edit and the applied spec is no more wrong than the shipped one. A fixer widening that sentence anyway should add both trees in the same breath. This is the SPEC-3 twin of the standing Deferred against spec/06:156 and docs/reference/state-machines.md:236.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:426]: it cites `start.go:2766-2779, :2873-2879` for "`classifySlotBindFailure` passes the error through". The second range is `applySlotRetryPolicy`'s terminal `SlotFailedError` return rather than that function. The load-bearing half (`:2766-2779`) is exact. What is true instead: drop the second range or re-point it at `classifySlotBindFailure`'s own body.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:465]: it says `usage_test.go:233` and the `podmcp_arming_internal_test.go` sites "need only the discard". `bindSessionForTest` (usage_test.go:350-363) calls `ensureSlotStateLocked` and returns nothing, so that caller needs the helper widened to hand the epoch back rather than a bare discard. What is true instead: `usage_test.go:233` takes the epoch from a widened `bindSessionForTest`, and only the `podmcp` sites take the discard. `pkg/adapter/usage_test.go` is already in `## Files touched`, which is why this is a wording repair rather than a missed edit site.

- DEFERRED [implementation-checklist.md, S1 (:17)]: it still tells the implementer that "§15.1 gains a catalog row for `ERROR_CODE_SLOT_BIND_ALREADY_STARTED` and for `ERROR_CODE_SLOT_BIND_ATTEMPT_SUPERSEDED` with each code's category and HTTP status, and the false claim that §15.1 mints no client-visible error code comes out of the deliberately-untouched list". Both halves are false. What is true instead: §15.1 takes NO row for either code, both are adapter-contract codes published through §15.4 alone on the `PROTOCOL_VERSION_INCOMPATIBLE` precedent, `spec-changes.md`'s "Spec sections deliberately untouched" now carries a §15.1 bullet saying the catalog takes no NEW row, and the §15.1 edit SPEC-5 does stage is the WIDENING of the `SETUP_COMMAND_FAILED` row to state both deterministic `FAILED_PRECONDITION` causes and qualify its setup-output remedy. S1 also still states the token/`mid_session` pairing unscoped, describes the started-entry refusal without naming `ConfigureWorkspace`'s idempotent-repeat exemption, and does not name the start confirmation among the §4.7.1 rules or among the §15.4 clause set. Four separate fix groups derived parts of this and none could edit the checklist.
- DEFERRED [implementation-checklist.md, S3 (:21)]: it says SPEC-2's §7.1 paragraph covers "the creation finalize block, the §15.1 start transition and the §7.3 re-attach" and describes it as landing after the atomicity paragraph alongside an edit to that paragraph. What is true instead: SPEC-2 stages ONE §7.1 change, an inserted paragraph, the atomicity paragraph is NOT edited, and the paragraph's scope is the §15.1 start transition onto a slot on a pod serving concurrent sessions (reserved at creation or placed by §5.2's retry policy) plus the §7.3 re-attach onto a replacement pod. S3's closing sentence, that the `**Max retries:**` bullet is left as it stands, is correct and needs no change.
- DEFERRED [implementation-checklist.md, S6 (:27)]: it lists SPEC-6's rows as "a compensation that removed an entry whose session had started, a compensation refused on a token mismatch, and a `Shutdown` that met an entry carrying no token", and carries the withdrawn "must be zero" expectation for the untokened-entry counter. What is true instead: SPEC-6 stages a row for a compensation ANSWERED `superseded` (meaning the adapter held an entry the compensation was not addressed to, so the reclaim released nothing), a row for a compensation answered with a reclaim outcome the gateway build does not recognize, and a row for a `Shutdown` that met an entry carrying no token; the §16.1 rows state no expected value and no expected rate. The phrase "refused on a token mismatch" names the BIND-path refusal, which is a different event on a different RPC that no staged counter observes.
- DEFERRED [implementation-checklist.md, S7 (:29)]: it still says "The published error catalog gains a row for each of the two new codes" and lists DOCS-3 on that basis. What is true instead: DOCS-3 is the `docs/reference/error-catalog.md:129` `SETUP_COMMAND_FAILED` row edit, stating both `FAILED_PRECONDITION` causes and qualifying the setup-output remedy in that page's own voice with no spec section numbers. That page is REST-only, so "a row for each of the two new codes" could never have meant it under the no-§15.1-row decision; the adapter-side codes' published home is DOCS-2's adapter-contract block.
- DEFERRED [implementation-checklist.md, S13 (CODE-6)]: it does not name `validateBindFields` or its placement. What is true instead: the `bind_attempt`/`mid_session` pairing is checked at each carrying handler's entry, before the resolve, returning `INVALID_ARGUMENT` having created and resolved nothing, and it is NOT one of the rules inside `ensureSlotStateLocked`. S1 may also gain "checked before the adapter resolves anything" on its existing clause about the non-empty-exactly-when rule.
- DEFERRED [implementation-checklist.md, S15 (CODE-1)]: it states "The empty-token row exists to fail closed and carries a counter, because no compensated path can produce an unstamped entry once every mint site and the mid-session creation bar are in place." That is false. What is true instead: the row is reachable on a conforming adapter, because `StartSession` and `ConfigureWorkspace` carry no token and create an untokened entry when they resolve none, which SPEC-5 records as an accepted residue; the counter's expected value is zero on the bind paths and non-zero when a late start left an untokened entry behind.
- DEFERRED [implementation-checklist.md, S17 (:—)]: it names two `noteRuntimeStarted` call sites as taking the start-confirmation refusal (`StartSession`, `Resume`). Under the universal obligation the SDK-warm site `pkg/adapter/sdkwarm.go:261` is a THIRD, and it takes the refusal through `releaseSessionSlot` with the §6.1 `DemoteSDK` fallback rather than a bare `Runtime.Close`.
- DEFERRED [implementation-checklist.md, S20 (:55)]: it still names the started-entry counter "which must be zero once the identity gate lands and is the regression detector", still says the untokened-entry counter "must be zero", still names the superseded counter "a compensation refused on a token mismatch, which is non-zero on a system that retries and is the evidence the leak is closed", still carries "`ShutdownResponse` reports whether the entry the compensation met had started", and still lists "the tier-3 closed-field-set gate" among CODE-9's sites. What is true instead: CODE-9's counters are the superseded counter (no stated expectation), the unrecognized-outcome counter (zero, and non-zero means the gateway and the adapter are at different contract versions) and the untokened-entry counter (zero on the bind paths, non-zero when an abandoned attempt's late `StartSession` left an untokened entry behind); `ShutdownResponse` carries no started signal; and the closed-field-set gate belongs to SCHEMA-1's step. S22 must also name CONF-1 clause (j).
- DEFERRED [non-spec-changes.md, CODE-6's production call-site table]: it gives the `StartSession` row a `bindAttempt` of `req.GetBindAttempt()`, which is not constructible, because SCHEMA-1 gives `StartSessionRequest` no such field and §4.7.1 says `StartSession` carries none. What is true instead: the row passes the empty string, exactly as the `ConfigureWorkspace` row already does. Filed by three lenses across two rounds.
- DEFERRED [non-spec-changes.md, the CODE-9 counters table's Expected value cell for `lenny_slot_compensation_superseded_total`]: it reads "non-zero on a system that retries, which is defect 17 being closed", which is false for the same reason the §16.1 row's old clause was. What is true instead: the cell should state no expected value, or state that the outcome means the adapter held an entry the compensation was not addressed to. The "Fires when" cell in the same row is already correct and must not be touched.
- DEFERRED [non-spec-changes.md, CONF-1's behavioural case list]: it has no case for the identity-versus-phase overlap. What is true instead: a bind-sequence RPC carrying attempt B against an entry stamped A whose session has already started must be refused `SLOT_BIND_ATTEMPT_SUPERSEDED` on `codes.Aborted` rather than `SLOT_BIND_ALREADY_STARTED`. The CONF-1 preamble precedence sentence and the DOCS-2 mirror sentence landed; the test case did not.
- DEFERRED [non-spec-changes.md, CONF-1's clause-precedence note]: it carries only the identity-before-phase example. What is true instead, now that §15.4 publishes the well-formedness precedence: a request meeting clause (g)'s condition is refused under (g) even when it also meets (h)'s, and even when the adapter holds no entry for the slot, because well-formedness is checked before the adapter resolves anything.
- DEFERRED [non-spec-changes.md, CONF-1's property count]: it says "Four properties, one per part of the §15.4 contract" while SPEC-5 lands TWO §15.4 blocks and all four properties are bind-attempt properties. What is true instead: the four cover the token block alone, and the reclaim hold is unpublished in the battery. Either drop the "one per part of the §15.4 contract" clause or add the hold's property.
- DEFERRED [non-spec-changes.md, the §15.1/`SETUP_COMMAND_FAILED` docs mirror]: the file has NO DOCS-3 section and no mention of `docs/reference/error-catalog.md` at all. `docs/reference/error-catalog.md:129` differs from spec/15:1136 in wording (it says "the runtime adapter reports as a deterministic failure", names no gRPC code, and carries its own Remediation column), so a DOCS-3 author must edit that page's own sentences rather than copy the spec replacement text.
- DEFERRED [summary.md, the "Watch out for" bullet at :302-307]: it still says the §7.1 connection sentence "reads today, in the staged text, that the reclaim never dials a new connection because the value it carries is latched off the responses that connection reported" and "must be rewritten rather than left standing". That is now false. What is true instead: the rewrite has landed, the staged text states the connection as a cost preference ("the reclaim is sent on the connection the failed attempt already holds when that connection is still open, because reusing it costs nothing; the fence does not depend on the connection"), and the bullet should be retired or restated as a record of a completed correction.
- DEFERRED [summary.md, the CODE-9 file-list bullet at :843]: it names the first counter "a compensation refused on a token mismatch". What is true instead: it counts a compensation ANSWERED `superseded`. The design for that group did not adjudicate the line as a site, so it was left under a lens that had not reviewed it.
- DEFERRED [non-spec-changes.md, SPEC-6's preamble mirrors]: the spec-side preamble ("Add three rows ... The first two are gateway series ... the third is an adapter series") and its non-spec twin ("The first two are gateway series. The third is adapter-side") still read as if the gateway pair were the started-entry and mismatch counters. Both must be rewritten count-free, because `doc-style.md` bans the count and the gateway pair is now superseded plus unrecognized.

- DEFERRED [proposals/0081_.../0081_....summary.md, the Deliverable index]: it carries no DOCS-3 entry at all (`grep -n DOCS-3 summary.md` returns nothing), so the index and its count are short by one, while `.non-spec-changes.md` stages DOCS-3 in full at :1838-1877 and `.spec-changes.md` now names it in two places. What is true instead: DOCS-3 · `docs/reference/error-catalog.md` · the `SETUP_COMMAND_FAILED` row mirrors its widened §15.1 row, tier 11, landing after SPEC-5. Filed twice in one round by two fix groups, neither of which could edit the summary.

- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, DOCS-1]: DOCS-1 stages only the per-slot sub-state table row. `docs/reference/state-machines.md:138` carries the same "or a claim deleted on a pod with `recycle.enabled: false`, projects `draining` then `terminated`" clause that SPEC-4 now corrects in spec/06, so the published mirror will contradict §6.2 once SPEC-4 lands. What is true instead: a claim deleted while the pod projects `claimed`, on a pool of either recycle setting, projects `draining` then `terminated`. DOCS-1 needs that sentence replacement added. Not landed by the spec lane, which may only repair statements its own edit falsified; adding staged docs content is authoring.
- DEFERRED [proposals/0081_.../0081_....implementation-checklist.md, S5 and S7]: S5 states SPEC-4's scope as the §6.2 per-slot fence edge plus the pre-`running` cleanup paragraph. SPEC-4 now also carries a THIRD §6.2 site, the occupancy-projection claim-deletion trigger in the fence and the matching clause in the projection prose. What is true instead: SPEC-4 has three §6.2 sites, and S7 (DOCS-1's scope) becomes stale once the `state-machines.md:138` deferral above is discharged, because DOCS-1 then has two edits in that file. The reconciliation pass owns the checklist; the spec fixer may not edit it.
- DEFERRED [proposals/0081_.../0081_....implementation-checklist.md, S1 (:17)]: S1 claims the §4.7.1 pointer at §5.2's reclaim hold is "the one citation this order cannot put first". It is not the only one. SPEC-2's staged §7.1 paragraph asserts that §5.2 "states that the adapter holds that identifier from the deregistration of its registry entry until the cleanup that reclaims it has finished" (spec-changes.md:463), and SPEC-5's §15.4 reclaim-hold block asserts the same of §5.2 (spec-changes.md:797). Both land at S1/S3, ahead of S4, which is the step that writes that sentence into §5.2. What is true instead: three staged citations forward-reference S4, not one. The remedy is either a reworded S1 line or moving SPEC-3 ahead of SPEC-2 and SPEC-5; the spec text itself is correct once all four steps land.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, CODE-9 and `pkg/adapter/metrics.go`]: this extends the standing CODE-9 catalog Deferred rather than replacing it, and adds the collector location it did not carry. `pkg/gateway/observability/catalog.go` does not exist; the CODE-9 body's "registered in `catalog.go` beside `SlotFailure` (`slotbinder.go:358`)" also conflates two files, because slotbinder.go:358 is `recordSlotFailure`, a hook forwarder rather than a catalog registration. What is true instead: the two gateway counters are registered in `pkg/observability/metrics/catalog.go` and transcribed into `spec161Metrics` in `pkg/observability/metrics/catalog_test.go`, while the Prometheus collector itself lives with the other gateway slot series in `pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go:200-226`, injected through a `Binder` hook the way `SlotFailure`, `SlotReplacement` and `SlotLeakGauge` already are; and the adapter-side `lenny_slot_shutdown_untokened_entry_total` must be registered in `pkg/adapter/metrics.go`, which no deliverable names and which is the source of truth the tier-11 adapter sweep regexes.
- DEFERRED [proposals/0081_.../0081_....implementation-checklist.md, the frame predicate]: recorded as a negative so the reconciliation pass does not go looking. NO checklist line restates the `PrepareWorkspace` per-frame predicate; S13 says only that `PrepareWorkspace` resolves both fields from the first frame, which remains true under the asymmetric rule. Checked against S1, S9, S13 and S15.

- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, CONF-1]: the battery is short four cases and one descriptor. It has no case for a multi-frame `PrepareWorkspace` admitted exactly once per call, no case for the identity-versus-phase overlap (a bind-sequence RPC carrying attempt B against an entry stamped A whose session has already started must be refused `SLOT_BIND_ATTEMPT_SUPERSEDED` rather than `SLOT_BIND_ALREADY_STARTED`), and no case for the well-formedness precedence §15.4 publishes; and its "four properties, one per part of the §15.4 contract" claim does not match the two §15.4 blocks SPEC-5 lands. Writing the cases is authoring staged test content, so the non-spec lane owns it.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, CODE-4]: CODE-4 opens `pkg/gateway/podlifecycle/podsession/binder.go` and stages no correction to `drain`'s doc comment at :1191-1194, which reads "On a `recycle.enabled: false` pool a claim DELETE projects `draining` then `terminated`; on a recycling pool under its limits the projection returns the pod to `idle`". Both halves are falsified by SPEC-4 and by the controller. What is true instead: a claim DELETE while the pod projects `claimed` projects `draining` then `terminated` on a pool of either recycle setting, and the return to inventory is the `reserved → idle` edge reached only after the claim has been patched through `recycling`. Comment-only, no behavioural change; fourth recording.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, CODE-6]: CODE-6's `ConfigureWorkspace` idempotent-repeat arm is specified to read the entry under a SECOND `s.mu` acquisition after `claimSessionSlot` returned, which is the pattern CODE-2's own doc comment rules out ("a read taken after the claim is as racy as the confirmation it anchors"). What is true instead: have `claimSessionSlotUnderLock` return what the caller needs on its `idempotentRepeat` early-return arm, as it already does on the fresh arm, so no post-claim read exists. Not filed because SDK-warm is `maxConcurrentSessions: 1` only and no staged caller races it.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, SCHEMA-1]: SCHEMA-1 quotes the tier-3 gate's failure text without the `= ` the shipped format string emits (`"%s declares an unexpected field %d = %q"`), and quotes the withdrawn epoch-era field name. What is true instead: the real message carries `= ` before the quoted name and the field is the token-era one. Cosmetic; the staged `wantReq` and `assertFieldSet` edits are correct as described.
- DEFERRED [proposals/0081_.../0081_....spec-changes.md, four corrections the spec lane's own passes could not take]: the "Spec files touched" list still describes the §4.7 `Shutdown` row edit as "(first sentence plus one sentence)" where the replacement swaps two sentences for seven or eight, and still describes the §6.2 client-visibility edit as "one clause re-keyed on the gRPC code" where the clause is now re-keyed on the request the adapter answered as well as on the code; the Design section's pointer list omits the SPEC-2 §7.2 snapshot-close edits; and the Design section's "SPEC-4 states that once, in §6.2" is false now that §4.6.1 is a SPEC-4 edit site. Every one of those statements is mutually consistent with the applied spec, so nothing breaks. A fixer with the spec lane open takes all four.
- DEFERRED [proposals/0081_.../0081_....non-spec-changes.md, the docs deliverables]: `docs/operator-guide/troubleshooting.md:41` reads `| Setup command failures | reason: setup_command_failed | Check runtime setup commands, increase setupPolicy.timeoutSeconds |`. After SPEC-5 that reason gains a second cause, a started-session refusal of the setup-command request, which runs no setup command and for which both listed remedies are wrong. What is true instead: the row's cause column needs the second cause and a remedy that does not point at the setup script. NOTE: an EARLIER deferral against this same line was STRUCK on the ground that the row is the `error_type` label of `lenny_warmpool_warmup_failure_total` rather than the REST envelope's `details.reason`. The two readings are about the same row and neither corrects the other; the newer one is kept here. UNVERIFIED: which reading governs. Read the section heading and the query above the row before editing it. CORRECTED (`non-spec.8.review-docs-alignment.1`): the warmup-label reading GOVERNS and this deferral is wrong. That row sits inside the **Warm Pool Exhaustion** section under a diagnosis block whose query is `rate(lenny_warmpool_warmup_failure_total[5m]) by (reason)`, so its `reason: setup_command_failed` is the warmup-failure label rather than the REST envelope's `details.reason`, and SPEC-5 does not reach it. The UNVERIFIED above is answered; do not apply this entry.

- DEFERRED [implementation-checklist.md, S15]: S15 is INCOMPLETE and partly false after the round-9 through round-11 guard work, and this SUPERSEDES every narrower form of the S15 deferral. Its opening "A per-slot mutex" names a representation CODE-6 no longer stages (the guard is a per-slot capacity-one channel); its "The guard is acquired ahead of `s.mu` in both" is false for `Shutdown`, which compares under `s.mu` first and acquires the guard only on the arm that destroys; its guard-scope enumeration omits `Resume`; its hand-out sentence contrasts the hold-checking form with "letting the caller block", which leads straight to the unbounded acquisition the change removes. What is true instead: a section acquires the slot's guard before the resolve that hands out its paths and holds it until its path work ends, which is `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup` and `Resume`; the three destructive sections acquire it before `s.mu`, except `Shutdown`; both hand-out forms take the caller's context and neither wait outlives it; a destructive section whose acquisition expires destroys unguarded and logs `slot_guard_not_acquired`, an admission RPC whose acquisition expires is refused; `onHoldTimeout`'s ten-second context is the pass's guard-acquisition deadline alone and each member mints its own ten-second close context for `emitFinalUsage` and `Runtime.Close`; and S15's test list gains the three tier-1 expiry cases (destructive fall-through, admission refusal, per-member close budget) plus a whole-file 4.7.1 credit for `pkg/adapter/holdstate_test.go` in `tests/spec-map.json`.
- DEFERRED [implementation-checklist.md, S7]: S7's DOCS-1 clause reads "The per-slot sub-state table in the state-machine reference gains the row matching the new §6.2 edge". What is true instead: DOCS-1 makes TWO edits on `docs/reference/state-machines.md` — that table row, and the three projection-clause replacements in the `## Pod state machine` paragraph at :138, each re-keyed on the phase the pod projects at the claim DELETE. S7 also names only the `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` extension where `## Testing` requires both shipped tests in `tests/tier11_docs/per_slot_substate_scope_doc_reconciliation_test.go` to be extended in place. The DOCS-1 half of this is LANDED in the staging; only the checklist line is stale. S7's `Depends on: S1, S2, S5` should also gain S4, because DOCS-2's own text says it lands after SPEC-3.
- DEFERRED [implementation-checklist.md, S22]: S22 describes CONF-1's battery as "cut one case per named §4.7.1 rule", which is an incomplete description now that the battery also carries one case titled for the evaluation order rather than for a rule. Its stated done-criterion ("every rule in that section's set has a case at tier 3 and at tier 10") is NOT falsified and stays. What is true instead: the clause needs the exception the CONF-1 preamble already carries.
- DEFERRED [implementation-checklist.md, S10 and the spec-map granularity lines]: S10 is accurate as written and needs no edit; recorded as a negative so the reconciliation pass does not re-derive it. What DOES fall due: the generic step-5 boilerplate ("registers its tests in `tests/spec-map.json` under the sections its annotations cite") is true but no longer sufficient, because the files this proposal adds cases to differ in registration granularity; each step that lands cases should say which granularity its file takes.
- DEFERRED [non-spec-changes.md, CODE-9's adapter-deferral sentence]: it says "SPEC-6's row for this series carries the deferral wording verbatim, so it follows the §16.1 rows that already carry it." It does not. The four shipped §16.1 adapter rows read "emitted by the adapter process inside the agent pod and therefore outside the [§16.9](#169-prometheus-scrape-targets-and-crds) default scrape target set until a deployer wires an adapter scrape target", while SPEC-6's staged row reads "Adapter-side; not scraped until the adapter metrics endpoint is wired" and cites no §16.9. What is true instead: the row states the same deferral in shorter words and drops the cross-reference. Below the bar on its own — no gate reads it and the paragraph's real justification (that `spec161Metrics` is reconciled against `MetricCatalog()` rather than against §16.1) is sound — so a fixer already inside either the SPEC-6 row or that paragraph should either match the sibling wording or drop the word "verbatim".
- DEFERRED [non-spec-changes.md, CODE-9's method-versus-free-function justification]: it argues `noteCompensationOutcome` is a method "rather than a free function, because the series is labeled by `pool` and `k8s_pod_name` and is emitted through a `Binder` hook, both of which a package-level function can reach". The clause inverts its own argument. What is true instead: "neither of which a package-level function can reach". One word, no mechanism turns on it; a fixer passing nearby should take it.
- DEFERRED [non-spec-changes.md, CODE-6's heading file list]: it omits `pkg/adapter/credentials.go`, `pkg/adapter/server.go` and `pkg/adapter/sdkwarm.go`, all three of which its own body and `## Files touched` assign to it. What is true instead: the heading should be re-derived from `## Files touched` rather than patched one name at a time. The identical defect against `resume.go` was confirmed and fixed in an earlier round and the CODE-2 half was fixed in round 12, so the class is live and this is the survivor.
- DEFERRED [summary.md:72]: the narrative bullet says the start confirmation binds "both RPCs" and cites three adapter files. What is true instead: it binds THREE request paths across four files, including the SDK-warm `ConfigureWorkspace` site and `pkg/adapter/sdkwarm.go`. Round 12's design explicitly barred touching it from the deliverable-heading fix.
- DEFERRED [summary.md:948]: the deliverable index says DOCS-3 takes "the four replacements that match SPEC-5's §15.1 edits". SPEC-5 stages only THREE §15.1 sentence replacements and leaves the exclusion sentence as it stands; DOCS-3's fourth replacement re-keys the page's own retryable-fallback sentence off the cause class and has no SPEC-5 counterpart, which the spec-changes docs-mirror trailer states correctly. What is true instead: "four replacements, three of them mirroring SPEC-5's".
- DEFERRED [summary.md:220-221 and :941]: both describe `holdstate.go`'s CODE-6 edit as "a deferred hold release" alone. What is true instead: `terminateHeldSession` gains a slot-guard acquisition with TWO deferred releases (the reclaim-hold release the `heldSession` carries, and the guard release), and the §10.1.4 pass's ten-second budget splits into a guard-acquisition deadline plus a per-member close context. INCOMPLETE rather than false, which is why it was recorded rather than filed; the 0080 impacts row's copy of the same claim was corrected by `non-spec.11.fix-F0.1`, so the summary is now the lone lagging carrier.
- DEFERRED [summary.md, the CODE-6 deliverable-index bullet]: it describes `acquireSlotGuardForResolve` as refusing on the reclaim hold alone. What is true instead: it refuses on two conditions, an open reclaim hold and an acquisition that outlives the caller's context. Nothing there is false, only incomplete.
- DEFERRED [non-spec-changes.md, CODE-4's `Targets:` list]: it omits `pkg/gateway/sessionserver/upload_to_session.go` and `pkg/gateway/sessionserver/start.go`, both of which CODE-4's own body edits (the §7.4 `mid_session` pair; the three `ReleaseSlotReservation` call sites) and both of which `## Files touched` carries. What is true instead: the Targets list owes both. Recorded rather than filed as the site-list-completeness class, but the `upload_to_session.go` edit is load-bearing for the checklist's own ordering guarantee and no step line names it.
- DEFERRED [non-spec-changes.md, the tier-1 wire-rule case]: it says both pairing directions are asserted "across `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup`, `AssignCredentials` and `Resume`", while SCHEMA-1 gives `mid_session` only to `PrepareWorkspaceRequest` and `FinalizeWorkspaceRequest`, so the mid-session-request-carrying-a-token direction is unconstructible on the other three. What is true instead: the empty-token direction is testable on all five and is the one that matters; the sentence is over-specified rather than a coverage gap.
- DEFERRED [non-spec-changes.md, the `answerShutdown` exit count]: it says "The one return that does not go through it is the two-field precondition's `INVALID_ARGUMENT`". The shipped empty-session-id check at `pkg/adapter/session.go:228-230` also returns without it. Both are `INVALID_ARGUMENT` no-ops that perform nothing including the scrub, so the RULE the sentence states is right and only its count is loose.
- DEFERRED [non-spec-changes.md, the tier-7a arm that asserts the §10.1.4 ordering]: the third arm's second run was non-discriminating in both directions and the close-budget property moved to a deterministic tier-1 case. What is true instead: the tier-7a arm keeps only the ordering and no-residue assertions, with a park SHORTER than the acquisition deadline so the guard is genuinely acquired; a park longer than the blocked caller's own context is a passing run that asserts nothing.
- DEFERRED [implementation-checklist.md and non-spec-changes.md CODE-4]: `pkg/gateway/sessionserver/upload_to_session.go` is a REQUIRED edit site owned by no deliverable's Targets list and named in no checklist step. `## Files touched` carries it with its value stated (non-spec-changes.md:3563, "the §7.4 pair sets `mid_session` true and carries no token") and CODE-4's prose calls it "the mirror" (:807), but CODE-4's heading scopes the deliverable to `pkg/gateway/podlifecycle/podsession/podsession` and its Targets list (:759-770) does not include it, and neither S13 nor S20 names it. Today `upload_to_session.go:128` calls `bind.Adapter.PrepareWorkspace(ctx, row.ID, uploads)` with no mid-session marker, so once CODE-6's `validateBindFields` lands, a §7.4 upload's `PrepareWorkspace` carries neither a token nor `mid_session` and is `INVALID_ARGUMENT`. Not filed: the client.go signature change at S13 breaks the caller at compile time in the same step and the value the caller must pass is stated twice, so the material bar ("Files-touched is the authoritative list") is not met. What is true instead: CODE-4's Targets owes one line and S13 owes one clause. This is the checklist half of the standing Deferred against CODE-4's `Targets:` list, which carries the non-spec half.
- DEFERRED [non-spec-changes.md, "Files touched", line 3592]: `pkg/gateway/sessionserver/start_test.go` is listed among the touched test files and is named NOWHERE else in the proposal — no deliverable, no Testing bullet, no checklist step states what edit it takes. This is the same item `### Open` carries as "`start_test.go` is listed as a tier-1 home for CODE-4/CODE-5 but carries no assigned case", and it has now survived at least three more rounds unresolved. It is a bare filename with no stated value, which is the class-2 pattern, but nothing breaks if an implementor ignores it, so it was not filed. The live hazard if they do NOT ignore it: landing a `BindReservedSlot(` literal in that file pulls the whole shipped file into `slotAddressCaseFiles` in WHOLE-FILE mode and turns tier 0 red on its PRE-EXISTING §15.1 and §4.4 citations. What is true instead: name the case or drop the file from the list.

### Retired

Retired in compaction pass 18, each with the reason that keeps it closed. Three Open items were retired because the 2026-09-16 revision deleted the mechanism they rested on, and each has a token-era sibling that stands: "Does CONF-1 owe a fifth property?" (its four properties were all bind-epoch and its `PrepareWorkspace` clause was about minting more than one epoch per call; the live form is "Does CONF-1 owe more properties, and does the reclaim hold owe one?"); "May an intermediate commit carry a `WIRED` claim row whose reader is two steps away?" (it turned on `Client.BindEpoch` at S9; the live form merges both halves as "Are the two `WIRED` claim-register rows owed at all, and may an intermediate commit carry one whose reader is two steps away?"); and "Does the tier-3 `adapter_bind_epoch` directory name survive the rename to the token?" (`tests/tier3_contract/adapter_bind_epoch/` is named by no staged deliverable, and the surviving half, that naming a test file with `slot` in its basename pulls it into `slotAddressCaseFiles`, is kept as its own entry). One Deferred was retired as superseded: the epoch-vocabulary duplicate of CONF-1's property count, whose token-vocabulary form stands unchanged beside it.

Three further Open items were retired in pass 18 as ANSWERED by round 8 rather than by the revision, their answers moved into `### Settled`: whether a coordinator handoff or a rolling update can strand a tokened entry (it cannot; only a SIGKILL, crash or OOM can); whether a `Shutdown` carrying neither a graceful window nor a deadline leaves the hold unbounded (the tree bounds it through `resolveShutdownGrace`'s fallback chain, and what the staged sentence lacks is the fallback rather than a bound); and the pre-attribution of the second of those to `spec.8.review-reliability.1`, which looked and did not file.

Retired in passes 1 and 2, each closed rather than dropped, with the closure reason that keeps it closed: the `coordination_generation` question (`Shutdown` is unfenced, validated only in `CoordinatorFence` and `CheckpointBarrier`); whether `Binder.Resume`'s compensation needs its own §7.3 sentence (answered by lifting the obligation into its own §7.1 paragraph scoped to any bind attempt); the CODE-2 second `ReportSessionScrub` DEFERRED (the call and the `errStartRaceReclaimed` sentinel were deleted); whether the Design paragraph's deferral of the credential file and the §4.9 timers to §5.2 is a false attribution (SPEC-3's first anchor widens the action list to name both); the four condensed checklist DEFERREDs of the pass-1 preamble (superseded by the fuller per-group versions); "Vacuous parenthetical at steps 2-8" (§29.2's mirror covers steps 2-10, which issue no pod-side RPC); "Recycle boundary on a failed bind" (`recycle` rather than `leaked` patches `bound → recycling`, and `ReleaseSlotReservation` passes `recycle=false`, so occupancy zero DELETEs the claim); "SPEC-3's closing note may be stale" (the edit landed and the four-site drift is gone); and the "On the default disposition the pod is replaced." dispute (one site rather than two, rewritten in Traps).

Two pass-2 retirements carry a residue that is still live. The OPEN "Resume path and the exclusion" was closed in the proposal's favour by nine lenses (`Binder.Resume` claims through `b.connect` → `podclaim.Claimer.Claim`, an idle-only whole-pod claim, never through `ClaimSlot`), and `spec.5.review-mechanism.1`'s disagreement is carried as the UNVERIFIED beside the Settled entry rather than dropped. The pass-1 six-anchor sweep entry was DELETED rather than retired, because a warning that a sweep is finished is worse than useless once nine more anchors have been minted; the sixteen-site list in Settled replaces it.

Retired in compaction pass 3, all closed rather than dropped:

- The OPEN "Does the resume compensation feed the slothealth ledger?". Answered NO and fixed: `podsession` reaches no tracker, gauge or registry, and CODE-5 now gives `accountSlotFailure` a third caller on `resumeOnPod`'s `podBinder.Resume` failure branch. The durable statement is in Settled.
- The UNVERIFIED "Is §29.4 scoped to a started session?". Settled by §29.4's own `**Preconditions.**` paragraph, which scopes the whole trace to a completed §29.2 startup. Four lenses read it cold and agreed, and the round-7 pair that filed the opposite is superseded rather than merely outvoted, because the paragraph they did not cite decides it.
- The two tier-7a items, "drain-gate assertion" and "closed exactly once". The first is closed: every `Shutdown` target in the shipped drain-gate tests is driven through `startDrainSession`, so CODE-1's gate move cannot reach them. The second is closed by deletion: the clause was false as a call count and as a teardown count, and the trap that replaces it says why.
- The UNVERIFIED "Does CODE-1's drain move lose a DRAINING obligation?". Answered: §15.4.2 owes none for a bound-but-unstarted session, and the withheld frame is now pinned by a tier-1 assertion.
- The OPEN "No section says positively that a reclaim can find a slot in `running`". Resolved as no-sentence-added: SPEC-4's `**Pre-`running` slot cleanup.**` paragraph already states it.
- The OPEN "Redis rehydration loses leaked occupancy". Resolved as its own proposal and recorded as a row under the summary's shipped-defects section.
- The UNVERIFIED "'on a pod of either concurrency' and the leaked sentence" and the OPEN "Pre-start cleanup failure and the §5.2 threshold". Both closed by the same concurrency split, which states the accounting for a concurrent pod and points at §7.1 for a one-session pod.
- The UNVERIFIED "tier 11 against the adapter-contract drift". Closed: the gate is a substring presence check and passes silently. The fact moved to Settled and the consequence to the Deferred entry it belongs with.
- The two fixture items, "Tier-4 fixture pool size" and "Tier-1 adapter fixtures". Both read: the tier-4 file seeds one Sandbox and `export_test.go:45` is already a `noteRuntimeStarted` seam. What survives is the narrower fixture-extension OPEN.
- The OPEN "0080 §1.19 membership" and its sibling DEFERRED against the summary row. Both discharged by the index-and-checklist reconciliation pass, which rewrote the row to state all three membership cases and the placement consequence.
- The OPEN "Unbound co-tenant and the drain gate". Superseded: after CODE-1 the signal needs `started` and no bound entry, and the residual case is an ordinary started session end. It is now a "do not file" line in Traps rather than an open question.
- The OPEN "This lens has swept itself out". Corrected in place rather than kept: the operational lens returned findings on the non-spec staging.
- Seven checklist and summary DEFERREDs (S1, S2, S3, S7, S8, the older S10 on the no-retry rule, and the 0080 §1.19 row), plus the CODE-2 entry and the blob-store edge-case clause. Each was applied: the first seven by the index-and-checklist reconciliation pass, the CODE-2 residue by round 2 taking the second branch and recording both orderings in the accepted-failure-modes list, and the blob-store clause by the edge-case rewrite that states the windowed-counter arithmetic honestly. The newer S9 and S10 corrections that landed after them are carried in Deferred, and the blob-store clause's SPEC-side twin is carried there too, because that pass could not edit `spec-changes.md`.

Retired in compaction pass 5, all closed rather than dropped:

- The UNVERIFIED "Does `releaseCredentials` cover user-source leases?", which was disputed with no correction between the two readings. Closed affirmative: `credassign.Service.ReleaseSession` iterates `s.leases.LeasesBySession([sessionID])` over the shared lease store and the `UserCredentialAssigner` doc comment states it outright. The reading that `releaseCredentials` touches only `b.Credentials` is right about the call and wrong about the consequence, because `b.Credentials` is the store both minters write into. Two lenses reached it independently.
- The OPEN "CODE-2's rollbacks run on the inbound context". Closed: all three `RuntimeProcess.Close` implementations use the context only to derive the SIGTERM grace and none checks `ctx.Err()`, so a cancelled context makes the close harsher rather than absent and the active-set removal the rollback exists for always lands. `context.WithoutCancel` is therefore not owed there.
- The OPEN "The reserved-branch defects row contradicts its own header". Closed by deletion: the `f4` firing removed the row from `## Defects in the shipped tree that this proposal does not stage`, because CODE-5 stages the fix for the exact defect it named and the section is the inventory a later reader uses to learn what 0081 leaves open. The shipped-tree fact survives in CODE-5's trade paragraph and the no-carve-out argument in its doc comment.
- The UNVERIFIED "Tier-1 resume accounting fixture". Closed on the seam question: `s.podBinder` is a concrete `*podsession.Binder` so no interface seam exists, but the package's own tests already build real `podsession.Binder` values against a fake or envtest client at five named sites and one of them already drives `resumeOnPod`. What remains is a fixture build rather than an open question.
- The OPEN "The operational lens is no longer swept out". It was a correction rather than a question, it has been acted on for several rounds, and the surfaces it named are recorded as Settled facts.
- The disputed DEFERRED on `docs/reference/state-machines.md:251`. Settled STALE by derivation rather than by vote: after application, staged §7.1 and SPEC-3's §5.2 scrub-model append both scope the `leaked` disposition to a pod serving concurrent sessions and give the exclusive pod §7.1's retirement disposition, so the doc prose agrees with the applied spec and is not an edit site. The sibling asymmetry it used to be paired with survives as the standing Open on §6.2's fence versus §6.2's prose at concurrency 1.
- Two claims this window superseded rather than closed, kept here so nobody re-derives the old form. `SlotID == SessionID` is stated in `spec/` (spec/05:395), so §7.1 restates rather than mints it; and `validate-maps` enforces per-file spec-map membership at tier 0, so the three entries that declined the spec-map item on "existence, version and dangling paths only" declined it on incomplete ground. Both corrections are applied in Settled and in Open.

Retired in compaction pass 6, all closed rather than dropped:

- The OPEN "The reclaim's deadline has no margin over the adapter's own cleanup budget". Closed by the fix it produced: `compensateFailedSlotBind` now sends `budget/2` as the graceful window inside an RPC deadline of `budget`, so the adapter's SIGTERM pivot and the gateway's give-up instant are no longer the same moment and a completed reclaim is no longer recorded `Leaked`. The fix shard named the Open as the finding it was filing. Its sibling, "Nothing at any tier pins that the compensating `Shutdown` stays inside its budget", is NOT closed: the tier-1 case now pins the two values' relation and nothing pins the wall-clock wait.
- The UNVERIFIED "Does `spec/18` need an edit?". Answered NO by three lenses on the same derivation: spec/18's phase-12c deliverable lines state the slot and recycle surfaces at the level of the split itself and enumerate no precondition, report rule or state-machine edge, so nothing SPEC-1 through SPEC-4 stages can make a phase deliverable stale. The durable statement is in Settled.
- The UNVERIFIED "Does §5.2's `Client error on exhaustion` want a fourth reason value for a reclaimed-slot race?". Answered NO: `codes.Aborted` passes through `classifySlotBindFailure` unclassified and lands on the existing retryable `STARTING_FAILED` / `SESSION_CREATION_FAILED` fallback in `writePodClaimError`, so no new client-visible code and no `docs/reference/error-catalog.md` row is owed.
- The DEFERRED against `spec-changes.md:98-102` (the blob-store edge-case clause). Retired because two entries disagreed and neither corrected the other: the Deferred said the spec half stood uncorrected while `non-spec-recheck-2.4.review-edit-sites.1`, reading `spec-changes.md:97-103` directly, reports the bullet already repaired to the windowed-counter wording and says not to re-apply it. The newer reading was kept, and the residual question is carried as an UNVERIFIED in Open so a fixer settles it before editing that bullet.
- One clause DELETED rather than retired: the standing Trap on the per-stage compensation table used to close with "the upload-free branch is listed separately only because it is the one stage failure that sends no RPC at all". `stageWorkspace` cannot fail on an upload-free plan at all, so the clause was false, it contradicted Settled #65, and the proposal had followed the Trap rather than the fact. The Trap now carries the correction and the filed finding.

Retired in compaction pass 7, both closed rather than dropped:

- The ground under the tier-7a co-tenanted variant, that it is the only thing exercising `SocketRuntimeProcess.Close`'s active-set early return. Closed: the shipped `TestSocketRuntimeProcessCloseScopedToSlot_spec_5_2` (socketruntime_test.go:252-300) exercises the sibling-active early return and the last close deterministically, and this proposal's tier-1 "Co-tenancy hazard" case covers the connected-with-empty-active-set state. The `gatedRuntime` fake holds no connection, child or listener, so the teardown effect is unobservable at tier 7a whichever fixture the case swaps in. The park-placement Open above SURVIVES, because it is about what the variant asserts rather than about which test owns the close.
- The pass-6 residual that the `budget/2` split's mechanism was traced by only one shard. Five independent lenses re-derived the whole chain this window — `Client.Shutdown`'s `time.Duration` fourth parameter, `drainViaLifecycle`, `contextWithGraceDeadline`'s parent-unchanged behaviour for a non-positive grace, `resolveShutdownGrace`'s preference for the ctx deadline, and both `Close` implementations returning nil on the kill arm — and each agreed. The Settled entries carry it; do not re-verify.

Two corrections applied in place rather than retired, kept here so the old forms are recognisable. The bind epoch is minted per admitted bind ATTEMPT, not per registry entry, and the per-entry form's conclusion ("neither the tree the retry staged nor the session it started can be taken down by the previous attempt's reclaim") was false in the ordering the mechanism exists for. The adapter-local property the proposal mints is the **slot-identifier reclaim hold**, windowed from deregistration to the end of the cleanup, not a second sense of "occupancy" starting at entry creation. SUPERSEDED IN PASS 8: the first of those two corrections was itself reversed by the r5→r6 fix pass, which restored per-entry minting and deleted the request-side echo. The per-attempt form is now the withdrawn alternative and is carried in Traps.

Retired in compaction pass 8:

- The OPEN "Can an adapter restart produce an epoch collision?". Closed, and the recorded ground was wrong: the agent podspec sets `RestartPolicy: Never` (pkg/controller/sandbox/podspec/podspec.go:980), so an adapter process cannot restart under a live pod. The no-re-dial rule does NOT carry the case, because `grpc.NewClient`'s `ClientConn` reconnects transparently. Two rounds reached the right conclusion from the wrong reason before the third caught it. CODE-6 additionally seeds the counter from `time.Now().UnixNano()`, which is stronger than the spec requires.
- The UNVERIFIED "How is a `Shutdown` carrying no epoch spelled on the wire?". Closed by the fix it produced: both SPEC-5 blocks now state that the epoch is a positive integer and that zero carries none, and §15.4 gained the missing non-conformance case for an adapter that compares a zero epoch or answers `superseded` for one. That clause is what keeps the §11.4 revoke fan-out, the occupancy-zero recycle edge and `Binder.ReleaseSlot` defined against a conforming third-party adapter.
- The OPEN "`AssignCredentialsResponse.bind_epoch = 1` is a hard field-number collision" and its sibling Trap. Both closed as REFUTED rather than resolved: the message is empty and the field number is correct. Eight lenses filed the correction and one recorded the `awk` artifact that produced the error. The correction is applied in Settled and the Trap now warns against acting on the old form.
- The OPEN "Nothing in the staged spec bounds the reclaim hold on an exclusive pool". Discharged by the r6→r8 rewrite: the hold paragraph now states its own three-way bound independent of §5.2's concurrency-scoped formula, and the edge-case bullet states the same three-way bound, so the two sites agree. The lens that opened it retired it.
- The OPEN "The epoch-reporting RPC set disagrees between the two staged blocks". Closed twice over: the r1→r2 fix replaced §4.7.1's wide predicate with the same closed seven-item list §15.4 carries, and the r6 revert then removed the request side entirely.
- The OPEN "The hold is stated with TWO different start points in the same document" and its later third site. Closed: §5.2, §6.2, §7.1 and §15.4 now agree word for word on both endpoints (the deregistration of the registry entry, to the cleanup finishing), and §6.2's own statement was replaced by a pointer so it cannot desynchronise again.
- The OPEN "Is `examples/runtimes/echo/` an adapter rather than a runtime binary?". Moot: `examples/` does not exist in the tree at all, so spec/15:1466 names a path with nothing behind it and there is no unstaged edit site. Pre-existing spec fiction, not 0081's.
- The OPEN "§4.7.1's caller rule may be wider than it needs to be for the two-step create path". Dissolved by the r6 revert, which deleted the request-side epoch the rule was about.
- The DEFERRED on the request side of the bind epoch (seven request fields, seven handlers, the client echo, a tier-3 echo case, and the field counts that would have become sixteen), and its §7.4 mid-session extension. Both dissolved by the r6 revert: no bind-sequence request carries an epoch, so there is nothing to wire, "nine fields" is correct again, and the mid-session upload needs no rule of its own. Recorded here rather than dropped, because the spec text those entries were written against was real for two rounds and the checklist still carries steps written for it.

Retired in compaction pass 9, all closed rather than dropped:

- The OPEN "Does the §7.1 compensation need an explicit exemption from the §10.1 generation fence?". Answered NO by the `f1.open-decisions.14` firing and deleted from the summary as open decision 14. A generation-stale rejection is a reclaim the adapter did not answer, which staged §7.1 already dispositions, and §10.1:66-68 tells such a replica to cancel and not retry, so an exemption would let a stale replica keep driving the pod. The unenforced fence is now a shipped-defect row.
- The OPEN "Does a `superseded` successor running on the predecessor's tree want a sentence?". Answered NO by the `f1.open-decisions.13` firing and deleted as open decision 13. The premise is false for `superseded`, whose definition requires the predecessor's entry to have been RELEASED, and a release runs the cleanup that removes the tree; the genuinely-inherited case is adoption of a surviving entry on the create-time-reserved path, which §5.2's policy does not place, and it is a shipped-defect row.
- The OPEN "Can an adapter restart produce an epoch collision?", re-retired with its ground restated once more because two rounds recorded the wrong one and a third caught it. The no-re-dial rule does NOT carry the case; `grpc.NewClient`'s `ClientConn` reconnects transparently. `RestartPolicy: Never` on the agent podspec is the ground, and CODE-6 additionally seeds the counter from `time.Now().UnixNano()`.
- The OPEN "The epoch-reporting RPC set disagrees between the two staged blocks", closed a second time. The r1→r2 fix gave §4.7.1 the same closed seven-item list §15.4 carries, and the same fix's exhaustiveness clause then wrongly excluded `Shutdown`; both blocks now carve `Shutdown` in. The predicate the later `spec.1` findings quote ("every RPC in the tables above that can create or resolve a slot registry entry") no longer exists anywhere in the proposal.
- The OPEN "How is a `Shutdown` carrying no epoch spelled on the wire?", and the §4.1 presence question with it. Both blocks now state that the epoch is a positive integer and that zero carries none, §15.4 gained the zero-comparison non-conformance case, and §4.1's epoch sentence was deleted outright because a bare proto3 scalar cannot engage a presence rule.
- The FILED "`ConfigureWorkspace` is documented idempotent while the admission rules refuse an epochless request onto a started entry". Dissolved by the r6 revert, which deleted the request-side epoch the admission rules were about. Two lenses had already refuted it under the r5 design.
- The two-start-points hold window, closed at its third site. §5.2, §6.2, §7.1 and §15.4 now agree word for word on both endpoints, and §6.2's own statement was replaced by a pointer so it cannot desynchronise again.
- The DEFERRED against the CODE-4 comment at non-spec-changes.md:558-560, which repeated the withdrawn "the state the failed attempt created is already gone" ground. It was in scope for the same fix that corrected the four spec sites and landed with them; its `return false` conclusion was correct throughout and stands.

Retired in compaction pass 10, all closed rather than dropped:

- The OPEN "Does the hold survive its own timeout?". Answered by the `f1.open-decisions.human-decisions.12` firing and deleted from the summary as open decision 12: the hold ends when the cleanup pass that took it RETURNS, whether that cleanup succeeded or failed. No staged text was edited, because the staging already states the terminal twice in §5.2, mirrors it in §15.4, and stages an unconditional `defer release()`. The two terminals that looked like they collided are different objects, and adding a `leaked` clause to the hold paragraph is barred by this log's own standing DECISION.
- The OPEN "Does CODE-4's `*SlotBindError` fold need a matching `isTransientPodClaimError` arm?". Answered YES and fixed: `non-spec.1.fix-G2.1` adds one arm keyed on the gRPC status code, owned by CODE-5, which also covers CODE-2's `Resume` rollback. The durable statement, including why the sentinel-matching alternative is unimplementable and where the test belongs, is in Settled.
- The OPEN "S1 lands spec text that cites a §4.7.1 statement S5 lands later". Dissolved by the resequence: SPEC-5 now leads at S1, so the §4.7 row's pointer resolves the moment the row lands at S2. The one citation the order cannot put first is SPEC-5's own pointer at §5.2's reclaim hold, which S1's line states so the residual is visible rather than silent.
- The OPEN "`compensateFailedSlotBind`'s signature on the resume path", which five lenses had recorded and none filed. Filed and fixed: the parameters narrow to scalars, matching the precedent CODE-5 had already set for `accountSlotFailure`. The rejected alternatives are in Settled so the synthetic-request form is not re-derived.
- The two spec-map OPENs, "`tests/spec-map.json`" and "The tier-7a case names no target file". Both filed as findings and both widened: the amendment added three more orphaned test surfaces beyond the tier-7a file the Opens named, and the registration is now attributed per creating step. The tier-7a case's filename is named in the staging; the map entry is what was missing.
- The UNVERIFIED "Is CODE-6's `Client.DemoteSDK` latch clear worth landing when no reachable trace exercises it?". Closed as landed: CODE-6 stages the clear on `DemoteSDK` and on a `RECLAIMED` reclaim, and a tier-1 case asserts `BindEpoch` answers zero after each. What survives is the narrower Trap that the clear does NOT fire on a plain `Shutdown`/`ShutdownRecycle`, which five lenses judged inert.
- Eleven Deferred entries applied by the three index-and-checklist reconciliation passes and the `non-spec.1.fix-*` groups: the two checklist entries on S9 and S10 (now S12 and S13, both rewritten), the summary's CODE-4 credential-lease line, the `TestValidTransitions_spec_6_2` "Watch out for" bullet, the `pkg/gen` path at all three sites, the CODE-6 `adapterclient` latch entry, the five-steps checklist entry (one repaired, four already true against the reverted staging), the open-decisions preamble, the design-narrative mirror entry, the CONF-1 and tier-3 entry, and the tier-3 case titled `Shutdown with the field unset`, which no longer survives in the file.
- Two Deferred entries discharged by a staged deliverable rather than by an edit: `docs/reference/adapter-contract.md:75` and its `:64` sibling. DOCS-2 now exists and folds into S6, after eleven lenses filed :75 independently across six loops. It was the single most-filed, least-acted item on the proposal.
- One Deferred dissolved: the summary's open-decision-9 framing entry. Entry 9 has left the section, and only the past-tense closure prose under `## Decisions` remains, which the entry itself states is correct.

Retired in compaction pass 11, all closed rather than dropped:

- The OPEN "Does §7.1's exclusive-pod clause need a mid-resume carve-out?". Answered NO by the `f1.open-decisions.human-decisions.15` firing and deleted from the summary as open decision 15. §7.1 already ends with the pre-attached disposition governing the pod and this reclaim governing the slot state, and §7.2 step 3 states no pod outcome by design, so no staged text changed. The pod's disposition is enumerated by §6.2's self-declared authoritative `resuming` subsection and disposed of in both pool configurations by shipped text; both facts are in Settled.
- The UNVERIFIED "Does §5.2's hold obligation reach the sixteen non-`Shutdown` release sites?". Retired as STALE in the proposal's favour: it rested on "CODE-6 takes the hold only inside `Shutdown`", and CODE-6's current text routes every site that deregisters an entry it then destroys through `reclaimSlotLocked`, of which there are three (`Shutdown`, `releaseSessionSlot`, and the §10.1.4 hold termination). The staged code implements the universal obligation rather than a `Shutdown`-only subset, so the spec-versus-code contradiction the item anticipated does not exist.
- The UNVERIFIED "Can the gateway placing a §5.2 retry actually see that a pod holds an incomplete reclaim?". Answered YES for the scope the staged sentence carries: §5.2 governs "the retries this policy places", and `applySlotRetryPolicy` places them inside one client request holding the `*SlotBindRequest` whose `ExcludePods` the same iteration appended after reading `sbe.Leaked`. The residual, that a SECOND client request cannot see the first one's incomplete reclaim, is the separate standing Open on the placement guarantee.

One item retired as MOOT rather than closed, kept so nobody re-derives it: the standing Open on `examples/runtimes/echo/` stays retired, and this window added the reason spec/15 is self-inconsistent about it as well as wrong about the tree — :1466 calls it a reference implementation of the ADAPTER while :1491 and :1812 describe it as a RUNTIME writing JSONL to stdout. Pre-existing spec fiction, not 0081's.


Retired in compaction pass 12, all closed rather than dropped:

- The Trap "MISTAKE, barred and NOT closed: SPEC-4's prose against the untouched `receiving_uploads ──→ running` trigger", and the UNVERIFIED beside the `running`-boundary DECISION. Both CLOSED by the fix that took a third route the trap did not consider: delete the start-handover sense of "acknowledge", restate the boundary in the trigger's own vocabulary, then delete the attribution clause as well, leaving the fence annotation and its docs mirror untouched. The durable statement is in Settled. The residual "dispatched versus acknowledged" question dies with it, because the staged prose no longer attributes its boundary to the trigger.
- The Trap "The `## Impacts on other proposals` section breaches its own one-row-per-proposal rule with four 0080 rows". Closed by the `f2.open-decisions.0080-consolidated` firing, which merged the four into one row carrying every assessment verbatim plus the §1.12 claim-map arithmetic the old rows lacked. The section now carries six rows.
- The Open "Does `ensureSlotPaths` widening its return break an unlisted test file?". Applied: the r2 fix added `pkg/adapter/exportpaths_test.go` and `pkg/adapter/one_session_only_test.go` to the `Tests:` bullet and one clause in CODE-6 saying their call sites are retargeted to the widened returns at S9. The exhaustive seven-file sweep is in Settled.
- The two Opens on an upload-free `PrepareWorkspace` reporting a zero epoch, merged into one and narrowed to third-party callers. Both gateway callers are now known to send at least one frame, so the case is unreachable from production.
- The Open "Is the epoch fence observable end to end?" in its S9 tier-3 dress, and the UNVERIFIED that read the observation route as blocked. CORRECTED: a server-side interceptor on the REAL adapter server records the inbound `ShutdownRequest` without introducing a fake, so "over the real gRPC transport rather than against a fake" is satisfiable at S9 even though the adapter neither compares nor answers on the epoch until S10.
- The DEFERRED asking whether SPEC-4's fence annotation should be reworded to the arrival moment, with `docs/reference/state-machines.md`'s mirrored row added to DOCS-1. Moot: that was the option-(b) remedy for a contradiction the third route closed without touching either file.
- One Settled claim SUPERSEDED rather than closed, kept so the old form is recognisable: "three refusals now travel out of `ensureSlotStateLocked`" was true only of the withdrawn r5 per-attempt design. Under the reverted per-entry epoch exactly one refusal travels out. The correction is applied in place.
- One Settled claim CORRECTED in place: `binder_test.go:533` is not the `cl.Resume` failure branch and carries no integer-division panic risk. A fixer acting on the old form edits the wrong test.

Retired in compaction pass 13, all closed rather than dropped:

- The Open "Does SPEC-2's §7.2 step-3 obligation reach any code deliverable or test?". Answered YES by `non-spec.7.review-test-coverage.1`, which traced all three orderings: a failing in-flight `Binder.Resume` is compensated by CODE-4 and pinned by the listed tier-1 "resume path" case; a succeeding one registers its binding at start.go:4053 and the ordinary release path sends the `Shutdown`; and the terminal handler holds no adapter connection to send one on, which is what staged step 3 concedes by saying the reclaim goes "on the connection that attempt still holds". §7.2 step 3 states no rule of its own; it points at §7.1. Two lenses had reached the question from opposite sides and neither had traced it.
- The Open "The claim-register surface symbol is spelled two ways". Applied by `non-spec.5.fix-G3.1`: SCHEMA-1's second row now names `Server.nextBindEpochLocked`, the name CODE-6 stages. The durable residue is the Trap that no tier-0 gate resolves a surface symbol, so a wrong-but-well-formed name passes silently.
- The two Traps recording the CODE-5 `slotBinder`-interface sentence as filed twice and then three times and still standing. Applied by `non-spec.5.fix-G2.1` after a fourth filing: the sentence now splits the pointer axis from the `leaked bool` axis, CODE-4's call-site table names both fakes, and `fakeSlotBinder.released` widens to carry the disposition. The durable statement is in Settled; the fourth-filing cost is recorded as a Trap of its own rather than dropped with the sentence.
- One Settled claim CORRECTED in place, kept here so the old form is recognisable: `claim_register_proto_agreement_test.go` does not force SCHEMA-1's row and the proto edit into one commit. Its `Message.field` parse is gated on the row's `surface` containing the proto path, and both staged rows name Go files. The same-commit requirement survives on the tier-3 descriptor gate's ground alone.
- One Settled claim CORRECTED in place: "extending a tier-4 file is free" was true of `validate-maps` and false of the credit gate, which checks a new case in an existing inventoried file.

Retired in compaction pass 14:

- The Open "Does a whole-file credit satisfy a case in a per-case-registered file?" and the Trap that recorded the two readings as uncorrected. CLOSED affirmative: `creditsMissing` (spec_map_slot_address_registration_test.go:950-963) consults `wholeFile` in BOTH branches, so a whole-file credit satisfies a case in a per-case-mode file. At least six shards across two lanes re-derived it and none reached the older reading. The durable statement is in Settled and the Trap now warns against re-opening it rather than against picking a side.
- The Open "`bindSessionForTest` has to be widened to return the epoch". Converted rather than dropped: it is a correction with a named remedy against a file the current lane may edit, so it belongs in Deferred, where the fix pass greps for it.
- The Trap "Do NOT read `pkg/sandbox/slotstate` as adapter-side", in the half that said the Redis-rehydration row's citation "does not support the sentence". FILED by `non-spec-recheck-3.2.review-citations.1` after five lenses had left it, and FIXED in the same window: the row keeps the adapter as its subject and cites `pkg/adapter/slotsession.go:398-407`. The gateway-versus-adapter fact survives in the Trap; the unfiled-citation half is gone.
- The Trap "`bash` output in this harness truncates at roughly 2KB". A duplicate of the later, fuller entry on reading this file with the Read tool; the two are merged into one.
- One Settled claim CORRECTED in place, kept here so the old form is recognisable: "a create-time-reserved row takes `BindReservedSlot` with no retry loop" was stated unqualified. `bindConcurrentSlot`'s guard is `row.PodAssignment != "" && !session.IsRecovery(row.State)`, so a RECOVERY-state row carrying a stale `PodAssignment` reaches `applySlotRetryPolicy`. Five rounds copied the unqualified form and the cited span `start.go:2594-2604` straddles the guard and the fallthrough.
- One Settled claim CORRECTED in place: the credit-inventory gate has THREE derived rules, not two, and an extra inventory entry is never a gate failure. The third rule is a file under `tests/tier11_docs/` whose body mentions `TEST-GAPS.md`.
- One mid-window shard SUPERSEDED rather than retired: `non-spec-recheck-2.1.review-mechanism.1` read `slotSubjectFileRE` as matching the whole repo-relative path. Four later shards read the `[^/]*` and reported the basename. The basename reading governs.

Retired in compaction pass 15:

- The Open "Does the 0080 impacts row owe an assessment of §1.14?". CLOSED: 0080 assigns §1.14 to proposal 0076 (proposals/0080_...md:196) and its subject is the §10.1 coordinator hold rather than this proposal's reclaim hold, so the omission is correct. The `f7` firing confirmed the reading one shard had already reached.
- The Open "The Decisions bullet names only two adapter files". SUPERSEDED: the bullet now names nine, and the live disagreement is nine (the bullet) against eleven (the 0080 row and `## Files touched`). The nine-versus-eleven Open carries it, rewritten so it no longer cites the retired form.
- The DEFERRED against `spec-changes.md`'s three sites carrying the refuted two-ground argument about what can meet the reclaim hold. RETIRED because the two records disagreed and neither corrected the other: the Deferred said the three spec-lane sites were unapplied, while `[non-spec.2.fix-reclaim-hold-callers]` records that it edited `spec-changes.md` at all three and states what each now says. The newer record is kept and the durable statement is in Settled. UNVERIFIED: whoever next opens `spec-changes.md` should confirm the three sites carry the scoped form before re-applying the repair, because re-applying it would scope a sentence that is already scoped.
- Two changelogs DELETED rather than retired, to hold this section's growth: compaction passes 12 and 13. Every correction either recorded is already applied in Settled, Traps, Open or Deferred, which is what those sections are for; a changelog's only remaining value after its corrections have landed is telling a later pass how the section grew, and passes 14 and 15 carry that.

Retired in compaction pass 16. The 2026-09-16 hand revision withdrew two whole mechanisms, so this
pass retires the entries that stated either of them as live. Every one is SUPERSEDED rather than
refuted, and the tree facts each carried survive in the entries beside it.

- The bind-epoch design, in all its forms. DELETED outright, because each would have told a later
  agent to act on a mechanism that no longer exists: the DECISION that the epoch is minted per
  registry entry; the DECISION that the epoch rules are stated once in §4.7.1 and published once in
  §15.4; the DECISION that deleted §4.1's epoch sentence; the two exhaustiveness-carve-in
  DECISIONS; the "epochless" wire definition; the escape-hatch entry phrased on an epochless
  `Shutdown`; the no-re-dial rule's corrected reason; and the lazy-seed anti-reuse argument. The
  REMAINING epoch-era entries are left in place and are governed by one rule, stated in the
  changelog and in the Settled "DESIGN GENERATION" entry: their TREE facts survive and their DESIGN
  claims do not. A later pass with a working line-editor should sweep the rest; this one could not.
  The reclaim hold SURVIVES unchanged and is the one half of the old pair that did not move.
- The `ExcludePod(s)` placement mechanism. DELETED: the DECISION that the no-retry clause is
  replaced by a placement rule, and its round-2 successor. `spec.1.fix-G3.1` filed the CORRECTS
  against both by name. The remaining `ExcludePods` entries (the `ClaimSlot` sentinel and
  one-`continue` entries, the pointer threading, the `req` helper inventory, the accounting-versus-
  placement split, the `slotretry_load_test.go` goroutine entry, the new-identifier sweep) are left
  in place under the same reading rule. What governs a retry meeting the reclaiming pod is now the
  attempt token or the reclaim hold, and both refusals are transient.
- The fourteen-step checklist. DELETED: the resequence-and-renumber entry. The sequence is
  twenty-two steps and the live step identities are in Settled; any other entry naming S1 through
  S14 with epoch-era contents is reading the superseded sequence.
- One Open CLOSED: "Is a third-party adapter that omits the start-side epoch confirmation
  conforming?" Answered no and published. `spec.6.fix-G2.1` landed the obligation as one §4.7.1
  paragraph plus §15.4 clause (j), stated universally rather than scoped to the starts the gateway
  compensates, because the scoping condition is invisible to the third-party adapter the clause is
  written for.
- Two Settled claims CORRECTED rather than deleted, kept here so the old forms are recognisable.
  The `## Spec files touched` list is no longer "six files, thirteen edit sites": the §7.1 atomicity
  parenthetical and the §5.2 `**Max retries:**` sentence are no longer staged and §16.1 joined, so
  the site count must be re-derived from the staged blocks. And `spec/16_observability.md` DOES take
  an edit now, so the entry saying it needs none holds only in its alert half: there is still no
  §16.5 alert or recording rule keyed on any `lenny_slot_*` series, so nothing orphans and no
  runbook obligation is created.
- Two Traps SUPERSEDED but deliberately KEPT, with this note as their correction: the two "dead
  end: `ConfigureWorkspace` is documented idempotent while the admission rules refuse an epochless
  request onto a started entry" entries. Both refutations rest on the retired epoch design. Under
  the token the started-session rule is unconditional and `ConfigureWorkspace` carries no token, so
  the idempotency conflict was LIVE and is now closed by an explicit exemption in §4.7.1 and §15.4.
  Do not re-file it as a dead end and do not delete the exemption.
- The withdrawn-alternative Traps for the per-attempt and per-entry epoch forms are kept rather than
  retired, because the token design is close enough to the per-attempt form that a later round
  re-deriving either needs to see why each was withdrawn and why the token replaced both.

Retired in compaction pass 17. This pass ran the deletion pass 16 recorded that it could not run,
and it retired the Open and UNVERIFIED items whose subject the 2026-09-16 revision deleted. Every
one below is SUPERSEDED rather than refuted, and the tree facts each carried survive in the entries
beside it.

- Twenty-one Settled entries whose subject was the bind epoch's DESIGN, deleted outright because
  each would have told a later agent to act on a mechanism no staged deliverable names: the
  epoch-and-hold two-mechanism DECISION; the banked request-field numbers for the withdrawn echo;
  the §4.1-obligation and §25-stem entries; the "exactly one refusal travels out of
  `ensureSlotStateLocked`" entry (written against the reverted per-entry form, and false under the
  token, where the function does compare); the two nine-number epoch field tables, superseded by the
  token table in Settled; the per-entry one-ordering fence entry; the latched-epoch session-end
  `Shutdown` entry; the `DemoteSDK` epoch-drop DECISION; the §4.1-sentence cascade; the §11.4
  fan-out-under-the-epoch entry; the `Server.bindEpoch`/`slotState.epoch` field-placement entry; the
  `claimSessionSlot` epoch-return entry; the `resolvePrepareStagingDir` epoch-widening entry; the
  `Server.nextBindEpochLocked` claim-register DECISION; the `idempotentRepeat`-cannot-report-an-epoch
  entry; and the eight-response-construction-site and five-`Shutdown`-caller-with-a-zero-epoch
  entries, whose tree halves survive at the `DialAdapter` and `Shutdown`-caller entries. Also deleted:
  the "Open decision 19 was minted and then DELETED" entry, superseded by the one-decision Settled
  entry, and the "`## Spec files touched` … six files, thirteen edit sites" entry, whose count pass 16
  had already corrected.
- Twenty-six Open and UNVERIFIED items, each naming a mechanism no staged deliverable now has. On
  the epoch: the third-party start-side-confirmation question (closed and published as §4.7.1 prose
  plus CONF-1 clause (j)); what a no-epoch `Shutdown` reports; the caller rule's per-connection
  versus per-session granularity; the zero-epoch `PrepareWorkspace` response and its
  `cmd/lenny-compliance` sibling; the multiplexed-connection epoch question; the
  `claimSessionSlot` epoch-return ownership; `StartSessionResponse.bind_epoch`'s missing write site,
  in both its FILED and its bookkeeping dress; the epoch-latch clear in CODE-6; the epoch's unwritten
  trust argument; `ConfigureWorkspaceResponse.refusal_reason` against the epoch-report clause; and
  the two epoch-named test surfaces (`tests/tier3_contract/adapter_bind_epoch/`,
  `bind_epoch_conformance_test.go`), whose live half is now the standing Open on the rename sweep and
  the one on a `slot`-named basename. On the withdrawn pod exclusion: excluded-pod exhaustion naming
  the wrong cause; the create-time-reserved retry's unbounded exposure (also open decision 17, since
  answered by the token); the placement guarantee not reaching a second client request; the
  client-level retry reaching the reclaiming pod; and the tier-4 case written as a second
  self-issued `BindSlot` with `ExcludePods` set. Closed by the `f1` firing: §7.1's trigger
  noun-phrase (entry 11), the exclusive-pool budget clamp (entry 16), and the 11/16/17/18 numbering
  item, all superseded by the one-decision Settled entry.
- One Open RESOLVED rather than retired, its answer lifted to Settled: whether §7.1's exclusive-pod
  disposition survives §7.2 step 3 and the §6.2 cancel clause. A pod serving one session has two
  exits and SPEC-4's §6.2 paragraph now states both.
- Four Open items DELETED AND RE-ADDED in token vocabulary rather than dropped, so their live half
  survives: the end-to-end observability of the refusal through `slotResolveError`; §7.4's
  mid-session upload and the CONF-1 case it still owes; the retry refused until an unanswered
  reclaim clears; and SPEC-3's exclusive-pod arm over-reaching its antecedent, which no longer cites
  the closed open decision 11.
- Four Deferred entries retired as APPLIED, on `spec.7.review-reliability.1`'s grep evidence that
  `spec-changes.md` no longer contains "different pod", "WARM_POOL_EXHAUSTED", "placed by neither",
  "neither mechanism" or "whatever state the failed stage": the `:109-111` edge-case pronoun, the
  Design section's "placed by neither" antecedent, the "whatever state the failed stage left it"
  clause, and the edge-case bullet carrying the withdrawn placement conclusion.
- One changelog DELETED rather than retired, to hold this section's growth: compaction pass 16's.
  Its corrections are applied in Settled, Traps, Open and Deferred, and its reading rule for
  surviving epoch-era entries is restated in pass 17's changelog.

Retired in compaction pass 19. The caller's instruction was to retire every Open and UNVERIFIED
entry whose subject the 2026-09-16 revision deleted. None remained: passes 17 and 18 retired the
epoch trust argument, the epoch latch, the `bind_epoch` response fields, the `claimSessionSlot`
epoch return, `tests/tier3_contract/adapter_bind_epoch/`, `bind_epoch_conformance_test.go` and the
excluded-pod items already, and `grep -ci epoch` on `spec-changes.md` and `grep -c ExcludePods` on
every staged file both return 0. What this pass retired instead is twelve Open items the rounds it
read ANSWERED, each closed rather than dropped:

- "'Does not acknowledge' versus an unclean answer". ANSWERED by the r9→r10 fix: §7.1's predicate
  now reads "one whose answer does not report a clean exit, whatever outcome that answer carries",
  and §4.7.1's reclaim-outcome rule ties `absent` and `superseded` to a clean exit. The answer is in
  Settled as the resolved open decision 23.
- "Does any gateway path patch `SandboxClaim.status.phase` to `failed`?". ANSWERED NO: the
  pre-attached failure disposition is a bare claim DELETE through `failPhase` → `drain` →
  `podclaim.DeleteClaim`, so any reasoning resting on a terminal disposition at that point is wrong.
- "What does a third-party adapter answering `SLOT_RECLAIM_OUTCOME_UNSPECIFIED` mean?". The
  mechanism it rested on is gone: CODE-4 no longer branches on the outcome value, so there is no
  `default: return false` arm and no unrecognized-outcome counter. The zero value's meaning is
  unstated and now inert.
- "Does `superseded` fix a value for `exited_cleanly`?". ANSWERED by SPEC-5's reclaim-outcome rule,
  which states it for both non-removing outcomes.
- "Two mentions of other proposals sit outside `## Impacts on other proposals`". ANSWERED by the
  `f1.post-spec-loop` firings, which wrote 0072 and 0079 rows; the section now carries one row per
  proposal or programme step.
- "Does `SLOT_BIND_ALREADY_STARTED` on `AssignCredentials` render as a credential-catalog code?".
  ANSWERED NO: on the concurrent path it is a `SlotFailedError` at the credential-assignment stage
  and on the base path a bare wrap, so no `CREDENTIAL_*` §15.1 row is falsified.
- "Does the §4.7.9 credential-rotation flow need naming in §7.1's obligation?". ANSWERED NO:
  rotation goes through `RotateCredentials`, and the adapter `AssignCredentials` RPC has exactly two
  non-test callers, both bind-path.
- "SCHEMA-1 says 'ten fields' and its table lists nine". APPLIED by the reconciliation pass, which
  changed "ten fields" to "nine fields" at four sites.
- "§16.1.1's 'Used on' cell for `k8s_pod_name`". ANSWERED: §16.1.1's normative rule is table
  MEMBERSHIP, both labels are members, the column is descriptive and no gate parses it. Three lenses
  paid for this call.
- "SPEC-6's adapter-row wording versus the shipped deferral sentence". ANSWERED: no tier-11 gate
  matches on the §16.9 anchor or on any deferral phrasing, so the shorter wording is style.
- "Is the one-session 'return-to-pool exit' reachable for this residue class?". ANSWERED YES: it is
  reached by the §7.2 step-3 path, where the half-claimed replacement pod is released back to the
  pool and the §7.1 reclaim may not complete. Do not simplify SPEC-4's paragraph to one exit.
- "Does the §10.1 hold-timeout path open a reclaim hold that never closes?". ANSWERED NO, on a false
  premise: `terminateHeldSession`'s pass 2 is not `emitFinalUsage` plus `Runtime.Close` alone, it
  calls `removeSlotTree` immediately after the close, so a reclaiming cleanup does run and the
  hold-release condition is satisfiable there.

One Settled claim CORRECTED in place rather than retired, kept here so the old form is recognisable:
SPEC-6 stages TWO §16.1 rows, not three. `lenny_slot_compensation_outcome_unrecognized_total` is
gone repo-wide, and at least four lenses spent a grep each on the stale "three rows" entry.

Retired in compaction pass 20. The caller's instruction was again to retire every Open and
UNVERIFIED entry whose subject the 2026-09-16 revision deleted, and again none remained: passes 17
through 19 retired every epoch- and exclusion-named item, and `grep -ci epoch` on `spec-changes.md`
and `grep -c ExcludePods` on every staged file both still return 0. What this pass retired instead
is four Open items the non-spec window ANSWERED and five Deferred entries it APPLIED, each closed
rather than dropped:

- The OPEN "Is CODE-9's `alert_catalog_crosscheck_test.go` entry needed at all when no alert is
  added?" ANSWERED NO. There is no such file under `tests/tier11_docs/`; the only file of that
  basename is `pkg/observability/metrics/alert_catalog_crosscheck_test.go`, a unit-tier package
  test that derives every set it compares at run time and reads no documentation page, so a
  counter with no alert referencing it owes it nothing. The citation must be DELETED rather than
  repointed, which is the durable half and is in Settled.
- The UNVERIFIED "Does SPEC-1's §15.1 widening leave the row's endpoint enumeration complete?"
  ANSWERED YES. `RunSetup` has exactly two gateway call sites, `materializeSlot` (the concurrent
  `/{id}/start` path) and `Binder.Prepare` (reached from `/finalize`, `/v1/sessions/start` and
  `/{id}/resume`), so the four endpoints the row names are exactly the reachable set and the
  started-session refusal cannot surface at a fifth.
- The OPEN "Is the `ABORTED` hold refusal owed an adapter `ErrorCode` of its own?" ANSWERED NO. The
  gateway branches on the gRPC status rather than on `Error.code` for it: CODE-7's double-`%w` keeps
  `status.Code` walking to the original status and CODE-5 adds one `codes.Aborted` arm to
  `isTransientPodClaimError`, so a bare status classifies correctly.
- The UNVERIFIED "Does the SIGTERM `ShutdownDemoteSDK` path race `sdkwarm.go:261`?" retired as
  STALE. It rested on CODE-2 making that site a bare `_ = s.noteRuntimeStarted(...)` with no
  rollback. CODE-2 now gives it a real refusal arm that calls `SDKWarmRuntime.DemoteSDK`, clears
  `s.sdkConnected`, removes no entry and no tree and answers `codes.Aborted`, with a deterministic
  tier-1 case in `pkg/adapter/sdkwarm_test.go`.
- The DEFERRED obliging DOCS-1 to take the three projection-clause replacements at
  `docs/reference/state-machines.md:138`, and its S7/summary sibling. APPLIED: DOCS-1 now carries
  "**The pod state machine paragraph.**" with all three replacements and the widened file-list line,
  and `summary.md`'s index line follows. Re-applying it would double the edit. Only the checklist
  half is still open and is re-stated as its own Deferred.
- The two DEFERRED entries against DOCS-2's bind-attempt block (the `ConfigureWorkspace`
  idempotency self-reference and the third-party-adapter-author audience). APPLIED by the hand
  re-stage, which rewrote the rationale to "the protocol between the adapter sidecar and the runtime
  binary" and moved the admission rules' published home to §15.4. Do not re-file them.
- The DEFERRED naming `pkg/gateway/observability/catalog.go` as the counter-registration site.
  APPLIED: CODE-9's body now splits the two counters by emitter, putting the gateway series in
  `pkg/observability/metrics/catalog.go` plus `spec161Metrics` and the adapter series in
  `pkg/adapter/metrics.go` alone. The durable statement is the metric-gate topology entry in
  Settled.
- The checklist DEFERRED entries against S1, S3, S6, S7's DOCS-3 half, S13, S17, S18, S19 and S20.
  APPLIED, S1/S3/S6/S7/S13/S17/S20 by the reconciliation pass and S18/S19 by
  `non-spec.11.fix-G4.1`. The S19 clause in particular ("`ReleaseSlotReservation` carries no such
  parameter") was false on both halves and would have produced code that does not compile at S20;
  it took three recordings before a lane whose lease covered the checklist landed it. What survives
  is the S15, S7-DOCS-1, S22 and S10 set, restated in `### Deferred` against the current text.

One Trap DELETED rather than retired, because a warning about a trap that no longer exists costs
every future agent a detour: "Do NOT file CODE-1's epoch early-return skipping clause three". Its
ground was the retired per-entry-epoch design. Under the token the staged `shutdownReclaimOutcome`
switch tests entry ABSENCE before the unconditional flag, so the concurrent `ShutdownRecycle` — which
always names a session the adapter holds no entry for — took the non-removing early return and
dropped the whole-pod scrub on a production path. The finding was confirmed and fixed by the single
`answerShutdown` exit. A Trap in its place records why the bar is dead.

One Settled claim CORRECTED in place, kept here so the old form is recognisable: "the occupancy-zero
recycle `Shutdown` ... always names a session the adapter holds no entry for" is true of the
CONCURRENT sender only. `Binder.Release`'s recycle branch sends the recycle-carrying request as the
session's only teardown, so the entry is live and the outcome is `reclaimed` on the removing arm.

Retired in compaction pass 21, each closed or superseded rather than dropped. One UNVERIFIED was
retired on the caller's instruction: the clause riding inside the Settled `ClaimSlot` pass-1 entry,
which asked whether a leaked slot's held occupancy makes the pod pass 1's own candidate and so
contradicts the §7.3 idle-inventory entry. Its subject is the placement exclusion, and no staged
deliverable now names that mechanism, so the question cannot be settled and cannot matter. The
Settled entry keeps its tree facts and its "do not generalise the idle-inventory argument to the
bind path" warning.

Three epoch-named Deferred entries were retired, each because a token-era sibling carrying the same
correction stands in `### Deferred`. The `## Files touched on application (non-spec)` entry against
"two of the seven epoch-reporting handlers", whose whole subject is the `bind_epoch` response fields
on `PrepareWorkspaceResponse` and `StartSessionResponse`: no staged deliverable names those fields.
The `non-spec-changes.md:1067-1070` entry against CODE-6's `ConfigureWorkspace` idempotent-repeat
arm, worded as a second read of the entry's EPOCH; the later entry against the same arm states the
same correction without the epoch ("have `claimSessionSlotUnderLock` return what the caller needs on
its `idempotentRepeat` early-return arm") and is the one to apply. The `SCHEMA-1's quoted gate
failure text` entry, whose quotation is the epoch-era `expected_bind_epoch` field name; the later
SCHEMA-1 entry carries both halves, the missing `= ` and the withdrawn field name, and supersedes it.

Three further Deferred entries were retired as duplicates their own successors carry whole: the
`spec-changes.md` Design-section pointer list omitting the SPEC-2 §7.2 edits, which the later
four-correction `spec-changes.md` entry carries; the first of the two identical CODE-4 `drain`
doc-comment entries, the survivor of which records itself as the fourth recording; and the second of
the two identical CODE-1 credential-file entries, the survivor of which carries the EVIDENCE lines.
The aggregate "outside every proposal file, five corrections" entry was retired for the same reason:
every one of its five (the proto comments, `Client.Shutdown`'s doc comment, spec/06:156 with its
docs mirror, §5.2's action list against `RemoveTree`'s four trees, and the release-by-session-id
class fix) stands as its own entry above it.

## Ledger

### [non-spec.14.fix-G1.1]

DECISION: closed the "typed refusal means this attempt minted none" falsehood by extending CODE-4's attempt-scoped release to `Binder.Prepare`'s credential-assignment failure arm, rather than recording the Prepare-path leak as accepted — BECAUSE both `assignSlotCredentials` (slotbinder.go:379, :393, RPC :403) and `assignCredentials` (binder.go:1225, :1248, RPC :1256) mint every provider's lease before the RPC CODE-6 can refuse, so one rule now holds at both bind entry points and the proposal no longer asserts opposite things about the same stage ordering in CODE-4 and CODE-8 — ALTERNATIVES: text-only "state the leak truthfully and accept it" (rejected: the CODE-8 test bullet is an assertion, so it would have pinned the leak as required behaviour and failed a correct implementation); release only on the refusal arm (rejected as an exception clause, the ordinary-failure arm reaches the same point holding the same leases); session-wide `ReleaseSession` on that arm (rejected: it is the defect CODE-4's narrowing exists to avoid, `LeasesBySession` at credassign.go:400-408 strips a successor's or a live session's leases).

FACT: `credassign.Service.Release(leaseID)` is a no-op for an unknown lease id (credassign.go:375-383), so the second call site cannot double-release. EVIDENCE: pkg/gateway/credentials/credassign/credassign.go:380

WATCHOUT: `fakeAssigner.released` records `ReleaseSession` calls alone, so "the release list stays empty" is true on every path and discriminates nothing. Two Testing bullets asserted exactly that; both now require a separate `Release(leaseID)` recorder field. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder_test.go:301, :322-324

WATCHOUT: `leaseAssigned` being false at binder.go:949 proves only that `failPhase` will not run the session-wide release, never that nothing was minted. CODE-8 read it as the latter ("the question does not arise there") and that inference is now deleted. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:949, :954, :1225, :1256

USEFUL [review-log-archive:41257]: the standing WATCHOUT that `CredentialAssigner` (binder.go:319-333) declares only `AssignProto` and `ReleaseSession`, so `releaseAttemptCredentials` needs the interface to gain `Release(leaseID string)`, and that an earlier round deliberately declined to file it. I did not re-derive or re-file it; the `fakeAssigner` recorder note I added to the Testing bullets is consistent with that interface change and adds no new obligation.

DEFERRED [implementation-checklist.md]: S19 states CODE-4's credential work as the slot path's alone ("releases the lease identifiers this attempt's own `assignCredentials` minted"), with no mention of `Binder.Prepare`'s credential-assignment failure arm, which non-spec-changes.md now stages under CODE-4's rule and names in CODE-8's rationale and in the file-touch list's `binder.go` bullet. What is true instead: the attempt-scoped release has two call sites, `materializeSlot`'s wrapper and `Binder.Prepare`'s credential-assignment failure arm, both unconditional and outside the refusal guard. The reconciliation pass owns this line.

FACT: summary.md:189-195 states the narrowing and the unconditional placement without asserting a refusal no-op, and it already names `assignCredentials` rather than `assignSlotCredentials`, so it stays true under this change and needed no edit. EVIDENCE: 0081_....summary.md:189-195

### [non-spec.14.fix-G2.1]

DECISION: closed the reclaim-hold contradiction by DELETING the staged §5.2 sentence "On a pod serving concurrent sessions a cleanup that does not finish within the timeout the **Slot cleanup:** bullet states is a failed cleanup under that bullet, so the slot is `leaked` and its identifier stays held until the pod terminates.", keeping CODE-1's and CODE-6's unconditional `defer release()` exactly as staged, and appending one rationale sentence to SPEC-3's explanatory prose recording why the paragraph states no failed-cleanup terminal — BECAUSE the deleted sentence's only unique content was identifier-hold persistence, which keys on a per-slot cleanup timeout no adapter implements, conflates the adapter-local identifier hold with the pod's Redis `leaked` occupancy, and buys nothing once the cleanup pass has returned and nothing is being deleted — ALTERNATIVES: the reviewer's preferred conditional `release()` on `closeErr`/`treeErr` (rejected: it needs an unimplemented per-slot timeout, creates a permanent identifier poison with no counter and no report in a paragraph that says refusal is the hold's only record, and falsifies four parallel sites plus two settled review-log DECISIONs); rewriting the sentence to say the hold ends when the cleanup returns (rejected: the paragraph already says "The hold lasts as long as the cleanup it covers" one sentence earlier); moving the terminal into §5.2's **Slot cleanup:** bullet (rejected: that bullet is scoped to `maxConcurrentSessions > 1` and carries no hold concept).

FACT: the per-slot cleanup timeout `max(cleanupTimeoutSeconds / maxConcurrentSessions, 5)` that spec/05 states is read nowhere under `pkg/adapter`; the only non-test `CleanupTimeout` read parameterizes the whole-pod scrub. EVIDENCE: spec/05_runtime-registry-and-pool-model.md:545; pkg/adapter/podscrub.go:128; pkg/adapter/scrub/scrub.go:306.

WATCHOUT: this location has now been rewritten three times, and each earlier round replaced a failed claim with a narrower restatement of the same claim rather than removing it. A later round that re-derives a failed-cleanup terminal for the hold paragraph is re-opening a settled deletion; the disposition of a cleanup that does not complete is owned by the scrub-model paragraph directly above the hold block, read by §7.1 off the response's clean-exit answer. EVIDENCE: 0081_....spec-changes.md:645 (the hold block) and the scrub-model paragraph immediately preceding it.

USEFUL [the review-log DECISION that `leaked` occupancy and the identifier hold are different objects]: it is the reason the deletion is correct rather than a loss. The entry stays true under this fix and is falsified only by the rejected conditional-release answer; the cascade count is what discriminated the two answers.

### [non-spec.14.fix-design-G1.1]

FACT: Both gateway credential-assignment helpers mint EVERY provider's lease into a local map BEFORE issuing the adapter RPC, and each has exactly one caller. `assignSlotCredentials` mints at slotbinder.go:379 (pool) and :393 (user) then returns `cl.AssignCredentials(...)` at :403; caller slotbinder.go:307. `assignCredentials` mints at binder.go:1226 and :1252 then returns `cl.AssignCredentials(...)` at :1256; caller binder.go:949. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:370-404, binder.go:1216-1257

FACT: On the base bind path a failure at `assignCredentials` releases NOTHING today. `reclaim()` calls `failPhase(ctx, sb, leaseAssigned, ...)` and `leaseAssigned` is set true only at binder.go:954, AFTER the call site at :949-952. `failPhase` gates `releaseCredentials` on that flag. So a partial mint, a failed `AssignCredentials` RPC, or (once this proposal lands) a typed refusal at that stage leaks every lease the attempt minted, with no reclaimer on the bind path. This is a shipped defect, not one this proposal introduces; the proposal widens its reachable causes by one. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:865-869,:949-954,:1072-1082

FACT: `credassign.Service.Release(leaseID)` is an explicit no-op for an unknown lease id, so an attempt-scoped release followed later by a session-wide `ReleaseSession` double-releases nothing. EVIDENCE: pkg/gateway/credentials/credassign/credassign.go:375-384

DECISION: CODE-4's attempt-scoped release is extended to `Binder.Prepare`'s credential-assignment failure arm rather than CODE-8's rationale being rewritten to record the leak as accepted — BECAUSE the two paths are the same rule at two sites, `releaseAttemptCredentials` already exists as a `*Binder` method in CODE-4, and correcting CODE-4's comment without it leaves one proposal asserting two opposite things about the same stage ordering; the alternative also makes the staged test bullet at non-spec-changes.md:2804 ("assert that no lease is released on either refusal arm") forbid the correct implementation. ALTERNATIVES: (a) text-only correction plus an accepted-failure-mode entry for the Prepare leak — rejected, it pins a leak by assertion; (b) releasing only on the refusal arm — rejected as an exception clause, the ordinary arm leaks identically; (c) calling the session-wide `releaseCredentials` on that arm — rejected, it strips a successor's and a live session's leases, which is the very defect CODE-4's narrowing exists to avoid.

WATCHOUT: `fakeAssigner.released` records ONLY `ReleaseSession` calls (binder_test.go:301,:322-324). Every staged test bullet phrased as "the release list stays empty" therefore holds on every path and discriminates nothing about the attempt-scoped release, which goes through `Release(leaseID)`. Two staged bullets are written that way (non-spec-changes.md:2764-2765, :2804-2805). Any test of the attempt-scoped release needs a second recorder on the fake. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder_test.go:296-324

WATCHOUT: CODE-8's rationale at non-spec-changes.md:1834-1840 is HALF right. The `Binder.Launch` half is correct and must survive: Launch mints no lease of its own, passes the literal `true` to `failPhase`, and running the session-wide release on its refusal arm would strip a LIVE session's leases. Only the `Binder.Prepare` half ("the question does not arise there") is false. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:996-1000,:1072-1082

MISTAKE: CODE-4's staged comment contradicts itself three sentences apart. non-spec-changes.md:992-994 states "a failed credential-assignment stage can already hold minted leases" as the reason the call sits outside the `errors.As` guard; :995-996 then states "On a typed refusal this attempt minted none, so the call is a no-op". Both were written into one comment block and neither round caught the contradiction.

UNVERIFIED: `releaseAttemptCredentials` is named in CODE-4's targets and file-touch lists (non-spec-changes.md:1013, :3552) and called at :997, but no staged text gives its body, and `podsession.CredentialAssigner` (binder.go:319-332) carries `AssignProto` and `ReleaseSession` only, with no `Release(leaseID string)`. `credassign.Service` has `Release` (credassign.go:380) and the credassign interface declares it (credassign.go:53), so the extension is available, but the proposal does not stage it. A later round should check whether CODE-4 needs to stage the `CredentialAssigner` extension and the helper body, and whether `fakeAssigner` needs the method.

DEFERRED [implementation-checklist.md]: S19 enumerates CODE-4's credential work as the slot path's alone ("It releases the lease identifiers this attempt's own `assignCredentials` minted..."). If the design in this shard lands, S19 must also state that `Binder.Prepare`'s credential-assignment failure arm releases its own minted leases by identifier, unconditionally, outside the refusal guard, and that `failPhase`'s `leaseAssigned`-gated session-wide release is untouched. The non-spec fix lane does not edit the checklist; reconciliation closes this.

### [non-spec.14.fix-design-G2.1]

DECISION: Close the reclaim-hold/failed-cleanup contradiction by DELETING one sentence from the staged §5.2 hold paragraph (spec-changes.md:645, "On a pod serving concurrent sessions a cleanup that does not finish within the timeout the **Slot cleanup:** bullet states is a failed cleanup under that bullet, so the slot is `leaked` and its identifier stays held until the pod terminates."), leaving the paragraph's existing bound ("The hold lasts as long as the cleanup it covers") as the single statement, and adding ONE rationale sentence to SPEC-3's explanatory prose recording why the paragraph states no failed-cleanup terminal. No code, test, checklist or CONF-1 change. — BECAUSE the deleted sentence is the only text in the proposal that ties the adapter-local identifier hold to the `leaked` disposition, it keys on a per-slot cleanup timeout no adapter implements, and its consequent contradicts both the staged `defer release()` and the log's own settled resolution of open decision 12. — ALTERNATIVES: (a) the reviewer's preferred conditional release (skip `release()` when closeErr/treeErr non-nil) — rejected: it needs the per-slot timeout of spec/05:545 actually implemented (nothing under pkg/adapter reads it), creates a permanent identifier poison with no counter and no report (the paragraph itself says "A refusal is the only record the adapter makes of the hold"), and falsifies four parallel sites plus two standing DECISIONs; (b) rewrite the sentence to "the hold ends when the reclaiming cleanup returns, success or failure" — rejected as hair: the paragraph already says exactly that one sentence earlier, and restating the bound is what the round that produced this sentence already deleted twice; (c) move the terminal into the `**Slot cleanup:**` bullet — rejected: that bullet is scoped to `maxConcurrentSessions > 1` and has no hold concept.

FACT: pkg/adapter implements NO per-slot cleanup timeout. spec/05:545's `max(cleanupTimeoutSeconds / maxConcurrentSessions, 5)` is read nowhere; the only non-test `CleanupTimeout` read under pkg/adapter is the whole-pod scrub. — EVIDENCE: pkg/adapter/podscrub.go:128; pkg/adapter/scrub/scrub.go:306-308; spec/05_runtime-registry-and-pool-model.md:545

FACT: the failed cleanup IS already observed without the hold. CODE-1 computes `exited_cleanly = closeErr == nil && (live || treeErr == nil)`, §7.1's reclaim reads that answer, and the staged scrub-model paragraph independently states the `leaked` disposition for a reclaiming Shutdown whose cleanup does not complete. Deleting the hold-paragraph sentence loses no disposition. — EVIDENCE: non-spec-changes.md:~435 (`answerShutdown(outcome, closeErr == nil && (live || treeErr == nil), false)`); spec-changes.md:~643 (scrub-model paragraph's "does not report a clean exit ... the slot is then `leaked`")

FACT: the staged §15.4 wire mirror (spec-changes.md:900) and summary.md:932 both already state the hold as ending "until the cleanup ... has finished" with no timeout or `leaked` clause. Only spec-changes.md:645 carries the offending sentence; `grep -n "stays held\|held until the pod"` across the staged files returns that one line. — EVIDENCE: spec-changes.md:900; summary.md:932

MISTAKE: the round that produced this sentence (review-log.md:570 DECISION) deleted two sentences arguing the bullet's timeout was "not a second bound" and then re-added the timeout as a bound in inverted form. Three attempts at this location have each been the previous one restated; the answer is that the statement does not belong in the paragraph at all.

WATCHOUT: review-log.md:420 and :1704 LOOK like they are falsified by any fix here. They are not falsified by the deletion — they assert exactly what the deletion leaves true (hold ends when the cleanup pass returns; `leaked` occupancy and the identifier hold are different objects; a `leaked` clause in the hold paragraph is barred). They ARE falsified by the conditional-release fix. A fixer that reads the potentially-related-sites list as a work order will edit the audit record to justify the wrong answer. — EVIDENCE: review-log.md:420, :1704

UNVERIFIED: whether a slot retry for the same session can land on the same pod after a failed `removeSlotTree`, so that a successor resolves a residual tree under the same identifier (slot identifier is the session id). If it can, that is a residue hazard independent of this contradiction and pre-existing in the tree, which discards the removal error at all three sites (`_ = removeSlotTree(...)` at pkg/adapter/holdstate.go:254, session.go:271, slotsession.go:217). Someone should file it against the whole-pod scrub's absence verification rather than against the hold. — EVIDENCE: pkg/adapter/session.go:271; spec/05_runtime-registry-and-pool-model.md:545 ("If cleanup fails, the slot is leaked")

OPEN: spec/05:545's per-slot cleanup timeout is unimplemented shipped spec text. This proposal no longer depends on it after the deletion, so it is BUILD-GAPS material rather than a finding here.

### [non-spec.14.review-applicability.1]

DECISION: returned an empty findings list — BECAUSE the whole checklist simulates clean (22 steps, every deliverable named exactly once except the three deliberate splits CODE-1/CODE-4/CODE-6, every Depends-on backward, every box unchecked, one lane per step, spec block leading, every tier a step's deliverable reaches present in its tier line), and the three candidates I built all land in enumeration-bookkeeping classes this loop has already refuted — ALTERNATIVES: see the three below.

FACT: the proposal has not moved since r12. `diff -rq -x '*review-log*'` against `non-spec-r13`, `non-spec-r13-start`, `non-spec-r14` and `non-spec-r14-start` is all empty; the newest snapshot that differs is `non-spec-r12`, whose delta is three edits (the two-recycle-sender correction in CODE-1's comment and its doc-comment bullet, CODE-2's heading gaining `sdkwarm.go`, the two recycle-scrub test arms, and the two client-envelope tier-1 cases). Run the snapshot diff chain newest-to-oldest as the first command of any round. EVIDENCE: scratchpad/cp-snap/0081_.../non-spec-r12 vs the live directory.

FACT: every checklist DEFERRED in `### Deferred` (review-log.md:1577-1584, :1598) is now DISCHARGED against the live checklist, verified line by line: S1 says §15.1 takes no new row and names the `SETUP_COMMAND_FAILED` widening (:17); S3 says the atomicity paragraph is not edited and gives SPEC-2's true scope (:21); S6 names two rows and "Neither row states an expected value" (:27), matching spec-changes.md:999-1000 and CODE-9's two-row table; S7 says the error catalog takes no new row (:29); S14 names `validateBindFields` at each handler's entry (:43); S16 says the untokened arm is reachable on a conforming adapter (:47); S18 names all three confirmation sites including the SDK-warm one (:51); S10 drops the withdrawn started-signal and the closed-field-set gate (:35). The orchestrator brief's "the checklist is known to be wrong" paragraph is itself stale: S1, S7 and S17/S18 all read correctly now. EVIDENCE: implementation-checklist.md:17,:21,:27,:29,:35,:43,:47,:51.

FACT: step numbers now live ONLY in `implementation-checklist.md`. `grep -n '\bS[0-9]\+\b'` returns nothing in non-spec-changes.md, spec-changes.md or summary.md, so the renumber-staleness class (review-log.md:1564) is closed by construction rather than by repair. EVIDENCE: the three greps.

FACT: S16's `UnconditionalTeardown: true` sweep set is exactly tiers 1, 2, 3, 4, 7a, 9 and 10 — `grep -rln "ShutdownRequest{" --include=*_test.go pkg/ tests/` names 10 `pkg/adapter` files plus `tier2_component/warmlayout`, two `tier3_contract` dirs, `tier4_integration` (x2), `tier7a_load_local` (x6), `tier9_security` (x3) and `tier10_conformance` (x1). S16's tier line lists 0,1,2,3,4,7a,9,10, so it omits none. The only non-test literal is `pkg/gateway/runtime/adapterclient/client.go:814` inside the single `Client.shutdown` builder. EVIDENCE: implementation-checklist.md:48; pkg/gateway/runtime/adapterclient/client.go:812-818.

FACT: CODE-2's `noteRuntimeStarted` call-site enumeration is exhaustive and exact against the tree — 3 production (session.go:163, resume.go:144, sdkwarm.go:261) and 6 test (export_test.go:45, usage_test.go:233, podmcp_arming_internal_test.go:84,:185,:230, adapterevents_test.go:95,:184). No site is missing and no cited line is off. EVIDENCE: `grep -rn "noteRuntimeStarted(" --include=*.go pkg/ | grep -v "func "`.

FACT: the `slotAddressCaseFiles` inventory rule the proposal paraphrases as "a file whose name matches `slot*_test.go`" is really `(slot|one_session_only|sole_session)[^/]*_test\.go$` over the BASENAME, plus a body-regex rule (`slotstate.|ClaimSlot(|ReleaseSlot(|ReserveSlotOnPod(|claimAtCreate(|BindReservedSlot(`) and a tier-11 TEST-GAPS.md rule. The proposal's generic wording ("Every new file whose name matches that pattern enters the inventory in its sorted position") is count-free and survives; the earlier three-versus-four counting defect cannot re-arm. EVIDENCE: tests/tier0_static/spec_map_slot_address_registration_test.go:1168,:1173,:1191; non-spec-changes.md:3478-3486.

WATCHOUT: `pkg/gateway/sessionserver/upload_to_session.go` is the one required edit site that appears in NO deliverable's `Targets:` list and in no checklist step — only in `## Files touched` (:3563) and as a descriptive sentence inside CODE-4's "What each attempt carries" paragraph (:806-809). It must set `mid_session` true on `PrepareWorkspace` (it already passes `true` to `FinalizeWorkspace` at upload_to_session.go:134; the client's `PrepareWorkspace` has no such argument today, upload_to_session.go:128). If it is not landed by S13, then from S14 onward `validateBindFields` answers `INVALID_ARGUMENT` for every §7.4 mid-session upload, and S14/S15's own tier-7a reclaim-hold §7.4 arm (which the proposal specifies is driven "through the production caller") goes red. I did NOT file it: CODE-4's body states the end value, the Files-touched bullet states it, and S13 is the natural home, which is the same reasoning the material skeptic used to refute the CODE-6 heading-file-list and CODE-4 target-list findings. A fixer already in CODE-4's Targets list should add the file. EVIDENCE: non-spec-changes.md:759-771,:806-809,:3563; pkg/gateway/sessionserver/upload_to_session.go:128,:134.

WATCHOUT: CODE-7 and CODE-4 both claim the same `client.go` request-side edits. CODE-7's body says "`Client` also gains the request-side halves the mechanism needs: `bind_attempt` on the six bind-sequence requests, `mid_session` on `PrepareWorkspace`, `unconditional_teardown = true` set inside `Shutdown` and `ShutdownRecycle`, and the new `ShutdownReclaim`" (:1758-1761), while CODE-4's `Targets:` list carries the identical set (:770-771). CODE-7 runs at S11 and CODE-4's mint half at S13. Landing the signature widenings at S11 breaks `binder.go:1327` and `upload_to_session.go:128` at compile time until S13 fills them in; landing them at S13 leaves S11 a pure-translation step. Either resolution works, so I did not file it, but a reader deciding S11's scope has two answers. EVIDENCE: non-spec-changes.md:770-771,:1758-1761; implementation-checklist.md:37,:41.

WATCHOUT: S16's `Depends-on` (S1, S2, S4, S9, S10, S11, S13, S14) omits S15, although S16's body requires `lockSlotGuard`, which S15 creates ("taken the raw slot guard and re-taken `s.mu`", :47). Harmless while the checklist is executed as a strict sequence, because S15 precedes S16; it would bite anything that schedules off the dependency edges. Same pattern at S18 (omits S1 while implementing SPEC-5's start-confirmation rule) and S21. EVIDENCE: implementation-checklist.md:45-48,:52,:58.

FACT: the spec block's internal forward references are recorded rather than latent. S1 and S3 both point at text S4 lands, and S1's line states the disposition ("Three staged citations forward-reference S4 ... the applied spec is correct once all four spec steps have landed"). No link gate fires in the interval, because every one of those cross-references resolves to `05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes`, a heading anchor that exists in the shipped tree. The two other interval gaps carry explicit dispositions too: S5's line defers tier 11 to S7 and tier 1 to S8. EVIDENCE: implementation-checklist.md:17,:25.

USEFUL [the Standing context reading rule "diff -rq across the whole snapshot chain, newest to oldest, is the first command of any round"]: it saved a full read-the-diff pass; r13, r13-start, r14 and r14-start are all byte-identical to the live proposal.

### [non-spec.14.review-citations.1]

FACT: The staging did not move between round 13 and round 14. `diff -rq` of `scratchpad/cp-snap/0081/non-spec-r13`, `non-spec-r14` and `non-spec-r14-start` against the live proposal shows only the review log differing (the compaction pass 21 rewrite). The newest snapshot that differs in a staged file is `non-spec-r12`, and its delta is exactly three hunks: the G3 two-recycle-sender correction in CODE-1's `answerShutdown` comment and its prose bullet, CODE-2's heading gaining `pkg/adapter/sdkwarm.go`, the tier-1 recycle-scrub case splitting into an `absent` arm and a `reclaimed` arm, the new "The refusals' client envelopes" tier-1 pair, and one summary deliverable-index line. EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.non-spec-changes.md:318-330,:548-556,:564,:2650-2665,:2837-2860

FACT: Round 14's citation lens re-verified the r12→r14 delta line by line and every citation resolves: `slotbinder.go:542` (`result.Adapter.Shutdown`) and `:574` (`ShutdownRecycle`); `binder.go:1994` (`b.shutdownAdapter(ctx, result, true)`) and `:2037` (`ShutdownRecycle` inside `shutdownAdapter`); `recycle_scrub_path_test.go:404` (`TestRecyclePathScrubReportedReuses_spec_5_2`) and `:429` (`binder.Release`); `slotretry_test.go:519-548` (`TestClassifiedSlotFailureKeepsSetupCommandEnvelope_spec_7_3`, which really does drive `writePodClaimError` from a wrapped `*SlotBindError` and assert 422 + `SETUP_COMMAND_FAILED`); `slotbinder.go:303-304` (the `SetupCommandFailure` wrap of a refused `RunSetup`); `start.go:87-90` (`writePodClaimError` + its `setupFail` var) and `:239-249` (`writeSetupCommandError`'s `status.Code(setupFail.Cause) == codes.FailedPrecondition` branch and the `Retry-After` fallback).

FACT: An independent whole-document sweep this round found no false citation anywhere in the non-spec staging, the checklist or the summary. Verified by opening the target: SCHEMA-1's entire field-number table against `schemas/lenny-adapter.proto` (note `PrepareWorkspaceRequest` 1-3 + reserved 4, `FinalizeWorkspaceRequest` 1-4 + reserved 5, `RunSetupRequest` 1-3 + reserved 4, `AssignCredentialsRequest` 1-2 + reserved 3, `ResumeRequest` 1-5,7-14 + reserved 6,15, `ShutdownRequest` 1-3,5,6 + reserved 4, `ShutdownResponse` 1-2, and `ERROR_CODE_*` ending at 27); CODE-8's twenty-one `binder.go` line cites (:865,:866,:867,:869,:883,:918,:923,:935,:950,:954,:977,:997,:998,:1000,:1010,:1021,:1032,:1072,:1079,:1081,:1082) all exact; CODE-9's metric cites (`gatewaymetrics_credential.go:185-193`, `gatewaymetrics.go:1170-1175`, `binder.go:137`, `slotbinder.go:353-361`, `metricsbackfill.go:149`, `catalog.go:146,:271`, `catalog_test.go:188-211`, `spec/16:186-189`, `slotbinder.go:287,294`); CODE-2's whole call-site and test-fixture set; CODE-6's `slot.go:105/:140`, `slotsession.go:174`, `staging.go:209-214`, `slotlayout.go:115-131/:136-139`; the DOCS-1/2/3 anchors in `docs/reference/state-machines.md:138` and `docs/reference/adapter-contract.md:10,:18,:53,:64,:75` and `docs/reference/error-catalog.md:129`; and SPEC-5's three "verbatim" §15.1 quotes plus the §6.2 client-visibility quote against `spec/15:1136` and `spec/06:290`. Do not re-run this sweep; it is now the second exhaustive one in two rounds.

FACT: `Client.Shutdown`/`Client.ShutdownRecycle` have exactly the five production callers CODE-4's table names. Derive it with `grep -rn "\.Shutdown(\|\.ShutdownRecycle(" --include=*.go pkg/ cmd/ | grep -v _test.go`; every other hit is an `http.Server`, an OpenFeature provider or the generated grpc stub. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:542,:574; binder.go:2037,:2043; cmd/lenny-gateway/user_revocation.go:129

FACT: S16's `UnconditionalTeardown: true` sweep set really is what its own grep names. `grep -rn "ShutdownRequest{" --include=*_test.go pkg/ tests/` returns 64 hits across 26 files, and a repo-wide run adds nothing outside `pkg/` and `tests/`; the only non-test literal is the client builder at `pkg/gateway/runtime/adapterclient/client.go:814`. EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.implementation-checklist.md:47

MISTAKE: The orchestrator brief carried into this round is stale on the checklist. It states that "the checklist's S7 line and the summary's deliverable index still describe DOCS-3 as two new rows and are wrong". Both have been reconciled: implementation-checklist.md:29 now reads "The published error catalog takes no new row; its `SETUP_COMMAND_FAILED` row takes the four sentence replacements and the replaced remedy cell", and summary.md:949 says the same. It is also stale on S1/S17/S7 generally: the checklist is now 22 steps and S1 stages the narrowed §15.1 edits rather than new rows. A round acting on the brief's checklist paragraph will chase three fixed items.

WATCHOUT: `non-spec-changes.md:897` calls `slotbinder.go:543` "the shipped compensation call site". That line is inside `Binder.ReleaseSlot`'s session-end teardown, and no compensation ships today; what is true, and what the sentence is actually asserting, is that `leaked = err != nil || !cleanly` is already computed there. Judged wording rather than a false citation this round (same component, same file, same expression, unchanged conclusion). Do not spend a verifier pair on it; a reconciliation pass may tidy it. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:543

WATCHOUT: `session.go:283-291` is cited for clause three's `if rc := req.GetRecycle(); rc != nil { s.startPodScrub(rc) }`. The `if` is at :288-290 and :291 is the `return`; the cited range starts at the "// Clause three." comment. It contains the statement and changes no meaning, so it is drift rather than a defect. EVIDENCE: pkg/adapter/session.go:282,:288-291

USEFUL [Settled: "the whole-staging citation sweep is COMPLETE and clean"]: true again at round 14, and the r12→r14 delta was the only text that needed a first read.

### [non-spec.14.review-client-surface.1]

DECISION: returned an empty findings list — BECAUSE every client-facing representation the staging
touches was verified mirrored, and the two candidates I built both collapsed onto recorded
pre-existing gaps — ALTERNATIVES: filing the proto `Error.ErrorCode` catalog widening (see the FACT
below) and filing the loose `start.go:87-90` citation (citation-range class the Traps already say to
normalise rather than file).

FACT: `cmd/lenny-compliance/schemaassert.go` DERIVES the "closed §15.1 error-code catalog" by
regexing the `enum ErrorCode` block out of the embedded `schemas/lenny-adapter.proto` and stripping
the `ERROR_CODE_` prefix; a runtime JSONL response whose `error.code` is outside that set is reported
non-conformant. So SCHEMA-1's two new values widen that conformance catalog and make the proto's own
"The catalog below mirrors spec §15.1" comment one instance looser, even though the staging states
§15.1 gains no row. This is NOT a finding: it is the same direction as the pre-existing gap the
proposal already records out of scope (summary.md:901-910 lists ten shipped enum values with no §15.1
row and names the `PROTOCOL_VERSION_INCOMPATIBLE` precedent). A later lens that reaches
schemaassert.go should stop here rather than spend two verifiers.
EVIDENCE: cmd/lenny-compliance/schemaassert.go:24-29,:56,:80-84; schemas/lenny-adapter.proto:555-557;
summary.md:901-910.

FACT: the closed-field-set audit is complete and came back clean. Over the messages SCHEMA-1 opens,
`tests/tier3_contract/gatewaycontrol_scrub/shutdown_recycle_wire_test.go:211-233` is the ONLY closed
field set (`assertFieldSet` at :259 errors on any declared field absent from `want`), which SCHEMA-1
already targets. `adapter_session_address/session_address_wire_test.go` asserts only no-`slot_id`,
`session_id` presence and reserved numbers/names, so it is additive-safe;
`adapter_generation_fence/generation_fence_wire_test.go:76,:79` pins one field number per message and
no count; `checkpoint_stream/checkpoint_stream_wire_test.go:151` pins a field COUNT but on
`CheckpointStart`, which SCHEMA-1 does not open. Do not re-derive this sweep.
EVIDENCE: tests/tier3_contract/gatewaycontrol_scrub/shutdown_recycle_wire_test.go:208-267;
tests/tier3_contract/adapter_session_address/session_address_wire_test.go:44-97;
tests/tier3_contract/checkpoint_stream/checkpoint_stream_wire_test.go:151.

FACT: `RotateCredentials` does NOT go through the resolve chokepoint, so the §4.7.1 cascade cannot
refuse a mid-session rotation. `rotateCredentialsSlot` uses `slotStateLocked` (a read-only lookup),
not `ensureSlotStateLocked`. This kills the most promising mechanism candidate against CODE-6's
"three production callers cover all seven RPCs" claim: the claim is exact, and the started-session
rule never reaches `RotateCredentials`, `Attach`, `SendMessage`, `Interrupt`, `ReportUsage` or
`Checkpoint`.
EVIDENCE: pkg/adapter/slotcreds.go:67-91 (`slotStateLocked` at :70); pkg/adapter/credentials.go:65-74
(AssignCredentials → assignCredentialsSlot), :89; pkg/adapter/slot.go:105,:140-143;
pkg/adapter/slotsession.go:75.

FACT: there is no OpenAPI, SDK, CRD, chart, JSONL-schema or docs/api mirror of anything this proposal
changes. `SETUP_COMMAND_FAILED` appears in no `openapi.json` (the served document is at
`pkg/gateway/externalapi/openapi/openapi.json`, NOT `pkg/gateway/openapi/openapi.json` as the lens
brief spells it); no `sdks/` file enumerates the adapter error-code catalog; the only copies of
`lenny-adapter.proto` outside `schemas/` are `scripts/specshift/testdata/` fixtures. The client-facing
mirrors that DO exist and ARE staged are exactly three: `docs/reference/error-catalog.md:129`
(DOCS-3), `docs/reference/adapter-contract.md:75,:64` (DOCS-2) and
`docs/reference/state-machines.md:138,:235` (DOCS-1).
EVIDENCE: pkg/gateway/externalapi/openapi/openapi.json; `grep -rn SETUP_COMMAND_FAILED docs/ spec/`
returns docs/reference/error-catalog.md:129 plus five spec sites that state sufficient conditions and
so are not falsified.

FACT: every verbatim anchor the docs deliverables quote resolves uniquely. DOCS-3's four sentences and
its remedy cell are all present in `docs/reference/error-catalog.md:129`; SPEC-5's three §15.1
sentences are present in `spec/15_external-api-surface.md:1136`; DOCS-1's three projection clauses and
SPEC-4's three are the same three strings, each occurring exactly once in
`docs/reference/state-machines.md` and once in `spec/06_warm-pod-model.md`. DOCS-2's `DemoteSDK`
replacement is true against the tree: `DemoteSDK` really does drop the registry entry, through
`s.releaseSessionSlot` at pkg/adapter/sdkwarm.go:298.
EVIDENCE: docs/reference/error-catalog.md:129; spec/15_external-api-surface.md:1136;
docs/reference/state-machines.md:138,:235; pkg/adapter/sdkwarm.go:296-301.

FACT: the new r12→now test block "The refusals' client envelopes" checks out on every citation.
`TestClassifiedSlotFailureKeepsSetupCommandEnvelope_spec_7_3` is at
pkg/gateway/sessionserver/slotretry_test.go:519; the refused-`RunSetup` wrap is
pkg/gateway/podlifecycle/podsession/slotbinder.go:303-304 and its stage constant `slotFailureSetup`
is the literal `"setup"` (binder.go:291); `writeSetupCommandError`'s branch on
`status.Code(setupFail.Cause) == codes.FailedPrecondition` is start.go:239 with the 503+`Retry-After`
fallback at :246-249. The block's premise that `status.Code` walks a `fmt.Errorf("%w: %w", ...)` wrap
holds on the pinned grpc-go: `status.FromError` falls back to `errors.As` (grpc v1.80.0,
status.go:112-113), and Go's `errors.As` traverses `Unwrap() []error`.
EVIDENCE: pkg/gateway/sessionserver/slotretry_test.go:519-548; pkg/gateway/sessionserver/start.go:87-90,:139-146,:236-249;
pkg/gateway/podlifecycle/podsession/slotbinder.go:303-304; pkg/gateway/podlifecycle/podsession/binder.go:291.

WATCHOUT: the lens brief names the served OpenAPI document as `pkg/gateway/openapi/openapi.json`,
which does not exist. The real path is `pkg/gateway/externalapi/openapi/openapi.json`. A grep against
the brief's path returns "No such file" and reads like a clean absence.
EVIDENCE: pkg/gateway/externalapi/openapi/openapi.json.

USEFUL [the Standing context Trap at review-log.md:1319 and the Ledger WATCHOUT at :2203]: the
orchestrator brief for round 14 still asserts `ShutdownResponse` "gains `slot_was_started (4)`". The
log's correction saved me from filing a phantom omission against SCHEMA-1's nine-row field table,
which ends at `slot_reclaim = 3`. Keep that entry until the brief is fixed.

USEFUL [the Settled entry at review-log.md:99 and the snapshot rule at :159]: `diff -rq` over the whole
snapshot chain first. `non-spec-r14`, `-r14-start`, `-r13` and `-r13-start` are all byte-identical to
the live proposal; the newest snapshot that differs is `non-spec-r12`, and the r12→now delta is three
hunks (the CODE-1 clause-three two-sender rewrite, CODE-2's heading gaining `sdkwarm.go`, and the new
"The refusals' client envelopes" tier-1 test block).

### [non-spec.14.review-docs-alignment.1]

DECISION: Returned an empty findings list for the documentation-alignment lens — BECAUSE every behaviour the staging changes that `docs/` currently describes resolves to a staged DOCS or CODE-9 edit, and the two lens-specific categories (accepted/deferred residues landing only in reasoning; a new operator-facing failure cause missing from narrative operator docs) produced only close variants of items the material skeptic has already refuted — ALTERNATIVES: rejected filing (a) the stranded-token residue as missing from `docs/runbooks/gateway-replica-failure.md` (whose lead already frames a gateway crash as benign for sessions), because the same residue's spec-text variant is on the refuted list; (b) the surviving registry entry and armed §4.9 lease-expiry timers as a new row in the `Residual state across sessions` columns of `docs/reference/execution-modes.md:62` and `docs/operator-guide/multi-tenancy.md:68`, because the residue is pre-existing rather than introduced and the staged §6.2 prose states it; (c) the `session release` / `slot release` vocabulary split between `adapter-contract.md`'s staged `Shutdown` row and its unedited `ReportSessionScrub` row, as wording.

FACT: the docs mirror set for this proposal is exactly four pages and all four are staged. `docs/reference/state-machines.md` is the ONLY page stating the occupancy projection (`grep -rn "no claim projects" docs/` returns :138 alone); `docs/reference/adapter-contract.md` is the only page with a `Shutdown` or `DemoteSDK` row; `docs/reference/error-catalog.md:129` is the only published `SETUP_COMMAND_FAILED` row; `docs/reference/metrics.md` is the only metric catalog. EVIDENCE: docs/reference/state-machines.md:138; docs/reference/adapter-contract.md:64,:75; docs/reference/error-catalog.md:129; docs/reference/metrics.md:166,:171.

FACT: DOCS-1's and DOCS-3's verbatim source quotes all resolve. DOCS-1's three projection clauses are verbatim substrings of state-machines.md:138 and match SPEC-4's §6.2 replacements clause for clause. DOCS-3's four description sentences and the remedy cell are verbatim substrings of error-catalog.md:129. EVIDENCE: docs/reference/state-machines.md:138; docs/reference/error-catalog.md:129; spec-changes.md:712-718 (SPEC-4's clause replacements).

FACT: DOCS-2's staged `Shutdown` row keeps all four substrings the shipped gate asserts ("end-of-session teardown", "recycle disposition", "ReportSessionScrub", "ReportPodScrub"), and the five substrings the proposal tells the implementor to ADD are each present verbatim in the staged row. EVIDENCE: tests/tier11_docs/basic_level_echo_stamp_doc_reconciliation_test.go:298-307; non-spec-changes.md:2273 (the staged row).

FACT: adding `"receiving_uploads ──→ slot_cleanup"` to `generalSlotEdges` is safe against the gate's NEGATIVE loop. That loop asserts no general edge appears in the concurrent-occupancy block, and that block carries only `running ──→ failed` and `slot_cleanup ──→ leaked`. EVIDENCE: tests/tier11_docs/per_slot_substate_scope_doc_reconciliation_test.go:32-37,:70-74; spec/06_warm-pod-model.md:146-148.

FACT: DOCS-3's "nothing holds the two to one text" claim is true — `grep -rn "error-catalog" tests/ scripts/ cmd/ Makefile` returns nothing. EVIDENCE: non-spec-changes.md:2313-2317.

WATCHOUT: `docs/api/internal.md` LOOKS like an unstaged gateway-to-adapter wire reference (it carries proto message bodies and a gRPC status-code table) but documents a different, already-stale RPC set — `StopSession` and `UploadFiles`, which the real proto does not declare — and names no bind-sequence RPC at all. `grep -n "Shutdown" docs/api/internal.md` returns nothing. Its status-code table already omits `ABORTED` and `INVALID_ARGUMENT`, both of which the shipped adapter already returns, so the omission is pre-existing and not this proposal's edit site. EVIDENCE: docs/api/internal.md:79-99,:489-499; pkg/adapter/credentials.go:68.

WATCHOUT: `docs/reference/error-catalog.md:156` says `STARTING_FAILED` is "Retryable for the same reason as `SESSION_CREATION_FAILED`: a retry rebinds a fresh pod", which the proposal's own create-time-reserved edge case contradicts ("the retried start reconnects to the same pod under the same slot identifier"). It is NOT a finding for this proposal: the spec's own §15.1 `STARTING_FAILED` row carries no fresh-pod clause, so the embellishment is a pre-existing docs-only defect, and this proposal edits neither row. EVIDENCE: docs/reference/error-catalog.md:156; spec/15_external-api-surface.md:1132; spec-changes.md:184-186.

USEFUL [orchestrator brief]: the brief's "ShutdownResponse ... gains slot_was_started (4)" is still false and the ledger's standing correction saved the check. `grep -rn slot_was_started` over the proposal returns hits only inside the review log. EVIDENCE: review-log.md:718,:1285,:2203.

FACT: the proposal text has not moved since round 13 — `diff -rq` across `non-spec-r13`, `non-spec-r13-start`, `non-spec-r14-start` and `non-spec-r14` differs only in the review-log files. Reading "the changed sections first" is a no-op this round. EVIDENCE: scratchpad/cp-snap/0081_.../non-spec-r14.

### [non-spec.14.review-edit-sites.1]

DECISION: returned an empty findings list — BECAUSE the edit-site sweep came back clean on every
surface my lens owns, and the two candidates I developed both died on verification (below) —
ALTERNATIVES: filing the `docs/reference/adapter-contract.md:81` `ReportSessionScrub` row ("at each
session release") as falsified by SPEC-3's withheld report, rejected because the staged §5.2 text
defines a pre-`running` cleanup as NOT a session release ("A cleanup that reclaims a slot the pod's
shared runtime process was given is a session release like any other", spec-changes.md:643), so the
row stays true; and filing the §4.6.1/§6.2 occupancy-projection correction as missing a `spec/29`
mirror, rejected because spec/29:744 already states the rule unqualified.

FACT: the r14 staging is byte-identical to the `non-spec-r14`, `non-spec-r14-start`, `non-spec-r13`
and `non-spec-r13-start` snapshots. The newest snapshot that differs is `non-spec-r12`, and the whole
r12→r14 delta is four hunks: CODE-1's two-recycle-sender comment rewrite, CODE-2's heading gaining
`pkg/adapter/sdkwarm.go`, the split of the recycle-scrub tier-1 bullet into an `absent` arm and a
`reclaimed` arm, and the new "The refusals' client envelopes" tier-1 bullet. — EVIDENCE:
`diff -rq scratchpad/cp-snap/0081.../non-spec-r12 proposals/0081...`

FACT: every citation in that r12→r14 delta resolves verbatim. `binder.go:1994` is
`b.shutdownAdapter(ctx, result, true)`, `:2037` is the `ShutdownRecycle` call inside
`shutdownAdapter`; `slotbinder.go:303-304` is the `slotFailureSetup` wrap of `&SetupCommandFailure`
and `slotFailureSetup = "setup"` is at `binder.go:291`; `slotretry_test.go:519-548` is
`TestClassifiedSlotFailureKeepsSetupCommandEnvelope_spec_7_3`; `start.go:239-249` is
`writeSetupCommandError`'s `status.Code(setupFail.Cause) == codes.FailedPrecondition` branch and
`:87-90` is `writePodClaimError`'s typed-handler block; `recycle_scrub_path_test.go:404` is
`TestRecyclePathScrubReportedReuses_spec_5_2` and `:429` its `binder.Release`. — EVIDENCE: those
files at those lines.

FACT: the new-identifier sweep is empty across the whole corpus, re-run this round.
`grep -rn "unconditional_teardown|bind_attempt|SlotReclaimOutcome|slot_reclaim|SLOT_BIND_" spec/ docs/
schemas/ charts/ pkg/ cmd/ tests/ sdks/` returns ZERO hits, and `grep -rln
"ShutdownRequest|ShutdownResponse|exited_cleanly|slot_reclaim" sdks/ docs/ schemas/ spec/ charts/`
returns exactly two files, `schemas/lenny-adapter.proto` and `spec/04_system-components.md`. So the
nine new proto fields, the new enum and the two new `ErrorCode` values falsify no prose site the
staging does not already open. — EVIDENCE: those greps.

FACT: the four docs mirrors are complete and each one's anchor text is verbatim-unique in its page.
All five DOCS-3 anchor sentences (`grep -cF` each) return exactly 1 in
`docs/reference/error-catalog.md`; DOCS-2's two rows are `docs/reference/adapter-contract.md:64`
(`DemoteSDK`) and `:75` (`Shutdown`), both one physical line; `docs/reference/metrics.md` carries the
`## Adapter metrics` table at :171 with the "outside the default scrape set" phrasing DOCS/CODE-9
reuses at :177-180. — EVIDENCE: docs/reference/error-catalog.md:129; adapter-contract.md:64,:75;
metrics.md:171,:173,:177-180.

FACT: DOCS-2's new `DemoteSDK` clause ("drop the adapter's slot registry entry for the session") is
TRUE against the tree and needs no companion spec edit to be true, even though spec/04:674's
`DemoteSDK` row does not state it. `Server.DemoteSDK` calls `s.noteRuntimeClosed(sessionID)` then
`s.releaseSessionSlot(sessionID)` on `s.anyRegisteredSession()`. A reviewer tempted to file "DOCS-2
publishes a registry effect the spec does not define" should read that handler first. — EVIDENCE:
pkg/adapter/sdkwarm.go:274-303; spec/04_system-components.md:674.

FACT: `pkg/controller/warmpool/occupancy.go:57-63,:78-80` ALREADY states the rule SPEC-4 installs
("the §6.2 state machine encodes the recycle-versus-one-session distinction in the phase the pod sits
in at the claim DELETE" … "no claim on a claimed pod → draining"), so SPEC-4 brings the spec into
line with shipped code rather than the reverse, and that file is not an edit site. The one code
comment that still carries the FALSE form is `pkg/gateway/podlifecycle/podsession/binder.go:1191-1194`,
which the standing Deferred already records as CODE-4's to take. — EVIDENCE: those two files.

FACT: the carriers of the "claim deleted on a pod with `recycle.enabled: false` projects `draining`"
clause are exactly four and the staging opens all four: spec/04:414-415 (SPEC-4), spec/06:95-97 (the
fence, SPEC-4), spec/06:80 (the projection prose, SPEC-4) and docs/reference/state-machines.md:138
(DOCS-1). `spec/29:744`, `docs/reference/state-machines.md:241` and
`docs/getting-started/architecture.md:237` each state a compatible weaker form and are NOT edit
sites. Do not re-run this sweep. — EVIDENCE: `grep -rn "projects \`idle\`|projects \`draining\`" spec/ docs/`.

FACT: the five-row `unconditional_teardown` caller table is exhaustive against the tree.
`grep -rn "\.Shutdown(ctx|\.ShutdownRecycle\(|Adapter\.Shutdown\(" pkg/ cmd/ --include=*.go` minus
`_test.go` returns exactly `slotbinder.go:542`, `:574`, `binder.go:2037`, `:2043` and
`user_revocation.go:129`, plus the client's own `c.rpc.Shutdown` at `client.go:814`. The builder
claim is exact too: `Client.Shutdown` at `:807`, the unexported `Client.shutdown` at `:813`,
`Client.ShutdownRecycle` at `:860`. — EVIDENCE: those files.

FACT: the `releaseSessionSlot` caller enumeration at non-spec-changes.md:1644-1656 is exhaustive and
exact against the tree, test seams included: resume.go:69,:73,:89,:107,:126,:134,:141;
session.go:133,:147,:157; sdkwarm.go:236,:241,:251,:298; podmcp_arming_internal_test.go:144,:195; and
`ReleaseSlotForTest` at export_test.go:32-35 with its five callers. Nothing is missing. One
imprecision, sub-threshold: the justification for the `sdkwarm.go` sites says "because `StartSession`
and `ConfigureWorkspace` hold no guard", but `:298` is inside `DemoteSDK`, a third handler. The
conclusion holds (DemoteSDK holds no guard either). — EVIDENCE: `grep -n "releaseSessionSlot|ReleaseSlotForTest" pkg/adapter/*.go`.

FACT: `pkg/gateway/podlifecycle/podsession/` (change-graph-coverage.yaml:227) and `pkg/adapter/`
(:43) are both coverage-baseline directory prefixes, so the two new production files
`pkg/adapter/bindattempt.go` and `pkg/gateway/podlifecycle/podsession/bindattempt.go` owe no
`tests/change-graph.json` key. The archive's version of this fact named the retired
`pkg/adapter/bindepoch.go`; it transfers unchanged. — EVIDENCE: tests/registers/change-graph-coverage.yaml:43,:227.

FACT: the R8 deferral SCHEMA-1's `ABSENT` claim row names is really declared at
`gateway-runtime-comms-remediation.md:980` ("### R8. Reciprocal host-conformance battery"), and
`#2851-gateway-to-pod` is really the anchor the sibling `coordination_generation` rows use
(scripts/seed-claim-register.py:86,:182,:383). Both halves of the tier-0 claim-register gate's
precondition hold. — EVIDENCE: those two files.

FACT: `tests/registers/identifier-senses.yaml` is occurrence-indexed by position within a file, so
inserting text into spec/04 that carried a RETIRED channel spelling would renumber every later entry
for that file. It cannot bite here: the retired spellings are `LifecycleChannel`, `controlchannel`,
`lifecycleChannel`, `lifecycle-socket`, `@lenny-lifecycle`, `lifecycle-events` and `lifecyclechannel`
(spec/28:151-158), and no staged block contains one. Recorded because the hazard is real for any
future spec/04 insertion. — EVIDENCE: spec/28_communication-channels.md:149-158;
tests/tier0_static/identifier_resolution_test.go:100-113.

USEFUL [Settled, "The docs surface this proposal can falsify is small and now fully enumerated"]:
saved me a full docs sweep; I re-ran only the two greps it names and both still return what it says.

USEFUL [Settled, "No alert anywhere references any slot metric"]: re-verified in one command —
`grep -rln "lenny_slot_failure_total|lenny_adapter_leaked_slots" charts/ docs/` returns only
`docs/reference/metrics.md`, and `docs/operator-guide/observability.md` names no `lenny_slot*` or
`lenny_adapter*` series at all. The alert/runbook half of the edit-site lens is structurally inert
and can be skipped entirely by future rounds.

OPEN: the edit-site lens has now returned nothing in rounds 13 and 14. If it runs again, the only
unexamined corner I can name is whether any `docs/adr/` page describes the §4.6.1 occupancy
projection in a form SPEC-4 falsifies; `docs/adr/0007-sandboxclaim-optimistic-locking.md:25` was the
one hit and it states only that the projection sets `claimed` from the claim's `bound` state, which
SPEC-4 does not touch.

### [non-spec.14.review-feasibility.1]

DECISION: returned an empty findings list — BECAUSE every actor assignment in the staging
resolves against a real symbol with the stated capability, and the snapshot check showed the
proposal text is byte-identical to `non-spec-r13`, the round in which all three lenses already
returned nothing — ALTERNATIVES: filing the `holdstate.go:189-205` citation looseness (the cited
range is `onHoldTimeout`'s pass-1 CALL, while pass 1 itself is `deregisterStartedSessions` at
`pkg/adapter/slotsession.go:375-396`); dropped as sub-threshold range imprecision of the class
the Traps section already records as "normalise, not file".

USEFUL [Standing context, "the snapshot trap fired in this window too"]: `diff -rq` newest-to-oldest
across the snapshot chain was the first command and cost one minute. `non-spec-r14`,
`non-spec-r14-start`, `non-spec-r13` and `non-spec-r13-start` are all byte-identical to the live
proposal except the review log; the newest snapshot that actually differs is `non-spec-r12-prefix`.

FACT: the actor-capability spine of this proposal is fully discharged in the tree. Verified this
round, first hand, all of it: `failPhase` drains outside the `leaseAssigned` block
(pkg/gateway/podlifecycle/podsession/binder.go:1072-1082); `leaseAssigned` declared :865, closure
:866-869, set true :954; `Binder.Launch`'s closure :997-1000 passes literal `true` at :998;
`SlotFailure func(errorType, pool, podName string)` at binder.go:137 and its sole production
wiring at cmd/lenny-gateway/metricsbackfill.go:149; `recordSlotFailure` at slotbinder.go:353-361.
Field availability for CODE-4's helper arguments holds on both request types: `SlotBindRequest`
carries `Pool`, `MaxConcurrentSessions` and `CleanupTimeoutSeconds` (slotbinder.go:28,:39,:108),
and `ResumeRequest` carries all three (binder.go:604,:645,:667).

FACT: the adapter CAN label an adapter-side series by `k8s_pod_name`. `Server.podID` is read once
from the Downward API at `pkg/adapter/server.go:380` (`os.Getenv(podNameEnvVar)`), the podspec
renderer emits exactly one such env (`pkg/controller/sandbox/podspec/podspec.go:175,:904`), and
the empty-on-unset case already has a shipped fail-closed precedent at
`pkg/adapter/sessionscrubreporter.go:70-76`. The standing WATCHOUT at review-log.md:3329 about
`pkg/adapter/metrics.go` carrying no `k8s_pod_name` series today is about precedent, not about
capability: SPEC-6's row is implementable as written.

FACT: a new tier-3 DIRECTORY entry in `tests/spec-map.json` is legal, so CONF-1's
`tests/tier3_contract/adapter_bind_attempt/...` entry is feasible even though every existing
`tier3_contract` entry is file-by-file. `validateTestFilesMapped` normalises a `/...` suffix and
then tries direct, parent-dir and parent's-parent matches
(cmd/lenny-test/cmd_validate.go:729-771). This complements, rather than contradicts, the Settled
entry that tier7a is mapped file by file: that is how tier7a HAPPENS to be written, not a
validator restriction.

FACT: the RPC senders that the wire rule constrains are a closed, small set, and no non-bind
sender exists that would be broken by the mandatory-token rule. `grep` over `pkg/gateway` and
`cmd` for adapter `AssignCredentials`/`RunSetup`/`FinalizeWorkspace`/`PrepareWorkspace`/`Resume`/
`Shutdown`/`ShutdownRecycle` senders returns exactly: slotbinder.go:291,:299,:403,:542,:574;
binder.go:921,:933,:1256,:1327,:1607,:2037,:2043; upload_to_session.go:128,:134;
start.go:4005 (via `podBinder.Resume`); cmd/lenny-gateway/user_revocation.go:129. Credential
ROTATION does not reuse `AssignCredentials` (`pkg/gateway/credentials/credassign/client.go:194`
targets `tokensv1`, a different service), so no rotation path is caught by the "always carries a
non-empty token" rule. The five-row `unconditional_teardown` caller table is therefore complete
against the tree.

FACT: the adapter already has the shipped pattern CODE-6's two typed refusals need. A
`status.New(...).WithDetails(&adapterv1.Error{...})` with an explicit `Retryable` sits at
pkg/adapter/staging.go:204-217, which is the precedent CODE-6 cites and it is accurate. CODE-7's
gateway-side translation therefore has something to translate.

UNVERIFIED: `errSlotReclaimInProgress` (bare `codes.Aborted`, no `adapterv1.Error` detail) matches
neither of CODE-7's two sentinels, so a hold refusal met inside `Binder.Prepare`/`Binder.Launch`
still falls through CODE-8's guard into `failPhase` and drains the pod. It reads as fail-closed
and defensible (something is mid-teardown on that identifier), and no staged sentence claims
otherwise, so it is not filed. A reliability or operational lens with binder.go and
bindattempt.go open should decide once whether that drain is intended.

### [non-spec.14.review-fresh.1]

DECISION: returned an empty findings list — BECAUSE a whole-document citation and mechanism sweep found nothing above the bar; every candidate I derived sits in a class this loop has already refuted on materiality — ALTERNATIVES: filing the three near-misses listed below, rejected because each has a recorded refutation or an explicit sub-threshold DEFERRED against it.

FACT: the r14 snapshot diff is review-log-only. `diff -ru scratchpad/cp-snap/.../non-spec-r14 proposals/0081_...` shows hunks in `0081_....review-log.md` alone, so round 13 produced no staging edits and the "read the changed sections hardest" instruction has no target this round. Do not spend a round looking for one. EVIDENCE: proposals/0081_.../0081_....review-log.md:2-1967 (the only changed file).

FACT: the orchestrator brief's three "known wrong checklist contradictions" are ALL FIXED in the current text and must not be re-derived. S1 now reads "§15.1 takes no new catalog row for either adapter error code" (implementation-checklist.md:17); S7 reads "The published error catalog takes no new row; its `SETUP_COMMAND_FAILED` row takes the four sentence replacements" (:29); S18 names three confirmation sites including "the SDK-warm `ConfigureWorkspace`" (:51). EVIDENCE: implementation-checklist.md:17,:29,:51.

FACT: the five production senders of an adapter `Shutdown`/`ShutdownRecycle` are exactly the five CODE-4's table names, and nothing else in the tree sends one. `grep -rn "\.Shutdown(\|\.ShutdownRecycle(" pkg/ cmd/ tests/ --include=*.go | grep -v _test.go | grep -v pkg/adapter/` returns only `adapterclient/client.go:814` (the builder), `slotbinder.go:542`, `slotbinder.go:574`, `binder.go:2037`, `binder.go:2043`, `cmd/lenny-gateway/user_revocation.go:129` — the rest are `http.Server.Shutdown` and OTel. The caller table is complete; do not re-file it as short. EVIDENCE: non-spec-changes.md:814-820.

FACT: `Client.shutdown`, the single unexported builder CODE-4 relies on to set `unconditional_teardown` without editing any caller, ALREADY EXISTS in the tree. It is not staged work. EVIDENCE: pkg/gateway/runtime/adapterclient/client.go:813-823, called from `Shutdown` (:807-809) and `ShutdownRecycle` (:859-867).

FACT: every field number in SCHEMA-1's table is free in `schemas/lenny-adapter.proto` exactly as stated, including `ResumeRequest` 16 (1-5 and 7-14 used, 6 and 15 reserved) and `ShutdownRequest` 7/8 (1-3 used, 4 reserved, 5 `recycle`, 6 `coordination_generation`). `ErrorCode` runs to 27 (`ERROR_CODE_PROTOCOL_VERSION_INCOMPATIBLE = 27`, schemas/lenny-adapter.proto:585), so 28/29 are next. Re-verified this round; do not re-check. EVIDENCE: schemas/lenny-adapter.proto:585, and the per-message field blocks.

FACT: `TestShutdownMessagePostRemovalDescriptor_spec_4_1` is the ONLY closed field set over any message SCHEMA-1 opens. The other descriptor gates are open-world: `adapter_session_address/session_address_wire_test.go` asserts absence of `slot_id` and presence of `session_id` per message and reserves retired numbers, `adapter_generation_fence` pins one field per message, `checkpoint_stream` closes sets over messages SCHEMA-1 does not touch. SCHEMA-1's claim holds. EVIDENCE: tests/tier3_contract/gatewaycontrol_scrub/shutdown_recycle_wire_test.go:208-236; tests/tier3_contract/adapter_session_address/session_address_wire_test.go:112-170.

FACT: `error_type` on `lenny_slot_failure_total` is NOT an enumerated set in spec/ or docs/. spec/16_observability.md:14 and docs/reference/metrics.md:166 name the label and no values. CODE-9's new `slotFailureWorkspaceFinalize` stage constant therefore opens no spec or docs edit site. EVIDENCE: spec/16_observability.md:14, docs/reference/metrics.md:166.

FACT: `docs/` mentions `ShutdownRequest`/`PrepareWorkspace` in exactly one file, `docs/reference/adapter-contract.md`, which DOCS-2 stages. There is no second reader-facing mirror of the adapter RPC contract. `schemas/` carries no JSON schema over these messages. The docs edit list is complete for the wire change. EVIDENCE: `grep -rln "ShutdownRequest\|PrepareWorkspace" docs/`.

FACT: `docs/reference/metrics.md` has a `## Adapter metrics` section (:171) whose preamble and sibling rows carry the exact "Outside the default scrape set" phrasing CODE-9 tells the implementor to reuse. EVIDENCE: docs/reference/metrics.md:171,:173,:177-178.

FACT: `heldSession` is declared in `pkg/adapter/slotsession.go:310`, not in `holdstate.go`. The Files-touched attribution of the new `release func()` field to `slotsession.go` (non-spec-changes.md:3520) is therefore CORRECT under CODE-6's own "Go declares a struct's fields in the file that declares the struct" rule. A future lens will want to file this as a misattribution; it is not one. EVIDENCE: pkg/adapter/slotsession.go:305-313.

FACT: `onHoldTimeout`'s shared-close-context comment is at `pkg/adapter/holdstate.go:197-200` exactly as the Files-touched bullet cites, and `removeSlotTree`'s two non-`Shutdown` callers are at `slotsession.go:217` and `holdstate.go:254` exactly. Every `releaseSessionSlot`, `ReleaseSlotForTest`, `deregisterSlot` and `noteRuntimeStarted` call site the proposal enumerates is exhaustive against the tree. EVIDENCE: pkg/adapter/holdstate.go:197-200,:254; pkg/adapter/slotsession.go:217; and the grep sets for the four symbols.

WATCHOUT: CODE-1's deferred ordering is correct and reads backwards. `defer unlockSlot()` is registered at non-spec-changes.md:361 and `defer release()` at :399, so LIFO makes `release()` run first and the guard outlive the hold — which is what the comment at :309-313 claims. A lens reading the comment against the code order will think they disagree. They do not.

WATCHOUT: `shutdownReclaimOutcome` is called with a possibly-nil `cur` (non-spec-changes.md:349-350). The `!ok` arm is first in the switch, so the nil is never dereferenced. Not a defect.

MISTAKE: the `### Open` entry "CODE-1 comment phrasing — UNVERIFIED: non-spec-changes.md:72-77 still carries 'the runtime teardown runs for a session the pod's shared runtime process has been given'" is STALE. Lines 60-84 of the current file are the mid-session-conditioning paragraphs and carry no such sentence. Compaction should retire it.

DEFERRED [proposals/0081_.../0081_....summary.md:941]: the deliverable index gives CODE-5 the file list "(`pkg/gateway/sessionserver/start.go`, `pkg/gateway/podlifecycle/podclaim/slotclaimer.go`)". No CODE deliverable edits `slotclaimer.go`, it appears nowhere in `## Files touched on application`, and it needs no edit: `SlotClaimer.ReleaseSlot` already takes the `leaked` parameter CODE-4 threads to it (`pkg/gateway/podlifecycle/podsession/slotbinder.go:559` calls `claimer.ReleaseSlot(ctx, result.SandboxName, result.Recycle, leaked)`). What is true instead: CODE-5's file list is `pkg/gateway/sessionserver/start.go` alone. Not filed — the loop has refuted over-listing in a descriptive worklist twice (the CODE-4 Shutdown-call-site row, the CODE-6 heading list).

DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:1911-1912 area / CODE-9's method justification at :920-922]: still reads "is a method on `Binder` rather than a free function, because the series ... is labeled by `pool` and `k8s_pod_name` and is emitted through a `Binder` hook, both of which a package-level function can reach." The clause argues for the opposite conclusion; it should read "neither of which a package-level function can reach". This is the third recording of the same one-word inversion (the standing `### Deferred` entry names it). A fixer inside CODE-9 should take it.

DEFERRED [proposals/0081_.../0081_....non-spec-changes.md:2457-2461, the tier-1 "wire rule's two directions" case]: it says both directions are asserted "across `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup`, `AssignCredentials` and `Resume`". SCHEMA-1 gives `mid_session` to `PrepareWorkspaceRequest` and `FinalizeWorkspaceRequest` only (spec-changes.md field table; schemas/lenny-adapter.proto has no `mid_session` on the other three), so the mid-session-request-carrying-a-token direction is unconstructible for three of the five. What is true instead: the empty-token direction is testable on all five; the mid-session direction on two. Re-derived independently this round, matching the standing Deferred; not filed because the table's cells are severable and the conclusion is unaffected.

USEFUL [the standing `### Deferred` block]: reading it first cut the round roughly in half. Eleven of its entries against `non-spec-changes.md` are now FIXED in the staging (the `StartSession` row's unconstructible `req.GetBindAttempt()`, the CODE-9 expected-value cell, CONF-1's identity-versus-phase case, CONF-1's property count, the missing DOCS-3 section, the `deregisterSlotLocked` "read-only caller" rationale, the `start.go:2873-2879` cite, the `usage_test.go:233` "needs only the discard" claim, and the hold-refusal bullet's `applySlotRetryPolicy` attribution). Compaction should retire those nine rather than carrying them; only the three I re-record above are live.

OPEN: does any lens still owe a check on `Client.Shutdown`'s doc comment at `pkg/gateway/runtime/adapterclient/client.go:793-799`? After CODE-1 it is false on two counts ("the adapter closes its runtime" is now gated on an admitted start; "a request naming a session the adapter has already released is a no-op reporting a clean exit" is now the `absent` outcome). CODE-4 and CODE-7 both open that file. Go doc comments are outside criterion (d)'s enumerated surfaces, which is why I did not file it, but a fixer inside client.go should take it in the same edit as the `ShutdownReclaim` addition.

### [non-spec.14.review-kubernetes.1]

DECISION: Returned an empty findings list for the Kubernetes-idiom lens — BECAUSE the whole staging touches the apiserver at exactly three places, and each verified clean against the current tree: `failPhase`'s `drain` (CODE-8, a `DeleteClaim` it now skips on a typed refusal), `DrainSandbox`'s pod-annotation stamp (CODE-5's new `resumeOnPod` caller reaching the same shipped tail), and SPEC-4's re-keying of the occupancy projection's claim-deletion clauses. No CRD field, no finalizer, no status-subresource writer, no admission webhook and no reconcile is added or moved by any deliverable — ALTERNATIVES: filing the §6.2 "a pod in a warm-inventory phase with no claim projects `idle`" replacement as a single-writer violation (rejected: see the MISTAKE-adjacent note below — it narrows a pre-existing over-broad sentence rather than introducing the imprecision).

FACT: `pkg/gateway/podlifecycle/podsession/binder_envtest_test.go` does not exist, but a new file in that package CAN host envtest cases with no scaffolding: the package's test files are `package podsession_test` and the harness is the shared helper `envtest.Start(t)` from `tests/testinfra/envtest`, called at `binder_test.go:180` with the import at `:41`. This closes the standing UNVERIFIED at review-log.md:1546 ("Does binder_envtest_test.go exist as a reachable envtest home?"). — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder_test.go:3,41,180

FACT: the tier-2 envtest paragraph's three K8s claims all hold. `failPhase` calls `b.drain` outside the `leaseAssigned` block (pkg/gateway/podlifecycle/podsession/binder.go:1072-1082), `drain` is exactly `podclaim.DeleteClaim` (`:1200-1201`), and the gateway is not a writer of `Sandbox.status` — `DrainSandbox` stamps `lenny.dev/drain-request` on the Pod and says so in its own doc comment (slotbinder.go:586-600), while `ProjectOccupancyPhase` is the sole WPC-side writer. — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1079,1200; pkg/gateway/podlifecycle/podsession/slotbinder.go:586-600

FACT: SPEC-4's re-keying matches `ProjectOccupancyPhase` exactly. With a claim: `Bound→Claimed`, `Recycling→Claimed`/`SDKConnecting`, `Reserved→Reserved`, `Released|Failed→Draining`, unknown binding → `("", false)`. With no claim: `Current==Reserved→Idle`, `Current==Claimed→Draining`, anything else → `("", false)`, deliberately so the projection "never fights the warm-fill writer for an unclaimed pod". The proposal's cites `occupancy.go:128-140` (the no-claim switch) and `:57-59` (the recycle-encoded-in-phase comment) both resolve. — EVIDENCE: pkg/controller/warmpool/occupancy.go:49-143

WATCHOUT: the staged §6.2 replacement "a pod in a warm-inventory phase with no claim projects `idle`" is loose against the controller: for `warming` and the pre-idle `sdk_connecting` leg the occupancy projection returns `ok=false` and writes nothing, so the phase is the Sandbox-to-Pod reconciler's. It is NOT a finding, because the shipped clause it replaces ("a pod with no claim projects `idle`") is over-broad in exactly the same way and the replacement strictly narrows it; the qualifier's stated job (keeping the sentence a partition against the widened claim-deletion clause) it does perform. A future lens should not re-file this. — EVIDENCE: spec-changes.md:710; spec/06_warm-pod-model.md:80; pkg/controller/warmpool/occupancy.go:113-143

FACT: `SlotClaimer.ReleaseSlot` returns early on `leaked=true` with NO counter decrement and NO apiserver write at all, so the tier-4 assertion that the per-pod `SandboxClaim` "survives at `bound`" after an unanswered reclaim is correct, and a leaked slot's pod is retired only by the ceil-threshold drain, the maxSessionsPerPod drain or the WPC uptime drain. This is what makes CODE-5's new `resumeOnPod` accounting caller load-bearing rather than cosmetic: without it the §7.3 re-attach is the one leaked path nothing ever drains. — EVIDENCE: pkg/gateway/podlifecycle/podclaim/slotclaimer.go:818-836

FACT: `POD_NAME` IS delivered to the adapter container through the Downward API (`podspec.PodNameEnvVar`, rendered by `podNameEnv`), so `Server.podID` is populated in production and SPEC-6's `k8s_pod_name` label on the adapter series is producible. The review-log WATCHOUT at :3329 about an empty `podID` is a test-fixture property, not a production one. — EVIDENCE: pkg/controller/sandbox/podspec/podspec.go:175,904; pkg/adapter/server.go:36,372

UNVERIFIED: the §6.2/§4.6.1 "warm-inventory phase" qualifier is prose this loop may still want to tighten to "a pod the projection does not own", but only a spec-lane fixer can touch it and the spec lane is closed. Nobody should spend a round on it without first deciding it clears the bar; I judged it does not.

### [non-spec.14.review-mechanism.1]

DECISION: returned an empty findings list for the end-to-end mechanism lens on the combined spec+non-spec staging — BECAUSE every flow I traced origin-to-effect closes (mint → carry → stamp → compare → refuse/admit → compensate → account), every predicate I cross-read between the Design section, CODE-1/2/4/5/6/7/8/9, SCHEMA-1, DOCS-1/2/3, the checklist and the summary agrees, and the four residual discrepancies I did find are all in the already-refuted enumeration-bookkeeping class — ALTERNATIVES: the four are listed under DEFERRED below rather than filed.

FACT: the proposal files are BYTE-IDENTICAL between the `non-spec-r13` and `non-spec-r14` snapshots and the live tree; only the review-log moved. The last fix round that touched the staging was r12→r13 (the `Binder.Release` recycle-arm G3 correction, CODE-2's sdkwarm.go heading, the removing-arm recycle-scrub tier-1 case, and the two client-envelope tier-1 cases). Do not spend a round re-diffing r14. EVIDENCE: `diff -rq scratchpad/cp-snap/.../non-spec-r13 scratchpad/cp-snap/.../non-spec-r14` reports only the two review-log files.

FACT: the r13 fix round's new citations all resolve and say what the proposal says they say. `Binder.ReleaseSlot`'s two senders are `slotbinder.go:542` (`Adapter.Shutdown`) and `:574` (`ShutdownRecycle`); `Binder.shutdownAdapter`'s are `binder.go:2037` (`ShutdownRecycle`) and `:2043` (`Shutdown`), reached from `binder.go:1994` (recycle) and `:2002` (retire). `slotFailureSetup = "setup"` at `binder.go:291`, so the new envelope tests' `Stage: "setup"` is right for the `RunSetup` wrap at `slotbinder.go:303-304`. `writeSetupCommandError` branches on `status.Code(setupFail.Cause) == codes.FailedPrecondition` → 422 `SETUP_COMMAND_FAILED` with no `Retry-After`, else 503 fallback with `Retry-After`, at `pkg/gateway/sessionserver/start.go:239-249`, reached through the `*SetupCommandFailure` arm in `writePodClaimError` at `:87-90`. The precedent harness is at `slotretry_test.go:519-548`.

FACT: the production `Shutdown` sender set is EXACTLY the five rows CODE-4's unconditional-teardown table names, verified by `grep -rn "\.Shutdown(\|\.ShutdownRecycle(" --include=*.go pkg/ cmd/ tests/` filtered to `adapterclient` receivers: slotbinder.go:542, :574, binder.go:2037, :2043, cmd/lenny-gateway/user_revocation.go:129. There is no sixth. Both exported forms really do funnel through one unexported builder `Client.shutdown` (`pkg/gateway/runtime/adapterclient/client.go:812-823`), so the "set in a single place" claim holds. EVIDENCE: as cited.

FACT: `binder.go:1327`'s `cl.PrepareWorkspace` is inside `stageWorkspace` (declared `binder.go:1283`), which both `Binder.Prepare` and `materializeSlot` call. It is the ONLY `PrepareWorkspace` sender besides `upload_to_session.go:128`. Every `PrepareWorkspace` frame already carries the session id (`sendUpload`, client.go:268-283), and an upload-free plan sends zero frames, so the first-frame latch and the "sends no PrepareWorkspace" branch are both consistent with the shipped streamer. EVIDENCE: pkg/gateway/runtime/adapterclient/client.go:247-285.

FACT: the two metric gates behave exactly as CODE-9 claims, and I re-derived both. `tests/tier11_docs/adapter_metric_catalog_test.go` derives its subject by regex over `Name:` literals in `pkg/adapter/metrics.go` (:26, :56) and errors when a registered name is absent from `docs/reference/metrics.md` (:97) or from §16.1 with no `specCatalogPending` entry (:110) — so registering the untokened series obliges the docs row and the §16.1 row and nothing else. `spec161Metrics` (catalog_test.go:15) is a HAND-MAINTAINED list, not derived from spec/16, so a §16.1 row landing at S6 with no `spec161Metrics` entry fails nothing, while entering the adapter series there WOULD fail `TestMetricCatalogIsCompleteAgainstSpec161` (:188-197). The deliverable's asymmetric treatment is correct. EVIDENCE: as cited.

FACT: the `Shutdown` defer order in CODE-1's staged body is correct as reasoned, and it is the non-obvious half. `defer unlockSlot()` is registered FIRST and `defer release()` (the reclaim hold) SECOND, so LIFO runs `release()` then `unlockSlot()` — the guard outlives the hold, which is what the comment claims. A reader who checks this by reading order rather than by LIFO will conclude the opposite. EVIDENCE: non-spec-changes.md:365-372, :393-401.

FACT: the tier-7a "fifth arm" (the interval between CODE-1's two decisions) is mechanically reachable and I walked it. `releaseSessionSlotUnderGuard` routes through `reclaimSlotLocked`, so its hold is open only for the duration of its own destruction and is cleared by defer before the successor's `AssignCredentials` runs; `AssignCredentials` is unguarded per the derivation table, so it can create the successor entry while the parked `Resume` still holds the guard; the blocked `Shutdown` then wakes, re-decides `superseded`, and removes nothing. No step of that rendezvous is barred by the hold or by the guard. Do not re-derive it.

FACT: DOCS-1's three source clauses are present VERBATIM at `docs/reference/state-machines.md:138` ("A pod with no claim projects `idle`", "a claim deleted on a recycling pod under its limits projects `idle`", "or a claim deleted on a pod with `recycle.enabled: false`"), and the four DOCS-2 tier-11 substrings the proposal adds to `requireAllContain` ("no other bound session", "a session whose start the adapter has admitted", "either the bind attempt", "reclaimed", "absent") are each present in the staged one-line `Shutdown` row. The `receiving_uploads ──→ slot_cleanup` edge string DOCS-1 adds to `generalSlotEdges` matches the arrow glyph the shipped slice uses (`per_slot_substate_scope_doc_reconciliation_test.go:32-37`) and the glyph SPEC-4's staged fence writes (spec-changes.md:769). Both doc gates will resolve.

WATCHOUT: `pkg/adapter/session.go` imports `"log"`, not `"log/slog"` (session.go:8). The proposal says the file "gains a `log/slog` import" for the `slot_guard_not_acquired` warning. Both imports coexisting is legal Go and is not a defect, but a reviewer grepping for an existing slog import in that file will find none.

DEFERRED [summary.md]: the deliverable index's CODE-5 entry names `pkg/gateway/podlifecycle/podclaim/slotclaimer.go` among CODE-5's files (summary.md:941). CODE-5 stages no edit there: its Targets are `applySlotRetryPolicy`, `bindConcurrentSlot`'s reserved branch, `resumeOnPod`'s failure branch, the `slotBinder` interface and `isTransientPodClaimError`, all in `pkg/gateway/sessionserver/start.go` (non-spec-changes.md:1127-1130), and the `## Files touched` list carries no `slotclaimer.go` bullet. What is true: CODE-5 touches `pkg/gateway/sessionserver/start.go` alone. Below the bar as a finding (same class the loop refuted for CODE-6's heading), but it is a false file attribution and reconciliation can drop it for free.

DEFERRED [summary.md]: the "What changes" bullet for the start confirmation (summary.md:~72) lists `pkg/adapter/runtimegeneration.go`, `pkg/adapter/session.go`, `pkg/adapter/resume.go` and omits `pkg/adapter/sdkwarm.go`, where CODE-2 now stages a whole refusal arm. The r13 fix corrected CODE-2's own heading and the deliverable index (summary.md:938) but not this bullet. What is true: the confirmation is staged at three sites, the third being `pkg/adapter/sdkwarm.go:261`. The bullet's prose ("on both RPCs whose admitted start the gateway's compensation can race") is still literally true, so only the file list lags.

DEFERRED [implementation-checklist.md]: S16 (CODE-1) lists "Depends on: S1, S2, S4, S9, S10, S11, S13, S14" and omits S15. CODE-1's staged handler calls `s.lockSlotGuard(ctx, sessionID)` (non-spec-changes.md:393), which S15 declares and S14 does not; CODE-1's own Targets say the two guard hand-out forms "are CODE-6's, which lands first" (:196-199). Harmless in practice because the checklist executes as one ordered sequence and S15 precedes S16, so this is a missing documented edge rather than an ordering break. What is true: S16 depends on S15 as well as S14.

DEFERRED [non-spec-changes.md]: `stageWorkspace` is never named as taking the new `bind_attempt`/`mid_session` arguments, although it is the shared `PrepareWorkspace` sender for BOTH minting paths (`Binder.Prepare` and `materializeSlot`) and `Client.PrepareWorkspace`'s signature changes under CODE-4. The Files-touched bullet for `binder.go` covers it only under "`Binder.Prepare`'s mint, carry". What is true: `stageWorkspace` (`pkg/gateway/podlifecycle/podsession/binder.go:1283`) gains the two parameters and both callers pass them. Below the bar (the loop refuted the equivalent fixture-threading finding as mechanical consequence), but one clause in CODE-4's Targets would close it.

MISTAKE (mine, nearly filed then withdrawn): "DOCS-2's staged `Shutdown` row contradicts itself — it says `superseded` means 'nothing was released', then two sentences later says the slot release 'runs whenever the adapter holds an entry for the named session'." The spec's §4.7 row carries the same unconditional sentence, but puts it FIRST and follows it with an explicit "[Section 4.7.1] states the named rules that decide both" pointer; DOCS-2 drops that pointer (docs may not cite spec sections) and moves the unconditional sentence LAST, so the narrowing is weaker on the published page. I withdrew it: the row's own outcome sentence states the narrowing before it, the sentence's actual contrast is bound-vs-unbound, and the loop has refuted the "general statement narrowed by the specific rule in the same block" pattern five times (review-log.md:4137). A future lens that rediscovers this should read that refutation before filing.

USEFUL [orchestrator brief]: the instruction not to re-derive the token mechanism and to treat the checklist's S1/S7/S17 contradictions as reconciliation's work saved the whole round from re-litigating settled ground. The pointer to review-log.md:3674 (the `removeSlotTree` / `exited_cleanly` handoff) was already discharged — that finding is on the refuted list — so it cost one lookup and nothing more.

### [non-spec.14.review-operational.1]

DECISION: returned an empty findings list for the operational-consistency lens (metrics, alerts, conditions, operator docs) — BECAUSE every observability surface the staging touches reconciles end to end against the tree, and the residual imprecisions I found are all prose accuracy in the proposal's own rationale rather than defects in the applied spec, the staged code, or the tests. ALTERNATIVES: I built and dropped four; each is written out below so a later round does not re-derive them.

FACT: the whole gateway/adapter metric wiring CODE-9 cites is verbatim correct in the tree. `gatewaymetrics_credential.go:185-193` is the `lenny_slot_failure_total` collector; `gatewaymetrics.go:1170-1175` is `IncSlotFailure(errorType, pool, podName)`; `binder.go:137` is `SlotFailure func(errorType, pool, podName string)`; `slotbinder.go:353-361` is `recordSlotFailure`; `metricsbackfill.go:149` is `w.podBinder.SlotFailure = gwMetrics.IncSlotFailure`. Do not re-verify these. EVIDENCE: pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go:189-192; gatewaymetrics.go:1170-1175; pkg/gateway/podlifecycle/podsession/binder.go:137; slotbinder.go:358; cmd/lenny-gateway/metricsbackfill.go:149

FACT: `spec161Metrics` is a HAND-WRITTEN list in `pkg/observability/metrics/catalog_test.go`, not derived from spec/16. Two tests reconcile it both ways against `MetricCatalog()` (`TestMetricCatalogIsCompleteAgainstSpec161` at :186-197, `TestMetricCatalogHasNoUnspecifiedMetrics` at :199-211). So CODE-9's claim that entering the adapter series there would turn the step red is exactly right, and its claim that the four shipped adapter-deferral §16.1 rows are absent from both is also right. EVIDENCE: pkg/observability/metrics/catalog_test.go:150-211; spec/16_observability.md:186-189

FACT: `tests/tier11_docs/adapter_metric_catalog_test.go` iterates only over names the regex finds in `pkg/adapter/metrics.go`, then requires each to appear in BOTH `docs/reference/metrics.md` and `spec/16_observability.md` unless listed in `undocumentedAdapterMetrics` or `specCatalogPending`. That makes CODE-9's "needs no edit" claim correct AND makes the S6-before-S10 ordering safe: a §16.1 row for an unregistered metric is invisible to the sweep. EVIDENCE: tests/tier11_docs/adapter_metric_catalog_test.go:81-131

FACT: the alert half of this lens is structurally inert and I re-confirmed it. No rule in `pkg/alerting/rules/rules.go` and no expression in `charts/lenny/files/alerting-rules.yaml` references any `lenny_slot_*` or `lenny_adapter_leaked_slots` series, and no runbook under `docs/runbooks/` does either. The four `error_type`-keyed alert expressions key on artifact-upload, oauth-token, warmpool-warmup and llm-translation series, so CODE-9's new `slotFailureWorkspaceFinalize` label value orphans nothing. No doc or spec text enumerates `lenny_slot_failure_total`'s `error_type` values, so the new stage constant needs no catalog edit. EVIDENCE: pkg/alerting/rules/rules.go:358,636,1110,1627; spec/16_observability.md:14; docs/reference/metrics.md:166

FACT: `docs/reference/state-machines.md:138` is the ONLY docs mirror of the §4.6.1/§6.2 occupancy projection, and all three clauses DOCS-1 quotes are verbatim there. The projection's "partition" claim holds against the code because `ProjectOccupancyPhase` switches only on `state.Reserved` / `state.Claimed` and returns `ok=false` for every other current phase, so a claim deleted while the pod projects `sdk_connecting` is not the projection's to answer and needs no clause. Do not file that as a partition gap. EVIDENCE: docs/reference/state-machines.md:138; pkg/controller/warmpool/occupancy.go:128-145

FACT: DOCS-3's four "reads" quotations are byte-verbatim in the shipped `SETUP_COMMAND_FAILED` row, and DOCS-2's new gate substrings all occur in its staged `Shutdown` row (`no other bound session`, `a session whose start the adapter has admitted`, `either the bind attempt`, `reclaimed`, `absent`), as do the four the shipped `TestAdapterContractNamesTheShutdownRPCUnderItsWireName` already asserts. EVIDENCE: docs/reference/error-catalog.md:129; docs/reference/adapter-contract.md:75; tests/tier11_docs/basic_level_echo_stamp_doc_reconciliation_test.go:302-307

FACT: the R12 claim-register row ("Adapter metric names carrying the retired channel spelling ... until R12 adds the adapter metrics endpoint and the catalog entries") is the standing adapter-scrape deferral the summary and SPEC-6 point at, so that reference resolves and the new adapter counter owes no new register row. EVIDENCE: tests/claim-map.json (deferral_id R12); .claude/rules/channel-naming.md N4; spec/28_communication-channels.md:47-48

DEFERRED [non-spec-changes.md, CODE-9's adapter-series paragraph]: it states "SPEC-6's row for this series carries the deferral wording **verbatim**, so it follows the §16.1 rows that already carry it." That is false. SPEC-6's staged row reads "Adapter-side; not scraped until the adapter metrics endpoint is wired" (spec-changes.md:1000), while the four §16.1 rows that carry the deferral read "emitted by the adapter process inside the agent pod and therefore outside the [§16.9](#169-prometheus-scrape-targets-and-crds) default scrape target set until a deployer wires an adapter scrape target" (spec/16_observability.md:186-189). What is true instead: the staged row states the same fact in its own words and cites no §16.9 anchor. I did not file it: the conclusion the sentence supports (no `catalog.go` row, no `spec161Metrics` entry) is independently and correctly justified two sentences earlier by the two-way reconciliation, no gate reads the phrasing, and no operator is misled by the staged wording. A fixer in CODE-9 should drop "verbatim" or align the staged row to the sibling phrasing.

MISTAKE (mine, nearly filed, four candidates dropped on verification):
(a) "§16.1.1's `k8s_pod_name` `Used on` column becomes non-exhaustive." Dropped: §16.1.1's normative rule (spec/16:293) is only that the label appear in the table, which it does; the `Used on` column is descriptive, no gate reads it, and `lenny_adapter_leaked_slots` already ships with a `pod_id` label that appears in no §16.1.1 row at all.
(b) "the adapter cannot populate `k8s_pod_name`." Dropped: `Server.podID` is read once from the Downward API `POD_NAME` at construction and is already the source for `ReportSessionScrub`'s pod argument, so a counter vec fed from it is implementable. `pkg/adapter/metrics.go` registering no `k8s_pod_name` series today is a fact about the current set, not a blocker. EVIDENCE: pkg/adapter/server.go:372-380; pkg/adapter/sessionscrubreporter.go:70-78
(c) "`docs/reference/adapter-contract.md:81`'s `ReportSessionScrub` row ('at each session release') is falsified by SPEC-3's withheld report and is in no edit list." Dropped: the staged §5.2 text redefines a pre-`running` cleanup as NOT a session release ("A cleanup that reclaims a slot the pod's shared runtime process was given is a session release like any other"), so the row stays literally true, and DOCS-2's staged `Shutdown` row on the same page states the narrowing explicitly.
(d) "checklist S6 says §16.1 gains a row 'for each counter S10 emits' but S10 emits neither; S16 and S19 do." Dropped as bookkeeping polish that misleads no implementor, and the checklist is the reconciliation pass's to rebuild.

USEFUL [Standing context Settled, review-log.md:253]: "No alert anywhere references any slot metric ... the alert half of the operational lens is structurally inert." Saved a full alert-catalog derivation; I spot-confirmed it rather than rebuilding it.
USEFUL [Standing context Settled, review-log.md:252,254,255]: the three pre-existing metric divergences (`adapter_metric_catalog_test.go`'s source of truth, `lenny_adapter_leaked_slots` absent from §16.1/metrics.md, `lenny_slot_pod_replacement_total` registered with `pool` alone against §16.1's `pool, k8s_pod_name`) are each real and each pre-existing. Knowing they were already adjudicated kept me from filing three non-findings.

### [non-spec.14.review-performance.1]

FACT: The snapshot chain is flat again this round. `diff -rq` of `non-spec-r14`, `non-spec-r14-start`, `non-spec-r13` and `non-spec-r13-start` against the live proposal all return nothing but the review-log; the newest snapshot that differs is still `non-spec-r12-prefix` (summary.md and non-spec-changes.md only). The "read the changed sections first" instruction had no changed sections to point at. EVIDENCE: scratchpad/cp-snap/0081_.../non-spec-r14

FACT: `slotCleanupBudget`'s divisor cannot be zero on either path that reaches it, so the obvious integer-divide-by-zero panic is not there. The resume path normalises: `maxConcurrentSessions(match.MaxConcurrentSessions)` clamps to a floor of 1 before the `ResumeRequest` is built. The `materializeSlot` path is gated: `slotBindRequest` is only built under `if match.MaxConcurrentSessions > 1` at all three call sites. A zero bound is a real resolved state (no `sessionPolicy` mirror row), so the check was worth doing. EVIDENCE: pkg/gateway/sessionserver/start.go:3353-3358, :4029, :2139, :2351, :2465; pkg/gateway/podlifecycle/podsession/resolve.go:358-369; pkg/gateway/podlifecycle/podsession/resolve_test.go:407-409

FACT: The §5.2 per-slot cleanup formula the budget helper reuses is verbatim in the spec, including the 5s floor and the admission rule that rejects `cleanupTimeoutSeconds < maxConcurrentSessions x 5`. EVIDENCE: spec/05_runtime-registry-and-pool-model.md:545

UNVERIFIED: On an exclusive pool the resume compensation's divisor is 1, so its budget is the whole `cleanupTimeoutSeconds` (the spec's own examples are 30 and 60), spent on a `context.WithoutCancel` context before the resume error returns to the caller. I judged this below the bar: it is latency on a failure path, bounded, derived from the spec's own constant, and well inside §7.3's 300s `resuming` watchdog. A later lens that wants to press it should price it against a stated start/resume budget rather than against the formula. EVIDENCE: non-spec-changes.md:834-843, :887-889, :1073-1096; spec/05_runtime-registry-and-pool-model.md:166,412

FACT: `slotGuards` cannot grow without bound in any pool configuration, not only the one CODE-6's lifetime paragraph names. The paragraph justifies the bound with `recycle.maxSessionsPerPod`, which is required only when `recycle.enabled: true`; the remaining concurrent configuration is §5.2's "bounded cohort" (N, `false`), which serves at most N sessions and then terminates. Both are bounded, so the incomplete justification is not a defect. EVIDENCE: non-spec-changes.md:1672-1680; spec/05_runtime-registry-and-pool-model.md:424-427

FACT: `k8s_pod_name` on the new gateway counter needs no new cardinality argument. §16.1's label table already sanctions it for "slot failure and replacement metrics", and the shipped `lenny_slot_failure_total` collector carries it with an inline comment saying so. EVIDENCE: spec/16_observability.md:297, :14; pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go:185-192

FACT: `ensureSlotStateLocked`'s create arm already runs `slotlayout.EnsureTree` (a filesystem mkdir) under `s.mu` in the shipped tree, so CODE-6's staged create body adds no new I/O under the adapter's registry mutex. EVIDENCE: pkg/adapter/slot.go:105-125

WATCHOUT: The §10.1.4 pass-2 context split changes a bound, and the shipped comment that justifies the old one is load-bearing. Today `onHoldTimeout` mints one 10s context shared by every member, deliberately, "so only the last member's close consumes the grace". CODE-6 splits it into a shared guard-acquisition deadline plus a per-member close context, which makes the pass's worst-case wall time `10s + N x 10s` instead of `10s`. The proposal carries the old rationale forward explicitly (a non-last close on a shared runtime returns without touching the child), which is what keeps the split honest, and §10.1.4 states no wall-clock bound on the pass. Do not file this without first finding a stated bound it breaches. EVIDENCE: pkg/adapter/holdstate.go:195-205; non-spec-changes.md:3527-3535, :3364-3371; spec/10_gateway-internals.md:58

FACT: The perf/scalability surface of this proposal creates no new control-plane write. No staged deliverable writes to etcd, Postgres or Redis per bind attempt; the only new per-unit-of-work traffic is one compensating `Shutdown` RPC per *failed* bind, on the connection the failed stage already holds. The two accounting changes that could move etcd traffic (CODE-8 skipping `failPhase` on a refusal, CODE-5's new `leaked` accounting on the reserved branch and the §7.3 re-attach) move it in opposite directions and are both failure-rate-proportional. The proposal prices the churn direction at non-spec-changes.md:3390-3395. EVIDENCE: non-spec-changes.md:152-156, :936-945, :3390-3395

DECISION: Returned an empty findings list — BECAUSE every candidate my lens generated is either already adjudicated on the record (the guard's teardown latency, the §11.4 window, the shared §10.1.4 budget, the stranded-token residue) or resolves to bounded-cost-on-a-failure-path — ALTERNATIVES: filing the exclusive-pool resume budget (logged as UNVERIFIED above rather than filed, because it names no breached budget) and filing the `slotGuards` lifetime justification (refuted by my own reading of §5.2's configuration table).

### [non-spec.14.review-reliability.1]

FACT: the proposal text did not move between r13 and r14 — `diff -rq` across the r14 snapshot shows only `review-log.md` differing, so the "read the changed sections hardest" instruction had no changed sections to point at this round. EVIDENCE: scratchpad/cp-snap/0081_.../non-spec-r14 vs proposals/0081_...

FACT: `Binder.assignSlotCredentials` mints EVERY §4.9 lease (pool leases through `AssignProto`, user leases through `MintProto`) BEFORE it issues `cl.AssignCredentials`, so an attempt refused at the credential-assignment RPC already holds minted leases. This falsifies CODE-4's staged comment "On a typed refusal this attempt minted none". EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:379, :393, :403; non-spec-changes.md:995-996.

FACT: the adapter enforces the §5.2 per-slot cleanup timeout `max(cleanupTimeoutSeconds/maxConcurrentSessions, 5)` NOWHERE. `grep -rn "CleanupTimeout" pkg/adapter/*.go` returns exactly one non-test hit, in the WHOLE-POD scrub. Any staged sentence that keys a disposition on "a cleanup that does not finish within that timeout" keys it on a state the reference adapter cannot detect. EVIDENCE: pkg/adapter/podscrub.go:128; spec/05_runtime-registry-and-pool-model.md:545.

WATCHOUT: `MaxConcurrentSessions` cannot reach `slotCleanupBudget` as zero, so the integer division is safe — do not re-derive this. The concurrent bind path is gated on `match.MaxConcurrentSessions > 1` (start.go:2351, :2465) and the resume path normalizes through `maxConcurrentSessions()` to a floor of 1 (start.go:3353-3358, applied at start.go:4029). EVIDENCE: pkg/gateway/sessionserver/start.go:2351,:3353,:4029.

FACT: `RecordSessionScrub` increments `sessions_served` before it branches on `leaked` and holds no per-session dedup, exactly as CODE-1 claims, so the adapter-side one-report invariant is load-bearing and cannot be absorbed gateway-side. EVIDENCE: pkg/gateway/mcpfabric/delegationtree/leasecontrol/scrubreport_server.go:450-481.

DEFERRED [proposals/.../non-spec-changes.md:2763]: the test bullet "**The compensation is suppressed on either typed refusal**, and so is the credential release ... the `fakeAssigner`'s release list stays empty" is only vacuously satisfiable. `fakeAssigner.released` records `ReleaseSession` calls (binder_test.go:301,:322), and `releaseAttemptCredentials` releases by lease identifier and calls `ReleaseSession` never, so that assertion is true on every path and discriminates nothing. What is true instead: an attempt refused at `AssignCredentials` must still release the identifiers it minted, and that is the property the arm should assert. Filed as a finding this round.

MISTAKE (nearly filed, recorded so the next agent does not spend the round on it): "the §10.1.4 pass-1 hold set is opened for every member at once while pass 2 terminates them one at a time, so the last member's identifier is held for N x close-budget". True, but it is a latency property of a fail-closed refusal, the identifier is held only against binds for sessions the pass is terminating, and the shipped pass already deregistered all members in one critical section. Below the bar.

OPEN: does any §4.9 lease expiry reclaim a lease minted by an attempt refused at `AssignCredentials` if an implementor guards `releaseAttemptCredentials` behind the refusal check? If the lease TTL is the only reclaimer, the window is the whole lease lifetime and the credential's `active` counter is wrong for that long. A credential-lifecycle lens should price it.

### [non-spec.14.review-security.1]

DECISION: Returned an empty findings list for the security lens in round 14 — BECAUSE the four security-bounding surfaces this change touches each resolve to a trusted source or a fail-closed refusal, verified against the tree rather than the proposal's own text. ALTERNATIVES: I built and then discarded four candidate findings, each listed below with the evidence that killed it, so a later security lens does not re-derive them.

FACT: The `unconditional_teardown` caller table at non-spec-changes.md:817-825 is exhaustive against the tree. `grep -rn "\.Shutdown(\|ShutdownRecycle(" --include=*.go pkg/ cmd/` returns exactly five adapter-client teardown senders — pkg/gateway/podlifecycle/podsession/slotbinder.go:542, :574, pkg/gateway/podlifecycle/podsession/binder.go:2037, :2043, cmd/lenny-gateway/user_revocation.go:129 — and the table names all five. No sixth production sender exists, so no teardown can reach the two-field precondition with neither field set and silently become a no-op. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:542; cmd/lenny-gateway/user_revocation.go:129.

FACT (candidate 1, refuted before filing): the §5.2 leak bound does NOT silently relax when the attempt-scoped disposition replaces the release-error test. CODE-5 passes `sbe.Leaked || relErr != nil` at `applySlotRetryPolicy` (non-spec-changes.md:1159-1163), which is a disjunction over the shipped discriminator rather than a replacement of it, so a compensation that did not complete still reaches the persistent `health.RecordLeak` / `slots.MarkLeaked` arm rather than the windowed `RecordFailure` arm. Had it passed `sbe.Leaked` alone, a leaked slot would have aged out of the 5-minute window and the `ceil(maxConcurrent/2)` drain would never fire while `SlotClaimer.ReleaseSlot` still withheld the decrement. EVIDENCE: pkg/gateway/sessionserver/start.go:2833-2852 (the shipped windowed-vs-persistent split); pkg/gateway/podlifecycle/podclaim/slotclaimer.go:832-841 (`if leaked { return false, nil }`).

FACT (candidate 2, refuted before filing): CODE-1's move of the `ReportSessionScrub` gate from `bound` to `live` does reduce `sessionsServed` increments, and `sessionsServed` is a residual-state bound (it drives the §5.2 `maxSessionsPerPod` retirement and, on a leaked outcome, `ledger.RecordLeak`). It is authorized and compensated in the staged spec, not silently dropped: spec-changes.md:643 states the withheld report and names the whole-pod boundary as where the residue is accounted instead ("the occupancy-zero whole-pod scrub ... removes every per-slot workspace tree and every per-slot credential file the pod still holds and verifies their absence"), and the pre-`running` failure is accounted on the response through `exited_cleanly` into `sbe.Leaked`. EVIDENCE: pkg/gateway/mcpfabric/delegationtree/leasecontrol/scrubreport_server.go:451-481 (what `RecordSessionScrub` drives); spec-changes.md:643.

FACT (candidate 3, refuted before filing): the started-session rule `st.started && !r.allowStarted` with `allowStarted: false` on `AssignCredentials` does NOT break §4.9 credential renewal. Proactive renewal pushes through `RotateCredentials`, not `AssignCredentials`, and `rotateCredentialsSlot` resolves through `slotStateLocked` rather than `ensureSlotStateLocked`, so it never meets the new phase gate. EVIDENCE: cmd/lenny-gateway/cred_renewal.go:211-217 ("pushes a proactively renewed lease to its session's pod via the §4.7 RotateCredentials RPC"); pkg/adapter/slotcreds.go:67-70.

FACT (candidate 4, refuted before filing): `RotateCredentials` and `extendCredentialLeaseSlot` take no per-slot guard and write `credentials.json` outside the guarded set, but they cannot race the reclaim's `removeSlotTree`. Both hold `s.mu` for their whole body and read the registry through `slotStateLocked`, and the reclaim's deregistration happens under `s.mu` before it releases the lock to do path work, so a rotation arriving after the deregistration is refused `FailedPrecondition`/`NotFound` and one arriving before completes under the lock. The derivation table's omission of them is correct. EVIDENCE: pkg/adapter/slotcreds.go:67-91; pkg/adapter/credentials.go:82-114.

FACT: The token is treated as a capability and is never written anywhere a client or a log can read it. `errSlotBindAttemptSuperseded` names the slot identifier only (non-spec-changes.md:1349-1356), no response field carries a token, and the `slot_guard_not_acquired` warning logs `slot_id` and `caller`. A future lens looking for a credential-material leak on this surface should check the same three places rather than re-reading the whole staging. EVIDENCE: non-spec-changes.md:1354-1356; non-spec-changes.md:449-452.

USEFUL [the round-13 ledger's snapshot rule]: `diff -rq` across the snapshot chain was the cheapest first move. `non-spec-r14-start` is byte-identical to the live proposal and `non-spec-r14` differs only in the review log, so every non-log file is unchanged since round 13 — which is why the reading-order instruction to "read the changed sections first and hardest" had nothing to point at this round.

USEFUL [the `slot_was_started` WATCHOUT at review-log.md:2203]: the orchestrator brief for round 14 still asserts `ShutdownResponse` "gains slot_was_started (4)". It does not exist anywhere in the staging. I checked before deriving anything from it and lost only one grep; without the ledger entry this would have been a phantom missing-test finding under the security lens (an unobserved fail-closed signal).

### [non-spec.14.review-test-coverage.1]

DECISION: Returned an EMPTY findings list for the test-coverage lens in round 14 — BECAUSE the proposal is byte-identical to both the r14 and the r13 snapshots (`diff -ru scratchpad/cp-snap/.../non-spec-r14 proposals/0081_... -x '*review-log*'` returns nothing, and `diff -q` against the r13 `non-spec-changes.md` likewise), and an independent walk of `## Testing` (non-spec-changes.md:2381-3260) against every staged deliverable found each changed behaviour pinned at the tier `.claude/rules/test-coverage.md` routes it to. ALTERNATIVES: none reached the bar; the candidates I checked are listed below.

USEFUL [non-spec.13.review-test-coverage.1]: its tier map of `## Testing` is accurate and saved a full re-derivation. I re-verified the map's spine (tier 1 bindattempt/orderings/slotsession/sdkwarm/socketruntime/holdstate/slotstate + four gateway files; tier 2 binder_envtest two arms; tier 3 tests/tier3_contract/adapter_bind_attempt/ descriptor + behavioural; tier 4 concurrent_workspace three arms + recycle_scrub_path one case; tier 7a slot_bind_attempt_race + slot_reclaim_hold_race; tier 8 compensation_loss; tier 9 slot_credential_reclaim_fence three arms; tier 10 slot_bind_attempt_conformance; tier 11 two shipped gates extended in place + slot_compensation_metric_reference) and it still holds.

FACT: the §11.4 revoke fan-out issues its teardown as `bind.Adapter.Shutdown(callCtx, bind.SessionID, reason, userTerminateDeadline)` — the shared `Client.Shutdown` builder, not a hand-built `ShutdownRequest` — so CODE-4's "the field is set in a single place" covers it and it needs no call-site-specific test under the new two-field precondition. EVIDENCE: cmd/lenny-gateway/user_revocation.go:129. Checked because a non-compensating caller that failed to set `unconditional_teardown` would hit the fail-closed `INVALID_ARGUMENT` arm and silently break user revocation; it does not.

FACT: the orchestrator brief's claim that "ShutdownResponse keeps slot_reclaim and gains slot_was_started (4)" is false against the current staging: `grep -n "slot_was_started\|SlotWasStarted"` over every proposal file except the review log returns nothing, and SCHEMA-1's field table (non-spec-changes.md:2137-2146) adds `ShutdownResponse.slot_reclaim` at 3 and nothing else on that message. Do not go looking for a test that pins a field the proposal does not add. EVIDENCE: non-spec-changes.md:2145; brief text.

UNVERIFIED: SCHEMA-1's tier-3 descriptor gate is described as pinning "each of the nine fields ... by message, number and type, and both new `ErrorCode` values by number and name" (non-spec-changes.md:2884-2887); it does not say the three `SlotReclaimOutcome` enum values are pinned by number and name, though the field's type pin reaches the enum. I judged this below the bar (the values are exercised behaviourally at tier 3 and tier 10, and every outcome has a named case). A later lens that disagrees should argue it as a wire-contract pin rather than as a coverage gap.

### [f5.other-proposals.0073-holdstate]

DECISION: extended the `## Impacts on other proposals` row for 0073 (summary.md) with the CODE-6 close-context replacement. The row's third column now records, after the `pkg/sandbox/slotstate` edge-list sentence, that CODE-6 replaces the single per-pass close context 0073 landed at `pkg/adapter/holdstate.go:197-203`: `onHoldTimeout`'s ten-second context becomes the guard-acquisition deadline alone, each member mints its own ten-second close context inside `terminateHeldSession`, and the explanatory comment is rewritten to state the split, with the grace bound preserved. Status column, action column and every other row are verbatim as they stood.

FACT: the shared close context and its comment are 0073's own deliverable, not an implementer's incidental choice. `git blame -L 195,206 pkg/adapter/holdstate.go` attributes lines 197-203 and 205 to commit `3997f502b2` (2026-08-22, "Terminate every started session when the coordinator hold times out"), whose trailer names 0073; only line 204, the call site, is `4c8e48134d` (0076). 0073's own staged text designs the shared context.

FACT: the grace bound survives the split. `SocketRuntimeProcess.Close` returns early while a sibling slot is still active (`pkg/adapter/socketruntime.go:441-446`), so under per-member contexts only the last member's close consumes real grace.

WATCHOUT: the 0080 row already describes the same `holdstate.go` split, under 0080's §1.20 file-collision inventory rather than as a statement about 0073. The two are about different proposals and neither is redundant; do not collapse them.

### [f5.other-proposals.0077]

DECISION: added a row for proposal 0077 (pre-materialize a workspace on a warm pod) to `## Impacts on other proposals` in summary.md, between the 0079 row and the first programme-step row. It records 0077 as an early draft that stages no text, so nothing of it loses its subject; names its per-slot derivation at slot assignment as newly constrained by CODE-6's entry-creation gate, the bind attempt token requirement and the reclaim hold with the per-slot guard over the per-slot tree; states that its §7 question 5 is unchanged because widening the §5.2 whole-pod scrub to enumerate the registry is a stated non-goal here; and leaves its other targets recorded as untouched. No other row was edited.

FACT: 0077 stages nothing and names targets only. `proposals/0077_new_pre-materialize-a-workspace-on-a-warm-pod.md:3-7` heads it "EARLY DRAFT OF A NEW CAPABILITY. Not reviewed, not converged, not ready for sign-off", `:8` carries `Date: 2026-08-19`, `:165` reads "Targets only, with no text staged" and `:227` reads "Not enumerable until §7 is answered". `git log -1` on the file returns `8364087f9`, 2026-08-19, so the date is both its own and its last-commit date.

FACT: the derivation target is on this proposal's chokepoint. `0077:136` places the per-slot derivation "When a slot is assigned" and `0077:171` names `pkg/adapter/` "per-slot derivation at assignment". In the shipped tree the per-slot tree is created by the same call that creates the registry entry (`pkg/adapter/slot.go:105-125`, `ensureSlotStateLocked` calling `slotlayout.EnsureTree` on a miss), which is exactly what CODE-6 fences.

FACT: 0077's spec targets do not collide. Its `spec/06` target is the warm-pod checklist and its `spec/05` target is the pool configuration (`0077:165-174`); SPEC-4 edits §6.2's per-slot fence and the occupancy projection and SPEC-3 edits §5.2's scrub model and slot-cleanup action list, so the two sets are disjoint.

WATCHOUT: the 0077 row asserts nothing about the recycle boundary beyond the non-goal at summary.md's "Widening the §5.2 whole-pod scrub to enumerate the registry as its source". If a later round takes that non-goal back, this row's §7-question-5 sentence becomes false and is the site to correct.

### [f5.cleanup]

FACT: the summary was already conforming when this firing's cleanup pass ran, and nothing was moved, renamed, renumbered or deleted. Its headings are, in order, `# Summary: A failed session bind leaves a stale adapter slot registry entry`, `## Summary` (holding only `**Problem statement.**`, `**What changes.**`, `**Decisions.**` and `**Watch out for.**`, in that order, with no prose of its own), `## Goals`, `## Non-goals`, `## Open decisions for human to make`, `## Defects in the shipped tree that this proposal does not stage`, `## Impacts on other proposals` and `## Deliverable index`. No other heading of any level exists in the file.

FACT: `## Open decisions for human to make` carries exactly 20, 21, 22, 26 and 27, each with the identifier it was stamped with. This firing changed none of them: 20, 22 and 26 were taken to a resolution whose gate refuted it and whose apply was not attempted, so each stays the human's; 21 and 27 were adjudicated to the human, their gates stood and their applies landed. Every out-of-scope marker this firing carried came back `stands` with `no-edit-needed`, so no entry moved into or out of the defects section.

FACT: the section's preamble is true as it stands. It says 21 and 27 carry a recommendation with its ground, its alternatives and a confidence and that 20, 22 and 26 carry the question and its ground alone; 21 and 27 each open with a `**Recommendation (moderate confidence)`, and 20, 22 and 26 carry none. Nothing this pass did falsified it, so it was left as written.

FACT: the counts the file's own structure implies are what it carries. `## Defects in the shipped tree that this proposal does not stage` holds twenty-two entries and no decision; `## Impacts on other proposals` holds nine rows, for 0080, 0073, 0075, 0078, 0072, 0079, 0077, R1b and R12, the 0077 row and the 0073 holdstate clause being this firing's own additions; `## Deliverable index` holds twenty deliverable lines and its trailing testing paragraph, and sits last, preserved line for line.

FACT: no `### Retired` block or equivalent exists anywhere in the summary, and no meta-list of staged items with a proposed disposition survives in it.

WATCHOUT: `**Accepted failure modes.**` still sits at the end of `## Non-goals`, where the `f1.cleanup` firing relocated it. The standing `OPEN` recorded against `f1.cleanup`, that no listed section is named for accepted failure modes, is unchanged and was not re-filed here. A later pass that chooses a different host for it should move the block verbatim, because several "recorded under the accepted failure modes" pointers elsewhere in the file resolve to it.

### [non-spec.15.review-reliability.1]

DECISION: Returned an EMPTY findings list for the reliability lens in round 15 — BECAUSE every recovery mechanism the round-15 fix touched traces correctly through crash, cancellation and restart against the tree, and the residues that remain are each named in `## Edge cases and accepted failure modes` with the reason they are not closed. ALTERNATIVES: five candidates I built and killed are recorded below so a later reliability lens does not re-derive them.

FACT: the only proposal files that moved between the r14 snapshot and r15 are `non-spec-changes.md` (the CODE-4/CODE-8 `Binder.Prepare` attempt-scoped lease release, 139 diff lines), `spec-changes.md`, `summary.md` and the two logs. Between the r15 snapshot and now, only `summary.md` (the 0073 holdstate impact clause and the new 0077 row) and the review log. `diff -rq` across `scratchpad/cp-snap/.../non-spec-r14`, `non-spec-r15` and the live directory is the cheapest first move and still is. EVIDENCE: scratchpad/cp-snap/0081_.../non-spec-r14 vs proposals/0081_...

FACT: the round-15 fix closes the round-14 OPEN at review-log.md:2565 ("does any §4.9 lease expiry reclaim a lease minted by an attempt refused at `AssignCredentials`"). It does not depend on lease expiry: `assignCredentials` now returns the identifiers it minted and `Binder.Prepare`'s credential-assignment failure arm releases them unconditionally, outside the refusal guard (non-spec-changes.md:1077-1082, :1849-1860). The three cited line numbers resolve exactly — `AssignProto` at binder.go:1225, `MintProto` at :1248, the RPC at :1256, the `reclaim()` call site at :949 and `leaseAssigned = true` at :954. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1225,:1248,:1256,:948-954.

FACT (candidate 1, refuted before filing): the started-entry refusal changing from the shipped `codes.Unavailable` (pkg/adapter/slotsession.go:84-86) to `codes.FailedPrecondition` does NOT newly demote a resumable session. `isTransientPodClaimError` matches only typed errors and sentinels and falls through to `return false` for a bare `Unavailable` status, so the shipped refusal already demoted the row to terminal `failed`; the classification is unchanged in effect. EVIDENCE: pkg/gateway/sessionserver/start.go:3648-3681.

FACT (candidate 2, refuted before filing): CODE-5's new `status.Code(err) == codes.Aborted` arm in `isTransientPodClaimError` cannot over-match. `grep -rn "codes.Aborted" --include=*.go pkg/ cmd/` outside tests returns only `pkg/adapter/checkpoint.go:115` and the `opLock` comments, and the op lock is reached by `Checkpoint`/`Interrupt`, never by `Resume`, so no pre-existing Aborted producer on the resume path is reclassified. EVIDENCE: pkg/adapter/oplock.go:27-40; pkg/adapter/checkpoint.go:115.

FACT (candidate 3, refuted before filing): the new `Binder.Prepare` release cannot double-release or strip a successor. All three `Binder.Prepare` call sites (`start.go:2634`, `finalize.go:280`, `binder.go:779`) do `return nil, err` and run no session-wide credential release of their own, and `failPhase`'s `releaseCredentials` is gated on `leaseAssigned`, which is false at that arm. `credassign.Service.Release(leaseID)` is also a documented no-op for an unknown id. EVIDENCE: pkg/gateway/sessionserver/start.go:2634-2636; pkg/gateway/sessionserver/finalize.go:280-282; pkg/gateway/podlifecycle/podsession/binder.go:779-781; pkg/gateway/credentials/credassign/credassign.go:380.

FACT (candidate 4, refuted before filing): the compensation has no unbounded wait and no stalled dependency. `compensateFailedSlotBind` runs on `context.WithTimeout(context.WithoutCancel(ctx), budget)` (non-spec-changes.md:889), and inside the adapter's `Shutdown` handler the only two outbound calls, `emitFinalUsage` and `reportSessionScrub`, both take the handler's own `ctx` and both swallow their errors rather than blocking. EVIDENCE: pkg/adapter/session.go:338-347; pkg/adapter/sessionscrubreporter.go:61-85.

FACT (candidate 5, refuted before filing): no stage inside the shipped `materializeSlot` can return an error that is not a `*SlotBindError`, so the compensation's `errors.As(err, &sbe) && !refused` guard suppresses nothing today. Every one of the five failure arms returns `b.slotBindError(...)`. The staged comment's "a future stage error that is not a *SlotBindError" is forward-looking rather than a live hole. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:286-325.

FACT: the two deferred releases in CODE-1's `Shutdown` are in the right LIFO order. `defer unlockSlot()` is registered at the guard acquisition (non-spec-changes.md:361) and `defer release()` at the reclaim hold (`:399`), so the hold release runs first and the guard outlives it, which is exactly what the comment at `:309-310` claims. A later fixer who "tidies" the two defers together will invert it. EVIDENCE: non-spec-changes.md:357-361,:399.

USEFUL [non-spec.14.review-reliability.1]: its `MaxConcurrentSessions` WATCHOUT and its `RecordSessionScrub` no-dedup FACT both still hold and saved re-derivation. Its DEFERRED about `fakeAssigner.released` was discharged by the round-15 fix at non-spec-changes.md:2793-2800 and :2833-2837, which adds the `Release(leaseID)` recorder field.

WATCHOUT: one staged test bullet still uses the vacuous form the round-14 DEFERRED named. "**The resume path.** ... releases no gateway-side credential lease: the `fakeAssigner` records every `ReleaseSession` call in `released` ..., so the assertion is that the list stays empty" (non-spec-changes.md:2853-2856). `Binder.Resume` calls neither `ReleaseSession` nor `Release(leaseID)`, so an assertion on `released` alone is true on every path. I judged it below the bar (the asserted property is genuinely about `ReleaseSession`, and the resume path mints nothing), but a test-coverage lens that wants it tightened should ask for the new `Release(leaseID)` field to be asserted empty too. EVIDENCE: non-spec-changes.md:2853-2856; pkg/gateway/podlifecycle/podsession/binder_test.go:296-324.

MISTAKE (nearly filed, recorded so the next agent does not spend the round on it): "checklist S16 requires `lockSlotGuard`, which only S15 creates, and S16's `Depends on:` line omits S15". True as bookkeeping — S16 depends on S1, S2, S4, S9, S10, S11, S13, S14 — but the checklist is executed as one ordered sequence and S15 precedes S16 in it, so no build order is actually wrong. Below the bar, and the checklist is reconciliation's to rebuild.

MISTAKE (nearly filed): "the §10.1.4 pass opens every member's reclaim hold in pass 1 while pass 2 terminates them one at a time, so member N's hold outlasts the ten-second graceful window §5.2's reclaim-hold paragraph names". The §5.2 sentence bounds the *close*, not the hold, and round 14 already recorded the same shape as below the bar (review-log.md:2563). The pod is self-terminating on that path and the refusal is transient.

UNVERIFIED: whether the whole-pod occupancy-zero scrub that `## Edge cases` names as the backstop for a failed per-slot cleanup (non-spec-changes.md:3344-3347) actually enumerates and removes a slot tree whose registry entry is already gone. I traced the staged prose and the §5.2 spec text but not `pkg/adapter/podscrub.go`'s enumeration source. A reliability or operational lens that wants to price that residue should read `podscrub.go` rather than the proposal's own sentence.

### [f6.cleanup]

FACT: the summary was already conforming when this firing's cleanup pass ran. Nothing was moved, renamed, renumbered or deleted, and the file is byte-identical to the state the write path left it in. Its headings are, in order, `# Summary: A failed session bind leaves a stale adapter slot registry entry`, `## Summary` (holding only `**Problem statement.**`, `**What changes.**`, `**Decisions.**` and `**Watch out for.**`, in that order, with no prose of its own), `## Goals`, `## Non-goals`, `## Open decisions for human to make`, `## Defects in the shipped tree that this proposal does not stage`, `## Impacts on other proposals` and `## Deliverable index`. No heading of any other level exists in the file.

FACT: `## Open decisions for human to make` carries exactly 20, 21, 22, 26 and 27, each with the identifier it was stamped with. This firing changed none of them: 20, 22 and 26 were taken to a resolution whose gate refuted it and whose apply was not attempted, so each stays the human's; 21 and 27 were adjudicated to the human, their gates stood and their applies landed. All twenty-two out-of-scope markers this firing carried came back `stands` with `no-edit-needed`, and the defects section carries exactly those twenty-two entries, so nothing moved into or out of it.

FACT: the section's preamble is true as it stands. It says 21 and 27 carry a recommendation with its ground, its alternatives and a confidence and that 20, 22 and 26 carry the question and its ground alone; 21 and 27 each open with a `**Recommendation (moderate confidence)` and 20, 22 and 26 carry none. Nothing this pass did falsified it, so it was left as written. `## Defects in the shipped tree that this proposal does not stage`, `## Impacts on other proposals` and `## Deliverable index` carry no preamble at all, so none could be falsified.

FACT: the structural counts are unchanged from the `f5.cleanup` reading. The defects section holds twenty-two entries and no decision; `## Impacts on other proposals` holds nine rows, for 0080, 0073, 0075, 0078, 0072, 0079, 0077, R1b and R12; `## Deliverable index` holds twenty deliverable lines (SPEC-1 through SPEC-6, SCHEMA-1, CODE-1 through CODE-9, CONF-1 and DOCS-1 through DOCS-3) plus its trailing testing paragraph, and sits last, preserved line for line.

FACT: no `### Retired` block or equivalent exists anywhere in the summary, no meta-list of staged items with a proposed disposition survives in it, and no block of corrections owed to files this loop could not edit survives in `**Watch out for.**`. Nothing was relocated, so this pass filed no `DEFERRED` entry.

WATCHOUT: `**Accepted failure modes.**` still sits at the end of `## Non-goals`, where the `f1.cleanup` firing relocated it. The standing `OPEN` recorded against `f1.cleanup`, that no listed section is named for accepted failure modes, is unchanged and was not re-filed here. A later pass that chooses a different host for it should move the block verbatim, because several "recorded under the accepted failure modes" pointers elsewhere in the file resolve to it.

### [spec-recheck-2.3.fix-G3.1]

The G3 fixer of this round left no shard of its own, so this follow-up opens the round's fix
subsection and records its five corrections here rather than in a new pass.

CORRECTS (citation, spec-changes.md §7.1): the replacement sentence "[Section 4.7.1] records the
entry such a reclaim leaves standing as a residue of the token contract" attributed a residue to
§4.7.1 for all three reclaim outcomes its antecedent quantified over. §4.7.1's staged
attempt-mismatch rule records exactly one residue, the untokened entry ("That is a residue this
contract records rather than one it closes"), and its no-entry rule answers `absent` because the
adapter holds no entry at all. Restored the single case without the closed enumeration the
pre-edit sentence carried: "An untokened entry an abandoned attempt's late start left behind
survives a `superseded` reclaim; [Section 4.7.1] records it as a residue of the token contract."

CORRECTS (contradiction, spec-changes.md §7.1 against non-spec-changes.md CODE-1): the `errors.Join`
re-key of the cleanup-outcome report makes a `running`-arm reclaim whose tree removal failed
report a clean exit on the response and file the cleanup `leaked` on the report, while §7.1 said
"a reclaim that reports a clean exit leaves the slot unleaked whatever outcome it carries". The
two answers were keyed on different predicates and the prose claimed they stated the same thing.
Kept the staged code as it is and stated the split in both places: §7.1 now scopes the clean-exit
test to the reclaim's own account and names the adapter's §5.2 cleanup-outcome report as the
separate statement about the per-slot cleanup, and CODE-1's rationale now says the hold and the
cleanup-outcome report share the completion predicate while `exited_cleanly` answers the
reclaiming request's own question.

CORRECTS (contradiction, non-spec-changes.md CODE-1 seam rationale): the sentence still said the
two other `removeSlotTree` callers "keep discarding its error; only the `Shutdown` path's result
becomes load-bearing", which this round's CODE-6 edit falsified at both sites (each now decides
the reclaim-hold release from that return). The clause now says both callers read the return
after CODE-6 and that what the seam adds is a test-only injection point on the `Shutdown` path,
the one path whose result also reaches the response and the cleanup-outcome report.

CORRECTS (contradiction, non-spec-changes.md Design, `**The reclaim hold.**`): "released on every
return path" and "retained from the epoch design unchanged" survived the change that made the
release conditional on the cleanup having completed. Both now state the completion predicate and
the pod-life retention arm.

CORRECTS (contradiction, summary.md): the "What changes" hold bullet and the retry-collision
bullet still bounded the hold by the reclaim's own duration after the Goals bullet, the
priced-residue paragraph and the CODE-1 index bullet had been extended. Both now name the
failed-cleanup arm, and the retry-collision bullet prices it as the pod's remaining life rather
than one spent attempt.

DEFERRED: `summary.md:184` says `superseded` and `absent` "are unleaked without a branch". It is
true of the reclaim's own account and none of these edits falsified it, but a reader coming from
the new §7.1 split may want the two statements brought into one vocabulary. For the pass between
the loops.

### [spec-recheck-2.3.fix-G1.1]

DECISION: Closed the §7.1 duplication by making §7.1 cite §4.7.1 for the `superseded` and `absent` meanings rather than restate them, and left SPEC-5 §15.4's "stated once" clause completely unedited — BECAUSE the single-siting discipline is asserted in three places (the §4.7 row, §15.4, and §4.7.1's own "stated here and nowhere else") and only one passage breached it; repairing the one breach makes all three true — ALTERNATIVES: the reviewer's option (a), weakening the exclusivity claims and keeping §7.1's restatement, was rejected because it leaves two statements of the attempt-mismatch condition that already diverged on the untokened arm.

DECISION: Narrowed the §4.7 `Shutdown` row's clause to "; this row restates neither" and deleted "A superseded reclaim is neither a failed reclaim nor a leaked slot." — BECAUSE the row restated a property of an outcome one sentence after declaring it restates none; the not-leaked fact reaches readers at §7.1's clean-exit sentence and at the §16.1 counter row.

DECISION: Narrowed the §4.7.1 attempt-mismatch rule to "No `Shutdown` naming a bind attempt removes such an entry; within this cascade only the unconditional-teardown rule does." instead of widening its release enumeration — BECAUSE a rule block scoped to `Shutdown` cannot carry an exhaustive cross-section inventory of entry removal without rotting — ALTERNATIVES: the reviewer's suggested widening (naming `DemoteSDK` and the §10.1 hold-timeout inside the rule) was rejected as an exception list bolted onto a rule to rescue an over-broad claim.

FACT: three shipped adapter paths release an untokened registry entry with no `Shutdown` at all, which falsified the proposal's closed enumeration: `Server.DemoteSDK` deregisters the registry's single entry and releases its slot; `ConfigureWorkspace`'s failure arms roll the entry back through `releaseSessionSlot`; and the §10.1.4 hold-timeout termination deregisters every started session. — EVIDENCE: pkg/adapter/sdkwarm.go:296-298, pkg/adapter/sdkwarm.go:236/:241/:251, pkg/adapter/holdstate.go:190

FACT: the refusal of a later bind at an untokened entry comes from the started-session rule, not from the attempt identity rule. The attempt identity rule's condition requires the resolved entry to carry a NON-EMPTY token, so it never reaches an untokened entry. The narrative bullet and the summary's accepted-failure-modes bullet both mis-attributed it and were corrected. — EVIDENCE: spec-changes.md:833 (attempt identity rule), spec-changes.md:835 (started-session rule)

WATCHOUT: the two "untokened entry" edge-case bullets describe different entries and must not be harmonised. The bullet at spec-changes.md:265ff (bullet starting "An abandoned attempt's start whose claim runs after the reclaim completed") describes an UNTOKENED entry and is refused by the started-session rule; the bullet at spec-changes.md:176-178 describes a surviving entry carrying a NON-EMPTY token and is correctly refused by the attempt identity rule. — EVIDENCE: spec-changes.md:265, spec-changes.md:176

WATCHOUT: the whole SPEC-2 §7.1 pod-side reclaim paragraph is ONE physical line (now :460). Three separate groups in this round edit different sentences inside it. Never re-emit the line; replace only the named sentences. — EVIDENCE: spec-changes.md:460

DEFERRED [proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.implementation-checklist.md]: no checklist step needs an edit from this group's edits. S1's line ("it is the single statement of both and no other carrier restates a rule's condition") was false before this fix and is now true, because §7.1's restatement was the only carrier that restated a rule's condition. No deliverable was added, removed, merged, split or resequenced. Recorded so the reconciliation pass does not re-derive it.

### [spec-recheck-2.3.fix-G2.1]

DECISION: closed the §4.7.9 step-5 contradiction by DELETING the trailing clause "rather than leaving the pod holding the session's state", leaving the sentence as a bare pointer at §7.1 — BECAUSE only the outcome half was false (it is false exactly for the one-session pod the staged §7.1 paragraph carves out at spec-changes.md:460 and that SPEC-3's §5.2 append and SPEC-4's §6.2 paragraph describe as retaining the registry entry, the credential file and the armed §4.9 timers), while the pointer half holds on a pod of either concurrency because §7.1 states the branch for both — ALTERNATIVES: (a) the reviewer's longer rewording naming §6.2 from §4.7.9, rejected because it would put a fourth copy of the one-session scope rule into a section whose own commentary declares it states no failure semantics, and four copies is what produced the sweep at review-log.md:1228; (b) withdrawing the SPEC-2 §4.7.9 deliverable, rejected because the pointer is the only thing that stops §4.7.9 reading as a happy path, which problem-statement.md:132-133 names as part of the defect; (c) qualifying step 5 by pod concurrency inline, rejected for the same reason as (a).

FACT: the one-session carve-out sweep recorded at review-log.md:1228 is now CLOSED. After this edit, a grep over the non-log proposal files for "holding the session", "leaves no"/"leaving no"/"no state on the pod", "every stage", "any stage", "whichever stage" and "always reclaim" returns no site asserting that a pre-`running` bind failure leaves nothing on the pod unconditionally. The surviving hits are all already scoped: spec-changes.md:166 (upload-free plan, no RPC sent), :444 (explicitly "On a pool serving one session"), :794 (the §6.2 residue paragraph itself), non-spec-changes.md:1061 ("every stage the reclaim covers") and :3220 ("A matching reclaim"). — EVIDENCE: proposals/0081_.../0081_....spec-changes.md:604

CORRECTS [review-log-archive.md:37896]: that entry's item (d) dropped this same candidate with the reason "the pod is retired at the claim DELETE, so nothing is left holding it". That reason is wrong under the current staging: SPEC-4's §6.2 paragraph (spec-changes.md:794) and SPEC-3's §5.2 append state that on a one-session pod's return-to-pool exit the whole-pod scrub does not reach the slot's registry entry or its armed §4.9 lease-expiry timers, so state does survive. The clause is now deleted rather than re-dropped.

WATCHOUT: the phrase "§4.7.9 step 5 point(s) at it/§7.1" appears in summary.md:86, summary.md:933 and implementation-checklist.md:21 (S3), and spec-changes.md:1044 records the edit as "§4.7.9 step 5 (one sentence)". All four stay TRUE after this deletion and need no edit; do not re-open them. — EVIDENCE: proposals/0081_.../0081_....summary.md:933

### [spec-recheck-2.3.fix-G3.1]

DECISION: Reinstated a terminal for the failing arm of the §5.2 slot-identifier reclaim hold, as the `**Slot cleanup:**` bullet's own terminal rather than as a new marker: the hold ends when the cleanup COMPLETED (every act it owed the slot returned without error), and a cleanup that does not complete releases nothing, so the identifier stays held for the life of the pod and a bind onto it is refused on the same transient terms as one refused mid-cleanup — BECAUSE spec/05_runtime-registry-and-pool-model.md:556 already carries the terminal ("If cleanup fails, the slot is leaked ... the slot is not reclaimed until pod termination") and SPEC-3 replaces only that bullet's first sentence, so no new mechanism, field, flag or state is invented; and because the residue includes `/run/lenny/slots/{sessionId}/credentials.json` while `slotlayout.EnsureTree` is `os.MkdirAll` + `os.Chmod` per directory (pkg/adapter/slotlayout/tree.go:24-47), so admitting a successor onto it is a credential-isolation fail-open — ALTERNATIVES: a per-slot "unreclaimed" marker (a second object with the hold's own lifetime, scope and refusal; not releasing the hold IS the marker); accepting the residue and qualifying spec/05:556's unconditional fresh-workspace guarantee (credential-isolation failure, and it edits a shipped unconditional guarantee); a dedicated non-retryable status or third error code for the permanent hold (new protocol surface for a case pod retirement already bounds, and it re-opens open decision 20's numbering); a counter or gauge for an identifier held past its failed cleanup (pulls in spec/16, the alert rules, a runbook and tier 11, and re-opens the N4 metric deferral).

DECISION: Implemented as one predicate rather than a marker. CODE-1's `Shutdown` gains `completed := false`, a `defer func(){ if completed { release() } }()`, and `completed = closeErr == nil && treeErr == nil` set after the tree removal; `releaseSessionSlot` and the §10.1.4 `terminateHeldSession` take the release only when their own `removeSlotTree` returned nil. In the same edit `reportSessionScrub` is re-keyed from `closeErr` to `errors.Join(closeErr, treeErr)` — BECAUSE one completion predicate then decides the hold, the `leaked` outcome and (on the pre-`running` arm) `exited_cleanly`, so the adapter's hold and the gateway's occupancy accounting cannot disagree about the same cleanup — ALTERNATIVES: leaving the report on `closeErr` alone, which puts the adapter in the state of holding the identifier while telling the gateway `released`. The staged `exited_cleanly` predicate `closeErr == nil && (live || treeErr == nil)` is UNCHANGED and its `live ||` justification still stands.

CORRECTS [spec-recheck round 14, recorded in `## Standing context` of the review log as the amendment to "DECISION: the reclaim hold's bound is stated by chaining through §5.2's own `**Slot cleanup:**` bullet"]: that round RETIRED the identifier-hold half on the reasoning that "the hold's bound is the cleanup's return on every arm, success or failure, and the disposition of a cleanup that does not complete is the scrub-model paragraph's `leaked` clause alone". That reasoning is wrong: `leaked` is a GATEWAY-side accounting state read off the `Shutdown` response's clean-exit answer, it fences nothing on the pod, and no adapter surface refuses admission on it. Post-retirement the failed-cleanup arm was the one arm of the design with no fence at all. The round-14 wording is also what the `f1` firing recorded when it deleted open decision 12; that resolution is superseded on the same ground. What survives from round 14 unchanged: the hold and the `leaked` occupancy are still separate objects, and ending the hold at the `**Slot cleanup:**` bullet's TIMEOUT is still rejected as actively unsafe.

WATCHOUT: the `**Slot cleanup:**` bullet at spec/05:545 sits under the heading `**Slot failure and cleanup (maxConcurrentSessions > 1).**` at :542, so its "not reclaimed until pod termination" sentence is concurrency-scoped. The staged hold paragraph therefore states the identifier terminal in its OWN terms on a pod of either concurrency and cites the bullet only for the concurrent-pod disposition. Do not "simplify" it into a bare pointer at the bullet: that is the seventh filing of the unscoped-`leaked`-terminal family the standing context already records. EVIDENCE: spec/05_runtime-registry-and-pool-model.md:542,:545,:556.

FACT: `terminateHeldSession` discards BOTH cleanup errors in the shipped tree (`_ = s.Runtime.Close(ctx, m.sessionID)` at pkg/adapter/holdstate.go:249, `_ = removeSlotTree(m.state)` at :254). The staging keys that site's completion predicate on the tree removal alone and says why (the runtime close is best-effort by §10.1.4's own terms). A later round that wants both captured there should argue it from §5.2's "every act that cleanup owes the slot", not from the hold paragraph as staged. EVIDENCE: pkg/adapter/holdstate.go:249,:254.

FACT: `errors` is already imported by pkg/adapter/session.go:7, so the `errors.Join` in CODE-1's staged body needs no import change and no entry in any files-touched list.

DEFERRED [/home/ec2-user/lenny/proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.implementation-checklist.md]: S4 (line 23) says SPEC-3's `**Slot-identifier reclaim hold.**` paragraph "states the hold from the deregistering critical section until the cleanup finishes". That is now false. What is true: the paragraph states the hold from the deregistering critical section until the cleanup that reclaims the slot has COMPLETED, and for the life of the pod where that cleanup does not complete, together with the transient refusal it imposes and what bounds the cleanup's runtime close.

DEFERRED [/home/ec2-user/lenny/proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.implementation-checklist.md]: S16 (line 47) instructs CODE-1 to take the reclaim hold "by routing its deregistration through `reclaimSlotLocked` and deferring the release that helper returns". Built literally that yields an unconditional release, which is the pre-fix behaviour. What is true: the deferred closure takes the release only when the cleanup completed, which the staged body expresses as `completed := false`, `defer func(){ if completed { release() } }()`, and `completed = closeErr == nil && treeErr == nil` after the tree removal; and the handler's cleanup-outcome report is keyed on `errors.Join(closeErr, treeErr)` rather than on `closeErr`.

UNVERIFIED: whether CONF-1's battery needs a case for the failed-cleanup arm. The design ruled one out and §15.4's reclaim-hold non-conformance sentence was widened in place from "while the cleanup is running" to "while the identifier is held", which the existing conformance case's wording now covers on its face. The non-spec lane, which has had zero rounds since the token revision, should confirm CONF-1's rule-by-rule correspondence against §15.4 still holds after that widening.

### [spec-recheck-2.3.fix-design-G1.1]

DECISION: G1 is closed by DELETION on both findings, not by adding qualifiers — BECAUSE the two findings are the same defect seen twice: §4.7.1 (and its restatements) claim more than the block's own scope licenses. Finding 0 is closed by option (b): §7.1 cites §4.7.1 for the `superseded`/`absent` meanings instead of restating them, which makes SPEC-5 §15.4's "each stated once, by that section" (spec-changes.md:899) TRUE with no edit, and makes §4.7.1's own "The named rules are stated here and nowhere else" (spec-changes.md:869) true. Finding 3 is closed by narrowing the attempt-mismatch rule's residue sentence to the `Shutdown` cascade it governs, rather than by enumerating the extra releasers. — ALTERNATIVES: (a) the reviewer's option (a) for finding 0 (weaken "stated only there" and "stated once", keep §7.1's duplication) — rejected: the two sites already DIVERGE in content (§7.1 names both arms of the mismatch condition, §4.7.1:868's gloss names only the different-token arm), so keeping both keeps the drift the sentence was written to stop, and it would need three claims plus implementation-checklist.md:17 weakened instead of one paragraph edited; (b) the reviewer's suggested fix for finding 3 (name `DemoteSDK` and the §10.1 hold-timeout inside §4.7.1's rule) — rejected as hair: it hands a rule block whose declared scope is `Shutdown` a cross-section completeness obligation that rots the next time any entry-removing path is added; (c) deleting the residue sentence from §4.7.1 outright — rejected because §7.1 and CODE-4's rationale both cite §4.7.1 as where the residue is recorded.

FACT: three shipped adapter paths release an untokened registry entry with no `Shutdown`, which is why §4.7.1's "released only under the unconditional-teardown rule or by the pod's retirement" is false — EVIDENCE: pkg/adapter/sdkwarm.go:296-298 (`DemoteSDK` calls `anyRegisteredSession` then `releaseSessionSlot`, synchronously inside its own RPC), pkg/adapter/sdkwarm.go:236/:241/:251 (`ConfigureWorkspace`'s three failure arms roll back the entry claimed at :217), pkg/adapter/holdstate.go:190 (`members := s.deregisterStartedSessions()` in `onHoldTimeout`).

FACT: the trailing clause "every later bind attempt at that session on that pod is refused while it stands" is wrong in the attempt-mismatch rule for a second reason nobody has filed: the attempt identity rule's condition requires the resolved entry to carry a NON-EMPTY token, so it never reaches an untokened entry. The refusal a later bind actually meets is the STARTED-SESSION rule, which carries an explicit exemption for a repeat `ConfigureWorkspace`. So the clause both mis-attributes the refusal and contradicts that exemption — EVIDENCE: spec-changes.md:832 (attempt identity rule, "non-empty token the resolved entry carries"), spec-changes.md:834 (started-session rule and its `ConfigureWorkspace` exemption), spec-changes.md:864 (the clause).

WATCHOUT: do NOT edit SPEC-5 §15.4 at spec-changes.md:899. Its "are each stated once, by that section" is scoped to a rule's CONDITION, ORDER, ANSWER and STATUS CODE. Once §7.1 stops restating the attempt-mismatch condition, that sentence is true unedited. Weakening it would undo six rounds of single-siting work — EVIDENCE: spec-changes.md:899.

WATCHOUT: the §16.1 metric-catalog row at spec-changes.md:1004 glosses the `superseded` meaning AND says "the slot is not leaked". It is NOT a site: it makes no exclusivity claim, a catalog row must say what its counter counts, and it cites §4.7.1. But it IS what makes the §4.7 row's absolute "they are stated only there" (about MEANINGS, not rules) untenable, which is why that half narrows to "this row restates neither" even under option (b) — EVIDENCE: spec-changes.md:1004 vs spec-changes.md:391.

DECISION: delete "A superseded reclaim is neither a failed reclaim nor a leaked slot." from the §4.7 `Shutdown` row — BECAUSE it is the exact restatement the clause one sentence earlier forbids, it is a leak-disposition claim §7.1 and §5.2/§6.2 own, and the row's neighbours in that table are one-line RPC descriptions. The reader who needs the not-leaked fact meets it at §7.1's clean-exit sentence and at the §16.1 counter row. — ALTERNATIVES: keeping it and weakening the clause further; rejected because the row would then restate a property in the sentence after saying it restates nothing.

UNVERIFIED: whether the reconciliation pass's rebuild of implementation-checklist.md:17 ("no other carrier restates a rule's condition") is still needed. Under option (b) that line becomes TRUE with no edit. The spec fixer is barred from the checklist anyway; reconciliation should confirm rather than rewrite it.

OPEN: after this fix §7.1 no longer states the `superseded`/`absent` meanings, so summary.md:932's SPEC-2 deliverable bullet must stop crediting it with "the meaning of the superseded and absent answers". That is IN SCOPE for this fix and listed in the design; flagged here because a later round reading the bullet alone would file it as a fresh contradiction.

### [spec-recheck-2.3.fix-design-G2.1]

DECISION: Fix finding 2 by DELETING the false trailing clause rather than rewording it. SPEC-2's §4.7.9 replacement block (spec-changes.md:602) ends step 5 at "A stage that fails takes the failure branch stated in [Section 7.1](07_session-lifecycle.md#71-normal-flow)." and drops "rather than leaving the pod holding the session's state." — BECAUSE the deleted clause is the only false part: §7.1 as staged does state the failure branch for BOTH cases (the obligation, and the carve-out sentence at spec-changes.md:458 which itself cites §6.2), so the surviving pointer is true on a pod of either concurrency and needs no scope qualifier. — ALTERNATIVES: (a) the reviewer's suggested longer rewording ("...which states which failures the gateway reclaims on the pod and cites [Section 6.2] for what becomes of the slot state on a pod it does not reach") — rejected: it restates the one-session scope split in a FOURTH place (§7.1:458, §5.2:643, §6.2:794 already carry it), and a restated scope rule is exactly what this proposal's sweep problem is made of; §7.1 already carries the §6.2 hop, so §4.7.9 needs only one hop. (b) withdraw the SPEC-2 §4.7.9 deliverable entirely — rejected: §4.7.9 is the section a reader of the bind sequence reaches first and today states no failure branch at all (spec/04_system-components.md:854); the pointer is the whole point of the deliverable. (c) add a per-concurrency conditional to step 5 — rejected as hair in an orientation list that "states no failure semantics of its own" (spec-changes.md:605-607).

WATCHOUT: do NOT touch the commentary under the block ("No other change to §4.7.9. The section stays a `type: agent` orientation list and states no failure semantics of its own.") — it is MORE true after the deletion, not less. — EVIDENCE: proposals/0081_.../0081_....spec-changes.md:605-607

FACT: nothing else in the proposal or the tree restates the deleted clause, and every downstream reference to this edit is phrased as "§4.7.9 step 5 points at it" / "(one sentence)", which stays true under the deletion, so the fix cascades nowhere. — EVIDENCE: proposals/0081_.../0081_....summary.md:86, :932; .implementation-checklist.md:21; .spec-changes.md:1042

FACT: the §7.1 staged paragraph's own carve-out sentence carries the §6.2 hop, which is why §4.7.9 does not need its own: "the pre-attached failure disposition deletes the pod's claim, and [Section 6.2] states what becomes of the slot state the attempt left there." — EVIDENCE: proposals/0081_.../0081_....spec-changes.md:458

MISTAKE: this same sentence was filed at round 8 (review-log.md:1506) and at :1228 as the third site of the one-session carve-out sweep, and was never landed because the fix pass hit the rate limit. It is the last known un-narrowed universal from that sweep; after it lands, re-run the sweep grep at review-log.md:1228 once and record the sweep as closed rather than leaving a fourth round to rediscover it. — EVIDENCE: proposals/0081_.../0081_....review-log.md:1228, :1506

### [spec-recheck-2.3.fix-design-G3.1]

DECISION: Reinstate the failed-cleanup terminal on the staged §5.2 reclaim-hold paragraph, but as the SHIPPED bullet's own terminal rather than as a new marker: the hold ends when the cleanup it covers COMPLETES (every act it owed returned without error), and a cleanup that does not complete never releases the identifier, so the slot "is not reclaimed until pod termination" exactly as the unedited `**Slot cleanup:**` bullet already says. — BECAUSE spec/05_runtime-registry-and-pool-model.md:556 (Slot cleanup bullet, NOT touched by SPEC-3, which replaces only the action-list sentence) already states "If cleanup fails, the slot is leaked — the pod continues but the slot is not reclaimed until pod termination", and the bullet's action list ends with "and releases the `slotId`". The hold is the adapter-side statement of that unreleased identifier, so no new mechanism, no marker field, no new state is invented. — ALTERNATIVES: (a) a per-slot "unreclaimed" marker as the finding suggested — rejected, it is a second object with the same lifetime and the same predicate as the hold, i.e. hair; (b) accept the residue and qualify spec/05:556's fresh-workspace guarantee — rejected, the residue includes `/run/lenny/slots/{sessionId}/credentials.json`, so admitting a successor over it is a credential-isolation failure and code-best-practices.md requires failing closed there; (c) a new permanent/non-retryable refusal code for a hold that can never lift — rejected, new protocol surface for a case the pod's own retirement already bounds.

DECISION: the hold-release predicate is `closeErr == nil && treeErr == nil` (cleanup completed), and `reportSessionScrub` is re-keyed from `closeErr` to `errors.Join(closeErr, treeErr)` in the same edit. — BECAUSE otherwise the `running` arm diverges: the adapter holds the identifier while the gateway is told `released` and decrements occupancy, which is precisely consequence (2) of the finding (spec/06_warm-pod-model.md:160 says leaked occupancy exists to stop the gateway over-assigning against unreleased resources). With the re-key, "the cleanup did not complete" is ONE predicate deciding the hold, the `leaked` outcome and (on the pre-`running` arm) `exited_cleanly`. — ALTERNATIVES: leave the scrub report `closeErr`-keyed and let the hold be stricter — rejected as an exception clause a later round files.

FACT: SPEC-3 replaces ONLY the first sentence of §5.2's `**Slot cleanup:**` bullet. The leaked terminal, the timeout formula and the CRD rule all stand unedited. — EVIDENCE: proposals/0081_.../...spec-changes.md:612-620 vs spec/05_runtime-registry-and-pool-model.md:556.

FACT: the staged `exited_cleanly` predicate is `closeErr == nil && (live || treeErr == nil)`; `live` (runtimeLive membership) deliberately excludes the tree error on the `running` arm because the cleanup-outcome report is supposed to carry it there. — EVIDENCE: ...non-spec-changes.md:452, :487-495.

FACT: `slotlayout.EnsureTree` is `os.MkdirAll` + `os.Chmod` per directory and is explicitly documented as idempotent, so a successor materializes INTO a residual tree rather than over a fresh one. — EVIDENCE: pkg/adapter/slotlayout/tree.go:24-47.

WATCHOUT: the site list handed to the design stage was incomplete. Four further restatements of the unconditional "until the cleanup finishes" bound exist and are IN SCOPE: spec-changes.md:151-153 (design commentary), :458 (SPEC-2's NORMATIVE §7.1 paragraph, not just its :64 rationale), :905 (SPEC-5's §4.7.1 conformance block, normative wire statement), and summary.md:301 (goal bullet). — EVIDENCE: grep -n "until the cleanup" on the proposal's staged files.

WATCHOUT: the CODE-6 tier-1 panic case at non-spec-changes.md:2502 currently asserts a later bind IS admitted after `Runtime.Close` panics. Under this fix a panic IS a cleanup that did not complete, so the assertion flips to refused, and the case's stated purpose changes: it no longer discriminates `defer` from per-return statements (both hold on panic). The case that now pins the `defer` is the SUCCESS arm (clean close + clean tree removal releases the hold), which already exists at :2495. — EVIDENCE: ...non-spec-changes.md:2495-2505.

FACT: the conformance battery (CONF-1) cannot drive a failed cleanup over the wire — there is no wire input that makes a third-party adapter's `os.RemoveAll` fail. The correct §4.7.1 edit is therefore to re-word :905's "while the cleanup is running" to "while the identifier is held" and add NO conformance case, keeping the hold's terminal defined once, in §5.2. — EVIDENCE: ...non-spec-changes.md:2050-2063 (the battery's single hold case), :468-480 (why the `removeSlotTreeFn` seam exists at all).

DEFERRED [implementation-checklist.md]: S4 (line 23) says SPEC-3's hold paragraph states the hold "until the cleanup finishes", and S16 (line 47) directs CODE-1 to defer `reclaimSlotLocked`'s release unconditionally. Both are false after this fix. What is true instead: SPEC-3's paragraph states the hold until the cleanup COMPLETES and, where it does not complete, until the pod terminates; CODE-1 releases the hold only on the completed-cleanup arm (`closeErr == nil && treeErr == nil`) and retains it otherwise. The spec-lane fixer is barred from this file; the reconciliation pass must land both.

OPEN: the retained hold makes a repeated transient refusal the only caller-visible symptom on a one-session pod, where neither the `leaked` sub-state nor the whole-pod replacement trigger applies (spec/06_warm-pod-model.md:148,:160 scope both to concurrent occupancy). This design adds no counter or gauge for it deliberately (new metric surface pulls in spec/16, docs/reference/metrics.md, a runbook and the N4 deferral). Whether a one-session pod needs an observable signal for an identifier it will hold for its life is a question for a later round or a human.

### [spec-recheck-2.3.review-applicability.1]

FACT: The delta for THIS round is one sentence, not the epoch-to-token rewrite. The orchestrator brief's snapshot pointer (`spec-recheck-r6`) is the right one; `spec-recheck-2-r2` and `spec-recheck-2-r3` are Sep-11 directories from an older loop that reuse the same name prefix, so `diff -ru .../spec-recheck-2-r2 proposals/...` yields a 1130-line diff spanning the whole 2026-09-16 hand revision and is misleading. `spec-recheck-2-r3-start` is byte-identical to the live tree. The real delta is `diff -u .../spec-recheck-r6/...spec-changes.md proposals/...spec-changes.md` = 25 lines: the deletion of the §5.2 reclaim-hold sentence "On a pod serving concurrent sessions a cleanup that does not finish within the timeout ... so the slot is `leaked` and its identifier stays held until the pod terminates", plus the five-line commentary added at spec-changes.md:653-658 explaining why the paragraph now states no terminal for a failed cleanup. EVIDENCE: scratchpad/cp-snap/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/ (`ls -l`, Sep 11 vs Sep 18 mtimes)

FACT: The deletion is self-contained. `grep -n "stays held\|until the pod terminates\|failed cleanup"` over spec-changes.md returns only the new commentary at :654-:670; no staged spec text depends on the deleted clause, so it is not a relocation-that-loses-content. EVIDENCE: proposals/0081_.../0081_....spec-changes.md:654,:655,:670

USEFUL [Traps :1228]: "The one-session carve-out is a SWEEP, not a single edit ... the §5.2 append, the accepted-failure bullet, and §4.7.9 step 5's trailing 'rather than leaving the pod holding the session's state'." Two of those three are now fixed; the §4.7.9 one is NOT, and it is the one finding I return. EVIDENCE: review-log.md:1228; spec-changes.md:602

USEFUL [Open :1506]: "Two round-8 findings recorded in commit 096b39656 were never landed ... '§4.7.9 step 5 contradicting the one-session carve-out'." Confirmed still unlanded on 2026-09-18. EVIDENCE: review-log.md:1506; spec-changes.md:602 still reads "...rather than leaving the pod holding the session's state."

WATCHOUT: Do NOT re-file spec/06:283's "The pod is marked `failed` and released back to the pool". Settled entry :1337 records it as a pre-existing spec/05-vs-spec/06 wording tension, CLOSED for this proposal, and :1438 leaves it as an UNVERIFIED for a prose-consistency lens. EVIDENCE: review-log.md:119,:1337,:1438

UNVERIFIED: SPEC-2's staged §7.2 step-3 replacement keeps "the half-claimed replacement pod is released back to the pool via the standard pod release path ([§6.2])", and the staged §6.2 `resuming → cancelled` clause keeps "which is then released to the pool". SPEC-4 re-keys the projection so a claim deleted while the pod projects `claimed` retires on a pool of EITHER recycle setting, which is a widening: before SPEC-4 a claim deleted on a recycling pod under its limits projected `idle`. Whether the half-claimed replacement pod's release is a claim DELETE while `claimed` (now: retires) or passes through the recycle boundary to `reserved → idle` (returns) is not stated by any staged text. I did not file it because it is one step from the already-adjudicated :1337 tension and I could not settle which release path applies. A §6.2/§7.2 mechanism lens should decide. EVIDENCE: spec-changes.md:530 (staged step 3), :581 (staged §6.2 clause), :713-719 (SPEC-4 re-key); spec/06_warm-pod-model.md:80,:95

FACT: All fifteen "text to replace" anchors still resolve byte-for-byte and uniquely, re-verified today: spec/04:157, :415, :416, :686, :854; spec/05:453, :545; spec/06:80, :95 (the Occupancy-projection group's own `claimed ──→ draining`, unique within that named group though the string occurs five times in the file), :152, :234, :290; spec/07:210, :213, :214, :414; spec/15:1136 (three sentences in one row); spec/29:711 (unique only under the "In §29.4's numbered step 13" scoping). Every minted markdown anchor already exists in spec/. EVIDENCE: re-ran the greps listed in review-log.md:99-100

FACT: `spec161Metrics` is a hand-maintained Go string list in pkg/observability/metrics/catalog_test.go:15, not parsed from spec/16. A SPEC-6 row added to spec/16_observability.md therefore breaks no tier-0 gate on its own; the two-way reconciliation is Go-list-versus-Go-list. EVIDENCE: pkg/observability/metrics/catalog_test.go:10-15,:188,:202

FACT: The three `tests/claim-map.json` rows the proposal stages all carry `spec_anchor: "#2851-gateway-to-pod"`, a §28 anchor, so §28.4's scope sentence ("Every normative statement this section makes") needs no widening edit. EVIDENCE: non-spec-changes.md:2217,:2224,:2231; spec/28_communication-channels.md:161-165


### [spec-recheck-2.3.review-citations.1]

FACT: Every file:line citation in the current spec staging resolves and every "reads, verbatim" quote matches byte-for-byte. Verified this round: spec/04_system-components.md:151 and :157 (§4.1 derivation + ShutdownRequest paragraph), :686 (§4.7 Shutdown row), :854 (§4.7.9 step 5), :415-416 (§4.6.1 claim-deletion bullets, incl. the false closing sentence), :411 (the warm-inventory qualifier the commentary says §4.6.1 already carries); spec/05:453 (Scrub model), :545 (Slot cleanup bullet), :455/:459/:461/:467/:471 (recycle lifecycle + scrub steps), :481-490 (onScrubFailure + retirement), :553-562 (slot retry policy, slotRetries default 1); spec/06:80 (projection prose, all three replaced clauses), :95-97 (fence trigger), :150-155 (per-slot fence), :158 (reserved hold paragraph anchor), :234 (mid-resume cancel bullet), :290 (Client visibility clause); spec/07:23-24 (atomicity paragraph inside the fenced flow listing), :210/:213/:214 (§7.2 close sequence), :402/:414 (§7.3 list); spec/15:1136 (SETUP_COMMAND_FAILED row, all three replaced sentences), :1469/:1471 (§15.4 insertion anchor), :1699 (PROTOCOL_VERSION_INCOMPATIBLE precedent), :2038ff (§15.4.6 drives a runtime binary over JSONL against a fake adapter); spec/16:14 (session-slot failure row); spec/29:586-591, :669-674, :697, :710-711; schemas/lenny-adapter.proto:1621-1627 (recycle comment) and :41 (PrepareWorkspace is the only client-streaming bind RPC); pkg/controller/warmpool/occupancy.go:57-59, :84, :128-140; pkg/gateway/podlifecycle/podsession/binder.go:867, :998, :1072-1082, :1200-1202. Every markdown anchor used in staged text resolves to a real heading.

FACT: §15.4's commentary "the wire edit ... adds ... the mid-session marker to `PrepareWorkspace`" is NOT a contradiction of §4.7.1's carries-table, which marks `mid_session` "carried" on both `PrepareWorkspace` and `FinalizeWorkspace`. `FinalizeWorkspaceRequest.mid_session` is already field 4 in the shipped proto — EVIDENCE: schemas/lenny-adapter.proto:724. Only the PrepareWorkspace one is new. Do not file this.

FACT: `writeSetupCommandError` is reached only from a `*podsession.SetupCommandFailure`, and that value is constructed at exactly two sites, both the `RunSetup` error arm — EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:304, binder.go:938, pkg/gateway/sessionserver/start.go:236-249. This confirms the edge-case bullet's "the gateway renders only the setup-command request's failure into that envelope, and there it branches on the gRPC code rather than on the window" (spec-changes.md:316-318).

FACT: The staged §5.2 sentence "A release that runs its cleanup inside the RPC that requested it ends the hold before that RPC answers, so the pod-warm bind sequence that follows an SDK demotion does not meet it" is true of the tree — EVIDENCE: pkg/adapter/sdkwarm.go:296-301 (`DemoteSDK` calls `releaseSessionSlot` inline before returning). The 10s figure the same paragraph gives the §10.1 hold-timeout termination matches pkg/adapter/holdstate.go:201.

WATCHOUT: The snapshot chain is misleading. `spec-recheck-2-r3-start` is byte-identical to the live proposal; `spec-recheck-2-r3`, `spec-recheck-2-r2`, `spec-recheck-2-r2-start` and the brief's `spec-recheck-r6` all still carry the withdrawn **bind epoch** design, so a diff against any of them returns essentially the whole file and is useless as a delta. Diff `spec-recheck-2-r3-start` first to establish that nothing moved, then read the staging whole.

OPEN: The §4.7 row's exclusivity clause ("they are stated only there", spec-changes.md:391) and the §15.4 block's "are each stated once, by that section" (:899) are both falsified by the staged §7.1 paragraph (:458), which restates the `superseded` and `absent` conditions. Filed this round as a contradiction. Note it is NOT the already-refuted "superseded is defined too narrowly at all three sites" item: that one is about the content of the gloss, this one is about the single-statement claim.

### [spec-recheck-2.3.review-client-surface.1]

DECISION: returned zero findings for the client-facing-surface lens on the spec staging — BECAUSE every client-visible parallel representation the staging touches resolves: §15.1's `SETUP_COMMAND_FAILED` row has no mirror in `pkg/gateway/externalapi/openapi/openapi.json` (grep for `SETUP_COMMAND_FAILED`/`STARTING_FAILED` returns 0 there) and none in `sdks/`, so `docs/reference/error-catalog.md:129` is its only mirror and DOCS-3 stages it; §16.1's two new rows mirror only into `docs/reference/metrics.md` (`docs/operator-guide/observability.md` is a subset, not a complete mirror — it carries no `lenny_slot_failure_total` row); `docs/reference/adapter-contract.md` carries a one-line-per-RPC table (`:59-75`) with no request-field or ErrorCode enumeration, so the `Shutdown` row DOCS-2 stages is the whole obligation there — ALTERNATIVES: rejected filing the §15.4 "status code and error category ... each stated once, by that section" over-claim (see WATCHOUT below) and the §15.1 "Surfaced wherever the gateway runs setup commands" sentence, both as explanatory-gloss items this loop has refuted repeatedly.

FACT: `docs/api/internal.md` documents the gateway↔adapter protocol only at the version-negotiation level (`:443`); it names no `Shutdown`, no `exited_cleanly`, and no slot registry, so it is NOT a mirror surface for this proposal. Nobody needs to re-derive that. — EVIDENCE: docs/api/internal.md:443

FACT: the shipped proto already carries `mid_session` on `FinalizeWorkspaceRequest` (field 4) and NOT on `PrepareWorkspaceRequest`, which is why the staged §15.4 sentence "the mid-session marker to `PrepareWorkspace`" names one message while the §4.7.1 carries-what table marks `mid_session` "carried" on both. That is correct, not a drift. — EVIDENCE: schemas/lenny-adapter.proto:724 (`bool mid_session = 4;` in `FinalizeWorkspaceRequest`), schemas/lenny-adapter.proto:682-696 (`PrepareWorkspaceRequest`, fields 1-3 plus `reserved 4`)

FACT: `ShutdownResponse` ships with exactly two fields (`exited_cleanly = 1`, `exit_code = 2`); `slot_reclaim` is new at 3 and there is no `slot_was_started` field anywhere in the staging. Any orchestrator note or earlier round mentioning `ShutdownResponse.slot_was_started (4)` is stale — SCHEMA-1's field table stages nine fields and `slot_was_started` is not among them. — EVIDENCE: schemas/lenny-adapter.proto:1665-1668; non-spec-changes.md:2152-2162 (the nine-row field table)

WATCHOUT: the staged §15.4 block asserts that §4.7.1 states "the status code and error category that answer is carried on" once per rule. §4.7.1 states an error category for only two of the six refusing rules (attempt identity → `CATEGORY_TRANSIENT`, started-session → `CATEGORY_PERMANENT`); the pairing rule, the mid-session-create rule, the start-confirmation rule and the teardown-pairing rule state a gRPC status and no category, and the start-confirmation rule states no `ErrorCode` either. I judged this below the bar because the staged tier-3/tier-10 cases assert status codes alone for those four (non-spec-changes.md:2037-2042, :2948-2950), so the absence is deliberate and nothing is unimplementable — but a later lens will rediscover it, and the cheapest closure is narrowing the §15.4 sentence rather than adding categories to §4.7.1. — EVIDENCE: spec-changes.md:899 (the §15.4 claim), spec-changes.md:819 (start-confirmation rule, "answered on `ABORTED`" with no code and no category), spec-changes.md:843 and :870 (pairing and teardown-pairing rules, `INVALID_ARGUMENT` with no category)

USEFUL [orchestrator delta pointer]: the spec-lane delta for this round is one deletion plus one added rationale paragraph in the §5.2 reclaim-hold block (spec-changes.md:645, :650-657). I verified the deleted "identifier stays held until the pod terminates" clause is now orphaned nowhere: `grep -n "stays held\|held until the pod\|until the pod terminates"` across the proposal's non-review-log files returns nothing, and the published §15.4 reclaim-hold block never restated it, so no client-facing surface lost its source.

### [spec-recheck-2.3.review-docs-alignment.1]

DECISION: returned an empty findings list for the docs-alignment lens on the r6→now spec-staging delta — BECAUSE the delta is exactly two hunks in the §5.2 `**Slot-identifier reclaim hold.**` block (the "its identifier stays held until the pod terminates" sentence deleted; a four-sentence rationale added at spec-changes.md:653-658 saying the hold's bound is the cleanup's return on every arm), and neither hunk has a docs/ mirror: `grep -rn "reclaim hold\|slot identifier" docs/` returns nothing repo-wide — ALTERNATIVES: I considered and dropped four candidates, listed below, each for a stated reason.

FACT: no page under docs/ describes the slot-identifier reclaim hold in any spelling. `grep -rn "reclaim hold\|slot-identifier\|slot identifier" docs/` is empty. A future round editing the §5.2 hold paragraph has no docs mirror to move. EVIDENCE: docs/ (empty grep)

FACT: the staged spec file's own docs-mirror trailer (spec-changes.md:1065-1076) is the in-scope surface for a docs finding in this lane, and it is currently accurate against the four DOCS/CODE deliverables: metrics.md (both counters, CODE-9), state-machines.md (per-slot row + the three :138 projection clauses, DOCS-1), adapter-contract.md (`Shutdown` row + `DemoteSDK` row, DOCS-2), error-catalog.md (four sentence replacements + remedy cell, DOCS-3). The trailer's DOCS-3 description matches the current DOCS-3 (four replacements), unlike the checklist S7 line and the summary index, which still say "two new rows". EVIDENCE: spec-changes.md:1073-1076 vs non-spec-changes.md:2329-2398

FACT: every docs/ page that mirrors a section this proposal edits is in the trailer. I checked, and each is either already staged or unaffected: docs/reference/glossary.md:371 (slot definition, untouched behaviour), docs/reference/execution-modes.md:63-68 and docs/operator-guide/multi-tenancy.md:67-72 and docs/operator-guide/security-principles.md:33 (all mirror §5.2's UNEDITED "per-slot cleanup runs at each session release" sentence, which SPEC-3 appends to rather than replaces, so they stay as true as the spec sentence they mirror), docs/api/internal.md (publishes StartSession/StopSession/Checkpoint/Upload/DemoteSDK messages and carries no `Shutdown` message at all, so the new proto fields add no edit there), docs/runtime-author-guide/* (no PrepareWorkspace/Shutdown/bind-sequence content). EVIDENCE: docs/api/internal.md:110-280; docs/reference/execution-modes.md:68

FACT: only docs/reference/metrics.md names any `lenny_slot_*` or `lenny_adapter_leaked_slots` series; no operator-guide or runbook page carries one, so the two new counters need no runbook/alert companion and none is staged. EVIDENCE: `grep -rn "lenny_slot_\|lenny_adapter_leaked" docs/` returns metrics.md rows only

WATCHOUT: docs/reference/adapter-contract.md:81's `ReportSessionScrub` row ("Report the per-slot cleanup outcome ... at each session release") reads as contradicting the staged DOCS-2 `Shutdown` row six lines above it, which says the adapter "reports no outcome for a cleanup on a slot the runtime was never given". It is NOT a defect: a pre-`running` bind reclaim is not a session release, which is why SPEC-3 leaves §5.2's own "on every session release" sentence intact and APPENDS ("It ALSO runs on a slot whose bind is abandoned..."). Do not file it; the docs row mirrors the unedited spec sentence exactly. EVIDENCE: docs/reference/adapter-contract.md:81; spec-changes.md:637-640 (the verbatim unedited scrub-model sentence)

WATCHOUT: DOCS-2's `DemoteSDK` amendment publishes a registry effect ("drop the adapter's slot registry entry for the session") that the §4.7 `DemoteSDK` row (spec/04_system-components.md:674) does not state, and cites it to "§4.7.1's caller rule", a rule name that does not appear in the staged §4.7.1 block. I did not file it: the release inside the demotion RPC is presupposed by staged spec text at spec-changes.md:645 ("A release that runs its cleanup inside the RPC that requested it ends the hold before that RPC answers, so the pod-warm bind sequence that follows an SDK demotion does not meet it"), it is shipped behaviour rather than new, and the "fresh entry stamped with that attempt's own token" half follows directly from the staged create-and-stamp rule. The loose "caller rule" citation lives in non-spec-changes.md and is out of this lane's scope. EVIDENCE: non-spec-changes.md:2322-2324; spec/04_system-components.md:674; spec-changes.md:645

WATCHOUT: docs/reference/state-machines.md:251 states the `leaked` trigger as "when the cleanup timeout is exceeded", narrower than §5.2:545's "If cleanup fails, the slot is leaked". A reclaim the adapter does not answer therefore reads as an unmirrored new cause. It is not: §5.2's shipped "If cleanup fails" already covers it, so the docs narrowing is PRE-EXISTING drift this proposal neither creates nor widens. EVIDENCE: docs/reference/state-machines.md:251; spec/05_runtime-registry-and-pool-model.md:545

USEFUL [orchestrator brief]: the instruction to read the delta first was decisive here. The whole spec-staging delta is two hunks, both inside one paragraph with no docs mirror, which made the lens's answer cheap to establish once the `diff -u` on spec-changes.md alone was run (rather than the whole-directory `diff -ru`, which is dominated by checklist renumbering noise).

### [spec-recheck-2.3.review-edit-sites.1]

DECISION: returned an EMPTY findings list for the edit-site-completeness lens on the spec staging — BECAUSE the whole delta since the `spec-recheck-r6` snapshot is confined to one hunk in `*.spec-changes.md` (the staged §5.2 reclaim-hold paragraph loses its "On a pod serving concurrent sessions a cleanup that does not finish within the timeout ... its identifier stays held until the pod terminates" sentence, and the commentary below the fenced block gains five lines justifying the absence), and that deletion strands no reference anywhere: `grep -n "stays held\|held until\|until the pod terminates\|failed cleanup"` over all five staged proposal files returns exactly one hit, the new commentary line itself. ALTERNATIVES rejected: (a) the §6.2 `leaked` slot semantics paragraph (spec/06_warm-pod-model.md:160) as a surface the deletion makes inconsistent, because a leaked slot's identifier is now re-bindable once the failed cleanup returns — killed by the standing Trap at review-log.md (Traps, "`SlotID == SessionID` falsifies §5.2's `new slot` and the Fresh workspace guarantee"), three lenses, and by the fixer's own explicit decoupling sentence ("The identifier hold and the `leaked` occupancy are separate objects"); (b) spec/05_runtime-registry-and-pool-model.md:545's surviving "The adapter reports each slot cleanup outcome" — the recorded dead end, killed at review-log.md:884 and five archive sites; (c) spec/07_session-lifecycle.md:208's pre-attached-visibility enumeration and spec/15_external-api-surface.md:625,:646,:647,:650,:720 as unstaged mirrors of the §15.1 `SETUP_COMMAND_FAILED` widening — each is a one-way scoped enumeration that stays true, and the standing Traps entry bars re-filing the cause drift.

FACT: every verbatim "reads, verbatim" quote in the staged spec file still resolves against the current tree, and every markdown anchor the staged text links to resolves to a real heading. Checked this round: spec/04_system-components.md:157 (§4.1 third sentence), :686 (§4.7 `Shutdown` row opening), spec/05_runtime-registry-and-pool-model.md:545 (`**Slot cleanup:**` action list), spec/06_warm-pod-model.md:80 (three projection clauses), :95-97 (the `claimed ──→ draining` trigger list in the `Occupancy projection` fence, which is the entry at :95 and not the four same-named entries at :114,:135,:137,:139), :149-154 (the either-concurrency per-slot fence, insertion point after `receiving_uploads ──→ running`), :234 (the mid-resume cancel bullet clause), :290 (`**Client visibility:**`), spec/07_session-lifecycle.md:210 (§7.2 preamble premise), :212 (step 2 tail), :214 (step 3), :414 (§7.3 list item 4), spec/15_external-api-surface.md:1136 (the three `SETUP_COMMAND_FAILED` sentences), spec/16_observability.md:14 (the session-slot failure row the new rows sit beside). EVIDENCE: spec/06_warm-pod-model.md:95; spec/07_session-lifecycle.md:214.

USEFUL [Standing context, Traps entry "Do NOT re-derive the whole-client-surface absence sweep"]: `grep -rnE 'bind_attempt|unconditional_teardown|slot_reclaim|SlotReclaimOutcome|SLOT_BIND_' spec/ docs/ schemas/ charts/` returning zero is the single fact this lens would otherwise spend a round re-establishing. Confirmed again for `slot_was_started` and `exited_cleanly`, which also appear nowhere under spec/ or docs/. Saved a round.

USEFUL [Standing context, Traps entries at review-log.md on `docs/reference/adapter-contract.md:81` and on the docs-mirror trailer versus DOCS-1's body]: both were the second and third things this lens landed on, and both entries name the exact ground. Two rounds saved.

### [spec-recheck-2.3.review-feasibility.1]

DECISION: returned an empty findings list for the actor-action feasibility lens on the r6→now spec staging — BECAUSE the only delta in spec-changes.md is the deletion of the §5.2 failed-cleanup identifier-hold sentence plus a five-line rationale append, and every actor the staging names can perform the act assigned to it, checked against the tree — ALTERNATIVES: filing the §5.2 `**Slot cleanup:**` bullet's surviving "reports each slot cleanup outcome" against the staged withheld-report rule (rejected: review-log.md:884 records that tension as a surviving dead end and bars re-opening it from the session-release framing); filing the §6.2 `**Client visibility:**` asymmetry after SPEC-5 widens only the exclusion clause (rejected: close variant of the already-refuted §15.1 exclusion-clause finding, and §15.1 assigns the case the bullet leaves unspoken).

FACT: the delta this run reviews is exactly two hunks in spec-changes.md — the deletion of "On a pod serving concurrent sessions a cleanup that does not finish within the timeout the **Slot cleanup:** bullet states is a failed cleanup under that bullet, so the slot is `leaked` and its identifier stays held until the pod terminates." from the staged §5.2 hold paragraph, and a five-sentence rationale append to SPEC-3's explanatory prose. — EVIDENCE: proposals/0081_.../0081_....spec-changes.md:645,:653-658

FACT: `lenny_adapter_leaked_slots` is emitted by the GATEWAY, not the adapter, despite spec/06_warm-pod-model.md's `**`leaked` slot semantics.**` paragraph attributing it to the adapter ("The adapter exposes a `leaked_slots` count ... and `lenny_adapter_leaked_slots` gauge"). This is a pre-existing spec-vs-tree divergence this proposal neither creates nor widens, and it is what makes the staged §5.2 "is surfaced on the `lenny_adapter_leaked_slots` gauge" clause feasible on the arm where the adapter never learns the reclaim failed. Do not file it here. — EVIDENCE: pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_elicitation_test.go:920-936; pkg/sandbox/slotstate/registry.go:10,:98; spec/06_warm-pod-model.md:161

FACT: the adapter CAN label a metric by pod name. `Server.podID` is read once from the Downward API `POD_NAME` env at `New` and cached, and the shipped `ReportSessionScrub` path already fails closed when it is empty. So SPEC-6's `lenny_slot_shutdown_untokened_entry_total{k8s_pod_name}` row is feasible actor-side. — EVIDENCE: pkg/adapter/server.go:36,:186-196,:380; pkg/adapter/sessionscrubreporter.go:70-78

FACT: the gateway counter's label set is precedented end to end. `lenny_slot_failure_total(error_type, pool, k8s_pod_name)` is assigned as a `Binder` hook at cmd/lenny-gateway/metricsbackfill.go:148-149 and fired from `materializeSlot` with `req.Pool` and `sandboxName` in scope, so SPEC-6's `lenny_slot_compensation_superseded_total{pool,k8s_pod_name}` is emittable from the compensation's caller as the §16.1 row claims. — EVIDENCE: cmd/lenny-gateway/metricsbackfill.go:148-149; pkg/gateway/podlifecycle/podsession/slotbinder.go:287-322,:358-360

FACT: the seven RPCs the staged §5.2 hold and the staged §4.7.1 Admission block name as entry-creating are exactly the seven production entry-creating sites in the tree, with no eighth. `PrepareWorkspace`/`FinalizeWorkspace` via `ensureSlotPaths` at staging.go:134,:181; `RunSetup` via staging.go:337; `AssignCredentials` via slotcreds.go:26; `ConfigureWorkspace` via sdkwarm.go:217; `Resume` via resume.go:50; `StartSession` via session.go:111. `ExportPaths` reaches `ensureSlotPaths` only from test files. — EVIDENCE: pkg/adapter/staging.go:133,:159,:317; pkg/adapter/slotcreds.go:26; pkg/adapter/sdkwarm.go:217; pkg/adapter/resume.go:50; pkg/adapter/session.go:111

FACT: the staged §5.2 "graceful window of ten seconds" for the §10.1 hold-timeout termination resolves. `pkg/adapter/holdstate.go:201` builds the close context with `context.WithTimeout(context.Background(), 10*time.Second)`. In the shipped tree that budget is SHARED across every held member of the loop at :204; the non-spec lane's per-member close-context split is what makes the staged per-session reading true, so the spec sentence and the code lane only agree once that split lands. — EVIDENCE: pkg/adapter/holdstate.go:201-204,:229

FACT: every verbatim anchor in the staging resolves in the current tree. spec/04:151, :157 (§4.1), :409-419 (§4.6.1 projection bullets), :673 (ConfigureWorkspace idempotent), :686 (the §4.7 `Shutdown` row), :853 (§4.7.9 step 5), :659/:695 (the §4.7.1/§4.7.2 boundary the SPEC-5 block inserts between); spec/05:453, :545; spec/06:80, :93-98, :150-156, :290; spec/07:23-24, :210, :213-214, :414; spec/15:1136, :1686 (§15.4.2 heading); spec/29:586-591, :705-711. — EVIDENCE: as listed

WATCHOUT: the §4.7 RPC table carries TWO `claimed ──→ draining` entries in spec/06's fence — one in the `Occupancy projection` block at :95-98 and one in the `Recycle edges` block at :113-118. SPEC-4's trigger-list replacement is scoped to the first by its own lead-in ("In the fenced `Occupancy projection` block"). An implementor or a reviewer who greps for the edge name alone will land on the wrong one. — EVIDENCE: spec/06_warm-pod-model.md:95-98,:113-118

USEFUL [review-log.md:884]: the "Dead end, and here are its five sites" entry saved a whole finding. The `**Slot cleanup:**` bullet at spec/05:545 still says "The adapter reports each slot cleanup outcome (`released` or `leaked`)" after the staging applies, which reads as contradicting the staged withheld-report rule on a `maxConcurrentSessions > 1` pod. The entry records that this has survived many rounds and that any re-opening must argue from the bullet's own trigger rather than from the session-release framing.

USEFUL [review-log.md:570]: the CORRECTED-in-round-14 clause on the reclaim hold's bound is what let me confirm in one read that this run's delta is the intended discharge of the standing DECISION rather than a fixer regression against it.

### [spec-recheck-2.3.review-fresh.1]

DECISION: filed exactly one finding — the staged §4.7.1 attempt-mismatch rule's closing claim that an
untokened entry "is released only under the unconditional-teardown rule or by the pod's retirement"
and that "every later bind attempt at that session on that pod is refused while it stands" — BECAUSE
three shipped adapter paths release an entry without a `Shutdown`: `Server.DemoteSDK`
(pkg/adapter/sdkwarm.go:296-298 → `releaseSessionSlot`), the SDK-warm `ConfigureWorkspace` failure
rollback (pkg/adapter/sdkwarm.go:236,:241,:251), and the §10.1.4 hold-timeout termination
(pkg/adapter/holdstate.go:190 `deregisterStartedSessions`). `ConfigureWorkspace` is one of the two
RPCs the same sentence names as the creator of an untokened entry, so the counterexample is inside the
sentence's own stated case — ALTERNATIVES: filing the §7.1 twin sentence separately (rejected: §7.1's
is scoped to "an untokened entry an abandoned attempt's late start left behind", which arises only on
a concurrent-session pod where no demotion runs; the fixer should still propagate).

FACT: `Server.DemoteSDK` deregisters the registry entry and removes the slot tree synchronously,
naming `anyRegisteredSession()` rather than a session id on the request. — EVIDENCE:
pkg/adapter/sdkwarm.go:274-303, :309-314.

FACT: the §10.1.4 hold-timeout termination's ten-second graceful window, which staged §5.2 states as
spec text, is a bare literal in the tree with no flag or config override. — EVIDENCE:
pkg/adapter/holdstate.go:200 `context.WithTimeout(context.Background(), 10*time.Second)`.

FACT: `mid_session` ships on `FinalizeWorkspaceRequest` (field 4) but NOT on
`PrepareWorkspaceRequest`, whose field 4 is reserved (ex-`slot_id`). The staged §4.7.1 carries-what
table ("Finalize … carried") and the §15.4 wire sentence ("the mid-session marker to
`PrepareWorkspace`") are consistent because of that asymmetry; do not read the table as a second
wire addition. — EVIDENCE: schemas/lenny-adapter.proto:682-696, :706-732.

FACT: every staged verbatim anchor in spec-changes.md resolves against the tree as of this round:
spec/04:157, :415-416, :686, :688/:695 (the §4.7.1 insertion window), :854;
spec/05:453, :545; spec/06:80, :95-97, :150-156, :158, :234, :290;
spec/07:23-24, :210, :213, :214, :414; spec/15:1136, :1469, :1471; spec/16:14; spec/29:704-711.
A round that re-checks anchors can start from this list rather than re-grepping all of them.

USEFUL [round-14 DECISION at review-log.md:2019 / :2049]: the whole r6→now delta in the spec staging is
that single §5.2 deletion plus a five-line rationale append in SPEC-3's prose. Reading those two log
entries first made the delta take five minutes instead of an hour.

WATCHOUT: review-log.md:2023 bars re-deriving a failed-cleanup terminal for the §5.2 hold paragraph.
The deletion leaves a derivable consequence — the hold is released while the tree removal has failed,
so a later attempt at the same session on a concurrent pod is neither held nor token-refused — but the
loop has settled that the `leaked` occupancy, not the identifier hold, is that case's disposition.
Do not file it. EVIDENCE: spec-changes.md:645, :650-659.

UNVERIFIED: the staged §4.7 row cites "[Section 15.4.2] graceful-shutdown signal", but §15.4.2 is the
RPC lifecycle state machine and names the signal only obliquely (the `DRAINING` row, spec/15:1702);
§29.4 step 13 calls the same thing the `terminate` frame on CH-RUNTIMEOPS. Nobody in this lane appears
to have checked whether §15.4.2 is the right citation for it. Cheap to check, low confidence it is a
defect.

### [spec-recheck-2.3.review-kubernetes.1]

DECISION: empty findings list for the Kubernetes-idiom lens on the spec staging — BECAUSE the whole staged spec file touches the apiserver at exactly one deliverable, SPEC-4, and every one of its replacements matches `ProjectOccupancyPhase` verbatim. Nothing in the staging adds a CRD field, a finalizer, a status-subresource writer, an admission webhook, a watch, or a reconcile; `grep -n "finaliz\|webhook\|RBAC\|reconcil\|\.status\|annotation\|SandboxClaim\|WarmPoolController\|informer\|watch"` over the staged file returns four hits, three of which are the word "finalize" in "finalize block" — ALTERNATIVES: filing the §16.1.1 `k8s_pod_name` "Used on" cell as a missed edit site (rejected, see below).

FACT: the SPEC-4 re-keying re-verified against the controller this round, independently of `non-spec.14.review-kubernetes.1`. With no claim, `ProjectOccupancyPhase` switches on the pod's current phase alone: `state.Reserved → (Idle, true)`, `state.Claimed → (Draining, true)`, everything else `("", false)`. The staged §4.6.1 bullets ("`idle` when the claim is deleted while the pod projects `reserved`"; "`draining`, then `terminated` ... or is deleted while the pod projects `claimed`, on a pool of either recycle setting") are exactly those two arms, and the deleted closing sentence ("the projection returns a pod from `claimed` to `idle` only on a recycling pool") is indeed false: no arm returns `Idle` from `Claimed` on any pool. — EVIDENCE: pkg/controller/warmpool/occupancy.go:113-143; spec/04_system-components.md:414-415; spec-changes.md:751-765

FACT: the edge-case bullet's gateway citations all resolve. `failPhase` is at binder.go:1072, its `b.drain` call at :1079 is outside the `leaseAssigned` block, `drain` is exactly `podclaim.DeleteClaim` at :1200-1201, and both `reclaim` closures that call `failPhase` sit at binder.go:866-869 and :998-1001. The gateway writes no `Sandbox.status` on any of these paths; `drain`'s own doc comment cites §4.6.3 for that. — EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:866,998,1072-1082,1196-1201

FACT: no gate reconciles §16.1.1's attribute table against §16.1's metric rows. `grep -rln "16.1.1\|Attribute Naming"` over `tests/` and `scripts/` returns six files, none of which reads the "Used on" column. — EVIDENCE: spec/16_observability.md:297; tests/tier11_docs/

DECISION: did not file the §16.1.1 "Used on" staleness — BECAUSE §16.1.1's normative rule is that a label "must not appear anywhere in the spec unless it is in this table", and `k8s_pod_name` is in the table; the "Used on" cell is descriptive, no gate reads it, and the cell's current enumeration ("Session-pool scrub and retirement metrics, slot failure and replacement metrics, and the session reuse histogram") plausibly already covers a slot-compensation counter. An earlier round refuted several findings of this exact class (a descriptive carrier lagging an authoritative one) as documentation completeness. ALTERNATIVES: filing it under (d) as a missed spec edit site; rejected on the uncertainty default.

USEFUL [non-spec.14.review-kubernetes.1]: its `ProjectOccupancyPhase` arm-by-arm derivation and its WATCHOUT on the "warm-inventory phase" looseness saved this round the whole controller read and stopped a re-file. Its FACT that `POD_NAME` reaches the adapter through the Downward API (`podspec.PodNameEnvVar`) is also what makes SPEC-6's adapter-side `k8s_pod_name` label producible; do not re-derive either.

FACT: the spec staging has not moved this round. `diff -rq` against `scratchpad/cp-snap/0081_.../spec-recheck-2-r3-start` reports no differences at all; `spec-recheck-2-r3`, `-r2`, `-r2-start` and `-r1-start` are byte-identical to each other and predate the 2026-09-16 token revision, so diffing against any of them shows the whole revision rather than this round's delta. Use `-r3-start`.

WATCHOUT: the orchestrator brief for this run names `scratchpad/cp-snap/0081_.../spec-recheck-r6` as the snapshot and describes the loop as "spec-recheck round 3". Both are stale: the live lane is `spec-recheck-2` and the review log has since run `non-spec.14` and `non-spec.15` rounds. Trust `diff -rq` over the whole snapshot directory rather than the brief's named snapshot. — EVIDENCE: review-log.md:2446,2635

### [spec-recheck-2.3.review-mechanism.1]

DECISION: returned no findings on the r6→now spec-changes delta — BECAUSE the delta is one deletion plus one rationale paragraph, and the deletion removes a statement that contradicted the other two statements of the same window. Deleted from the staged §5.2 reclaim-hold paragraph: "On a pod serving concurrent sessions a cleanup that does not finish within the timeout the **Slot cleanup:** bullet states is a failed cleanup under that bullet, so the slot is `leaked` and its identifier stays held until the pod terminates." After the deletion §5.2 (spec-changes.md:644), §6.2's pointer paragraph and §15.4's published hold block all say the same thing: the hold runs from the deregistration to the cleanup's return, on every arm. ALTERNATIVES: filing the deletion as an orphaned disposition — rejected, see FACT below.

FACT: deleting the failed-cleanup hold clause orphans nothing, because the SHIPPED `**Slot cleanup:**` bullet still carries the disposition: "If cleanup fails, the slot is leaked — the pod continues but the slot is not reclaimed until pod termination." EVIDENCE: spec/05_runtime-registry-and-pool-model.md:545. The staged commentary at spec-changes.md:654-657 attributes the disposition to the scrub-model paragraph's `leaked` clause instead, which is narrower (that clause is scoped to a pre-`running` cleanup a reclaiming `Shutdown` performs, spec-changes.md:643). The attribution is incomplete rather than false, so it is below the bar; a future round that wants to tighten it should widen the rationale, not re-add the deleted sentence.

FACT: adapter-side there is no such thing as a held-forever identifier after a failed cleanup. `Shutdown` deregisters, discards `removeSlotTree`'s error with `_ =`, and reports the outcome from `closeErr` alone; `leaked` is a GATEWAY ledger state (`Binder.ReleaseSlot` sets `leaked = err != nil || !cleanly`). EVIDENCE: pkg/adapter/session.go:271,:279,:290. This is why the standing DECISION at review-log.md:335 ("carries no `leaked` clause") and the delta agree with the tree.

WATCHOUT: the accepted-failure-mode bullet at spec-changes.md:168-176 says a retry naming the held identifier "is refused, on the reclaim hold while the cleanup runs and on the attempt token afterwards". The "afterwards" arm holds only in the ordering where the adapter never received the reclaim, so the entry survives. In the ordering where the adapter ran the cleanup and only the ANSWER was lost — which is the bullet's own headline case, "a reclaim the adapter does not acknowledge" — the entry is gone and the retry is ADMITTED by the create-and-stamp rule (spec-changes.md:833). I did not file it: the sentence is proposal commentary about an accepted mode, no staged spec edit depends on it, and admission there is the benign outcome. A later round that wants it exact should narrow the bullet, not the staged text. EVIDENCE: spec-changes.md:168-176, :833.

FACT: the §15.4 rule-name list (spec-changes.md:899) names BOTH "the pairing rule" and "the teardown-pairing rule"; they are two different rules and both are defined — the pairing rule at spec-changes.md:823 (non-`Shutdown` requests, empty token with `mid_session` false, or non-empty token with `mid_session` true → `INVALID_ARGUMENT`) and the teardown-pairing rule at :858. Do not read the pair as a duplicate or a typo.

### [spec-recheck-2.3.review-operational.1]

FACT: The delta this round is confined to the staged §5.2 reclaim-hold paragraph: the sentence
"On a pod serving concurrent sessions a cleanup that does not finish within the timeout ... is a
failed cleanup under that bullet, so the slot is `leaked` and its identifier stays held until the
pod terminates" was deleted, and a four-sentence rationale was appended separating the identifier
hold from the `leaked` occupancy. — EVIDENCE: spec-changes.md:645, :650-658

FACT: `lenny_adapter_leaked_slots` is a GATEWAY-registered gauge despite its name
(`pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go:223`), published from
`cmd/lenny-gateway/sessionsrv.go:382` and driven by `applySlotRetryPolicy`'s
`ReleaseSlotReservation`-errored arm (`pkg/gateway/sessionserver/start.go:2834-2848`). It is
deliberately absent from the §16.1 typed catalog. Do not look for an adapter emitter.
— EVIDENCE: pkg/gateway/sessionserver/start.go:2848

FACT: `spec161Metrics` (pkg/observability/metrics/catalog_test.go:15) is reconciled BOTH ways
against `MetricCatalog()` (catalog_test.go:188-211), and its comment claiming it lists "every
metric name §16.1 defines" is false: the four adapter-scrape-deferral rows at
spec/16_observability.md:186-189 are absent from it. CODE-9's exclusion of the untokened-entry
series from that list is therefore correct. — EVIDENCE: pkg/observability/metrics/catalog_test.go:15-30

FACT: the shipped §6.2 per-slot fence states exactly one trigger for `slot_cleanup ──→ leaked`,
"cleanup timeout exceeded", and docs/reference/state-machines.md:251 mirrors it verbatim in prose.
No shipped spec text ties `leaked` to a `Shutdown` response's clean-exit answer; the staged §7.1
paragraph is the first. — EVIDENCE: spec/06_warm-pod-model.md:148, docs/reference/state-machines.md:251

UNVERIFIED: SPEC-6's staged §16.1 row for `lenny_slot_shutdown_untokened_entry_total` says
"Adapter-side; not scraped until the adapter metrics endpoint is wired"
(spec-changes.md:1005), while CODE-9 asserts "SPEC-6's row for this series carries the deferral
wording verbatim, so it follows the §16.1 rows that already carry it"
(non-spec-changes.md:1922-1923). The four sibling rows carry
"emitted by the adapter process inside the agent pod and therefore outside the
[§16.9](#169-prometheus-scrape-targets-and-crds) default scrape target set until a deployer
wires an adapter scrape target" (spec/16_observability.md:186-189). The staged row is a
paraphrase that drops the §16.9 pointer. I judged this below the correctness bar (the substance
is conveyed and no gate reads the wording) and did NOT file it; a later round or the
reconciliation pass may want to align one of the two texts.

OPEN: the §16.1 superseded row says a `superseded` reclaim means "the slot is not leaked"
(spec-changes.md:1004) without the clean-exit qualifier §7.1's fail-closed predicate carries
(":458, a reclaim ... whose answer does not report a clean exit, whatever outcome that answer
carries ... leaves the slot `leaked`"). I decided this is NOT a defect, because §4.7.1's
reclaim-outcome rule makes `superseded` imply a clean exit normatively (spec-changes.md:866), so
the row states the conforming meaning and §7.1's widening is a gateway-side defence against a
non-conforming adapter. Recorded so a later lens does not re-derive and file it.

WATCHOUT: no runbook and no alert reference slot leaks, so a leaked-slot change carries no
alert-to-runbook obligation under tests/tier11_docs/alert_runbook_test.go. Do not look for one.
— EVIDENCE: `grep -rln slot docs/runbooks/` returns only redis-failure.md,
token-store-unavailable.md and ephemeral-container-cred-guard-unavailable.md

### [spec-recheck-2.3.review-performance.1]

FACT: The staged edits create no new control-plane write path. No CRD field, no status
condition, no new informer or watch, and no per-request etcd/Postgres/Redis write is added;
the only additions are two Prometheus counters (spec-changes.md:1004-1005) whose label sets
(`pool`, `k8s_pod_name`) duplicate the shipped `lenny_slot_failure_total` row, so cardinality
at the top tier is unchanged. — EVIDENCE: spec/16_observability.md:14-15, :297
FACT: The reclaim hold serializes per slot identifier only, never pod-wide, so it adds no
single-key or single-leader bottleneck; the one extra `Shutdown` per failed bind scales with
the failure rate rather than with request volume. — EVIDENCE: spec-changes.md:645
FACT: `slotlayout.EnsureTree` is `os.MkdirAll` + `os.Chmod` per directory, so it reuses a
residual tree in place rather than recreating it. Any successor admitted onto a slot whose
cleanup failed inherits that residue. — EVIDENCE: pkg/adapter/slotlayout/tree.go:24-47
WATCHOUT: This round's delta deleted the §5.2 clause "so the slot is `leaked` and its
identifier stays held until the pod terminates" and replaced it with commentary asserting
"the hold's bound is the cleanup's return on every arm, success or failure". That deletion is
correct against the staged code (every staged reclaim site releases the hold unconditionally),
but it removes the only fence that kept a successor off a leaked slot's residue, and it
falsifies the edge-case bullet that still claims the retry "is refused ... on the attempt
token afterwards". — EVIDENCE: spec-changes.md:652-658, :175-179
FACT: The `leaked` disposition is a gateway-side accounting state read off the `Shutdown`
response's clean-exit answer. The adapter holds no leaked-slot set and nothing in
`pkg/adapter` refuses admission on one, so "the slot is leaked" fences nothing on the pod.
— EVIDENCE: spec-changes.md:643, :458; pkg/adapter/sessionscrubreporter.go:33-41
UNVERIFIED: whether a §5.2 slot retry genuinely reuses the failed attempt's slot identifier.
The proposal asserts it throughout (spec-changes.md:176-178, :458) while shipped §5.2 says
"The retry is always assigned to a **new slot**" (spec/05:555) and shipped §5.2:395 says the
slot identifier IS the session identifier. The two shipped sentences are in tension
independently of this proposal; the token design depends on the identifier being shared.
Someone should settle which reading the retry policy intends.

### [spec-recheck-2.3.review-reliability.1]

DECISION: returned an empty findings list for the reliability lens on this recheck — BECAUSE the whole delta in the spec staging is one deleted sentence in the §5.2 reclaim-hold paragraph plus a five-line rationale appended to SPEC-3's commentary, and both verify clean: the hold's remaining bound ("The hold lasts as long as the cleanup it covers", spec-changes.md:645) still terminates on every arm, and the failed-cleanup disposition it stopped stating is stated independently by the scrub-model paragraph (spec-changes.md:643, "does not report a clean exit ... the slot is then `leaked`") and read by §7.1 (spec-changes.md:462-463) — ALTERNATIVES: filing that the deletion frees the slot identifier after a cleanup that failed, so a same-pod retry is admitted onto residue (rejected, see MISTAKE-avoided below).

FACT: the identifier hold and the `leaked` occupancy really are separate objects in the tree, which is what makes the deletion safe. `leaked` is gateway-side accounting: `applySlotRetryPolicy` calls `slots.MarkLeaked`/`health.RecordLeak` on a release error and the Redis slot counter keeps the occupancy, while the adapter's own registry has already dropped the entry. — EVIDENCE: pkg/gateway/sessionserver/start.go:2833-2849; pkg/gateway/podlifecycle/podsession/slotbinder.go:529-545

MISTAKE avoided (do not re-file): "the delta lets a retry inherit the failed slot's residue, falsifying spec/05:556's fresh-workspace guarantee". The guarantee is already violated in the shipped tree and the delta does not widen it: `slotlayout.EnsureTree` is `MkdirAll` only and purges nothing (pkg/adapter/slotlayout/tree.go:24-45), `RemoveTree` is best-effort and can leave residue (tree.go:48-66), and shipped `claimSessionSlotUnderLock` refuses only a *started* entry, so a retry onto an unstarted stale entry is admitted today (pkg/adapter/slotsession.go:66-88). The residue path is therefore pre-existing, and the token rules the proposal adds narrow it rather than widen it.

FACT: the "graceful window of ten seconds" the staged §5.2 hold paragraph attributes to the §10.1 hold-timeout termination is not in spec/10 — it is the code's shared close context in `onHoldTimeout` (`context.WithTimeout(context.Background(), 10*time.Second)`), and the surrounding comment explains why one shared budget is still a per-close bound ("only the last member's close consumes the grace"). The staged sentence cites §10.1 for the identity of the termination, not for the figure, so it is not a false citation. — EVIDENCE: pkg/adapter/holdstate.go:196-205; spec/10_gateway-internals.md:58 (states `coordinatorHoldTimeoutSeconds`, no 10s window)

USEFUL [review-log.md:570]: the standing DECISION that mandated the now-deleted sentence carries its own round-14 CORRECTED clause, so it no longer contradicts the staging. A reviewer who reads only the first half of that entry will re-derive a dead finding.


### [spec-recheck-2.3.review-security.1]

FACT: The slot identifier is the session identifier in the shipped adapter — `Server.slots` is
keyed by `sessionID` (`pkg/adapter/slot.go:159`, `:222`; `pkg/adapter/slotsession.go:175`;
`pkg/adapter/slotsession_test.go:216-217` keys on `"alice"`/`"bob"`). This is load-bearing for
the security lens: no slot identifier is ever reused across tenants, so every "a successor binds
onto residue the failed attempt left" argument is confined to one session and one tenant, and
none of them is a cross-tenant exposure. Any future finding that argues residual workspace or
credential state is exposed to another tenant by slot reuse is wrong for this reason.
EVIDENCE: pkg/adapter/slot.go:159

FACT: This round's only spec-staging delta is the §5.2 reclaim-hold paragraph. It deletes the
sentence "On a pod serving concurrent sessions a cleanup that does not finish within the timeout
... is a failed cleanup under that bullet, so the slot is `leaked` and its identifier stays held
until the pod terminates", and adds commentary stating the hold's bound is the cleanup's return
on every arm and that the identifier hold and the `leaked` occupancy are separate objects.
I checked the deletion for a control regression and it is not one: the occupancy fence that
actually prevents the gateway from assigning over unreleased resources is §6.2's `leaked`
semantics, which is untouched and still reads "remains counted in the pod's Redis slot-counter
occupancy (preventing the gateway from over-assigning new slots that would conflict with the
leaked slot's unreleased resources)" (spec/06_warm-pod-model.md:160), and the shipped
`**Slot cleanup:**` bullet still carries "If cleanup fails, the slot is leaked — the pod
continues but the slot is not reclaimed until pod termination"
(spec/05_runtime-registry-and-pool-model.md:545).
EVIDENCE: spec/06_warm-pod-model.md:160

FACT: The staged SPEC-3 claim that the §5.2 whole-pod credential purge "is addressed from disk"
and reaches a credential file a per-slot cleanup left behind is accurate, and so is the staged
§6.2 claim that the scrub "removes every per-slot workspace tree and every per-slot credential
file the pod still holds and verifies their absence". Step 0 removes every
`/run/lenny/slots/{sessionId}/credentials.json` under `/run/lenny/slots/` before any deployer
code runs, step 2 is `rm -rf /workspace/slots/*`, and step 6 stat-checks both paths and marks the
scrub failed if either is non-empty.
EVIDENCE: spec/05_runtime-registry-and-pool-model.md:461, :467, :471

FACT: SPEC-4's re-keying of every claim-deletion projection clause on the phase the pod projects
at the claim DELETE preserves the scrub-before-idle security invariant rather than relaxing it.
`ProjectOccupancyPhase` returns `Draining` from `state.Claimed` on every pool, with the in-tree
comment "Returning it to idle would break the scrub-before-idle invariant (§5.2), because the
whole-pod scrub runs in the reserved-bound recycle path the pod never reached".
EVIDENCE: pkg/controller/warmpool/occupancy.go:134-140, :57-63

DECISION: I filed nothing — BECAUSE every candidate I developed under the two security checks
either was pre-existing behaviour the staging records rather than introduces, or was already
adjudicated. ALTERNATIVES considered and rejected, so a later security lens does not re-derive
them: (a) the deleted failed-cleanup hold sentence as a residual-state regression, rejected on
the session-scoped-identifier FACT above plus the intact §6.2 occupancy fence; (b) the
§11.4 revoke fan-out defeated by the new teardown-pairing rule (a `Shutdown` carrying neither
field performs nothing), rejected because the staged §4.7 row states normatively that every
request carries one of the two, spec/11_policy-and-controls.md:263 states no field set of its
own and cites §4.7, and the unconditional-teardown rule is addressed to whatever entry the
adapter holds and is outside the reclaim hold, so the revoke path is fail-closed as staged;
(c) the accepted "attempt recreates its own entry after an unconditional teardown" residue as a
revoke bypass, rejected because the race pre-exists (today's adapter creates the entry on
`ensureSlotPaths` and records the start unconditionally) and the new start-confirmation rule
makes that path strictly no worse; (d) `exited_cleanly` as an attacker-influenceable self-report
bounding the leak count, rejected because the adapter already reports `released`/`leaked` via
`ReportSessionScrub` today (spec/05_runtime-registry-and-pool-model.md:545) and the staged §7.1
predicate quantifies over every outcome precisely so it fails closed against a non-conforming
adapter.
EVIDENCE: spec/11_policy-and-controls.md:263

WATCHOUT: spec/13_security-model.md:30 states that a deployer needing strict credential-lease
isolation "MUST set `sessionPolicy.maxConcurrentSessions: 1`", and grounds that on sequential
reuse releasing each lease before the next session begins. A future lens will be tempted to read
the staged "no per-slot cleanup runs at all for a bind that fails at the workspace-preparation,
setup or credential-assignment stage" on a one-session pod as falsifying it. It does not: the
pre-attached failure disposition deletes that pod's claim while it projects `claimed`, so the pod
retires rather than being reused, and the staged §6.2 paragraph says so explicitly.
EVIDENCE: spec/13_security-model.md:30

FACT: The orchestrator's brief names `ShutdownResponse.slot_was_started` (field 4). No such field
exists anywhere in the current staging; `ShutdownResponse` gains only `slot_reclaim` at field 3
(implementation-checklist.md:33). Do not review a field the staging does not carry.
EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.implementation-checklist.md:33

### [spec-recheck-2.4.review-applicability.1]

DECISION: returned an EMPTY findings list for the applicability-and-sequencing lens on the spec staging — BECAUSE every "reads, verbatim" anchor still resolves byte-for-byte and uniquely, every minted markdown anchor resolves to a real heading, every artifact the spec lane creates has its properties stated, and the r3 delta (the reinstated failed-cleanup terminal on the §5.2 reclaim hold) was propagated to all three of its dependent restatements. ALTERNATIVES rejected: (a) the §15.4 published reclaim-hold block not carrying the new "and for the life of the pod where that cleanup does not complete" clause — rejected, "until the cleanup has completed" entails it for a cleanup that never completes, and the §15.4 bind-attempt block already defers the hold's scope to §5.2 explicitly ("the reclaim hold's scope, its refusal and its answer are stated by [Section 5.2] ... whose wire form the reclaim-hold block below publishes"); (b) no error *category* stated for the pairing, mid-session-create, teardown-pairing and start-confirmation rules while §15.4 asserts "the status code and error category ... are each stated once, by that section" — rejected, those four mint no `ErrorCode` so they carry no `Error` envelope and no category applies; (c) the §4.7 DemoteSDK row gaining no spec statement while DOCS-2 adds the registry-entry drop to the published mirror — barred by the standing Trap at review-log.md:1068.

FACT: the delta this round is NOT the snapshot the brief names. `spec-recheck-2-r4` is byte-identical to the live proposal, and `spec-recheck-2-r2`/`-r3`/`-r2-start` are Sep-11 directories from an older loop that still carry the withdrawn epoch design. The real r3→r4 delta is `diff -u scratchpad/cp-snap/.../spec-recheck-2-r3-start/...spec-changes.md proposals/...spec-changes.md` = 13 hunks, and its substance is one change: the §5.2 reclaim hold now ends only when the cleanup has *completed* ("every act that cleanup owes the slot has returned without error") and stays held for the life of the pod otherwise. EVIDENCE: `ls -ld scratchpad/cp-snap/0081_*/spec-recheck-2-r*` (Sep 11 vs Sep 18 mtimes); spec-changes.md:652

FACT: the r3 fix propagated the life-of-pod clause to three of the four sites that restate the hold window — §5.2 (spec-changes.md:652, normative), §7.1's paragraph (:466) and §6.2's pointer paragraph (:807) — and NOT to §15.4's published block (:915), which still ends at "until the cleanup that reclaims the slot has completed". Judged below the bar (entailment, and §15.4 defers scope to §5.2), but a future fixer touching any of the four should touch all four.

FACT: the §4.7.9 step-5 finding the r3 applicability shard filed is LANDED. The staged replacement now reads "...takes the failure branch stated in [Section 7.1]." with the "rather than leaving the pod holding the session's state" tail removed. EVIDENCE: spec-changes.md:609

FACT: the anchor set re-verified today, byte-for-byte and unique: spec/04:157, :415, :416, :674(untouched), :686, :854; spec/04 insertion point (`*Adapter → Gateway RPCs:*` at :688, `#### 4.7.2` at :695); spec/05:453, :545; spec/06:80 (all three replaced projection clauses, one line, one occurrence each), :95 (unique within the `Occupancy projection` group though the string occurs five times in the file), :152-153, :158, :234, :290; spec/07:23 (inside the ```-fenced flow listing, with the continuation line `(executionMode, isolationProfile, scrubPolicy summary)` at :24, which is exactly where the staged §7.1 paragraph says to insert), :210, :213, :214, :414; spec/15:1136, :1469, :1471; spec/16:14 (two-column table, matching the staged rows); spec/29:711 (unique only under "In §29.4's numbered step 13"; the same trailing citation occurs at :645, :887 and :982).

FACT: §4.7.1's sixteen named rules and §15.4's non-conformance list are the SAME sixteen, in the same spelling, with nothing in one that is missing from the other. Counted both this round: pairing, reclaim hold, mid-session-create, create-and-stamp, attempt identity, started-session, admit, stamp-once, start-confirmation, first-frame, teardown-pairing, no-entry, unconditional-teardown, attempt-mismatch, attempt-match, reclaim-outcome. EVIDENCE: spec-changes.md:820-880, :905

FACT: DOCS-3's "four sentence replacements and a replaced remedy cell" is arithmetically right against the page, not a double count. `docs/reference/error-catalog.md:129` carries a cause sentence, a surfaced sentence, a `details.reason`+setup-output sentence, a "Not retryable" sentence and a non-deterministic-fallback sentence in the description column, plus a separate Remedy column. Three of the four sentence replacements mirror §15.1; the fourth is the fallback sentence §15.1 keys on the gRPC code instead. EVIDENCE: docs/reference/error-catalog.md:129; spec-changes.md:1083-1086

WATCHOUT: `FinalizeWorkspaceRequest.mid_session` is ALREADY field 4 in the shipped proto, so §4.7.1's carries-table marking `mid_session` "carried" on both `PrepareWorkspace` and `FinalizeWorkspace` while SCHEMA-1 adds the field only to `PrepareWorkspace` is NOT a contradiction. EVIDENCE: schemas/lenny-adapter.proto:724

USEFUL [review-log.md:884 / :2926]: the "Dead end, and here are its five sites" entry saved a whole finding again. After SPEC-3 applies, the unedited `**Slot cleanup:**` bullet at spec/05:545 still says "The adapter reports each slot cleanup outcome (`released` or `leaked`)" while the appended scrub-model text withholds the report on the pre-`running` path. That reads as a §5.2-internal contradiction on a `maxConcurrentSessions > 1` pod and is a recorded dead end; any re-opening must argue from the bullet's own trigger, never from the "session release" framing.

USEFUL [review-log.md:1068]: barred me from filing `adapter-contract.md:64` (the `DemoteSDK` row gaining a registry-entry statement with no §4.7 spec source). Three lenses already declined it on the ground that the row is a verbatim mirror of spec/04:674, which this proposal does not edit, and that §4.7.1 leaves the summary incomplete rather than false.


### [spec-recheck-2.4.review-citations.1]

FACT: `diff -ru` between the spec-recheck-2-r4 snapshot and the live proposal directory is EMPTY — nothing changed since the previous round of this lane, so this recheck had no delta to look at and the whole staging had to be re-walked. EVIDENCE: scratchpad/cp-snap/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/spec-recheck-2-r4
FACT: every file:line citation in the spec-changes file resolves and says what the proposal says it says. Verified individually: binder.go:867/:998/:1072-1082/:1200-1202 (reclaim closure, Launch reclaim closure, failPhase, drain→DeleteClaim); occupancy.go:57-59/:128-140/:132-140/:134-140 (the comment and the no-claim switch; `ProjectOccupancyPhase` is the real symbol name at :84); lenny-adapter.proto:1621-1627 (the `recycle` comment); spec/04:151 and :157; spec/29:586-588, :589-591, :669-674, :697. EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.spec-changes.md:235-237,368,377,382,435-441,702-706,755
FACT: every verbatim anchor block the staging quotes for replacement is byte-accurate against the tree. Checked: spec/04:157 (§4.1 third sentence), :686 (§4.7 `Shutdown` row opening), :854 (§4.7.9 step 5), :415-416 (§4.6.1's two claim-deletion bullets); spec/05:453 (`**Scrub model.**`), :545 (`**Slot cleanup:**`); spec/06:80 (the three projection prose clauses), :95-97 (the fence `claimed ──→ draining` trigger), :152 (`receiving_uploads ──→ running`), :234 (mid-resume cancel clause), :290 (`**Client visibility:**` clause); spec/07:23 (atomicity paragraph tail), :210/:213/:214 (§7.2 preamble, step 2, step 3), :414 (§7.3 list item 4); spec/15:1136 (the three `SETUP_COMMAND_FAILED` sentences); spec/29:711 (step 13 tail). Only cosmetic drift found: §4.6.1's deleted closing sentence is quoted with `§6.2` where the file writes `[Section 6.2](...)`. Not a finding.
FACT: all staged markdown anchors resolve to real headings (§4.6.1, §4.7, §4.7.1, §4.7.9, §4.9, §5.2, §6.2, §7.1-§7.4, §10.1, §15.1, §15.4, §15.4.2, §15.4.3, §16.1). The §7.1 insertion point really is inside the fenced flow listing (fence opens spec/07_session-lifecycle.md:5), and the existing atomicity paragraph already carries markdown links inside that fence, so the new paragraph's links are precedented.
FACT: §15.4's wire-edit sentence saying the schema edit "adds ... the mid-session marker to `PrepareWorkspace`" is CORRECT and reads like a bug but is not: `FinalizeWorkspaceRequest` already carries `bool mid_session = 4`, `PrepareWorkspaceRequest` does not. EVIDENCE: schemas/lenny-adapter.proto:724 vs :682-696
FACT: the seven-request "can create an entry" list in the staged §5.2 hold and §4.7.1 Admission paragraphs matches the tree exactly — ensureSlotPaths from staging.go:134/:181/:337 (Prepare/Finalize/RunSetup), ensureSlotStateLocked from slotcreds.go:26 (AssignCredentials), claimSessionSlot from session.go:111 (StartSession), resume.go:50 (Resume), sdkwarm.go:217 (ConfigureWorkspace).
FACT: §15.4's rule-name roll-call is complete — it lists exactly the sixteen rules §4.7.1 names, with none missing and none invented. Do not re-derive this; it was counted.
FACT: the DemoteSDK claim in the staged §5.2 hold ("a release that runs its cleanup inside the RPC that requested it ends the hold before that RPC answers") is true of the tree: `DemoteSDK` calls `releaseSessionSlot` synchronously. EVIDENCE: pkg/adapter/sdkwarm.go:296-298
WATCHOUT: `docs/reference/adapter-contract.md:81` (the `ReportSessionScrub` row) says the outcome is reported "at each session release, on a pod of any concurrency and any recycle setting". SPEC-3 withholds that report on the pre-`running` path, and DOCS-2's own replacement `Shutdown` row on the same page says the adapter "reports no outcome for a cleanup on a slot the runtime was never given". No deliverable touches :81. This is the one finding this pass filed. EVIDENCE: docs/reference/adapter-contract.md:81 vs non-spec-changes.md:2348
UNVERIFIED: three operator/reference pages carry the same unqualified sentence ("A per-slot cleanup runs at each session release ... and the adapter reports its outcome to the gateway") and are equally unstaged: docs/operator-guide/security-principles.md:33, docs/operator-guide/multi-tenancy.md:72, docs/reference/execution-modes.md:68. I did not file them, because they are audience-level summaries rather than the RPC's reference mirror and they do not contradict a sibling row on their own page. A later docs lane should decide whether the §5.2 narrowing reaches them.
UNVERIFIED: the staged §5.2 hold states "a graceful window of ten seconds" for the §10.1 hold-timeout termination. No spec section states that figure today (`grep "graceful window" spec/` returns nothing, and §10.1's hold-timeout bullet names none), and `terminateHeldSession` passes no deadline — it calls `s.Runtime.Close(ctx, ...)` on the bare context. It is a new normative value rather than a citation, so it is not a citation-lens finding, but nobody has checked it against the §10.1.4 close budget. EVIDENCE: pkg/adapter/holdstate.go:246-248

### [spec-recheck-2.4.review-fresh.1]

DECISION: returned an EMPTY findings list for the fresh-holistic lens on the spec staging — BECAUSE every candidate I built independently this round resolved either to a verified-clean fact or to a dead end the Standing context / archive already records with the exact ground it was killed on. ALTERNATIVES rejected, in the order I hit them: (a) §4.7.1's reclaim-outcome clause "which it takes from the runtime close and the cleanup the removing rule performed" (spec-changes.md:879) versus the NEW §7.1 sentence "a reclaim that closes the runtime cleanly and fails to remove the slot tree reports a clean exit and reports that cleanup `leaked`" (:465) — they are compatible, because :879 names the two SOURCES an unclean exit can derive from rather than asserting that every cleanup failure surfaces on the flag, and §5.2:650 does the partition explicitly (pre-`running` failure → not clean on the response; started-path failure → clean on the response and `leaked` on `ReportSessionScrub`); (b) §5.2:545's surviving "The adapter reports each slot cleanup outcome" versus SPEC-3's withheld-report rule — the recorded dead end at review-log.md:884; (c) §7.3 re-attach onto a ONE-SESSION replacement pod being inside §7.1's obligation while §5.2/§6.2 say the obligation does not reach such a pod — killed, and confirmed again from the tree below; (d) the §6.2 `**Client visibility:**` bullet excluding a case from the `STARTING_FAILED` fallback without assigning it — close variant of the already-refuted §15.1 exclusion-clause finding, and §15.1 assigns it; (e) the Design section's "SPEC-4 states that once, in §6.2" (:59) against SPEC-4's own §4.6.1 bullet restating the same rule (:772) — recorded below the bar at archive:37850.

FACT: `Binder.Resume` issues ONLY `cl.Resume` after `reserveResumeSlot` — no `PrepareWorkspace`, `FinalizeWorkspace`, `RunSetup` or `AssignCredentials`. That is what keeps SPEC-3's §5.2 sentence ("no per-slot cleanup runs at all for a bind that fails at the workspace-preparation, setup or credential-assignment stage there") and SPEC-4's §6.2 twin from being FALSE for a §7.3 re-attach onto a `maxConcurrentSessions: 1` replacement pod, even though §7.1's scope parenthetical puts that re-attach inside the obligation with no concurrency qualifier. A later round re-deriving the one-session/§7.3 tension must start here: the resume path has none of the three stages those sentences quantify over. EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1590-1630

FACT: every verbatim "reads, verbatim" anchor in the staging still resolves in the tree, re-checked this round one by one: spec/04:157 (§4.1 third sentence), :415-416 (§4.6.1 both claim-deletion bullets, including the false closing sentence SPEC-4 deletes), :686 (§4.7 `Shutdown` row opening), :854 (§4.7.9 step 5), spec/05:453 (`**Scrub model.**`), :545 (`**Slot cleanup:**` action list), spec/06:80 (all three projection-prose clauses), :95-97 (the `claimed ──→ draining` fence entry), :234 (mid-resume cancel clause), :290 (`**Client visibility:**` clause), spec/07:210,:213,:214 (§7.2 preamble, step 2, step 3), :414 (§7.3 list item 4), spec/15:1136 (all three §15.1 `SETUP_COMMAND_FAILED` sentences), spec/29:711 (§29.4 step 13 anchor — note :645, :887 and :982 carry the SAME `([§15.4.3]…, §28.5.3).` string in §29.3, §29.7 and §29.9, so an implementor must locate step 13 by its enclosing section, not by grep). Both insertion points are clean: §4.7.1's table closes at spec/04:688 with §4.7.2 at :695, and §15.4's `**SDK-warm demotion contract:**` is at spec/15:1469 with §15.4.1 at :1471.

FACT: `pkg/controller/warmpool/occupancy.go` backs SPEC-4 exactly as cited — the comment at :57-59 is verbatim ("the §6.2 state machine encodes the recycle-versus-one-session distinction in the phase the pod sits in at the claim DELETE"), `ProjectOccupancyPhase` is at :84, and the `Reserved → Idle` / `Claimed → Draining` switch is at :128-141, so `idle` is never returned from `claimed`. The edge-case bullet's gateway cites also hold: `failPhase` at binder.go:1072-1082 calls `drain` → `DeleteClaim` (:1200-1202) and the two `reclaim` closures are at :866-868 and :997-999.

FACT: "warm-inventory phase", the qualifier SPEC-4 adds to §6.2's no-claim clause, appears exactly once in the whole tree, at spec/04:411 (§4.6.1's `idle` bullet). The proposal's claim that §4.6.1 "already carries the warm-inventory qualifier and is untouched" is accurate, and the term is undefined in both places. Do not file the undefinedness as new: SPEC-4 imports a precedent rather than minting a term.

FACT: the §15.4 rule-name roster (spec-changes.md:910) lists sixteen names and all sixteen are actually stated in the staged §4.7.1 block (stamp-once :828, start-confirmation :830, pairing :834, reclaim hold :834, mid-session-create :838, create-and-stamp :840, attempt identity :842, started-session :844, admit :846, first-frame :865, teardown-pairing :869, no-entry :871, unconditional-teardown :873, attempt-mismatch :875, attempt-match :877, reclaim-outcome :879). The roster is complete; re-deriving it costs twenty minutes.

FACT: this round's fixer swept "has finished" → "has completed" plus the "and for the life of the pod where that cleanup does not complete" extension at ALL FIVE hold sites and left none behind: Design :65 and :154, §7.1 :465, §5.2 :652, §6.2 prose :807, and §15.4 :916 (whose non-conformance clause was correspondingly re-keyed from "while the cleanup is running" to "while the identifier is held"). `grep -n "has finished" spec-changes.md` returns nothing.

WATCHOUT: the pod-life hold has an unstated consequence at the SDK-demotion site. §5.2's staged sentence reads "A release that runs its cleanup inside the RPC that requested it ends the hold before that RPC answers **when that cleanup completes**", so a `DemoteSDK` whose per-slot cleanup FAILS holds the identifier for the pod's life, and the pod-warm bind sequence the demotion exists to enable is then refused for the same session on the same pod until the pod is replaced. I did not file it: the staging states the general rule, every sentence stays true, and the remedy is a residue bullet, which this loop's material skeptic has refuted as documentation completeness five times. EVIDENCE: spec-changes.md:652

WATCHOUT: the accepted-failure bullet at spec-changes.md:277-279 calls the §10.1 hold-timeout termination one of the "request[s] that remove an entry without naming a bind attempt", while staged §5.2 says that same termination "runs under no request" (:652). Real, and internal to the staging, but it is rationale prose in the proposal's own edge-case list rather than staged spec text, and it changes no applied statement. Below the bar as I read it; a later fixer touching either sentence should bring them into line.

USEFUL [review-log.md:884, the "withheld report contradicts §4.7 or §12" dead end, and its CORRECTED note]: spec/05:545's surviving "The adapter reports each slot cleanup outcome" was, as three earlier shards predicted, the first thing this lens landed on. The entry names the exact ground (argue from the bullet's own trigger and SPEC-3's pointer sentence, never from the "session release" framing) and saved the finding.

USEFUL [review-log-archive.md:38852]: the §7.3-re-attach-on-a-one-session-pod entry told me precisely what a filing would have to show ("a staged sentence that is FALSE, not one that is silent") and pointed at spec-changes.md:643. I built the case independently, found the two candidate-false sentences, and then killed it on the code fact above. Without that entry it would have cost a full round.

### [spec-recheck-2.4.review-performance.1]

DECISION: returned an EMPTY findings list for the capacity/failure-mode lens on the spec staging — BECAUSE every candidate I generated independently resolved to a recorded dead end, a pre-existing condition, or an accepted-and-stated residue, and the standing context's own capacity entry already states the delta adds no control-plane or data-plane write — ALTERNATIVES: I built and dropped six candidates, listed below, rather than file a speculative one.

USEFUL [Standing context / Settled, "The amendment adds no control-plane or data-plane write of any kind ... The capacity half of a performance lens is structurally inert on the delta"]: this is the single highest-value entry for this lens. It let me stop re-deriving etcd/Postgres/Redis write amplification after one confirmation pass. Keep it verbatim, and keep it worded for the TOKEN design (its text still says "the epoch is an in-memory `int64` per registry entry"; the token is an in-memory string per entry, so the conclusion is unchanged but the noun is stale).

USEFUL [Standing context / Traps, "Dead end: `releases the slot reservation afterwards` contradicts the leaked hold"]: I reconstructed this candidate from scratch (staged §7.1 says the gateway "releases the slot reservation afterwards" while the same paragraph says an incomplete reclaim "holds the slot's occupancy") and the Trap plus `podclaim/slotclaimer.go`'s early return on `leaked=true` killed it in one read. Four lenses have now hit it.

FACT: the common-case retry-versus-hold worry is closed by the staged ORDERING, and a future lens should reach for this before re-deriving it. Staged §7.1 puts the compensating `Shutdown` before the slot-reservation release, and §5.2's retry is placed after that release, so a policy-placed retry meets neither a surviving entry nor a live hold unless the reclaim RPC itself timed out. The "retry spends its last attempt on the hold" edge case is therefore the lagging/failed-reclaim tail, not the ordinary path. — EVIDENCE: spec-changes.md:466 ("sends `Shutdown` ... and releases the slot reservation afterwards"); spec-changes.md:210-224 (the hold edge case).

FACT: the gateway-crash residue is NOT a regression against the SHIPPED design, only against the withdrawn entry-scoped fence. Shipped leaves a stale entry a later attempt adopts (the defect); the token design leaves a stranded entry no attempt can use. The staged text's own phrase "strictly worse than what an entry-scoped fence left" (spec-changes.md:297-306) compares against a prior draft, not against the tree, so a lens reading it as a shipped-behaviour regression will file a finding that fails the bar. — EVIDENCE: spec-changes.md:297-306; problem statement's defect description.

FACT: the staged §5.2 whole-pod-scrub claim resolves. "removes every per-slot workspace tree and every per-slot credential file the pod still holds and verifies their absence" is exactly scrub step 2 (`rm -rf /workspace/slots/*`), step 0 (credential purge) and step 6 (stat-check of `/workspace/slots/`, `/tmp`, `/dev/shm`, `/run/lenny/slots/`). — EVIDENCE: spec/05_runtime-registry-and-pool-model.md:461,:468,:471.

FACT: the two new §16.1 rows are cardinality-clean. `k8s_pod_name` is an approved global label whose stated domain is "slot failure and replacement metrics" (spec/16:297), and the shipped `lenny_slot_failure_total` / `lenny_slot_pod_replacement_total` rows already carry `pool` + `k8s_pod_name` (spec/16:14-15). Neither new counter carries a label on the §16.1.1 forbidden list (spec/16:312). Do not re-derive this. — EVIDENCE: spec/16_observability.md:14,:15,:297,:312.

FACT: `lenny_adapter_leaked_slots` has NO §16.1 catalog row despite being named normatively in spec/05:545 and spec/06:160. So SPEC-6's choice to give the new adapter-side counter a §16.1 row is a departure from that precedent rather than a requirement it satisfies. It is not a defect either way; a lens tempted to file "the adapter row breaks the catalog's adapter-series convention" should note the convention does not exist. — EVIDENCE: `grep -rn lenny_adapter_leaked_slots spec/` returns only spec/05:545 and spec/06:160.

FACT: `materializeSlot` is shared by BindSlot AND BindReservedSlot, so the shipped `recordSlotFailure` calls at slotbinder.go:287,:294,:302,:309,:322 already cover the create-time-reserved path for the five post-reconnect stages. Only the connect-stage branch (slotbinder.go:250) records nothing. The staged §5.2 commentary sentence "the gateway's slot-failure accounting covers the retry-placed bind path today, and the code lane extends the same helper to the create-time reserved path and to the §7.3 re-attach" is therefore imprecise about what is already covered. I did NOT file it: it is commentary rather than staged spec text, it changes no applied edit, and the remedy is a CODE-lane scope statement. Somebody on the non-spec lane should check whether CODE-5's `accountSlotFailure` caller list double-counts the five stages `materializeSlot` already records. — EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:265-325; spec-changes.md (§5.2 SPEC-3 commentary, "extends the same helper to the create-time reserved path").

WATCHOUT: §5.2's **Fresh workspace guarantee** bullet says a retried slot is materialized from scratch "even if the failed slot's cleanup has not yet completed" — the exact case the staged reclaim hold now REFUSES. A lens will read this as an unstaged edit site. The Trap "Dead end: `SlotID == SessionID` falsifies §5.2's `new slot` and the Fresh workspace guarantee" already records three lenses building and dropping the family, on the grounds that the ordering keeps the hazard off every policy-placed retry and that the shipped tree is worse. — EVIDENCE: spec/05_runtime-registry-and-pool-model.md:556; review-log.md Traps ("Dead end: `SlotID == SessionID` ...").

WATCHOUT: the "ten seconds" figure the staged §5.2 hold paragraph attributes to the §10.1 hold-timeout termination is not in spec/10 at all (spec/10:58 states only `coordinatorHoldTimeoutSeconds`, default 120s). It comes from `pkg/adapter/holdstate.go`'s close context. Two earlier shards already recorded this and concluded the staged sentence cites §10.1 for the IDENTITY of the termination rather than for the figure, so it is not a false citation. — EVIDENCE: review-log.md:2920,:3100; spec/10_gateway-internals.md:58.

MISTAKE (mine, avoided at cost): I spent a pass checking whether the §15.1 start transition's post-bind Postgres persist failure leaves pod-side residue outside the §7.1 obligation ("the obligation ... ends when that attempt succeeds"). It does not: `Server.createAndStart`'s `store.Create` failure calls `s.rollbackBinding`, which drives the unconditional teardown. Do not re-derive it. — EVIDENCE: pkg/gateway/sessionserver/start.go:865-877.

### [spec-recheck-2.5.fix-G1.1]
DECISION: Split the staged §7.1 clean-exit sentence on the `running` boundary rather than deleting it or changing the mechanism — BECAUSE staged §5.2 already partitions the two reporting channels there (`spec-changes.md:650`: a pre-`running` cleanup "reports no outcome" and a failed one "is accounted on that request's response, which does not report a clean exit") and the staged code agrees (`non-spec-changes.md:445-446` gates the report on `live`; `:479` keys `ExitedCleanly` on `closeErr == nil && (live || treeErr == nil)`). §7.1 was the only unconditional restatement. — ALTERNATIVES: (a) delete both sentences and point at §5.2, rejected because §7.1's own next sentence turns on what the clean-exit flag means, so the operator-facing inference would be lost; (b) file the cleanup-outcome report on both arms or key `exited_cleanly` on `closeErr` alone, rejected because it breaks the one-report-per-session-release invariant and the served-session count semantics and reopens the already-refuted "reported clean on both channels" item; (c) a trailing exception clause, rejected as a rule stated then withdrawn.
FACT: §7.1's "Pod-side reclaim on a failed bind." paragraph is a single physical line inside a fenced block; sed-style line edits are useless on it and a whole-sentence string replace is the only safe form — EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.spec-changes.md:465
FACT: the split is already stated correctly at three other sites, so no cascade was owed: staged §5.2 (`spec-changes.md:650`), SPEC-5's reclaim-outcome rule (`spec-changes.md:879`), the CODE-1 commentary explicitly scoped to the `running` arm (`non-spec-changes.md:466-474`), and the summary's CODE-1 index entry (`summary.md:943`, "reports the slot-release failure for a reclaim the shared runtime process was never given").
WATCHOUT: `non-spec-changes.md:466-474` contains the sentence "the staged §7.1 paragraph states that split", which reads as a pointer to the now-qualified §7.1 text. It remains true because its own paragraph is scoped to the `running` arm. A later round tempted to "fix" it should read the paragraph above it first — EVIDENCE: proposals/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry.non-spec-changes.md:466-474

### [spec-recheck-2.5.fix-design-G1.1]

DECISION: fix the §7.1 clean-exit/cleanup-outcome sentence by splitting it on the `running` boundary (three sentences: the flag answers for the reclaim and what it carries depends on whether the shared runtime process had been given the session; the `live` arm keeps the existing clean-exit + `leaked`-report statement; the pre-`running` arm states that §5.2 withholds the report and the failed removal is carried on the reclaim's own non-clean exit) — BECAUSE every other site in the proposal already partitions on that same boundary (staged §5.2 at spec-changes.md:650, SPEC-5's reclaim-outcome rule at spec-changes.md:879, the CODE-1 commentary at non-spec-changes.md:466-474, DOCS-2's `Shutdown` row at non-spec-changes.md:2348, and the CODE-1 test case at non-spec-changes.md:2549-2554 which asserts BOTH arms explicitly); §7.1 is the only unconditional restatement — ALTERNATIVES: (a) delete both sentences and point at §5.2 — rejected because §7.1's own leak predicate in the very next sentence turns on the clean-exit flag, so an operator reading §7.1 alone needs to know a failed pre-`running` removal surfaces there; (b) make §7.1 true by changing the code/§5.2 (report on both arms, or key `exited_cleanly` on `closeErr` alone) — rejected, it breaks the at-most-one-report-per-release invariant and the served-session count, and re-opens the refuted finding the `errors.Join` re-key closed.

FACT: the two arms are keyed on different predicates in the staged code and this is deliberate: report gate is `if live` (runtimeLive), response gate is `ExitedCleanly: closeErr == nil && (live || treeErr == nil)`. EVIDENCE: non-spec-changes.md:445-446 and :479.

FACT: no cascade. `non-spec-changes.md:470` ("the staged §7.1 paragraph states that split") stays true after the fix and needs no edit; it already scopes itself to the `running` arm in the preceding clause. No other proposal file restates the §7.1 sentence unconditionally (grepped every .md for "reports a clean exit", "clean-exit flag", "cleanup-outcome"). EVIDENCE: non-spec-changes.md:466-474.

WATCHOUT: the §7.1 paragraph is a single fenced verbatim block one line long (spec-changes.md:465). Edit inside the fence only; the prose at :469-473 is commentary about the paragraph's *incompleteness predicate* and is a different rule — do not touch it.

MISTAKE: the sentence was added this round (absent from the r3-prefix snapshot) as the fix for the refuted "reported clean on both channels" item, and was written unconditionally in a paragraph whose scope (from an attempt's first pod-side RPC until it succeeds) is dominated by the pre-`running` arm. Cost: an operator-facing inversion pointing at a `ReportSessionScrub` outcome the pre-`running` path never files.

### [spec-recheck-2.5.review-applicability.1]

FACT: The spec staging did not change between snapshots `spec-recheck-2-r4` and the current tree — `diff -qr` over the whole proposal directory against `spec-recheck-2-r5` is empty, and against `spec-recheck-2-r4` only `review-log.md` differs. The "delta first" reading order has no delta in this lane; the only usable delta is `spec-recheck-r6` → current. EVIDENCE: scratchpad/cp-snap/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/spec-recheck-2-r5

FACT: Full anchor sweep of the staged spec edits against the tree — every "reads, verbatim" block resolves to exactly one site and matches character-for-character. Sites verified this round: spec/04_system-components.md:157 (§4.1 third sentence), :686 (§4.7 `Shutdown` row opening), :415-416 (§4.6.1 claim-deletion bullets incl. the false closing sentence), :688/:695 (the §4.7.1 insertion window between the Adapter→Gateway table and the 4.7.2 heading), :854 (§4.7.9 step 5); spec/05_runtime-registry-and-pool-model.md:453 (Scrub model), :545 (Slot cleanup bullet); spec/06_warm-pod-model.md:80 (three projection prose clauses, each `grep -c` = 1), :95-98 (`claimed ──→ draining` inside the Occupancy projection group, distinct from the four other `claimed ──→ draining` entries at :114/:135/:137/:139), :150-156 (per-slot fence, insertion after `receiving_uploads ──→ running` at :152-153, before `**\`reserved\` hold semantics.**` at :158), :234 (resuming cancel bullet), :290 (Client visibility clause); spec/07_session-lifecycle.md:210 (§7.2 preamble premise), :213 (step 2), :214 (step 3), :23-24 (atomicity paragraph inside the fenced flow, continuation line `(executionMode, isolationProfile, scrubPolicy summary)` at :24), :414 (§7.3 list tail); spec/15_external-api-surface.md:1136 (all three SETUP_COMMAND_FAILED sentences), :1469/:1471 (§15.4 insertion window); spec/16_observability.md:14 (row format beside `lenny_slot_failure_total`); spec/29_communication-scenarios.md:711 (step 13 tail).

FACT: Every markdown anchor the staged text emits resolves to a live heading: `#471-role-and-gateway-rpc-contract` (spec/04:659), `#479-startup-sequence-for-type-agent-runtimes` (spec/04:848, already used at spec/README.md:36), `#47-runtime-adapter` (spec/04:657), `#49-credential-leasing-service` (spec/04:1099), `#461-...` (spec/04:338), `#52-...` (spec/05:365), `#62-pod-state-machine` (spec/06:78), `#71-normal-flow` (spec/07:3), `#73-retry-and-resume` (spec/07:378), `#74-upload-safety` (spec/07:438), `#101-horizontal-scaling` (spec/10:3), `#151-rest-api` (spec/15:614), `#1542-rpc-lifecycle-state-machine` (spec/15:1686), `#1543-runtime-integration-levels` (spec/15:1707), `#161-metrics` (spec/16:3).

FACT: The §4.7.1 carries-what table's `FinalizeWorkspace` / `mid_session: carried` cell is correct and is NOT a mismatch with SCHEMA-1 adding `mid_session` to `PrepareWorkspaceRequest` alone: `FinalizeWorkspaceRequest.mid_session` is already field 4 in the shipped proto. EVIDENCE: schemas/lenny-adapter.proto:713-724. A future lens will be tempted to file this; it is a false positive.

FACT: §15.4's rule enumeration and the staged §4.7.1 block agree exactly on all sixteen named rules (pairing, reclaim hold, mid-session-create, create-and-stamp, attempt identity, started-session, admit, stamp-once, start-confirmation, first-frame, teardown-pairing, no-entry, unconditional-teardown, attempt-mismatch, attempt-match, reclaim-outcome). No rule is named by one and defined by neither.

FACT: The staged §5.2 hold's "graceful window of ten seconds" for the §10.1 hold-timeout termination matches the tree. EVIDENCE: pkg/adapter/holdstate.go:201 `context.WithTimeout(context.Background(), 10*time.Second)`.

FACT: The new "entry stands until … an unconditional teardown, an SDK demotion, or the §10.1 hold-timeout termination" clause is consistent with the cascade order: an untokened started entry is reached by the started-session rule rather than the attempt identity rule, because the attempt identity rule's condition requires a NON-EMPTY token on the resolved entry. EVIDENCE: spec-changes.md:838 (attempt identity rule) and :840 (started-session rule).

FACT: "below" in the §5.2 append is correct on every use — the Scrub model paragraph is spec/05:453 and every target it points at is later in the file: the `**Slot cleanup:**` bullet (:545), the whole-pod replacement trigger (:561), the occupancy-zero whole-pod scrub / step 0 (:455, :461), `onScrubFailure` behaviors (:481). The `**Slot failure and cleanup (maxConcurrentSessions > 1)**` heading that scopes the bullet is at :544, which is what the commentary's concurrency-scoping argument rests on.

DECISION: Returned an empty findings list — BECAUSE the staging applies end to end with no forward reference, no unresolvable or ambiguous anchor, no created artifact whose properties another edit needs and no sub-step states, and no relocation whose destination text is missing; and this loop's brief bars the checklist/deliverable-index drift that is the lens's only other live subject. ALTERNATIVES: I considered filing the §7.1 clause "Whether the per-slot cleanup that reclaim performed released or leaked the slot is the separate statement the adapter's cleanup-outcome report carries under §5.2" (spec-changes.md:463) as a contradiction with the staged §5.2 rule that a pre-`running` cleanup "reports no outcome" and is instead accounted on the reclaim response's clean-exit flag (spec-changes.md:649). I dropped it: it is a correctness/precision question rather than an applicability one, the clause's own subordinate example ("a reclaim that closes the runtime cleanly") scopes it to the post-`running` half, the surrounding two sentences keep the pre-`running` accounting intact, and the material skeptic has already refuted two neighbouring findings on exactly this partition.

OPEN: The §7.1 clause above is still the weakest sentence in the staging. If a later mechanism or contradiction lens wants it, the precise defect is that the clause is unqualified while §5.2 withholds the cleanup-outcome report on the very path §7.1's obligation mostly covers; the fix is one qualifier ("for a reclaim the shared runtime process was given the session"). Someone with the contradiction lens should decide, not applicability.

### [spec-recheck-2.5.review-citations.1]

DECISION: returned zero findings — BECAUSE every concrete citation in the staged spec edits resolves and says what the proposal claims — ALTERNATIVES: rejected filing the `coordination_generation` claim-register gloss (spec-changes.md:931-932 says the ABSENT row follows "the rows the `coordination_generation` fence already carries", and those 13 rows carry status `UNWIRED` not `ABSENT`) because the sentence cites the rows as precedent for *having* a row rather than for the status value; refuted on the plain reading.

FACT: the full file:line citation set of spec-changes.md is now verified exhaustively this round. Each of these resolves and supports the claim made on it: binder.go:867 (`b.failPhase` inside the pre-attached `reclaim` closure), :998 (the Launch `reclaim` closure), :1072-1082 (`failPhase` body), :1200-1202 (`drain` → `podclaim.DeleteClaim`); occupancy.go:57-59 (the verbatim "the §6.2 state machine encodes the recycle-versus-one-session distinction in the phase the pod sits in at the claim DELETE" comment), :128-140 (the `switch o.Current` with Reserved→Idle / Claimed→Draining), :132-140 and :134-140 (the Claimed arm; the function is `ProjectOccupancyPhase`); schemas/lenny-adapter.proto:1621-1627 (the `recycle` field comment, verbatim "beside the named session's teardown rather than selecting a scope"); spec/04:151 (the field-set derivation sentence) and :157 (the §4.1 third sentence); spec/29:586-588, :669-674, :697. EVIDENCE: proposals/0081_.../0081_....spec-changes.md:235-237,368,377,382,435,702,706,755

FACT: every verbatim "reads, verbatim" anchor in the staging resolves to exactly one site in its named section: spec/04:157, :686 (`Shutdown` row opening), :854 (§4.7.9 step 5), :415 and :416 (§4.6.1 claim-deletion bullets); spec/05:545 (`**Slot cleanup:**` action list), :453 (`**Scrub model.**`); spec/06:80 (three projection prose clauses), :95-97 (the Occupancy-projection `claimed ──→ draining` trigger list — note :114, :135, :137 and :139 carry the same edge in OTHER blocks, and the instruction's "In the fenced `Occupancy projection` block" scoping is what disambiguates), :234 (mid-resume cancel clause), :290 (Client-visibility clause); spec/07:23 (atomicity paragraph tail), :210 (§7.2 preamble premise), :213 (step-2 fragment), :214 (step 3), :414 (§7.3 list item 4); spec/15:1136 (all three `SETUP_COMMAND_FAILED` sentences); spec/29:711.

FACT: the §29.4 step-13 anchor scoping still holds after this round's edits. "([§15.4.3](...), §28.5.3)." occurs at spec/29:645, :711, :887 and :982, and §29.4 spans :575-762, so :711 is the unique in-section site and step 13 is the only "13." item in §29.4. USEFUL [Traps #43]: that entry is what sent me to check rather than assume.

FACT: the two new non-`Shutdown` entry-removing paths the round-6 edit names at spec-changes.md:277-279 both check out. `DemoteSDK` removes the registry entry with no bind attempt named (pkg/adapter/sdkwarm.go:294-298, `s.anyRegisteredSession()` then `releaseSessionSlot`), and §10.1's hold-timeout termination deregisters every `started` entry (pkg/adapter/slotsession.go:375-395, `deregisterStartedSessions`; spec/10_gateway-internals.md:58). The residue entry the bullet is about is `started`, so the hold-timeout path does reach it.

FACT: the new commentary at spec-changes.md:660-669 is accurate against the shipped bullet. spec/05:545 does list "releases the `slotId`" among the cleanup's actions, does say "If cleanup fails, the slot is leaked — the pod continues but the slot is not reclaimed until pod termination", and does sit under the heading "**Slot failure and cleanup (`maxConcurrentSessions > 1`).**", which is what makes the staging's "The bullet states that disposition for a pod serving concurrent sessions" true.

FACT: the §16.1 catalog is a two-column table (`| description | Counter |`); SPEC-6's staged rows match its form, and the "beside the session-slot failure count" placement resolves to spec/16_observability.md:14.

WATCHOUT: `ShutdownResponse` in the shipped tree carries only `exited_cleanly = 1` and `exit_code = 2` (schemas/lenny-adapter.proto:1665-1668). spec-changes.md:920-924 calls the added outcome field "the outcome to `Shutdown`" without naming it; the field name and number live in SCHEMA-1 in the non-spec staging. A reviewer checking the spec file alone will not find the outcome field's name anywhere in it, which is correct (§15.4 blocks deliberately state no field number) rather than a gap.

### [spec-recheck-2.5.review-client-surface.1]

FACT: the spec staging did not change in round 5. `diff -q` of
proposals/.../0081_*.spec-changes.md against every cp-snap snapshot shows it is byte-identical
to spec-recheck-2-r4, -r4-start, -r5 and -r5-start; it last changed at spec-recheck-2-r3.
So the "delta to look at first" for this round is empty and the whole staging is the scope.
EVIDENCE: scratchpad/cp-snap/0081_.../spec-recheck-2-r4/0081_*.spec-changes.md

FACT: `mid_session` exists TODAY only on `FinalizeWorkspaceRequest` (field 4), not on
`PrepareWorkspaceRequest` (fields 1-3, field 4 reserved for the removed `slot_id`). SCHEMA-1's
"`mid_session` on `PrepareWorkspaceRequest` field 6" is therefore a genuinely new field, and the
§4.7.1 carries-what table's FinalizeWorkspace row describes a shipped field. The string
`mid_session` appears nowhere in spec/ or docs/ today, so the staged §4.7.1 table is its first
spec statement and there is no stale parallel to update.
EVIDENCE: schemas/lenny-adapter.proto:682-695 (PrepareWorkspaceRequest), :713-724 (Finalize)

FACT: `ShutdownResponse` today carries only `exited_cleanly` (1) and `exit_code` (2).
`slot_reclaim` is NEW at field 3 under SCHEMA-1, and no `slot_was_started` field is staged —
checklist S10 says explicitly "`ShutdownResponse` carries no started signal". Any orchestrator
note describing `slot_was_started` at field 4 is stale.
EVIDENCE: schemas/lenny-adapter.proto:1665-1668; implementation-checklist.md:35

FACT: no client SDK carries any adapter-proto type. `grep -rln "ShutdownRequest|bind_attempt|
SlotReclaim" sdks/` returns nothing, and `pkg/gateway/openapi/openapi.json` enumerates no REST
error codes. So the two new adapter `ErrorCode` values and every new wire field have exactly two
parallel representations to keep in step: `schemas/lenny-adapter.proto` and
`docs/reference/adapter-contract.md`. Both are staged (SCHEMA-1, DOCS-2).
EVIDENCE: sdks/ (no matches); pkg/gateway/openapi/openapi.json (no SETUP_COMMAND_FAILED)

FACT: `docs/runtime-author-guide/adapter-contract.md` does NOT exist; the only adapter-contract
page is `docs/reference/adapter-contract.md`. TEST-GAPS.md:11502 cites the runtime-author-guide
path and is stale. Do not go looking for a second mirror of §15.4.
EVIDENCE: docs/runtime-author-guide/ listing; TEST-GAPS.md:11502

FACT: `docs/reference/adapter-contract.md` mirrors §4.7's RPC tables only. It carries no mirror
of §15.4's `**SDK-warm demotion contract:**` prose (grep for "demot" hits only the `DemoteSDK`
table row at :64 and the artifact list at :682). So the two NEW §15.4 published-contract blocks
(bind attempt token, reclaim hold) inherit no docs-mirror obligation under the staged trailer's
"each reference page that mirrors these sections moves with the section it mirrors" rule. I
checked this specifically as a candidate unstaged-site finding and it does not hold.
EVIDENCE: docs/reference/adapter-contract.md:51-101, :64, :682

FACT: every verbatim anchor SPEC-1/SPEC-5 quote resolves exactly, and every markdown anchor the
staged text mints resolves to a real heading. Checked: spec/04_system-components.md:157 (§4.1
third sentence), :686 (§4.7 Shutdown row opening), :673 (ConfigureWorkspace "Idempotent", which
the started-session rule's exemption cites), :659 (#471-role-and-gateway-rpc-contract), :848
(#479-startup-sequence-for-type-agent-runtimes, form confirmed at spec/README.md:36);
spec/15_external-api-surface.md:1136 (all three SETUP_COMMAND_FAILED sentences), :1458/:1462
(§15.4 names schemas/lenny-adapter.proto), :1471 (the 15.4.1 insertion boundary), :1686
(#1542-rpc-lifecycle-state-machine), :614 (#151-rest-api);
spec/06_warm-pod-model.md:290 (the client-visibility clause);
spec/16_observability.md:14 (the §16.1 two-column row format the staged rows copy).

FACT: `Binder.Resume` issues `Resume` and no `RunSetup` (pkg/gateway/podlifecycle/podsession/
binder.go:1589-1618), so the started-session rule cannot refuse an attempt's own `RunSetup`
because that same attempt's `Resume` already set `started`. I chased this as a possible
self-blocking bind sequence (claimSessionSlotUnderLock sets `st.started = true` at the claim,
pkg/adapter/slotsession.go:85-86, and `Resume` goes through the claim) and it does not occur.
EVIDENCE: pkg/gateway/podlifecycle/podsession/binder.go:1607; pkg/adapter/slotsession.go:74-88

FACT: the seven entry-creating RPCs the staged §4.7.1 Admission block and the staged §5.2 reclaim
hold both enumerate match the tree exactly: staging.go:134/:181/:337 (PrepareWorkspace,
FinalizeWorkspace, RunSetup via ensureSlotPaths), slotcreds.go:26 (AssignCredentials),
slotsession.go:75 (claimSessionSlot, reached by StartSession, ConfigureWorkspace and Resume).
EVIDENCE: pkg/adapter/staging.go:134,181,337; pkg/adapter/slotcreds.go:26; pkg/adapter/slotsession.go:75

DECISION: returned an empty findings list — BECAUSE every client-facing representation the
staging touches is mirrored: the two new adapter `ErrorCode` values follow the shipped
`PROTOCOL_VERSION_INCOMPATIBLE` precedent of an adapter code with no §15.1 row
(schemas/lenny-adapter.proto:585 with no match in §15.1), the §15.1 `SETUP_COMMAND_FAILED`
widening is mirrored by DOCS-3 into docs/reference/error-catalog.md:129 including its Remedy
cell (which §15.1's four-column table has no counterpart for, so no §15.1 remedy edit is owed),
the §16.1 rows are mirrored by docs/reference/metrics.md, and the §6.2/§4.6.1 projection and
per-slot-fence edits are mirrored by DOCS-1 into docs/reference/state-machines.md —
ALTERNATIVES: I considered and dropped (a) the §15.4 blocks having no docs mirror (no mirrored
source, see the adapter-contract.md FACT above); (b) `docs/reference/execution-modes.md:68`'s
"at each session release, on a pod of any concurrency" — the same partition argument that
refuted the adapter-contract.md `ReportSessionScrub` finding defuses it, because the
pre-`running` reclaim is not a session release; (c) the §6.2 client-visibility bullet being left
with a gap (its clause 1 names only the setup-command exit while SPEC-5's clause 2 now excludes
every deterministic FAILED_PRECONDITION at the setup-command request, so the bullet is silent
on the refusal case) — this is a close variant of the already-refuted "§15.1 exclusion clause no
longer partitions the setup window" finding, where the verifier held that restricting a subject
is not an affirmative denial and silence is not contradiction.

WATCHOUT: the §5.2 reclaim hold as staged refuses every request that would "create or resolve a
registry entry", and the staged §4.7.1 sentence extends that to "every other RPC on this
contract". Read literally that reaches `Attach`, `SendMessage`, `Interrupt` and `Checkpoint`
during an ordinary session end's cleanup window, answering them `ABORTED` = transient = retry.
I judged this below the bar (the retry then gets the correct permanent answer once the entry is
gone) and did not file it, but a later lens on gateway-internal or client-retry behaviour may
want to look at it deliberately rather than rediscover it.
EVIDENCE: spec-changes.md §5.2 reclaim-hold paragraph; §4.7.1 Admission block "Every other RPC
on this contract resolves an entry the session already holds ... apart from the reclaim hold"

### [spec-recheck-2.5.review-docs-alignment.1]

DECISION: returned an empty findings list for the docs-alignment lens on the spec-lane recheck — BECAUSE every spec section the staging edits either has its reader-facing mirror staged (state-machines.md via DOCS-1, adapter-contract.md via DOCS-2, error-catalog.md via DOCS-3, metrics.md via CODE-9) or has no mirror in docs/ at all, and the two candidate gaps I found (execution-modes.md:68, the unbacked DemoteSDK registry-entry clause) each fail a confirm test — ALTERNATIVES: filing execution-modes.md:68 as an unstaged §5.2 scrub-model mirror, rejected because the r-earlier verifier pre-empted it by name when refuting the adapter-contract.md:81 variant; filing the DemoteSDK clause, rejected because no staged spec sentence becomes false.

FACT: the delta this round is *not* r4→r5. spec-changes.md has been byte-identical since before the spec-recheck-2-r4 snapshot (mtime 19:50, r4 snapshot 19:53); the last spec-changes edit is the r3-prefix→now hunk set. EVIDENCE: `diff -q scratchpad/cp-snap/.../spec-recheck-2-r4/*.spec-changes.md proposals/.../*.spec-changes.md` returns clean; `spec-recheck-2-r3-prefix` is the newest snapshot that differs.

FACT: that last spec-changes edit is the reclaim-hold permanence rewrite — "until the cleanup that reclaims it has completed, **and for the life of the pod where that cleanup does not complete**" — applied at five sites (Design, §7.1 paragraph, §5.2 reclaim-hold paragraph, §6.2 prose, and the edge-case retry bullet). EVIDENCE: proposals/0081.../0081....spec-changes.md:62, :158, :606, :660, :195-201.

FACT: docs/ has exactly four pages that mirror anything this proposal edits. `grep -rln "leaked\|slot_cleanup\|ReportSessionScrub" docs/` returns only docs/reference/{adapter-contract,state-machines,glossary}.md plus unrelated hits; docs/api/internal.md documents no adapter RPC (grep for Shutdown/PrepareWorkspace/slot returns nothing); no runbook and no operator-guide page enumerates leaked-slot or slot-bind causes, so the lens's "new cause of an existing operator-facing failure" category has no landing site to be missing from. EVIDENCE: docs/reference/adapter-contract.md:75,:81; docs/reference/state-machines.md:138,:238,:251; docs/reference/glossary.md:371.

WATCHOUT: docs/reference/execution-modes.md:68 carries the same "per-slot cleanup runs at each session release ... and the adapter reports its outcome" phrasing that the refuted adapter-contract.md:81 finding was filed on, and the verifier that refuted that one explicitly named :68 as the site a survivor would live at while predicting the same partition argument defuses it. Anyone tempted to file it should read that refutation first. EVIDENCE: docs/reference/execution-modes.md:68.

UNVERIFIED: DOCS-2's staged `DemoteSDK` row asserts the demotion "drop[s] the adapter's slot registry entry for the session", which no spec text states normatively — spec/04_system-components.md:674 says only "Tear down the pre-connected SDK process and return the pod to pod-warm state", and this round's §4.7.1 narrowing deliberately stopped enumerating which non-`Shutdown` paths remove an untokened entry ("within this cascade only the unconditional-teardown rule does", spec-changes.md:857). The staged §5.2 hold paragraph references the demotion's release only obliquely ("the pod-warm bind sequence that follows an SDK demotion does not meet it", spec-changes.md:660). I judged this below the bar (no staged sentence becomes false), but the non-spec lane should confirm the shipped demotion path actually deregisters, since DOCS-2 publishes it as contract.

USEFUL [orchestrator brief]: the instruction to treat the standing context's epoch-era Open entries as retired saved a full pass; `grep -ci epoch` on the staging is still 0.

### [spec-recheck-2.5.review-edit-sites.1]

DECISION: returned an EMPTY findings list for the edit-site-completeness lens on the spec staging — BECAUSE there is NO delta to review this round and every identifier sweep the lens prescribes came back clean. ALTERNATIVES rejected, in the order I hit them: (a) spec/04:674's `DemoteSDK` row as an unstaged spec site whose docs mirror DOCS-2 edits (killed by the standing Traps entry "Do NOT file `adapter-contract.md:64`" — the demotion's registry effect leaves a one-line RPC summary INCOMPLETE rather than false, and the same reasoning applies to the spec row it mirrors); (b) spec/06:402's "Combined with the one-session-only invariant" after SPEC-4 deletes the §4.6.1 sentence that glosses that term (a stranded term, not a false statement, and §6.4:395 already uses the loose "one-session-only pod lifecycle" form); (c) spec/05:545's surviving "The adapter reports each slot cleanup outcome" (the recorded dead end at review-log.md:884); (d) docs/reference/state-machines.md:241's "or through `draining` otherwise" as a recycle-keyed claim-deletion statement SPEC-4 re-keys (it describes the clean occupancy-zero path, a different projection input); (e) the ten-second graceful window the staged §5.2 hold paragraph fixes for the §10.1.4 hold-timeout termination, which §10.1.4 itself does not state (an addition of detail on the section that owns the cleanup, not a falsification of §10.1.4).

FACT: **The spec staging has not moved for two rounds.** `diff -q` of `*.spec-changes.md` against the `spec-recheck-2-r4` and `spec-recheck-2-r5` snapshots is empty; the last edit was the r3→r4 fix round. The whole proposal directory is byte-identical to `spec-recheck-2-r5`. A lens told to "spend attention on what changed" has nothing to spend it on — run `diff -rq` across the snapshot chain before budgeting a round (this is the third window in which that advice, at review-log.md:1130, has paid). — EVIDENCE: scratchpad/cp-snap/0081_.../spec-recheck-2-r5 vs proposals/0081_...

FACT: **The `mid_session` field exists on `FinalizeWorkspaceRequest` but NOT on `PrepareWorkspaceRequest`.** `schemas/lenny-adapter.proto:724` declares `bool mid_session = 4` inside `FinalizeWorkspaceRequest` (message opens at :706); `PrepareWorkspaceRequest` (:682-696) has fields 1-3 and `reserved 4` (ex-`slot_id`). So the staged §15.4 wire sentence "adds ... the mid-session marker to `PrepareWorkspace`" (spec-changes.md:922) is CORRECT, and the §4.7.1 carries-what table's `mid_session` column is correct on both rows. A single `grep -n mid_session schemas/lenny-adapter.proto` returns one hit and reads as if Prepare already had it; check which message the hit is inside. — EVIDENCE: schemas/lenny-adapter.proto:682-696, :706-732

FACT: **There is no gateway-side metric-catalog sweep, so `lenny_slot_compensation_superseded_total` is held to neither catalog by any gate.** `tests/tier11_docs/adapter_metric_catalog_test.go` reads `pkg/adapter/metrics.go` only (`metricNamePattern` at :26, `registeredAdapterMetrics` at :54), and `tests/tier2_component/observability/catalog_crosscheck_test.go` is the ALERT catalog, not the metric catalog. `runtime_page_metric_names_resolve_test.go` only holds two runtime-facing pages to §16.1. The adapter-side counter `lenny_slot_shutdown_untokened_entry_total` IS gated and the staging satisfies it (a §16.1 row under SPEC-6 plus a `docs/reference/metrics.md` row under CODE-9, which is exactly what `TestAdapterMetricsReachTheDocumentationCatalogs` requires). This CLOSES the standing Open at review-log.md:1513. — EVIDENCE: tests/tier11_docs/adapter_metric_catalog_test.go:26,:54,:80-113

FACT: **Every staged verbatim anchor resolves in the tree.** Checked by grep, one by one: spec/04:157 (§4.1 third sentence), :686 (§4.7 `Shutdown` row opening), :854 (§4.7.9 step 5), :415 and :416 (§4.6.1's two claim-deletion bullets, including the closing sentence SPEC-4 deletes), :673 (`ConfigureWorkspace` idempotency the started-session rule cites), :700 (`#### 4.7.2` insertion boundary); spec/05:453 (`**Scrub model.**`), :545 (`**Slot cleanup:**` action sentence); spec/06:80 (projection prose, all three clauses), :95-97 (fence `claimed ──→ draining` in the *Occupancy projection* group — note there are two other `claimed ──→ draining` entries at :114 and :135 in other groups), :152 (`receiving_uploads ──→ running`, the insertion point), :234 (`resuming → cancelled` clause), :290 (`**Client visibility:**` clause); spec/07:42 (atomicity paragraph and the fenced-listing position), :210 (§7.2 preamble premise), :214 (step 3), :402-414 (§7.3 list); spec/15:1136 (all three `SETUP_COMMAND_FAILED` sentences), :1469/:1471 (§15.4 insertion boundary); spec/16:14 (the row the two new rows sit beside, two-column format); spec/29:697 (step 13).

USEFUL [Standing context Traps, "Do NOT re-derive the whole-client-surface absence sweep"]: re-ran `grep -rnE 'bind_attempt|unconditional_teardown|slot_reclaim|SLOT_BIND_|lenny_slot_compensation_superseded_total|lenny_slot_shutdown_untokened_entry_total|bind attempt' spec/ docs/ schemas/ charts/ sdks/` and it still returns zero. One command instead of a round.

USEFUL [Standing context Traps, the `adapter-contract.md:64` `DemoteSDK` entry and the `adapter-contract.md:81` "at each session release" entry]: both were the first two candidates this lens built independently. Each was killed by the recorded ground before I spent a verifier on it.

CORRECTS [orchestrator brief]: the brief still says "`ShutdownResponse` keeps `slot_reclaim` and gains `slot_was_started` (4)" and "THE NON-SPEC LANE HAS HAD ZERO ROUNDS". Both remain false, for what the ledger counts as more than ten rounds (review-log.md:1285, :2595, :2870). `slot_reclaim` is NEW at field 3 and `slot_was_started` exists nowhere outside the review log. I checked before deriving; cost one grep.

WATCHOUT: the brief also tells this lane the delta lives in `*.spec-changes.md` since the `spec-recheck-2-r5` snapshot, and names a *different* snapshot (`spec-recheck-r6`) earlier in the same brief. Neither yields a spec-staging delta: `spec-recheck-2-r5` is byte-identical to the current tree, and the `spec-recheck-r6` diff is the whole accumulated r6→now history rather than this round's. EVIDENCE: scratchpad/cp-snap/0081_.../spec-recheck-2-r5


### [spec-recheck-2.5.review-feasibility.1]

DECISION: Returned zero findings from the actor-action-feasibility lens on the r6→now spec-changes delta — BECAUSE every actor the staging names exists, holds the data its check needs, and stays inside its §4.6.3 / §10.3 / §13.2 boundary; the delta is confined to the reclaim hold's pod-life extension, the §4.7 row's "this row restates neither", the §7.1 clean-exit/leaked split, and the §4.7.1 attempt-mismatch narrowing, none of which move an action across an actor boundary — ALTERNATIVES: filing the §10.1-hold-timeout-is-a-request mislabel (below), rejected as wording in non-landing commentary.

FACT: The adapter DOES have its own pod name, so the staged §16.1 `lenny_slot_shutdown_untokened_entry_total` `k8s_pod_name` label is feasible: `Server.podID` is read once from the Downward API `POD_NAME` at `New` — EVIDENCE: pkg/adapter/server.go:36 (`const podNameEnvVar = "POD_NAME"`), :380 (`podID: os.Getenv(podNameEnvVar)`), and the fail-closed empty case at pkg/adapter/sessionscrubreporter.go:70-78. This retires the standing WATCHOUT at review-log.md:3329 as a feasibility question; what remains is only that an empty `POD_NAME` yields an empty label value, which the shipped scrub reporter already treats as fail-closed.

FACT: The adapter serves no `/metrics` HTTP handler anywhere in the tree (`grep -rn "promhttp\|/metrics" cmd/lenny-adapter/ pkg/adapter/` returns only the import line pkg/adapter/metrics.go:8), so the staged §16.1 row's "not scraped until the adapter metrics endpoint is wired" is true of the tree. The shipped §16.1 precedent rows phrase the same deferral as "outside the §16.9 default scrape target set until a deployer wires an adapter scrape target" — EVIDENCE: spec/16_observability.md:186-189.

FACT: The staged §4.7.1 Admission enumeration of the seven entry-creating RPCs is exactly the shipped create-site set. `ensureSlotStateLocked` is reached from pkg/adapter/staging.go:134,181,337 (PrepareWorkspace, FinalizeWorkspace, RunSetup), pkg/adapter/slotcreds.go:26 (AssignCredentials), and `claimSessionSlotUnderLock` at pkg/adapter/slotsession.go:74 from pkg/adapter/session.go:111 (StartSession), pkg/adapter/resume.go:50 (Resume), pkg/adapter/sdkwarm.go:217 (ConfigureWorkspace). No eighth handler creates one.

FACT: The §29.4 step-13 claim that the graceful-shutdown frame "is pod-global and names no session" is true of the wire schema, not merely of the prose: the `terminate` frame's properties are `type`, `deadlineMs`, `reason` only, `additionalProperties: false` — EVIDENCE: schemas/runtime-ops-events.schema.json:174-185. The shipped adapter already gates it on `!boundRemains` — EVIDENCE: pkg/adapter/session.go:252-260.

FACT: `DemoteSDK` really does remove a registry entry without naming a bind attempt, so the edge-case bullet's remover list is substantively right: pkg/adapter/sdkwarm.go:296-298 calls `releaseSessionSlot(s.anyRegisteredSession())`. The §10.1.4 hold termination removes only entries whose `started` flag is set — EVIDENCE: pkg/adapter/slotsession.go:383-391 (`if st.started`) — which is sufficient for the untokened-residue case the bullet describes, because that entry was created by a late `StartSession`.

FACT: `ProjectOccupancyPhase` reads only `o.HasClaim`, `o.Binding`, `o.RewarmStarted` and `o.Current`, so SPEC-4's re-key onto "the phase the pod projects at the claim DELETE" is data the WarmPoolController already has. The proposal's citations resolve exactly: occupancy.go:57-59 is the recycle-policy comment and :128-140 is the no-claim switch (`case state.Reserved: return state.Idle` at :129-133, `case state.Claimed: return state.Draining` at :134-140).

OPEN (below the bar, recorded so nobody re-derives it): spec-changes.md:277-279 calls the §10.1 hold-timeout termination "a request that removes an entry without naming a bind attempt", while the staged §5.2 hold paragraph at spec-changes.md:652 says that same termination "runs under no request", and the tree agrees (pkg/adapter/slotsession.go:374-391 is an internal pass, not an RPC handler). The remover set is correct; only the genus noun is wrong. It sits in the Edge-cases commentary, which lands in no spec file, so I did not file it. A fixer touching that bullet for another reason should say "an unconditional teardown, an SDK demotion, or the §10.1 hold-timeout termination, which runs under no request" or similar.

USEFUL [orchestrator note on dead standing context]: the Open entries about the epoch, the epoch latch and `bind_epoch` response fields are indeed absent from the staging; treating them as retired saved a full re-derivation pass.

WATCHOUT: the snapshot the prompt points at for the "what changed" diff, scratchpad/cp-snap/.../spec-recheck-2-r5, is byte-identical to the live proposal, so `diff -ru` against it prints nothing and reads as "nothing changed". The delta this lane exists for is against scratchpad/cp-snap/.../spec-recheck-r6 (165 diff lines on spec-changes.md). Use that one.

### [spec-recheck-2.5.review-fresh.1]

FACT: The spec staging did not change in rounds 4 or 5. `diff -q` of `*.spec-changes.md` against the snapshots `spec-recheck-2-r4`, `spec-recheck-2-r4-start`, `spec-recheck-2-r5` and `spec-recheck-2-r5-start` all return identical; the last spec-staging edit landed between `spec-recheck-2-r3` and `spec-recheck-2-r4`. A "recheck the delta" instruction therefore has no delta to read this round, and the lens must re-read the whole staging. EVIDENCE: /home/ec2-user/lenny/scratchpad/cp-snap/0081_fix_a-failed-session-bind-leaves-an-adapter-slot-registry/spec-recheck-2-r4/

FACT: Every "reads, verbatim" anchor in the staging still resolves against the tree. Checked mechanically with `grep -nF` this round: spec/04_system-components.md:157 (§4.1 third sentence), :686 (§4.7 `Shutdown` row opening), :854 (§4.7.9 step 5), :415 and :416 (§4.6.1's two claim-deletion bullets), :673 (ConfigureWorkspace published idempotent, which the started-session rule's exemption cites); spec/05:453 (`**Scrub model.**`), :545 (`**Slot cleanup:**` first sentence); spec/06:80 (the three projection prose clauses), :95 (`claimed ──→ draining`), :152 (`receiving_uploads ──→ running`), :234 (the `resuming → cancelled` clause), :290 (`**Client visibility:**` clause); spec/07:23 (atomicity paragraph), :24 (the continuation line the new paragraph goes above), :210, :213, :214, :414; spec/15:1136 (all three `SETUP_COMMAND_FAILED` sentences), :1469 (`**SDK-warm demotion contract:**`); spec/29:704-711 (step 13). No anchor drift.

FACT: Every in-repo citation the staging makes outside the anchors resolves. `pkg/controller/warmpool/occupancy.go:57-59`, `:128-140`, `:134-140` all say what SPEC-4 claims; `pkg/gateway/podlifecycle/podsession/binder.go:867`, `:998`, `:1072-1082`, `:1200-1202` are the `reclaim` closure, the second `reclaim` closure, `failPhase` and `drain`/`DeleteClaim` respectively; `schemas/lenny-adapter.proto:1621-1627` carries the quoted `recycle` comment verbatim. SPEC-3's "shipped behaviour rather than new obligations" claim holds: `pkg/adapter/slotlayout/tree.go:58-68` removes `p.CredentialsDir`, and `pkg/adapter/slotsession.go:174-182` cancels every armed expiry timer under `s.mu` before deleting the entry.

FACT: §7.1's atomicity paragraph (spec/07_session-lifecycle.md:23) sits INSIDE the ``` fenced flow listing that opens at :5, so its markdown links already render literally. SPEC-2's new paragraph goes in the same fence and inherits that. Pre-existing precedent; do not file it.

WATCHOUT: `ShutdownResponse` in the tree today has only `exited_cleanly = 1` and `exit_code = 2` (schemas/lenny-adapter.proto:1665-1668). The orchestrator brief's "ShutdownResponse keeps slot_reclaim and gains slot_was_started (4)" is false twice over: `slot_reclaim` is new, and the string `slot_was_started` appears in no proposal file (`grep` over spec-changes, non-spec-changes and summary returns nothing). Do not derive a finding from that brief sentence. EVIDENCE: schemas/lenny-adapter.proto:1665-1668

FACT: No spec-wide adapter `ErrorCode` catalog exists; `PROTOCOL_VERSION_INCOMPATIBLE` appears only in spec/15_external-api-surface.md:1699 (the §15.4.2 state table). So the staging's "§15.1 takes no new row" reasoning has no second catalog it must also feed. `Shutdown` appears nowhere in spec/28_communication-channels.md, so the "§28's registers untouched" claim holds too.

FACT: The reclaim hold's refusal carries no `ErrorCode` enum value. The non-spec lane answers it as a plain `codes.Aborted` status whose message is `slot_reclaim_in_progress` (non-spec-changes.md:1395), so SCHEMA-1's two new enum values are the whole enum delta and §15.4's "refused with the gRPC status code `ABORTED`" is the whole published contract for the hold.

FACT: The staged §5.2 "graceful window of ten seconds" for the §10.1 hold-timeout termination is real in the tree but is ONE budget shared across every terminated member, not a per-cleanup window: `pkg/adapter/holdstate.go:201` builds a single `context.WithTimeout(context.Background(), 10*time.Second)` and passes it to every `terminateHeldSession` in the pass-2 loop (:203-205). The spec sentence reads as per-cleanup. I judged this below the bar for the spec lane (the shared-budget consequence is already a confirmed non-spec finding), but a later lens should not re-derive it as new.

UNVERIFIED: after SPEC-3 lands, spec/05_runtime-registry-and-pool-model.md:545's unedited sentence "The adapter reports each slot cleanup outcome (`released` or `leaked`) to the gateway via `ReportSessionScrub`" is a universal the appended scrub-model text contradicts for the pre-`running` path. I did not file it, because the loop has refuted four variants of "general statement narrowed by the specific rule" (review-log.md:4137) and the staging explains the placement choice deliberately. Someone should decide once whether the bullet's universal is in scope, rather than each round re-deriving it.

OPEN: SPEC-5 narrows §6.2:290's `**Client visibility:**` exclusion clause to "other than a deterministic `FAILED_PRECONDITION` answer to the setup-command request" but leaves that bullet's inclusion clause keyed on "a deterministic non-zero setup-command exit". After the edit the bullet assigns the started-session refusal to neither envelope. §15.1's row does cover it, and the equivalent §15.1 asymmetry was already refuted this loop on "restricting a subject is not an affirmative denial", so I left it. If a fixer touches the §6.2 clause for any other reason, widen the inclusion clause in the same edit.

### [spec-recheck-2.5.review-kubernetes.1]
FACT: The spec-changes.md staging is byte-identical to the spec-recheck-2-r5 snapshot; `diff -ru` over the whole proposal dir returns nothing. The delta this round was told to look at first is empty, so the round is a full-staging re-read rather than a delta read. — EVIDENCE: scratchpad/cp-snap/0081_.../spec-recheck-2-r5 vs proposals/0081_...
FACT: The Kubernetes-idiom surface this proposal touches is confined to SPEC-4 (the occupancy projection in spec/06 §6.2 and spec/04 §4.6.1). Every other staged edit is gateway↔adapter gRPC (§4.1, §4.7, §4.7.1, §4.7.9, §5.2, §7.1/7.2/7.3, §15.1, §15.4, §16.1, §29.4) and carries no CRD, status-subresource, finalizer, webhook, or reconcile-path obligation. A future K8s-lens pass can scope its reading to SPEC-4 plus the §7.1 claim-deletion sentence and stop. — EVIDENCE: proposals/0081_.../0081_....spec-changes.md:695-815
FACT: SPEC-4's re-keying is a spec-to-code alignment, not a design change. `ProjectOccupancyPhase` switches on `o.HasClaim` then on `o.Current` alone and never returns `Idle` from `Claimed`; its own doc comment already states the rule SPEC-4 adopts ("the §6.2 state machine encodes the recycle-versus-one-session distinction in the phase the pod sits in at the claim DELETE"). The shipped §6.2 fence entry SPEC-4 replaces is at spec/06_warm-pod-model.md:95-97 and reads as the staging quotes it. — EVIDENCE: pkg/controller/warmpool/occupancy.go:56-58, :83-145; spec/06_warm-pod-model.md:95-97
FACT: Gateway-performed SandboxClaim CREATE/DELETE, which the staged §7.1 and §6.2 paragraphs rely on ("the pre-attached failure disposition deletes the pod's claim"), is the shipped ownership: §4.6's CRD table states the claim is "Created by the gateway when it acquires an idle pod and deleted when the reserved hold expires or the pod terminates", and the admission webhook gates CREATE only and reads no phase. No staged sentence has a non-owner write another component's status. — EVIDENCE: spec/04_system-components.md:395, :405
DECISION: Returned zero findings under the Kubernetes-idiom lens — BECAUSE the fence this revision settled on is a caller-minted token carried on gRPC requests, which is the idiomatic answer: it keeps etcd out of the per-request path, keeps the CRD as coordination rather than a message bus, adds no finalizer, puts no controller reconcile or work-queue on the synchronous bind path, and leaves the occupancy projection the single writer of Sandbox.status.phase. ALTERNATIVES: I considered filing the projection's self-reference (it reads `o.Current`, the phase it itself wrote) as a level-triggering concern, and rejected it — the behaviour is shipped, the staging only aligns the prose to it, and the `ok=false` default keeps the warm-fill writer unchallenged.


### [spec-recheck-2.5.review-mechanism.1]

FACT: THE SPEC STAGING DID NOT MOVE THIS ROUND, OR LAST ROUND. `diff -q` of
`0081_....spec-changes.md` against the snapshots `spec-recheck-2-r3`, `-r4`, `-r5-start` and
`-r5` shows it last changed between r3 and r4; r4 and r5 both reviewed byte-identical text and
this round makes a third. The orchestrator brief's "THIS IS A RECHECK, AND THE DELTA IS WHERE
YOU LOOK FIRST" has no delta to point at, and the brief also names two different snapshot
directories (`spec-recheck-r6` in the standing-context block and `spec-recheck-2-r5` in the
delta block); only the latter is this lane's. Do not spend a tool call hunting the delta.
EVIDENCE: scratchpad/cp-snap/0081_.../spec-recheck-2-r4/0081_....spec-changes.md vs
proposals/0081_.../0081_....spec-changes.md (identical).

DECISION: returned an empty findings list — BECAUSE every mechanism thread I traced end to end
closes, and the three candidates I developed each land on a refutation the log or the refuted
list already records — ALTERNATIVES: (a) the §15.1 `SETUP_COMMAND_FAILED` row's unedited
"Surfaced wherever the gateway runs setup commands" sentence, which sits oddly beside SPEC-5's
new "a refused setup-command request runs no setup command" closer; dropped because the
sentence describes the endpoint set rather than the cause, and the endpoint set is unchanged.
(b) the §4.7 row's flat "the slot release ... runs whenever the adapter holds an entry for the
named session", which the attempt-mismatch rule falsifies as a sufficient condition; dropped
because the row delegates in the same breath ("[Section 4.7.1] states the named rules that
decide both") and §4.7.1:867 fixes the layering ("the two teardowns follow from the removal").
This is the "general statement narrowed by the specific rule in the same block" pattern
review-log.md:4137 records four findings refuted under. (c) the started-session rule's
`ConfigureWorkspace` exemption citing §4.7 as publishing a repeat ConfigureWorkspace
"idempotent", where §4.7's parenthetical scopes idempotency to the same `cwd` path
(spec/04_system-components.md:673); dropped as an imprecise gloss with no reachable divergence,
since the gateway's `cwd` for a session is always `/workspace/slots/{sessionId}/current`.

USEFUL [the `### Open` entry at review-log.md:1411]: "Does the §10.1.4 self-termination path owe
a scrub-report carve-out?" saved me from filing it. I re-derived it independently from the
staged §5.2 sentence "A cleanup that reclaims a slot the pod's shared runtime process was given
is a session release like any other and reports its outcome" against
pkg/adapter/holdstate.go:216-221 ("The loop sends no CH-RUNTIMEOPS drain signal and reports no
§5.2 session scrub") and holdstate.go:248-254, which does close the runtime and call
`removeSlotTree` on STARTED members. The log's refutation is the right one and I could not beat
it: shipped §5.2:453 already says the same thing ("a per-slot cleanup runs on every session
release, reported by the adapter via `ReportSessionScrub`"), so SPEC-3's append does not change
the sentence's truth value. The sibling `DemoteSDK` entry at review-log.md:1413 dies on
the identical argument; it is filed as OPEN but a spec-lane finding against SPEC-3's complement
sentence would be refuted for the same reason.

FACT: the §7.1 obligation's parenthetical scopes the §15.1 start transition to "a pod serving
concurrent sessions" but leaves the §7.3 re-attach clause unqualified, so a re-attach onto a
ONE-SESSION replacement pod is inside the obligation. I chased this as a contradiction against
the §5.2 and §6.2 one-session paragraphs and it does not land: both of those are scoped to "a
bind that fails at the workspace-preparation, setup or credential-assignment stage", and
`Binder.Resume` issues exactly one pod-side RPC (`cl.Resume`) plus the connect handshake —
no Prepare/Finalize/RunSetup/AssignCredentials on the resume path. EVIDENCE:
pkg/gateway/podlifecycle/podsession/binder.go:1589-1630; spec-changes.md:805.
Residual, unfiled and thin: a §7.3 re-attach whose reclaim does NOT complete on a one-session
replacement pod has no stated disposition anywhere (§7.1 states `leaked` only "on a pod serving
concurrent sessions"; §5.2's and §6.2's one-session paragraphs are stage-scoped past it). If a
later lens wants it, the fix is a clause in the staged §7.1 paragraph, not a new mechanism.

FACT: verbatim-anchor sweep of the staged spec edits is CLEAN. Every "reads, verbatim" block
resolves: spec/04:157 (§4.1 third sentence), spec/04:686 (§4.7 `Shutdown` row opening),
spec/04:854 (§4.7.9 step 5), spec/04:413,414 (the two §4.6.1 claim-deletion bullets),
spec/05:545 (`**Slot cleanup:**`), spec/05:453 (`**Scrub model.**`), spec/06:80 (the three
projection-prose clauses), spec/06:95-97 (the `claimed ──→ draining` trigger, unique inside the
`Occupancy projection` block — four other `claimed ──→ draining` lines live in the Recycle-edges
and Concurrent-occupancy groups at :114,:135,:137,:139), spec/06:234 (mid-resume cancel clause),
spec/06:290 (`**Client visibility:**`), spec/07:210,213,214 (§7.2 preamble, step 2, step 3),
spec/07:414 (§7.3 list tail), spec/15:1136 (all three §15.1 sentences), spec/29:711 (§29.4 step
13's tail, unique within §29.4's 575-762 range). Every anchor link target exists as a heading.
Do not re-run this sweep unless the staging moves.

FACT: the staged §5.2 "graceful window of ten seconds" for the §10.1 hold-timeout termination is
the shipped constant, and it is ONE pod-wide budget rather than per member:
`context.WithTimeout(context.Background(), 10*time.Second)` at pkg/adapter/holdstate.go:201,
shared across the pass-2 loop at :202-205. (Duplicates review-log.md:729; re-confirmed today.)

FACT: `PrepareWorkspaceRequest` carries NO `mid_session` field today — the field exists only on
`FinalizeWorkspaceRequest` (schemas/lenny-adapter.proto:724). The staged §4.7.1 carries-what
table says `mid_session` is "carried" on both, and the §15.4 trailer accounts for the gap ("the
wire edit ... adds ... the mid-session marker to `PrepareWorkspace`", spec-changes.md:921-924).
Not a defect; do not file it. `PrepareWorkspaceRequest` has `reserved 4`/`slot_id`, so 5 and 6
are the free numbers there.

FACT: §16.1.1's attribute table (spec/16_observability.md:295-297) declares itself "the single
source of truth for attribute names" and its `k8s_pod_name` row's "Used on" column reads
"Session-pool scrub and retirement metrics, slot failure and replacement metrics, and the
session reuse histogram". SPEC-6 adds two rows using that label without widening the column. I
judged the column a description rather than an enumeration and did not file it; a citations lens
may reach a different view, so the call is recorded rather than hidden.

### [spec-recheck-2.5.review-operational.1]

FACT: The spec-changes.md delta this round is against `spec-recheck-2-r3-prefix`, not against the snapshot the brief names. `spec-changes.md` is byte-identical in `spec-recheck-2-r4`, `-r4-start`, `-r5` and `-r5-start`; only `spec-recheck-2-r3-prefix` (168-line diff) and older differ. EVIDENCE: `diff -u scratchpad/cp-snap/0081_.../spec-recheck-2-r5/*.spec-changes.md proposals/0081_.../*.spec-changes.md` returns 0 lines. Loop over every snapshot dir with `diff | wc -l` first; it costs one command and finds the real base.

FACT: The delta is one mechanism change plus five consequent rewordings: the reclaim hold now persists **for the life of the pod** where the cleanup does not complete (staged §5.2 at spec-changes.md:652, §7.1 at :465, §6.2 at :804), and the §4.7.1 untokened-entry residue bullet was re-scoped from "every later bind attempt is refused" to "meets the started-session rule" (:281, :244-249, :268-281). `finished` was swept to `completed` throughout.

FACT: §15.4's published hold block (spec-changes.md:913) does NOT restate the pod-life clause, but it is not wrong: "holds ... until the cleanup that reclaims the slot has completed" never terminates for a cleanup that never completes, and the fixer replaced §15.4's conformance clause "while the cleanup is running" with "while the identifier is held" in this same delta, which is exactly the change that keeps it true. Do not file this as a §5.2/§15.4 divergence; it was checked and it holds.

FACT: `spec161Metrics` (pkg/observability/metrics/catalog_test.go:15,:193,:204) is a hand-maintained Go slice, not a parse of §16.1, so SPEC-6 adding a §16.1 row does not by itself move the two-way `MetricCatalog()` reconciliation. CODE-9's reasoning for entering only the gateway series there is sound.

FACT: The shipped §16.1 adapter-scrape deferral wording is "emitted by the adapter process inside the agent pod and therefore outside the [§16.9](#169-prometheus-scrape-targets-and-crds) default scrape target set until a deployer wires an adapter scrape target" (spec/16_observability.md:186-189). SPEC-6's staged untokened-entry row instead reads "Adapter-side; not scraped until the adapter metrics endpoint is wired" (spec-changes.md:1016). Judged below the bar (no gate reads the wording; `adapter_metric_catalog_test.go` checks name presence only, :100,:110), but see DEFERRED below.

DEFERRED [0081_....non-spec-changes.md]: CODE-9 states at non-spec-changes.md:1963-1965 that "SPEC-6's row for this series carries the deferral wording verbatim, so it follows the §16.1 rows that already carry it." That is false: SPEC-6's staged row (spec-changes.md:1016) says only "Adapter-side; not scraped until the adapter metrics endpoint is wired" and cites no §16.9. What is true is that the staged row states the same deferral in its own words and cites no section. Either reword the SPEC-6 row to the shipped §16.9-citing form or drop "verbatim" from CODE-9.

FACT: `superseded` and `absent` arms of the staged `Shutdown` handler answer `exitedCleanly = true` (`answerShutdown(outcome, true, untokened)` at non-spec-changes.md:355,:370), so SPEC-6's superseded-counter row claim "the slot is not leaked" is consistent with the gateway's shipped `leaked = err != nil || !cleanly`. Checked; do not re-derive.

WATCHOUT: The staged §7.1 clean-exit/cleanup-outcome split sentence (spec-changes.md:465) is true only on the `live` arm. On the pre-`running` arm, which is the paragraph's own dominant case, CODE-1 gates the report on `live` (non-spec-changes.md:445-446) and computes `ExitedCleanly: closeErr == nil && (live || treeErr == nil)` (:479), so a failed tree removal makes the response UNCLEAN and files NO report. FILED as a finding this round. Do NOT collapse it into the refuted "A failed per-slot cleanup on a session that reached running is reported clean on both channels": that one was about the LIVE arm and the `errors.Join(closeErr, treeErr)` re-key at :446 closed it; this one is about the unqualified new sentence at :465 and the PRE-`running` arm.


### [spec-recheck-2.5.review-performance.1]

DECISION: returned an empty findings list for the performance/scalability/failure-mode lens — BECAUSE the staged spec text has not moved since the r4 snapshot and every capacity mechanism in it is either unchanged from the tree or already recorded as an accepted failure mode — ALTERNATIVES: I derived and then dropped four candidates, each listed below with the evidence that killed it.

FACT: `spec-changes.md` is byte-identical across `spec-recheck-2-r4`, `-r4-start`, `-r5`, `-r5-start` and the live proposal (md5 `ad04d382`); the last change was between `spec-recheck-2-r3-prefix` and `-r4`. `diff -ru spec-recheck-2-r5 <live proposal>` is EMPTY, so this round's "delta" for the spec lane is nothing. Run the md5 sweep over the snapshot chain before planning any reading. EVIDENCE: scratchpad/cp-snap/0081_.../spec-recheck-2-r{3,4,5}*/

FACT: the "§7.1 releases the slot reservation afterwards vs §6.2 leaked slot remains counted" contradiction is NOT real, and it is the most natural false lead in this lane. Shipped `ReleaseSlotReservation` hard-codes `claimer.ReleaseSlot(ctx, name, false, false)` (pkg/gateway/podlifecycle/podsession/slotbinder.go:493-504, "leaked=false: a reservation rollback frees a slot that never held runtime resources"), which would decrement the counter §6.2:160 requires a `leaked` slot to keep. CODE-4 widens the signature with a `leaked bool`, so the staged §7.1 "releases the slot reservation afterwards" and "a reclaim that did not complete leaves the slot `leaked`" are reconcilable. EVIDENCE: pkg/gateway/podlifecycle/podsession/slotbinder.go:493-504; spec/06_warm-pod-model.md:160; review-log.md:796.

FACT: SPEC-4's re-key of the occupancy projection onto the phase at the claim DELETE creates NO new pod churn at the top tier. `ProjectOccupancyPhase` already returns `state.Draining` from `state.Claimed` unconditionally, with a comment naming the scrub-before-idle invariant; the spec was wrong and the code was right, so a pre-attached bind failure on a `recycle.enabled: true` pool already retires the pod today. EVIDENCE: pkg/controller/warmpool/occupancy.go:57-59, :131-140.

FACT: the staged §5.2 clause bounding the §10.1 hold-timeout cleanup "by a graceful window of ten seconds" matches the tree, and the N-member sharing is deliberate. `onHoldTimeout` builds ONE `context.WithTimeout(..., 10*time.Second)` shared by every member of pass 2, with the stated rationale that a shared runtime returns from a non-last close without touching the child, so only the last close consumes the grace. The staged sentence is an upper bound and stays true at `maxConcurrentSessions: 8`. `terminateHeldSession` does call `removeSlotTree`, so this path really is a per-slot cleanup the staged reclaim hold covers. EVIDENCE: pkg/adapter/holdstate.go:196-205, :253.

FACT: the two new §16.1 rows add no metric-cardinality class. `lenny_slot_failure_total` and `lenny_slot_pod_replacement_total` already carry `pool` + `k8s_pod_name`, and §16.2's attribute table already lists that pairing. EVIDENCE: spec/16_observability.md:14, :15, :297; spec-changes.md:1015-1016.

USEFUL [Standing context, review-log.md:1096]: the "do NOT re-derive the capacity family" entry named all five candidates a perf lens reaches on this proposal and why each was declined. It saved a full re-derivation of the Redis/etcd write-amplification question, and its closing note (the `leaked` disposition becomes the routine answer to an unanswered compensating `Shutdown`, so this proposal changes the RATE of a pre-existing durability asymmetry rather than the mechanism) is the one live perf observation on this proposal. It is recorded, not filed, and should stay that way unless the staged text grows a new mechanism.

### [spec-recheck-2.5.review-reliability.1]
DECISION: returned zero findings for the reliability lens on the spec staging — BECAUSE every recovery path the staging adds is either bounded by an existing shipped mechanism or explicitly recorded as an accepted residue with a named future closure — ALTERNATIVES: I considered filing (a) the lost-compensation stranded-token residue, (b) the adapter-restart loss of the in-memory reclaim hold, and (c) the mid-resume reclaim on a pod whose claim deletion now retires it under SPEC-4's re-keying; (a) is already on the refuted list twice, (b) is not a regression because no hold existed before this proposal and the on-disk residue is reached by the occupancy-zero whole-pod scrub, and (c) reduces to shipped §7.2 wording that SPEC-4 does not touch.
FACT: the staged §5.2 hold sentence's "for the [§10.1] hold-timeout termination ... a graceful window of ten seconds" resolves in the tree — EVIDENCE: pkg/adapter/holdstate.go:201 `context.WithTimeout(context.Background(), 10*time.Second)`; the §10.1 hold itself is spec/10_gateway-internals.md:58 (`coordinatorHoldTimeoutSeconds`, default 120s), which is a different figure and is not what the staged sentence claims.
FACT: the staged §6.2 pre-`running` paragraph's recovery claim ("the occupancy-zero whole-pod scrub ... removes every per-slot workspace tree and every per-slot credential file the pod still holds and verifies their absence") is accurate against the shipped scrub procedure — EVIDENCE: spec/05_runtime-registry-and-pool-model.md:461 (step 0, credential files), :467 (step 2, `rm -rf /workspace/slots/*`), :472 (step 6, stat-check verification incl. `/run/lenny/slots/`). The same paragraph's carve-out ("The slot's registry entry and its armed §4.9 lease-expiry timers are not reached by that scrub") is also accurate: steps 0-6 touch disk only.
FACT: the failure-to-complete path fails closed rather than open — the identifier stays held for the life of the pod (spec-changes.md staged §5.2 reclaim-hold paragraph), and session identifiers are unique, so a held identifier on a reused recycling pod refuses nothing a later session needs — EVIDENCE: spec-changes.md staged `**Slot-identifier reclaim hold.**` block; spec/05_runtime-registry-and-pool-model.md:545 (shipped "If cleanup fails, the slot is leaked — the pod continues but the slot is not reclaimed until pod termination").
WATCHOUT: the spec staging has not changed since the spec-recheck-2-r4 snapshot — `diff -q` against both `spec-recheck-2-r4` and `spec-recheck-2-r5` is empty, so two consecutive rounds produced no spec-changes edits. A reviewer told to "look at the delta first" should widen to the r3 snapshot instead — EVIDENCE: scratchpad/cp-snap/0081_fix_.../spec-recheck-2-r3 is the last snapshot that differs.

### [spec-recheck-2.5.review-security.1]

DECISION: Returned zero findings from the security lens on the staged spec edits — BECAUSE every control the lens owns is either untouched or tightened by the staging, and the two residual-state bounds it names (the `leaked` occupancy count and the `ceil(maxConcurrentSessions/2)` fail-or-leak trigger) are gateway-authoritative rather than pod-self-reported. ALTERNATIVES: I worked up and dropped four candidates, each recorded below so a later lens does not re-derive them.

FACT: `lenny_adapter_leaked_slots` is published by the GATEWAY, not the adapter, from the Redis slot-counter occupancy — so the staged §5.2 sentence "is surfaced on the `lenny_adapter_leaked_slots` gauge" does not route a security bound through a pod self-report. EVIDENCE: pkg/gateway/metrics/gatewaymetrics/gatewaymetrics_credential.go:219-223 registers it; pkg/sandbox/slotstate/registry.go:10 names the Redis slot counter "the sole occupancy authority". Do not file "the leak bound is adapter-sourced".

FACT: SPEC-4's occupancy-projection re-key is a TIGHTENING of one-session-only pod reuse, not a relaxation: `ProjectOccupancyPhase` already returns `Draining` for a claim DELETE on any `state.Claimed` pod regardless of recycle setting, and its own comment states the rule SPEC-4 adopts. The deleted §4.6.1 closing sentence ("the projection returns a pod from `claimed` to `idle` only on a recycling pool") is false against the controller. EVIDENCE: pkg/controller/warmpool/occupancy.go:56-63, :133-141.

FACT: `ShutdownRequest` already carries `coordination_generation = 6` (schemas/lenny-adapter.proto:1630-1635), so the new `unconditional_teardown` destructive form is still fenced by the §10.1 replica fence. The staged §4.7.1 sentence "Where both appear on one message, as on `Shutdown`, each is checked on its own terms" is accurate. Do not file "the unconditional teardown is unfenced".

FACT: the two new §16.1 counters do not leave a fail-closed event unobservable even though the adapter-side one is deferred until the adapter scrape target is wired. The staged attempt-mismatch rule answers `superseded` for BOTH the different-token arm and the untokened arm, and the gateway-side `lenny_slot_compensation_superseded_total` is scraped, so the fail-closed untokened case surfaces on a scraped series. EVIDENCE: spec-changes.md:857-860 (attempt-mismatch rule), :894-895 (the two rows).

WATCHOUT: §5.2:517 states that on `maxConcurrentSessions > 1` every slot's `/run/lenny/slots/{sessionId}/credentials.json` is readable by every other slot's agent process through the shared `lenny-cred-readers` group, and the deployer acknowledges that at admission. A finding built on "a residual credential file is readable by a co-tenant" therefore claims nothing the acknowledged model does not already permit between concurrent slots. EVIDENCE: spec/05_runtime-registry-and-pool-model.md:517.

MISTAKE (avoided, for the next lens): the sharpest-looking security candidate here is that the staged scrub-model append withholds `ReportSessionScrub` for the whole pre-`running` class, including the case where that cleanup FAILS, whereas shipped §5.2:545 says "The adapter reports each slot cleanup outcome (`released` or `leaked`) to the gateway via `ReportSessionScrub`" unconditionally — so a failed adapter-handler cleanup on a concurrent pod no longer increments the persistent leaked count. I did not file it, for three reasons a future lens should weigh before re-deriving it: (a) on a concurrent pod the §7.1 reclaim reaches the same attempt and its clean-exit flag carries the accounting, so the uncovered set is narrower than it first reads; (b) the shipped code never reported it either (`pkg/adapter/session.go:271` discards `removeSlotTree`'s error and `pkg/adapter/sessionscrubreporter.go:39-44` keys the outcome on `closeErr` alone), so the gap is tree-vs-shipped-§5.2 and predates the proposal; (c) two closely adjacent versions of this argument are already on the refuted list ("A failed per-slot cleanup on a session that reached running is reported clean on both channels" and "Staged §5.2 cleanup cancels the §4.9 enforced lease deadline on a path the same bullet lets fail"). Filing a third variant would cost two verifiers and close nothing.

UNVERIFIED: whether a `Shutdown` carrying the recycle disposition still runs the §5.2 whole-pod scrub when the staged no-entry rule fires first (the rule says it "removes nothing, performs neither teardown, and answers `absent`" and is ordered ahead of the unconditional-teardown rule). SPEC-1's §4.1 replacement keeps the scrub independent ("runs the whole-pod scrub when the recycle disposition is set"), so the spec staging reads consistent to me, and the code-lane twin of this was already found and fixed ("the staged Shutdown handler's non-removing early return bypasses clause three"). A later lens with the §4.7 row's unedited remainder in front of it should confirm the row does not re-couple the scrub to the removal.
