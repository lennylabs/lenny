// SPDX-License-Identifier: MIT

// Fixture: a second tier-0 gate, so the enumeration has a population left when
// the deleted-file tree drops the first one.

package tier0_static

import "testing"

func TestOtherGateCertifiesTheTree(t *testing.T) {
	t.Parallel()
}
