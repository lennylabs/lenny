{
  "scenario": "credential_lifecycle",
  "version": "0.1.0",
  "recorded_at": "2026-05-20T00:00:00Z",
  "duration": "1m",
  "vus": 10,
  "metric_ms": {
    "avg": 95.0,
    "p50": 80.0,
    "p90": 160.0,
    "p95": 200.0,
    "p99": 320.0,
    "p999": 580.0
  },
  "error_rate": 0.0,
  "throughput_per_sec": 100.0,
  "notes": "Placeholder §4.9 / §15.1 end-user credential lifecycle baseline (register → list → rotate → revoke → delete against /v1/credentials). SLO: P95 < 250ms per round-trip with the Postgres-backed credentialstore + KMS envelope. Smoke against the e2e Kind cluster passes (error rate 0%, p99 ~85ms) per the BUILD-PROGRESS Phase 11.5 done entry. Per-iteration synthetic user id because credentialstore deduplicates by (tenant, user, provider)."
}
