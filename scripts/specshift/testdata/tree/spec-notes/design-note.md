A design note kept beside the specification tree rather than inside it.
Its directory name begins with the characters of the specification
directory, so a confinement matching by substring would cover it while a
confinement matching by path segment does not. It carries a citation at
spec: §10 line 437, so a run that reached it would rewrite it.
