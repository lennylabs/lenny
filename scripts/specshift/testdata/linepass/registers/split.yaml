# SPDX-License-Identifier: MIT
#
# The per-file citation counts that drive the line pass over the fixture
# whose carriers sit on both sides of the specification directory. One
# entry names a carrier a run confined to that directory covers and the
# other names a carrier the complementary confinement covers, so a
# confined run holds an entry it defers.
kind: line-citation-count-baseline
version: 1
files:
    - path: pkg/carrier/carrier.go
      count: 1
    - path: spec/17_deployment-topology.md
      count: 1
