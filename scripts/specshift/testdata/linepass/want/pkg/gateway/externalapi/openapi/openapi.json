{
  "openapi": "3.1.0",
  "x-lenny-spec-section": "§4.6",
  "x-lenny-spec-sections": ["§4.6.1", "§4.8", "§4.9"],
  "components": {
    "schemas": {
      "Sandbox": {
        "description": "The claim is a lease the gateway grants.",
        "properties": {
          "lease": { "type": "string", "description": "The lease renews while the session runs." },
          "renewal": { "type": "string", "description": "renewal is idempotent while the lease stands." }
        }
      },
      "Plan": {
        "description": "authored beside the session.",
        "properties": {
          "drain": { "type": "string", "description": "Drain a pool (stop admitting new sessions, report the in-flight count)." }
        }
      }
    }
  }
}
