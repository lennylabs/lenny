// SPDX-License-Identifier: MIT

syntax = "proto3";

message LifecycleAck {
  // Delivery flag for the CH-PODLIFECYCLE; false when the pod's runtime has
  // no CH-PODLIFECYCLE of its own.
  bool delivered = 1;
}
