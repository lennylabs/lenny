# Runtime snippets

The snippet below is Go, so the line after a wrapped comment line opens
behind the marker of the same comment and the phrase folded across the
two is one site.

```go
// Subscribe attaches the pod to the CH-PODLIFECYCLE and returns once the first frame arrives.
func Subscribe() {}
```
