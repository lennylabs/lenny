# Runtime notes

A paragraph line here ends on the word lifecycle
## Channels of the runtime

The heading above interrupts the paragraph, and a markdown document
carries no comment marker, so the line break before it is a line break
the author wrote rather than the continuation of one comment.

An adapter opens one control
* channel entry per session, where a list item interrupts the paragraph
  the same way.
