## 13.2 Network isolation

The table below cites an identifier rather than declaring one. The registers in
the communication-channels section are the declaration position, and this
section carries no register.

| Identifier | Note |
|:--|:--|
| `CH-ELSEWHERE` | cited by a section that declares no identifier |
