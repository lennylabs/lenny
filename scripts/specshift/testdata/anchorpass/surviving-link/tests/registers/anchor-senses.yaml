# SPDX-License-Identifier: MIT
#
# A well-formed register, so what this tree exercises is the link class
# alone: the link into the anchor the anchor-move map retires is
# redirected, and the link into the anchor its document still declares is
# left as it is written.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
