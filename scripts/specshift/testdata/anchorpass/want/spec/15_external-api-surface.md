# 15. External API Surface

## 15.4 Runtime Adapter Specification

The runtime adapter contract points at the wire artifacts it is defined by.
The channel prose that stood here has moved to [the channel contract card](28_communication-channels.md#2851-ch-adapterbinary).

#### MessageEnvelope — Unified Message Format

The envelope the carve-out keeps in this file. Its fields are the ones the
runtime SDKs expose.

The same-page link to a surviving anchor is [the versioning rules](#155-api-versioning-and-stability).
The same-page link to the retired anchor is [the adapter protocol](28_communication-channels.md#2851-ch-adapterbinary).
The same-page link to a retired anchor whose successor stays on this page is
[the message format](#messageenvelope--unified-message-format).

## 15.5 API Versioning and Stability

The compatibility window and the deprecation rules.
