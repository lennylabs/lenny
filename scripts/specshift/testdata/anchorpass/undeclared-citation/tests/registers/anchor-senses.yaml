# SPDX-License-Identifier: MIT
#
# A well-formed register that resolves both occurrences of the sibling
# carrier. The other carrier cites a section no specification file of
# the tree states a heading for, which the anchor-move map retires no
# anchor for either, so it is outside the citation class and needs no
# entry. The run redirects the sibling and leaves it as it is written.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 2
    destination: spec/28_communication-channels.md#2851-ch-adapterbinary
