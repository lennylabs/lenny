# SPDX-License-Identifier: MIT
#
# A well-formed register, so what this tree exercises is the link class
# alone: the link into an anchor the map retires no reference into is
# left byte-identical, while the sibling link the map does redirect is
# rewritten in the same run.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
