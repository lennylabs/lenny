# SPDX-License-Identifier: MIT
#
# The register resolves both occurrences of the sibling carrier and
# records none of the three citations the other carrier holds, because
# none of those three names a section whose anchor the anchor-move map
# retires. The run redirects the sibling and leaves all three as they are
# written, rather than requiring an entry for a citation this migration
# does not touch.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 2
    destination: spec/28_communication-channels.md#2851-ch-adapterbinary
