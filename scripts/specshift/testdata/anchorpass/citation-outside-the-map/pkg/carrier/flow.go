// SPDX-License-Identifier: MIT

package carrier

// Flow reads the flow-control rules.
//
// spec: §15.9.9 (a section no specification file of the tree declares
// and the anchor-move map retires no anchor for, which is a citation
// the tree carried stale before the run rather than one this reduction
// invalidated)
func Flow() []byte { return nil }

// Audit writes the operability record.
//
// spec: §25 (a section a specification file states only in its
// level-one title, which the anchor-move map retires no anchor for)
func Audit() []byte { return nil }
