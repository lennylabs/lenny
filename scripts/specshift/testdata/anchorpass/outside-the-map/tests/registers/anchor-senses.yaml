# SPDX-License-Identifier: MIT
#
# A well-formed register, so a run over this tree turns on the link the
# anchor-move map does not name rather than on the register.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
