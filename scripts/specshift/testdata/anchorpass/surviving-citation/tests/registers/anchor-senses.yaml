# SPDX-License-Identifier: MIT
#
# The register resolves both occurrences of the sibling carrier and
# records neither of the two citations the other carrier holds, because
# a specification file still declares both of those sections. The run
# redirects the sibling and leaves both as they are written, rather than
# requiring an entry for a citation this migration does not touch.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 2
    destination: spec/28_communication-channels.md#2851-ch-adapterbinary
