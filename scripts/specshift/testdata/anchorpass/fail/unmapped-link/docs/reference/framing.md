# Adapter framing

The adapter framing is stated in
[the adapter protocol](../../spec/15_external-api-surface.md#1541-adapterbinary-protocol),
an anchor the anchor-move map does carry a successor for, so this link is the
carrier the run would otherwise rewrite and it stays byte-identical while the
unresolved link beside it stops the run.
