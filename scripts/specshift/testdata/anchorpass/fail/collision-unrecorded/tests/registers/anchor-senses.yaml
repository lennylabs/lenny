# SPDX-License-Identifier: MIT
#
# The register resolves the sibling carrier's occurrences and records
# none for the carrier that cites the section a documentation heading
# numbers, so the run stops over that citation with every carrier left
# byte-identical.
kind: anchor-senses
version: 1
entries:
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 1
    destination: spec/15_external-api-surface.md#messageenvelope--unified-message-format
  - file: sdks/runtime/go/runtime/types.go
    occurrence: 2
    destination: spec/28_communication-channels.md#2851-ch-adapterbinary
