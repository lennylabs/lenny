// SPDX-License-Identifier: MIT

package carrier

// Flow reads the flow-control rules.
//
// spec: §3.4 (a section number no specification file of the tree states
// a heading for and no anchor-move map entry retires)
func Flow() []byte { return nil }

// Frame writes the framing onto the adapter connection.
//
// spec: §15.4.1 (an anchor the map does retire, whose occurrence the
// sense register does not record)
func Frame() []byte { return nil }
