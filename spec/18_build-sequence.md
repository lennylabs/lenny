## 18. Build Sequence

> **Note:** This section described a tentative phasing of the development of v1. Its goal is to inform the sequence in which AI agents should build and test v1, in order to reduce risk. However, this entire technical spec describes v1 and everything needs to be implemented.

| Phase | Components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Milestone                                                                                                                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0     | **Pre-implementation gating decisions** (must be complete before Phase 1 begins): (1) **Open-source license selection (ADR-008):** evaluate candidate licenses (MIT, Apache 2.0, AGPL + commercial, BSL) against enterprise legal requirements, runtime author copyleft clarity, `kubernetes-sigs/agent-sandbox` upstream compatibility, and competitive positioning (see [Section 23.2](23_competitive-landscape.md#232-community-adoption-strategy)). Record decision as ADR-008 in `docs/adr/`. The license must be committed to the repository root before any contributor engagement, `CONTRIBUTING.md` publication, or external PR acceptance. This is a hard prerequisite for Phase 2's CONTRIBUTING.md and community onboarding. (2) **ADR-007** (`SandboxClaim` optimistic-locking and failover fencing verification, [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) must be complete before Phase 1 implementation begins. | License committed; ADR-007 and ADR-008 both recorded; repository publicly visible for early community awareness |
| 1     | Core types: `Runtime` (unified, with `labels`), `type` field, `capabilities` (including `interaction: one_shot \| multi_turn`, `injection`), `executionMode`, `allowedExternalEndpoints` on delegation lease, `input_required` task state, messages array in TaskRecord, `suspended` session state. Agent-sandbox CRDs (`SandboxTemplate`, `SandboxWarmPool`, `Sandbox`, `SandboxClaim`). `Connector` resource with `labels`. `environmentId` nullable field on billing event schema. `crossEnvironmentDelegation` structured form schema slot. **Machine-readable wire-contract artifacts committed to the repository** ([§15.4](15_external-api-surface.md#154-runtime-adapter-specification), [§14.1](14_workspace-plan-schema.md#141-workspaceplan-schema-versioning)): `schemas/lenny-adapter.proto`, `schemas/lenny-adapter-jsonl.schema.json`, `schemas/outputpart.schema.json`, `schemas/workspaceplan-v1.json`. These are spec-first artifacts; Phase 2 implements against them and the CI build verifies the implementation stays in sync. **Prerequisite:** ADR-007 (`SandboxClaim` optimistic-locking and failover fencing verification, [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) must be resolved before Phase 1 begins — the `SandboxClaim` CRD design depends on its outcome. **Prerequisite:** ADR-008 (license selection, [Section 23.2](23_competitive-landscape.md#232-community-adoption-strategy)) must be complete and the license committed to the repository root before Phase 1 begins.                                    | Foundation; wire contract published before implementation                                                                                                                                                                             |
| 1.5   | **Database migration framework**: establish the migration tooling, conventions, and CI gate that all subsequent phases depend on. Deliverables: (1) **Migration tool selection and setup** — select and configure a migration tool (e.g., `golang-migrate`, `atlas`, or `goose`) committed to the repository; (2) **Initial schema migration** — all data models introduced in Phase 1 (sessions, tasks, tenants, billing events) must be expressed as numbered migration files (`migrations/0001_initial.sql`, etc.) rather than ad-hoc `CREATE TABLE` scripts; (3) **CI migration gate** — CI must run all pending migrations against a clean Postgres instance and verify the schema matches the expected state before any test suite executes; (4) **Rollback documentation** — each migration file has a corresponding rollback script; `docs/runbooks/db-rollback.md` documents the procedure for rolling back a failed migration in production; (5) **Convention document** — `docs/contributing/migrations.md` specifies naming conventions, backward-compatibility requirements (additive-only for in-flight sessions), and the process for reviewing migration PRs. (6) **Schema and query linters** — `scripts/lint-schema.sh` verifies that every tenant-scoped table has `tenant_id` as the leading column of its primary index or of every composite index on high-frequency query paths ([§12.3](12_storage-architecture.md#123-postgres-ha-requirements) R-01), with an exception annotation for session-sharded tables (`-- session-sharded`). `scripts/lint-queries.sh` verifies that no SQL JOIN between tenant-scoped tables omits `a.tenant_id = b.tenant_id` in the ON clause ([§12.3](12_storage-architecture.md#123-postgres-ha-requirements) R-02), with suppression via `-- platform-admin-cross-tenant-allowed` + `-- platform-admin-cross-tenant-justification` annotations. Both linters run in CI on every pull request that modifies a migration file or a `.sql`/`.go` file containing SQL. The R-02 annotation inventory check ([§12.3](12_storage-architecture.md#123-postgres-ha-requirements)) is integrated into `lint-queries.sh`. Every phase from 1.5 onward that introduces new data models must include migration files as part of its definition of done. | Migration framework in place; all Phase 1 schemas expressed as migrations; CI gate enforces migration hygiene from this phase forward |
| 2     | Adapter binary protocol implementation against the Phase 1 wire-contract artifacts (`schemas/lenny-adapter.proto`, `schemas/lenny-adapter-jsonl.schema.json`, `schemas/outputpart.schema.json`): unified `{type:"message"}` (no separate `prompt`), `slotId` field for concurrent-workspace multiplexing, multi-server MCP adapter + lifecycle channel, adapter manifest written before binary spawns. CI gate verifies the gateway, controller, and echo runtime binaries conform to the published schemas (schema-driven compliance tests per [§24.8](24_lenny-ctl-command-reference.md#248-external-adapter-management)). Includes `make run` local dev mode with embedded stores and echo runtime. **SQLite dev-mode schema**: the `make run` SQLite schema is generated at build time from the Postgres migration files by stripping Postgres-specific features (RLS policies, `SET LOCAL`, PL/pgSQL triggers); a CI test verifies the SQLite schema is structurally equivalent to the Postgres schema for core tables (columns, types, constraints minus RLS). This prevents schema drift between the `make run` path and production. **Startup benchmark harness**: automated test measuring per-phase startup latency (pod claim, credential assignment, workspace materialization, setup commands, agent session start, first prompt dispatch) for each runtime class; produces pod-warm vs SDK-warm comparison; runs in CI and ad-hoc. **Checkpoint duration benchmark (best-effort baseline only)**: measures end-to-end checkpoint time across workspace sizes (10MB, 100MB, 500MB) using the Minimum-tier echo runtime's best-effort checkpoint path and validates the < 2s SLO for ≤ 100MB workspaces (see [Section 4.4](04_system-components.md#44-event--checkpoint-store)). Because the Full-tier lifecycle channel (cooperative quiescence handshake) is not available until Phase 2.8's `streaming-echo` runtime, this benchmark does not include quiescence handshake overhead; Phase 2.8 re-validates the checkpoint duration SLO with Full-tier checkpoints. | Can start an agent session; contributors can run locally; startup latency measurable; best-effort checkpoint duration baselined                                                                    |
| 2.5   | **Basic observability foundation**: structured logging with correlation fields (`tenantId`, `sessionId`, `taskId`, `sandboxId`) across all components, OpenTelemetry trace propagation wired into gateway and controller gRPC calls, request-scoped correlation ID generation and header propagation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | All subsequent phases debuggable with correlated traces and structured logs                                                                                                            |
| 2.8   | **Extended test runtime** (`streaming-echo`): a built-in test runtime that extends the Phase 2 echo runtime with (1) simulated streaming output — emits a configurable sequence of `OutputPart` chunks on each prompt to exercise the streaming and reconnect path, (2) `ReportUsage` implementation — reports deterministic simulated token counts (prompt + completion) so quota enforcement can be exercised without a real LLM provider, and (3) Full-tier lifecycle channel support — handles `checkpoint_request` and emits `checkpoint_ready` on a configurable delay, enabling checkpoint/resume tests in CI. Ships as a built-in fixture alongside the echo runtime. **Checkpoint duration SLO re-validation (Full-tier):** re-run the Phase 2 checkpoint duration benchmark using `streaming-echo`'s Full-tier lifecycle channel (cooperative quiescence handshake) across all workspace sizes (10MB, 100MB, 500MB); the < 2s SLO for ≤ 100MB workspaces ([Section 4.4](04_system-components.md#44-event--checkpoint-store)) must hold with quiescence handshake overhead included. Compare against the Phase 2 best-effort baseline to quantify quiescence overhead. Milestone gate: Phase 6, 7, and 8 CI pipelines MUST validate their milestones using `streaming-echo` in addition to any real-LLM smoke tests. | Phase 6–8 milestones fully CI-validatable without real LLM credentials; streaming, quota, and checkpoint paths exercised in every CI run; Full-tier checkpoint duration SLO re-validated with quiescence overhead |
| 3     | PoolScalingController. `DelegationPolicy` resource. `setupPolicy` enforcement. `taskPolicy.cleanupCommands`. **`RuntimeUpgrade` state machine** ([Section 10.5](10_gateway-internals.md#105-upgrade-and-rollback-strategy)): `Pending → Expanding → Draining → Contracting → Complete` state machine with pause/resume/rollback, admin API upgrade endpoints (`/v1/admin/pools/{name}/upgrade/*`), `lenny-ctl admin pools upgrade` CLI commands, and integration with PoolScalingController reconciliation. Testable against the echo runtime (upgrade from echo v1 to echo v2 image). **mTLS PKI setup**: cert-manager installation, `ClusterIssuer` configuration (self-signed or Vault-backed), certificate issuance for gateway replicas and controller, SPIFFE-compatible URI SANs for agent pod certificates ([Section 10.3](10_gateway-internals.md#103-mtls-pki)), trust bundle distribution to agent pods, mTLS enforcement on gateway↔pod gRPC channel (see [Section 10.3](10_gateway-internals.md#103-mtls-pki)). This is required before PoolScalingController creates pods that communicate with the gateway.                                                                                                                                                                                                                                                                                                                                                                                          | Pods stay warm; delegation policy works; runtime upgrades operational; gateway↔pod mTLS operational                                                                                                                  |
| 3.5   | **Basic security hardening**: default-deny NetworkPolicy for agent namespaces, gVisor RuntimeClass validation (verify gVisor is functional before proceeding), digest-pinned base images for all controller-created pods, **admission policy deployment** (RuntimeClass-aware PSS enforcement, `shareProcessNamespace` validation, **`lenny-sandboxclaim-guard` ValidatingAdmissionWebhook** for double-claim prevention ([Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) — deployed with `failurePolicy: Fail`, `replicas: 2`, PodDisruptionBudget, and the `SandboxClaimGuardUnavailable` Critical alert — see [Section 17.2](17_deployment-topology.md#172-namespace-layout)), integration tests verifying controller-generated pod specs pass admission policies. **mTLS end-to-end verification**: integration tests confirming gateway↔pod and gateway↔controller mTLS is functional (not just admitted by policy), certificate auto-renewal works, and plain-text connections are rejected.                                                                                                                                                                                 | Agent pods run with network isolation, validated sandboxing, admission policy enforcement, and mTLS verified end-to-end before any external connectivity or credentials are introduced |

> **Note:** Digest-pinned images from a private registry are required from Phase 3 onward. Full image signing and attestation verification (Sigstore/cosign + admission controller) is Phase 14.

| Phase | Components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Milestone                                                                                                                                 |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 4     | Session manager + session lifecycle + REST API                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Full create → upload → attach → complete flow                                                                                             |
| 4.5   | **Admin API foundation**: runtimes, pools, connectors, delegation policies, tenant management, external adapters registry. Gateway loads config from Postgres. Capability inference from MCP annotations at connector registration. **Bootstrap seed mechanism**: `lenny-ctl bootstrap` CLI command + Helm init Job ([Section 17.6](17_deployment-topology.md#176-packaging-and-installation)). **Authentication infrastructure**: OIDC/OAuth 2.1 JWT validation wired into the gateway ([Section 10.2](10_gateway-internals.md#102-authentication)), `tenant_id` claim extraction and propagation, `UserStateStore` integration for role resolution, multi-tenant JWT test fixtures in CI (covering `platform-admin`, `tenant-admin`, and `user` roles across multiple tenants). This deliverable is the named auth-complete milestone that Phase 7's `AuthEvaluator` depends on. | All operational config API-managed; fresh install seeds Day-1 resources automatically; authentication verified for multi-tenant scenarios |
| 5     | Gateway `ExternalAdapterRegistry`. MCP adapter, Completions adapter, Open Responses adapter all active. `list_runtimes`, `GET /v1/runtimes`, `GET /v1/models` with identity-aware filtering. `type: mcp` runtime endpoints at `/mcp/runtimes/{name}`. Tenant RBAC config API. `noEnvironmentPolicy` enforcement. **REST/MCP contract tests** ([Section 15.2.1](15_external-api-surface.md#1521-restmcp-consistency-contract)): OpenAPI→MCP schema generation step in build pipeline; CI contract tests asserting semantic equivalence across both API surfaces for all overlapping operations.                                                                                                                                                                                                                                           | Clients can create and use sessions via REST, MCP, and Completions API; API surface consistency enforced by CI                            |

> **Note:** Phase 5 sessions use the zero-credential echo runtime (Phase 2). Phase 5.4 establishes mandatory etcd encryption-at-rest before any credentials are written to the cluster. Phase 5.5 introduces basic credential leasing and the Basic Token Service (multi-replica HA, K8s Secrets backend protected by etcd encryption, no application-layer KMS). **Real LLM provider testing is gated on Phase 5.75** — minimum viable policy enforcement (JWT auth, per-tenant quota enforcement, and basic budget checks) must be operational before any session using real credentials proceeds to Phase 6 interactive testing. The Token Service is the authoritative owner of `TokenStore` from Phase 5.5 onward; the Phase 12a hardening phase adds application-layer KMS envelope encryption and OAuth flows.

> **Note — `noEnvironmentPolicy` access gap (Phases 5–14).** Phase 5 activates `noEnvironmentPolicy` enforcement; the full Environment resource (tag-based selectors, member RBAC) does not arrive until Phase 15. Because the platform default is `deny-all`, any deployment that reaches Phase 5 without environments configured will block all `user`-role principals from accessing any runtime. **Resolution:** the Phase 4.5 bootstrap seed ([Section 17.6](17_deployment-topology.md#176-packaging-and-installation)) must address this by either (a) setting `noEnvironmentPolicy: allow-all` on the default tenant (suitable for single-tenant or development deployments), or (b) seeding at least one environment that covers all initial users. Option (a) is the recommended approach for pre-Phase 15 builds; it can be tightened to Option (b) or replaced by full environment-based RBAC once Phase 15 is delivered. `platform-admin` and `tenant-admin` roles are unaffected — their API access is governed by platform RBAC, not `noEnvironmentPolicy`.

| Phase | Components                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Milestone                                                                                                                                                                                            |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 5.4   | **etcd encryption at rest (mandatory prerequisite for credential storage):** Enable Kubernetes etcd encryption-at-rest (`EncryptionConfiguration`) for all Kubernetes Secret resources and CRD data before any real credentials are written to the cluster. Required providers: `aescbc` (minimum) or `kms` (recommended for production). Helm chart must include an `etcdEncryption.enabled` value defaulting to `true` and fail deployment if set to `false` in a non-development profile. Deliverables (self-managed clusters only; managed K8s uses provider-specific encryption — see [Section 4.9](04_system-components.md#49-credential-leasing-service) topology table): (1) `EncryptionConfiguration` manifest included in Helm chart; (2) CI gate verifying that a test Secret written to the cluster is stored encrypted in etcd (confirmed via `etcdctl get` on the raw key); (3) runbook for key rotation (`docs/runbooks/etcd-key-rotation.md`). **This phase is a hard prerequisite for Phase 5.5.** Without encryption at rest, credentials written to Kubernetes Secrets are stored in plaintext in etcd. The Phase 5.5 Token Service must not be deployed until this gate is cleared. | etcd encryption verified in CI; credentials will be encrypted at rest from Phase 5.5 onward |
| 5.5   | **Basic credential leasing** + **Basic Token Service**: `CredentialProvider` interface, `anthropic_direct` provider implementation, single-pool credential assignment (`least-loaded` strategy), `AssignCredentials` RPC to push credentials to the adapter, lease creation and expiry. Admin API endpoint for credential pool registration. **Basic Token Service (single-replica, no KMS envelope encryption)**: a minimal Token Service deployment that owns `TokenStore` writes/reads and serves `AssignCredentials` / `RevokeCredentials` RPCs to the gateway over mTLS. Credentials are read from Kubernetes Secrets at this stage — no application-layer KMS envelope encryption, no OAuth flows, no credential rotation. Kubernetes Secrets are protected by etcd encryption-at-rest (Phase 5.4 gate). **Token Service multi-replica HA** is introduced here: the Token Service is deployed with `replicas: 2` and a `PodDisruptionBudget` (`minAvailable: 1`) from Phase 5.5 onward — single-replica is not acceptable once real credentials are present. This component is the authoritative owner of `TokenStore` from Phase 5.5 onward; Phase 7's `AuthEvaluator` (backed by `TokenStore` and `UserStateStore`) and Phase 11's advanced credential leasing both depend on it being present. **Limitations at this stage:** K8s Secrets backend only (with etcd encryption), no OAuth token storage, no application-layer KMS, no credential rotation — KMS envelope encryption and OAuth flows are addressed in Phase 12a hardening. **LLM proxy delivery mode (`deliveryMode: proxy`) is not yet available** — all credential delivery in this phase is direct mode only (`deliveryMode: direct`). Proxy mode is introduced in Phase 5.8; until then, multi-tenant deployments must use sandboxed isolation (`isolationProfile: sandboxed`) with direct mode, and direct mode with `isolationProfile: standard` is blocked by admission control in `tenancy.mode: multi` (see [Section 4.9](04_system-components.md#49-credential-leasing-service)). **Prerequisite:** Phase 5.4 (etcd encryption) must be complete and CI-verified before this phase begins. | Credential infrastructure ready; Token Service present as the authoritative credential owner (multi-replica HA, etcd-encrypted Secrets); real LLM provider testing begins after Phase 5.75 policy gate |
| 5.6   | **Targeted security design review — credential injection surface**: review credential leasing design (Phase 5.5) for: credential scope leakage between tenants, Token Service RPC authentication, K8s Secrets access controls, and lease expiry enforcement. This is a focused design review, not a pentest. Any architectural findings must be resolved before Phase 6 proceeds. | Credential injection attack surface reviewed; blocking findings resolved |

> **Note — Phase 5.75 policy gate:** Phase 5.75 is a hard prerequisite for Phase 6. No session using real LLM credentials may be exercised in integration tests or development until Phase 5.75 is complete. This gate ensures that real credentials are always protected by authentication and quota enforcement, even in early testing environments.

| Phase | Components | Milestone |
| ----- | ---------- | --------- |
| 5.75  | **Minimum viable policy enforcement** (gated prerequisite for real-credential sessions): Wire `AuthEvaluator` (JWT validation + `tenant_id` extraction, backed by Phase 4.5 authentication infrastructure) and `QuotaEvaluator` (per-tenant concurrency limits and basic token budget checks) into the gateway interceptor chain. These two built-in interceptors (priority ≤ 100 for `AuthEvaluator`, see [Section 4.8](04_system-components.md#48-gateway-policy-engine)) must be active and enforcing before any session using a real LLM credential is permitted. At this stage: deny unauthenticated session creation, enforce per-tenant `maxConcurrentSessions` from `credentialPolicy`, and reject sessions when the tenant's token budget is exhausted. Full rate-limit rules, `DelegationPolicyEvaluator`, and audit logging ship in Phase 7; this phase installs only the controls necessary to prevent unauthenticated or unbudgeted real-credential access. | Real LLM credentials are protected by JWT auth and basic quota enforcement; Phase 6 real-credential integration testing may begin |

> **Note — Phase 6–8 CI test coverage:** Phase 5.75 enables real LLM provider testing from Phase 6 onward, which is sufficient for integration testing of streaming reconnect (Phase 6), quota enforcement (Phase 7), and checkpoint/resume (Phase 8). However, CI pipelines must not depend exclusively on real LLM credentials for milestone validation. The `streaming-echo` runtime (Phase 2.8) provides simulated streaming output, `ReportUsage` token reporting, and Full-tier lifecycle channel support (`checkpoint_request`/`checkpoint_ready`) specifically to cover these paths in CI without external credentials. Phase 6, 7, and 8 CI pipelines are required to include `streaming-echo`-based test cases as part of their milestone validation.

| 5.8 | **LLM Proxy subsystem + LiteLLM sidecar** — the gateway's fourth internal subsystem boundary ([Section 4.1](04_system-components.md#41-edge-gateway-replicas), [Section 4.9](04_system-components.md#49-credential-leasing-service) LLM Reverse Proxy): implement the credential-injecting reverse proxy for LLM provider traffic in the two-component form (Lenny proxy subsystem + LiteLLM sidecar container in the gateway pod). Deliverables: (1) **Lease-token validation** — Lenny proxy validates the opaque `leaseToken` supplied by the agent pod on every LLM proxy request, binding it to the session's active `CredentialLease`; (2) **OpenAI/Anthropic receive surface** — Lenny proxy exposes `POST /v1/chat/completions` and `POST /v1/messages` endpoints per pool `proxyDialect`; agent pods use standard OpenAI/Anthropic SDKs; (3) **LiteLLM sidecar deployment** — LiteLLM container ships alongside the gateway pod, reads real upstream API keys from a gateway-populated tmpfs file, translates OpenAI/Anthropic requests to upstream provider wire formats (Anthropic, Bedrock, Vertex, Azure, etc.), holds all upstream credentials; (4) **Loopback forwarding** — Lenny proxy forwards validated requests to LiteLLM over pod-local loopback with a shared-secret auth header; streaming responses stream back through the subsystem; (5) **`PreLLMRequest` / `PostLLMResponse` interceptor chain** ([Section 4.8](04_system-components.md#48-gateway-policy-engine)) wired into the Lenny proxy path (pre-LiteLLM and post-LiteLLM); token-usage extraction from LiteLLM's upstream-provider-native usage metadata as the authoritative record ([Section 11.2](11_policy-and-controls.md#112-budgets-and-quotas)); (6) **`deliveryMode: proxy` on `CredentialPool`** — pool registration accepts `deliveryMode: proxy` plus required `proxyDialect: openai | anthropic` matching the Runtime's `credentialCapabilities.proxyDialect`; admission rejects mismatches with `422 INVALID_POOL_PROXY_DIALECT`; (7) **SPIFFE-binding for lease tokens** ([Section 4.9](04_system-components.md#49-credential-leasing-service) — v1 requirement for multi-tenant deployments) — the Lenny proxy extracts the peer SPIFFE URI from the mTLS connection and verifies it matches the lease record before forwarding to LiteLLM; (8) **Per-subsystem isolation** — dedicated goroutine pool, `maxConcurrent` config, and per-subsystem metrics (`lenny_gateway_llm_proxy_active_connections`, `lenny_gateway_llm_proxy_request_duration_seconds`, `lenny_gateway_llm_proxy_circuit_state`) as specified in [Section 4.1](04_system-components.md#41-edge-gateway-replicas); circuit breaker behavior per [Section 4.9](04_system-components.md#49-credential-leasing-service); (9) **Admission control enforcement** — `lenny-direct-mode-isolation` webhook blocks `deliveryMode: direct` + `isolationProfile: standard` when `tenancy.mode: multi` ([Section 4.9](04_system-components.md#49-credential-leasing-service)); (10) **Integration tests** verifying proxy mode end-to-end with `streaming-echo` runtime and a mock upstream provider behind LiteLLM: lease-token validation, credential injection inside LiteLLM, streaming relay, and circuit breaker open-state rejection; (11) **LiteLLM sidecar hardening deliverables** ([Section 4.9](04_system-components.md#49-credential-leasing-service) LiteLLM sidecar hardening): hardened wrapper image (`lenny/litellm-hardened:<lenny-version>`) built, cosign-signed, and published; container security context (non-root, all-caps-dropped, read-only rootfs, `seccompProfile: RuntimeDefault`); LiteLLM configuration lockdown (admin UI/routes disabled, telemetry off, route allowlist enforced at the process level, loopback-only binding); admission-webhook verification that the sidecar image is the signed wrapper; `lenny_gateway_litellm_route_anomaly_total`, `lenny_gateway_litellm_egress_anomaly_total`, and `lenny_gateway_litellm_process_restart_total` metrics emitted; `allow-gateway-egress-llm-upstream` NetworkPolicy rendered ([Section 13.2](13_security-model.md#132-network-isolation) NET-046); (12) **Phase 5.8 proxy-mode overhead benchmark (early signal)** — measure per-request overhead of the proxy-mode path vs. direct mode using `streaming-echo` and a mock upstream provider at Tier 2 load (500 concurrent streams). Record the four Go-rewrite-trigger ratios ([Section 4.9](04_system-components.md#49-credential-leasing-service) LiteLLM sidecar hardening table) as a preliminary Tier 2 baseline. Any ratio crossing its threshold here is a leading indicator that Phase 13.5 may trip the full rewrite trigger — flag for attention but do not gate Phase 5.8 on the result. **Limitations at this stage:** single-provider proxy (`anthropic_direct`); no deny-list enforcement (Phase 11); no multi-provider proxy injection beyond `anthropic_direct` (Phase 11). **Prerequisite:** Phase 5.75 (JWT auth + quota enforcement gate) must be complete. | Proxy delivery mode operational; real API keys live only in the LiteLLM sidecar within the gateway pod and never enter agent pods; recommended multi-tenant security baseline (`deliveryMode: proxy` + `isolationProfile: sandboxed`) achievable from this phase onward |
| 6 | Interactive session model (streaming, messages, reconnect with event replay). **Client SDKs (Go + TypeScript/JavaScript)**: official SDK packages ([Section 15.6](15_external-api-surface.md#156-client-sdks)) generated from the OpenAPI spec and MCP tool schemas, with hand-written streaming and reconnect-with-cursor logic layered on top. SDKs ship alongside the Phase 6 interactive model so consumers have a supported client that handles streaming reconnect, file upload multipart handling, webhook signature verification, and error handling with retries from day one. | Full interactive sessions work; official Go and TypeScript SDKs available |
| 6.5   | **Incremental load test — streaming path**: measure performance of the interactive session model under load before proceeding. Key scenarios: (1) concurrent session creation throughput at Tier 1 and Tier 2 scale; (2) streaming reconnect latency (P95, P99) under 500 concurrent sessions; (3) gateway memory and CPU at sustained streaming load; (4) event replay correctness under concurrent reconnect. Results are captured as Phase 6.5 baseline. Any P99 regression against [Section 6.3](06_warm-pod-model.md#63-startup-latency-analysis) latency budgets must be investigated and resolved before Phase 7. These are incremental measurements, not final SLO validation — Phase 13.5 establishes the full pre-hardening baseline. | Streaming path load characteristics understood; regressions caught before policy and delegation layers are added |
| 7 | Policy engine (rate limits, auth, budgets, tenant_id) | Production-grade admission |
| 8 | Checkpoint/resume + artifact seal-and-export | Sessions survive pod failure; artifacts retrievable |
| 9 | `lenny/delegate_task` handles internal and external targets. `lenny/send_message`, `lenny/request_input`, `lenny/get_task_tree`. `lenny/discover_agents` with policy scoping. Multi-turn fully operational. **Delegation-capable test runtime** (`delegation-echo`): a scripted test runtime that executes pre-defined tool call sequences (e.g., `lenny/delegate_task`, `lenny/send_message`), delegates to child sessions, and handles results — enabling end-to-end delegation and recursive delegation testing without a real LLM provider. Ships alongside the echo runtime as a built-in test fixture. **`DelegationPolicy` enforcement gate (prerequisite for merging):** Phase 9 is the first phase where `lenny/delegate_task` is exercised and `DelegationPolicyEvaluator` fires on live delegation calls. The Phase 9 test suite must include integration tests validating enforcement of `DelegationPolicy` resources registered from Phase 3 onward. Required coverage: (1) a delegation attempt that satisfies an allow rule in a pre-registered policy succeeds; (2) a delegation attempt that violates a deny rule (e.g., disallowed runtime tag, exceeded `maxDepth`) is rejected with the expected error code; (3) `lenny/discover_agents` returns only targets authorized by the calling session's effective policy; (4) budget propagation (`maxTokenBudget`, `maxTreeSize`) is enforced across a parent→child chain. These tests must pass using the `delegation-echo` runtime (no LLM credentials required) before Phase 9 is considered complete. | Parent → child task flow with multi-turn; delegation testable without LLM credentials; `DelegationPolicy` enforcement verified against policies registered from Phase 3 |
| 9.1   | **Targeted security design review — delegation attack surface**: review the delegation design (Phase 9) for: unauthorized cross-tenant delegation, parent-forging attacks, delegation loop / depth exhaustion, and `lenny/discover_agents` scope leakage. This is a focused design review, not a pentest. Any architectural findings must be resolved before Phase 10 proceeds. | Delegation attack surface reviewed; blocking findings resolved |
| 9.5   | **Incremental load test — delegation path**: measure delegation chain performance under load before adding recursive MCP delegation and advanced credential complexity. Key scenarios: (1) delegation fan-out throughput: N parallel `lenny/delegate_task` calls from a single parent session (N = 10, 50); (2) delegation depth throughput: chains of depth 3, 5, 10 measured end-to-end; (3) `lenny/discover_agents` latency under 200 concurrent sessions; (4) delegation budget propagation latency under concurrent extension requests. Results are captured as Phase 9.5 baseline. Any delegation path regression must be resolved before Phase 10. | Delegation path load characteristics understood; fan-out and depth limits empirically measured before recursive MCP layer |
| 10 | `agentInterface` in discovery. Adapter manifest includes summaries. MCP fabric (virtual child interfaces, elicitation chain with provenance). | Recursive delegation with MCP semantics |
| 11 | **Advanced credential leasing**: multi-provider implementations (Bedrock STS, Vertex AI, Azure), credential rotation (`RotateCredentials` RPC), fallback chains with cooldown, user-scoped credentials (pre-authorized registration via `POST /v1/credentials` and resolution at session creation time), credential health scoring, **LLM proxy multi-provider credential injection** (extends Phase 5.8's `anthropic_direct`-only proxy to all providers: Bedrock STS, Vertex AI, Azure), `credentialPolicy` with source preference logic. **Emergency credential revocation**: revocation admin endpoint, credential deny list propagation via Redis pub/sub, **LLM proxy deny-list enforcement** (builds on Phase 5.8 LLM Proxy subsystem — [Section 4.9](04_system-components.md#49-credential-leasing-service)). Note: the pre-authorized credential flow mechanics (endpoints, scoping, resolution, security treatment) are fully specified in [Section 4.9](04_system-components.md#49-credential-leasing-service); this phase implements them. | Full credential lifecycle with rotation, fallback, user-scoped keys, emergency revocation, and multi-provider LLM proxy coverage |
| 11.5  | **Incremental load test — credential lifecycle path**: measure credential rotation and fallback performance under load before adding Token Service KMS hardening and concurrent execution modes. Key scenarios: (1) credential rotation (`RotateCredentials`) latency under 200 concurrent sessions with active leases; (2) fallback chain activation latency when primary provider is degraded; (3) emergency credential revocation propagation time (deny-list Redis pub/sub) to all active gateway replicas; (4) user-scoped credential registration and resolution throughput (`POST /v1/credentials` latency under concurrent load; session creation with `preferredSource: user` latency). Results are captured as Phase 11.5 baseline. Any credential-path regression must be resolved before Phase 12a. | Credential lifecycle load characteristics understood; rotation and revocation latency baselines captured before KMS hardening |

> **Note — Phases 12a / 12b / 12c parallelism:** These three phases are independent of each other and may proceed in parallel once Phase 11.5 is complete. Phase 12a depends only on Phase 5.5 (Token Service); Phase 12b depends on Phase 5 (`ExternalAdapterRegistry`), Phase 5.5 (credential assignment), and core session infrastructure; Phase 12c depends only on Phase 5.5 and Phase 4 (session initialization). There is no ordering constraint between 12a, 12b, and 12c — they are numbered for reference, not to imply sequencing.

| Phase | Components | Milestone |
| ----- | ---------- | --------- |
| 12a | **Token Service hardening** (builds on Phase 5.5 Basic Token Service): application-layer KMS envelope encryption for stored OAuth tokens (the multi-replica HA deployment and PodDisruptionBudget were established in Phase 5.5), OAuth flows for external tool connectors (GitHub, Jira, etc.), full credential pool management, per-user OAuth token storage. Phase 5.5's K8s-Secrets-backed service is promoted to the full production-grade deployment described in [Section 4.3](04_system-components.md#43-token-service) with KMS-backed token storage. (`RotateCredentials` RPC is delivered in Phase 11; this phase hardens the underlying Token Service storage and adds OAuth flows.) | Token Service production-hardened: KMS-backed token storage, OAuth connector auth operational |
| 12b | `type: mcp` runtime support. MCP runtime endpoints, lifecycle management, and discovery integration. **Integration test gate (prerequisite for merging):** the Phase 12b implementation must include an end-to-end integration test suite covering: (1) MCP runtime session creation and credential assignment from Phase 5.5 Token Service; (2) `type: mcp` runtime lifecycle (start, idle, claim, release) exercised against the credential-injection path; (3) session initialization against an MCP runtime with valid credentials, confirming no cross-tenant credential leakage. These tests must pass in CI before the Phase 12b branch is merged. | External MCP runtimes operational; credential assignment and session initialization verified by integration tests |
| 12c | Concurrent execution modes: both `concurrencyStyle: workspace` (with `slotId` multiplexing) and `concurrencyStyle: stateless` (with tenant-affinity Service routing). **Integration test gate (prerequisite for merging):** the Phase 12c implementation must include an end-to-end integration test suite covering: (1) concurrent workspace multiplexing via `slotId` exercised against the credential-assignment path (Phase 5.5 Token Service) and session initialization path (Phase 4); (2) credential isolation between concurrent slots — verify no slot receives another slot's credentials; (3) concurrent-mode session initialization for all runtime types (`agent`, `task`, `mcp`); (4) concurrent-stateless tenant-affinity routing — verify that requests for the same tenant are routed to tenant-pinned pods and that cross-tenant routing is rejected. These tests must pass in CI before the Phase 12c branch is merged. | Both concurrent modes (workspace and stateless) available for all runtime types; credential assignment, session initialization, and tenant-affinity routing verified by integration tests |
| 13 | **Full observability stack**: **complete audit logging** (Postgres audit tables with append-only grants, hash-chain integrity, SIEM connectivity, compliance profile enforcement — [Section 11.7](11_policy-and-controls.md#117-audit-logging); note: Phase 7 ships basic policy-decision audit events as part of the policy engine, but the durable audit storage, integrity controls, and SIEM pipeline described in [Section 11.7](11_policy-and-controls.md#117-audit-logging) are introduced here), OpenTelemetry metrics and dashboards, distributed tracing visualization, alerting rules, SLO monitoring. Builds on structured logging and trace propagation established in Phase 2.5. **Pre-Phase 13 audit coverage:** Between Phase 7 and Phase 13, policy-decision events (auth denials, quota rejections, rate-limit hits) are emitted via structured logs (Phase 2.5) and are not persisted to durable append-only audit tables. This is an accepted gap: all pre-Phase 13 activity occurs in development/testing environments with no regulated tenants, and the full audit infrastructure (append-only tables, hash chaining, SIEM) is not required before GA readiness begins at Phase 13. | Operational readiness; durable append-only audit trail with integrity controls and optional SIEM pipeline active |
| 13.5 | **Pre-hardening full-system load baseline**: benchmark all documented scaling concerns and SLOs at full-system scale to establish pre-hardening baselines before security hardening. This phase builds on the incremental baselines from Phases 6.5, 9.5, and 11.5 (which caught path-specific regressions early). Key scenarios: (1) pod claim + workspace materialization latency under concurrent session creation (target: P99 from [Section 6.3](06_warm-pod-model.md#63-startup-latency-analysis)), (2) checkpoint duration across workspace sizes vs < 2s SLO for ≤ 100MB ([Section 4.4](04_system-components.md#44-event--checkpoint-store)), (3) gateway horizontal scaling to 10,000 concurrent sessions ([Section 16.5](16_observability.md#165-alerting-rules-and-slos)), (4) pool scaling controller responsiveness under burst demand, (5) delegation chain depth and fan-out throughput ([Section 8](08_recursive-delegation.md)), (6) streaming reconnect under load ([Section 6](06_warm-pod-model.md)), (7) credential rotation latency during active sessions ([Section 11](11_policy-and-controls.md)), (8) **Lenny-specific Postgres write-pattern benchmark (SCL-040):** measure actual sustained and burst write IOPS under the Lenny workload mix at Tier 3 scale, explicitly capturing: (a) RLS trigger overhead — measure `pg_stat_user_functions` execution counts and duration for the `lenny_tenant_guard` trigger under peak session throughput to quantify per-write overhead not captured by generic WAL benchmarks; (b) quota flush burst patterns — drive the quota flush path (Redis→Postgres periodic sync, [Section 10.1](10_gateway-internals.md#101-horizontal-scaling)) at its configured flush interval under 10,000 concurrent sessions and record the instantaneous IOPS spike relative to the steady-state ~100/s estimate; (c) compare measured sustained and burst IOPS against the [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements) ceiling table and update `postgres.writeCeilingIops` in the Helm defaults if the empirical ceiling differs by more than 10% from the estimate. The `PostgresWriteBurstIops` alert threshold ([Section 16.5](16_observability.md#165-alerting-rules-and-slos)) must be calibrated against the measured burst pattern. (9) **LiteLLM sidecar footprint at Tier 3 (Go-rewrite trigger evaluation)** — drive 5,000 concurrent proxy-mode streams against a gateway pod hosting the LiteLLM sidecar and measure the four ratios defined in [Section 4.9](04_system-components.md#49-credential-leasing-service) LiteLLM sidecar hardening ("Scalability and the Go-rewrite trigger"): (a) LiteLLM sustained CPU as a fraction of gateway-pod total CPU; (b) LiteLLM RSS as a fraction of gateway-pod total RSS; (c) LiteLLM processing time as a fraction of `lenny_gateway_llm_proxy_request_duration_seconds` P95 (excluding upstream provider's own latency); (d) `maxSessionsPerReplica` delta between proxy mode (all pools `deliveryMode: proxy`) and direct mode (all pools `deliveryMode: direct`) on identical hardware. Record each as a per-replica observation. Compare against the Phase 5.8 early-signal baseline. **Rewrite trigger evaluation:** if any two of the four ratios cross their [Section 4.9](04_system-components.md#49-credential-leasing-service) thresholds, the Phase 13.5 exit-gate ADR MUST record the trigger fired and include the measured values; the platform team then scopes the native Go translator replacement (wire contract unchanged, sidecar substituted for a Go implementation) as a post-GA item. If fewer than two ratios cross, the sidecar model stays and the measured values become the authoritative baseline for future regression checks. Compare against Phase 6.5/9.5/11.5 incremental baselines to validate no regression was introduced during Phases 10–13. Produces full capacity planning baselines and identifies remaining bottlenecks. Results are pre-hardening measurements, not final SLO validation — Phase 14.5 re-validates after security hardening. | Pre-hardening load baseline established; bottlenecks identified; capacity plan documented; incremental baselines confirmed at full-system scale; Lenny-specific Postgres write-pattern benchmark completed and [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements) ceiling table updated with empirical values; LiteLLM Go-rewrite trigger evaluated and recorded in the Phase 13.5 exit-gate ADR |
| 14 | **Comprehensive security hardening**: image signing and admission enforcement, advanced NetworkPolicy refinement (per-runtime egress rules), seccomp profile tuning, pod-level security context hardening, comprehensive security audit and penetration testing. Basic network isolation and gVisor enforcement are already in place since Phase 3.5; targeted design reviews of the credential injection and delegation surfaces were completed at Phases 5.6 and 9.1. This phase is the full end-to-end security audit covering the complete platform. | Production-grade security posture validated by audit |
| 14.5 | **Post-hardening SLO re-validation**: re-run Phase 13.5 load test scenarios with all security hardening in place (image signing admission, advanced NetworkPolicy, seccomp profiles, hardened security contexts). Compare results against Phase 13.5 baselines to quantify security overhead. All SLOs from [Section 16.5](16_observability.md#165-alerting-rules-and-slos) must hold at sustained Tier 2 load with full security controls active. Any SLO regression triggers investigation and resolution before GA. This is the final SLO validation gate referenced by [Section 16.5](16_observability.md#165-alerting-rules-and-slos)'s benchmark requirement. | All scaling SLOs validated under load with full security hardening; delta from Phase 13.5 baseline documented |
| 15 | Environment resource: tag-based selectors, member RBAC, `mcpRuntimeFilters` with capability model, `connectorSelector`, cross-environment delegation enforcement, billing rollup endpoint, membership analytics endpoints, explicit environment endpoints across all adapters. | Full RBAC and environment support |
| 16 | Experiment primitives, PoolScalingController experiment integration. | A/B testing infrastructure |
| 16.5 | **Experiment load test — SLO re-validation under experiment workloads**: re-run the Phase 14.5 SLO scenarios (pool claim latency, gateway horizontal scaling, pool scaling controller responsiveness) with at least one active A/B experiment routing 20% of traffic to a variant pool. The goal is to verify that PoolScalingController experiment integration — variant pool sizing, base pool `minWarm` adjustment, and experiment status transition handling — does not regress any SLO from [Section 16.5](16_observability.md#165-alerting-rules-and-slos) that was validated at Phase 14.5. Key scenarios: (1) session-creation latency P99 with one active variant at 20% weight under sustained Tier 2 load; (2) base pool `minWarm` adjustment correctness and pool fill timing when an experiment becomes active mid-load; (3) `PoolScalingController` reconciliation latency under concurrent experiment status transitions (`active → paused`, `paused → active`). Any SLO regression relative to Phase 14.5 baselines must be resolved before Phase 17a. Deployments that never define any `ExperimentDefinition` resources are unaffected and may treat the Phase 14.5 gate as sufficient. | All SLOs from [Section 16.5](16_observability.md#165-alerting-rules-and-slos) confirmed valid under experiment-active workloads; Phase 14.5 SLO baseline remains authoritative for non-experiment deployments |
| 17a | **Documentation, governance, and community launch**: production-grade docker-compose, `CONTRIBUTING.md` and `GOVERNANCE.md` review and finalization, operator runbook review, comparison guides (Lenny vs E2B, Daytona, Fly.io Sprites, Temporal, Modal, LangGraph), community communication channels activated, open-source license confirmed in all repository artifacts (LICENSE, NOTICE, package manifests). This phase gates community launch — no external contributor PR solicitation before 17a completes. | Documentation complete; governance finalized; project publicly announced for community contributions |
| 17b | **Advanced platform features**: Memory (`MemoryStore` + platform tools), semantic caching, guardrail interceptor hooks, eval hooks. These features are independent of documentation and can proceed in parallel with or after Phase 17a. | Full feature set including memory, semantic caching, and extensibility hooks |

> **Open-source readiness:** Lenny is designed as an open-source project. The open-source license is selected in Phase 0 (ADR-008, see [Section 23.2](23_competitive-landscape.md#232-community-adoption-strategy)) and committed to the repository before any external contributor engagement. Contribution guidelines (`CONTRIBUTING.md`), governance model (BDfN transitioning to steering committee), and community communication channels will be established as part of Phase 2 (alongside the `make run` quick-start). The Phase 2 milestone includes a < 5-minute Time to Hello World (TTHW) target validated by CI. See [Section 23.2](23_competitive-landscape.md#232-community-adoption-strategy) for the full community adoption strategy including target personas and governance details. The technical design prioritizes community extensibility through pluggable credential providers, a published runtime adapter contract, and a clear SDK boundary.

### 18.1 Build Artifacts Introduced by Section 25

[Section 25](25_agent-operability.md) adds the following build-pipeline requirements. They are layered into the existing phases above; this index makes them discoverable from the build sequence.

**New container images** (produced on every release):

- `lenny-ops` — binary of the operability control plane ([§25.4](25_agent-operability.md#254-the-lenny-ops-service)).
- `lenny-backup` — binary used by transient backup / restore / verify Jobs scheduled by `lenny-ops` ([§25.11](25_agent-operability.md#2511-backup-and-restore-api)).

The existing gateway, controller, and token-service images gain small additions: new operability endpoints ([§25.3](25_agent-operability.md#253-gateway-side-ops-endpoints)) and `EventEmitter` wiring for subsystems that emit operational events ([§25.3](25_agent-operability.md#253-gateway-side-ops-endpoints) Event Emission).

**OpenAPI and MCP tool generation.** Phase 5 already introduces the OpenAPI → MCP schema generation step in the build pipeline. Section 25 extends the contract: every admin-API endpoint MUST carry the `x-lenny-mcp-tool`, `x-lenny-scope`, `x-lenny-required-role`, `x-lenny-category`, `x-lenny-idempotency-key`, `x-lenny-dry-run-support`, and `x-lenny-guards` extensions. A CI step fails the build if any endpoint lacks these, if any `x-lenny-scope` value is outside the documented `tools:<domain>:<action>` taxonomy ([§15.1](15_external-api-surface.md#151-rest-api)), or if the MCP tool inventory (§25.12) is out of sync with the generated OpenAPI.

**Alerting-rule artifacts.** `docs/alerting/rules.yaml` is generated from the shared `pkg/alerting/rules` Go package and committed on each release. The Helm chart's `PrometheusRule` / ConfigMap templates consume the same package output; the chart build must embed or reference the generated rules so deployer-visible manifests and the in-process compiled rules never diverge ([§16.5](16_observability.md#165-alerting-rules-and-slos), [§25.13](25_agent-operability.md#2513-bundled-alerting-rules)).

**Release-channel signing.** Release metadata is Ed25519-signed by the build pipeline ([§25.8](25_agent-operability.md#258-platform-lifecycle-management) Release Channel). The `lenny-ops` upgrade-check verifier rejects unsigned or mismatched-signature releases; the signing key must be available to the release automation. Mirroring deployers can override the verifying key via `platform.releaseChannel.publicKeyPath`.

**Pre-GA ordering.** The new images (`lenny-ops`, `lenny-backup`), the shared `pkg/alerting/rules` and `pkg/recommendations/rules` packages, and the `pkg/common/registry/resolver.go` `ImageResolver` are prerequisites for the Phase 17a community-launch documentation pass — operability must be complete before external deployers are invited. Image signing (Phase 14) is likewise a prerequisite for `lenny-ops` upgrade-check signature verification.

