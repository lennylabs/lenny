## 16. Observability

### 16.1 Metrics

| Metric                                                                                                                                                                                                                 | Type            |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| Active sessions (by runtime, pool, state, tenant)                                                                                                                                                                      | Gauge           |
| Warm pods available (`lenny_warmpool_idle_pods`, gauge labeled by `pool` — number of pods in `idle` state ready to be claimed; used by `WarmPoolLow`, `WarmPoolExhausted`, and `PodClaimQueueSaturated` alerts; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Gauge           |
| Stale warm pods (idle beyond threshold, by pool)                                                                                                                                                                       | Gauge           |
| Task-mode per-pod scrub failure count (`lenny_task_pod_scrub_failure_count`, per pod, labeled by `pod_name`, `pool`, `runtime_class`)                                                                                  | Gauge           |
| Task-mode pod retirement (`lenny_task_pod_retirement_total`, labeled by `reason`: `task_count_limit`, `uptime_limit`, `scrub_failure_limit`, `pool`, `runtime_class`)                                                  | Counter         |
| Concurrent-workspace slot failure count (`lenny_slot_failure_total`, labeled by `reason`, `pool`, `pod_name`)                                                                                                          | Counter         |
| Concurrent-workspace slot pod replacement (`lenny_slot_pod_replacement_total`, labeled by `pool`, `pod_name`)                                                                                                          | Counter         |
| End-to-end session startup duration (`lenny_session_startup_duration_seconds`, histogram labeled by `pool`, `runtime_class`, `isolation_profile` — measures wall-clock time from pod claim to session ready, excluding file upload; used by `StartupLatencyBurnRate` and `StartupLatencyGVisorBurnRate` burn-rate alerts; see [Section 16.5](#165-alerting-rules-and-slos)) | Histogram       |
| End-to-end time to first token (`lenny_session_time_to_first_token_seconds`, histogram labeled by `pool`, `runtime_class` — measures wall-clock time from session start request to first streaming event emitted to the client; used by `TTFTBurnRate` burn-rate alert; see [Section 16.5](#165-alerting-rules-and-slos)) | Histogram       |
| Session creation latency (phases)                                                                                                                                                                                      | Histogram       |
| Time-to-claim (session request to pod claimed)                                                                                                                                                                         | Histogram       |
| Pod state transition durations (per state)                                                                                                                                                                             | Histogram       |
| Upload bytes/second and queue depth                                                                                                                                                                                    | Counter + Gauge |
| Upload extraction aborted (`lenny_upload_extraction_aborted_total`, labeled by `reason`: `zip_bomb`, `size_limit`, `path_traversal`, `symlink`, `format_error`)                                                        | Counter         |
| Token usage (by user, runtime, tenant)                                                                                                                                                                                 | Counter         |
| Retry count (by failure classification)                                                                                                                                                                                | Counter         |
| Resume success/failure rate                                                                                                                                                                                            | Counter         |
| Delegation depth distribution                                                                                                                                                                                          | Histogram       |
| Delegation tree size distribution                                                                                                                                                                                      | Histogram       |
| Gateway replica count                                                                                                                                                                                                  | Gauge           |
| Gateway active streams (per replica)                                                                                                                                                                                   | Gauge           |
| Gateway rejection rate (`lenny_gateway_rejection_rate`, per replica — requests rejected with 429/503 per second; used as a leading HPA scale-out indicator alongside `request_queue_depth`, see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))           | Gauge           |
| Policy denials (by reason, tenant)                                                                                                                                                                                     | Counter         |
| Checkpoint size and duration                                                                                                                                                                                           | Histogram       |
| Stale checkpoint session count (`lenny_checkpoint_stale_sessions`, gauge labeled by `pool`, `tier` — number of active sessions whose last checkpoint age exceeds `periodicCheckpointIntervalSeconds`; replaces per-session `session_id` labeling to avoid unbounded Prometheus cardinality, consistent with the `root_session_id` exclusion principle in [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease); per-session checkpoint age is available via structured logs and the admin API) | Gauge           |
| CheckpointBarrier ack outcomes (`lenny_checkpoint_barrier_ack_total`, counter labeled by `pool`, `outcome`: `success`, `timeout`, `error` — tracks per-pod barrier ack results; the `timeout` outcome corresponds to the existing `lenny_checkpoint_barrier_ack_timeout_total` inline metric in [Section 10.1](10_gateway-internals.md#101-horizontal-scaling); `success` enables computing barrier success rate) | Counter         |
| CheckpointBarrier ack duration (`lenny_checkpoint_barrier_ack_duration_seconds`, histogram labeled by `pool` — wall-clock time from `CheckpointBarrier` send to `CheckpointBarrierAck` receipt per pod; measures how much of the `terminationGracePeriodSeconds` budget is consumed by checkpoint flushing during rolling updates) | Histogram       |
| Checkpoint storage fallback to Postgres minimal state (`lenny_checkpoint_eviction_fallback_total`, labeled by `pool`, `had_prior_checkpoint`)                                                                          | Counter         |
| Postgres connection pool utilization (per replica)                                                                                                                                                                     | Gauge           |
| Redis memory usage and eviction rate                                                                                                                                                                                   | Gauge + Counter |
| mTLS handshake latency (gateway-to-pod)                                                                                                                                                                                | Histogram       |
| Credential lease assignments (by provider, pool, source)                                                                                                                                                               | Counter         |
| Credential rotations (by reason: rate_limit, auth_expired, provider_unavailable)                                                                                                                                       | Counter         |
| Credential pool utilization (active leases / total credentials, by pool)                                                                                                                                               | Gauge           |
| Credential pool health (credentials in cooldown, by pool)                                                                                                                                                              | Gauge           |
| Credential lease duration                                                                                                                                                                                              | Histogram       |
| Credential pre-claim mismatch (check passed, assignment failed; by pool, provider)                                                                                                                                     | Counter         |
| Elicitation round-trip latency (`lenny_elicitation_roundtrip_seconds`)                                                                                                                                                 | Histogram       |
| Elicitation requests pending (`lenny_elicitation_pending`)                                                                                                                                                             | Gauge           |
| Elicitation requests suppressed (`lenny_elicitation_suppressed_total`)                                                                                                                                                 | Counter         |
| Elicitation requests timed out (`lenny_elicitation_timeout_total`)                                                                                                                                                     | Counter         |
| Delegation budget utilization ratio (`lenny_delegation_budget_utilization_ratio`)                                                                                                                                      | Gauge           |
| Delegation lease extensions (`lenny_delegation_lease_extension_total`)                                                                                                                                                 | Counter         |
| Delegation tree token usage (`lenny_delegation_tree_token_usage_total`)                                                                                                                                                | Counter         |
| Delegation budget reconstruction events (`lenny_delegation_budget_reconstruction_total`, by `outcome`: `success`, `irrecoverable`)                                                                                     | Counter         |
| Delegation tree memory bytes (`lenny_delegation_tree_memory_bytes`, gauge labeled by `pool`, `tenant_id` — aggregated across active trees; per-tree values available via structured logs. Tracks cumulative in-memory footprint against `maxTreeMemoryBytes`; see [Section 8.2](08_recursive-delegation.md#82-delegation-mechanism)) | Gauge           |
| Delegation memory budget utilization ratio (`lenny_delegation_memory_budget_utilization_ratio`, histogram labeled by `pool`, `tenant_id` — sampled at tree completion; `current_tree_memory_bytes / maxTreeMemoryBytes`) | Histogram       |
| Delegation tree memory rejections (`lenny_delegation_tree_memory_rejection_total`, counter, labeled by `pool`, `tenant_id`, `reason: memory_budget_exhausted` — increments each time a `delegate_task` call is rejected because adding the new node would exceed `maxTreeMemoryBytes`) | Counter         |
| Delegation Lua script duration (`lenny_redis_lua_script_duration_seconds`, histogram labeled by `script` — tracks Redis Lua execution latency for `budget_reserve` and `budget_return` scripts; P99 > 5 ms indicates contention; see [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease)) | Histogram       |
| Delegation parallel children high-watermark (`lenny_delegation_parallel_children_high_watermark`, histogram labeled by `pool`, `tenant_id` — maximum simultaneous in-flight children per delegation tree at tree completion; used to detect fan-out approaching `maxParallelChildren` ceiling; see [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease)) | Histogram       |
| Delegation deadlock detections (`lenny_delegation_deadlock_detected_total`, counter labeled by `pool` — increments each time the gateway identifies a deadlocked subtree; see [Section 8.8](08_recursive-delegation.md#88-taskrecord-and-taskresult-schema)) | Counter         |
| Delegation deadlock resolutions (`lenny_delegation_deadlock_resolution_total`, counter labeled by `pool`, `resolution`: `client_input`, `child_cancel`, `timeout` — tracks how deadlocks are resolved; `timeout` corresponds to `DEADLOCK_TIMEOUT` error code; see [Section 8.8](08_recursive-delegation.md#88-taskrecord-and-taskresult-schema)) | Counter         |
| Delegation deadlock duration (`lenny_delegation_deadlock_duration_seconds`, histogram labeled by `pool` — time from deadlock detection to resolution; enables validating `maxDeadlockWaitSeconds` tuning; see [Section 8.8](08_recursive-delegation.md#88-taskrecord-and-taskresult-schema)) | Histogram       |
| Delegation budget return usage lag (`lenny_delegation_budget_return_usage_lag_total`, counter — increments when a budget return cannot read the parent's actual usage counter; indicates a degraded budget accounting path; see [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease)) | Counter         |
| Delegation budget keys expired (`lenny_delegation_budget_keys_expired_total`, counter labeled by `pool`, `tenant_id` — increments when `budget_reserve.lua` or `budget_return.lua` returns `BUDGET_KEYS_EXPIRED`; triggers `DelegationBudgetKeysExpired` alert; see [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease)) | Counter         |
| Checkpoint storage bytes (`lenny_checkpoint_storage_bytes_total`, labeled by `tenant_id`, `pool`)                                                                                                                      | Gauge           |
| Pod claim queue depth (`lenny_pod_claim_queue_depth`, by pool)                                                                                                                                                         | Gauge           |
| Pod claim queue wait time (`lenny_pod_claim_queue_wait_seconds`, by pool)                                                                                                                                              | Histogram       |
| Pod claim optimistic-lock conflicts (`lenny_pod_claim_conflict_total`, by pool)                                                                                                                                        | Counter         |
| Pod claim timeouts (`lenny_pod_claim_timeout_total`, by pool)                                                                                                                                                          | Counter         |
| **Token Service**                                                                                                                                                                                                      |                 |
| Token Service request duration (`lenny_token_service_request_duration_seconds`, by operation: `assign`, `rotate`, `refresh`)                                                                                           | Histogram       |
| Token Service errors (`lenny_token_service_errors_total`, by error type)                                                                                                                                               | Counter         |
| Token Service circuit breaker state (`lenny_token_service_circuit_state`, 0=closed, 1=half-open, 2=open)                                                                                                               | Gauge           |
| Token Service secret reloads (`lenny_token_service_secret_reloads_total`, by secret name, outcome: `success`, `not_found`, `parse_error`)                                                                              | Counter         |
| **Gateway Subsystems**                                                                                                                                                                                                 |                 |
| Per-subsystem request duration (`lenny_gateway_{subsystem}_request_duration_seconds`, where `{subsystem}` ∈ {`stream_proxy`, `upload_handler`, `mcp_fabric`, `llm_proxy`})                                             | Histogram       |
| Per-subsystem errors (`lenny_gateway_{subsystem}_errors_total`, by error type)                                                                                                                                         | Counter         |
| Per-subsystem queue depth (`lenny_gateway_{subsystem}_queue_depth`)                                                                                                                                                    | Gauge           |
| Per-subsystem circuit breaker state (`lenny_gateway_{subsystem}_circuit_state`, 0=closed, 1=half-open, 2=open)                                                                                                         | Gauge           |
| LLM Proxy active connections (`lenny_gateway_llm_proxy_active_connections`)                                                                                                                                            | Gauge           |
| GC pause P99 per replica (`lenny_gateway_gc_pause_p99_ms`, labeled by `replica_id`) — process-level GC pause; sustained >50 ms signals shared-process boundary pressure (see [§4.1](04_system-components.md#41-edge-gateway-replicas) Shared-process GC pressure signal) | Gauge           |
| Fleet-wide GC pressure indicator (`lenny_gateway_gc_pause_fleet_p99_ms`) — the 99th-percentile GC pause aggregated across all active gateway replicas (computed as `max(lenny_gateway_gc_pause_p99_ms)` over all replica instances); used as the Tier 3 aggregate health indicator for the extraction decision and the `Tier3GCPressureHigh` alert below | Gauge           |
| **Warm Pool Replenishment**                                                                                                                                                                                            |                 |
| Pod startup duration (`lenny_warmpool_pod_startup_duration_seconds`, by pool, by isolation profile — time from pod creation to `idle` state)                                                                           | Histogram       |
| Replenishment rate (`lenny_warmpool_replenishment_rate`, pods/min entering `idle` state, by pool)                                                                                                                      | Gauge           |
| Warmup failures (`lenny_warmpool_warmup_failure_total`, counter labeled by `pool`, `runtime_class`, `reason`: `image_pull_error`, `setup_command_failed`, `resource_quota_exceeded`, `node_pressure` — increments each time a pod fails to reach `idle` state during warm-up; sustained rate triggers `WarmPoolReplenishmentFailing` alert; primary diagnosis signal for the warm pool exhaustion runbook; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Counter         |
| Cold-start sessions served (`lenny_warmpool_cold_start_total`, by pool — increments when a session is served from a cold pod in a `minWarm: 0` pool or when the warm pool is exhausted and a pod is created on demand) | Counter         |
| Cold-start pool fill duration (`lenny_warmpool_fill_duration_seconds`, by pool — time from pool creation to reaching `minWarm` ready pods; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle))                                                          | Histogram       |
| Warm pod claims (`lenny_warmpool_claims_total`, labeled by `pool`, `runtime_class` — increments each time a warm pod transitions from `idle` to `claimed`; used to compute the SDK-warm demotion rate when divided by `lenny_warmpool_sdk_demotions_total`) | Counter         |
| SDK-warm demotions (`lenny_warmpool_sdk_demotions_total`, labeled by `pool`, `runtime_class` — increments each time an SDK-warm pod is demoted to pod-warm state before session assignment, typically because the session uploads files matching `sdkWarmBlockingPaths`; see [Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like)) | Counter         |
| Task-mode pod reuse count (`lenny_task_reuse_count`, labeled by `pool`, `pod_name` — number of tasks executed on a single pod in task mode; used to track recycling efficiency and enforce `maxTasksPerPod` retirement. Observed per-pod at task completion; the PoolScalingController uses `histogram_quantile(0.50, ...)` to derive `mode_factor` for task-mode pools — see [Section 5.2](05_runtime-registry-and-pool-model.md#52-pool-configuration-and-execution-modes)) | Histogram       |
| **Pool Config Reconciliation**                                                                                                                                                                                         |                 |
| Pool config reconciliation lag (`lenny_pool_config_reconciliation_lag_seconds`, by pool — time since last successful CRD reconciliation from Postgres; see [Section 4.6.2](04_system-components.md#462-poolscalingcontroller-pool-configuration))                                              | Gauge           |
| Pool bootstrap mode (`lenny_pool_bootstrap_mode`, gauge per pool, `1` = bootstrap active, `0` = converged to formula-driven scaling; used by `PoolBootstrapMode` alert; see [Section 17.8.2](17_deployment-topology.md#1782-capacity-tier-reference))                           | Gauge           |
| **Warm Pool Controller**                                                                                                                                                                                               |                 |
| SandboxClaim admission rejections (`lenny_sandboxclaim_guard_rejections_total`, counter labeled by `replica_id` — admission webhook rejections of double-claim attempts; non-zero rate during normal operation indicates a claim-path bug; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Counter         |
| Warm pool idle pod-minutes (`lenny_warmpool_idle_pod_minutes`, counter labeled by `pool`, `resource_class` — cumulative idle pod-minutes; used to estimate warm pool cost and trigger `WarmPoolIdleCostHigh` alert; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Counter         |
| Pod claim Postgres fallback activations (`lenny_pod_claim_fallback_total`, counter — increments each time the Postgres-backed fallback claim path is used; sustained non-zero rate indicates primary API-server claim path is degraded; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Counter         |
| PodRegistry operation duration (`lenny_pod_registry_operation_duration_seconds`, labeled by `operation`: `get`, `update_state`, `claim`, `release`, `list`, `count`, `create`, `delete`, `watch`, and `pool` — per-operation latency for the underlying storage primitive; see [Section 12.6](12_storage-architecture.md#126-interface-design) PodRegistry observability contract) | Histogram       |
| PodRegistry errors (`lenny_pod_registry_error_total`, labeled by `operation`, `pool` — per-operation error count; see [Section 12.6](12_storage-architecture.md#126-interface-design)) | Counter         |
| PodRegistry watch lag (`lenny_pod_registry_watch_lag_seconds`, labeled by `pool`, `implementation` — delay between `updated_at` on the pod state row and event delivery to the `WatchPods` channel; required for all implementations; see [Section 12.6](12_storage-architecture.md#126-interface-design) `WatchPods` delivery contract) | Gauge           |
| Controller leader lease renewal age (`lenny_controller_leader_lease_renewal_age_seconds`, gauge per controller — seconds since the current leader last renewed its Lease; triggers `ControllerLeaderElectionFailed` critical alert when age exceeds `leaseDuration` (15s); see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Gauge           |
| Controller work queue overflow (`lenny_controller_queue_overflow_total`, counter — increments each time a reconciliation event is dropped because the work queue exceeds its maximum depth (default: 500); see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Counter         |
| Orphaned SandboxClaim deletions (`lenny_orphaned_claims_total`, counter labeled by `pool` — increments for each orphaned SandboxClaim deleted by the GarbageCollect loop; triggers `SandboxClaimOrphanRateHigh` alert; see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) | Counter         |
| **Delegation Tree Recovery**                                                                                                                                                                                           |                 |
| Tree recovery duration (`lenny_delegation_tree_recovery_duration_seconds`, histogram labeled by `pool`, `outcome`: `full_success`, `partial_failure`, `total_timeout` — wall-clock duration per tree recovery operation; enables validating `maxTreeRecoverySeconds` tuning against actual recovery times; see [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery)) | Histogram       |
| Tree recovery timeout type (`lenny_delegation_tree_recovery_timeout_total`, counter labeled by `pool`, `timeout_type`: `level`, `tree` — distinguishes per-level timeouts from total-tree timeouts; see [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery))   | Counter         |
| Orphan cleanup job runs (`lenny_orphan_cleanup_runs_total`, counter — total background orphan cleanup job executions; see [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery))                                                                                 | Counter         |
| Orphan tasks terminated (`lenny_orphan_tasks_terminated`, counter — number of orphan tasks terminated by the cleanup job; see [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery))                                                                            | Counter         |
| Orphan tasks active (`lenny_orphan_tasks_active`, gauge — currently active orphan tasks awaiting cleanup; alert when exceeds deployment-specific threshold (suggested: 50); see [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery))                          | Gauge           |
| Orphan tasks active per tenant (`lenny_orphan_tasks_active_per_tenant`, gauge labeled by `tenant_id` — per-tenant active orphan task count; triggers `OrphanTasksPerTenantHigh` alert at 80% of `maxOrphanTasksPerTenant`; see [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery)) | Gauge           |
| **Memory Store**                                                                                                                                                                                                       |                 |
| Memory store operation duration (`lenny_memory_store_operation_duration_seconds`, labeled by `operation`: `write`, `query`, `delete`, `list`, and `backend`: `postgres`, `custom`) | Histogram       |
| Memory store errors (`lenny_memory_store_errors_total`, labeled by `operation`, `backend`, `error_type`) | Counter         |
| Memory store record count (`lenny_memory_store_record_count`, labeled by `tenant_id` — approximate count of stored memory records per tenant; sampled periodically) | Gauge           |
| **Experiment Targeting**                                                                                                                                                                                               |                 |
| Experiment targeting webhook latency (`lenny_experiment_targeting_webhook_duration_seconds`, by `provider`)                                                                                                             | Histogram       |
| Experiment targeting webhook failures (`lenny_experiment_targeting_webhook_error_total`, by `provider`, `error_type`)                                                                                                   | Counter         |
| Experiment targeting circuit breaker state (`lenny_experiment_targeting_circuit_open`, labeled by `tenant_id`, `provider` — `1` when the per-tenant circuit breaker is open, `0` when closed; see [Section 10.7](10_gateway-internals.md#107-experiment-primitives))       | Gauge           |
| Experiment sticky cache invalidations (`lenny_experiment_sticky_cache_invalidations_total`, labeled by `experiment_id`, `transition` — incremented on each flush of sticky user assignments; see [Section 10.7](10_gateway-internals.md#107-experiment-primitives))         | Counter         |
| Session errors by variant (`lenny_session_error_total`, labeled by `tenant_id`, `session_type`, `variant_id` — used in manual rollback trigger thresholds; see [Section 10.7](10_gateway-internals.md#107-experiment-primitives))                                           | Counter         |
| Sessions total by variant (`lenny_session_total`, labeled by `tenant_id`, `session_type`, `variant_id` — denominator for variant error rate in rollback triggers; see [Section 10.7](10_gateway-internals.md#107-experiment-primitives))                                    | Counter         |
| Eval score by variant (`lenny_eval_score`, labeled by `tenant_id`, `scorer`, `variant_id` — each eval run records one observation; Histogram enables mean computation via `rate(lenny_eval_score_sum[w]) / rate(lenny_eval_score_count[w])` for rollback trigger conditions; see [Section 10.7](10_gateway-internals.md#107-experiment-primitives). **Built-in `/eval` endpoint only** — deployers whose runtimes use runtime-native eval platforms will not have data in this metric; see [Section 10.7](10_gateway-internals.md#107-experiment-primitives) rollback triggers footnote) | Histogram       |
| **EventBus**                                                                                                                                                                                                           |                 |
| EventBus publish count (`lenny_event_bus_publish_total`, labeled by `topic` — total publishes per topic; see [Section 12.6](12_storage-architecture.md#126-interface-design) EventBus observability contract) | Counter         |
| EventBus publish duration (`lenny_event_bus_publish_duration_seconds`, labeled by `topic` — time to publish to the underlying transport; see [Section 12.6](12_storage-architecture.md#126-interface-design)) | Histogram       |
| EventBus handler duration (`lenny_event_bus_handler_duration_seconds`, labeled by `topic` — time spent in the caller-supplied handler function, not transport; see [Section 12.6](12_storage-architecture.md#126-interface-design)) | Histogram       |
| EventBus handler errors (`lenny_event_bus_handler_error_total`, labeled by `topic` — handler error count; see [Section 12.6](12_storage-architecture.md#126-interface-design)) | Counter         |
| **StoreRouter**                                                                                                                                                                                                            |                 |
| Scatter-gather duration (`lenny_store_router_scatter_gather_duration_seconds`, labeled by `query_type`: `list_sessions`, `gdpr_erasure`, `tenant_deletion`, `delegation_budget_purge` — wall-clock duration of `AllSessionShards()` scatter-gather operations; triggers `ScatterGatherSlowQuery` alert; see [Section 12.6](12_storage-architecture.md#126-interface-design) scatter-gather execution constraints) | Histogram       |
| Scatter-gather shard count (`lenny_store_router_scatter_gather_shard_count`, labeled by `query_type` — number of shards queried per scatter-gather invocation; see [Section 12.6](12_storage-architecture.md#126-interface-design)) | Gauge           |
| **Billing**                                                                                                                                                                                                            |                 |
| Billing write-ahead buffer utilization (`billing_write_ahead_buffer_utilization`, labeled by `tenant_id`) — ratio of in-memory billing event buffer currently in use to `billingFlushMaxPending` (default: 500). Value 0.0–1.0. Alert before reaching 0.80 (`BillingWriteAheadBufferHigh`). Used as back-pressure signal: when buffer is exhausted, new billable work is rejected. See [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements). | Gauge           |
| Billing Redis stream depth (`lenny_billing_redis_stream_depth`, labeled by `tenant_id`) — number of billing events currently staged in the per-tenant Redis stream (`t:{tenant_id}:billing:stream`) awaiting Postgres flush. Normal value is near zero; elevated values indicate sustained Postgres write failures. See [Section 11.2.1](11_policy-and-controls.md#1121-billing-event-stream). | Gauge           |
| **Coordination & Reconciliation**                                                                                                                                                                                      |                 |
| Dual-store unavailable (`lenny_dual_store_unavailable`, gauge — 1 while both Postgres primary and Redis are simultaneously unreachable; used by `DualStoreUnavailable` critical alert; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))               | Gauge           |
| Coordinator handoff stale (`lenny_coordinator_handoff_stale_total`, counter — increments when a replica receives a generation-stale rejection; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))                                                       | Counter         |
| Orphan session reconciliations (`lenny_orphan_session_reconciliations_total`, counter — increments on each orphan session forcibly transitioned to `failed`; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))                                          | Counter         |
| Adapter coordinator hold (`lenny_adapter_coordinator_hold`, gauge — 1 while the adapter is in hold state awaiting a new coordinator; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))                                                                 | Gauge           |
| Coordinator handoff duration (`lenny_coordinator_handoff_duration_seconds`, histogram labeled by `pool`, `outcome`: `success`, `fenced`, `timeout` — wall-clock time of the 3-step coordinator handoff protocol; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling)) | Histogram       |
| Coordinator fence retries (`lenny_coordinator_fence_retry_total`, counter labeled by `pool` — increments each time a coordinator retries after a fencing rejection during handoff; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))                    | Counter         |
| Coordinator fence relinquished (`lenny_coordinator_fence_relinquished_total`, counter labeled by `pool` — increments when a coordinator relinquishes leadership after failing all fence retries; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling))      | Counter         |
| Runtime upgrade state (`lenny_runtime_upgrade_state`, gauge labeled by `pool`, `state`: `pending`, `expanding`, `draining`, `contracting`, `complete`, `paused` — current state of the 6-state upgrade machine; see [Section 10.5](10_gateway-internals.md#105-upgrade-and-rollback-strategy)) | Gauge           |
| Runtime upgrade phase duration (`lenny_runtime_upgrade_phase_duration_seconds`, gauge labeled by `pool`, `phase` — wall-clock time spent in the current upgrade phase; see [Section 10.5](10_gateway-internals.md#105-upgrade-and-rollback-strategy))                               | Gauge           |
| Runtime upgrade draining sessions (`lenny_runtime_upgrade_draining_sessions`, gauge labeled by `pool` — number of sessions still draining during a runtime upgrade; see [Section 10.5](10_gateway-internals.md#105-upgrade-and-rollback-strategy))                                  | Gauge           |
| **Checkpoint Failure Tracking**                                                                                                                                                                                        |                 |
| Partial manifest cleanup (`lenny_partial_manifest_cleanup_total`, counter labeled by `outcome`: `success`, `failed_deleted`, `gc_collected` — tracks partial checkpoint manifest cleanup disposition; see [Section 4.4](04_system-components.md#44-event--checkpoint-store))  | Counter         |
| Checkpoint orphaned objects (`lenny_checkpoint_orphaned_objects_total`, counter labeled by `pool`, `trigger` — increments when checkpoint abort cleanup fails to delete partial MinIO objects; see [Section 4.4](04_system-components.md#44-event--checkpoint-store))         | Counter         |
| Checkpoint size exceeded (`lenny_checkpoint_size_exceeded_total`, counter labeled by `pool`, `tier` — increments when a pre-checkpoint workspace size probe exceeds `workspaceSizeLimitBytes`; see [Section 4.4](04_system-components.md#44-event--checkpoint-store))         | Counter         |
| Checkpoint storage failure (`lenny_checkpoint_storage_failure_total`, counter labeled by `pool`, `tier`, `trigger` — increments when a non-eviction checkpoint upload fails after all retries; see [Section 4.4](04_system-components.md#44-event--checkpoint-store))         | Counter         |
| Checkpoint eviction partial keys logged (`lenny_checkpoint_eviction_partial_keys_logged_total`, counter labeled by `pool`, `keys_committed`: `0`, `1+` — tracks how often partial MinIO key sets were logged during eviction total-loss path; see [Section 4.4](04_system-components.md#44-event--checkpoint-store)) | Counter         |
| **Suspension Pod Release**                                                                                                                                                                                             |                 |
| Session pod released during suspension (`lenny_session_pod_released_during_suspension_total`, counter labeled by `pool`, `tenant_id` — increments when `maxSuspendedPodHoldSeconds` fires and pod is successfully released; see [Section 6.2](06_warm-pod-model.md#62-pod-state-machine)) | Counter         |
| Suspension checkpoint failed (`lenny_session_suspension_checkpoint_failed_total`, counter labeled by `pool`, `tenant_id` — increments when the checkpoint attempt before pod release fails; pod is held and retry occurs on next 60s interval; see [Section 6.2](06_warm-pod-model.md#62-pod-state-machine)) | Counter         |
| **Erasure & Tenant Lifecycle**                                                                                                                                                                                         |                 |
| Erasure job failures (`lenny_erasure_job_failed_total`, counter labeled by `tenant_id`, `failure_phase` — increments on each erasure job failure; used by `ErasureJobFailed` alert; see [Section 12.8](12_storage-architecture.md#128-compliance-interfaces))                  | Counter         |
| Tenant deletion duration (`lenny_tenant_deletion_duration_seconds`, histogram labeled by `tenant_id` — tracks time from `disabling` to `deleted`; used by `TenantDeletionOverdue` alert; see [Section 12.8](12_storage-architecture.md#128-compliance-interfaces))             | Histogram       |
| KMS key deletion failures (`lenny_kms_key_deletion_failed_total`, counter labeled by `tenant_id` — increments when Phase 4a KMS key deletion fails; used by `KmsKeyDeletionFailed` alert; see [Section 12.8](12_storage-architecture.md#128-compliance-interfaces))            | Counter         |
| Billing correction pending (`lenny_billing_correction_pending_total`, gauge labeled by `state`: `pending`, `approved`, `rejected`, `expired` — tracks correction approval queue; used by `BillingCorrectionApprovalBacklog` alert; see [Section 11.2.1](11_policy-and-controls.md#1121-billing-event-stream)) | Gauge           |
| Storage quota bytes used (`lenny_storage_quota_bytes_used`, gauge labeled by `tenant_id` — per-tenant artifact storage bytes; used by `StorageQuotaHigh` alert; see [Section 11.2](11_policy-and-controls.md#112-budgets-and-quotas))                                      | Gauge           |
| **Pool Drain & Lifecycle**                                                                                                                                                                                             |                 |
| Pool draining sessions (`lenny_pool_draining_sessions_total`, gauge labeled by `pool` — in-flight sessions during pool drain; see [Section 15.1](15_external-api-surface.md#151-rest-api))                                                                        | Gauge           |
| MCP deprecated version active sessions (`lenny_mcp_deprecated_version_active_sessions`, gauge — sessions still active on a deprecated MCP protocol version; emitted by `lenny-preflight` Job; see [Section 15.2](15_external-api-surface.md#152-mcp-api))        | Gauge           |
| **Circuit Breaker (Global)**                                                                                                                                                                                           |                 |
| Circuit breaker open (`lenny_circuit_breaker_open`, gauge labeled by `circuit_name` — 1 when the named breaker is open, 0 when closed; used by `CircuitBreakerActive` alert; see [Section 11.6](11_policy-and-controls.md#116-circuit-breakers))                         | Gauge           |
| **Workspace Seal**                                                                                                                                                                                                     |                 |
| Workspace seal duration (`lenny_workspace_seal_duration_seconds`, histogram labeled by `pool`, `outcome`: `success`, `timeout` — tracks seal-and-export completion time; used by `WorkspaceSealStuck` alert; see [Section 7.1](07_session-lifecycle.md#71-normal-flow)) | Histogram       |
| **Audit Integrity**                                                                                                                                                                                                    |                 |
| Audit grant drift detections (`lenny_audit_grant_drift_total`, counter — increments when unexpected UPDATE/DELETE grants are detected on audit tables; used by `AuditGrantDrift` alert; see [Section 11.7](11_policy-and-controls.md#117-audit-logging))               | Counter         |
| pgaudit grant events forwarded (`lenny_pgaudit_grant_events_total`, counter labeled by `statement_type`: `GRANT`, `REVOKE`, `DDL` — pgaudit-sourced events forwarded to the sink; used by `PgAuditSinkDeliveryFailed` alert; see [Section 11.7](11_policy-and-controls.md#117-audit-logging)) | Counter         |
| **Disaster Recovery**                                                                                                                                                                                                  |                 |
| Restore test success (`lenny_restore_test_success`, gauge — 1 if the latest automated restore test passed, 0 if it failed; emitted by `lenny-restore-test` CronJob; see [Section 17.1](17_deployment-topology.md#171-kubernetes-resources))                                  | Gauge           |
| Restore test duration (`lenny_restore_test_duration_seconds`, gauge — elapsed time of the latest automated restore test; emitted by `lenny-restore-test` CronJob; see [Section 17.1](17_deployment-topology.md#171-kubernetes-resources))                                     | Gauge           |

These four templated metrics fulfill the "per-subsystem metrics" contract from [Section 4.1](04_system-components.md#41-edge-gateway-replicas). The subsystem-specific extraction-threshold metrics defined in [Section 4.1](04_system-components.md#41-edge-gateway-replicas)'s extraction readiness table (`lenny_stream_proxy_queue_depth`, `lenny_upload_handler_active_uploads`, `lenny_mcp_fabric_active_delegations`, `lenny_llm_proxy_active_connections`, etc.) are also canonical and must be instrumented; they provide finer-grained signals for the extraction decision described in [Section 4.1](04_system-components.md#41-edge-gateway-replicas).

### 16.2 Key Latency Breakpoints

Instrument four timestamps per session:

1. Pod claimed
2. Workspace prep done
3. Session ready
4. First event/token emitted

This lets operators identify whether bottlenecks are in pod allocation, file upload, session start, or the agent runtime itself.

### 16.3 Distributed Tracing

Lenny supports two tiers of distributed tracing, serving different purposes:

**Tier 1: Infrastructure tracing (automatic).** OpenTelemetry trace ID propagation through the entire delegation tree. This covers gateway, controller, and adapter spans — platform-level observability that operators use for debugging latency, errors, and resource usage. Fully automatic; runtimes do not need to participate.

**Tier 2: Runtime-level tracing (runtime-managed).** Opaque `tracingContext` propagation through delegation for runtime-native eval and observability platforms (LangSmith, Braintrust, Humanloop, etc.). Runtimes register tracing identifiers via `lenny/set_tracing_context` (MCP) or `set_tracing_context` (JSONL), and the gateway attaches them to child delegation leases automatically. Child runtimes receive the parent's `tracingContext` in the adapter manifest and use it to stitch their native traces into the parent's trace tree. This enables end-to-end eval tracing across delegation boundaries without Lenny needing to understand the tracing platform's wire format. See [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease) for `tracingContext` validation rules.

**Tier 1 trace context flows through:**

- Client → Gateway (HTTP headers)
- Gateway → Pod (gRPC metadata)
- Pod → Gateway (delegation tool calls carry parent trace context)
- Gateway → Child Pod (inherited trace context)
- Gateway → External MCP tools (HTTP headers)

**Span boundaries (instrumented):**

| Span                         | Component                             |
| ---------------------------- | ------------------------------------- |
| `session.create`             | Gateway                               |
| `session.claim_pod`          | Controller                            |
| `session.upload`             | Gateway + Pod                         |
| `session.finalize_workspace` | Pod                                   |
| `session.run_setup`          | Pod                                   |
| `session.start`              | Pod                                   |
| `session.prompt`             | Gateway + Pod (per prompt)            |
| `session.tool_call`          | Pod (per tool invocation)             |
| `delegation.spawn_child`     | Gateway                               |
| `delegation.await_child`     | Gateway + Parent Pod                  |
| `delegation.export_files`    | Gateway + Parent Pod                  |
| `delegation.budget_reserve`  | Gateway (Redis Lua — attributes: `outcome`, `tenant_id`, `root_session_id`, `lua_queue_wait_ms`) |
| `delegation.budget_return`   | Gateway (Redis Lua — attributes: `outcome`, `tenant_id`, `root_session_id`, `lua_queue_wait_ms`) |
| `mcp.external_tool_call`     | Gateway connector                     |
| `mcp.elicitation`            | Full chain (each hop is a child span) |
| `credential.assign`          | Gateway (credential service)          |
| `credential.rotate`          | Gateway (credential service)          |
| `credential.fallback_chain`  | Gateway (credential service)          |
| `credential.proxy_request`   | Gateway (LLM proxy)                   |
| `session.checkpoint`         | Gateway + Pod                         |
| `session.seal_and_export`    | Gateway + Pod                         |
| `coordinator.handoff`        | Gateway (attributes: `pool`, `outcome`, `generation` — covers the 3-step coordinator handoff protocol; see [Section 10.1](10_gateway-internals.md#101-horizontal-scaling)) |

**Sampling and backend:** The gateway emits 100% of traces to the OpenTelemetry Collector; the **Collector** applies tail-based sampling to decide which traces to export to the backend. The default probabilistic sampling rate is 10% for normal operations (configurable via `global.traceSamplingRate` Helm value). 100% sampling is applied for errors (any span with error status), slow requests (session creation exceeding 500ms, the P99 SLO target), and delegation trees (all spans in a tree are sampled if the root is sampled, preserving trace completeness). Because sampling decisions are made in the Collector after spans are complete (tail-based), error and latency conditions are known at decision time. The trace pipeline uses OpenTelemetry Collector; the platform emits OTLP traces and the collector handles sampling, batching, and export. The backend is deployer-configured — Jaeger, Tempo, Zipkin, or cloud-native options (Cloud Trace, X-Ray) are all supported. In dev mode, 100% sampling is enabled with a local Jaeger instance (or stdout exporter for `make run`).

**Error codes:** Structured error taxonomy with categories:

- `TRANSIENT` — retryable (pod crash, network timeout)
- `PERMANENT` — not retryable (invalid workspace, policy denial)
- `POLICY` — denied by policy engine (quota exceeded, unauthorized runtime, `CREDENTIAL_POOL_EXHAUSTED`)
- `UPSTREAM` — external dependency failure (MCP tool error, auth failure)

### 16.4 Logging

- Structured JSON logs from gateway, token service, pool controller, and runtime adapter
- Correlation fields in every log line: `session_id`, `tenant_id`, `trace_id`, `span_id`
- Setup command stdout/stderr captured and stored in EventStore
- Audit events for all policy decisions
- Error events include structured error codes (TRANSIENT/PERMANENT/POLICY/UPSTREAM)
- **Credential-sensitive RPCs** (`AssignCredentials`, `RotateCredentials`) are excluded from payload-level logging, gRPC access logs, and OTel trace span attributes. Only RPC name, lease ID, provider type, and outcome are recorded.

**Log retention and EventStore management.** EventStore tables (audit events, session logs, stream cursors) are partitioned by time using native Postgres range partitioning. A background job drops partitions beyond the retention window: 365 days for audit events (default), 30 days for session logs, 7 days for stream cursors. **SIEM delivery guard:** When `audit.siem.endpoint` is configured, the partition GC MUST NOT drop an audit partition whose most recent event has a `sequence_number` greater than the SIEM forwarder's last acknowledged high-water mark (read from the `siem_delivery_state` table). If the forwarder has not consumed all events in a partition that is past its retention TTL, the GC holds the partition and emits an `AuditPartitionDropBlocked` warning alert identifying the partition, its age, and the SIEM delivery lag. This prevents permanent data loss when the SIEM forwarder is stalled or misconfigured for an extended period. The held partition is dropped once the forwarder's high-water mark advances past its last event, or when an operator explicitly acknowledges the data loss via `lenny-ctl admin audit drop-partition --force <partition>`. Estimated volume is ~10 KB per session for audit events and ~50 KB for logs; at 10K sessions/day this is ~600 MB/day before retention cleanup. This estimate corresponds to Tier 3 daily throughput; see [Section 17.8](17_deployment-topology.md#178-capacity-planning-and-defaults) for per-tier volume estimates. Deployers should configure an external log aggregation stack (ELK, Loki, CloudWatch, etc.) for long-term retention beyond the Postgres window.

**GDPR erasure receipt rows (`event_type LIKE 'gdpr.%'`) are exempt from the standard partition GC.** The audit GC applies a separate `audit.gdprRetentionDays` window (default: 2555 days / 7 years) to these rows. This is a hard minimum floor: the GC will not delete `gdpr.*` rows until they exceed this window regardless of the general `audit.retentionDays` setting. See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces) for the full GDPR audit event exemption policy.

**Compliance-aware audit retention.** The default 365-day Postgres retention satisfies SOC2 (12-month audit evidence window) and FedRAMP Low/Moderate (1-year AU-11). Deployers subject to stricter frameworks should set `audit.retentionDays` using named presets or a custom value:

| Preset           | Retention           | Frameworks                       | `complianceProfile` pairing        |
| ---------------- | ------------------- | -------------------------------- | ---------------------------------- |
| `soc2` (default) | 365 days            | SOC2, FedRAMP Low/Moderate       | `soc2` or `fedramp` (default)      |
| `fedramp-high`   | 1095 days (3 years) | FedRAMP High                     | `fedramp` + this preset            |
| `hipaa`          | 2190 days (6 years) | HIPAA §164.312(b)                | `hipaa`                            |
| `nis2-dora`      | 1825 days (5 years) | EU NIS2, DORA (financial sector) | `none` or `soc2` + this preset     |
| `custom`         | Deployer-specified  | Any                              | Any                                |

The `audit.retentionPreset` is independent of `complianceProfile`: the profile governs which runtime controls are enforced (SIEM requirement, grant-check interval, pgaudit), while the preset governs how long data is retained in Postgres. FedRAMP High deployments must set both `complianceProfile: fedramp` and `audit.retentionPreset: fedramp-high`. See [Section 11.7](11_policy-and-controls.md#117-audit-logging) for the FedRAMP baseline note.

Helm configuration: `audit.retentionPreset: soc2` or `audit.retentionDays: 2190`. The preset drives the Postgres partition retention policy automatically.

**SIEM responsibility split.** When an external SIEM is configured (see [Section 11.7](11_policy-and-controls.md#117-audit-logging)), the SIEM is the system of record for long-term regulatory retention. The Postgres audit partition serves as a queryable hot tier for operational use. In this configuration, the Postgres retention period may be shorter than the regulatory requirement — the SIEM satisfies the long-term obligation. If `audit.retentionDays` is set below 365 and `LENNY_ENV=production`, the gateway emits an `AuditRetentionLow` warning at startup. This warning is suppressed if `audit.siem.endpoint` is configured, since the SIEM provides the durable long-term copy.

### 16.5 Alerting Rules and SLOs

**Critical alerts (page):**

| Alert                            | Condition                                                                                                                                                                                                                                                                                                   | Severity |
| -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `WarmPoolExhausted`              | Available warm pods = 0 for any pool for > 60s                                                                                                                                                                                                                                                              | Critical |
| `PostgresReplicationLag`         | Sync replica lag > 1s for > 30s                                                                                                                                                                                                                                                                             | Critical |
| `GatewayNoHealthyReplicas`       | Healthy gateway replicas below tier minimum (see [Section 17.8](17_deployment-topology.md#178-capacity-planning-and-defaults)) for > 30s                                                                                                                                                                                                                                    | Critical |
| `SessionStoreUnavailable`        | Postgres primary unreachable for > 15s                                                                                                                                                                                                                                                                      | Critical |
| `CheckpointStorageUnavailable`   | Checkpoint upload to MinIO failed after all retries during eviction; Postgres minimal state fallback was attempted                                                                                                                                                                                          | Critical |
| `EtcdUnavailable`                | API server etcd connectivity errors sustained > 15s                                                                                                                                                                                                                                                         | Critical |
| `CredentialPoolExhausted`        | Any credential pool has 0 assignable credentials (all credentials exhausted, in cooldown, or revoked) for > 30s; new session creation will return `CREDENTIAL_POOL_EXHAUSTED` for this pool                                                                                                                 | Critical |
| `CredentialCompromised`          | A credential in `revoked` state has active leases still alive against it for > 30s, indicating revocation propagation failure — the compromised key may still be in use                                                                                                                                     | Critical |
| `TokenServiceUnavailable`        | Token Service circuit breaker in open state for > 30s; new sessions requiring credentials will fail (existing sessions unaffected until lease expiry). See [§17.7](17_deployment-topology.md#177-operational-runbooks) runbook `docs/runbooks/token-service-outage.md`.                                                                                           | Critical |
| `ControllerLeaderElectionFailed` | Any controller's Lease (`lenny-warm-pool-controller` or `lenny-pool-scaling-controller`) has not been renewed within `leaseDuration` (15s); failover is imminent or in progress. Auto-resolves when a new leader acquires the lease. Tracked via `lenny_controller_leader_lease_renewal_age_seconds` gauge. | Critical |
| `DedicatedDNSUnavailable`        | All dedicated CoreDNS replicas for the agent namespace have zero ready pods. Agent pods lose DNS resolution entirely and cannot reach external tools or LLM endpoints. Fires when `kube_deployment_status_replicas_ready{deployment="lenny-agent-dns"} == 0` for > 30s. See [Section 13.2](13_security-model.md#132-network-isolation) (network policy). | Critical |
| `CosignWebhookUnavailable`       | The cosign ValidatingAdmissionWebhook (configured `failurePolicy: Fail`) has become unreachable. Pod admission is blocked for the agent namespace — no new pods can be scheduled, halting warm pool replenishment. Fires when the webhook endpoint returns errors for > 60s. See [Section 13.2](13_security-model.md#132-network-isolation) (image verification). See [§17.7](17_deployment-topology.md#177-operational-runbooks) runbook `docs/runbooks/admission-webhook-outage.md`. | Critical |
| `AuditGrantDrift`                | The periodic background grant check (every `audit.grantCheckInterval`, default: 5 min) detected that unexpected UPDATE or DELETE grants have been added to audit tables for the `lenny_app` role after startup. Tracks `lenny_audit_grant_drift_total > 0` in the last check interval. If `audit.hardFailOnDrift` is enabled, the gateway initiates graceful shutdown. See [Section 11.7](11_policy-and-controls.md#117-audit-logging) (audit integrity). | Critical |
| `NetworkPolicyCIDRDrift`         | The WarmPoolController's continuous CIDR drift check detected that the installed `internet` egress NetworkPolicy `except` blocks no longer match the cluster's actual pod or service CIDRs (`lenny_network_policy_cidr_drift_total > 0`). Agent pods with `internet` egress may be able to reach internal cluster IPs, enabling lateral movement. Operator must re-run `helm upgrade` with corrected CIDR values. See [Section 13.2](13_security-model.md#132-network-isolation). | Critical |
| `AdmissionWebhookUnavailable`    | The RuntimeClass-aware admission policy webhook (OPA/Gatekeeper or Kyverno) has been unreachable for more than 30 seconds. With `failurePolicy: Fail`, pod admission is denied — no new pods can be scheduled, halting warm pool replenishment. See [Section 17.2](17_deployment-topology.md#172-namespace-layout). See [§17.7](17_deployment-topology.md#177-operational-runbooks) runbook `docs/runbooks/admission-webhook-outage.md`. | Critical |
| `SandboxClaimGuardUnavailable`   | The `lenny-sandboxclaim-guard` ValidatingAdmissionWebhook has been unreachable for more than 30 seconds. With `failurePolicy: Fail`, all `PATCH` and `PUT` operations on `SandboxClaim` resources are blocked — new pod claims are prevented, halting session creation. Double-claim prevention is suspended until the webhook recovers. See [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle). | Critical |
| `DualStoreUnavailable`           | Both the Postgres primary and Redis are simultaneously unreachable. The gateway is operating in dual-store degraded mode: new session creation is rejected (503) and `PLATFORM_DEGRADED` events are pushed to all active client streams. Fires immediately on detection (`lenny_dual_store_unavailable` gauge = 1). See [Section 10.1](10_gateway-internals.md#101-horizontal-scaling). | Critical |
| `DataResidencyWebhookUnavailable` | The `lenny-data-residency-validator` ValidatingAdmissionWebhook (configured `failurePolicy: Fail`) has been unreachable for more than 30 seconds. With `failurePolicy: Fail`, all operations on tenant-scoped CRD resources with a `dataResidencyRegion` field are denied. See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces). | Critical |
| `DataResidencyViolationAttempt`   | A `StorageRouter` write or pod pool delegation was rejected because the resolved region did not match the session's `dataResidencyRegion`. Indicates a misconfiguration or code-path bypass — a cross-border transfer attempt. Fires immediately on the first occurrence (`lenny_data_residency_violation_total > 0`). See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces). | Critical |
| `PgBouncerAllReplicasDown`       | All PgBouncer pods in `lenny-system` have zero ready replicas (self-managed profile only). Postgres is unreachable for all gateway components — session creation and state writes will fail immediately. Suppressed on `deploymentProfile: cloud-managed`. See [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements). | Critical |
| `SessionEvictionTotalLoss`       | `lenny_session_eviction_total_loss_total` incremented — both MinIO and Postgres were unavailable during an eviction checkpoint, leaving the session unrecoverable with no durable state saved. Any non-zero value is immediately actionable. See [Section 4.4](04_system-components.md#44-event--checkpoint-store) (total-loss path). | Critical |
| `DelegationBudgetKeysExpired`    | `lenny_delegation_budget_keys_expired_total` incremented — a Lua script (`budget_reserve.lua` or `budget_return.lua`) returned `BUDGET_KEYS_EXPIRED`, indicating the delegation budget keys for a root session have expired while the tree was still active. The gateway initiates tree cleanup (cascade cancel + root → failed). TTL may be configured too short or [§12.8](12_storage-architecture.md#128-compliance-interfaces) explicit purge ran prematurely. See [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease). | Critical |
| `BillingStreamEntryAgeHigh`      | The oldest unacknowledged entry in any per-tenant billing Redis stream (`lenny_billing_redis_stream_oldest_entry_age_seconds`) exceeds 80% of `billingStreamTTLSeconds` (default threshold: 2880s, i.e., 80% of 3600s). Unlike `BillingStreamBackpressure` (which monitors stream depth), this alert detects the scenario where a small number of billing events have been sitting unflushed long enough to be at imminent risk of TTL expiry and permanent loss — for example, during a prolonged Postgres outage with low billing volume. The billing flusher emits a CRITICAL-level log (`billing_stream_entry_near_ttl_expiry`) when any entry exceeds 80% of the TTL, providing per-entry visibility before loss occurs. See [Section 11.2.1](11_policy-and-controls.md#1121-billing-event-stream) (billing durability). | Critical |

**Warning alerts:**

| Alert                            | Condition                                                                                                                                                                                                                                    | Severity |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| `WarmPoolLow`                    | Available warm pods < 25% of `minWarm` for any pool                                                                                                                                                                                          | Warning  |
| `RedisMemoryHigh`                | Redis memory > 80% of maxmemory                                                                                                                                                                                                              | Warning  |
| `CredentialPoolLow`              | Available credentials < 20% of pool size                                                                                                                                                                                                     | Warning  |
| `CredentialProactiveRenewalExhausted` | All proactive renewal retries for a credential lease were exhausted before expiry (`lenny_credential_proactive_renewal_exhausted_total` incremented). The session falls through to the standard credential fallback flow. Labeled by `pool` and `provider`. See [Section 4.9](04_system-components.md#49-credential-leasing-service). | Warning  |
| `GatewayActiveStreamsHigh`       | Active streams per replica > 80% of configured max                                                                                                                                                                                           | Warning  |
| `GatewaySessionBudgetNearExhaustion` | `lenny_gateway_active_sessions / gateway.maxSessionsPerReplica` > 90% on any replica for > 60s; HPA scale-out is lagging behind session arrival rate — new sessions may be queued or rejected if scale-out does not complete in time. See [Section 4.1](04_system-components.md#41-edge-gateway-replicas) for `maxSessionsPerReplica` calibration. | Warning  |
| `Tier3GCPressureHigh`               | `lenny_gateway_gc_pause_fleet_p99_ms` (fleet-wide P99 GC pause, see [§16.1](#161-metrics)) exceeds 50 ms for > 5 min AND the deployment's configured capacity tier is Tier 3 (a static Helm/config-derived label `deployment_tier="tier3"`, not a runtime session count). A minimum session floor of 500 active sessions suppresses the alert during cold-start with near-zero sessions, where GC pause data is not yet meaningful. Indicates that the combined gateway binary is experiencing shared-process GC pressure across the fleet — either the LLM Proxy subsystem should be extracted (see [§4.1](04_system-components.md#41-edge-gateway-replicas) extraction thresholds) or `maxSessionsPerReplica` must be reduced until extraction is complete. The alert is inactive for Tier 1/2 deployments by virtue of the `deployment_tier` label condition. | Warning  |
| `ArtifactGCBacklog`              | Expired artifacts pending cleanup exceeds tier-dependent threshold ([Section 17.8](17_deployment-topology.md#178-capacity-planning-and-defaults))                                                                                                                                                            | Warning  |
| `CheckpointStale`                | `lenny_checkpoint_stale_sessions` > 0 for any `pool`/`tier` for > 60s; one or more active sessions have a last checkpoint age exceeding `periodicCheckpointIntervalSeconds` (default: 600s) and are at elevated risk of workspace loss on eviction | Warning  |
| `RateLimitDegraded`              | Rate limiting in fail-open mode                                                                                                                                                                                                              | Warning  |
| `CertExpiryImminent`             | mTLS cert expiry < 1h (should auto-renew, so this indicates cert-manager failure)                                                                                                                                                            | Warning  |
| `ElicitationBacklogHigh`         | Pending elicitation requests > 50 for > 30s                                                                                                                                                                                                  | Warning  |
| `DelegationBudgetNearExhaustion` | Delegation budget utilization ratio > 90% for any active delegation tree                                                                                                                                                                     | Warning  |
| `DelegationBudgetIrrecoverable`  | Any delegation tree moved to `awaiting_client_action` due to `BUDGET_STATE_UNRECOVERABLE` during Redis recovery                                                                                                                              | Warning  |
| `AuditSIEMNotConfigured`         | `LENNY_ENV=production` and `audit.siem.endpoint` is not set; fires at startup and persists until endpoint is configured. Severity: **Warning** only — when all tenants have `complianceProfile: none`, the gateway starts and the alert fires normally. When any active tenant has a regulated `complianceProfile` (`soc2`, `fedramp`, `hipaa`), the gateway **refuses to start** ([Section 11.7](11_policy-and-controls.md#117-audit-logging)), so a Prometheus-scraped alert cannot fire; detection in that case relies on Kubernetes `CrashLoopBackOff` and the fatal log message. Operators should configure a kube-state-metrics alert on `kube_pod_container_status_waiting_reason{reason="CrashLoopBackOff"}` for the gateway Deployment to cover this scenario. | Warning  |
| `AuditChainGap`                  | Startup chain-continuity check detected a broken hash chain (gap in audit sequence) for one or more tenants. Indicates buffered events lost during a prior crash. Operators should replay from SIEM or document the gap.                     | Warning  |
| `CheckpointStorageHigh`          | Per-tenant checkpoint storage (`lenny_checkpoint_storage_bytes_total`) exceeds deployer-configurable threshold (default: 80% of tenant's storage quota)                                                                                      | Warning  |
| `StorageQuotaHigh`               | Per-tenant artifact storage (`lenny_storage_quota_bytes_used`) exceeds 80% of the tenant's `storageQuotaBytes`. New uploads or checkpoints will be rejected at 100%. See [Section 11.2](11_policy-and-controls.md#112-budgets-and-quotas) storage quota enforcement mechanism.                   | Warning  |
| `CheckpointDurationHigh`         | P95 of `lenny_checkpoint_duration_seconds` for Full-tier or embedded-adapter pools exceeds 2.5 seconds over a 5-minute window (25% headroom above the 2s SLO for ≤100MB workspaces). See [Section 4.4](04_system-components.md#44-event--checkpoint-store).           | Warning  |
| `PodClaimQueueSaturated`         | `lenny_pod_claim_queue_depth > 0.25 × pool.minWarm` for > 30s AND `lenny_warmpool_idle_pods > 0`; indicates claim queue is backing up even though warm pods exist — applicable to all pool execution modes                                   | Warning  |
| `KMSSigningUnavailable`          | `lenny_gateway_kms_signing_errors_total` rate exceeds 1 error/30s for > 60s; the gateway `JWTSigner` circuit breaker has tripped open — new session creation is failing with `KMS_SIGNING_UNAVAILABLE`. Existing sessions are unaffected until JWT expiry. Operator should verify KMS reachability and IAM role binding for the gateway ServiceAccount. See [Section 10.2](10_gateway-internals.md#102-authentication). | Warning  |
| `GatewaySubsystemCircuitOpen`    | Any gateway subsystem circuit breaker (`lenny_gateway_{subsystem}_circuit_state`) in open state (value = 2) for > 60s; affected subsystem is rejecting requests while other subsystems continue serving                                      | Warning  |
| `PoolConfigDrift`                | Any pool's Postgres `pool_config_generation` differs from its CRD `lenny.dev/config-generation` annotation for > 60s; PoolScalingController reconciliation is stalled or the controller is down. Detected by the gateway. See [Section 4.6.2](04_system-components.md#462-poolscalingcontroller-pool-configuration). | Warning  |
| `WarmPoolReplenishmentSlow`      | P95 of `lenny_warmpool_pod_startup_duration_seconds` exceeds 2× the pool's `scalingPolicy.podWarmupSecondsBaseline` (default: 30s) for > 5 min; pool refill is slower than expected and `WarmPoolLow` may not self-correct                   | Warning  |
| `WarmPoolReplenishmentFailing`   | `lenny_warmpool_warmup_failure_total` rate exceeds 1 failure/min for any pool for > 5 min; pods are failing to reach `idle` state during warm-up (image pull errors, setup command failures, or resource pressure). Warm pool replenishment is impaired and `WarmPoolLow` / `WarmPoolExhausted` may follow. Check `reason` label for root cause classification. See [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) and runbook `docs/runbooks/warm-pool-exhaustion.md` ([§17.7](17_deployment-topology.md#177-operational-runbooks)). | Warning  |
| `SDKConnectTimeout`              | `lenny_warmpool_sdk_connect_timeout_total` rate > 0.1/min for > 5 min on a given pool; SDK warm startup is systematically failing to complete and phantom warm pods may be accumulating. See [Section 6.1](06_warm-pod-model.md#61-what-a-pre-warmed-pod-looks-like) sdk_connecting watchdog.            | Warning  |
| `ErasureJobFailed`               | A user-level erasure job has failed (`lenny_erasure_job_failed_total` incremented). The user's `processing_restricted` flag remains set, blocking all new sessions for that user. Retry via `POST /v1/admin/erasure-jobs/{job_id}/retry` or clear the restriction with justification. See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces). | Warning  |
| `ErasureJobOverdue`              | A user-level erasure job has exceeded its tier-specific deadline (72h for T3, 1h for T4) without completing. Requires operator investigation. See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces).                                                                              | Warning  |
| `TenantDeletionOverdue`          | A tenant has been in `disabling` or `deleting` state longer than 80% of its tier SLA (T3: 72h, T4: 4h) without reaching `deleted`. Indicates a stalled deletion phase — operator investigation required. Tracked via `lenny_tenant_deletion_duration_seconds`. See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces). | Warning  |
| `KmsKeyDeletionFailed`           | Phase 4a of the tenant deletion lifecycle failed to disable or schedule deletion of the per-tenant KMS key (`lenny_kms_key_deletion_failed_total` incremented). The tenant's T4 KMS key may remain active, violating least-privilege. Operator must manually inspect the KMS key identified in the erasure receipt and schedule its deletion. See [Section 12.8](12_storage-architecture.md#128-compliance-interfaces). | Warning  |
| `BillingCorrectionRateHigh`      | Billing correction events exceed deployer-configurable percentage (default: 5%) of total billing events in a rolling 24h window. May indicate compromised credentials, operational error, or systematic metering bug. See [Section 11.2.1](11_policy-and-controls.md#1121-billing-event-stream).    | Warning  |
| `BillingStreamBackpressure`      | Per-tenant billing Redis stream depth (`lenny_billing_redis_stream_depth`) has reached 80% of `billingRedisStreamMaxLen` (per-tier defaults: 50,000 at Tier 1/2, 72,000 at Tier 3) for more than 60 seconds. Indicates sustained Postgres write failure — billing events are accumulating in the Redis fallback tier and are at risk of being permanently lost if the stream TTL (`billingStreamTTLSeconds`, default: 3600s) expires before Postgres recovers. See [Section 11.2.1](11_policy-and-controls.md#1121-billing-event-stream) (billing durability). | Warning  |
| `BillingCorrectionApprovalBacklog` | The pending billing correction approval queue (`lenny_billing_correction_pending_total{state="pending"}`) exceeds the deployer-configurable threshold (default: 10) for more than `billing.approvalBacklogAlertMinutes` (default: 60) minutes. Corrections awaiting approval may block dispute resolution workflows. See [Section 11.2.1](11_policy-and-controls.md#1121-billing-event-stream). | Warning  |
| `BillingWriteAheadBufferHigh`    | The in-memory billing write-ahead buffer utilization (`billing_write_ahead_buffer_utilization`) exceeds 0.80 for any tenant for more than 30 seconds. When the buffer reaches capacity (`billingFlushMaxPending`, default: 500), the gateway begins rejecting new billable work. See [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements) (write-ahead buffer back-pressure). | Warning  |
| `AuditRetentionLow`              | `LENNY_ENV=production` and `audit.retentionDays` is below 365 days without a configured SIEM (`audit.siem.endpoint`). Fires at startup. Suppressed when SIEM is configured (SIEM provides long-term regulatory retention). See [Section 16.4](#164-logging). | Warning  |
| `PostgresWriteSaturation`        | Sustained Postgres write IOPS exceed 80% of the estimated instance ceiling (see [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements) write classification table) for > 5 minutes. Indicates the primary instance is approaching its write capacity — operators should activate the billing/audit instance separation or scale up the instance class before the ceiling is breached. | Warning  |
| `ScatterGatherSlowQuery`         | P99 of `lenny_store_router_scatter_gather_duration_seconds` for any `query_type` exceeds 30 s sustained for > 5 min. Indicates a multi-shard scatter-gather path is degraded — operators should investigate per-shard timeouts (`scatter_gather_shard_timeout` structured events) and connection pool saturation on the slow shards. Trivially suppressed in v1 (single shard). See [Section 12.6](12_storage-architecture.md#126-interface-design) scatter-gather execution constraints. | Warning  |
| `PostgresWriteBurstIops`         | Instantaneous Postgres write IOPS (`pg_stat_wal` wal_bytes/s converted to equivalent IOPS over a 30-second window) exceed the burst ceiling for the configured instance class ([Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements) per-tier write ceiling table: ~600 IOPS Tier 1, ~1,800 IOPS Tier 2, ~4,800 IOPS Tier 3). A single burst event is not actionable but repeated triggers (> 3 in 10 minutes) indicate quota flush storms or session-creation spikes that are regularly saturating burst headroom. At Tier 3, where the margin between sustained load (~1,300/s) and the instance ceiling (~1,600/s) is approximately 19%, burst saturation is a leading indicator of sustained saturation during load spikes. Operators should review `postgres.writeCeilingIops` against Phase 13.5 Lenny-specific write-pattern benchmark results ([Section 18](18_build-sequence.md)). | Warning  |
| `PgBouncerPoolSaturated`         | PgBouncer `cl_waiting_time` exceeds 1s for > 60s (self-managed profile only). Indicates client requests are queuing behind connection limits — pool size or max_client_conn must be increased. See [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements). Cross-reference: [§17.7](17_deployment-topology.md#177-operational-runbooks) runbook `docs/runbooks/pgbouncer-saturation.md`. | Warning  |
| `PoolBootstrapMode`              | Any pool has `status.scalingMode: bootstrap` for more than 72 hours. Indicates the pool has not received sufficient traffic for formula-driven convergence or the operator has not reviewed the initial sizing. See [Section 17.8.2](17_deployment-topology.md#1782-capacity-tier-reference) cold-start bootstrap procedure. | Warning  |
| `PoolBootstrapUnderprovisioned`  | The PoolScalingController's formula-computed `target_minWarm` for a bootstrapping pool exceeds 3× the current `bootstrapMinWarm` override. The static override is significantly undersized relative to observed demand — the operator must increase `bootstrapMinWarm` manually before the controller can converge. | Warning  |
| `WarmPoolIdleCostHigh`           | Cumulative `lenny_warmpool_idle_pod_minutes` for any pool exceeds the deployer-configured cost threshold over a rolling 24 h window. Indicates the `minWarm` target or scale-to-zero schedule may need adjustment to reduce idle spend. See [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) (idle cost visibility). | Warning  |
| `SandboxClaimOrphanRateHigh`     | `lenny_orphaned_claims_total` rate exceeds 10 orphaned claims in any 15-minute window. Indicates potential gateway instability (gateways creating claims but crashing before persisting sessions). See [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) (orphaned claim detection). | Warning  |
| `OrphanTasksPerTenantHigh`       | Per-tenant active orphan task count (`lenny_orphan_tasks_active_per_tenant`) exceeds 80% of `maxOrphanTasksPerTenant` (default: 100). A malicious or misbehaving orchestrator is accumulating detached orphan tasks; when the cap is reached the gateway falls back to `cancel_all` instead of detaching. See [Section 8.10](08_recursive-delegation.md#810-delegation-tree-recovery). | Warning  |
| `EtcdQuotaNearLimit`             | etcd backend database size exceeds 80% of `--quota-backend-bytes` (recommended quota: 4 GB for Tier 1, 8 GB for Tier 2/3 — see [Section 17.8.2](17_deployment-topology.md#1782-capacity-tier-reference)). Operators must defragment or increase quota before etcd enters alarm state and write operations are blocked. Computed as `etcd_debugging_mvcc_db_total_size_in_bytes / etcd_server_quota_backend_bytes > 0.80`. See [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) (etcd quota monitoring). | Warning  |
| `EtcdWriteLatencyHigh`           | P99 etcd WAL fsync latency (`etcd_disk_wal_fsync_duration_seconds`) exceeds 25 ms for more than 2 minutes. At Tier 3, sustained CRD status-update traffic (~800 raw writes/s before dedup, capped at ~120 QPS after dedup — see [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle)) approaches shared-etcd write limits; elevated latency is the leading indicator of saturation before quota exhaustion or `EtcdUnavailable`. Operators should reduce `maxSessionsPerReplica`, increase `statusUpdateDeduplicationWindow`, or migrate to a dedicated etcd cluster ([Section 17.8.2](17_deployment-topology.md#1782-capacity-tier-reference)). | Warning  |
| `ControllerWorkQueueDepthHigh`   | Instantaneous `ControllerWorkQueueDepth` (labeled by controller and queue name) exceeds 50% of the configured `Work queue max depth` ([Section 17.8.2](17_deployment-topology.md#1782-capacity-tier-reference): 500 / 2,000 / 10,000 by tier) for more than 2 minutes. A sustained high queue depth means the controller cannot process reconciliation events as fast as they arrive — when the backlog eventually drains, the resulting burst of status writes amplifies etcd write pressure. At Tier 3, this alert often precedes `EtcdWriteLatencyHigh`; investigate controller CPU throttling, rate-limiter settings, and `--max-concurrent-reconciles` ([Section 17.8.2](17_deployment-topology.md#1782-capacity-tier-reference)). | Warning  |
| `WorkspaceSealStuck`             | A session's seal-and-export operation has been retrying for longer than `maxWorkspaceSealDurationSeconds` (default: 300s) without success (`lenny_workspace_seal_duration_seconds{outcome="timeout"}` incremented). Indicates sustained MinIO unavailability. Pods held in `draining` beyond this deadline are forcibly terminated and the session is transitioned to `failed` with reason `workspace_seal_timeout`. See [Section 7.1](07_session-lifecycle.md#71-normal-flow) (seal-and-export invariant). | Warning  |
| `FinalizerStuck`                 | A `Sandbox` pod has been in `Terminating` state with the `lenny.dev/session-cleanup` finalizer present for more than 5 minutes. The warm pool controller could not confirm session checkpoint and safe finalizer removal. Follow the stuck finalizer runbook (`docs/runbooks/stuck-finalizer.md`). See [Section 4.6.1](04_system-components.md#461-warm-pool-controller-pod-lifecycle) (sandbox finalizers). | Warning  |
| `DedicatedDNSDegraded`           | The number of ready dedicated CoreDNS replicas for the agent namespace has dropped below the configured minimum but not to zero. DNS is degraded — query logging, rate limiting, and response filtering are partially unavailable. See [Section 13.2](13_security-model.md#132-network-isolation) (network policy). | Warning  |
| `AuditSIEMDeliveryLag`           | The lag between the latest committed audit event in Postgres and the latest SIEM-acknowledged event (`siem_delivery_lag_seconds`) exceeds `audit.siem.maxDeliveryLagSeconds` (default: 30s). Events are at risk of being unacknowledged if the outbox forwarder crashes. See [Section 12.3](12_storage-architecture.md#123-postgres-ha-requirements) (outbox forwarder requirements). | Warning  |
| `AuditPartitionDropBlocked`      | An audit partition has exceeded its retention TTL but the SIEM forwarder has not consumed all events in the partition. The partition GC is holding the partition to prevent permanent data loss. Operator must either resolve the SIEM forwarder backlog or explicitly acknowledge data loss via `lenny-ctl admin audit drop-partition --force`. See [Section 16.4](#164-logging). | Warning  |
| `WarmPoolBootstrapping`          | Any pool with `minWarm > 0` has `status.conditionPoolWarmingUp = True` for more than `warmupDeadlineSeconds` (default: 300s). Indicates the pool has not reached its `minWarm` target — possible causes: image pull errors, node resource pressure, or insufficient quota. See [Section 4.6.2](04_system-components.md#462-poolscalingcontroller-pool-configuration) and runbook [Section 17.7](17_deployment-topology.md#177-operational-runbooks). | Warning  |
| `CRDSSAConflictStuck`            | The SSA conflict counter (`lenny_crd_ssa_conflict_total`) for any single resource exceeds 10 in a 5-minute window. Indicates an abnormal ownership dispute between controllers on the same CRD field — requires operator investigation. See [Section 4.6](04_system-components.md#46-pod-lifecycle-controllers). | Warning  |
| `ExperimentTargetingCircuitOpen` | The experiment targeting circuit breaker (`lenny_experiment_targeting_circuit_open`) has been open for more than 60 seconds. Experiment assignments are falling back to the control variant. See [Section 10.7](10_gateway-internals.md#107-experiment-primitives). | Warning  |
| `PgAuditSinkDeliveryFailed`      | The pgaudit log shipper has been reporting delivery failures (`lenny_pgaudit_grant_events_total` delivery errors) for more than `audit.pgaudit.sinkFailureAlertMinutes` (default: 5) minutes. Audit events from the pgaudit source are not reaching the configured sink. See [Section 11.7](11_policy-and-controls.md#117-audit-logging). | Warning  |
| `DelegationLuaScriptLatencyHigh` | `lenny_redis_lua_script_duration_seconds{script="budget_reserve"}` P99 exceeds 5 ms for more than 2 minutes. Indicates that Redis Lua serialization for delegation budget reservation has breached the `LeaseStore` SLO ceiling — lease renewal `SET` operations issued during the blocking window are delayed, risking false lease expirations for unrelated tenants. Operators should reduce fan-out (`maxParallelChildren`) or split the delegation budget workload onto a dedicated `DelegationBudgetStore` Redis instance. See [Section 8.3](08_recursive-delegation.md#83-delegation-policy-and-lease) ("Cross-tenant aggregate Lua contention") for the aggregate blocking formula and remediation steps. Note: a pre-alert monitoring threshold of P99 > 2 ms sustained also indicates reduced headroom (< 3 ms remaining before the SLO ceiling) and is logged as an operational warning in the gateway. | Warning  |
| `CoordinatorHandoffSlow`         | P95 of `lenny_coordinator_handoff_duration_seconds` exceeds 5 seconds for any pool over a 5-minute window. Indicates the 3-step coordinator handoff protocol is experiencing sustained delays — possible causes: lease contention, network partition, or high fence-retry rate (`lenny_coordinator_fence_retry_total`). See [Section 10.1](10_gateway-internals.md#101-horizontal-scaling). | Warning  |
| `RuntimeUpgradeStuck`            | `lenny_runtime_upgrade_state` for any pool has remained in a non-terminal state (`expanding`, `draining`, or `contracting`) for longer than `runtimeUpgrade.phaseTimeoutSeconds` (default: 600s). The 6-state upgrade machine is stalled — operator must investigate draining sessions (`lenny_runtime_upgrade_draining_sessions`) or manually advance/abort the upgrade. See [Section 10.5](10_gateway-internals.md#105-upgrade-and-rollback-strategy). | Warning  |
| `CircuitBreakerActive`           | Any global circuit breaker (`lenny_circuit_breaker_open{circuit_name=...}`) has been open for more than 5 minutes without a close action. See [Section 11.6](11_policy-and-controls.md#116-circuit-breakers). | Warning  |

**Capacity tiers:**

The following tiers define the scale points at which SLOs and alerting rules must hold. Infrastructure sizing for each tier is detailed in [Section 17.8](17_deployment-topology.md#178-capacity-planning-and-defaults).

| Parameter                         | Tier 1 (Starter) | Tier 2 (Growth) | Tier 3 (Scale) | Tier 4 (Platform) |
| --------------------------------- | ---------------- | --------------- | -------------- | ----------------- |
| Max concurrent sessions           | 100              | 1,000           | 10,000         | 100,000           |
| Session creation rate (sustained) | 5/s              | 30/s            | 200/s          | 2,000/s           |
| Gateway RPS (all endpoints)       | 500              | 5,000           | 50,000         | 500,000           |
| Delegation fan-out (concurrent)   | 10               | 100             | 500            | 5,000             |
| Active tenants                    | 5                | 50              | 500            | 5,000             |
| LLM proxy concurrent streams      | 50               | 500             | 5,000          | 50,000            |
| Per-replica session capacity budget (`maxSessionsPerReplica`) | 50 (provisional) | 200 (provisional) | 400 (provisional — requires LLM Proxy extraction; see [§4.1](04_system-components.md#41-edge-gateway-replicas)) | 400 (provisional — same per-replica budget as Tier 3; scale-out via replica count and infrastructure changes below) |

**Tier 4 (Platform) definition.** Tier 4 is the first tier that requires infrastructure changes beyond horizontal scaling of v1 components. Tiers 1–3 are achievable with the v1 architecture by scaling replica counts, instance sizes, and Redis/Postgres topology. Tier 4 requires swapping one or more of the [§12.6](12_storage-architecture.md#126-interface-design) scaling extension interfaces to their high-scale implementations: `PostgresPodRegistry` (replacing `CRDPodRegistry` to eliminate etcd write pressure), multi-shard `StoreRouter` (replacing `SingleShardRouter`), durable `EventBus` (replacing `RedisEventBus`), and potentially `ClusterRegistry` for multi-cluster delegation routing. The interface boundaries in [§12.6](12_storage-architecture.md#126-interface-design) are designed so that each swap is a configuration-level change — no application code restructuring is required, though accompanying infrastructure provisioning (Postgres schema migrations, NATS/Kafka deployment, additional cluster federation) is needed. Tier 4 targets are indicative planning figures (10× Tier 3 on most dimensions); exact values must be validated by load testing after the relevant interface swaps are deployed. Tier 4 is **not** a GA deliverable — it is a post-GA scaling milestone. All references to "Tier 4" elsewhere in this document describe the design-time intent of the [§12.6](12_storage-architecture.md#126-interface-design) interfaces, not a committed implementation timeline.

**Workload profile assumptions.** The tier sizing formulas throughout this document (warm pool sizing, Redis ops budgets, Postgres IOPS, gateway replica counts) depend on assumptions about session duration, delegation rates, and workspace size. These assumptions are expressed as explicit Helm values under `capacityPlanning.*` so that operators with atypical workloads can substitute their own measurements without reverse-engineering the formulas. The following table lists the parameters, their defaults, and where each feeds into sizing calculations:

| Helm value | Default | Unit | Used by |
| --- | --- | --- | --- |
| `capacityPlanning.avgSessionDurationSeconds` | 333 | seconds | Warm pool claim rate (Little's Law: `concurrent_sessions / avg_duration`); delegation budget Redis ops/s |
| `capacityPlanning.delegationParticipationRate` | 0.05 | fraction | Fraction of sessions that perform at least one delegation; scales delegation budget operations |
| `capacityPlanning.avgDelegationsPerDelegatingSession` | 10 | integer | Average number of child delegations per delegating session; scales `budget_reserve.lua` invocation rate |
| `capacityPlanning.avgChildSessionSeconds` | 60 | seconds | Average duration of a child delegation session; used in delegation fan-out `minWarm` formula ([§17.8.2](17_deployment-topology.md#1782-capacity-tier-reference)) |
| `capacityPlanning.avgWorkspaceSizeMB` | 100 | MB | Checkpoint bandwidth estimates; MinIO throughput budget ([§17.8.2](17_deployment-topology.md#1782-capacity-tier-reference) object storage table) |
| `capacityPlanning.sessionIdleFraction` | 0.30 | fraction | Fraction of active sessions that are idle (no active LLM call); scales LLM Proxy concurrent connection estimate |

Derived quantities: at Tier 3 defaults, implied claim rate = `10,000 / 333 ≈ 30/s` (matching the warm pool sizing table); delegation budget Lua invocation rate = `10,000 × 0.05 × 10 / 333 ≈ 15/s` sustained `budget_reserve.lua` invocations (time-averaged mean; the denominator is `avgSessionDurationSeconds` because it represents the parent session lifetime over which delegations are spread — not `avgChildSessionSeconds`, which governs concurrent child pod demand). Peak instantaneous Lua invocation rates can be significantly higher than the sustained average because orchestrator sessions typically burst all child delegations at session start; operators should monitor `lenny_redis_lua_script_duration_seconds{script="budget_reserve"}` burst percentiles and the [§8.3](08_recursive-delegation.md#83-delegation-policy-and-lease) cross-tenant aggregate contention formula for burst sizing. **Reconciliation with [§12.4](12_storage-architecture.md#124-redis-ha-and-failure-modes) and [§17.8](17_deployment-topology.md#178-capacity-planning-and-defaults) Redis ops/s figures.** The ~15/s figure is the `budget_reserve.lua` script invocation rate, not the total Redis ops/s for delegation budgets. Total ops/s is higher for three reasons: (1) `budget_return.lua` runs at a comparable rate (~15/s), giving ~30/s total Lua invocations; (2) each Lua script atomically executes ~10–15 internal Redis commands across 5–6 keys (GET, INCR, DECR, SET, EXPIRE); (3) gateway reads of budget state (e.g., remaining tokens for progress reporting) add ~5/s of non-Lua reads. The resulting sustained total is ~30 × 12 + 5 ≈ **365 ops/s** for a single-tenant Tier 3 deployment, consistent with the ~400 ops/s estimate in the [§17.8](17_deployment-topology.md#178-capacity-planning-and-defaults) single-tenant topology table. The [§12.4](12_storage-architecture.md#124-redis-ha-and-failure-modes) figure of ~500–1,000/s applies to multi-tenant Tier 3 (up to 500 active tenants, though not all delegate simultaneously) and includes burst headroom — the upper bound reflects peak instantaneous rates when multiple tenants burst delegations concurrently. Operators must update these Helm values before relying on any formula-derived sizing output. The PoolScalingController reads `capacityPlanning.*` values at startup and logs a warning if defaults are in use for a Tier 2 or Tier 3 deployment: `[WARN] capacityPlanning Helm values are at defaults — review Section 16.5 workload profile table and substitute observed values before production promotion`.

**Design-time scale target:** All architectural decisions (queue depths, connection pool sizes, controller reconciliation intervals, warm pool sizing) must be validated against **Tier 2** as the minimum. Tier 3 must be achievable with horizontal scaling only (no architectural changes). Tier 4 requires swapping one or more [§12.6](12_storage-architecture.md#126-interface-design) scaling extension interfaces to their high-scale implementations — see "Tier 4 (Platform) definition" above. Tier 1 exists for single-node development and CI environments.

**Benchmark requirement:** Before GA, load tests must demonstrate that all SLOs below hold at sustained Tier 2 load, and that Tier 3 is reachable with linear horizontal scaling of gateway, controllers, and data stores. Phase 13.5 establishes pre-hardening baselines; Phase 14.5 re-validates all SLOs with full security hardening active — Phase 14.5 is the final GA gate for SLO compliance.

**SLO targets — PROVISIONAL (first-principles estimates; must be validated before GA):**

> **PROVISIONAL VALUES.** All targets in this table are first-principles design estimates derived from architecture analysis, not load-test measurements. They MUST NOT be used in customer-facing SLA commitments or capacity agreements until the Phase 14.5 validation gate is complete (see benchmark requirement above). Any deployment where these values remain at their defaults (i.e., Phase 14.5 has not yet run) MUST surface a startup warning: `[WARN] SLO targets in Section 16.5 are provisional first-principles estimates and have not been validated by load testing. Do not use for customer SLA commitments until Phase 14.5 benchmark gate is complete.`

| SLO                                     | Target (provisional) | Measurement                                                  |
| --------------------------------------- | -------------------- | ------------------------------------------------------------ |
| Session creation success rate           | 99.5%                | Successful session starts / total attempts (30d window)      |
| Session creation latency                | P99 < 500ms          | `POST /v1/sessions` receipt through `session_id` response (steps 1–8 of [§7.1](07_session-lifecycle.md#71-normal-flow): auth, policy, credential pre-check, pod claim, Postgres persist, credential assignment, pod assignment) — excludes workspace upload. Measured at all tiers. |
| Time to first token                     | P95 < 10s            | From session start request to first streaming event          |
| Session availability                    | 99.9%                | Uptime of sessions not in retry/recovery state               |
| Gateway availability                    | 99.95%               | Healthy replicas serving requests                            |
| Startup latency (pod-warm, runc)        | P95 < 2s             | Pod claim through agent session ready, excluding file upload |
| Startup latency (pod-warm, gVisor)      | P95 < 5s             | Pod claim through agent session ready, excluding file upload |
| Checkpoint duration (≤ 100MB workspace) | P95 < 2s             | Quiescence request through snapshot upload complete          |

**Startup warning requirement:** The gateway MUST emit the warning above at startup whenever the SLO configuration values remain equal to the defaults listed above and the `slo.validated` Helm value is not set to `true`. The `slo.validated` flag is set by the Phase 14.5 benchmark automation after it records benchmark results to the Phase 14.5 exit gate ADR and annotates the Helm release. This ensures that any deployment that has not completed the validation gate cannot silently operate with unvalidated SLO defaults.

**SLO error-budget burn-rate alerts:**

Multi-window burn-rate alerting is required for all availability and latency SLOs. The fast window (1 h at 14× burn rate) catches acute outages quickly; the slow window (6 h at 3× burn rate) catches slow-burn degradation that would exhaust the monthly budget without triggering threshold-only alerts.

| Alert                                   | SLO                         | Fast window | Slow window | Severity |
| --------------------------------------- | --------------------------- | ----------- | ----------- | -------- |
| `SessionCreationSuccessRateBurnRate`    | Session creation ≥ 99.5%    | 1 h, 14×   | 6 h, 3×    | Critical (fast) / Warning (slow) |
| `SessionCreationLatencyBurnRate`        | Session creation P99 < 500ms | 1 h, 14×  | 6 h, 3×    | Critical (fast) / Warning (slow) |
| `SessionAvailabilityBurnRate`           | Session availability ≥ 99.9% | 1 h, 14×  | 6 h, 3×    | Critical (fast) / Warning (slow) |
| `GatewayAvailabilityBurnRate`           | Gateway availability ≥ 99.95% | 1 h, 14× | 6 h, 3×    | Critical (fast) / Warning (slow) |
| `StartupLatencyBurnRate`               | Startup latency P95 < 2s (runc); metric: `lenny_session_startup_duration_seconds{isolation_profile="runc"}` | 1 h, 14× | 6 h, 3×  | Critical (fast) / Warning (slow) |
| `StartupLatencyGVisorBurnRate`         | Startup latency P95 < 5s (gVisor); metric: `lenny_session_startup_duration_seconds{isolation_profile="gvisor"}` | 1 h, 14× | 6 h, 3× | Critical (fast) / Warning (slow) |
| `TTFTBurnRate`                          | Time to first token P95 < 10s; metric: `lenny_session_time_to_first_token_seconds` | 1 h, 14×  | 6 h, 3×    | Critical (fast) / Warning (slow) |
| `CheckpointDurationBurnRate`            | Checkpoint duration P95 < 2s (≤ 100MB) | 1 h, 14× | 6 h, 3× | Critical (fast) / Warning (slow) |

**Burn-rate calculation:** For availability SLOs, burn rate is `error_rate / (1 - slo_target)` over the window. For latency SLOs, burn rate is `rate(slow_requests[window]) / rate(total_requests[window]) / error_budget_fraction`. A burn rate of 1× means the budget is consumed at exactly the rate that would exhaust it over the 30-day window. At 14× for 1 hour the alert fires if more than `14 * (1h / 720h) = 1.94%` of the monthly budget is consumed in one hour. At 3× for 6 hours the alert fires if more than `3 * (6h / 720h) = 2.5%` is consumed. Both conditions must be present simultaneously for a page (fast window page; slow window warn only) to reduce false positives. Burn-rate thresholds are configurable via Helm values (`slo.burnRate.fastMultiplier`, default 14; `slo.burnRate.slowMultiplier`, default 3).

