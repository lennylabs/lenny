version: v2

# Code generation is deferred to Phase 2 (when the gateway and runtime
# adapter components are implemented). This file is committed at Phase 1
# so the configuration surface is reviewed once and the Phase 2 patch is
# a one-line opt-in. The plugins below are commented out by intent.

plugins:
  # - remote: buf.build/protocolbuffers/go
  #   out: ../pkg/proto
  #   opt:
  #     - paths=source_relative
  # - remote: buf.build/grpc/go
  #   out: ../pkg/proto
  #   opt:
  #     - paths=source_relative
  #     - require_unimplemented_servers=false
